# RL187 target — multiscale zero-prefix capacity and weighted-flow escalation

Date: 2026-08-30

## Authoritative objective

Continue the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from RL186.

RL186 proves exact first-defect arithmetic and late-tail height restrictions, culminating in

- `sum_ordinary |f_i|>354`;
- both signed flow masses `>177`;
- both directional K-variation masses `>59`.

This remains far below the inherited K-corridor scale. The next task is to obtain a **distribution**, not another single tail cutoff.

## Preferred theorem

For clean RL184 corridors let `tau` be the first nonzero-defect offset. Prove strong simultaneous upper bounds for

`N_n = #{clean starts: tau>=n}`

at several thresholds, ideally spanning the middle and upper half of `0,...,39`.

Start from the exact RL186 law

`C_tau = 3^tau odd(C_0)`, with `2^tau|C_0`,

and combine it with:

- the strict shallow starting-numerator intervals;
- RL182 ternary suffix ownership and reset;
- RL183 parity/ternary defect sensor;
- actual RL184 mechanical factors/common-mechanical affine transport;
- zero-block packing with physical shallow-start isolation;
- exact terminal normalized-gap bounds.

The desired output is a staircase or distributional capacity theorem that places a large fraction of first defects at substantially lower endpoint heights, then feeds those counts into the refined first-defect charging table.

## Secondary consumer

Only if the resulting weighted flow becomes comparable with the inherited K corridor, attack chronological sign-reversal capacity and prefix excursion.

## Do not

- merely extend unconstrained template enumeration;
- count arbitrary ternary residues as physical states;
- treat necessary affine maps as existence certificates;
- infer K excursion from total variation alone;
- revive RL168-RL171 generic rank/lattice routes;
- use RL173's auxiliary `3^-G` functional in place of corrected physical `2^G`;
- claim branch/global closure without a genuine quota contradiction.
