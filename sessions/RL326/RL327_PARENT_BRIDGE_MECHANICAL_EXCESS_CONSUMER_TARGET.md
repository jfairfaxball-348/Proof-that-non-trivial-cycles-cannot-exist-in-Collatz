# RL327 — mechanical-excess consumer for the remaining R1 parent bridge

Date prepared: 2026-09-15
Status: PREPARED, NOT STARTED

## GLOBAL PROOF ROADMAP

`CURRENT_STAGE = R1`
`CURRENT_STAGE_NAME = Parent Bridge`
`CURRENT_STAGE_PROGRESS = 68%`
`GLOBAL_PROOF_STATUS = OPEN`

## Absolute objective

Close or materially contract the remaining genuine ordered `g=2`, `Z0>0`, `K<0` parent branch under

`1<=n<=32839291403`.

## First priority

Consume the RL326 positive-excess density structurally rather than repeating endpoint-by-endpoint carry exclusions.

The most concrete next attacks are:

1. enumerate and connect the finite length-48 ideal plateaux that remain after the forbidden length-49 theorem, using actual odd-state ownership at both ends;
2. replace the uniform `1/(12 Lambda)` loss by the exact residue-weighted loss of every mandatory excess hit;
3. prove a shorter forbidden ideal block on the live post-contraction state interval;
4. show that a positive excess excursion creates a strictly smaller genuinely owned balanced return.

Any exact finite certificate must cover every genuinely owned factor/transition in its stated range. Do not enumerate the original unbounded support.

## Binding barriers and scope

Preserve RL324's local propagation barrier, the linear-only telescope, the prohibition on the late-row use of root-aligned `G<2^35`, and the externally conditional status of `2^71<=m<2^75`.

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49547666544`.
Gate A OPEN. Gate B OPEN. R1 OPEN. `g=1` separate. Global positive non-trivial-cycle exclusion OPEN.
