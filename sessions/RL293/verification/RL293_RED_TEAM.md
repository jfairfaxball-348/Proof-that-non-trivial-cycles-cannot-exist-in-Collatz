# RL293 target-specific red team

Date: 2026-09-10

Result: **PASS for the promoted RL293 scope.**

This ledger tests the promoted claims against the mandatory red teams in the incoming RL293 target. It does not assert Gate A is closed.

## 1. Arbitrary positive zero-height boundary retention

PASS. The static-tree pullback theorem is exact under arbitrary retained boundary words: such words only prefix/graft the same static tree. The checkpoint-8 area-two theorem explicitly includes arbitrarily many zero-height `5<->3` cycles. The first-d=1 splice reduction copies the entire later boundary suffix from an identical physical state, so no bounded-retention assumption is used.

## 2. RL292 all-zero pullback family with unbounded Beta

PASS. RL293 does not replace the static tree by a finite affine maximum. The min-area formulation retains every static ball. The normalized-K corridor/principal-cylinder theorem applies only to a hypothetical first-checkpoint failure and derives an additional fixed-seed size/drift condition; it does not claim the all-zero local family is impossible for arbitrary boundary inputs.

## 3. RL287 legal-segment cylinder isometry

PASS. Section 2 is obtained by composing RL287's cylinder isometry with RL292's static tree and proves exact preservation of 2-adic ball precision. The later principal-representative result does not contradict arbitrary lift transparency: it proves that the extra first-checkpoint drift/size inequalities force the particular dangerous entry to have lift zero.

## 4. RL289 finite-suffix transparency

PASS. No bounded recent suffix is used to price arbitrary future phase. Transparency remains fully active until the first-checkpoint corridor proves `n_b<2^t`; only then does the actual integer coincide with the least cylinder representative.

## 5. Neutral `(101)` fixed-seed gauge

PASS. All owner/splice statements are formulated on physical states and minimum historical cost. Repeating a zero-height/neutral seed gauge cannot improve or invalidate the physical-state minimum. No absolute prefix rank or chosen-history seam is used.

## 6. Physical mergers and equal-area diamonds

PASS. The central owner object is minimum cost to a physical state. A higher-cost duplicate is discarded only by exact shortest-path dominance. The H<=28 k=29 proof deliberately splices at the identical physical first checkpoint.

## 7. RL283 adjacent-swap valuation jumps

PASS. No per-cell or per-edge valuation Lipschitz theorem is claimed. The support-count barrier explicitly rejects a coarser local charging rule. The k=29 certificate uses exact reachability, not valuation monotonicity.

## 8. Homogeneous `(010)^R` Rank-1 danger towers

PASS. These remain valid local danger mechanisms. RL293 does not bound Bellman threat by a continuous endpoint potential. The all-depth open problem is precisely whether fixed-seed minimum ownership/ballot ancestry can reach the dangerous physical states cheaply enough.

## 9. Shifted phase-reset family and genuine `10922 -> 6146`

PASS. RL293 does not use a finite shifted-valuation table or a bounded local phase increment. Ball pullback is isometric and min-plus ownership is allowed to absorb large local arithmetic jumps through mergers.

## 10. RL290 chain `2514 -> 5378 -> 21842 -> 8192` and nested `1074 -> 1364`

PASS. The new statements concern minimum historical ownership or exact finite terminal reachability, not monotonicity along a selected edge. Large jumps along non-minimum histories therefore do not contradict the promoted results.

## 11. Fake/generalized families

PASS with explicit scope. The H<=28 and H<=60 computations deliberately start from RL288's enlarged first-positive envelope. They are used only in the safe direction: every genuine first-positive state in the claimed cap is contained in the relaxed seed set at assigned cost no greater than its genuine cost. The three relaxed owner exceptions are repaired using a promoted genuine fixed-seed lower bound and a tiny exact genuine cut. No relaxed state is promoted as genuinely reachable.

## Additional certificate checks

- Checkpoint-8 A<=7 closure is complete in the exact positive physical graph under the stated area cap; the nonempty return to 8 is treated separately from its zero-cost source state.
- Checkpoint-2 domination uses an analytic tower split; the first unresolved tower depth cannot return below the claimed cost, and the depth-6 equality return has only four cheap boundary exits.
- The k=29 proof separates the compact H<=28 first-checkpoint splice from the independent P-source A<=27 exact closure. Neither certificate assumes the desired Gate-A inequality.
- The P-source verifier reproduces independent RL292 checkpoint/power-minimum regressions before using `2^29` absence.

## Mechanical repair

The H<=60 owner verifier originally labelled 225 as the size of the tiny H<=8 `dist` map. The map includes the two terminal checkpoint states `2` and `8`, so total stored states are 227 and transient states are 225. The repaired verifier asserts both counts. The endpoint set `{2@3,8@3}`, exception repair, and mathematical classification are unchanged. Per `docs/PROOF_STATE_CLASSIFICATIONS.md`, this is a mechanical bookkeeping repair, not a mathematical demotion.

## Scope conclusion

No promoted RL293 claim implies the full all-depth first-d=1 owner theorem or `Bcal(2,3)<=1`. Gate A remains open with the newly contracted exact residual `k>=31`, odd, `H_can<k`.
