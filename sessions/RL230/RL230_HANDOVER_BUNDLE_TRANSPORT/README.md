# RL230 handover bundle transport

This directory documents the lossless Git-tree transport for the completed RL229 authoritative package.

The deterministic canonical ZIP is:

`RL229_H21_Rank_Blind_Charge_and_Physical_Incidence_Barrier_2026-09-01.zip`

Canonical ZIP SHA256:

`f2ec9e2d4ee407ebe1dd36d327b72c469082acaa8a36750054fd29b48d176840`

The completed package directory is stored directly in Git with tree SHA:

`83c9ecd1357a1e27ee77fe59630d52038b2ccefd`

Run `reconstruct_rl229_bundle.py` inside that package to reproduce the canonical ZIP, then compare it to the outer `.zip.sha256` sidecar.

The package contains the RL229 rank-blind H21 charging theorem/barrier, proof/correction ledgers, proof state, red team, provenance, exact certificate summary, portable verifiers, internal `SHA256SUMS.txt`, session handover, and the unique RL230 target.

RL230 is prepared but NOT STARTED.
