# RL229 certified facts and proof-state ledger

Date: 2026-09-01.

## Classification

RL229 promotes **one exact structural charging theorem**, one exact optimization-persistence
corollary, and a **scoped decisive physical-incidence barrier**. This satisfies target
success class **C — decisive barrier**.

It promotes no new terminal-rank deletion, no physical-H21 existence theorem, no sole-high-branch
contradiction, no Gate closure, and no global nontrivial-cycle exclusion.

## Frozen inherited state

- necessary terminal-rank frontier: **13,415,865,870**;
- above-p source count: **7,091,831,283**;
- below-p source count: **6,324,034,587**;
- e=16 terminal rank `34,124,151,203`: excluded;
- e=4 terminal rank `31,435,476,727`: live;
- e=4 combined arithmetic survivors certified through transition 43: **3,856,660,232**;
- dangerous physical H21 {33,34,35} necessary terminal-rank core:
  `D=[23,369,453,298,41,775,866,136]`;
- inherited monotone two-level H21 charge budget:
  `2x+y <= 2^-22`, with `x>=y`;
- inherited optimum: `x=y=1/(3*2^22)`;
- physical H21 incidence/charge bridge, sole-high-branch contradiction, Gate A, Gate B,
  and global exclusion remain open.

## RL229-T1 — rank-blind duplicate-constraint theorem

**Classification: proved exact structural theorem at the inherited H21 charging scope.**

Let `S` be any nonempty set of still-admissible dangerous-H21 terminal ranks in `D`.
For every `r in S`, the inherited H21 {33,34,35} co-ownership argument imposes the
same charge row

`2x+y <= 2^-22`.

The coefficients and right-hand side contain no terminal-rank coordinate. Therefore

`intersection_(r in S) {(x,y): 2x+y <= 2^-22}`

is exactly the single half-space `2x+y <= 2^-22` whenever `S` is nonempty.

Consequently deleting any proper subset of the admissible H21 ranks produces **zero
relaxation** of this rank-blind charging constraint. The constraint disappears through
rank elimination only after the entire relevant H21 rank-support set has been excluded,
or after a new physical theorem replaces it by a rank-sensitive incidence/multiplicity law.

This is not a claim that any remaining necessary rank is physically realized.

## RL229-T2 — exact current optimization-persistence corollary

**Classification: exact analytic/combinatorial corollary.**

RL190 gives

- `CLEAN=10,075,174,499`;
- `N35=9,168,536,354`;
- `N36=7,238,318,174`.

Hence the two relevant populations are

- `a=CLEAN-N35=906,638,145`;
- `b=N35-N36=1,930,218,180`;

and

`2b-a=2,953,798,215>0`.

On the active H21 budget `y=2^-22-2x`, the two-level objective has slope
`a-2b<0`. Together with `x>=y`, this forces the optimum to the equality point

`x=y=1/(3*2^22)`,

exactly as inherited.

The current live e=4 terminal rank `31,435,476,727` lies inside `D`, and its inherited
arithmetic family remains nonempty with `3,856,660,232` certified combined survivors
through transition 43. Therefore the present proof state has not eliminated the final
dangerous-H21 rank support needed to remove the rank-blind row. The charging optimum
and the inherited `>443` ordinary absolute-flow floor receive **no improvement** from
the isolated-rank arithmetic contractions obtained so far.

Again, arithmetic nonemptiness is not physical realization.

## RL229-B1 — exact physical-incidence information boundary

**Classification: scoped decisive barrier / missing-invariant theorem.**

The inherited physical machinery already proves substantial incidence and ownership:

1. RL184 forces at least `10,075,174,499` clean 40-edge physical corridors and at least
   `251,879,363` distinct physical nonzero-defect phases.
2. RL187 proves exact first-defect ownership: clean starts selecting the same physical
   first defect share the same terminal numerator, endpoint height and terminal mechanical rank;
   its joint-offset charging handles overlap/multiplicity without leakage.
3. RL190 identifies the dangerous H21 {33,34,35} rank core and its rank-blind charge row.

What is **not** inherited is a theorem forcing those physical nonzero defects to occur in
the H21 height-21 rank family with a rank distribution that can consume arithmetic rank
deletions. RL187's H21 families are necessary co-ownership possibilities, not existence
claims, and `D` is a necessary core, not a physical population.

Therefore the exact missing invariant is one of:

- a **rank-sensitive physical incidence/multiplicity measure** for dangerous H21 owners;
- a **family-wide exclusion** eliminating every dangerous-H21 rank that can support the
  binding charge row; or
- a **direct owned-macro / high-branch contradiction** bypassing H21 rank charging.

Without one of these, further isolated H21 rank deletions cannot improve the current
physical charging LP. This is the RL229 success-C pivot.

## Conditional candidate-to-physical transfer retained

For a fixed terminal rank `r`, if an inherited arithmetic candidate family `C_r` is proved
necessary for a physical H21 realization at `r`, then `C_r=empty` soundly excludes a physical
H21 realization at that rank. This implication needs no separate existence theorem.

Its converse is forbidden: `C_r` nonempty does not prove physical incidence. And deleting
one such rank does not relax the current rank-blind H21 charge row while another admissible
rank remains.

## Global scope locks

- Necessary terminal ranks remain **13,415,865,870**.
- e=4 terminal rank `31,435,476,727` remains live.
- e=4 certified combined survivor count remains **3,856,660,232 through transition 43**.
- No transition-44 deletion is promoted.
- No physical H21 realization/existence theorem is promoted.
- No exhaustive H21 charge theorem is promoted.
- The inherited `>443` ordinary absolute corrected-flow floor is not sharpened.
- Sole-high-branch contradiction, Gate A, Gate B and global nontrivial-cycle exclusion remain open.
- Knowledge catalogues remain `stale/deferred`.
