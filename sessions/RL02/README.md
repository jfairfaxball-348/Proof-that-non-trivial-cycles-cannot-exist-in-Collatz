# Collatz R# RL-2 Handover

Session 2 reclassified the inherited suffix-signature tower: `Phi_h` is the exact 3-adic inverse-integrality code for any history ending at the same state, so raw tail/cycle `Phi_h` matching is automatic rather than a new pruning constraint.

The session replaces that state with exact physical height defect above `R#` and introduces cycle/tail-relative divergence and xi side-branch probe valuations.

No nontrivial-cycle exclusion is claimed.
