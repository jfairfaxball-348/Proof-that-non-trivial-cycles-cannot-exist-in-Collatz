# RL201 correction / demotion ledger

Date: 2026-08-31. No inherited theorem, rank range, or certificate conclusion is
demoted. Two repair events are recorded explicitly.

1. Mechanical incoming packaging repair, before mathematics: the conveyor did
   not recognize the RL200 reconstruction-script-only transport. The unchanged
   script reproduced the exact existing outer SHA256, fresh verification passed,
   and only that physical ZIP was added at commit
   `e47dcb720028b4465d125c342fe86fb8daac3aa9`. No RL number was consumed and no
   payload, protocol, verifier or mathematical classification changed.
2. Verifier-support contract correction during RL201: RL200 ln_bounds returned
   `[partial,partial+tail]` for x<1, whereas the valid two-sided interval is
   `[partial-tail,partial]`. The first invalid object is the generic helper
   contract. Its sole affected actual input was KLO/2^37; the inherited asserted
   lower bound subtracts the old upper endpoint, which is conservative. All
   used inequalities independently pass corrected bounds. New support uses the
   correct contract, and frozen history is not silently rewritten.

Required scope locks:

- The new exclusion is only two lift residues at one necessary rank. It does not
  remove an entire rank or eta class; 16192674019 necessary ranks remain.
- Rank labels/CRT witnesses/local reverse strips are not physical populations
  or globally realized height words.
- Coupled-strip invariance uses the completed carry gap. Ordinary uncompleted
  gaps need not be invariant at the seam. Full periodic/global vertex
  identifications, absolute anchors, signed successors and complete moments are
  not covered by the translation theorem.
- A fixed compatible state template retains both parities and every nu=1..21;
  this alone does not establish state blindness. The separate state-partner
  lemma changes the first two exponent pairs and tau35 height, and needs
  positive tau36 heights.
- The continuous-mass theorem preserves a starting K0 value; branch value 2^37
  is an additional starting hypothesis. It supplies no actual branch-normalized
  word, no dyadic admissibility and no verified eta-class crossing.
- The complete-word dyadic-gap denominator theorem remains binding: do not
  declare integrality an extra missing test once its full hypotheses hold.
- All inherited finite-cutoff, canonical-lift, rank/time, physical/necessary,
  variation/excursion, H21 ownership/budget, branch/Gate/global locks persist.
