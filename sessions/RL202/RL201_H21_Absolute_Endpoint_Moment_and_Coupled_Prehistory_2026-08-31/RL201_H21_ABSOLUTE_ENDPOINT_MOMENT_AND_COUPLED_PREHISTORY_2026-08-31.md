# RL201 — H21 absolute endpoint moment and coupled prehistory

Date: 2026-08-31. Incoming handover RL200; BASE_HEAD
`e47dcb720028b4465d125c342fe86fb8daac3aa9`. Physical claims remain conditional
on the sole high branch `(37,0,23,-1)`.

RL201 does not eliminate a whole eta class, decide state e35, or decide terminal
sign. It obtains a rank-specific lift exclusion and sharpens the exact global
information needed to go further.

1. Over every rank in the inherited refined core, both signs and all nu=1..21,
   the terminal-plus-successor K corridor excludes exactly
   `(r,sigma,nu)=(25583192106,+,21)`. Equivalently it adds forbidden eta residues
   `37544868` and `20767652` modulo37748736 at that rank only. Necessary-rank
   count and all four mod18 classes remain unchanged.
2. The full rank-chain moment and exact fixed-K0 moment determine the absolute
   endpoint through `J_a=alpha*P_p-(alpha-1)*P_a`, where `P_v=sum_(j<v)q_j`:
   `3(alpha-1)rho_a(2^33 eta-1/2)=3K0-J_a`.
   Its spacing per unit eta is `D_a=3(alpha-1)rho_a2^33`, with exact bounds
   `1/20<D_a<2/15`. A fixed K0 scalar alone does not provide J_a.
3. A precisely scoped continuous-mass perturbation preserves fixed starting K0,
   protected local K, masses and gaps, with explicit cap/corridor bounds. This
   is conditional on stated slack and is not an admissible integer-height word
   deformation or a proof that another eta class is reached.
4. With a fixed compatible pair of finite reverse exponent words, translating
   eta by a multiple of `3^(m+1)` preserves all exact local coupled gaps,
   heights, exponents and rank-resolved K, including the p-shift carry. CRT
   realizes every one of 42 terminal sign/valuation pairs within that state.
   Global vertex identifications, absolute anchors, closure, full-word moments
   and signed post-terminal data are outside this barrier's scope.
5. At tau36 the lower/upper tau34-labelled trajectories have respectively even
   and odd heights, so the earlier defect is odd and nonzero. A separate
   conditional state-partner lemma preserves earlier heights and completed gaps
   when both tau36 heights are positive; it changes the first two exponent pairs
   and tau35 height and is not fixed-word state blindness.

The three proof components give complete derivations and exact coverage. The
successor proof covers the whole rank interval analytically via monotonicity;
the coupled verifier has 168 exact witnesses at two stated ranks, while its
infinite claim follows from algebra/CRT. The moment verifier uses exact actual-
constant spacing comparisons plus 80 toy perturbations; toys are not actual
high-branch height-word certificates.

An inherited helper's two-sided logarithm contract for x<1 was explicitly
corrected. Every actually used RL200 inequality remained conservative and was
reconfirmed with corrected rational enclosures; no inherited theorem, rank
range or certificate conclusion is demoted. A separate mechanical commit before
RL201 restored the exact missing incoming ZIP and consumed no RL number.

The successor must control J_a on a genuine dyadic admissible word or supply an
absolute height/phase identification that breaks the local translation. More
unanchored reverse depth or a rerun of the same coarse successor corridor will
not select a class. All H21 charging, branch/Gate/global obligations remain open.
