# RL201 certified facts and proof ledger

Date: 2026-08-31. Canonical status-bearing ledger. Physical implications retain
the sole-high-branch scope `(37,0,23,-1)` and every inherited scope lock.

## Proved analytic mathematics

- Complete signed successor-corridor classification: only
  `(r=25583192106,sign=+,nu=21)` is newly excluded by this specific consumer.
- Corresponding new eta residues `37544868` (class0) and `20767652` (class8)
  modulo37748736 are forbidden at that rank only. No whole rank/class removed.
- Exact absolute rank-chain and fixed-K0 oriented endpoint identities in
  `proofs/RL201_ABSOLUTE_ENDPOINT_MOMENT.md`; new content is their explicit eta
  specialization, not rediscovery of the inherited telescope/window inversion.
- Exact joint-prefix lattice spacing `1/20<D_a<2/15` for the stated interface.
- Conditional continuous-mass flexibility with explicit positive-coordinate,
  cap, placement and optional strict-corridor hypotheses. It does not preserve
  integer-height/dyadic structure and does not certify mod18 class reachability.
- Fixed finite local coupled reverse-strip translation preserves both exponent
  words, induced heights, unit odd states, completed gaps and local K; each
  compatible fixed state template permits all signs and nu=1..21 before extra
  signed/absolute/global data. No global-identification or closure claim.
- Forced tau36 parity: minus-labelled height even, plus-labelled height odd,
  hence earlier defect odd/nonzero. Absolute zero height is not established.
- Conditional state-partner lemma: when both state011 tau36 heights are positive,
  changing the specified first two exponent pairs produces a state111 partner
  with equal earlier heights and completed gaps. Leading fields differ; this is
  not a proof that one fixed exponent word permits both states.

## Exact finite certificates and regressions

- `verification/verify_rl201_successor_corridor.py`: exact rational boundary and
  all-valuation checks, CRT residues, seam and deletion accounting. Together
  with analytic monotonicity it covers all integer ranks in the stated core,
  both signs and nu=1..21; no unreported rank scan.
- `verification/verify_rl201_endpoint_moment.py`: exact actual-constant rational
  spacing comparisons and 80 complete signed perturbation regressions in the
  two toy Bezout systems/ranges stated in the proof. Toys are algebraic support,
  not actual-constant orbit or class-survival certificates.
- `verification/verify_rl201_coupled_reverse.py`: exactly 168 coupled witnesses
  (2 stated ranks x 2 state templates x 2 signs x 21 valuations), including an
  actual mechanical seam. This finite set does not enumerate all ranks/eta;
  the universal claim is proved algebraically with CRT.
- `verification/verify_rl201_log_support_repair.py`: every used inherited RL200
  log inequality rerun with valid sign-aware enclosures; no conclusion changed.

## Externally inherited certificate/state

Incoming RL200 is accepted after exact ZIP restoration, SHA256/internal manifest,
complete fresh unpack and fast suite. Its refined necessary-rank core,
16192674019 post-deletion count, terminal K law, first prehistory bits and
uncoupled barrier remain binding. RL199 interface/Hensel exclusions and RL195
complete-word denominator equivalence remain inherited, with exact source
pointers in provenance and load-bearing definitions restated in this handover.

## Method barriers and open obligations

The specific two-K corridor cannot eliminate an entire eta class. Unanchored
finite coupled reverse strips cannot select terminal sign/valuation. The real-
mass deformation is only a relaxation barrier, not a full exact admissible-word
barrier. No completed global admissible word or physical H21 occurrence is given.
All four classes 0,8,9,17 mod18, state e35, terminal sign, H21 charging/ownership,
sole-branch, Gate A/B, nontrivial-cycle and global Collatz obligations remain open.
