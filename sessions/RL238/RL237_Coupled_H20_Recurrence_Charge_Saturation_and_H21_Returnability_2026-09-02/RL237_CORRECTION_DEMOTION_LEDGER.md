# RL237 correction / demotion ledger

RL237 introduces **no demotion of valid RL236 or RL235 mathematics**.

Scratch-state corrections and supersessions:

- the early one-dimensional continuation after spacing `>=210` is superseded by the coupled `a,b,c` optimisation; its tiny intermediate flow value is not the final RL237 theorem;
- the provisional suggestion that the next coupled blocker would be the H20 `T=150094635296999121` subcell was based on the old 13-cell shortlist and is **superseded / NOT PROMOTED**;
- the mandatory full `7531`-cell sweep shows the true first new uncapped boundary is H21 `T=550346996088996777`, owners `{34,35}`;
- intermediate approximate flow bounds `742.097...` and `742.423077...` are superseded by the final fully swept bound `>742.4232`;
- the final H21 spacing `>=3032` theorem is promoted only for its exact invariant and is **not** sufficient to pay its `<=6182` incidence requirement for further middle-band charging;
- no recurrence cap is transferred between distinct terminal invariants or owner cells.

The RL236 withdrawn `>763` scratch lift remains withdrawn. Historical RL231 charging defects and associated demotions remain unchanged.

The RL231–RL237 programme is frozen for strategy, not demoted for correctness.
