# RL86 — RL↑ roof-feeder ray: canonical affine odometer, finite re-entry window, and closure-neutrality barrier

Date: 2026-08-25

## 0. Executive outcome

RL86 executed the authoritative RL↑ target from the checksum-clean RL86 handover and preserved the frozen RL85 ledger under the verification-economy rule.

**No Gate A, Gate B, RL/nontrivial-cycle exclusion, or Collatz closure is claimed.**

The session separates one genuinely useful finite-roof consequence from a decisive closure-architecture barrier.

### Main negative conclusion

The bare upper ray

`RL↑={F_k=2^k M:k>=1}`

is mathematically automatic for every positive integer base `M`.  Its existence therefore imposes **no new condition at all** on the fact that `M` is the maximum of a hypothetical loop.  RL80's stronger theorem already proves that dyadic saturation and even full backward saturation are cycle-intersection neutral.

Anchoring the ray at the genuine maximum does create a canonical ordinary-`+1` side structure, but it is exactly the old 3-adic sibling odometer in roof coordinates rather than a new ownership mechanism.

Put

`F_{2j}=4^j M`

and define its legal odd shortcut predecessor

`Y_j=(2F_{2j}-1)/3=(2*4^j M-1)/3`.

Then

`Y_0=P=(2M-1)/3`,

`T(Y_j)=F_{2j}`,

and

`boxed: Y_{j+1}=4Y_j+1.`

For every `j>=1`, `Y_j>M`, so these side feeders are external to the loop.  For every ternary depth `a>=1`, the block

`Y_0,Y_1,...,Y_{3^a-1}`

is a complete residue system modulo `3^a`.  This is the roof-specialized realization of the historical sibling odometer, not an address restriction.  In physical size the same family is only logarithmically numerous below `X`, so **maximal finite-depth 3-adic coverage coexists with zero physical density**.

### New positive RL86 splice: first-Farey cap makes pure-odd re-entry finite

A deeper consecutive-odd inverse chain from the external even ray level `F_{2j}=4^jM` has the exact form

`boxed: X_{j,r}=2^r(4^j M+1)/3^r -1}`

and exists for `r` steps exactly when

`boxed: 3^r | 4^j M+1.}`

Thus its maximum consecutive-odd depth is

`v_3(4^jM+1)`.

On the exact first-Farey branch, RL85 gives

`M<3^h`, `h=27,021,536,997`.

RL86 proves:

> **Finite pure-odd re-entry theorem.** If `j>=1`, a legal consecutive-odd descendant `X_{j,r}` of `F_{2j}` satisfies `X_{j,r}<=M`, then
>
> `boxed: r<=42,828,136,343}`
>
> and
>
> `boxed: j<=12,526,437,560.}`

The arithmetic uses the exact finite rational certificate

`3^1,000,000 < 2^1,584,963`,

so

`log_2 3 < 1.584963`

and

`log_2(3/2)<0.584963`.

This is a real consequence of combining the finite roof with the infinite feeder ray: **any pure accelerated-`r=1` feeder corridor that can descend back to the roof scale must originate inside a finite initial part of the ray.**

It is not a contradiction.  The finite window still contains more than twelve billion possible even ray levels, the actual high 3-adic valuations are not forced, and no inherited historical lower-barrier theorem converts this finite window into an exclusion.

### Historical least-red bridge: exact but scale-paid

If historical `R#` exists as the least positive red integer, then every legal inverse ancestor of a ray node is red.  Therefore every legal `X_{j,r}` obeys

`boxed: (3/2)^r (R#+1) <= 4^j M+1.}`

This is the exact off-cycle feeder form of the historical minimality constraint.  It does not remove the route barrier: increasing `j` pays for increasing odd depth through the factor `4^j`.  The infinite ray supplies unbounded physical scale at exactly the place where the old corridor mechanism needs an independent lower barrier.

### Route decision

RL↑ is therefore frozen as a **closure-neutral architecture under the current hypotheses**, with one retained quantitative lemma: the first-Farey finite pure-odd re-entry window.

The direct RL↑ route is not selected for RL87.  Per the RL86 target, the authoritative live obligation returns to the strongest RL85 splice:

> first-Farey physical maximum cap -> proper synchronized return span `<=43,234,459,194` -> at least `930,959` spaced-distinct quotient states on the RL73 giant-macro intersection -> **find an ownership-sensitive physical/full-`D` packing consumer, or prove that no such consumer exists.**

---

## 1. Incoming authority and verification economy

The supplied RL86 handover was checked before new mathematics:

- outer SHA-256 sidecar: PASS;
- fresh-unpack internal `SHA256SUMS.txt`: PASS;
- `bash verification/run_fast_rl85_verifiers.sh`: PASS.

The RL85 comparative-bridge verifier reproduced:

- first Farey pair `(114,208,327,604, 72,057,431,991)`;
- `h=27,021,536,997`;
- `45,035,894,994` forced leading ternary zeros;
- proper synchronized-return span `<=43,234,459,194`;
- `10` repeat depth `<=21,617,229,597`;
- `101` repeat depth `<=13,510,768,498`;
- RL73-intersection spaced-distinct quotient count `>=930,959`;
- roof predecessor classes `(17,53,101) mod108`.

Under the verification-economy rule the frozen RL85 theorem/evidence/barrier ledger is accepted without recursively replaying historical expensive certificates.

Retain globally:

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The inherited `R>=2^71` statement remains an **external computational cycle-minimum input**.  RL86 does not silently promote it to a lower bound for historical global `R#`.

---

## 2. Object taxonomy: freeze the transverse/longitudinal distinction

Use

`T(n)=n/2` for even `n`,

`T(n)=(3n+1)/2` for odd `n`.

For a chosen hypothetical nontrivial cycle `C`, write

`R=min C`, `M=max C`.

| object | on chosen cycle? | preperiodic to chosen loop? | repeating as a cycle phase? | individual forward orbit bounded? | unbounded only as a family? |
|---|---:|---:|---:|---:|---:|
| historical `R#` | not determined | not determined | not determined | not determined; could belong to RL or RO | no fixed classification |
| cycle minimum `R` | yes | already periodic | yes | yes | no |
| cycle interior state | yes | already periodic | yes | yes | no |
| `RL♭=M` | yes | already periodic | yes | yes | no |
| `F_1=2M` | no | yes, one step | no before entry | yes | member of an unbounded family |
| `F_k=2^kM`, `k>=1` | no | yes, `k` steps | no before entry | yes | yes |
| genuine `RO` state | no finite loop ownership assumed | no | no | no under the project RO meaning | unbounded longitudinally along one orbit |

For every `k>=1`,

`T^k(F_k)=M`.

Hence each individual `F_k` trajectory is bounded by the finite starting value `F_k`: it takes `k` deterministic halvings into the same finite loop.

The set `{F_k}` is unbounded because the **initial condition changes with `k`**.  This is transverse unboundedness.  It is not an `RO` orbit.

---

## 3. RL80 red-team: the bare ray is universal and cycle-neutral

For every positive integer `x`, not merely for a cycle maximum,

`2^k x -> 2^(k-1)x -> ... -> x`.

Therefore the existence of the dyadic ray over `M` cannot be a new maximum theorem.

RL80 proved the stronger general statement.  If `C` is a periodic orbit and `S` any seed set, then

`C intersect Sat_2(S) != empty`

iff

`C intersect S != empty`.

Even for the full backward closure

`Pre*(S)={x:T^r(x) in S for some r>=0}`,

one has

`C intersect Pre*(S) != empty`

iff

`C intersect S != empty`.

Apply this to the external ray

`S=RL↑={F_k:k>=1}`.

Since every `F_k>M`,

`C intersect S = empty`.

Consequently

`boxed: C intersect Pre*(RL↑)=empty.}`

Thus even arbitrarily thick legal inverse trees grown **above the external ray** cannot become a cycle-intersection argument by themselves.  If a new theorem is to emerge, it must use an absolute-size/minimality/ownership condition rather than mere backward abundance.

Classification: **inherited RL80 no-go theorem + new direct specialization**.

---

## 4. Canonical maximum fork and the complete odd side ladder

RL85 freezes

`P=(2M-1)/3`,

`F_1=2M`,

`F_1=3P+1`,

with `P` the cycle-owned odd predecessor of `M` and `F_1` the external even predecessor.

The maximum classes imply

`M==26,80,152 (mod162)`.

In particular

`M==2 (mod3)`

and indeed

`M==8 (mod9)`.

### 4.1 Which ray levels admit an odd predecessor?

Since `2==-1 (mod3)` and `M==-1 (mod3)`,

`F_k=2^kM == (-1)^(k+1) (mod3)`.

Therefore

- `F_k==1 (mod3)` for odd `k` and has no odd shortcut predecessor;
- `F_k==2 (mod3)` for even `k` and has one legal odd shortcut predecessor.

Write the even levels as

`F_{2j}=4^jM`, `j>=0`.

Define

`boxed: Y_j=(2F_{2j}-1)/3=(2*4^jM-1)/3.}`

Then `Y_j` is a positive odd integer and

`T(Y_j)=F_{2j}`.

At `j=0`,

`Y_0=P`.

For every `j>=1`,

`Y_j>M`,

because

`2*4^jM-1>3M`.

Hence every `Y_j`, `j>=1`, is an external preperiodic red feeder if the loop exists.

### 4.2 Exact affine recurrence

Direct subtraction gives

`Y_{j+1}-4Y_j=1`.

Thus

`boxed: Y_{j+1}=4Y_j+1.}`

Equivalently

`boxed: 3Y_j+1=4^j(3P+1)=2*4^jM.}`

This is exactly the historical sibling-odometer recurrence.  RL86 has not discovered a new odometer; it has identified the canonical roof ray as a concrete realization of the frozen historical object.

Classification: **new roof specialization of inherited analytic odometer structure**.

---

## 5. Exact ternary residue completeness of the roof side ladder

The roof anchor gives

`3Y_0+1=2M==1 (mod3)`.

For `a>=1`, consider residues modulo `3^(a+1)`.  LTE gives

`v_3(4^(3^a)-1)=a+1`.

Hence `4` has exact order `3^a` in the subgroup

`U_a={u mod 3^(a+1): u==1 (mod3)}`,

whose order is also `3^a`.

Therefore `4` generates `U_a`.  Multiplication by the fixed principal unit `2M` merely cyclically shifts the same group, so

`2M, 4(2M), ..., 4^(3^a-1)(2M)`

runs through every member of `U_a` exactly once.

The map

`u -> (u-1)/3`

is a bijection from `U_a` modulo `3^(a+1)` to all residues modulo `3^a`.  Since

`Y_j=(4^j(2M)-1)/3`,

we obtain:

### Theorem 5.1 — roof-side odometer completeness

For every `a>=1`,

`boxed: Y_0,Y_1,...,Y_(3^a-1)}`

form a complete residue system modulo `3^a`.

Classification: **analytic specialization of the inherited RL00 sibling-odometer theorem**.

### Consequence 5.2 — no fixed-depth ternary stabilization

The roof side ladder does not stabilize to, avoid, or prefer any fixed residue cylinder modulo a power of `3`.  It hits them all.

Therefore an attempted theorem of the form

> “the infinite roof feeder must eventually enter a forbidden fixed-depth ternary address”

cannot work: every address occurs, and occurrence itself carries no cycle ownership.

This directly reproduces the RL80 warning that maximal residue coverage does not imply equality with a basin/cycle state.

---

## 6. 3-adic abundance versus physical sparsity

The same family that is residue-complete is archimedeanly sparse.

From

`Y_j=(2*4^jM-1)/3`,

`Y_j<=X`

is equivalent to

`4^j <= (3X+1)/(2M)`.

Hence the number of side-ladder nodes below `X` is at most logarithmic in `X`:

`# {j>=0:Y_j<=X} = O(log X)`.

Likewise

`# {k>=1:F_k<=X} = O(log X)`.

Therefore the union of the bare ray and its canonical first odd side ladder has natural density zero.

### Structural lesson

At ternary depth `a`, the first `3^a` side feeders already cover **every** residue modulo `3^a`, but the largest one is of order

`4^(3^a) M`.

Thus perfect finite-depth 3-adic coverage is purchased by doubly severe physical separation.  This is an exact roof instance of the recurring address/physical-scale decoupling barrier.

Classification: **new analytic corollary / route barrier**.

---

## 7. Consecutive odd inverse descendants of the ray

The first side feeder is not the whole inverse story.  Starting from an even ray level

`F_{2j}=4^jM`,

apply consecutive legal odd inverse steps

`x -> (2x-1)/3`.

If `X_{j,r}` denotes the result after `r` such steps, then adding `1` linearizes the recurrence:

`X_{j,r}+1=(2/3)^r(F_{2j}+1)`.

Therefore

`boxed: X_{j,r}=2^r(4^jM+1)/3^r-1.}`

### Theorem 7.1 — exact legality/depth criterion

A chain of `r` consecutive odd inverse steps from `F_{2j}` exists iff

`boxed: 3^r | 4^jM+1.}`

Consequently the maximal consecutive-odd depth is

`boxed: r_j=v_3(4^jM+1).}`

Classification: **new elementary analytic roof-feeder theorem**.

The deepest node has

`X_{j,r_j}+1=2^(r_j) a_j`,

where `3` does not divide `a_j`.  Hence

`X_{j,r_j} != 2 (mod3)`.

So the terminal residue of a maximal odd feeder chain is automatically compatible with the historical necessary condition `R# !=2 (mod3)`.  The residue test does not create a contradiction.

### 7.2 Base consistency: the inherited `OO` roof grammar

At `j=0`, `X_{0,1}=P` is the owned predecessor of `M`.

Moreover `P>M/2` for `M>2`.  The even inverse `2P` is therefore larger than the maximum and cannot be the preceding cycle state.  Hence the previous cycle state must be

`X_{0,2}=(4M-5)/9`,

forcing `9 | M+1`.

This is precisely the inherited leading `OO` maximum grammar and is recorded here only as a consistency check, not as a new RL86 promotion.

For `j>=1`, every `X_{j,r}` is external to the cycle: its forward orbit reaches `F_{2j}>M`, which a periodic cycle state cannot do.

---

## 8. Exact historical least-red feeder inequality

Assume historical

`R#=least positive red integer`

exists.

Every `F_{2j}` is red if the chosen nontrivial loop exists.  Every legal inverse ancestor `X_{j,r}` is then also red.  Therefore

`X_{j,r}>=R#`.

Using the exact formula from Section 7 gives

`2^r(4^jM+1)/3^r -1 >=R#`.

Equivalently:

### Theorem 8.1 — off-cycle depth/scale payment

For every legal pure-odd feeder branch,

`boxed: (3/2)^r (R#+1) <= 4^jM+1.}`

Classification: **new analytic bridge to historical least-red minimality**.

This is the strongest direct historical transplant found in RL86 that genuinely uses the external feeder status.

### Why it does not close

The inequality says precisely that odd inverse depth must be paid for by physical starting scale.  But the ray supplies

`F_{2j}=4^jM`

with unbounded `j`.  Therefore the right-hand side is unbounded, and arbitrarily large allowed odd depth is compatible with historical minimality.

This is not merely a heuristic.  The odometer completeness theorem ensures arbitrarily deep 3-adic legality occurs somewhere along the side family, while Theorem 8.1 explains why those deep occurrences need not leak below the least-red barrier: their physical base has moved far enough outward.

Thus the deliberate off-cycle application of historical minimality produces a true theorem, but it reproduces the inherited **scale-payment barrier** rather than escaping it.

---

## 9. New first-Farey finite pure-odd re-entry theorem

The scale-payment theorem still leaves a sharper question:

> If the base roof itself is absolutely bounded by RL85, can arbitrarily remote ray levels send a pure odd inverse corridor back down to roof scale?

On the exact first-Farey branch RL85 proved

`M<3^h`,

with

`h=27,021,536,997`.

Because `M` is an integer,

`M+1<=3^h`.

RL86 adds the exact finite rational comparison

`boxed: 3^1,000,000 < 2^1,584,963.}`

Hence

`log_2 3 < 1.584963`

and

`log_2(3/2)<0.584963`.

Suppose `j>=1`, the pure odd chain of depth `r` is legal, and

`X_{j,r}<=M`.

Write

`a=(4^jM+1)/3^r`,

so `a` is a positive integer and

`X_{j,r}+1=2^r a`.

Then

`2^r <= X_{j,r}+1 <= M+1 <=3^h`.

Therefore

`r < h*(1,584,963/1,000,000)`.

The exact integer consequence is

`boxed: r<=42,828,136,343.}`

Next, rearrange the exact descendant equation:

`4^jM+1=(3/2)^r(X_{j,r}+1)`.

Since `M>=2`,

`4^j < (3/2)^r(1+1/M) <= (3/2)^(r+1)`.

Therefore

`2j < (r+1) log_2(3/2)`

and hence

`2j < (r+1)*(584,963/1,000,000)`.

Using the preceding exact bound on `r` gives

`boxed: j<=12,526,437,560.}`

### Theorem 9.1 — finite pure-odd re-entry window

Under the exact first-Farey physical maximum theorem, every consecutive-odd inverse branch from an external even ray level that reaches `M` or below lies inside

`1<=j<=12,526,437,560`,

`1<=r<=42,828,136,343`.

For every

`j>=12,526,437,561`,

all legal consecutive-odd descendants of `F_{2j}` remain strictly above the genuine roof `M`.

Classification: **new analytic theorem + exact finite rational logarithm certificate**.

### Interpretation

This is the strongest positive RL↑ result of the session.

The first-Farey cap does not bound the infinite ray itself; `2^kM` remains unbounded with `k`.  What it does bound is the portion of the ray from which a pure accelerated-`r=1` feeder corridor can physically return to the roof scale.

The infinity is therefore removable for this specific historical corridor architecture: all potentially roof-reentering pure-odd feeders lie in a finite initial window.

### Why the theorem is still not a consumer

The result does **not** prove that any such re-entry exists, nor that its endpoint lies below historical `R#`, nor that the endpoint is cycle-owned.

The window is also far beyond direct brute-force enumeration, and `M` is not a known integer: it is the unknown physical maximum selected by a huge word cylinder.

No inherited theorem couples the required valuations

`v_3(4^jM+1)`

through this finite `j` window to the RL85 maximum cylinder strongly enough to force a forbidden endpoint.

Accordingly RL86 records the theorem but does not select a new giant residue search over this window.

---

## 10. First-Farey cylinder-height interaction

RL85 proved that at the exact first Farey pair:

- `M` is the least positive even representative of its `q`-digit maximum cylinder;
- `M<3^h` with `h=27,021,536,997`;
- the zero-padded `q`-digit ternary representative has at least `45,035,894,994` leading zeros.

### 10.1 Pure doubling remains exactly neutral

For

`F_k=2^kM`,

any normalized cylinder-height quantity that scales the modulus by the same dyadic factor is unchanged.  This is the RL84/RL85 feeder observation and RL86 finds no exception.

The physical cap becomes only

`F_k<2^k3^h`,

which has no uniform upper bound in `k`.

### 10.2 The side ladder sees the base but gives no lower height

Normalize the canonical odd side feeder by its dyadic scale:

`Y_j/4^j = 2M/3 - 1/(3*4^j)`.

Thus the only non-homogeneous correction is the vanishing term `1/(3*4^j)`.  The dominant normalized scale converges to `2M/3`.

At fixed ternary depth the side ladder is residue-complete, so it cannot supply an excluded cylinder.  At physical scale it is logarithmically sparse, so residue completeness does not become packing density.

No independent lower cylinder-height theorem is produced.

---

## 11. Ordinary `+1` versus RL79 generalized-increment red-team

The roof side recurrence

`Y_{j+1}=4Y_j+1`

looks promising because it explicitly contains the ordinary unit increment.

However RL79 requires a stronger test: does the architecture actually distinguish `s=1` from the generalized odd-step map

`T_s(n)=n/2` for even `n`,

`T_s(n)=(3n+s)/2` for odd `n`?

Take any positive odd scaling factor `c`.  If an ordinary `s=1` cycle with maximum `M` existed, scaling all states by `c` gives the corresponding `T_c` fake cycle with maximum

`M_c=cM`.

Its canonical roof objects are

`P_c=cP`,

`F_{k,c}=cF_k`,

`Y_{j,c}=cY_j`.

The side recurrence becomes

`boxed: Y_{j+1,c}=4Y_{j,c}+c.}`

Thus the whole finite-roof / infinite-ray / side-ladder architecture is covariant under

`(states,1) -> (c*states,c)`.

Classification: **new RL79-style homogeneity red-team**.

### Consequence

The appearance of `+1` in roof coordinates is real, but without an absolute lattice/equality theorem the feeder geometry itself does not isolate `s=1`.  It is the `s=1` instance of a scaled generalized-increment feeder geometry.

The first-Farey cap is an absolute `s=1` ownership theorem, but RL86's ray supplies no second independent absolute condition to combine with it.

---

## 12. RL↑ versus RO: transverse infinity is not longitudinal escape

The two infinities are now exact:

### RL↑

For each `k`, the orbit of `F_k` is

`F_k,F_(k-1),...,F_1,M,...`

and is eventually periodic.  Its preperiod length to the roof is exactly `k`.

The family is unbounded only because `F_k` changes with `k`.

### RO

A genuine `RO` object is one starting state whose single forward trajectory remains nonperiodic/nonconvergent and becomes unbounded in time.

### Theorem 12.1 — no RO extraction from the ray by tail ownership

Every node in the forward basin of the chosen loop, including every ray node and every legal inverse ancestor of a ray node, is preperiodic to the same finite loop.  None is an `RO` trajectory.

Changing the starting state with `k` cannot be promoted to one longitudinal orbit without an additional theorem identifying the different initial states along one forward trajectory.  No such identification exists; the deterministic relation points the opposite way, from larger `F_k` down to smaller `F_(k-1)` and then into the loop.

Classification: **analytic distinction / route barrier**.

No RL/RO bridge is claimed.

---

## 13. Mandatory route red-teams

### RL80 — dyadic/backward saturation neutrality

**Passed as a negative red-team.**  The bare ray and its entire backward closure cannot force cycle intersection.  RL86 therefore does not use abundance as a set trap.

### RL81 — auxiliary/address information versus physical ownership

**Passed as a negative red-team.**  RL86's strongest residue statement concerns external physical feeder integers, but congruence of those integers with a cycle address is not equality with a cycle phase.  For `j>=1` equality is impossible by size because `Y_j>M`.  No auxiliary/residue-to-cycle lift is inferred.

### RL20 — address/continued-fraction decoupling

**Passed as a negative red-team.**  The side ladder is complete modulo every fixed `3^a` while remaining only logarithmically numerous in physical space.  It supplies no new count ratio, continued-fraction restriction, or full-cycle slope condition.

### RL83 — count-only first-surplus insufficiency

**Passed as a negative red-team.**  The ray adds no owned cycle word and no new first-surplus count.  The finite re-entry theorem is a physical-size statement, not a word-count closure.

### RL84/RL85 — cylinder height without lower consumer

**Passed as a negative red-team.**  Pure doubling preserves normalized height; the side ladder is residue-complete; the finite re-entry window gives no forced lower height.

### Generic infinitely-many-preimages fact

**Passed as a negative red-team.**  The existence of the ray is universal.  The only roof-sensitive additions are the already-owned base fork and its affine odometer specialization.

### RL79 — generalized-increment/homogeneity

**Passed as a negative red-team.**  The entire feeder architecture scales to `T_c`; no new theorem uses the absolute unit `1` in a non-homogeneous way strong enough to distinguish ownership.

---

## 14. Route verdict

RL86 asked whether a finite genuine roof can coexist with an infinite external ray in a nontrivial sense.

The answer under current hypotheses is:

> **Yes.  The coexistence is the generic functional-graph geometry of a map with an always-available even inverse.  The ray itself is tautological; its canonical odd side structure is the already-known residue-complete affine odometer.**

The true maximum contributes one boundary fact: the first fork is ownership-split, with `P` internal and `2M` external.  RL82/RL85 already owned that boundary.

RL86 adds one quantitative consequence when the RL85 first-Farey physical cap is imposed: a pure consecutive-odd feeder branch can return to roof scale only from a finite initial ray window.  That theorem is retained, but no inherited consumer forces a forbidden branch inside the window.

### Closure-architecture classification

- bare RL↑ ray: **dead as a standalone closure route**;
- fixed-depth residue/address route on RL↑: **dead; residue-complete**;
- density from bare ray + first odd side ladder: **dead; logarithmically sparse**;
- historical pure-`r=1` corridor transplant: **finite-window reduction achieved, but no lower-barrier consumer**;
- RL↑ -> RO compactness/scaling bridge: **not established; current direct bridge rejected**;
- first-Farey finite pure-odd re-entry theorem: **retained analytic lemma**.

The route is therefore frozen as **mathematically neutral for closure, with one useful finite-window byproduct**.

---

## 15. Proof-state ledger additions

### New proved analytic mathematics

1. Roof side-feeder ladder:
   `Y_j=(2*4^jM-1)/3`, `T(Y_j)=4^jM`, `Y_{j+1}=4Y_j+1`.
2. For `j>=1`, every `Y_j` is external and strictly above `M`.
3. Roof-specialized odometer completeness: `Y_0,...,Y_(3^a-1)` is a complete residue system modulo `3^a` for every `a>=1`.
4. Physical sparsity of the ray and first odd side ladder: each has only `O(log X)` nodes up to `X`.
5. Exact pure-odd descendant formula
   `X_{j,r}=2^r(4^jM+1)/3^r-1`.
6. Exact legality criterion `3^r |4^jM+1`, hence maximum depth `v_3(4^jM+1)`.
7. Historical least-red depth/scale inequality
   `(3/2)^r(R#+1)<=4^jM+1` for every legal red feeder branch.
8. First-Farey finite pure-odd re-entry theorem:
   if `X_{j,r}<=M`, then
   `r<=42,828,136,343` and `j<=12,526,437,560`.
9. Generalized-increment scaling of the whole roof-feeder architecture:
   `Y_{j+1,c}=4Y_{j,c}+c` under `T_c`.
10. Exact transverse/longitudinal separation: all ray nodes and their inverse ancestors are preperiodic to the loop, not RO trajectories.

### New exact finite arithmetic certificates

The RL86 fast verifier checks:

- inherited roof classes and their `mod9`/`mod3` consequences;
- side-ladder recurrence and physical ordering on representative classes;
- exact orders of `4` in the principal-unit groups for small audited depths;
- residue completeness of the roof side ladder for small audited depths;
- primitive-root coverage of `2` modulo small powers of `3` as a consistency red-team;
- pure-odd descendant/valuation formulas on exact sample branches;
- exact rational logarithm certificate
  `3^1,000,000<2^1,584,963`;
- exact re-entry bounds
  `42,828,136,343` and `12,526,437,560`;
- retained RL85 fallback constants.

These checks guard arithmetic/transcription.  The proofs are analytic in this note.

### Externally inherited inputs

Unchanged:

- the project cycle-minimum floor `R>=2^71` as external computational input;
- RL83/RL84 first-Farey logarithm/defect certificate.

RL86 does not reclassify either.

### Computational evidence

No new bounded Collatz search is promoted.

### New barriers / dead subroutes

1. finite roof + infinite dyadic ray as a contradiction by itself;
2. fixed-depth residue stabilization/exclusion along the ray;
3. physical density from the bare ray or canonical side ladder;
4. promoting residue coincidence of external feeders to cycle ownership;
5. extracting a longitudinal RO orbit from the transverse family;
6. treating `Y_{j+1}=4Y_j+1` as an `s=1` ownership discriminator without an absolute lattice consumer;
7. using the finite re-entry window as a brute-force target without a new symbolic/cylinder theorem.

### Corrections / demotions

No inherited RL85 theorem is demoted.

The terminology `RL↑` is frozen as an **external feeder ray/set**, not a greatest red integer and not an unbounded orbit.

---

## 16. RL87 route selection

Per the RL86 operating instructions, the RL↑ route is now sufficiently interrogated to stop expanding it.

RL87 returns to the exact RL85 live obligation rather than launching another generic residue, dyadic, or continued-fraction ladder.

Carry forward:

`B=43,234,459,194` — uniform proper synchronized quotient-return span on the exact first-Farey/full-phase branch;

`Z=40,249,491,324,522,944` — inherited RL73 giant aligned-zero count;

`D_J>=930,959` — spaced-distinct quotient states forced on the intersection branch.

The target is:

> **Find a genuine ownership-sensitive physical/full-`D` upper budget that those spaced-distinct quotient states must consume, or prove that all natural lifts/packings retain an unbounded common mode and therefore cannot close.**

The next target must explicitly preserve RL81's quotient-to-physical warning and RL79's generalized-increment ownership barrier.
