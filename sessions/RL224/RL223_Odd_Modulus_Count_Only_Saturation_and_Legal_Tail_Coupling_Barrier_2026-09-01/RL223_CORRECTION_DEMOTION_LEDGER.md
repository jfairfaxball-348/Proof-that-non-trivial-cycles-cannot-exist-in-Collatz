# RL223 correction / demotion ledger

Date: 2026-09-01.

No inherited mathematical claim requires correction or demotion in RL223.

## Scope protections recorded by RL223

1. `S_(N,M)(q)=Z/qZ` is proved for all ordinary binary words with the exact
   length and odd count.  It is not proved for the actual continuations of any
   fixed `y16` or for the legal H21 tail language.
2. The ordinary parity-cylinder proof realizes each word with a start that may
   vary with the word.  It may not be used to replace the exact current start.
3. The fixed-prefix corollary retains a verified finite prefix but its formal
   suffix still varies independently.  It is not a continuation certificate.
4. The displayed modulo-5 word replays the inherited phase-51 prefix exactly;
   only the following selectable blocks are formal.  It is neither an arithmetic
   candidate deletion nor a physical survivor.
5. The analytic theorem is uniform only through `q=283635` under `gcd(q,6)=1`.
   Direct finite DP is certified only for `5<=q<=1023` with the same coprimality.
6. Endpoint-return equivalence modulo `q` is necessary for a legal tail, but
   count-only endpoint saturation does not prove that the actual endpoint set is
   saturated.
7. A raw-depth observation about one witness continuation is not part of the
   RL223 target, is not an inherited contradiction, and is not promoted.
8. No candidate, prefix, terminal rank, state, eta class, physical H21
   incidence/charge, Gate A closure, Gate B closure, branch contradiction, or
   global Collatz exclusion is added.

Knowledge catalogues remain `stale/deferred` and are not proof-state authority.
