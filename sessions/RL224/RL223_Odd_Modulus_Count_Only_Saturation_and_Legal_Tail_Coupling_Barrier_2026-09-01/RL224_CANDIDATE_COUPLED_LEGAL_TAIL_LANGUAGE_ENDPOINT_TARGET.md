# RL224 target — Candidate-Coupled Legal-Tail Language / Endpoint Return

Date: 2026-09-01.

Status: successor handover target, **NOT STARTED**.

## Mission

Continue from the frozen RL223 count-only saturation theorem.  Construct one
exact endpoint/tail-numerator state system that preserves the relation between a
current arithmetic candidate and its actual legal continuation constraints.

The required new datum is not another free fixed-count word set.  It is a
certified candidate-to-legal-tail language, recurrence, quotient automaton, or
equivalent endpoint observable.

## Exact interface

For each live phase16 tuple,

`N=A-24`, `M=L-16`,

`T(c)=2^N y0-3^M y16`.

A physical return requires a legal continuation with

`Qtail=T(c)`.

For `gcd(q,6)=1`, this necessary congruence is equivalently

`y_end=y0 (mod q)`.

The target slope on an inherited `k`-lift is

`T(k+1)-T(k)=3*2^34*D`.

## Primary attack

For at least one exact odd modulus or equivalent nonlocal endpoint coordinate:

1. specify the candidate information retained by every state;
2. prove that the transition system is necessary for every physical
   continuation in its declared H21 scope;
3. distinguish the one fixed exact starting `y16` from starts that merely share
   a parity cylinder;
4. compute attainable endpoint/numerator residues independently of `T(c)`;
5. project the finite affine `k`-windows to the same coordinate;
6. intersect exactly and report candidate, prefix, state, eta-class, and rank
   effects;
7. provide a gap-free portable verifier and an explicit survivor whenever an
   empty-intersection claim fails.

An exact finite prefix may be used only if the state system proves how its
endpoint couples to every later legal transition.

## Barred retries and type locks

Do not use as a selector:

- a state space containing only remaining length, remaining odd count, and
  `Q mod q` for `q<=283635` coprime to 6;
- ownership-derived `Qfull mod D^2` or `(Qfull/D) mod D`;
- leading-tail numerator comparisons modulo powers of two;
- finite-blue, pump, bounded-star, S-unit, b38, or raw-depth screens without a
  new proved physical-to-route bridge;
- the formal switchable suffix from RL223 as though it were the actual
  continuation of the exact phase-51 endpoint.

Arithmetic candidates remain necessary supersets; physical H21 incidence and
charge are not inherited facts.

## Success criteria

Produce at least one of:

1. an exact candidate deletion from an independently certified legal endpoint;
2. a strict selector reducing the 139,581,280 family;
3. an empty scoped intersection proving a branch contradiction with minimal
   assumptions;
4. a theorem-quality impossibility or blindness result for a precisely defined
   candidate-coupled language, with the first missing datum identified.

No result from this target exists until a future RL224 start gate is completed.
