# RL203 correction / demotion ledger

Date: 2026-08-31.

No inherited theorem, finite certificate, rank range, eta restriction or
ownership conclusion is demoted.

## Scope locks added by RL203

- `U_a mod 2^35` exposes the parity bit only if `U_a` is determined
  independently; rewriting the endpoint identity does not itself select parity.
- The geometric-prefix calculation proves information equivalence with the
  inherited H21 tail. It must not be described as a new terminal-sign theorem.
- The depth-63 statement applies to necessary sources with canonical phase
  `a<p`; no mirrored `a>p` theorem is promoted.
- "Root anchor invisible through modulus `2^56`" means the named root-prefix and
  root-normalization contributions vanish at that truncation after exact
  height-one normalization. It is not a statement that all global information
  vanishes.
- The exact necessary-rank count remains 16,188,727,234. No new deletion is
  promoted.
- All physical/necessary, H21 charging/ownership, branch, Gate and global locks
  remain binding.
