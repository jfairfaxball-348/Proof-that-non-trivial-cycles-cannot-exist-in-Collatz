# RL343 exact certificate and boundary data

The certified range is **every** `k=2049,2050,...,65536`, endpoint `E=2^60 k-1`, under the inherited `q=0` band and external floor `2^71`. The 63,488 checks have no uncovered member; the maximum deterministic accelerated odd-step escape depth is 403, first at `k=58658`. Ordered-depth SHA-256: `6b865b436278792435dc59fcf4f15975f9e4961f403af18a4cbd2ff12be616ad`. The current portable fast verifier and independent red team execute separate full-range implementations and require these exact values. A cycle rooted at its least state cannot contain any endpoint that descends below the external floor.

For general 75-gap words, the endpoint residue and symbolic profile count are exact as stated in `RL343_PROOF_LEDGER.md`, but neither is an escape certificate for the remaining words. No residual member is declared physically owned or surviving.
