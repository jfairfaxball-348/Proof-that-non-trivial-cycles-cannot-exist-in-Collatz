# RL344 target — eliminate the exact residual canonical CRT return set

Status: UNIQUE LIVE TARGET FOR RL344; prepared, not started. R1 Parent Bridge remains OPEN. Work only after a new kickoff and fresh start gate.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, actual full two-row physical cycle, all inherited ownership/pruning and H-carry qualifications, and the **external** least-state floor `m>=2^71`.

## One exact residual obstruction

Let `O_75` be the set of **exact physically owned complete q=0-to-q=0 returns** on the RL343 closed decorated two-row cycle that (i) are phase-potential nondecreasing, (ii) satisfy the exact predecessor, return, and successor inverse-gap words and their mixed 2/3-adic CRT congruences, and (iii) do not descend below the actual least state `m` at any physical intermediate state. The cycle bridge forces at least one such return if the parent exists; `O_75` is an obstruction definition, **not** an asserted nonempty class or finite certificate.

Partition `O_75` at fixed depth: short returns have fewer than 75 inverse gaps and require exact full-word handling. Long returns have final suffix `v` of 75 gaps, `H(v)=sum(v)>=76`, with at least one gap `>=2` among the final 60 positions. Let `C(v)` be the exact inverse carry satisfying `3^75 E+C(v)=2^H Y`. Then their endpoint `E` must lie in `[2^71,2^76+2^36)` and satisfy

`E ≡ -3^{-75} C(v) (mod 2^H)`.

The band width is `<2^76`, so each **fixed suffix** has at most one possible endpoint. It must still pass exact arbitrary-prefix/CRT compatibility, physical ownership, the phase inequality, and least-state descent. The local grammar has 456,795,521,589,204,614,286,133,576,990,998 symbolic non-tail profiles, not a list of owned endpoints. Row-contact/wrap carriers are included; do not silently restrict to the late row.

## Smallest stage-advancing theorem

Prove `O_75=∅` under the inherited parent assumptions. RL343's full closed-walk bridge then contradicts existence of the parent and closes R1. Seek a structural compression of the suffix residue, prefix CRT lift, ownership, and phase potential rather than raw enumeration of the symbolic grammar. Treat the short class and long class as parts of this **one** quantified obstruction. Preserve the `m>=2^71` qualification and every inherited verifier scope.

If a proof fails, identify the first precise unresolved `O_75` subclass and its exact conditions. Do not promote a local profile as a physical survivor or replace the obstruction with a defect-threshold ladder. Do not work on the separate Lean project. The roadmap stays R1, with R2–R7 OPEN.
