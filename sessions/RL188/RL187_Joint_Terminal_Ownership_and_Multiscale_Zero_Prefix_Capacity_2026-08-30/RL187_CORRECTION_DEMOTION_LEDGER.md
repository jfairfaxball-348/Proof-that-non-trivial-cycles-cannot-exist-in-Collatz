# RL187 correction and demotion ledger

Date: 2026-08-30

## New corrections / demotions

None.

## Frozen scope locks retained

1. The corrected physical defect is `G_i=S_(i+p)-S_p-S_i`.
2. The physical corrected-flow functional uses `2^G`; RL173's `3^-G` quantity remains auxiliary only.
3. Necessary local templates/maps remain one-way filters.
4. RL175 sparse resultant information is not a generic gap obstruction.
5. RL168-RL171 generic rank/lattice/inverse-rank barriers remain in force.
6. RL180-RL187 high-branch statements remain internal to `(37,0,23,-1)`.

## RL187-specific scope locks

- Joint terminal ownership applies only to first defects selected from RL184 clean shallow-start corridors.
- The exact high-tail families are necessary co-ownership possibilities. No enumerated family is promoted as physically realized.
- `N_n` bounds count clean starts, not all cycle phases and not all defects.
- The zero-block span is `tau+1` physical positions; no `tau`-length off-by-one shortcut is used.
- The `tau={35,36,37}` terminal family at `(3^37,21)` is an open survivor. RL187 does not prove it impossible.
- `sum |f_i|>443`, signed masses `>221.5`, and directional K variation `>73.8` are variation statements, not net or prefix K excursion.
- The carry contribution is kept separate from ordinary corridor charging.
- No high branch, preferred branch, Gate A/B, non-trivial-cycle, or Collatz closure claim is promoted.
