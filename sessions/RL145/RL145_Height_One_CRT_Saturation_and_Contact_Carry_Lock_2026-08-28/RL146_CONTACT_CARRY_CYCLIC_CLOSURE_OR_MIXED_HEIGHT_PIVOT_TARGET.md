# RL146 target — contact-carry cyclic closure or mixed-height pivot

Start from the RL145 ordinary-owned carry theorem, not from the retracted
RL143 interval claim.

For a hypothetical primitive height-one owner, use

`c_(t+1)-c_t = Y d_t-X d_(t+1)`,
`sum_t d_t=0`,
`0<|d_t|<L/3`,

together with:

- every adjacent reduced-block contact set differs;
- first changed residue at each interface is `<=22`;
- last changed residue is `>=L-23`;
- the exact cross-interface valuation budget in RL145.4e;
- the local height-one transition rules;
- ordinary affine ownership and the RL20/RL79/RL81 red teams.

Primary target: turn this finite-endpoint / small-carry system into either a
direct contradiction for arbitrary scattered height-one support or a
positive-density physical packing bound strong enough to beat the repaired
height-one coefficient.

Do not spend another session trying to improve `162/13` using only local run
counts, parity splitting, or mechanical run-length restrictions: RL145 gives
an explicit saturation barrier for that information.

If the carry system admits a rigorous non-owning model or otherwise hits a
proved barrier before yielding a quantitative gain, pivot in the same session
to the inherited mixed-height and negative-defect branches. Preserve all
proof-state classifications and verification-economy rules.
