# RL145 correction / demotion ledger

No new claim is demoted in RL145.

Carry forward the RL144 correction unchanged:

## RL143 height-one interval exclusion — DEMOTED / RETRACTED

Do not cite the former exclusion
`303,279,262,681 <= g <= 771,316,334,039`.

The invalid step was the population identification

`C_(2,1)=O_2`.

Correct semantics:

- `O_2` counts all two-odd windows;
- `C_(2,1)=C_2` counts one terminal depth-2 odd-to-even boundary per
  qualifying odd run.

RL144's authoritative replacement remains

`W >= (162/13)(g(2L-A)-3)`.

RL145 additionally proves that local height-one mechanical/run-population
information can saturate this coefficient, so any stronger exclusion requires
a genuinely ordinary-owned global discriminator such as the RL145 contact
carry theorem.
