# RL209 certified facts and proof ledger

Date: 2026-08-31. Canonical current classification record.

## New proved analytic mathematics

**RL209-T1 (above-p cancellation boundary).**  For a canonical tau34 source
`a=p+e>p`, with `R_(p,a)=sum_(p<=j<a)q_j`, the exact endpoint functional is

`E_a=3*2^(u+37)-2^u P_p+(3^p-2^u)R_(p,a)`.

At source height one,
`S_a=u+floor((Ae+1)/L)-1>=u`.  Thus the `2^u P_p` term is not a high-depth
vanishing contribution after normalization.  The corrected below-p 37/60/97
argument cannot be reflected across p; cancellation with the global prefix/tail
data remains essential.

**RL209-T2 (pointwise root cone).**  Exact logarithm bounds prove

`K_0+1/9 < K_L < K_0+1/8`.

For every backward terminal phase `i=L-d` with `1<=d<=2^35`, the path to L is
carry-free and

`K_0+1/9-d/3 < K_i < K_0+1/8+d/3`.

This uses the actual distance d and is strictly sharper than the inherited
common-outer-radius consumer.

## New exact finite certificate

**RL209-CERT1.**  Apply RL209-T2 against the complete RL208 reduced predicate.
An exact pointwise check deletes 983 of the 1,969 current survivors in the old
backward root window.  A 4,096-way refinement of each of the eleven inherited
backward dyadic bands deletes a further 7,740,057.  Exact floor sums and certified
logarithmic crossings prove zero overlap with inherited isolated/anchor cuts.

Total new above-p deletion: **7,741,040**.

The frontier is therefore exactly:

- total necessary terminals: **13,415,865,871**;
- canonical source `a>p`: **7,091,831,284**;
- canonical source `a<p`: **6,324,034,587** (unchanged).

**RL209-CERT2 (scoped barrier).**  On the 986 exact pointwise survivors in the
old backward root window, the inherited signed successor formula was checked for
both signs and all 21 terminal valuations against the independently anchored
next-phase corridor.  All **41,412** cases survive.  This specific consumer
therefore selects no sign or valuation on that set.

Portable verifier:
`python3 verification/verify_rl209_pointwise_root_cone.py`.

## Inherited mathematics and corrections

RL208-T1/CERT1, RL206-T1/T2/T3, all inherited H21 identities, RL202's absolute
root theorem, RL206-C1 and RL206-C2 retain their exact recorded scopes.  The
corrected below-p constants remain 37 / 60 / 97.  Eta classes remain
`0,8,9,17 mod18`.  The RL208 reduced predicate is the exact input to RL209-CERT1.

The complete-word denominator equivalence of RL195 and RL201's real-mass and
coupled-reverse barriers remain binding.  Necessary-rank survival is not physical
H21 occurrence, incidence, or charge.

## Open obligations and barriers

The main unresolved above-p consumer is now the cancellation/global-prefix datum
in RL209-T1: an independent restriction on `P_p`, `R_(p,a)`, or equivalently the
absolute joint prefix moment at the exact physical word.  The canonical wrap of
a lifted p-shift does not create a second below-p tau34 source and supplies no
reflected 37/60/97 theorem.

The 4,096-way count is an exact certified stratification, not a claim that the
pointwise inequality has been exhausted to its theoretical limit.  Purely finer
stratification is lower priority than genuinely independent global information.
RL209-CERT2 also shows that the immediate signed-successor/root-anchor test does
not select the terminal spectrum on its named 986-rank scope.

No independent eta/state/sign/valuation selector is proved.  No physical H21
incidence or charge is proved.  No contradiction of `(37,0,23,-1)` is proved.
Gate A globally open; Gate B globally open; nontrivial-cycle exclusion open.
