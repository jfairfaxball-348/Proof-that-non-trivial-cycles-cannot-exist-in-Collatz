# RL243 — double-deep-counterflow Radius-4 closure target

## Mission

Continue only the active audited Radius-4 global bridge from the exact RL242 survivor.

Assume a retained primitive genuine full-`D` one-excursion/full-phase object. RL242 has already proved:

- full phase iff the canonical first-half drift is zero;
- all half-phase and full-word dyadic carries vanish;
- both physical half-orbits are the canonical integer trajectories;
- every physical cyclic root lies in the canonical full-word dyadic box;
- for each RL240 determinant-one selected rotation flow of signed mass 2, `beta<=1` forces an eligible exact Radius-4 encounter.

Therefore every surviving object must satisfy

`boxed: beta(G)>=2 and beta(Ghat)>=2.`

## Unique target

Prove that the simultaneous double-deep-counterflow regime is impossible under the retained full-phase/owned-prefix structure, or prove that it nevertheless forces an eligible primitive full-`D` exact-distance-4 self-rotation.

Equivalently, prove at least one of

`beta(G)<=1`, `beta(Ghat)<=1`,

or derive a direct contradiction from assuming both are at least 2.

## Preferred attack order

1. Use zero-carry canonical physical trajectories essentially; do not fall back to abstract real/log flows.
2. Couple negative selected-flow valleys to exact physical prefix states and the RL64–RL69 `d,T,H,J` / rank-displacement recurrence.
3. Use `Ghat-G=J_half` and the inherited counterflow lower bound only together with exact ownership/order information.
4. Look for a minimal negative-valley configuration compatible with `beta>=2` on both flows; exhaust structural cases analytically where possible.
5. If an exact distance-4 encounter appears, match every audited RL238 hypothesis line-by-line before applying Radius 4.

## Mandatory red teams

- No bare-word monotone radius-shell principle.
- No pure logarithmic/physical-scale anti-concentration closure.
- No generic packing, product, or continued-fraction closure.
- Do not transplant the RL43 Bezout coefficient into an RL19 step parameter.
- Do not assume blanket half-prefix determinant positivity.
- Preserve the `(65,41)` stress rejection by missing `X-Y` phase.
- Gate A remains separate.
- Radius 5 remains fallback only and is not active.

## Checkpoint classifications

- `R4_BRIDGE_PROVED`
- `R4_BRIDGE_REDUCED`
- `R4_BRIDGE_BARRIER`

Only `R4_BRIDGE_PROVED` permits the global Radius-4 application and Gate-B closure.
