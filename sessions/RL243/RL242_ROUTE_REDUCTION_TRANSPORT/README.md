# RL242 route-reduction transport

Completed generation: **RL242 — Radius-4 bridge zero-carry and low-counterflow reduction**.

Classification: **`R4_BRIDGE_REDUCED`**.

Promoted outcomes:

- all RL241 dyadic half-phase carries are eliminated: retained full phase has `c=0`;
- full phase is exactly zero canonical drift, `Delta_u=0`;
- companion wrap also vanishes and both half-orbits are canonical;
- every physical cyclic root lies in the canonical full-word dyadic box;
- for either selected signed-mass-2 rotation flow, `beta<=1` forces exact eligible Radius 4;
- the unique successor obstruction is simultaneous `beta(G)>=2` and `beta(Ghat)>=2`.

The canonical RL242 ZIP is carried losslessly as base64 text parts in this directory. Verify `PART_SHA256SUMS.txt`, reconstruct with `reconstruct_rl242_bundle.py`, then check the outer authoritative `.zip.sha256` sidecar.

Canonical ZIP SHA256:
`e59bc4f369d5de0d172696fd9d77c2f2ad795bf1b66c92774f7dd7dccd2e95ef`.

Gate A remains separate. Gate B and global non-trivial-cycle exclusion remain open. Radius 5 remains inactive. Knowledge catalogues stale/deferred.
