# RL213 exact global-consumer barrier certificate

Date: 2026-09-01. Classification: **proved analytic mathematics / exact finite certificate / scoped method barriers** as separated below.

All physical conclusions remain conditional on the inherited sole high branch `(37,0,23,-1)` and a physical H21 realization. Necessary ranks and abstract exponent prefixes are not physical populations.

## 1. e=16 absolute endpoint moment collapse

At `a=p+16`, RL210 gives `h_t=h_(p+t)` for `0<=t<=16` with no p-shift carry, hence

`q_(p+t)=q_t/alpha`, `rho_(p+16)=rho_16/alpha`,

and therefore

`P_(p+16)=P_p+P_16/alpha`.

Insert these identities into the inherited RL201 endpoint moment

`3(alpha-1)rho_a(2^33 eta-1/2)=3K0-alpha P_p+(alpha-1)P_a`

and multiply by `alpha`:

`3(alpha-1)rho_16 x_a = 3alpha K0-alpha P_p+(alpha-1)P_16`,

where `x_a=2^33 eta-1/2`.

The matched e=16 p-gap gives `rho_16 x_a=rho_16 x_16+K0`, while the root z-telescope gives

`P_16=3(rho_16 x_16-y_0)`.

After cancellation, the endpoint moment is exactly equivalent to

`alpha P_p=3(K0-(alpha-1)y_0)`.

But the inherited root relations `rho_p=1/alpha`, `y_p-y_0=K0` and
`P_p=3(z_p-z_0)` give precisely the same identity:

`alpha P_p=3(y_p-alpha y_0)=3(K0-(alpha-1)y_0)`.

Thus the existing RL201 absolute endpoint moment is **not an independent e=16 consumer** once the inherited matched-prefix, local p-gap, and root telescope identities are imposed. It cannot by itself refine eta modulo 2187 or delete the e=16 rank. This is a scoped algebraic collapse, not a no-go for every complete-word invariant or for a newly supplied restriction on `P_p`.

## 2. Exact G56 first-divergence saturation

RL210's large-offset necessary system has first mismatch `24<=m<=37`, with the smaller exponent equal to 37. At mismatch m the other exponent can be any integer

`k in [38,b_m]`,

and either orientation is abstractly possible before the inherited m=24 orientation check.

There are exactly **304** such `(m,k,orientation)` start cases. The portable verifier works modulo `2^56`, factors the G56 congruence by `2^37`, and exhaustively continues the two strictly increasing exponent sequences through phase 55 subject to `S_t,S'_t<=b_t`.

Result:

- the single infeasible start case is the already inherited RL210 obstruction `m=24`, `k=38`, `S_24=37<S'_24=38` (root-smaller orientation);
- **all other 303 start cases have an explicit valid 56-bit continuation satisfying G56**;
- feasible/total counts by m are `1/2, 4/4, 8/8, 10/10, 14/14, 16/16, 20/20, 24/24, 26/26, 30/30, 32/32, 36/36, 40/40, 42/42` for `m=24,...,37`.

The canonical witness digest is

`dd7dc63df94e0360f556fa255fea04e75463f8502f57588e051b49b081be2c5a`.

This proves a scoped method boundary: **G56 plus strict exponent increase and the mechanical upper bounds alone yields no further first-divergence start-case deletion beyond RL210's m=24 orientation obstruction.** The 303 witnesses are abstract prefix continuations, not physical cycles or full-word realizations.

## 3. Frontier and scope

The e=16 terminal rank remains `34,124,151,203`. No state, mod18 class, terminal parity, valuation, physical H21 incidence, H21 charge, branch contradiction, Gate closure, or global nontrivial-cycle exclusion is proved.

The global necessary frontier remains **13,415,865,871**:
- above-p source: **7,091,831,284**;
- below-p source: **6,324,034,587**.

The next useful consumer must introduce information not algebraically contained in the endpoint moment/root telescope and not merely repeat G56. A discrete full-word ownership quotient/residue is the preferred next target; independently anchored signed successor-K remains a fallback only where a genuine global anchor is proved.
