# RL296 target-specific red team

Date: 2026-09-11

Result:

`RL296_RED_TEAM_PASS_FOR_PROMOTED_SCOPE`

This red team applies only to the finite frontier reconstruction and exact Bellman/owner certificates promoted by RL296.

## Coverage and gap-freedom

- Every finite prefix cut used as complete is checked for prefix-freeness and exact Kraft sum 1 where represented as a binary prefix code.
- The `(6,807)` Q17 decomposition is checked as the sixteen first-deviation prefixes plus the exact distinguished spine.
- The `(5,206)` verifier checks that 27 ordinary owner leaves, two analytic sinks, six late-wall owner leaves, and three explicit residual leaves form one complete prefix partition.
- No capped exploratory tree is used as complete coverage.

PASS.

## Forward reachability and owner legitimacy

Every finite P-owner claim is replayed forward from `P=(2,3)` using the canonical `(d,J,K)` recurrence. No locally legal but globally unreachable predecessor state is accepted as an owner.

PASS.

## Exact same-state splice

Every owner splice checks equality of the full physical `(d,J)` endpoint, not only K residue, valuation, or a quotient label.

This neutralises physical-merger/equal-area ambiguity and avoids chosen-history seam charges.

PASS.

## Area accounting

All finite comparisons use the exact step cost `d-1` accumulated after each legal edge. No fictitious `-1` zero-depth wall cost is introduced.

The RL296 Bellman offsets are derived from exact source costs. The corrected `(5,206)` allowance is +16.

PASS.

## Zero-depth / boundary legality

Portable verifiers reject `x=1` at `d<=1`; no formal wall identity is used as a legal physical state.

PASS.

## Infinite-tail scope

RL296 does not infer an infinite theorem from finite search. The only all-depth tail used in the `(6,807)` closure is RL295's already-promoted Q17 theorem. The `(6,736)` all-zero remainder uses exact same-state ownership of `R_11`.

PASS.

## Mandatory inherited attacks

The following RL295 red-team families do not invalidate the promoted finite certificates:

- arbitrary zero-height boundary retention/trivial cycle: excluded by legal forward replay;
- unbounded static pullbacks: not used to infer coverage;
- cylinder isometry/finite-suffix transparency: same-state splicing is explicit;
- neutral `(101)` gauge: no gauge-only endpoint identification;
- physical mergers/equal-area diamonds: full state equality required;
- adjacent-swap valuation jumps: no swap invariance assumed;
- homogeneous Rank-1 danger towers: finite cuts plus inherited analytic sinks cover the claimed scopes;
- shifted phase-reset families: no finite-depth extrapolation;
- RL290 recursive Bellman chains: exact local inequalities retain all residual branches;
- locally legal but globally unreachable predecessor families: owners are forward P paths;
- RL293 immediate-danger corridor exclusions: not used as universal closure;
- H<=60 owner exceptions: no blanket H<=60 theorem is substituted for explicit owner words;
- principal-cylinder scope: no principal-cylinder claim is globalised;
- formal identity/nonzero wall debt accounting: no unpriced formal identity edge is charged as physical history.

PASS for promoted scope.

## Adversarial non-claims

Red team explicitly confirms RL296 does **not** prove:

- `Bcal(5,206)<=Bcal(P)+16`;
- `Bcal(6,699)<=Bcal(P)+26`;
- `Bcal(4,39)<=Bcal(P)+2`;
- `Bcal(P)<=1`;
- Gate A.

The three `(5,206)` late walls and `(6,699)` remain live.

PASS.
