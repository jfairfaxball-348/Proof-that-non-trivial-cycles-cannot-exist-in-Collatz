# RL175 session state and RL176 kickoff

RL175 is complete.

Promoted results:

- exact corrected endpoint/arc-mass decomposition of `F2`;
- `h_p=0` makes `y_p` the second-smallest physical state;
- zero- and one-positive-height profiles are excluded;
- physical `h_p=0` profiles lose more than `5/8` of mechanical mass;
- internal half-barrier: `F2<1/2-Delta/16`;
- conditional `h_p>=1` lower flow: `F2>6,365,000,000(2^H-1)` under inherited `m>=2^71`.

No global cycle exclusion is obtained.

RL176 should attack the integer endpoint gap `g_p=y_p-y_0` in the `h_p=0` branch, seeking a genuinely independent congruence/divisibility/multi-support restriction that can be combined with `F2=3(lambda-1)g_p` and the new half-barrier. Do not return to RL173's demoted coordinate, fixed-radius stacking, unordered valuation statistics, or same-root elimination without an additional independent inequality.
