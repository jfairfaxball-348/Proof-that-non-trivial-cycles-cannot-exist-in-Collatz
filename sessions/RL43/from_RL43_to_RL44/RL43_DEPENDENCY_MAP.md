# RL43 dependency map for audit

Date: 2026-08-22

## Inherited package

The complete RL42->RL43 handover is retained intact under `inherited/`. Use its own dependency map and proof ledger for earlier stages.

## RL43 rho=49 elimination depends on

- inherited near-resonant order-2 / `g=2` branch;
- inherited `G=4` consequence at `rho=49`;
- inherited effective-mass lower bound `M_eff >= 12(z+1)/z^2`;
- inherited excursion decomposition `rho=P+E`;
- inherited first physical gap 9 for `G=4`;
- inherited terminal gap form `-4*2^t`;
- RL43 excess/gap lemma and exact low-excess crossing catalogue.

## RL43 defect-support bridge depends on

- inherited half-word `Q` convention and `(X+Y)G=U-V`;
- excursion decomposition;
- RL43 combinatorial inequality `z<=e+1`;
- exact run compression of `Q`.

## RL43 gap-9 theorem depends on

- inherited one-excursion `G=4` gap evolution `4->6->9`;
- exact normalized `T` transition;
- fixed-excess finite quotient after neutral-pump identification.

## RL43 full-denominator phase bridge depends on

- inherited RL21 two-factor decomposition / full `D` divisibility;
- inherited `gcd(a,ell)=1` in the retained branch;
- Bezout phase conversion modulo `X-Y`;
- RL43 run/defect support bound;
- positivity of determinant exponents from the inherited prefix cap;
- irreducibility/nonvanishing argument for `3T^q-2`;
- inherited RL19 radius-3 use of the same short binomial as a comparison target, not as an automatically transplanted theorem.

## Candidate `J/K` invariant depends on

Only the cutoff-free automaton state laws plus the one-excursion terminal geometry. It is not yet a theorem and must not be used as an inherited premise.
