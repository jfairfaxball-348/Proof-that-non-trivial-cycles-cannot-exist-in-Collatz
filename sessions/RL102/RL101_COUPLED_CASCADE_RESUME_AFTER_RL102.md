# RL101 — exact coupled-cascade resume point after RL102

Date: 2026-08-25

## Status

The numerical cascade remains **paused, not abandoned**.

RL101 did not run any new cascade scan. RL102 is assigned to the higher-leverage full-ownership/cylinder-content theorem first.

This file preserves the exact repaired restart state unchanged from RL100.

## 1. Frozen branch scope

The cascade applies only to the

**exact first-Farey reduced slope, raw `g=1`, full-phase Gate-A branch**.

RL101's new multiplicity theorems do not lift these interval exclusions to `g>=2` or global Gate A.

## 2. Exact tier state

- wide `(D_w,R_w,L_w)=(5,000,030,7,000,000,5,000,053)`;
- middle `(D_m,R_m,L_m)=(7,500,031,4,500,000,7,500,053)`;
- deep `(D_d,R_d,L_d)=(10,000,032,3,250,000,10,000,053)`;
- ultra `(D_u,R_u,L_u)=(15,000,035,2,005,000,15,000,053)`.

Mandatory repaired middle value:

`D_m=7,500,031`.

It was forced in RL100 by the exact new balanced-residue minimum

`7,500,033 at r=4,478,962`.

Other retained minima:

- deep `10,000,034 at r=378,722`;
- ultra `15,000,037 at r=575,974`, with RL100 tie at `r=1,919,735`.

## 3. Final geometry

Corners:

`A=(0,7,000,001)`

`B=(5,000,031,4,500,001)`

`C=(7,500,032,3,250,001)`

`D=(10,000,033,2,005,001)`

`E=(15,000,036,0)`.

Feasible exact nonnegative supports:

- `A-C`;
- `C-D`;
- `D-E`.

Active terminal support:

`lambda=234375/3281264468752`

`mu=1/7000001`.

`floor(lambda*C)=3010>2`.

Common successor charge:

`L_common=15,000,053`.

## 4. Live terminal

First live odd:

`k=2,921,813,805`.

Exact margin:

`-3,297`.

Previous odd:

`2,921,813,803`, margin `+10,323`.

Branch upper endpoint:

`42,150,931,559`, odd.

## 5. Completed scan coverage

Do not rescan unless a verifier/provenance problem requires it:

- ultra through `r=2,005,000` modulo `2^15,000,056`;
- middle through `r=4,500,000` modulo `2^7,500,056`;
- deep through `r=3,250,000` modulo `2^10,000,056`.

No gap may be skipped on resumption.

## 6. Resumption procedure

When deliberately resuming:

1. choose the next deep radius checkpoint;
2. solve exact staircase support thresholds using repaired `D_m=7,500,031`;
3. extend every required 2-adic band gap-free;
4. compare every completed band minimum with the live global minimum immediately;
5. repair any tier depth before geometry promotion if a new minimum appears;
6. rebuild all corners from actual depths/radii;
7. enumerate every feasible pair support exactly;
8. verify the terminal-direction optimal edge;
9. verify `floor(lambda*C)>2` or redo block-count optimization;
10. propagate only the consecutive odd interval whose exact margin is positive.

## 7. Strategic warning

The cascade remains locally productive but globally expensive.

Do not resume long blind scans merely because RL102 encounters one failed lemma. Resume only after the ownership/content route is genuinely frozen or a new theorem materially improves scan economics.
