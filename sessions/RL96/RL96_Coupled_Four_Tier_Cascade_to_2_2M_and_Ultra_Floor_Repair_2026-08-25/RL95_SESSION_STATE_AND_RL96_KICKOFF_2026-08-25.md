# RL95 SESSION STATE AND RL96 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Incoming RL95 gate

- outer RL95 SHA-256: PASS;
- fresh internal RL95 manifest: PASS;
- supplied RL95 fresh-unpack verification: PASS;
- RL94 fast verifier suite: PASS;
- GitHub incoming provenance commit: `5d4bf0125bd31c21191d1a09dd93f9ba395d193b`.

Verification economy applied. No recursive historical expensive verification was performed.

## Frozen RL95 exact tiers

### Wide inherited tier

`d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`.

### Middle RL95 tier

New exact scan `r=3,675,001..3,800,000` has minimum

`7,500,039 at r=3,785,526`.

The inherited combined minimum remains

`7,500,034 at r=1,635,300`.

Effective tier:

`d<=7,500,032`, `r<=3,800,000` => successor `<=7,500,053`.

### Deep RL95 tier

New exact scan `r=2,000,001..2,200,000` has minimum

`10,000,040 at r=2,090,420`.

The inherited combined minimum remains

`10,000,034 at r=378,722`.

Effective tier:

`d<=10,000,032`, `r<=2,200,000` => successor `<=10,000,053`.

### Ultra RL95 tier — repaired floor

New exact gap-free scan `r=335,001..605,000` has a new global minimum

`15,000,037 at r=575,974`.

Therefore the extended safe depth is repaired to

`D_u=15,000,035`.

Effective tier:

`d<=15,000,035`, `r<=605,000` => successor `<=15,000,053`.

The old `D_u=15,000,038` remains valid on its frozen RL94 domain but is not valid as the full-radius RL95 tier.

## Frozen final four-tier support

`mu=1/7,000,001`

`lambda=1,600,000/17,500,079,500,011`

Active corners:

`A=(0,7,000,001)`

and

`C=(7,500,033,2,200,001)`.

Other final corners are

`B=(5,000,031,3,800,001)`

`D=(10,000,033,605,001)`

`E=(15,000,036,0)`

and all lie on or above the support line, with `B,D,E` strictly above.

Exact feasible pair supports are

`A-C`, `C-D`, `D-E`.

`A-C` is optimal for every aggregate direction tested over the promoted RL95 interval and at the first failure.

`floor(lambda*C)=3,853`, so `b=k-1` remains the worst block-count stratum.

## Ultra-depth robustness reserve

At the final `A-C` support, corner `E` remains valid whenever

`D_u>=10,937,549`.

Current corrected ultra depth:

`D_u=15,000,035`.

Exact reserve:

`4,062,486`.

Thus small future ultra-floor losses must be applied but do not immediately threaten the active `A-C` geometry.

## Conservative successor charge

Retain

`L_common=15,000,053`.

No tier-specific population theorem was proved in RL95.

## Frozen RL95 interval result

Every odd

`2,921,758,705 <= k <= 2,921,766,549`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `3,923` additional consecutive odd values.

At `k=2,921,766,549`:

- margin `+2,508`.

At

`k6=2,921,766,551`:

- margin `-12,798`.

Updated branch-specific surviving odd window:

`2,921,766,551 <= k <= 42,150,931,559`, `k` odd.

## Frozen geometry/coupling state

At `R_d=2,200,000`, keeping the current `A-C` support requires

- `R_m >= 3,799,995`; actual `3,800,000`;
- `R_u >= 600,008`; actual `605,000`.

The middle-radius slack is only `5`.

For the next 25,000-step deep move to `R_d=2,225,000`, unchanged middle/deep depths and the current active support require

- `R_m >= 3,816,661`;
- `R_u >= 633,341`.

Thus one middle 25,000-step chunk to `3,825,000` and ultra replenishment to at least `635,000` are geometrically sufficient if their bit-length floors hold.

The corrected ultra depth-axis remains nonbinding through deep radius floor

`3,499,992`.

## Authoritative RL96 live obligation

Attack `k6=2,921,766,551` using the corrected coupled four-tier cascade.

Priority:

1. extend ultra from `605,001` to at least `635,000`;
2. recompute the ultra global minimum immediately and adjust `D_u=B_u-2` if it falls;
3. extend middle to at least `3,825,000`;
4. extend deep to `2,225,000`;
5. rebuild the exact corrected staircase;
6. enumerate all feasible support edges and re-optimize;
7. verify `floor(lambda*C)>2` or redo the block-count optimization;
8. retain `L_common=15,000,053` unless rigorously improved;
9. propagate the exact odd interval immediately;
10. continue through adjacent batches rather than stopping at the first restored endpoint.

## Sustained attack rule

A theorem, endpoint elimination, floor repair, or improved bound is a checkpoint rather than a close trigger. Continue through coupled frontier propagation, support-edge changes, floor changes, constant sharpening, adjacent regimes, and red-team while the route remains productive and reliability is not threatened by compute/context pressure.

## Verification economy rule

After the current bundle checksum, internal manifest, and fast verifier suite pass, accept this frozen proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize new mathematics on the stated live obstruction.
