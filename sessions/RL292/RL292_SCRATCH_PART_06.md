More generally, let `f_1,...,f_m` be any finite collection of nonzero affine rational/integer forms in `n`. Choose `L` so no `f_i(-2^L)` vanishes. As `R->infinity`, `n_(R,q)` converges 2-adically to `-2^L`, so for all sufficiently large `R`, every `nu_2(f_i(n_(R,q)))` stabilizes at the finite value `nu_2(f_i(-2^L))`, while `Beta(n_(R,q))>=R` diverges.

The parameter `q` may simultaneously be chosen in an odd residue class modulo 3 so that the associated positive odd boundary state `J=2n+1` lies in the promoted reachable mod-3 classes `{0,2}`. Thus the obstruction persists even after imposing that necessary local reachability filter.

Classification candidate:

`STATIC_BOUNDARY_HAZARD_PREIMAGE_TREE_AND_FINITE_AFFINE_TEMPLATE_BARRIER_PROVED`.

This decisively rules out closing the transformed-D resonance programme by taking a finite maximum of local affine valuations. The surviving object must retain the full weighted preimage tree (or an equivalent infinite-profile quotient) and couple its deep balls to paid fixed-seed / ballot ancestry.

### Regression

Local verifier:

`/mnt/data/rl292_scratch/verify_rl292_beta_static_tree.py`

Frozen output:

`/mnt/data/rl292_scratch/RL292_BETA_STATIC_TREE_OUTPUT.txt`

PASS:
- 1,050 direct truncated-Beta versus static-tree identities;
- explicit all-zero pullback family `L=4`, `R=5..24`;
- wall four-form valuations fixed at `(1,2,0,0)` while truncated Beta equals `R`.

### Red-team correction

The observed finite fact that the positive checkpoint closure from `8` reproduces the full low-height checkpoint set does NOT imply that every genuine terminal path passes through checkpoint `8`. Exact fixed-seed search avoiding `(1,8)` finds, for example, `2^5=32` at height `13` without visiting `8` (and further powers as well). Checkpoint `8` remains the tight Bellman branch, not a universal mandatory gateway.

Gate A remains open. Local unpromoted finite residual remains odd `k>=29`, `H_can<k`.
