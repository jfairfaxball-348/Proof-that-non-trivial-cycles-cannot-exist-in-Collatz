# RL194 target — sign-weight ordering and owned prefix congruence

Date: 2026-08-31

## Frozen incoming state

On the sole high branch `(v,H,J,d)=(37,0,23,-1)`, RL193 proves:

- exact full-period tail numerator `(2^A-3^L)/2^21` on both atoms;
- termwise physical K telescoping and automatic total valuation -21;
- `sum epsilon=1`, with carry overpayment and strictly negative ordinary
  weighted net;
- ordinary cancellation exceeding `72912057143/733007751850` on the
  `2^38` atom and `91625968981/733007751850` on the `2^37` atom;
- 24 deleted E ranks and 14 deleted H21 ranks, listed in the main report;
- canonical terminal floors 71 / 67 and carry buffers 11/5 / 4/7;
- strictly positive canonical K-prefix flow on the lower/`2^38` atom and
  strictly negative prefix flow on the upper/`2^37` atom.

The definitions and constants are in Section 0 of the main RL193 report.
The total weighted-debt and total-valuation identities alone are method
barriers; neither atom has been excluded or realized.

## Primary route — physical sign-weight ordering

Retain the exact ordinary formula `epsilon_i=2^(-h_(i+p))-2^(-h_i)` and the
one carry formula `epsilon_z=2-2^(-h_z)`.  Seek a proved correlation between
their signs, the rho weights, and their chronological locations that can
consume the atom-specific prefix signs or the ordinary cancellation floors.
Useful outcomes include an actual partial-K displacement inequality,
an ordering/cancellation theorem, or a precisely scoped obstruction/barrier.

Do not infer excursion from total variation or from the sign of a total net.
The known early signature is anchored at canonical phase zero, not freely
relabelled.  Handle quasi-periodic K/rho lifts explicitly.

## Alternative route — owned local congruence

The total congruence `2^21 D == -3^L (mod 2^m)` is an endpoint identity.
Investigate a prefix congruence only when it is coupled to exact owned pair
numerators, height transitions and locations.  Any finite state propagation
must cover its stated range completely and remain a necessary-state
certificate unless physical realization is separately proved.

## Conditional H21 route

Use `D_H21=[23369453298,41775866136]` minus the 14 certified deletions only
with a concrete physical incidence, sign, recurrence or multiplicity theorem.
Rank-cardinality changes alone do not release the `{33,34,35}` binding budget.

## Preserved scope

Spacing at least 1001, `N35<=7559400754`, ordinary absolute flow `>480` and
directional K variation `>80` remain inherited.  No branch, Gate A, Gate B,
non-trivial-cycle or global Collatz conclusion may be claimed without the
complete missing physical proof.  Preserve corrections, red teams and
verification economy.  Close RL194 as its own atomic transition.
