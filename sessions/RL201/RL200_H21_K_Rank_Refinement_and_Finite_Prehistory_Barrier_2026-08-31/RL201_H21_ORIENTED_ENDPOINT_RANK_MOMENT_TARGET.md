# RL201 target — H21 oriented endpoint rank moment

Date: 2026-08-31.

Accept RL200's refined H21 terminal core

`D_K=[25583192106,41775866136]`

with 12 retained isolated deletions and `16192674019` remaining necessary ranks.

Accept RL199's oriented endpoint interface

`Y_0^-=2^34 eta-1`,
`Y_0^+=2^34(eta+21)-1`,
`eta=0,8,9,17 mod18`

with state determined modulo 9 and terminal sign determined by parity.

## Primary target

Couple the **absolute** lower endpoint lift eta to the global canonical phase/rank strongly enough
to remove at least one eta class, decide `e35`, or decide terminal sign.

Preferred consumers:

1. complete p-shift-chain reconstruction / exact fixed-`K0` moment locating the normalized lower
   endpoint at the `tau=34` rank;
2. a coupled reverse defect/numerator prehistory in which the two endpoints cannot choose reverse
   exponents independently;
3. exact rank-resolved successor-K information capable of consuming
   `±rho(2^nu-1)/(3*2^21)`;
4. another phase/ownership identity that ties absolute endpoint residue to canonical rank.

If class selection still fails, prove a named coupled consumer insufficient and isolate the
minimal missing global datum.

## Guardrails

RL200 already proves that finite independent reverse integrality/unit/nonnegative-height lifting,
even with actual prescribed mechanical bits, is class-blind.  Do not repeat that route.
Terminal K itself is eta-blind.  Do not infer physical populations from necessary ranks, revive the
H21 zero-edge route, substitute N0/J00 abundance, or turn aggregate K variation into excursion.
All finite-cutoff and Gate/global guardrails remain binding.
