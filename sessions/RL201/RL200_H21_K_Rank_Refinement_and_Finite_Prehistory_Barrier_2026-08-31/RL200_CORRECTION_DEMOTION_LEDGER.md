# RL200 Correction / Demotion Ledger

Date: 2026-08-31.

No inherited theorem or exact finite certificate is demoted.

Scope locks:

- The refined core `[25583192106,41775866136]` is a necessary terminal-rank set, not a set of
  physically realized H21 terminals.
- The count `2,213,738,806` is the number of previously surviving necessary ranks removed by the
  new K-corridor constraint.  It is not a physical population count.
- The terminal K refinement is deliberately orientation-blind: `K_H21(r)` depends on terminal
  rank and the common normalized gap, not on `eta mod 18`, `nu`, or terminal sign.
- The reverse-prehistory insufficiency theorem is restricted to **uncoupled** finite consumers
  that impose prescribed local mechanical bits plus independent odd-integrality/unit and
  nonnegative-height constraints on each endpoint.
- That barrier does not cover a coupled pair numerator/defect word, complete-word reconstruction,
  exact fixed-`K0` moment, p-chain absolute position, prescribed global height word, or an exact
  rank-resolved K-successor sign constraint.
- Arithmetic survival of a local reverse extension is not physical cycle realization.
- RL199's four classes remain necessary classes only; none is promoted as physically realized.
- All inherited correction/demotion history and finite-cutoff, rank/time, physical/necessary, and
  variation/excursion guardrails remain binding.
