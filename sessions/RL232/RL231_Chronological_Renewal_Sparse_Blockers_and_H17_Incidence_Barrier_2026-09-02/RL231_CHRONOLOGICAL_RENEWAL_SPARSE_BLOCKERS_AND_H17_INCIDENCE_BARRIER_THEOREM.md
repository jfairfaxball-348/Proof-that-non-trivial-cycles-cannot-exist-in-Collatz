# RL231 — Chronological Renewal, Sparse Blockers, and H17 Incidence Barrier

Date: 2026-09-02

## 0. Outcome and scope

RL231 continues the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` under the inherited corridor

`128,081,997,553 < K_i < 146,795,909,391`

and ordinary drift

`K_(i+1)-K_i=f_i/3`.

The session achieves **Success B**: exact chronological recurrence restrictions on owned zero-prefix terminal families are converted into a quantitatively stronger branch constraint,

`ordinary absolute corrected flow > 234,578,279,671 / 352,321,536 > 665`.

It also reaches a decisive stopping barrier: after resolving H17 exactly, any further improvement of the same uniform early-charge mechanism requires the **combined** incidence of two H17 families to be at most 1,615 over the whole period. A spacing-only route would require combined-event spacing at least 85,103,989 phases, far beyond the scale of the successful finite recurrence scans.

This is not a branch contradiction. No necessary terminal rank is deleted. Gate A, Gate B and global exclusion remain open.

## 1. Fixed-origin sign-order saturation barrier

The inherited corridor is symmetric around

`K_0=2^37=137,438,953,472`:

`K_0-128,081,997,553 = 146,795,909,391-K_0 = 9,356,955,919`.

Thus a fixed-origin corrected-flow prefix needs more than

`3*9,356,955,919 = 28,070,867,757 = W_UPPER+2`

units of one-sided corrected flow to reach either strict corridor boundary.

Using the exact RL181 rational logarithm/exponential machinery, RL231 certifies

- `N^- < W_UPPER + 1/6`,
- `F2 < 3/8`,
- `N^+ < W_UPPER + 13/24`,

with `W_UPPER=28,070,867,755`.

Therefore even perfect sign ordering of the inherited corrected-flow mass cannot force fixed-origin corridor escape. Pure sign reordering is not the missing RL231 object.

Classification: analytic consequence + exact rational interval certificate; method barrier.

## 2. Boundary-sensitive H21 consumer

For p-shift residue order `i_r=pr mod L`, exact algebra from `Ap-uL=1` gives

`omega_r=rho_(i_r)=exp(-r s-q Delta)`, `q=floor(pr/L)`.

The dangerous H21 `{33,34,35}` family has

`(T,H)=(350,220,815,692,997,949,21)`

and full-prefix physical terminal core

`D=[23,369,453,298,41,775,866,136]`.

At the worst endpoint `r=41,775,866,136`, exact rational exponential bounds prove

`K = (T/2^21) omega_r > 2^37-2^31 = 135,291,469,824`.

Because its zero-defect prefix has zero corrected flow, this is also a block-entry restriction. Thus this binding family is impossible on any owned block entering with `K<=135,291,469,824`.

A related exact calculation places the H20 `{31,...,36}` family above `K>130,459,631,616` throughout its terminal core.

Classification: analytic boundary-state theorem + exact rational interval certificate.

## 3. Exact terminal families and chronological recurrence

RL231 reconstructs the inherited RL187 joint-ownership rows and applies the full-prefix physical-gap test. The relevant exact families are:

| family | `(T,H)` | full-prefix terminal core | recurrence result |
| --- | --- | --- | --- |
| H21 `{33,34,35}` | `(350220815692997949,21)` | `[23369453298,41775866136]` | spacing `>=1001` |
| H21 `{34,35,36,37}` | `(450283905890997363,21)` | `[72797034370,103818202602]` | inherited RL191 spacing `>=1001` |
| H20 `{31,...,36}` | `(150094635296999121,20)` | `[0,18406412838]` | spacing `>=1001` |
| H19 `{30,...,34}` | `(83385908498332845,19)` | `[15717738363,46738906595]` | spacing `>=1001` |
| H19 `{31,...,36}` | `(150094635296999121,19)` | `[129876330377,137528045311]` | spacing `>=1001` |
| H18 `{28,...,35}` | `(50031545098999707,18)` | `[49427581072,52116255547]` | spacing `>=3032` |

The recurrence certificates use the inherited universal carry-completed transport

`2^c Delta_(i+1)=3 Delta_i+epsilon_i`, `|epsilon_i|<2`

with exact mechanical-word atoms. The new finite scans are gap-free over:

- H21 `{33,34,35}`: separations 36..1000, 8,610 atoms, 258 nonempty separations, minimum safety margin `>10,499,297`;
- H20 `{31,...,36}`: 37..1000, 8,609 atoms, 257 nonempty separations, margin `>5,999,587`;
- H19 `{30,...,34}`: 35..1000, 24,178 atoms, 436 nonempty separations, margin `>7,499,467`;
- H19 `{31,...,36}`: 37..1000, 1,556 atoms, 108 nonempty separations, margin `>11,999,231`;
- H18 `{28,...,35}`: 36..3031, 1,829 atoms, 118 nonempty separations, margin `>11,999,245`.

The H21 `{34,35,36,37}` spacing theorem is inherited from RL191 and replayed over its promoted 46..1000 scan.

Classification: exact finite certificates supporting chronological non-recurrence theorems.

## 4. Sparse-family corrected charging

Let `U=2^-25`. RL231 uses the nominal charge schedule

- `tau<=34`: `(128/7)U = 1/(7*2^18)`;
- `tau=35,36`: `(4/3)U = 1/(3*2^23)`;
- `tau>=37`: `U`.

The inherited RL191 populations are

- clean `tau<=34`: at least `2,515,773,745`;
- tau35 boundary population: `321,082,580`;
- tau36 population: `185,597,902`;
- `N37=7,052,720,272`.

The nominal schedule is safe for conservative heights `H<=16`. H22–H24 are also safe. H18–H21 exceed their local budgets, but only on the exact terminal families listed above. Subtracting each family’s exact excess at its worst recurrence-count cap is therefore a valid pessimistic correction.

Occurrence caps:

- every 1001-spaced family: at most `floor(L/1001)=137,390,654`;
- H18: at most `floor(L/3032)=45,358,853`.

After the exact sparse-family correction,

`ordinary absolute corrected flow > 234,578,279,671 / 352,321,536 > 665`.

The inherited signed-total/carry conversion yields

`each signed corrected-flow mass > 234,578,279,671 / 704,643,072`

and

`each directional K variation > 234,578,279,671 / 2,113,929,216 > 110`.

These are total-variation statements, not a prefix-excursion theorem.

Classification: analytic charging lemma + exact finite recurrence/population certificates.

## 5. Exact H17 refinement

The former conservative H17 envelope allowed eight offsets `{26,...,33}` simultaneously. Exact ownership enumeration over 3,400 admissible H17 rows and 1,914 terminal invariants proves that no such eight-offset terminal exists.

There are exactly two maximal H17 joint families:

1. H17-A `{26,27,28,29,30,31,32}`:
   - `(T,H)=(20,383,222,077,370,251,17)=(11*3^32,17)`;
   - full-prefix core `[5,377,348,952,28,746,802,249]`.

2. H17-B `{27,28,29,30,31,32,33}`:
   - `(T,H)=(27,795,302,832,777,615,17)=(5*3^33,17)`;
   - full-prefix core `[72,797,034,370,103,818,202,602]`.

Thus the H17 budget is `7x<=128` in U-units, giving the exact present ceiling `x=128/7`.

Classification: exact finite ownership theorem.

## 6. Decisive next barrier

After all already certified sparse-family penalties, increasing the common early charge by `d U` above `128/7 U` produces pre-H17 net slope only

`11,310 d U`.

Each occurrence of either exact H17 family then costs `7dU`. If `N17` denotes their combined period incidence, further gain requires

`11,310-7N17>0`,

hence

`N17<=1,615`.

For a hypothetical theorem that directly spaces the combined H17 event sequence, spacing alone would have to be at least

`floor(L/1616)+1 = 85,103,989`

to force that incidence cap.

Therefore another modest extension of the current recurrence scans is not the missing theorem. The successor must control **combined H17 incidence**, cross-family return, or an equivalent stronger state variable.

## 7. Preserved open state

Unchanged:

- necessary terminal-rank frontier: `13,415,865,870`;
- e=4 terminal rank: `31,435,476,727`, still live;
- e=4 combined arithmetic survivors through transition 43: `3,856,660,232`;
- sole high branch remains open;
- Gate A remains open;
- Gate B remains open;
- global non-trivial-cycle exclusion remains open.

No isolated necessary rank is promoted as physically realized or globally excluded.
