# RL223 target — Full-Tail Return-Defect / Odd-Modulus Endpoint Coupling

Date: 2026-09-01.

## Mission

Continue from the frozen RL222 structural-blindness result.  Obtain one genuinely
independent candidate-wise return predicate from the remaining legal full-word tail.

Do not retry:

- ownership-derived `Qfull mod D^2` or `(Qfull/D) mod D`;
- leading-tail numerator comparisons modulo powers of two;
- RL219/RL220 finite-blue, pump, bounded-star, S-unit, b38, or raw-depth screens.

## Exact candidate target

At phase16, for each live tuple,

`N=A-24`,
`M=L-16`,
`T(c)=2^N y0 - 3^M y16`.

A physical full return requires the independently generated remaining-tail numerator
`Qtail(w)` to satisfy `Qtail(w)=T(c)`.

RL222 proves that `Qtail(w)=T(c) mod 2^m` is automatic for any locally generated
leading prefix and therefore cannot be used as the discriminator.

## Primary attack

Construct an exact finite-state or algebraic description of **attainable endpoint /
tail-numerator residues** for legal H21 continuations using at least one modulus
whose information is not swallowed by the binary-denominator identity.  Prefer an
odd modulus `q` with `gcd(q,6)=1`, or an equivalent nonlocal endpoint observable.

For every chosen modulus:

1. define precisely which legal continuation constraints are represented;
2. compute the attainable word-side residue set independently of `T(c)`;
3. project `T(c)` to the same residue coordinate on the finite affine k-windows;
4. intersect exactly and report candidate/prefix/rank effects;
5. provide a gap-free verifier.

Do not count a residue restriction unless it is necessary for every physical
continuation in the scoped branch.

## Success criteria

Produce at least one of:

1. exact candidate deletion from an independently computed endpoint/tail residue;
2. a strict selector reducing the 139,581,280 family;
3. an empty scoped intersection proving a branch contradiction with minimal assumptions;
4. a theorem-quality blindness/surjectivity result for the tested nonlocal observable,
   with the next candidate-wise missing return datum identified in falsifiable form.

Any surviving explicit tuple is a falsifier of an asserted empty core.

Keep all RL222 proof-state classifications, type locks, and verification-economy rules.
