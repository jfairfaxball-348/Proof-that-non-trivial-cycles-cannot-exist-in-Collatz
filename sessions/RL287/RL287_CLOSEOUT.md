# RL287 closeout

Date: 2026-09-09

## Incoming

Base `main` at closeout lock:

`965409d2ed7458d20578da617887b23b878ded63`

Incoming authority:

`authoritative/RL287_RETURN_NORMALIZED_DEFECT_RESERVE_GATE_A_TARGET.md`

Incoming target blob:

`9a1b7ae6503f29f6bbe466490ffd6e5452aca288`

Incoming `authoritative/START_HERE.md` blob:

`1b819b99d07a0c3aa38362fc73b77fa4239feed3`

## Closed classification

`STATE_RESERVE_PHASE_TRANSPORT_AND_GLOBAL_REACHABILITY_BARRIERS_PROVED`

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

No correction or demotion of inherited mathematics is made.

## Frozen promoted results

See the main RL287 report. Principal promoted analytic results are:

- one-zero boundary quotient/defect reserve identity;
- unique affine positive reserve ledger;
- normalized global Ferrers reserve equals accumulated coupon defect;
- hybrid reserve ledger and vanishing-cost phase-reset barrier;
- general legal-segment 2-adic cylinder isometry;
- height-bounded non-boundary skeleton;
- universal low-height direct-terminal local-predecessor barrier;
- residual linear boundary-one surplus condition;
- arbitrary boundary surplus with global `Beta=3` barrier.

## Verification

Portable verifier:

`verification/verify_rl287_phase_reserve.py`

Recorded output:

`verification/RL287_FAST_VERIFIER_OUTPUT.txt`

Proof-state/scope red team:

`verification/RL287_RED_TEAM.md`

Candidate verifier and red team pass.

## Successor

RL288 target:

`RL288_FIXED_SEED_2ADIC_HIGH_DIVISIBILITY_SIGN_GATE_A_TARGET.md`

It returns directly to the fixed-seed global RL285 high-divisibility/sign theorem and explicitly forbids another local scalar-reserve programme as the principal route.

## Transport and catalogue status

Connector-worker direct Git-object transport is used. The atomic commit and per-file blob/tree identities provide the lossless handover; no ZIP archive is required.

Generated knowledge catalogues are left unchanged and are `stale/deferred` under the connector-worker policy. They are not authority and are not part of the proof-state gate.

## Atomicity

The intended transition is one Git tree/commit from the recorded base head, followed by one fast-forward update of `main`, then remote readback of:

- the new `main` SHA;
- `sessions/RL287/`;
- `authoritative/START_HERE.md`;
- the unique RL288 target.

No second RL is started in this closeout.
