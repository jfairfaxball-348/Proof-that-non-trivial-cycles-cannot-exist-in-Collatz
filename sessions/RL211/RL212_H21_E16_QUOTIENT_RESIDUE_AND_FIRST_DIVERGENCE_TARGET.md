# RL212 target — e=16 quotient residue and first-divergence absolute consumer

Date: 2026-08-31.

Accept RL211's exact e=4 selector and flat-K denominator barrier, together with the full
RL210 first-divergence interface.

## Primary target

For short above-p offset `e=16`, use:
- RL210 p-separated height equality through the source;
- `K_0=...=K_17=2^37`;
- the absolute H21 source `y_(p+16)=2^34 eta-1`, source height one;
- root anchors `h0=h1=hp=h_(p+1)=0`;
- RL199 state law `011 <=> eta=0 mod9`, `111 <=> eta=8 mod9`;
- physical cycle states are units modulo3.

Do **not** enumerate and promote tens of thousands of raw prefixes. Build an exact compressed
automaton / quotient-state residue description for the root 16-step recurrence and intersect it
with the H21 eta/state/prehistory conditions. Preferred success is:
1. exclusion of one H21 state or one/two mod18 classes at e=16;
2. a finite exact deletion of the e=16 terminal rank;
3. a small canonical residue family suitable for a stronger absolute consumer.

## Secondary target

If e=16 cannot be compressed to a useful selector, transfer the same quotient-state idea to
RL210's `e>=56` first mismatch:
`24<=m<=37`, `min(S_m,S'_m)=37`.
At phase m, flat K makes the normalized gap dyadic automatically, so the consumer must retain
the quotient-state residue/orientation, absolute endpoint value, complete-word moment, or
another non-denominator datum.

## Locks

Do not revive the rejected “normalized gap must be integer” argument.
Do not extend free-prefix DP without an absolute/global consumer.
Preserve all rank counts, RL206 corrections, below-p 37/60/97 constants, p-shift carry,
physical/charge/Gate/global locks unless an exact new proof changes them.
