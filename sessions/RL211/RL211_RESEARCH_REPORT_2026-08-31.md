# RL211 research report — absolute e=4 closure selector

RL211 followed the requested global-prefix/absolute-closure route rather than extending
the free-prefix DP.

The productive branch is the small offset `e=4`. RL210's flat K segment reaches the
actual H21 tau34 source at `p+4`, allowing the inherited absolute endpoint
`y_(p+4)=2^34 eta-1` to be transported backward through the exact root p-gap and then
through the first four physical chronological steps.

This produces a genuine oriented selector:
`eta=207 mod243`, root heights `00001`, accelerations `1211`, and inherited H21 state
`011`. Therefore state `111` and eta classes `8,17 mod18` cannot occur at the e=4
necessary terminal rank. The parity/sign bit remains open, and no terminal rank is deleted.

A second candidate branch was deliberately rejected: treating the flat normalized gap
as an integer. RL195 permits a dyadic denominator; with `K=2^37` the normalized gap is
automatically dyadic. RL211 records this as an exact method barrier so future sessions
do not repeat it.

The `e=16` source admits the same absolute strategy but no comparably small closed case
split after the first pass. It is handed to RL212 as an exact quotient-state/residue
compression problem. The large-offset `24<=m<=37` first-divergence family remains live
and should be coupled to quotient residues or an absolute complete-word moment rather
than denominator rechecks.
