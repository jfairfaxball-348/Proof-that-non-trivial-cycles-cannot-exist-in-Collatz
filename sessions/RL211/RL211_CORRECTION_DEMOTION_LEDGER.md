# RL211 correction / demotion ledger

Date: 2026-08-31.

No inherited theorem or exact finite certificate is demoted.

## New scope corrections / route retirement

- A candidate shortcut considered during RL211 treated `K_i/rho_i` as though it had to
  be an integer. This is **rejected and not promoted**. RL195 proves only that its reduced
  odd denominator must disappear; the normalized gap may retain a power-of-two denominator.
- The resulting flat-K denominator calculation is promoted only as a method barrier:
  `K_t=2^37` makes `K_t/rho_t` manifestly dyadic and therefore passes the exact RL195
  ownership-equivalence test automatically.
- `eta=207 (mod243)` is necessary only for a physical H21 realization of the exact
  above-p short offset `e=4`. It is not a global eta-class theorem and does not assert
  physical realization of that rank.
- Necessary terminal ranks remain necessary ranks, not a physical population.
- All RL206 correction locks, below-p `37/60/97` constants, p-shift carry treatment,
  physical/charge/Gate/global locks, and inherited finite-cutoff guardrails remain binding.
