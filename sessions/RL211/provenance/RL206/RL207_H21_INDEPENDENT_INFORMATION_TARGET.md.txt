# RL207 target — H21 independent-information completion

Incoming job RL207 follows completed RL206. Apply both correction ledgers first.
The exact necessary terminal-rank predicate/count is unchanged at 16,188,727,234.
The sole high branch `(37,0,23,-1)` and physical/necessary/charge locks persist.

## Primary task

Recover the exact source-versus-terminal and lifted p-shift interface from
`RL206_INHERITED_H21_INTERFACE.md`, then seek an independent consumer of the
complete-word endpoint moment. First examine the unproved `a>p` side with
its actual prefix decomposition and carry behavior, or a genuinely deeper
post-terminal completion constraint. Do not reflect a<p across p by analogy.

Freeze a specific independent datum, the canonical positions it uses, and the
exact physical hypotheses needed before computing eta bits. A residual or
congruence is progress only if it adds information not implied by the existing
endpoint identity, forced tau34 tail, orientation law, or complete-word
denominator equivalence. If the data constrain eta modulo 2^22, combine them
with the terminal Hensel exclusion only after proving the 56-bit precision.

The corrected a<p boundary is offset>=37, root-prefix depth>=60 and first
root-normalization depth>=97. It is one-sided and still makes the two root
terms invisible at 56 bits; do not repeat the invalid 39/63/100 values.

## Meaningful outcomes

Aim for a nonzero exact rank deletion, independent eta/state/sign/valuation
selection, physical H21 incidence/charge, or a branch contradiction. A proved
independent bridge or sharply specified obstruction to a genuinely new
consumer may be retained at its exact scope. Merely restating shallow dyadic
parity or a finite-strip affine family does not accomplish the primary target.
Continue adjacent productive work; a local lemma alone is not an automatic
trigger for a new RL number. Reserve capacity for complete independent closeout.

## Required red teams

1. Source phase a and terminal phase a+34 must use their correct ranks and bounds.
2. Root/anchor and p-shift carry/quasiperiodic conventions must be exact.
3. Existing eta parity/state/valuation laws cannot be relabelled new information.
4. The real-mass relaxation and coupled local-reverse barriers remain binding.
5. No generic denominator/integrality re-test substitutes for an independent
   complete-word constraint; preserve the RL195 equivalence.
6. Rank evidence is not a physical H21 occurrence or charge. Any finite exclusion
   requires gap-free exact coverage and disjoint subtraction from the predicate.
7. Branch results do not close Gate A, Gate B or global nontrivial cycles.

Unfinished work is NOT PROMOTED. Preserve exact uncovered ranges. If a live
dependency fails, stop and repair its first invalid step without altering history.
Broader quotient-residue methods remain viable outside RL206's tested families;
H21 is the selected strategic fallback, not the only mathematically possible route.
