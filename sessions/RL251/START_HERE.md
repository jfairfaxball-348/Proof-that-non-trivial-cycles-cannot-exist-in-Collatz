# Authoritative start — RL251 Gabriel's-horn exploration

Incoming state: **RL251**.

Completed incoming generation: **RL250 — beta=6 exact support lift and terminal-window covering**.

RL250 classification: **R4_BRIDGE_REDUCED**.

Strongest promoted RL250 frontier:

- `z=a-ell>=289` on Branch C with `beta(P)=6`;
- first arithmetic scale not eliminated by the terminal-window lemma is uniquely `(a,ell,z,q,r)=(783,494,289,317,200)`;
- its canonical terminal selector obeys `31<=k<=59`;
- Gate A remains open;
- Gate B remains open;
- Radius 4 was not invoked;
- Radius 5 remains inactive.

RL251 is a **temporary idea exploration**, not a replacement of the RL250 proof line.

Read first:

1. `RL251_FROZEN_RETURN_TARGET.md` — immutable return point.
2. `RL251_GABRIEL_HORN_CAPACITY_DISCRETENESS_EXPLORATION_TARGET.md` — exploration brief.

The pivot asks whether existing RL narrowing/capacity upper bounds can be paired with a genuinely comparable arithmetic minimum-cost lower bound so that eventually even one RL object cannot fit.

Do not equate vanishing measure/density/width with emptiness. Stress-test infinite compatible branches and compactness explicitly.

If the pivot is inconclusive, circular, merely equivalent to current machinery, or unfruitful, close it and restore the exact frozen RETURN TARGET.

RL251 is **PREPARED, NOT STARTED**.
