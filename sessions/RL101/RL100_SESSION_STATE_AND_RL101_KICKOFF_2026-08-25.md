# RL100 session state and RL101 kickoff

Date: 2026-08-25

## 1. RL100 closeout status

RL100 completed the RL99 structural lifting programme, pivoted through the ranked fallbacks after the primary theorem failed its success condition, and then resumed the exact coupled cascade under the RL99 resumption trigger.

No GitHub writes were made.

The outgoing package contains an exact fast verifier for the arithmetic scope constants, repaired four-tier geometry, support enumeration, and full promoted odd interval.

## 2. Frozen global proof status

Retain exactly:

- primitive/full-`D` radius-3 obstruction: **closed local theorem**;
- Gate A even terminal `k`: **closed analytically**;
- Gate A terminal `k<=25`: **closed by exact finite-certificate corollary**;
- Gate A odd terminal `k>=27`: **open globally**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 3. Mandatory RL100 scope repair

Do not inherit the phrase “first-Farey branch” without qualification.

RL83 writes the raw first-surplus counts as

`(j,o)=(g p,g q)`.

The downstream RL85 physical scale used by RL87–RL100 is proved at the raw pair

`(j,o)=(p*,q*)=(114,208,327,604,72,057,431,991)`,

i.e. `g=1`.

Therefore the frozen cascade scope is:

**exact first-Farey reduced slope + raw `g=1` + full-phase Gate-A branch.**

At the same reduced slope, the first-crossing Beatty condition alone permits

`1<=g<=125,777,718,029`.

The exact RL82 fixed-count envelope is strictly increasing with `g`, so the current physical-cap argument does not dominate those multiplicities.

## 4. Frozen structural RL100 results

Retain:

1. general multiplicity envelope
   `U_(p,q)(g)=2^(g(p-q))(3^(gq)-2^(gq))/(2^(gp)-3^(gq))`;
2. `U_(p,q)(g)` is strictly increasing in `g` for every fixed reduced surplus `(p,q)`;
3. every admissible reduced slope distinct from the first mediant has
   `q>=78,644,250,661`
   under inherited `R#>=2^71`;
4. canonical gcd-block width inequality
   `(lambda-1)R >= g(1-(2/3)^ell)`,
   but it is generalized-increment homogeneous and is not the missing `s=1` ownership theorem;
5. ordinary-`+1` basin route remains blocked at the equality-to-owned-physical-state bridge.

## 5. Frozen resumed cascade state

Exact tier state:

- wide `(D_w,R_w,L_w)=(5,000,030,7,000,000,5,000,053)`;
- middle `(D_m,R_m,L_m)=(7,500,031,4,500,000,7,500,053)`;
- deep `(D_d,R_d,L_d)=(10,000,032,3,250,000,10,000,053)`;
- ultra `(D_u,R_u,L_u)=(15,000,035,2,005,000,15,000,053)`.

The middle depth `7,500,031` is a **repaired** value caused by the exact new minimum

`7,500,033 at r=4,478,962`.

Do not restore `7,500,032` in later geometry.

Other global minima retained:

- deep `10,000,034 at r=378,722`, hence `D_d=10,000,032`;
- ultra `15,000,037 at r=575,974`, with a new RL100 tie at `r=1,919,735`, hence `D_u=15,000,035`.

Final corners:

`A=(0,7,000,001)`

`B=(5,000,031,4,500,001)`

`C=(7,500,032,3,250,001)`

`D=(10,000,033,2,005,001)`

`E=(15,000,036,0)`.

Feasible supports remain exactly `A-C`, `C-D`, `D-E`.

Active terminal support:

`lambda=234375/3281264468752`,

`mu=1/7000001`,

`floor(lambda*C)=3010`,

`L_common=15,000,053`.

First live odd on the raw-`g=1` first-Farey/full-phase branch:

`boxed: k=2,921,813,805`.

Margin there:

`-3,297`.

Previous odd `2,921,813,803` is excluded with margin `+10,323`.

Branch upper endpoint remains

`42,150,931,559`, odd.

## 6. RL101 primary target

Read first:

1. `RL100_FAREY_MULTIPLICITY_SCOPE_REPAIR_AND_RESUMED_CASCADE.md`;
2. this file;
3. `RL101_RAW_MULTIPLICITY_CYLINDER_OWNERSHIP_TARGET.md`;
4. `RL100_COUPLED_CASCADE_RESUME_AFTER_RL101.md` only as deferred state.

Then selectively recover from GitHub as needed:

- RL83 full first-surplus/Farey/global-slope theorem;
- RL82 exact cylinder and fixed-count envelope;
- RL84 cylinder-height theorem;
- RL85 raw first-Farey physical scale;
- RL79 generalized-increment ownership barrier;
- RL20 `D∤Q` fake / gcd-block geometry;
- RL81 common-mode warning;
- RL87–RL90 physical/reset interfaces only after a multiplicity bridge has been obtained.

## 7. RL101 success condition

A genuine success is one of:

- prove every genuine first-Farey reduced-slope violation has `g=1`;
- analytically eliminate every `g>=2`;
- reduce `g>=2` to a finite explicit set and provide exact branch parameters sufficient for certified elimination;
- prove a multiplicity-aware domination theorem that justifies extending the existing branch sieve to all `g`.

A new upper bound such as `g<10^11` without a finite elimination mechanism is not sufficient.

## 8. RL101 mandatory red-team

Every proposed theorem must answer:

1. **RL79:** where is ordinary increment `s=1` / exact denominator content used?
2. **RL20 fake:** would the theorem also hold when `D∤Q`? If yes, it is not yet ownership-sensitive.
3. **RL81:** is any quotient/cylinder coordinate being mistaken for a bounded physical state?
4. **raw-multiplicity test:** does the theorem truly quantify over all `g>=2`, not just `g=1` or a reduced slope?
5. **scope test:** are first-Farey reduced slope and raw first-surplus pair being kept distinct?
6. **external floor:** every use of `R#>=2^71` remains an inherited external computational input.

## 9. Verification economy rule

> After the current bundle checksum, internal manifest, and fast verifier suite pass, accept the frozen incoming proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize new mathematics on the stated live obstruction.

## 10. Sustained-attack / checkpoint rule

Do not close RL101 after one congruence or one multiplicity bound.  Continue through theorem shaping and red-team until the multiplicity bridge is genuinely resolved or structurally blocked.

If compute/context is interrupted during an exact scan fallback, preserve only gap-free completed bands and compare every completed band against the live global floor before promoting geometry.  Never promote across an unscanned gap.
