# RL178 correction / demotion ledger

No inherited theorem is newly corrected or demoted in RL178.

Retained scope locks:

- RL173's `3^(-G)` weighted functional remains auxiliary only.
- RL174's corrected physical p-shift `G_i=S_(p+i)-S_p-S_i` remains authoritative.
- The physical identity remains `F2=sum q_i(2^G_i-1)=3(lambda-1)(y_p-y_0)`.
- RL175's sparse ownership resultant is not used as an independent gap obstruction.
- RL177's 15,872-state local automaton remains a local-feasibility certificate only.
- RL178's second-transition propagation is also one-way: every physical path must survive it, but surviving necessary states are not promoted as physical existence.
- The external `m>=2^71` minimum is not used in any new RL178 theorem.
