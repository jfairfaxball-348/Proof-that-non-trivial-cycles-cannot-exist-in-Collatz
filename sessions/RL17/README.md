# RL17 repair + strategy bundle

This bundle supersedes the optimistic RL16 audit frontier.  It preserves the entire RL16 audit package under `baseline/`, surfaces the key audit files under `audit_sources/`, and adds two newly frozen RL17 results:

- analytic promotion of the cubic one-orbit sparse congruence from cofactor `C` to full discrepancy `D`;
- exact reproduction of the `a<=80` / 2,785-parameter sparse uniqueness experiment.

The main purpose is to drive the next session through proof-chain repair and into a serious global RL bridge attempt.
