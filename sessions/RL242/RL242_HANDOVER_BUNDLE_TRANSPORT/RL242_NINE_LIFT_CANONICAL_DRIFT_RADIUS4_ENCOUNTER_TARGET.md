# RL242 — nine-lift canonical-drift elimination / Radius-4 encounter target

## Mission

Continue only the active Radius-4 global bridge.

RL241 proved that, for every retained one-excursion proper-factor datum, genuine `X-Y` full phase is equivalent to a canonical first-half base drift

`Delta_u = c M`

with

`c in {0,1,2,3,4,5,6,7,8}`,

and the genuine physical state is

`N=R_u+c 2^a`.

The entire two-half physical cycle is an explicit affine function of this single carry (with the companion half differing by one canonical wrap bit).

**Target:** eliminate all nine carries for retained primitive Gate-B objects, or prove that any surviving carry forces two eligible full-`D` rotations at exact cyclic adjacent-transposition distance `4`, matching every audited Radius-4 hypothesis.

Do not start Radius 5.

## Preferred attack order

1. **Canonical drift arithmetic.**
   Express `Delta_u` as sharply as possible in the one-excursion rank/owned-return variables. Seek sign, residue, valuation, or exact return restrictions that exclude `Delta_u=cM`.

2. **Carry-by-carry structural compression.**
   Because there are only nine carries, derive structural consequences of fixed `c` before any finite enumeration. A finite check is legitimate only after proving it exhausts every physical case.

3. **Owned prefix trajectories.**
   Use
   `A_j=A_j^0+c 2^(a-j)3^(p_j)` and the companion-half formula to compare canonical owned returns, paired states, and selected rotations. Seek an exact distance-4 encounter.

4. **Rank/T coupling.**
   The RL65–RL69 ordered rank/phase ladders may be used if they genuinely consume integer phase and preserve ordered path data. Do not replace them by separable rank caps.

5. **Apply Radius 4 only after encounter.**
   If an eligible exact-distance-4 pair is proved, explicitly match the RL238/RL239 local theorem hypotheses before claiming Gate-B closure.

## Required red teams

- `(65,41)` proper-factor model must remain rejected by the missing `X-Y` phase, not by an assumption it actually satisfies.
- Do not return to pure logarithmic/state-scale anti-concentration; RL241 proves that mechanism extends to positive real affine cycles.
- Do not use bare-word monotone radius shells.
- Do not use generic state packing, product growth, or continued fractions as the closure mechanism (RL76/RL77).
- Preserve the correction that the selected Bezout coefficient is not automatically the RL19 physical step parameter.
- Do not assume blanket half-prefix determinant positivity.
- Keep Gate A separate.
- Radius 5 remains fallback only.

## Checkpoint classifications

- `R4_BRIDGE_PROVED`
- `R4_BRIDGE_REDUCED`
- `R4_BRIDGE_BARRIER`

Only `R4_BRIDGE_PROVED` permits global application of Radius 4.
