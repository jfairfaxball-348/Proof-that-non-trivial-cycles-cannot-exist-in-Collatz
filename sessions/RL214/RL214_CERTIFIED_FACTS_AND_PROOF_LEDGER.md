# RL214 certified facts and proof ledger

Date: 2026-09-01. Canonical classification after completing RL214.
All physical conclusions remain conditional on the sole inherited high branch `(37,0,23,-1)` and a physical H21 realization.

## New proved analytic mathematics

### RL214-T1 — exact ownership-quotient coordinate bridge
After expanding the accelerated odd-step word to the complete binary word and aligning full phase zero with the H21 odd root, the RL206 owned state is exactly

`y_0 = Q(d)/(2^A-3^L)`.

Thus the H21 root coordinate is the full-word ownership quotient state. The alignment/expansion is part of the theorem; no notational identification is assumed.

### RL214-T2 — positive p-arc root cap
The complete positive p-arc satisfies

`2^u K_0=(3^p-2^u)y_0+P`, with `P>0`.

Hence `0<y_0<K_0/(alpha-1)`. Using `Ap-uL=1`, inherited `0<ln(lambda)<2^-40`, `p<2^36`, and `ln2>2/3`, this yields the portable rational necessary inequality

`29 y_0 <48 L K_0`

and therefore `y_0<=31,285,589,992,934,194,300,574`.

This is a new global inequality, not the collapsed RL201 endpoint moment.

## New exact finite certificate

### RL214-CERT1 — complete bounded e=16 root-lift classification
The verifier reconstructs all 108,950 RL212 e=16 root prefixes and all 45,046 H21-compatible prefixes. For each exact prefix the unique root-unit lift modulo `3^17` has a positive canonical root `y_0^*<3*2^58`; every exact lift is
`y_0=y_0^*+3*2^58*k`.

RL214-T2 forces:
- `k<=36,180` for 34,652 prefixes;
- `k<=36,181` for 10,394 prefixes.

There are exactly **1,629,819,720** bounded arithmetic root candidates. Applying the complete inherited terminal Hensel condition removes **789** individual candidates, but no prefix. Exactly **1,629,818,931** bounded arithmetic candidates survive.

Every prefix retains at least 36,180 candidates. Both H21 states, all four mod18 classes `0,8,9,17`, and all 469 RL212 reachable eta residues modulo2187 remain represented.

## New method barrier

### RL214-B1 — ownership quotient needs one extra D-adic digit
Bare ownership supplies `Q(d)=D y_0` and therefore `Q modD=0`, but it does not determine `y_0 modD`. That residue requires `Q modD^2` or equivalent independently anchored full-word information, precisely the exception left open by RL206.

RL214-T2 bounds the quotient/root coordinate but does not determine that missing residue. The bounded family plus the ordinary terminal valuation deletes no H21 prefix.

## Inherited state retained

The e=16 terminal rank remains `34,124,151,203`.
The global necessary terminal count remains **13,415,865,871**:
- above-p source: **7,091,831,284**;
- below-p source: **6,324,034,587**.

RL212's 17 forbidden eta residues modulo2187, RL211's e=4 selector `eta=207 mod243`, RL210's first-divergence theorem, all RL206 corrections, below-p `37/60/97`, p-shift carry, and every physical/charge/Gate/global lock remain binding.

No physical H21 incidence/charge, branch contradiction, Gate closure or global nontrivial-cycle exclusion is proved.
