# RL214 provenance and authority record

Date: 2026-09-01.

Incoming remote/default-branch head: `f0dbcb3fa0aba3d943cf4f1f91d2ff407ba9520e`.
Incoming authoritative tree: `3f568c15ee37b418c4d1113c05460b98c9170098`.
Incoming job: RL214. Successor job: RL215.

RL214 uses no new external mathematical or computational certificate. The coordinate bridge is derived from the frozen RL206 full-word recurrence and the inherited H21 accelerated recurrence. The root cap uses only inherited exact constants, `Ap-uL=1`, the inherited `0<ln(lambda)<2^-40` defect bound, positivity of the exact p-arc numerator, and the elementary positive atanh series for `ln2`.

The finite certificate uses standard-library Python and exact integer/modular arithmetic. It reconstructs the complete RL212 e=16 prefix domain and performs exact modular counting over the complete bounded lift interval; it does not enumerate or claim physical cycles.

The exact incoming RL213 authority is frozen by Git tree identity. Connector closeout uses lossless Git-object transport: unchanged inherited Git objects are reused exactly, while the RL214 overlay is sealed by an internal SHA256 manifest, deterministic ZIP reconstruction, outer SHA256 sidecar, clean-unpack verifier replay and proof-state guard before one atomic ref advance.

The exact incoming authority is frozen under `sessions/RL213`; successor authority is completed RL214 with one RL215 target.

Knowledge catalogues remain stale/deferred and are not part of the proof-state safety boundary.
