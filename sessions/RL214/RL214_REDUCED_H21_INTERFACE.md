# RL214 reduced H21 interface

Date: 2026-09-01. Compact successor interface.

## Constants and frontier

`A=217976794617`, `L=137528045312`, `B=80448749305`,
`p=65470613321`, `u=103768467013`, `z=72057431991`,
`Ap-uL=1`, `pB=1 modL`, `K0=2^37`.

Necessary terminals remain **13,415,865,871**:
- above-p source: `7,091,831,284`;
- below-p source: `6,324,034,587`.

Global necessary eta classes remain `0,8,9,17 mod18`;
`011 <=> eta=0 mod9`, `111 <=> eta=8 mod9`.
Below-p constants remain corrected `37/60/97`.

## RL214 full-word/root bridge

For the complete binary word expanded from the accelerated word and phase-aligned at the H21 odd root,

`y_0 = Q(d)/(2^A-3^L)`.

Bare ownership does not determine the quotient residue; `y_0 modD` requires `Q modD^2` or equivalent new information.

## RL214 positive p-arc cap

The complete p-arc obeys

`2^u K0=(3^p-2^u)y_0+P`, with `P>0`.

Hence `0<y_0<K0/(alpha-1)`. A portable inherited-defect bound is

`29 y_0 <48 L K0`,

so `y_0<=31,285,589,992,934,194,300,574`.

## RL214 exact e=16 bounded family

RL212's 45,046 H21-compatible root prefixes each have one canonical root-unit lift modulo `3^17` with `0<y_0^*<3*2^58`.

All exact lifts are
`y_0=y_0^*+3*2^58*k`.
Positivity and the new cap force:
- kmax 36,180 for 34,652 prefixes;
- kmax 36,181 for 10,394 prefixes.

Total bounded arithmetic root candidates: **1,629,819,720**.

The inherited terminal Hensel condition removes 789 candidates but no prefix. Remaining bounded candidates: **1,629,818,931**. Every prefix retains at least 36,180 candidates; both states, all four mod18 classes and all 469 RL212 reachable eta classes modulo2187 survive.

## Retained locks

RL213 endpoint-moment collapse and G56 saturation remain binding.
RL211 e=4 remains `eta=207 mod243`, state011.
Do not revive normalized-gap integrality or treat the bounded root family as physical realizations.
Do not import RL209's root-window signed-successor anchor to the e=16 terminal phase without a new proof.

No rank deletion, physical H21 incidence/charge, branch contradiction, Gate closure or global nontrivial-cycle exclusion is proved.
