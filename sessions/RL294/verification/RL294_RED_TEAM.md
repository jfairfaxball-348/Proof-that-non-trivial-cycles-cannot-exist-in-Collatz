# RL294 target-specific red team

Date: 2026-09-10
Result: PASS for promoted RL294 scope.

RL294 does not claim Gate A or the all-depth first-d=1 P-owner theorem. The red team below tests that the promoted cascade/algebra/Bellman statements do not silently cross the known barriers.

## 1. Positive zero-height boundary retention and trivial cycle

PASS.

The cascade algebra is a physical one-column identity. The all-depth checkpoint-2 theorem splices at an identical physical state and copies the complete suffix, so arbitrary subsequent zero-height boundary motion is preserved rather than bounded or discarded.

No chosen boundary representative is used.

## 2. RL292 all-zero static-tree pullbacks with unbounded Beta

PASS.

RL294 does not bound Beta by a finite local potential. The wall interpretation explicitly retains formal rejected-tube debt and states that the composition cross-term alone cannot price it.

## 3. RL287 cylinder isometry / RL289 finite-suffix transparency

PASS.

No promoted theorem assumes phase mixing or loss of 2-adic precision after a fixed suffix. Exact same-state splice is used for checkpoint-2 domination.

## 4. Neutral `(101)` fixed-seed gauge

PASS.

The promoted cascade identities concern physical `(d,K)` states and propagated columns. No absolute prefix length/rank is used as complexity. Identity factors are explicitly treated as gauge and must be quotiented at walls.

## 5. Physical branch mergers / equal-area diamonds

PASS.

No chosen-history observable is promoted. `Bcal` domination uses minimum/physical-state splice. The cascade factorisation is an algebraic representation, not a claim of unique ancestry.

## 6. RL283 adjacent-swap valuation jumps

PASS.

No per-edge valuation-Lipschitz inequality is asserted.

## 7. Homogeneous `(010)^R` Rank-1 danger towers

PASS.

RL294 does not claim a locally bounded Bellman potential. The residual wall debt is explicitly allowed to carry arbitrarily deep danger.

## 8. Shifted phase-reset family and `10922->6146`

PASS.

Dual G/T/N coordinates are exact recurrences only; no bounded shifted-phase potential is inferred from them.

## 9. RL290 chains `2514->5378->21842->8192` and `1074->1364`

PASS.

The all-depth checkpoint-2 theorem is same-state dominance and remains valid under arbitrary suffixes, including recursive Bellman-threat chains. No one-generation hazard bound is substituted for Bcal.

## 10. Locally legal terminal predecessor families not globally reachable

PASS.

RL294 does not infer fixed-seed reachability from numerical predecessor/cascade factor equality. The exact front-door factorizations are algebraic only.

## 11. RL293 immediate-danger family excluded by normalized-K corridor

PASS.

No contradiction or replacement of the corridor is claimed. The new cascade machinery is compatible with retaining that theorem for the eventual P-source attack.

## 12. H<=60 owner exceptions `J=2,3,6`

PASS.

RL294 does not generalize the H<=60 finite certificate. Checkpoint 2 is handled by a new independent all-depth Bellman theorem; no claim is made that its first fixed-seed arrival has a new height.

## 13. Principal-cylinder reduction scope

PASS.

The principal-cylinder theorem is not extended to arbitrary later visits.

## 14. Cascade-specific wall tests

PASS with explicit limitation.

- The formal identity `I=(0,0)` is compositionally neutral; retaining it as a history factor would create artificial negative signed area. RL294 requires immediate quotienting.
- A nonzero formal `d=0` factor is not deleted; it is identified with rejected-tube debt.
- The area cross-term `+L` does not by itself pay the tube depth; this tempting shortcut is explicitly rejected.
- The finite aligned base-9 carry cannot be absorbed by a scalar `g(r)`; an explicit four-edge contradiction is promoted as a method barrier.

## 15. Verification

`verify_rl294_cascade.py` checks:

- unified column recurrence;
- dual G identities;
- associative cascade law under propagated bits;
- named front-door factorisations;
- Z/E formal wall transducer;
- checkpoint-2/P tower comparison for d=2..60;
- checkpoint-8/S_d path formula for d=3..40;
- base-9 carry system and four-edge scalar contradiction.

Frozen output: `RL294_FAST_VERIFIER_OUTPUT.txt`.

Result:

`RL294_RED_TEAM_PASS_FOR_PROMOTED_SCOPE`.

The stronger `(4,39)->(5,191)` and P×P route is not part of the promoted theorem set and therefore is not certified by this PASS.
