# RL301 — proof ledger, barriers, and scope

Date: 2026-09-11

## Promoted analytic/exact results

1. **B-ancestry crossing of the RL300 direct-start boundary.** For `N=19+24h`, `Y=16h+15` reaches `B0=81h+80` after exactly four odd half-steps. At the incoming boundary record `(206745572560704147,130441933147714940)`, the full B-ancestor envelope stays below the inherited RL300 delay threshold.
2. **Exact J transfer.** If `J_Y` is the half-step imbalance from Y to 1, then `J_B=J_Y-4` because the removed prefix consists of four odd half-steps.
3. **Extended resonance certificate.** Exact rational logarithm arithmetic certifies the required one-sided records through `(7354673373747273033,4640282259296926456)` and diagnostic records beyond it.
4. **New selector frontier.** Combining B-ancestry with the external class-record certificate gives elimination of every retained selector through `a=7354673373747273032`.

## Promoted external finite dependency

RL301 consumes exactly:

`D(n)<=2456 for every positive n<46500000000000000000`.

This follows from the externally completed class-record computation below that bound plus the class-record definition as the least element of each delay class. The in-repository verifier independently replays `D(28019077177231758495)=2456`.

The historical exhaustive class-record computation is not reproduced internally.

## Strategic barrier

At the next resonance record

`(7354673373747273033,4640282259296926456)`

the exact B-ancestor envelope reaches

`216988486345374634367`,

outside the external certificate. Later record requirements escalate further to approximately `5.78e20`, `1.72e21`, and `6.57e22`.

Together with RL299's uniform reverse-ancestry coefficient-16 barrier, this demonstrates that the current external-certificate mechanism is a finite ladder rather than structural compression.

Classification:

`FINITE_EXTERNAL_LADDER_METHOD_BARRIER_FOR_CURRENT_B_ANCESTRY_CONSUMER`.

## Route decision

Return the next authoritative research job to the parked RL297 Bellman/P shared-endpoint route. Preserve this physical frontier as a frozen branch for future reuse.

## Open status

Gate A OPEN.
Gate B OPEN.
Bellman/P route weak-green, unproved.
Front-door residual engineering remains open.
Radius 6+ frozen.
RO/divergent-orbit work out of scope.
Lean formalisation separate.
No global Collatz or non-trivial-cycle theorem.
