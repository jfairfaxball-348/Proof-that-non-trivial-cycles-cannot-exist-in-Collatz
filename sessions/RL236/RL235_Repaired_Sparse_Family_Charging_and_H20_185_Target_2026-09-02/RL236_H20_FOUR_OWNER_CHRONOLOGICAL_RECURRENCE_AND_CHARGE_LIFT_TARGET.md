# RL236 target — H20 four-owner chronological recurrence and repaired-charge lift

## Mission

Continue from the gap-free RL235 charging repair. Attack the exact H20 blocker

`T=5*3^35=250157725494998535`, `H=20`, owners `{32,33,34,35}`, full-prefix core `[96166487668,98855162143]`.

The first quantitative objective is to prove its chronological occurrence count over one period is at most `746,997,478`. A direct spacing theorem `>=185` suffices; spacing `184` does not.

## Binding preserved state

- repaired ordinary absolute corrected flow `>712`;
- each signed corrected-flow mass `>356`;
- each directional K variation `>118`;
- exact 7,531-cell reconstructed charging state space and exhaustive verifier;
- exact H21 `{33,34,35}` spacing `>=1001` and its occurrence cap `137,390,654`;
- RL234 K-priced defect lemma;
- RL232 combined H17 spacing `>=1001` and exact H17 results;
- RL233 finite-modulus decomposition;
- sole high branch `(37,0,23,-1)`;
- K corridor `128081997553 < K < 146795909391`;
- frontier `13415865870`;
- live e=4 rank `31435476727` with `3856660232` survivors through transition 43;
- Gate A and Gate B remain open.

## Required attack

1. Reconstruct the exact H20 invariant and its full-prefix/K-compatible terminal core from the RL235 reconstructed state space.
2. Apply the inherited universal carry-completed return transport to exact self-return separations through `184`.
3. If all separations through `184` are excluded, certify spacing `>=185`, recompute the exact H20 occurrence cap, and re-optimize only the already repaired charging schedule.
4. Any penalty must remain attached to this exact terminal invariant; do not transfer a cap from another H20 family or owner subset.
5. If spacing `>=185` fails, classify the surviving exact return states and identify the smallest stronger state variable or owned-return restriction needed for an equivalent `N20<=746,997,478` incidence theorem.
6. Do not revive the demoted H17 `1,615` / `85,103,989` targets unless independently rederived from a new valid theorem.

## Success ladder

### A
The repaired charge lift plus preserved results contradicts the sole high branch.

### B
Certify H20 spacing `>=185` or another rigorous `N20<=746,997,478` theorem, obtain a materially stronger repaired charging consequence, and identify the next exact bottleneck.

### C
Prove a sharp recurrence/incidence barrier for this exact H20 invariant and identify the next missing state without weakening RL235's gap-free repaired schedule.

## Mandatory red teams

- necessary cells are not physical realizations;
- occurrence caps are invariant-specific;
- K-pricing is a local budget, not an occurrence cap;
- total variation is not prefix excursion;
- chronological order is not p-rank order;
- Gate A and Gate B remain independently open unless separately proved.
