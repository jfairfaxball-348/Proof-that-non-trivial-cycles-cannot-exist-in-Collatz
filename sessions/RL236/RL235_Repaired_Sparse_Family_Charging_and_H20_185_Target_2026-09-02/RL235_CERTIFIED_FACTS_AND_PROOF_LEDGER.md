# RL235 certified facts and proof-state ledger

## Promoted

1. **Exact finite certificate:** the repaired full-prefix terminal partition contains 7,531 atomic simultaneous-owner cells for the charged `tau=26..39` state space.
2. **Analytic + exact interval theorem:** for each cell, local charge safety may use the maximum of the generic defect floor, corridor K-pricing `K_LO/T`, and monotone rank-endpoint K-pricing `K(r_hi)/T`.
3. **Gap-free charging theorem:** under the repaired monotone three-band schedule, exactly 13 cells exceed generic height budgets; twelve are covered by local K-pricing and the sole remaining H21 `{33,34,35}` cell is covered by its exact preserved spacing-1001 occurrence cap.
4. **Flow theorem:** ordinary absolute corrected flow is `>712` (verified numerical lower endpoint `712.303259566878...`).
5. **Derived variation consequences:** each signed corrected-flow mass `>356`; each directional K variation `>118`. These are total variation, not prefix excursion.
6. **Rebased bottleneck:** further early-band improvement by this repaired mechanism requires H20 `{32,33,34,35}`, `T=5*3^35`, incidence `<=746,997,478`; direct chronological spacing `>=185` suffices, while `184` does not.

## Preserved inherited facts

- sole high branch `(37,0,23,-1)` and K corridor;
- frontier `13415865870`;
- e=4 rank `31435476727` live with `3856660232` survivors through transition 43;
- RL231 exact ownership enumeration and individual recurrence certificates not dependent on the flawed charging proof;
- RL232 combined H17 spacing `>=1001`, exact H17 cores and local owned-prefix results;
- RL233 finite-modulus decomposition;
- RL234 analytic K-priced defect lemma `|f|>=K/T`.

## Open

- H20 exact-invariant chronological spacing `>=185` or equivalent incidence cap;
- sole high branch contradiction;
- Gate A;
- Gate B;
- global non-trivial-cycle exclusion.
