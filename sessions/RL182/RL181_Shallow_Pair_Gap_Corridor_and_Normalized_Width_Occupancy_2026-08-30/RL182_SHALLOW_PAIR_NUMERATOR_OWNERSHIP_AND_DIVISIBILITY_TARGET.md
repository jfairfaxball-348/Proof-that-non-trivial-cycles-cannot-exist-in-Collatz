# RL182 target — shallow pair numerator ownership and divisibility

Continue from the surviving high branch `(v,H,J,d)=(37,0,23,-1)` and the RL181 normalized p-shift spacing theorem.

## Primary target

Seek an independent **physical arithmetic** theorem that constrains the noncarry pair-state numerators forced by RL181 strongly enough to reduce or exclude the high branch.

The principal certified inputs are:

- at least `35,000,224,335` noncarry p-shift gaps with both endpoint heights `<=4`, hence normalized denominator dividing `16`;
- at least `10,075,174,577` noncarry p-shift gaps with both endpoint heights `<=1`, hence denominator dividing `2`;
- an ordered `h<=1` height type repeated at least `2,518,793,645` times and an ordered `h<=4` type repeated at least `1,400,008,974` times;
- mandatory shallow normalized width `>11m/64` at height four, with the full RL181 occupancy ladder;
- the uniform `K` corridor `128,081,997,553 < K_i < 146,795,909,391`;
- the inherited exact accelerated transition law and RL180 delayed-support requirements.

For an ordinary p-shift edge with heights `(a,b)`, use the actual physical numerator

- `C=2^(a-b)y_j-y_i` if `a>b`;
- `C=y_j-2^(b-a)y_i` if `a<b`;
- `C=y_j-y_i` if `a=b`.

Preferred consumers are divisibility/congruence, numerator ownership, repeated-type incompatibility, or a correlation between these numerators and chronological transition data. A successful result must cap the possible count or total width of the shallow family below RL181's mandatory quota, or produce another exact physical exclusion.

## Do not spend the session on

- generic dyadic lattice capacity without a numerator restriction;
- rank-only, monotone-chain, or universal permutation packing blocked by RL168-RL170;
- inverse-phase/rank coordinate identities blocked by RL171;
- a wider local phase BFS without a new global consumer;
- blind higher odd-modulus lifting after the RL180 barrier;
- RL173's demoted `3^-G` physical interpretation;
- treating necessary automaton states or arbitrary lattice points as physically realized;
- recursive historical verification absent a live failure or unresolved dependency.

## Scope discipline

Preserve analytic proof, exact finite certificate, inherited certificate, evidence, conjecture, and barrier classifications. RL180/RL181 state floors and shallow populations apply only inside the surviving high branch. Excluding that branch would require proof-state reconciliation before selecting the next branch and would not by itself prove global Collatz closure.
