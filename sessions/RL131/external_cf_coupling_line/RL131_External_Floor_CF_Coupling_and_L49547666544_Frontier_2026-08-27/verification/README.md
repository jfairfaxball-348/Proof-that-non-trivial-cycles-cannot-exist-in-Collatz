# RL131 verification

Run `./run_fast_rl131_verifiers.sh`. It reruns the inherited RL130 minimum-element finite scan and the exact RL20 rational-interval continued-fraction certificate. The latter remains conditional on external `R#>=2^71`.
