# RL147 completion review

RL147 is complete.  It derives the exact bounded-height superlevel expansion
of the ordinary affine numerator and the resulting combined carry ladder.
The expansion yields only a weighted combined ownership relation, not one
relation per binary layer.  A valid height-two local instance collapses a
strict carry comparison, so the RL146 scalar cyclic-order lock cannot be
promoted beyond height one from this information alone.

This is a proved algebraic reduction and a frozen method barrier, not a
mixed-height exclusion.  The required pivot is to genuine negative defect.
