# RL94 SESSION STATE AND RL95 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Incoming RL94 gate

- outer RL94 SHA-256: PASS;
- fresh internal RL94 manifest: PASS;
- supplied RL94 fresh-unpack verification: PASS;
- RL93 fast verifier suite: PASS;
- GitHub incoming provenance commit: `5bde2c7201e82c5acc19993690c7c2fcb5a3d187`.

Verification economy applied. No recursive historical expensive verification was performed.

## Frozen RL94 exact tiers

### Wide inherited tier

`d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`.

### Middle RL94 tier

New exact scan `r=3,525,001..3,675,000` has minimum `7,500,038 at r=3,546,704`; inherited combined minimum remains `7,500,034 at r=1,635,300`.

Effective tier:

`d<=7,500,032`, `r<=3,675,000` => successor `<=7,500,053`.

### Deep RL94 tier

New exact scan `r=1,775,001..2,000,000` has minimum `10,000,037 at r=1,817,629`; inherited combined minimum remains `10,000,034 at r=378,722`.

Therefore

`d<=10,000,032`, `r<=2,000,000` => successor `<=10,000,053`.

### Ultra RL94 tier

New exact scan `r=35,001..335,000` has minimum `15,000,040 at r=86,111`; inherited combined minimum remains `15,000,040 at r=2,900`.

Therefore

`d<=15,000,038`, `r<=335,000` => successor `<=15,000,053`.

## Frozen four-tier support

`mu=1/7,000,001`

`lambda=5,000,000/52,500,238,500,033`

Active corners: `A=(0,7,000,001)` and `C=(7,500,033,2,000,001)`.

Other final corners lie strictly above the support line.

Exact feasible pair supports at the final staircase are `A-C`, `C-D`, `D-E`; `A-C` is optimal for every aggregate direction in the promoted RL94 interval and at the first failure.

`floor(lambda*C)=4,014`, so `b=k-1` remains the worst block-count stratum.

## Conservative successor charge

Retain

`L_common=15,000,053`.

No tier-specific population theorem was proved in RL94.

## Frozen RL94 interval result

Every odd

`2,921,750,257 <= k <= 2,921,758,703`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `4,224` additional consecutive odd values.

At `k=2,921,758,703`:

- margin `+11,277`.

At `k5=2,921,758,705`:

- margin `-4,350`.

Updated branch-specific surviving odd window:

`2,921,758,705 <= k <= 42,150,931,559`, `k` odd.

## Frozen geometry/coupling state

At `R_d=2,000,000`, keeping the current `A-C` support requires

- `R_m >= 3,666,661`; actual `3,675,000`;
- `R_u >= 333,341`; actual `335,000`.

The ultra-radius slack is again only `1,659`.

A next 25,000-step deep move to `R_d=2,025,000` requires, at unchanged depths,

- `R_m >= 3,683,328`;
- `R_u >= 366,674`.

Thus one middle 25,000-step chunk to `3,700,000` and seven ultra 5,000-step chunks to `370,000` are enough geometrically if their bit-length floors hold.

The ultra depth-axis remains nonbinding through deep radius floor `3,499,993` under unchanged depths.

## Authoritative RL95 live obligation

Attack `k5=2,921,758,705` using the coupled four-tier cascade.

Priority:

1. extend ultra to at least `r=370,000`;
2. extend middle to at least `r=3,700,000`;
3. extend deep to `r=2,025,000`;
4. recompute combined minima, safe depths, all feasible support edges, optimal weights, top-stratum floor, and exact interval endpoint;
5. if the endpoint closes, continue through adjacent coupled batches rather than stopping at the milestone;
6. retain `L_common=15,000,053` unless rigorously improved;
7. preserve first-Farey scope and all inherited red-team constraints.

## Sustained attack rule

A theorem, endpoint elimination, or improved bound is a checkpoint rather than a close trigger. Continue through coupled frontier propagation, support-edge changes, constant sharpening, adjacent regimes, and red-team while the route remains productive and reliability is not threatened by compute/context pressure.

Verification economy remains mandatory.
