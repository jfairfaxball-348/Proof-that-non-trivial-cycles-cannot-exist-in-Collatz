# Collatz R# RL-3 Seed

This bundle is seeded from the attached RL-2 handover and begins the requested next research phase.

New endpoint:

- the **k=0 root-only xi route is analytically refuted as a closure mechanism**: all root xi ceilings leave positive 3-adic survivor measure, and six explicit arbitrarily large integer families cover all inherited mod144 root classes while satisfying every ceiling;
- the **k>0 relative-height route gains an exact entry amplification theorem**, a relative-order crossing recurrence, and a quantitative exponent-1 contraction debt when the tail starts above the periodic inverse branch.

RL is still open. The next proof-bearing target is cycle-coupled: phase-aware relative height for `k>0`, and cycle-state xi/common-denominator coupling for `k=0`.

Run:

```bash
python3 tools/verify_rl3_rootxi_relative.py
```

The complete RL-2 handover is retained under `parent_rl2/`.
