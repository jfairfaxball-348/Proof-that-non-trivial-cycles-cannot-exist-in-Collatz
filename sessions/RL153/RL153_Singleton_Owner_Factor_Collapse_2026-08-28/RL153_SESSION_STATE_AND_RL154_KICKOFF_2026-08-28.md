# RL153 session state / RL154 kickoff

RL153 proves an exact owner-method boundary on the inherited direct `g=1`
target.

For `F_g=(X^g-Y^g)/(X-Y)`, the repeated-block contact/carry mechanism uses
only a necessary `F_g` divisibility.  At `g=1`, `F_1=1`; that condition is
vacuous and there is no cyclic block-interface vector.  This explains, with
the full numerator retained, why RL146 cannot close the singleton height-one
case.

The live singleton condition is instead the original affine congruence

`Q_c + sum_(ell=1)^H 2^(H-ell) R_ell == 0 (mod 2^A-3^L)`.

It is coupled across defect levels.  The zero-defect branch remains closed by
RL133, but nonzero nonnegative defect excursions remain open.

RL154 target: construct a bounded, exact prefix-residue transfer for this
full one-block modulus which preserves the ordinary `3y+1=2^a y'` ownership
and the RL133 least-state constraints.  It must either eliminate a finite
residual class or record a gap-free compact certificate; it must not replace
the full modulus with the vacuous `F_1`, divide levels into independently
owned conditions, or use an unstructured scan of the absolute state window.

