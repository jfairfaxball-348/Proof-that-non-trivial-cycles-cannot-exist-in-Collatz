# RL153 authoritative handover — start here

RL153 resolves a scope question in the direct singleton-owner route: the
inherited repeated-block factor is exactly `F_1=1`, so its contact/carry
divisibility is tautological at `g=1`.  The remaining exact singleton datum
is the full ordinary affine congruence modulo `2^A-3^L`.

No survivor is excluded.  Read the RL153 report and begin RL154's
full-modulus prefix-residue attack.

