# Open obligation frontier after RL153

Frozen live results include RL135 nonnegative defect through `g<=6`, RL146's
primitive height-one closure for `g>1`, RL147's mixed-height layer barrier,
and RL152's negative-defect reciprocal-width bridge with its frozen missing
population/concentration input.

RL153 adds the singleton factor-collapse boundary:

- the repeated-block factor is `F_1=1`;
- the associated contact/carry divisibility is vacuous at one block;
- the non-vacuous singleton owner condition is the full congruence modulo
  `2^A-3^L`.

Live next branch: physical `g=1` owners with a nonzero nonnegative defect
excursion, attacked through a full-modulus prefix-residue transfer.

Still open: `g=1`; mixed nonnegative-height configurations generally;
negative-defect configurations beyond the frozen dependency; Gate A; Gate B;
global nontrivial-cycle exclusion; and Collatz.

