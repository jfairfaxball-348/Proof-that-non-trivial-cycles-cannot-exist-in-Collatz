# RL246 correction and demotion ledger

Date: 2026-09-04

## C246.1 — absolute phase-packet lower bound

Affected scratch statement from the live RL246 research interval:

`DeltaPhi_packet > 1/4374` uniformly.

Disposition: **DEMOTED / NOT PROMOTED**.

Reason: the derivation supplied a uniform lower bound for the q-shift **ratio** `c`, but not a uniform positive lower bound for the absolute entry prefix scale `S_e`. The packet lower bound must therefore remain relative to `S_e`.

Safe retained replacement:

- general extracted internal singleton: strictly positive packet;
- rigid all-odd arm:
  `DeltaPhi_packet > [2/3+(4/9)3^(-2/27)] S_e > 1.07637 S_e`.

No committed prior theorem is affected because the over-strong bound existed only in RL246 scratch conversation state.

## C246.2 — Radius-4 local topology

The rigid `d=1` companion valley has local magnitude pattern `(1,2,1)`, one of the RL238 local Radius-4 topologies.

Disposition: **NO GLOBAL RADIUS-4 APPLICATION PROMOTED**.

The missing hypothesis remains an eligible primitive full-D self-rotation at exact **global cyclic adjacent-transposition distance 4** satisfying the full audited Radius-4 theorem scope. Local topology alone is insufficient.
