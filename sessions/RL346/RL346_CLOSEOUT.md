# RL346 closeout

Date: 2026-09-17
Status: CLOSED/FROZEN
Successor: RL347

RL346 did not close R1 or Phase 4, but it replaced the apparent long-return total-gap ladder by a
global cyclic-signature/deterministic-decoder formulation.

Promoted exact results:

- every surviving Phase-4 return reaches the final-72 boundary with `76<=G_72<=114` and
  `1<=q_(L-72)<=39`;
- every q=0 vertex can be represented by bounded incoming predecessor data;
- exact source/end phase tags leave at most two return-length representatives in one genuine
  two-row traversal and force total gap `H` for each;
- fixed physical endpoints plus `(L,H)` admit at most one complete inverse gap word by exact
  backward carry decoding.

The attempted `G_72=76` numerical escape enumeration is not promoted: its terminal-60 positional
filter had the wrong endpoint-forward orientation. The erroneous scratch remained uncommitted and
is frozen only for forensic reference.

The live RL347 target is therefore not `G_72=77`. It is an all-length skip/pumping/ranking theorem
or bounded mixed-adic quotient for the deterministic decoder, preserving all inherited CRT,
q-profile, row/contact/wrap, ownership, phase-sign and least-state-descent conditions.

Phase 5 remains scratch-only. Phase 6 remains conditional. `O_75=empty` and R1 remain OPEN.
