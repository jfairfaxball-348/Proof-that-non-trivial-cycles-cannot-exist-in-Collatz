# RL347 target — close Phase 4 via the cyclic signature decoder

Status: UNIQUE LIVE TARGET FOR RL347; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent
`(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited
ownership/pruning, and external conditional least-state floor `m>=2^71`.

## Objective

Prove `O_75=empty` by completing Phase 4 from RL346's bounded cyclic-signature/deterministic-decoder
interface. Do not continue a suffix-total ladder.

## Inherited RL346 interface

For every surviving long return:

- the final-72 total gap satisfies `76<=G_72<=114`;
- the exact terminal profile height satisfies `1<=q_(L-72)<=39`;
- the source q=0 state is itself the endpoint of a preceding return and therefore has a bounded
  incoming signature;
- the endpoint q=0 state has the same kind of bounded incoming signature;
- exact decorated source/end phase tags leave at most two possible `L` values in `1..2ell`, and
  force `H` for each;
- fixed physical endpoints plus `(L,H)` admit at most one complete inverse gap word by the exact
  affine-carry decoder.

Thus arbitrary-middle branching has been replaced by deterministic reconstruction from bounded
cyclic boundary data.

## Primary target — an all-length decoder theorem

Find and prove one of the following equivalent-strength closure mechanisms:

1. **Skip/ranking theorem.** A well-founded quantity on decoder states that permits large portions of
   the unique middle word to be skipped while proving eventual rejection/descent, uniformly in `L`.
2. **Pumping theorem.** Repetition of a bounded decoder signature forces a removable/contractible
   block, a forbidden phase sign, incompatible mixed-adic data, or a strictly smaller genuinely
   owned return.
3. **Bounded mixed-adic quotient.** Consecutive q=0 signatures plus a bounded carry/residue state
   determine all predecessor/successor CRT and ownership tests, and every reachable state is
   rejecting or descending.
4. **Exact residual singleton.** If none of the above closes, isolate the first exact physical
   bounded signature that survives every inherited test, including the fully decoded middle, and
   freeze it as the unique residual obstruction.

## Mandatory acceptance tests

Any claimed closure must retain all of:

- predecessor mixed 2-adic congruence and oddness;
- successor mixed 3-adic congruence;
- exact q-record/profile conditions;
- exact source/end row-phase decoration;
- early-row q=0 row contact;
- genuine two-row wrap orientation;
- inherited physical ownership/pruning;
- exact phase-potential sign;
- deterministic least-state descent below `2^71` when applicable.

## Prohibited regressions

Do not:

- enumerate `G_72=77,78,...` as the main route;
- use the invalid RL346 `G_72=76` scratch enumeration as evidence;
- restart sigma thresholds, coefficient ladders, q-upgrades, or the old linear consumer;
- resume Phase 5 before Phase 4 closes;
- treat the old P/Q Bellman/cascade algebra as a current physical-return theorem without a new
  exact bridge.

## Success condition

Prove every phase-potential nondecreasing complete return of length at least 75 impossible or
least-state descending under the inherited parent assumptions, i.e. `O_75=empty`.

Then and only then resume the short-return Phase 5 from reconstructed/red-teamed RL344 scratch. If
Phase 5 also closes, the inherited RL343 closed-walk bridge yields the R1 contradiction.
