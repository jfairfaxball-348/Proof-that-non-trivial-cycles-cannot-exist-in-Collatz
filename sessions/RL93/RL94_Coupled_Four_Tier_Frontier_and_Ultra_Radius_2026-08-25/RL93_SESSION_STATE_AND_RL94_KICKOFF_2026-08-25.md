# RL93 SESSION STATE AND RL94 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Incoming RL93 gate

- outer RL93 SHA-256: PASS;
- fresh internal RL93 manifest: PASS;
- supplied RL93 fresh-unpack verification: PASS;
- frozen RL92 fast verifier suite: PASS.

Verification economy applied. No recursive historical expensive verification was performed.

## Frozen RL93 exact tiers

### Wide inherited tier

Modulo `2^5,000,056`:

`d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`.

### Middle RL93 tier

Modulo `2^7,500,056`, completed scan `r=1,250,001..3,525,000`:

- minimum bit length `7,500,034 at r=1,635,300`;
- effective tier, using deep coverage below `r=1,775,000`:

`d<=7,500,032`, `r<=3,525,000` => successor `<=7,500,053`.

### Deep RL93 tier

Modulo `2^10,000,056`, new completed scan `r=1,100,001..1,775,000`:

- new-band minimum `10,000,036 at r=1,693,348`;
- inherited combined global minimum remains `10,000,034 at r=378,722`;

therefore

`d<=10,000,032`, `r<=1,775,000` => successor `<=10,000,053`.

### Ultra RL93 tier

Modulo `2^15,000,056`, combined exact range `r=0..35,000`:

- inherited `r<=1,000` minimum `15,000,048 at r=88`;
- new-band minimum `15,000,040 at r=2,900`;

therefore

`d<=15,000,038`, `r<=35,000` => successor `<=15,000,053`.

## Frozen four-tier support

`mu=1/7000001`

`lambda=5225000/52500238500033`

Active corners:

`(0,7,000,001)` and `(7,500,033,1,775,001)`.

All other staircase complement corners satisfy the support inequality strictly.

`floor(lambda*C)=4,195`, so `b=k-1` remains the worst block-count stratum.

## Conservative successor-charge repair

Final four-tier interval arithmetic must use

`L_common=15,000,053`.

The preliminary live-session 10M common charge is demoted. This changes the final margins but not their signs or the exact endpoint.

## Frozen RL93 interval result

Every odd

`2,921,652,723 <= k <= 2,921,750,255`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `48,767` additional consecutive odd values.

At `k=2,921,750,255`:

- margin `+5,210` with the corrected 15M common charge.

At `k4=2,921,750,257`:

- margin `-10,779`.

Updated branch-specific surviving odd window:

`2,921,750,257 <= k <= 42,150,931,559`, `k` odd.

## Frozen geometry/coupling state

At `R_d=1,775,000`, keeping the current `A-C` support requires

- `R_m >= 3,516,661`; actual `3,525,000`;
- `R_u >= 33,341`; actual `35,000`.

The ultra-radius slack is only `1,659`.

A next 25,000 deep step to `R_d=1,800,000` would require, at unchanged certified depths,

- `R_m >= 3,533,328`;
- `R_u >= 66,675`.

Thus RL94 should replenish ultra radius first or in lockstep.

## Authoritative RL94 live obligation

Attack `k4=2,921,750,257` using the coupled four-tier frontier.

Priority:

1. extend the exact 15M ultra scan beyond `r=35,000`, initially targeting enough radius to support the next 10M deep chunk;
2. extend the 10M deep frontier beyond `r=1,775,000` in completed chunks;
3. advance the 7.5M middle frontier as required by exact convex-envelope feasibility;
4. recompute all global minima, safe depths, supporting weights, active corners, and exact interval endpoint after each coupled batch;
5. preserve `L_common=15,000,053` unless a rigorous tier-specific charging theorem is proved;
6. if `k4` closes, propagate immediately to the exact next failure point;
7. preserve first-Farey scope and all inherited red-team constraints.

## Sustained attack rule

A theorem, endpoint elimination, or improved bound is a checkpoint rather than a close trigger. Continue through coupled frontier propagation, constant sharpening, adjacent support regimes, and red-team while the route remains productive.

Verification economy remains mandatory.
