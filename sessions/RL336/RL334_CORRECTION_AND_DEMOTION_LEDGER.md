# RL334 correction and demotion ledger

Date: 2026-09-15
Status: FROZEN

No inherited authoritative theorem was found false.

1. An early RL334 scratch ratio `q=1/2` ignored potential jumps and was immediately rejected before promotion. It carries no proof status.
2. Valid intermediate state-dependent bounds `q=7/6`, `q=1/9`, and later refinements are superseded by the final q=22 theorem `22(K-2H)-S<=44`.
3. The session checkpoint reported a refined-graph cardinality of `17,348` edges. Final fixed-floor/source-aware reconstruction shows that number belonged to an intermediate support projection. The promoted final graph has exactly `385` states and `15,835` edges. The q=22 potential remains range `0..44`; this is a bookkeeping correction, not a theorem demotion.
4. Finite diagnostics excluding selected positive-run lengths (including the sharp owned return through lengths 3..11) are not promoted as all-length theorems. RL329's finite-run enumeration barrier remains binding.
5. The new zero-run theorem, graph theorem and cap remain conditional on the inherited external least-state floor exactly as stated in the proof ledger; no conditional scope is silently upgraded.
