# RL137 target — defect occurrence or nonnegative repeated-block dispersion

Date: 2026-08-27

Start from the RL136 owned-excursion structure at the first reduced survivor

`(A,L)=(217,976,794,617,137,528,045,312)`.

The central dichotomy is now:

1. **negative-excursion branch:** one or more `h=-1` phases occur;
2. **no-negative branch:** every proper prefix has `h>=0`.

The next useful theorem should close or materially compress one side of this dichotomy.

## Priority A — force excursion occurrence or a quantitative lower population

Use the exact cycle-closing identity, repeated-block structure, least-state ownership, or a new affine/support invariant to seek a lower bound on

`P=#{j:h_j=-1}`.

A useful result must do more than restate the determinant candidate count. If a lower bound is obtained, consume RL136.2 immediately:

- negative states are `3 mod 4` near the least state;
- exits are odd `-1 mod 3` states;
- entries carry dyadic depth `2^(H+c)`;
- RL122/RL123 physical packing is available.

Do not assume a shell is realized merely because its lattice translate is admissible.

## Priority B — no-negative branch as a global repeated-block object

Assume `P=0`, so all proper prefixes satisfy `h>=0` on the applicable branch.

For `g<=6` this is inherited unconditionally. For larger `g`, treat it as the explicit no-negative side of the dichotomy.

Exploit the repeated reduced mechanical blocks. At markers

`H_t=h_(tL)`

separate:

- contact returns `H_t=0`;
- strict no-contact markers `H_t>=1`.

Seek a theorem converting this rooted defect excursion into **global** sliding-window count dispersion, self-rotation transport, forced run-depth occupancy, or another owned physical packing quantity.

RL123.5 is available:

`dist_cyc(w,rot_s(w))=min_c sum_i |h_i^(s)-c|`.

Do not infer global dispersion from one canonical mean window or from block markers alone.

## Priority C — terminal return policy

RL136 proves:

- no negative defect can occur in the final five full reduced blocks;
- throughout the final `320,125,202,432` reduced-block units, any negative phase is isolated.

Use this long controlled return region to seek a genuine final-return or fibre theorem that is unavailable to the frozen RL110 local-grammar barrier because of the new global defect premise.

If the argument collapses back to generic least-root grammar without using RL136's certified defect strip, freeze it as the inherited dead route.

## Priority D — consume, do not merely extend, the state ceiling

Internally,

`m<2^75` through `g=56,336,298,016`.

Conditional on inherited external `m>=2^71`, the least state is therefore confined to a factor-16 absolute band throughout this range.

Use this only when coupled to ownership, CRT/run fibres, residue classes, or a finite exact certificate with a structurally reduced search space. Do not replace RL137 by an unstructured scan of odd integers below `2^75`.

## Mandatory scope and red teams

- external `R#>=2^71` remains externally sourced;
- internal frontier remains `L>=190,537` unless independently advanced;
- conditional external frontier remains `L>=49,547,666,544`;
- no multiplicity upper bound is inherited;
- no Gate A/B or global Collatz closure without discharging the actual obligations;
- preserve RL20, RL79, RL81, primitivity, physical-versus-quotient, correction/demotion, verification economy, and sustained attack;
- freeze canonical-contact arguments that reduce only to `S=D(x'-x)`;
- do not resume bare CF/product scans;
- do not enumerate further determinant shells unless the result feeds an owned physical obstruction or a certified population theorem.
