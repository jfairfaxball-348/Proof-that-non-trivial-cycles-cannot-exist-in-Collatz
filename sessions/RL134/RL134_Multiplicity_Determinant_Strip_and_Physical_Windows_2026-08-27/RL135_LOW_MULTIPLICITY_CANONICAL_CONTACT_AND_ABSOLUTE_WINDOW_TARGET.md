# RL135 target — low-multiplicity canonical contacts and absolute physical windows

Start from the unchanged conditional frontier `L>=49,547,666,544` under inherited external `R#>=2^71` and the first reduced survivor

`(A,L)=(217,976,794,617,137,528,045,312)`.

RL134 has converted the open multiplicity branch into exact determinant-shell families and has trapped the `g=1` least state below `2^75`.

## Priority A — canonical-contact ownership for `2<=g<=6`

For these multiplicities every positive proper prefix must be exactly

`(S,j)=(kA,kL)`.

Use the actual affine prefix/complement identities at such contacts. The corresponding prefix ratio is exactly `exp(k Delta)` and the endpoint physical state is forced close to the least state. Seek:

- a distinct-state spacing contradiction;
- a block-numerator divisibility obstruction;
- a CRT/fibre incompatibility;
- a forced repeated state / imprimitive block;
- or another exact physical ownership contradiction.

Do not infer that a canonical contact must exist; treat separately the branch with no positive proper prefix.

## Priority B — determinant-one families for `7<=g<=11`

The only off-axis positive candidates are

`W+k(A,L)`, `0<=k<=g-7`,

and

`U+k(A,L)`, `6<=k<=g-1`.

Compute their exact prefix ratios/state bands and couple them to canonical contacts or ownership. Preserve the distinction between candidate count pairs and physically realized prefixes.

## Priority C — consume the `g=1` absolute window

On `g=1`:

`m<2^75`.

Conditional on inherited external `m>=2^71`, at least

`176,421,674`

odd states lie below `2^79`, and at least

`3,399,794,205`

lie below `2^80`.

Combine these absolute windows with exact ownership, CRT fibre spacing, run/depth information, or RL123 sliding-window transport. Do not substitute an unstructured scan to `2^80`.

## Priority D — higher-shell continuation only if useful

At `g=12`, determinant-two shells first enter. Use RL134's exact lattice translate theorem to add only the newly opened determinant shells and determine their exact discrepancy thresholds.

## Mandatory scope

- external `R#>=2^71` remains externally sourced;
- internal frontier remains `L>=190,537` unless independently advanced;
- no bound `g<=11` is inherited;
- bare CF/product, fixed-depth complete residue-prefix, and count-only gcd-block routes remain retired at this survivor;
- preserve RL20/RL79/RL81 red teams, primitivity, correction/demotion, verification economy, and sustained attack;
- no Gate A/B or global Collatz closure claim without discharging the actual obligations.
