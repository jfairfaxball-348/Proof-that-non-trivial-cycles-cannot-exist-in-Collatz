# RL16 audit handover

This is a review-oriented bundle.  Begin with `START_HERE.md` and `AUDIT_STATUS_AND_DISTANCE_TO_RL.md`; use `NEXT_SESSION_KICKOFF_PROMPT.md` verbatim to start a fresh audit session if desired.

The bundle intentionally separates **frozen proof artifacts** from **unverified conversational leads**.  Its main purpose is to prevent the next session from confusing “one apparent radius-3 branch remains” with “RL is one lemma from closure.”
