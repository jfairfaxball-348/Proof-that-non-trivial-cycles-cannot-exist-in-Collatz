# RL327 proof ledger — owned-bridge excess density, residue weighting, and fixed-point contraction

Date: 2026-09-15
Status: FROZEN CANDIDATE FOR RL327 CLOSEOUT
Incoming BASE_HEAD: `f37a2984065548440a8ae0973060f97a40a44a2b`
Successor: RL328

## Scope retained

Work remains in the ordered genuine `g=2`, `Z0>0`, `K<0` late-row parent branch at

`(a,ell)=(217976794617,137528045312)`, `d=a-ell=80448749305`,
`lambda=2^a/3^ell<1+2^-40`,

with the externally conditional least-state floor `m>=2^71` and the inherited state-band consequence

`2^71 <= P < 2^76+2^36`

for the owned backward states used by the finite certificates. RL326 supplies the incoming carry cap

`1<=n<=32839291403`

and the high-carry ownership threshold

`n>=20390252058`.

The RL324 local-propagation barrier remains binding. The telescope remains linear; no cyclic wrap is used. Gate A, Gate B, R1, and global positive non-trivial-cycle exclusion remain open.

Write `T=ell-rho` and let `q_0,...,q_T` be the RL326 mechanical excess sequence with `q_0=0`. Let `Z` be the total number of zero positions and `K` the total number of positive positions, so `T+1=Z+K`.

## RL327.1 — exact singleton owned-bridge certificate

A positive run of length one has shape `0,1,0`. If the adjacent zero runs have lengths `z_left,z_right`, then the joining mechanical gaps change by `+1,-1`. RL327 exhausts every ordered pair

`1<=z_left,z_right<=49`, `47<=z_left+z_right<=98`,

every rational-mechanical factor of the required length, and every endpoint lift in the physical band. Initial endpoint odd parity is enforced explicitly. High-carry least-state ownership retains only realizations whose reconstructed minimum does not already contradict `n>=20390252058`.

The exact retained multiplicities by total adjacent zero length are

`47:1034, 48:342, 49:106, 50:35, 51:10, 52:1`,

and zero above 52. There are 175 retained ordered pair types.

Consecutive singleton bridges are not joined merely by pair type. Equality of the first state on the shared zero plateau is indexed exactly; determinism then fixes the whole shared plateau. This leaves exactly 5 physical links, represented conservatively by 4 ordered pair-type links:

`(1,46)->(46,1)`,
`(2,45)->(45,2)`,
`(2,45)->(45,3)`,
`(2,45)->(45,4)`.

Classification: **exact gap-free finite certificate over the stated pair/factor/lift range, coupled to inherited least-state ownership**.

## RL327.2 — complete two-positive bridge certificate

For a positive run of length two returning to zero, positivity and baseline gaps in `{1,2}` force exactly the two shapes

`0,1,1,0` and `0,2,1,0`.

RL327 exhausts both shapes, every ordered adjacent zero-run pair with total 47 through 98, every mechanical factor, and every endpoint lift in the physical band, again with parity and least-state filtering. The high-carry multiplicities are

`47:468, 48:163, 49:62, 50:20, 51:4`,

and zero above 51.

Classification: **exact gap-free finite certificate for all two-positive shapes in the stated range**.

## RL327.3 — owned-bridge max-plus density theorem

Build a conservative finite automaton with states

- `N(z)`, `1<=z<=49`, after a short singleton or a positive run of length at least two;
- `L(a,b)` for each retained singleton pair type of total at least 47.

The graph admits:

- every singleton transition with adjacent zero total at most 46 without restriction;
- every exact retained large singleton pair, with exact pair-to-pair ownership links when two large singletons are consecutive;
- every two-positive transition whose adjacent zero total is at most 51;
- every transition generated by a positive run of length at least three.

Thus the graph is a conservative superset of every physical high-carry word. It has 224 states and 21,805 edges. An integer potential, checked on every edge, proves the telescoping bound

`Z <= 24 K + 72`.

Since `T+1=Z+K`,

`K >= ceil((T-71)/25)`.

At `rho=60`, this gives

`K>=5501121808`.

Classification: **proved analytic finite-state density theorem backed by exact ownership certificates and a complete edge-potential check**.

## RL327.4 — residue-weighted telescope consumer

RL326 used a uniform loss per positive `q_t`. RL327 uses the exact residue dependence. Since `gcd(a,ell)=1`, the residues

`r_t = a t mod ell`

are distinct for distinct `t` in the matched interval. The ideal coefficient can be written in residue form, and every positive `q_t` removes at least one sixth of that coefficient. Therefore `K` positive ranks lose at least the `K` smallest distinct residue weights, not `K` copies of a global minimum.

A quartic rational lower enclosure for the exponential weight, using exact power sums, gives at `rho=60`

`weighted residue sum > 5578093269.968765`,
`weighted gain over the uniform estimate > 6414288.497391`,
`density loss > 464841105.83030766`.

Inserted into the inherited exact ideal-sum/omitted-mass enclosure, this gives

`n < 32603663706.51567...`,

hence

`n<=32603663706`.

The companion inequality handles `rho<=59`. For `rho>=60`, increasing `rho` removes more ideal mass than the largest possible one-step relaxation of the rounded weighted-density floor, so the upper bound is monotone decreasing.

Classification: **proved analytic residue-weighted refinement plus exact rational constant certificate**.

## RL327.5 — self-consistent total-46 fixed-point bootstrap

Assume for contradiction

`n>=32596612663`.

At this stronger threshold, rerun the exact singleton ownership filter including adjacent zero total 46. The corrected parity-aware layer leaves

- 2,414 owned singleton endpoints;
- 187 ordered pair types;
- 9 exact physical singleton-to-singleton links.

The two-positive maximum remains total 51. A second conservative automaton and integer potential prove

`19 Z <= 449 K + 1347`.

Hence

`K>=5583403544`

at `rho=60`. Applying the same residue-weighted telescope gives

`n < 32596612662.03308...`,

contradicting the bootstrap assumption. Therefore

`n<=32596612662`.

This improves RL326 by exactly

`242678741`.

Classification: **proved self-consistent fixed-point contraction backed by an exact finite ownership certificate, complete potential check, and exact rational telescope consumer**.

## Corrections, barriers, and non-results

1. An earlier scratch enumerator omitted initial endpoint odd parity. Red team caught this before promotion. Every affected scratch count was discarded; all promoted counts above come from a parity-aware rerun.
2. Pre-repair total-46 exploratory counts were invalidated and rerun independently before use.
3. A naive endpoint-lift enumeration modulo `3^(z_left-1)` for short left plateaux was abandoned as intractable; no partial range from it is trusted. The promoted route uses the full-word congruence modulo `3^(z_left+z_right)`.
4. No promoted result uses root-aligned `G<2^35` in the live late-row-root branch.
5. The result contracts but does not close R1. The surviving range is `1<=n<=32596612662`; the high-carry regime below the new cap remains open.

`PARENT_DIFFICULTY_DELTA = EASIER`.
