# RL120 target — non-homogeneous ordinary `+1` cocycle diagnostic

For selected actual owned rotations, seek an exact unnormalized affine
identity retaining the absolute ordinary `+1` increment and an independent
size or valuation force. The identity must be false or quantitatively
different for generalized increment `s!=1`; it must not normalize to
`D|S`, `S/D`, or a scaled coboundary.

Specify the physical states, the cocycle, the selected-support relation, and
the separate quantitative force. RL20 must fail at ownership, RL79 must not
permit factor cancellation, RL81 must use actual rotations, and primitivity,
scope, and finite evidence must be explicit.

If the strongest identity scales as `s` or reduces to the known normalized
transport, record the barrier. Then follow the RL113 return policy rather than
reviving a stopped route.
