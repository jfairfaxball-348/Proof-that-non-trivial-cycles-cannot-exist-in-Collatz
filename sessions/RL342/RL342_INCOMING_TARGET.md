# RL342 target — uniform physical recurrence-carrier descent

Status: UNIQUE LIVE TARGET FOR RL342; prepared, not started.

Work only in the ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, retaining the external least-state-floor qualification `m>=2^71`, q=0 band, `z<=35`, exact ownership/pruning, RL337 fixed-depth suffix compression, RL339 defect bookkeeping, and RL341 exact overlap/recurrence-descent certificate.

## Primary objective

Turn RL341's finite statement `no three consecutive complete returns have sigma<=25` and its consequence `26K <= 3S42 + 57` into a parent-level absolute obstruction.

The preferred target is a **uniform recurrence-carrier descent theorem**: characterize an exact finite/canonical interface for a physical state that can be both the run-exit of one return and the source of the next, and prove that any state capable of carrying continued near-critical recurrence either descends below the least state, moves strictly in an acyclic owned order, or belongs to one exact quantified exceptional family that can be eliminated separately.

Use RL337's right-suffix profile compression or an equivalent fixed-depth interface so the argument is genuinely all-length.

## Diagnostic fallback

RL341 generated the complete `sigma=26` recurrence family at the symbolic level: 199,350 progression families representing approximately 7.54 billion recurrence realizations; full escape partition unfinished and unpromoted.

Use this layer to falsify/test a proposed uniform criterion or, if necessary, to locate the first non-descending exact carrier. Do not make threshold-by-threshold extension the default research programme.

## Route discipline

Do not restart the `c=18,c=19,...` coefficient ladder as the main route. Do not make another q-upgrade, endpoint constant, phase refinement, or ordinary linear consumer the main route: RL340 already quantified that architecture's barrier.

## Success criterion

R1 closure for this ordered parent branch, or a theorem-level reduction to one exact, quantified, non-anonymous physical recurrence obstruction. Preserve all conditional scope and report R1 OPEN unless closure is actually proved.
