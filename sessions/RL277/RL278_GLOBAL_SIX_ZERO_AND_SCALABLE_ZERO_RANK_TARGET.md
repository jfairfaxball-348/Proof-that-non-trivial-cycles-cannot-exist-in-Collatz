# RL278 — global six-zero and scalable zero-rank induction target

Date prepared: 2026-09-07
Status: PREPARED, NOT STARTED

## Incoming classification

RL277 closed as

`ONE_SIDED_COMMON_MODE_CONSUMER_PROVED`

with:

- `ABSOLUTE_ORDERED_RANK_MOMENT_BUDGET_PROVED`;
- `GLOBAL_DANGEROUS_ZERO_BUDGET_Z_GE_K_PLUS_4`.

Gate A remains

`H_can >= k`

at terminal `d=1, J=2^k`.

## Exact inherited conclusions to preserve

1. The global terminal rank identity admits the exact zero-rank rewrite
   `sum 2^(t-1)[3(2/3)^u_t-(2/3)^v_t]
    = 12 + 2^m0(2^k+1)(2/3)^r`.
2. Full-phase scale forces the left positive envelope above `51/2`.
3. Hence every retained object has at least four internal zeros.
4. Four and five internal zeros are exactly Gate-A safe.
5. Therefore every hypothetical Gate-A violator satisfies
   `m0=z-k+2>=6`, equivalently `z>=k+4`.
6. The absolute ordered-rank moment budget is
   `413/16 < sum 2^eta_j <61`.

## Mission

Extend the exact global zero-rank/canonical programme beyond five zeros.

Preferred order:

1. Re-derive the six-zero finite reduction from the RL277 zero-rank identity.
2. Prove an exact bound on the sixth zero-rank `u_6`; do not rely on an arbitrary enumeration cutoff.
3. Exhaust the resulting canonical prefixes and all-one closures.
4. If six zeros are Gate-A safe, attempt to abstract the reduction into an induction or run-compressed automaton for general fixed `m0`.
5. Couple the resulting zero-budget growth to `H_can=sum delta_j` if a scalable pattern appears.

## Success criteria

Preferred:

- prove `H_can<k => z>=k+5` or stronger;
- or prove a reusable theorem showing fixed zero budget gives an exact finite canonical family under the global full-phase scale;
- or isolate a precise obstruction preventing that induction.

## Forbidden repeats

Do not:

- universalize Branch-C-only beta/counterflow results;
- restart the selector conveyor belt;
- treat exploratory six-zero counts from RL277 as promoted;
- repackage the full CRT reconstruction;
- revive the RL48 separable rank relaxation;
- use total variation as excursion without a new boundary theorem;
- start Radius 6+.

Gate B remains frozen unless an explicit new coupling is proved.
