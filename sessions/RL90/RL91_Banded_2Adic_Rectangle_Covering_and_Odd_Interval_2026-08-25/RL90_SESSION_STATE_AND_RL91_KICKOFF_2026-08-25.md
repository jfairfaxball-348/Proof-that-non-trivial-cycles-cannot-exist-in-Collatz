# RL90 SESSION STATE AND RL91 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Incoming RL90 gate

- outer RL90 SHA-256: PASS;
- fresh internal RL90 manifest: PASS;
- RL89 fast verifier suite: PASS.

Verification economy applied. No recursive historical expensive verification was performed.

## Frozen RL90 results

### Physical synchronized-11 cap

For every genuine first-Farey synchronized block,

`boxed: s <= S_*=26,594,276,905.}`

### Exponent deficiency

With

`d_i=C-n_i`,

`r_i=S_*-s_i`,

RL88's aggregate reset budget gives an explicit safe upper bound on `sum r_i` for every `(k,b)`.

### Exact 2-adic rectangle certificate

The exact GMP scan modulo `2^500056` checks every

`0<=r<=550,000`.

The minimum balanced inverse-residue bit length is `500038`, at `r=298,303`; the extension tail `500001..550000` has minimum bit length `500041`.

Therefore:

`boxed: d<=500,036 and r<=550,000 + minimal reset => n_next<=500,053.}`

### Two-budget transition theorem

If `D_b=Cb-Q` and `R_b^max` is the safe total exponent-deficiency budget, then every survivor must satisfy

`b-floor(D_b/500037)-floor(R_b^max/550001)`

`<= (k-1-b)+floor(D_b/(C-500053)).`

### Top block count is worst

For fixed `k`, the contradiction margin strictly decreases with `b`; the ordinary-deficit term alone loses at least `84,295` per added block while the explicit block-count contribution gains only `2`.

Hence it is enough to test `b=k-1`.

### Odd interval eliminated

Every odd

`boxed: 2,921,384,821 <= k <= 2,921,406,839}`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `11,010` consecutive odd values.

Updated branch-specific surviving odd window:

`boxed: 2,921,406,841 <= k <= 42,150,931,559.}`

At `k=2,921,406,839`, the top-stratum contradiction margin is `197,551`.

At the next odd

`k2=2,921,406,841`,

the same fixed certificate has margin `-67,747`, so it no longer closes.

## Authoritative RL91 live obligation

Attack `k2=2,921,406,841` using a **tiered or asymmetric 2-adic rectangle covering**.

Priority:

1. derive the exact `(ordinary deficit, exponent deficiency)` population trade curve for `b=k-1`;
2. optimize rectangle thresholds rather than growing both sides uniformly;
3. use the minimum modulus that gives a useful successor-length charge for each tier;
4. combine nested rectangle populations without double counting;
5. close `k2` if possible, then propagate immediately through subsequent odd values;
6. stop only at genuine compute/context pressure, a rigorous route barrier, or completed target propagation.

Do not brute-force all cofactors below `2^D`. Do not restart independent full-`D` midpoint congruence accumulation. Do not infer primitivity from repeated differences or cofactors.

## Sustained attack rule

A theorem, endpoint elimination, or improved bound is a checkpoint rather than a close trigger. Continue through case propagation, constant sharpening, the next admissible odd values, interval consequences, and red-team while the route remains productive.

Verification economy remains mandatory.
