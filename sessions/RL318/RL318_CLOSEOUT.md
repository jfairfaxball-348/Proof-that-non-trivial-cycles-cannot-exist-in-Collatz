# RL318 closeout — external-floor splice, first-survivor relocation, and cross-content crossing frontier

Date: 2026-09-14
Status: CLOSED AND FROZEN
Session type: CONDITIONAL EXTERNAL-FLOOR SPLICE + FIRST-SURVIVOR RECONNAISSANCE

## 0. Executive conclusion

RL318 does not prove Gate A, Gate B, global positive non-trivial-cycle exclusion, or the Collatz conjecture.

It does materially simplify the active research frontier.

The key change is the restoration of the externally certified least-state floor `R>=2^71` using modern peer-reviewed provenance, combined with the already-promoted RL131 continued-fraction consumer and RL315 reduced-shadow extraction. Conditional on this external input, every `g>1` reduced survivor satisfies

`ell >= 49,547,666,544`.

The first above-resonance reduced pair surviving that gate is the historical RL132 pair

`(a,ell)=(217,976,794,617,137,528,045,312)`.

This relocates the live conditional `g>1` problem from the internal first fibre `(301994,190537)` to the much later first external survivor. The internal-only frontier remains unchanged at `ell>=190537`.

RL318 then spliced the later RL315--RL317 dual-shadow machinery into the historical first-survivor programme RL134 onward. This recovered strong physical structure at `g=2`: nonnegative mechanical defect at every accelerated phase, an internal least-state ceiling `m<2^75`, and an externally conditional state window `2^71<=m<2^75`.

Generic global constraints still do not kill this fibre: bare CF/product, fixed-depth residue-prefix methods, count-only gcd restrictions, and the RL310 physical segment-packing inequality all survive red-team checks at `g=2`. Likewise, the historical contact-polynomial obstructions RL140--RL142 do not automatically cover multiplicity two without a new bounded-contact theorem.

The surviving new analytic frontier is therefore genuinely local and non-homogeneous: in the RL317 nonzero-residue branch, the content-`h` physical `T_h` trajectory and coprime-content shadow trajectory start a balanced row with signed gap `+E` and end it with signed gap `-E`. Equality is impossible because the physical state is always divisible by `h` while the shadow state is coprime to `h`. Thus a strict order crossing is forced. The first parity disagreement cannot perform that crossing; a later reverse-parity disagreement must.

This exact forced-crossing theorem is the successor target. Broad historical archaeology should stop here; the next session should work the crossing algebra directly.

`PARENT_DIFFICULTY_DELTA = EASIER`

Reason: the external-floor splice removes a huge contiguous reduced region and relocates the conditional frontier to one exact historical first survivor. The remaining obstruction is narrower even though it is not yet closed.

## 1. External least-state certificate splice

RL318 identified a current peer-reviewed source for the inherited external least-state input used by RL131:

David Barina, "Improved verification limit for the convergence of the Collatz conjecture", The Journal of Supercomputing 81 (2025), article 810, DOI 10.1007/s11227-025-07337-0.

The paper reports verification of convergence for all starting values below `2^71` and identifies the released code revision used for the computation. The research repository already has precedent (RL300) for consuming an external finite computational certificate with frozen provenance rather than rerunning the historical external computation.

RL318 did not rerun Barina's computation. The external statement is consumed only as an explicitly conditional input:

`R >= 2^71`.

Classification: **external computational certificate dependency with peer-reviewed provenance**.

## 2. RL131 conditional continued-fraction frontier recovered

RL131 already contains an exact rational-interval continued-fraction verifier using `R0=2^71`.

Its certified output is

`qmax=49,547,666,543`,

hence every compatible primitive reduced pair satisfies

`ell >= 49,547,666,544`.

RL315 later proved that every `g>1` candidate has a genuine reduced balanced return `(a,ell)` and explicitly recorded that the RL131 external frontier transfers to this reduced pair when the external floor is admitted.

Therefore, conditional on `R>=2^71`, every `g>1` candidate obeys

`boxed: ell >= 49,547,666,544`.

The internal-only reduced frontier remains

`ell>=190537`.

Classification: **exact finite consequence of an external certificate plus inherited analytic reduction**.

## 3. First conditional external survivor

RL132 identifies the first above-side convergent surviving the RL131 external gate as

`(a,ell)=(217,976,794,617,137,528,045,312)`.

Its ordinary/non-shortcut full length is

`a+ell=355,504,839,929`,

while its accelerated/shortcut period is `a`.

At this pair

`Delta=a log(2)-ell log(3)>0`

is extremely small,

approximately `8.986548708617925e-13`.

RL132 already proves that bare continued-fraction/product inequalities, fixed-depth complete residue-prefix descent, and count-only gcd restrictions do not exclude this pair.

Classification: **historical exact first-survivor identification and method barrier**.

## 4. New RL318 red-team of RL310 physical packing

RL318 checked the later RL310 physical segment-packing inequality directly at the first external survivor for `g=2` and `R0=2^71`.

Using the exact RL310 inequality and rigorous elementary logarithmic bounds, the left side satisfies approximately

`2 Delta < 1.798e-12`,

while the right side is rigorously greater than

`3.883e-11`.

The margin exceeds a factor of 21.

Therefore the generic RL310 physical packing theorem does **not** exclude multiplicity two at the first external survivor.

Classification: **new exact single-fibre barrier / red-team result**.

This prevents a false shortcut: the external-floor relocation is real, but generic packing alone does not finish the new fibre.

## 5. Historical first-survivor structure now available to RL317

RL134--RL137 studied this exact reduced pair before the later RL315--RL317 dual-shadow theory existed.

For `g=2`, inherited results include:

1. RL134 determinant strip: every positive proper accelerated prefix is forced to the canonical reduced-block contact; there are no off-axis positive prefixes for `g<=6`.
2. RL135 nonnegative defect: every proper prefix satisfies `h_j>=0` for `g<=6`, hence in particular for `g=2`.
3. RL135/RL137 least-state ceiling: any such physical realization has `m<2^75` internally.
4. Conditional on the external floor, the physical least state lies in the narrow window
   `2^71<=m<2^75`.
5. The contact-polynomial line RL139--RL142 supplies strong ordinary full-ownership obstructions for compressed/height-one and bounded-interface profiles, but its current multiplicity-two theorem requires a three-contact exceptional-block bound not supplied by RL317.

These results are inherited, not re-proved in RL318.

## 6. First-survivor `g=2` physical gap bound

For the genuine balanced rows at the first survivor, write

`X=2^a`, `Y=3^ell`, `D0=X-Y`,

with physical boundary states `R<x=R+G` and return-row numerator

`V=D0R-YG>0`.

Then

`G/R < D0/Y = X/Y-1 = exp(Delta)-1`.

Thus

`x/R = 1+G/R < exp(Delta)`.

At the first external survivor, this is an extraordinarily tight relative boundary window because `Delta` is about `8.99e-13`.

This is an analytic consequence of physical positivity. By itself it does not exclude the branch because `R` has no internal absolute upper bound stronger than the inherited first-survivor ceilings already available.

Classification: **analytic quantitative consequence**.

## 7. RL317 dual-shadow splice at the first external survivor

For `g=2`, RL317 gives the exact dichotomy.

Let the late/early row envelopes be `tau,sigma`, and let

`epsilon=q_tau-U>=0`.

### 7.1 Ordered-row branch

If `epsilon=0`, then

`tau=u`, `sigma=v`.

The two physical rows are rankwise ordered but may remain distinct. RL21 remains the required negative control: order plus `H=X+Y` ownership alone is insufficient; genuine `D0=X-Y` ownership is load-bearing.

The historical first-survivor defect theorems sharpen the ambient row geometry but do not currently force row equality.

Status: **open**.

### 7.2 Cross-content branch

If `epsilon>0` and `H` does not divide `epsilon`, define

`d=gcd(H,epsilon)`, `h=H/d>1`, `E=epsilon/d`.

RL317 gives two primitive `T_h` trajectories over the same balanced row:

- physical content-`h` trajectory with boundaries `hR -> hx`;
- coprime-content shadow trajectory with boundaries `hR-E -> hx+E`.

Define the signed gap

`delta_j = physical_j - shadow_j`.

Then

`delta_0=+E`,

while after the row

`delta_a=-E`.

Moreover, at every phase the physical state is divisible by `h`, whereas the shadow state is coprime to `h`. Hence equality of the two trajectories is impossible at every phase.

Therefore a strict sign crossing must occur somewhere in the row.

Classification: **analytic forced-crossing consequence of RL317**.

## 8. Exact mismatch orientation and forced reverse mismatch

Before the first parity disagreement, the two trajectories have the same parity and the gap only rescales:

- even/even step: `delta -> delta/2`;
- odd/odd step: `delta -> 3delta/2`.

Thus its sign stays positive.

At the first disagreement, the RL317 late-envelope orientation is physical odd / shadow even. Writing physical state `P`, shadow state `S`, `delta=P-S>0`, the next gap is

`delta' = P + (delta+h)/2 >0`.

So the first mismatch cannot cause the required sign change.

Since the row ends with gap `-E`, there must later exist at least one reverse mismatch

`physical even / shadow odd`.

At such a step

`delta' = P/2 - (3S+h)/2
        = (delta-2S-h)/2`.

The first strict crossing therefore occurs at a reverse mismatch satisfying

`0<delta<2S+h`,

and after the step

`delta'<0`.

Because equality is impossible modulo `h`, the crossing cannot pass through zero.

This is the exact RL318 successor object.

Classification: **analytic cross-content theorem / frontier localization**.

## 9. Bounded full-row affine carry identity

Writing

`epsilon=kH+r`, `0<r<H`,
`M=R-k`, `N=x+k`,

RL318 derived the exact affine row endpoints

`F_tau(M)=N+r/X`,
`F_sigma(N)=M-r/X`.

Because `0<r<H=X+Y<2X`, the endpoint affine defect is less than two. Thus:

- if `r<X`, the natural integer offsets are `+1/-1`;
- if `r=X`, the row lands on the exact boundary case;
- if `X<r<H`, the offsets are `+2/-2`.

Also

`ceil(F_tau(M))+floor(F_sigma(N))=M+N`.

However these affine endpoints are not automatically the endpoints of the actual ordinary integer trajectories after their first parity mismatch. RL318 does **not** promote a closure from this identity.

Classification: **exact algebraic identity plus bridge barrier**.

## 10. Historical contact-polynomial splice: what it does and does not give

RL140 proves, for `g=2` in a height-one contact profile, that one deviant reduced block is impossible if the exceptional block changes at most three contact residues.

RL141/RL142 extend bounded consecutive interfaces when enough untouched reduced blocks remain, but their hypotheses do not apply automatically at `g=2`.

RL318 found no theorem forcing the RL317 dual-shadow interface to alter at most three contact residues. Therefore the historical contact-polynomial machinery does not presently close either RL317 branch.

Classification: **method compatibility audit / negative result**.

## 11. Red teams and preserved barriers

1. The external `2^71` input is conditional and externally sourced; it is not internalized.
2. Bare CF/product arithmetic does not exclude the first survivor.
3. Fixed-depth residue-prefix descent does not exclude it.
4. Count-only gcd restrictions do not exclude it.
5. RL310 generic physical segment packing does not exclude `g=2` there.
6. RL140--RL142 do not automatically close `g=2` without a new bounded-contact theorem.
7. A second `T_h` cycle is not itself a contradiction.
8. Standalone homogeneous `T_h` invariants remain blocked by RL79.
9. The bounded affine carry identity does not identify actual post-mismatch integer endpoints.
10. No local denominator ownership is inferred; RL206 and RL233 remain binding.
11. The internal-only reduced frontier remains `ell>=190537`.
12. `g=1` remains separate.

No inherited theorem is demoted.

## 12. Exact successor frontier

The next authoritative session should **not** repeat broad historical survey work.

Primary target:

> In the RL317 nonzero-residue branch at a reduced survivor, analyze the first reverse mismatch where the cross-content signed gap changes from positive to negative. Use the exact `T_h` additive step, the divisibility/coprimality split modulo `h`, the boundary symmetry `+E -> -E`, and the complementary full-D remainder data to derive either a contradiction, a forced bounded carry, or a new support-independent invariant.

Concrete starting equations:

`delta_0=E`,
`delta_a=-E`,

same parity:
`delta' = delta/2` or `3delta/2`,

first mismatch (physical odd / shadow even):
`delta' = P + (delta+h)/2 >0`,

first possible crossing mismatch (physical even / shadow odd):
`delta'=(delta-2S-h)/2<0`.

At every phase:

`physical == 0 (mod h)`,
`gcd(shadow,h)=1`,

so `delta != 0`.

Secondary target if the crossing route stalls:

> In the `epsilon=0` branch at the first external survivor, combine rankwise row dominance, true `D0|U+V`, state swap `R<x`, and the inherited all-prefix nonnegative-defect theorem to seek a row-equality theorem. RL21 remains the required negative control.

Keep two frontiers explicitly separate:

- internal-only: `ell>=190537`;
- conditional external certificate: `ell>=49,547,666,544`, first above-side survivor `(217976794617,137528045312)`.

Gate A remains open.
Gate B remains open.
Global positive non-trivial-cycle exclusion remains open.
No Collatz conjecture claim is made.
Lean formalisation remains separate.
