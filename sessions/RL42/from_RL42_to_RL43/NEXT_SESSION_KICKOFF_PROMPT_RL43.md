# RL43 next-session kickoff prompt

Continue the Collatz R-sharp / RL research from the attached `Collatz_Rsharp_RL42_to_RL43_Handover_2026-08-22.zip` as a skeptical research mathematician.

Start by reading `START_HERE_RL43.md`, `RL42_PROOF_STATUS_AND_RL43_ATTACK.md`, `RL42_REPRODUCIBILITY_LEDGER.md`, and `RL42_DEPENDENCY_MAP.md`. Then run `bash verification/run_all_rl42_verifiers.sh` and verify `SHA256SUMS.txt`. Any failure is a stop-and-repair event.

Treat the current independently reproducible frontier in the surviving near-resonant order-2 / `g=2` balanced-return branch as `rho>=49`, with the following status split:

- the ordered-rank transport-efficiency result `rho>(45/4)G`, hence `rho>=46`, is analytic;
- the upgrade to `rho>=48` uses a bounded exact certificate excluding physical crossing excursions with `e<=3` in the contradiction range;
- elimination of `rho=48` is an exact finite certificate over a rigorously reduced equality case;
- RL remains open and the `g=2` branch remains open.

Do not reconstruct the old RL41 area-26/27 billion-scale tables unless a retained verifier or dependency audit reveals an actual need. RL42 has removed them from the current frontier.

Work on two linked tracks.

**Track A — attack rho=49.** Use the excess decomposition. Only `(P,E)=(45,4),(44,5),(43,6)` remain at this layer, with `P_+>=43`. Prefer a symbolic/parametric classification of crossing-capable `e=4,5,6` local families and a safe-superset boundary DP. Handle the at-most-two units of negative moved mass explicitly. Do not claim any exploratory sparsity observation until reproduced and frozen. If `rho=49` is eliminated, write a standalone theorem/certificate note, verifier, and retained run before using `rho>=50` downstream.

**Track B — push the scalable bridge.** The main RL42 quantity is `M_eff=sum_+(1-2^-delta_m)` with `M_eff>=3(z+1)G/z^2`, equivalently `P_+-sum_+2^-delta_m>=3(z+1)G/z^2`. Combine this with the inherited endpoint-loss / packing / correction-product machinery to close the distortion-versus-concentration loophole. Seek a sparse/dense dichotomy that works across the next continued-fraction gate, rather than merely increasing `rho` by finite enumeration.

Maintain a strict ledger distinguishing analytic proof, inherited theorem, exact finite certificate, and exploratory evidence. RL is not solved unless every surviving branch is closed with an auditable dependency chain.
