# Collatz R-sharp RL42 -> RL43 handover

Date: 2026-08-22

## Current audited frontier

In the surviving near-resonant order-2 / `g=2` balanced-return branch, the current verifier-backed frontier is

> **`rho >= 49`.**

This is not a proof of RL or of the Collatz conjecture. RL remains open, and the surviving `g=2` branch remains open.

The key RL42 advance is a scalable ordered-rank transport-efficiency bridge. It first proves `rho >= 46` analytically, then a bounded crossing-excess certificate raises this to `rho >= 48`, and an exact reduced boundary DP eliminates `rho=48`, yielding `rho >= 49`.

The old RL41 area-26/27 transient searches are no longer needed for this frontier. They are retained only in the inherited provenance bundle.

**Start with `START_HERE_RL43.md`.**
