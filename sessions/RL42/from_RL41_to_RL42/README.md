# Collatz R-sharp RL41 -> RL42 handover

Current working checkpoint: `rho>=28` in the surviving near-resonant order-2 / `g=2` branch.

**Start with `START_HERE_RL42.md`.**

Important: RL remains open. The `g=2` branch remains open. The inherited `rho>=18` theorem is fully verifier-backed. RL41's completed area-26/27 computations support the working `rho>=28` checkpoint, but some large transient search artifacts were not retained; reconstruct/rerun those certificates in RL42 before calling `rho>=28` fully artifact-audited.

Primary next target: the RL38/RL39 distortion-versus-concentration bridge, not merely another constant-area increment.
