# RL191 target — phase-46 exceptional transition and H21 early-core capacity

Date: 2026-08-30

## Authoritative objective

Continue the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from RL190.

RL190 proves:

- consecutive extremal `{35,36,37}` triple terminals are separated by at least 46 phases;
- if a pair occurs exactly 46 phases apart, the first terminal rank is necessarily one of
  `{90789138715,101129528126}`;
- `N_35<=9168536354`, so at least `906638145` clean starts have `tau<=34`;
- the dangerous height-21 `{33,34,35}` charging co-owner has necessary joint terminal-rank core
  `D=[23369453298,41775866136]` and exact block span 36;
- ordinary absolute corrected-flow remains inherited `>443`.

## Preferred theorem

Close or further collapse the **phase-46 exceptional seam**.

At `r=90789138715`, the required nine-transition path hits the ordinary mechanical-switch source at offset 3 and the carry source at offset 4.

At `r=101129528126`, it hits the ordinary mechanical-switch source at offset 8.

Seek, in order:

1. the exact physical pair-gap/numerator transition law across these exceptional sources and compare the resulting `t+9` gap with the required `2^37` or `2^38`;
2. a terminal-prefix, ternary-reset, parity, height, or p-shift ownership contradiction at either exact rank;
3. a proof that separation 46 is impossible, which would permit the next spacing/density update.

## Parallel charging target

Use the dangerous H21 core

`D=[23369453298,41775866136]`

and exact block span 36 to obtain a physical incidence/sign/spacing theorem for `{33,34,35}` terminals.

A valid charging improvement must be safe against every co-owned terminal family and recompute the guaranteed corrected-flow floor exactly. Merely observing that `D` is early-phase or disjoint from the extremal core is insufficient.

## Do not

- assume an exceptional transition obeys the ordinary/common-mechanical affine law;
- treat either phase-46 candidate rank, `D`, or `E` as a physical existence set;
- substitute auxiliary `3^-G` for physical `2^G` corrected flow;
- turn total variation into chronological K excursion without a sign-order theorem;
- claim isolated-triple, branch, Gate A/B, non-trivial-cycle, or Collatz closure without a genuine contradiction.
