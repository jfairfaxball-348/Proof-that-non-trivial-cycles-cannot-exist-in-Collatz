# RL158 completion review

RL158 closes the bare dense resultant as a standalone ownership-discrimination route. An exact valid-grammar example has resultant exactly `D=1631` while the physical phase gives residue `17`. The failure is explained by different nontrivial eighth-root twists modulo `7` and `233`.

A stronger exact interface survives: the physical phase is the simultaneous root of `2T^L-1` and `3T^(A-L)-2`, and the resultant of those two binomials has absolute value `D`. This is the RL159 target. No singleton or cycle exclusion is claimed.
