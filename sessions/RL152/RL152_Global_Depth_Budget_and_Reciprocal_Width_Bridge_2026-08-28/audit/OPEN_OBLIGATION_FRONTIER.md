# Open obligation frontier after RL152

Frozen live results:

- RL135: `h_j>=-1` through `g=771,316,334,039`, and `h_j>=0` through `g<=6`.
- RL136: all negative phases are isolated through `g=320,125,202,432`.
- RL148: exact-depth negative entry fibres have `2^(d+2)` spacing.
- RL149/RL150: local shell-depth and separated-template routes do not supply
  cross-excursion depth/order control.
- RL151: `Q_-<=3Q/5`, the multiplicity-independent isolated-range state
  ceiling, and reciprocal predecessor ownership.
- RL152: `sum d_j<=g(A-L)` and the direct physical bridge
  `W>9M_- -11 = 6Q_pre -11`.

Frozen negative-defect dependency:

- no theorem forces a useful lower bound on negative mass/population or
  exact-depth concentration; without that input the RL152 bridge is not a
  contradiction.

Live next branch:

- RL153 direct `g=1` primitive-owner attack.

Still open:

- `g=1`;
- higher nonnegative/mixed-height configurations generally;
- negative-defect configurations beyond the frozen dependency;
- Gate A and Gate B globally;
- global nontrivial-cycle exclusion;
- Collatz.
