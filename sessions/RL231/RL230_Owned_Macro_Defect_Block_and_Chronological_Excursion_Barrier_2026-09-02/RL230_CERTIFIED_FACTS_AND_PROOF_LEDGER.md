# RL230 certified facts and proof ledger

Date: 2026-09-02.

## Classification

RL230 closes with **success class C — decisive owned-macro / renewal-interface barrier**, together with one new multiscale physical-defect incidence theorem.

All new mathematical statements below remain internal to the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` unless explicitly described as a method/scope statement.

## Frozen inherited state

- Necessary terminal-rank frontier: `13,415,865,870`.
- e=4 terminal rank `31,435,476,727` remains live with `3,856,660,232` combined arithmetic survivors through transition 43.
- RL181 K corridor: `128,081,997,553 < K_i < 146,795,909,391`, with ordinary drift `K_(i+1)-K_i=f_i/3`.
- RL181 shallow-shallow p-edge floors for endpoint cutoffs `k=1,2,3,4`:
  `10,075,174,578`, `25,683,594,700`, `32,081,399,276`, `35,000,224,336`.
- RL184 zero-defect recurrence on clean common-mechanical transitions:
  `2^d C' = 3C`, `d>=1`.
- RL184 physical nonzero-defect floor: `251,879,363`.
- RL187 exact first-defect ownership / leakage-free charging remains authoritative; ordinary absolute corrected flow is `>443`.
- RL180 mechanical mass interface: `W < 28,070,867,755`, with base envelope `2^(-r/L) <= omega_r`.
- Gate A, Gate B, the sole high branch, and global non-trivial-cycle exclusion remain open.

## RL230.1 — multiscale shallow-corridor defect incidence

**Class:** proved analytic mathematics + exact integer certificate.

Let `E_k` be the inherited number of directed p-shift edges with both endpoint heights at most `k`, for `k=1,2,3,4`. Put `n_k=k+38`.

For an ordinary p-gap with endpoint maximum `M<=k`, write its normalized gap as `C/2^M`. RL181 gives the strict normalized-gap ceiling

`C/2^M < 293,591,818,782 < 2^39`,

hence `0<C<2^(k+39)=2^(n_k+1)`.

If the first `n_k` clean common-mechanical successor transitions all have zero defect, then RL184's homogeneous recurrence gives `2^(sum d_j)|C`, with `sum d_j>=n_k`. Therefore the only possible initial numerator is `C=2^n_k`, every `d_j=1`, and the terminal numerator is `3^n_k`, which is odd. The inherited parity sensor then forces the terminal p-edge defect to be nonzero.

Thus every clean corridor of `n_k+1=k+39` consecutive p-edges starting from an `h<=k` p-edge contains a nonzero defect.

After removing at most one carry edge and at most `n_k` possible future carry crossings plus `n_k` possible mechanical-switch crossings, the certified clean-corridor floors are:

- `k=1`: `10,075,174,499` length-40 corridors;
- `k=2`: `25,683,594,619` length-41 corridors;
- `k=3`: `32,081,399,193` length-42 corridors;
- `k=4`: `35,000,224,251` length-43 corridors.

A fixed physical p-edge belongs to at most the corridor length many such windows. Therefore the distinct physical nonzero-defect floors are:

- `k=1`: `251,879,363`;
- `k=2`: `626,429,138`;
- `k=3`: `763,842,838`;
- `k=4`: **`813,958,704`**.

The `k=1` row reproduces RL184; the `k=2,3,4` rows are new.

## RL230.2 — phase-uniform block-amplitude scale

**Class:** proved arithmetic/scope theorem + exact integer certificate.

The inherited K corridor has full width

`Delta_K = 146,795,909,391 - 128,081,997,553 = 18,713,911,838`.

A block theorem intended to force a one-sided K escape **from an arbitrary allowed block-boundary K value** must force corrected-flow displacement exceeding

`3 Delta_K = 56,141,735,514`.

Every ordinary defect obeys `|f_i|<1`. Therefore such an arbitrary-boundary, one-pass escape cannot be guaranteed from fewer than

**`56,141,735,515`**

distinct same-sign ordinary defects, even under the deliberately optimistic assumptions that every selected defect is distinct, coherently signed, and nearly maximal.

Scope lock: this is a robust arbitrary-boundary block threshold. It is **not** promoted as a necessary count for an excursion starting from the fixed physical `K_0`, from a favorable boundary value, or from a theorem that couples several blocks through their boundary states.

## RL230.3 — shallow-density requirement for that robust one-pass route

**Class:** proved analytic combinatorics + exact integer certificate.

RL181 gives at least `2N-L` shallow-shallow directed p-edges from a shallow population of size `N`, where `L=137,528,045,312`.

Even if every such edge were an ordinary, distinct, same-sign, nearly unit defect, the RL230.2 robust one-pass threshold would require

`2N-L >= 56,141,735,515`,

hence

**`N >= 96,834,890,414`.**

This is a route requirement under the maximally favorable one-pass model, not a universal physical shallow-population theorem.

## RL230.4 — RL180 base-envelope saturation barrier

**Class:** exact method barrier + integer certificate.

To force `N>=96,834,890,414`, the number of phases above a chosen shallow cutoff would have to satisfy

`M <= 40,693,154,898`.

The RL180 mechanical-mass proof method lower-bounds the mass of `M` high phases by placing them at the `M` smallest base-envelope weights `2^(-r/L)` and multiplying by the high-height factor `1-2^(-H)`.

Make this method as strong as possible by replacing that factor by its limiting value `1`. For the `M=40,693,154,898` smallest base weights, every selected index satisfies

`r >= 96,834,890,414 > 7L/10`.

Therefore every base weight is `<2^(-7/10)<5/8`, and the entire lower-envelope expression is strictly below

`5M/8 = 25,433,221,811.25 < 28,070,867,755`.

The exact inequalities are

`10*96,834,890,414 > 7L`,
`128*5^10 > 8^10`,
and
`5M < 8*28,070,867,755`.

Consequently the inherited RL180 **base-envelope mechanical-mass interface**, by merely raising the height cutoff, cannot certify the shallow population required by RL230.3. This is a barrier for that proof interface; it is not a theorem that the physical orbit has too few shallow phases.

## RL230.5 — exact missing invariant

**Class:** strategic/method conclusion, not a new Collatz theorem.

The strengthened physical-defect incidence is still unordered. RL187 supplies exact first-defect co-ownership and strong absolute/directional variation, but no inherited theorem couples consecutive owned defect blocks in chronological order strongly enough to stop compensation.

The p-rank height-cycle threshold-balance identities cannot be silently substituted for a chronological renewal theorem: the owned zero blocks are chronological objects, while the inherited balance identity lives in the p-shift height cycle.

The next useful consumer must therefore control at least one of:

1. chronological return/renewal of a controlled block-boundary state;
2. sign order or sign-run length across consecutive owned defects;
3. a boundary identity whose terms cannot absorb accumulated corrected flow;
4. a fixed-origin (`K_0`) prefix theorem that bypasses the arbitrary-boundary full-width threshold.

## Corrections / demotions

No inherited certified theorem is corrected or demoted.

One scratch-level wording is tightened before promotion: the `56,141,735,514` flow scale is the threshold for a **phase-uniform arbitrary-boundary one-pass block escape**, not a universal necessary condition for every possible K excursion.

## Global status

No terminal rank is deleted. The frontier remains `13,415,865,870`; e=4 remains live with `3,856,660,232` combined arithmetic survivors through transition 43. The sole high branch, Gate A, Gate B, and global non-trivial-cycle exclusion remain open.
