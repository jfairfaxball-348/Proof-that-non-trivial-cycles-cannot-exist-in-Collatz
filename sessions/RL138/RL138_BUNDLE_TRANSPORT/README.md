# RL138 bundle transport

Run `python3 reconstruct_rl138_bundle.py` to verify the line-wrapped ASCII
transport, decoded ZIP hash, size, and member CRCs. The root sidecar records
the same verified ZIP hash.
