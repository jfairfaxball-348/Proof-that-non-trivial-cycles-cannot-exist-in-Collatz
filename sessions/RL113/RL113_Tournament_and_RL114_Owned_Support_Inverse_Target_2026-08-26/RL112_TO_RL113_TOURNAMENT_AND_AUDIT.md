# RL112 to RL113 — tournament, audit, and route order

Date: 2026-08-26

## Outcome and scope

RL113 ran the twelve attacks required by the RL112 target against the live mixed-base obstruction. The RL112 outer sidecar, internal manifest, fresh-unpack manifest, and structural verifier passed at `BASE_HEAD` `ffa2e9a5da71b0fa170963e652ffe1d8951c0db9`.

The inherited audit is retained without alteration: Gate A is closed only for even terminal `k` and certified `k<=25`; odd terminal `k>=27`, Gate A globally, Gate B globally, nontrivial-cycle exclusion, and Collatz closure remain **open**.

The winner is not a theorem. It is selected as the next research route because it directly seeks the missing owned restriction on the exact RL109 sparse multiple after RL110 excluded unrestricted geometry.

## Live interface and controls

For selected owned rotations of a hypothetical primitive ordinary shortcut cycle, RL109 supplies `0 != S = sum_j epsilon_j 2^(a_j)3^(b_j) = D(x_v-x_u)`, with `D=2^A-3^L`, `D|Q(u)`, `D|Q(v)`, and canonical swap count `R_*`.

RL110 proves fixed-content diameter and maximum legal coefficient cannot yield `|S|<D` for `R_*>=4`. A route must add an ordinary-owned fact beyond this equality.

The mandatory controls remain: RL20 has radius four but `D` does not divide its numerator; RL79 supplies only `D|sS` for generalized increment `s`; RL81 forbids quotient-as-physical-state without actual `D|Q`; primitivity supplies `S!=0`; Raw/Farey work remains branch-scoped.

## Tournament decisions

| Order | Attack | Decision and precise first obligation |
|---:|---|---|
| 1 | Owned-support inverse theorem | **Winner; retained.** Classify two-/three-term near-extremizers in the ordered canonical support using `D|Q(u)=D|Q(v)=0`, then force a repeated rotation or primitivity violation. Stop if it becomes an all-legal-support bound or `S=D(x_v-x_u)`. |
| 2 | Dyadic–triadic Fourier discrepancy | Retained. Define a zero-flow-cut character statistic that changes at the RL20 ownership failure and gives a strict selected-support mass loss. Stop if it holds for every fixed-content word. |
| 3 | Two-base finite transducer | Retained. Give a finite state map preserving variable `D|Q`, with RL20 rejected by ownership rather than local radius. Stop on an unbounded modulus/state requirement. |
| 4 | Effective S-unit gap with legality | Retained. Isolate a legal support-coordinate constraint not implied by `|A log2-L log3|`; otherwise stop before using a generic logarithmic estimate. |
| 5 | Modular-expander lifting | Retained. Prove a deterministic selected-support prime/order inequality unavailable to arbitrary subsets. Stop if only a density heuristic remains. |
| 6 | p-adic transfer operator | Retained exploratory route. Define a finite conditioned owned-rotation level with a non-random stationary law. Stop if stationarity is heuristic. |
| 7 | Entropy-increment rigidity | Demoted. First derive entropy loss from full ownership rather than radius. Stop if the claim survives the RL20 fake. |
| 8 | Geometry-of-numbers transference | Demoted. Build a lattice containing `D|Q` as a real condition and show the RL20 discriminator. Stop on a slope-lattice restatement. |
| 9 | Non-homogeneous `+1` cocycle | Retained only as diagnostic. Write an unnormalized identity with independent size force. Stop if it normalizes to RL79. |
| 10 | Symbolic-dynamical specification failure | Demoted. Derive an extendability obstruction from `D|Q`; stop if it is local grammar only. |
| 11 | Mahler/automatic-sequence obstruction | Parked. Produce the actual ownership-preserving functional equation; stop if it encodes only the binary word. |
| 12 | Counterexample-guided strip invariant | Parked as discovery only. Declare state space and range; stop if empirical or if it fails either RL20/RL79 discriminator. |

This is an ordering of research dependencies, not a probability statement or a claim that any retained route is true. A route may not be upgraded without its stated obligation and all mandatory red teams.

## Exact finite audit

RL113 reran inherited exact checks. RL109 checked 4,097 swap identities, 12,264 generalized-scaling identities, 8,513 zero-flow-cut pairs, 2,026 rotation-radius identities, and the exact length-184 RL20 fake. RL110 exhausts primitive canonical necklaces of lengths 8 through 17 with positive `D` and radius four: 1,580 necklaces and 27,866 closest pairs, with zero `D`-divisible closest pairs.

That finite range is gap-free only under those stated conditions. It is evidence and a red-team certificate, not a global cycle exclusion or a test of the unobserved owned-cycle premise.

## Fallback and return policy

If route 1 closes, fails its first obligation, or is blocked by a proved barrier, begin the highest remaining displayed route whose obligation has not already failed. Preserve the entire tournament ledger and the RL112 audit; do not rerun a failed route under a new name without a recorded new input.

If all retained new routes close, block, or fail, return in this order: seek a genuinely global owned bridge to the closed primitive/full-`D` radius-three engine; seek a new global consumer for the Gate-A multiplicity/Farey material while preserving Raw/first-Farey scope; then revisit Gate A/B only with a new structural physical-strip or full-ownership theorem. Do not restart generic radius ladders, local grammar, normalized coboundaries, or bulk cascade scanning without that trigger.

## Classification and correction ledger

- New analytic theorem: **none**.
- New exact finite certificate: **none**; inherited certificates were rerun.
- New result: an auditable strategic ordering and explicit stop/fallback rules.
- Correction/demotion: entropy, symbolic specification, and geometry-only formulations are demoted because they have not yet consumed full ownership; no inherited theorem is altered.
- Open obligation: the route-1 two-/three-term owned near-extremizer lemma.
