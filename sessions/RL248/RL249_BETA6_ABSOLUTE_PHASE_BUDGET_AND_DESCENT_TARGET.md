# RL249 target — beta=6 absolute phase budget and descent compensation

Status: **PREPARED, NOT STARTED**.

## Frozen incoming frontier

RL248 closes as `R4_BRIDGE_REDUCED` under the audited RL247 Radius-4 scope.

Every simultaneous Gate-A/Gate-B survivor has `beta(P)>=6`. Branch A/B/C remain the RL245 trichotomy. Radius 4 still requires the full positive/full-D/primitivity/exact cyclic-distance/same-root/full-phase eligibility audited in RL247.

For Branch C, RL248 additionally freezes:

1. at least `beta(P)-2` shifted singleton `10` edges are wholly internal;
2. the companion physical envelope satisfies
   `beta(E)>=4(beta(P)-2)+H_I`, with `0<=H_I<=H`;
3. event count `e_I` obeys `H_I>=e_I`;
4. internal zero count `m=(a-ell)-k+2` satisfies the exact finite-certificate bound `m>=17`, hence
   `a-ell>=k+15`; with the inherited simultaneous-survivor `k>=31`, `a-ell>=46`;
5. the exact normalized prefix-scale cocycle is
   `F_(i+q)/F_i=3^(P_i-2/a)`, equivalently
   `G_(i+q)/G_i=3^(aP_i-2)`;
6. for `b=beta(P)`, every physical prefix scale satisfies
   `S_i>3^(-(b+2))`;
7. each wholly internal singleton `01` block has phase gain at least `(2/3)S_entry`, and each internal `10` block at least `(1/3)S_entry`;
8. when `beta(P)=6`, at least three disjoint internal `01` blocks therefore have aggregate phase gain
   `DeltaPhi_selected_01 > 2/6561`.

RL246's old uniform single-packet `>1/4374` statement remains demoted. The RL248 aggregate `b=6` theorem is differently scoped and must not be conflated with it.

## Active mission

Attack **Branch C with `beta(P)=6` first**.

Use the absolute scale cocycle and the three forced positive internal `01` gains against the exact RL47/full-phase ordered-state ledger. Establish one of:

1. an exact contradiction to full-phase completion / terminal `(d,J)=(1,2^k)`;
2. a proof that compensating phase/rank motion necessarily creates explicit canonical `10` descent events whose accumulated height/rank contribution closes Gate A on this branch;
3. an eligible primitive positive/full-D exact cyclic-distance-4 self-rotation satisfying every audited RL238 hypothesis.

If compensation is possible, classify it exactly: which selected shifted `10` blocks must be event arms, their heights, their contribution to `H_I`, and their canonical residue/terminal constraints. Do not replace this with generic cancellation estimates.

If `beta(P)=6` is eliminated, propagate the bounded-counterflow scale/phase mechanism to the next `beta(P)` levels before returning to Branch A/B as needed.

## Forbidden shortcuts

- no inference from the `m=17` computational resource wall;
- no revival of the demoted single-packet absolute bound;
- no generic physical-scale anti-concentration independent of `beta(P)`;
- no `beta(E)`-alone Radius-4 inference;
- no local-shape-to-global-distance-4 shortcut;
- no bare `D|Q` invocation over signed/non-owned objects.

Gate A: open. Gate B: open. Radius 5: inactive.
