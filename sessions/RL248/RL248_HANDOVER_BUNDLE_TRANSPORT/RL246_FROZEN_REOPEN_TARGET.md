# RL246 frozen mathematical target — reopen only after the Radius-3/Radius-4 audit

Status: **FROZEN AT RL246 CLOSEOUT**.

This file preserves the mathematical attack that was active before the user-directed Radius-3/Radius-4 integrity audit.

After the audit reaches one of its required classifications and all mandatory repair/demotion work is complete, return to this target only if its dependencies remain valid.

## Frozen frontier

Every simultaneous Gate-A/Gate-B survivor still has `beta(P)>=6` and Branch A/B/C geometry from RL245.

RL246 additionally proves for Branch C:

- at least one singleton q-shift `01 <-> 10` signature lies wholly inside the canonical internal path;
- it has an exact factor-three q-shift physical prefix-scale notch;
- it produces a positive RL47 phase packet;
- at its shifted `10` boundary either a canonical even-J mismatch/descent event occurs, or the genuine companion physical envelope is exactly `(-d,-d-1,-d)` with exact J mod-3 fingerprints recording height parity.

## Reopen target

Consume the remaining rigid arm, and then Branch A/B as needed, by proving either:

1. an eligible primitive positive/full-D exact cyclic-distance-4 self-rotation satisfying **every Radius-4 hypothesis as repaired/audited by the intervening audit**; or
2. an exact contradiction to full-phase extendability using the canonical `(d,J)` automaton, terminal `J=2^k`, ordered rank/phase selectors, or another exact global-state identity.

Do not reopen using pre-audit Radius-3/Radius-4 wording if the audit changes its scope or dependencies.

Gate A and Gate B remain open. Radius 5 remains inactive.
