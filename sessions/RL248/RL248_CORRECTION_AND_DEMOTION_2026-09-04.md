# RL248 correction and demotion ledger

Date: 2026-09-04
Classification: closeout safety ledger.

## C248.1 — RL246 uniform absolute single-packet bound

The historical RL246 scratch statement

`DeltaPhi_packet > 1/4374`

uniformly for one singleton packet remains **DEMOTED / NOT PROMOTED**.

RL248 proves a new exact bounded-counterflow scale cocycle and, specifically for Branch C with `beta(P)=6`, an aggregate lower bound from at least three disjoint internal `01` blocks:

`DeltaPhi_selected_01 > 2/6561 > 1/4374`.

This is a new conditional aggregate theorem and must not be cited as reinstating the old uniform single-packet claim.

## C248.2 — m=17 resource boundary

The unrestricted exact run-compressed graph was fully enumerated only through internal zero budget `m=16`. An attempted `m=17` expansion exceeded the live-session memory/resource envelope after producing a very large pre-dedup candidate stream.

Disposition: **NOT EVIDENCE / NOT PROMOTED**.

Only the exact gap-free `m<=16` certificate is promoted.

## C248.3 — local Radius-4 shapes and large physical counterflow

The exact companion-envelope amplification `beta(E)>=4(beta(P)-2)+H_I` and the rigid local magnitude topology `(1,2,1)` remain physical/local information only.

Disposition: **NO RADIUS-4 APPLICATION** unless the actual candidate is proved primitive, positive/full-D/full-phase owned, and at exact global cyclic adjacent-transposition distance 4 with every audited RL238 hypothesis.

## C248.4 — fixed-form q-window regression

The exact word

`u=1101010111010101101100111101001101111100101`

with `(a,ell,q,r)=(43,27,27,17)` has abstract Branch-C q-window geometry and `beta(P)=6`, but its canonical path fails legality at internal step 4 at `(d,J,u)=(1,-6,1)`.

Disposition: permanent **red-team countermodel to word-geometry-only arguments**, not a retained full-phase object.
