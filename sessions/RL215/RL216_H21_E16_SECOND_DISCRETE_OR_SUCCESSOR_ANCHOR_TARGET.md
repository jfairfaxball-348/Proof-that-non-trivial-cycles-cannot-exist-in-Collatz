# RL216 target — second discrete/absolute consumer for the e=16 two-sided family

Date: 2026-09-01.

Accept all RL215 results and inherited scope locks.

## Primary target

RL215 leaves exactly **331,935,285** bounded arithmetic e=16 root candidates after
the terminal filter, spread across all 45,046 H21 prefixes.

Add a genuinely independent **discrete or second-phase** datum:

1. Analyze the complementary return numerator `P_z` in
   `2^(A-u)y_0=3^z(y_0+K0)+P_z`.
   Seek a residue, valuation, divisibility condition, or canonical subarc cut that
   uses complete-word/least-state/global information not already encoded by the
   e=16 root prefix.
2. In parallel, revisit the open quotient residue only with genuinely new input:
   determine a restriction on `Q(d) modD^2`, equivalently `(Q/D) modD`.
3. If those do not bite, prove a new e=16 terminal/successor anchor (at `p+50` or
   the actual successor phase) and only then deploy signed successor-K. RL209's
   backward-root anchor is not inherited there.
4. Project any result onto
   `y_0=y_0^*+3*2^58*k` using modular/range counting, not naive enumeration.

## Success criterion

Delete at least one exact e=16 prefix, H21 state, mod18 class, or the terminal rank
`34,124,151,203`; otherwise prove the precise no-go scope of the attempted consumer
and hand off the strongest remaining route.

## Locks

Do not count a sharper real-valued approximation of the RL214/RL215 interval as a
new independent consumer unless it changes the exact progression population.
Do not treat `Q modD=0` as a quotient residue.
Do not revive the collapsed RL201 endpoint moment, saturated G56 continuation,
flat-K normalized-gap integrality, or terminal valuation alone.
Do not promote arithmetic candidates as physical words/populations.
Preserve all rank counts, RL206 corrections, below-p `37/60/97`, p-shift carry,
and physical/charge/Gate/global locks unless an exact new proof changes them.
