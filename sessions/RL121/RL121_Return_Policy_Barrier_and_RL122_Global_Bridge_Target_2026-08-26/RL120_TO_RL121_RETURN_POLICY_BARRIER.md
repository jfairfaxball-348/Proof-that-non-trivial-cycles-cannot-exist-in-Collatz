# RL121 — canonical-registry return-policy audit and exact open-interface barrier

Date: 2026-08-26

## Outcome and classification

RL121 executes the RL120/RL113 return policy against the canonical RL112 lemma/result registry and the subsequent RL113–RL120 route ledger.

**No new analytic Collatz theorem is promoted.** The new result is an **exact proof-state / registry audit barrier**: no currently established analytic theorem or exact certificate supplies any of the three missing interfaces authorized by the return policy.

Global status therefore remains unchanged:

- primitive/full-`D` radius-three engine: **closed local theorem**;
- Gate A even terminal `k`: **closed analytic**;
- Gate A terminal `k<=25`: **closed exact-certificate corollary**;
- Gate A odd terminal `k>=27`: **open globally**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 1. Return-policy item 1 — global owned bridge to radius three

The canonical registry contains the closed radius-three engine, but labels it local and usable only after a genuinely global radius reduction. The strongest later global interface is RL109: for a canonical closest pair of distinct owned rotations,

`0 != S = Q(v)-Q(u) = D(x_v-x_u)`, with `D|S`.

RL109 does not prove `R_*<=3`. RL110 proves that unrestricted fixed-content diameter and termwise coefficient bounds are too weak on `R_*>=4`. RL114–RL120 then stop the retained inverse, Fourier, transducer, S-unit/prime, modular, p-adic, and cocycle formulations under their stated obligations.

**Audit verdict:** no established registry result manufactures every input of the primitive/full-`D` radius-three engine for an arbitrary hypothetical cycle. The required global owned-radius bridge is absent.

## 2. Return-policy item 2 — global consumer for Gate-A multiplicity/Farey material

The registry explicitly classifies RL89–RL100 cascade exclusions as exact first-Farey, raw `g=1`, full-phase results. RL101 adds genuine multiplicity-cylinder and primitive-boundary theorems, and RL106 adds an aligned all-even-run exclusion, but all retain first-Farey/raw-multiplicity scope. RL102–RL108 record barriers to the tested content, basin-equality, and word-local consumers.

No established theorem proves that every Gate-A violation has the first-Farey/raw-`g=1` parameters, proves a finite universal complement, or supplies a global theorem that consumes those branch certificates without strengthening their scope.

**Audit verdict:** the branch-to-global Gate-A interface remains absent. The finite cascade and Farey certificates cannot be promoted beyond their recorded Raw/first-Farey scope.

## 3. Return-policy item 3 — new physical-strip or full-ownership theorem

The registry repeatedly identifies the missing resource as an independently useful ordinary-owned quantitative restriction: a physical strip, support-selection theorem, absolute state-size force, or another full-ownership statement not equivalent to rotation transport or generalized-increment scaling.

RL110 proves the geometry-only sparse-support baseline cannot supply that force. RL111 freezes the direct owned-support-selection attempt without a solution. RL113 ranks new formulations; RL114–RL120 close all retained formulations as method barriers, ending with the affine-cocycle scaling barrier.

**Audit verdict:** no current established result is the required physical-strip/full-ownership theorem. Rephrasing any stopped formulation is not a new input.

## 4. Exact interface-separation table

| Required bridge | Strongest established nearby input | Exact missing edge | RL121 verdict |
|---|---|---|---|
| Global owned radius-three access | RL109 owned closest-pair sparse `D`-multiple | force `R_*<=3` or otherwise manufacture the full radius-three input contract | absent |
| Global Gate-A Farey consumer | RL101 multiplicity compression + RL89–RL100 branch certificates | universal multiplicity/Farey coverage or a theorem consuming all complementary branches | absent |
| Structural physical strip / ownership | RL109 physical owned endpoints + RL110 support barrier | independent ordinary-owned quantitative bound not equal to `S=D(x_v-x_u)` and not generalized-scale invariant | absent |

The table is a proof-state statement about the canonical registry, not a theorem that such bridges cannot exist.

## 5. Mandatory red teams

- **RL20:** local geometry cannot be promoted to ownership; the exact radius-four fake remains rejected only because `D` does not divide its numerator.
- **RL79:** any candidate invariant under generalized increment scaling remains insufficient; no factor `s` is cancelled.
- **RL81:** quotient labels are not physical states without actual full `D|Q` ownership.
- **Primitivity:** used only for distinct rotations/states and nonzero owned differences; it does not itself force radius three.
- **Raw/Farey scope:** every first-Farey, raw-`g=1`, or finite cascade result retains its exact branch qualification.
- **Finite work:** the primitive-necklace and other finite scans retain only their stated bounded ranges.

## 6. Return-policy barrier

The three authorized return directions all terminate at an **interface mismatch**, not at a contradiction:

1. the radius-three theorem has a strong consumer but lacks a global producer of its hypotheses;
2. the Gate-A Farey/cascade machinery has strong branch conclusions but lacks a universal selector/coverage theorem;
3. the global owned sparse interface has exact divisibility but lacks an independent physical/support size force.

Therefore RL121 must not claim Gate closure and must not restart any stopped RL113 formulation unchanged. The correct frozen frontier is to require a genuinely new theorem that crosses one of these interfaces.

## 7. Correction/demotion ledger

No inherited theorem is demoted or repaired by RL121. The only classification action is negative: no canonical registry entry is upgraded into a return-policy bridge.
