# RL307 lossless transport note

The successor can resume without conversation history by reading:

1. `authoritative/START_HERE.md`;
2. `authoritative/RL308_NEGATIVE_FRONTDOOR_FIXED_RESIDUE_SCALAR_GATE_A_TARGET.md`;
3. this session's `RL307_CLOSEOUT.md`;
4. `RL307_REPORT.md`;
5. `RL307_PROOF_AND_SCOPE.md`;
6. `RL307_RED_TEAM.md`;
7. `RL307_SCRATCH_FREEZE.md`;
8. `verification/verify_rl307_closeout.py` and `RL307_FRESH_VERIFICATION.txt`.

The incoming RL307 target is preserved byte-for-byte as `RL307_INCOMING_TARGET.md`.

The fixed-96 P/Q work remains intentionally frozen. The main live frontier is now the two fixed scalar residues `X=(4,43)` and `Y=(6,504)` inside the exact `(-28)` dependency reduction, with the coarse R3/D0 shell contractions preserved for reuse.
