# RL226 target — e=4 dyadic-cylinder compression and candidate-coupled height closure

## Mission

Continue from RL225's exact finite e=4 candidate family and mandatory-height certificate. The primary objective is to compress the exact surviving dyadic cylinders beyond transition 41 and push the deterministic nonnegative-height test far enough to delete terminal rank **31,435,476,727**, if possible.

## Binding incoming data

Use the RL225 reduced interface and verifier as authority:

- `eta=207+243k`;
- `y_0=1,267,492,570,907 + 1,649,267,441,664 k`;
- `15,106,005,985 <= k <= 18,969,385,559`;
- raw family **3,863,379,575**;
- terminal-Hensel deletion **1,842**;
- height deletion through transition 41 **1,988,460**;
- exact combined remainder **3,861,389,273**;
- e=4 terminal rank **31,435,476,727** remains live.

## Required method

1. Preserve exact candidate identity or an explicitly proved equivalence class that is sufficient to reconstruct the deterministic trajectory.
2. Do not enumerate billions of k values individually.
3. Replace the rapidly growing unmerged dyadic partition by exact state merging, a quotient/valuation automaton, symbolic congruence compression, or another proved lossless representation.
4. Keep terminal-Hensel filtering and height filtering correctly coupled; do not assume disjointness beyond transition 41 without proof.
5. Use only candidate-coupled deterministic continuation. RL223's free count-only language remains a forbidden relaxation for selection.
6. If complete e=4 closure is achieved, state the exact bridge from family emptiness to terminal-rank deletion and update necessary-rank counts by exactly one.
7. If compression provably stalls, identify the first exact state datum preventing further collapse and hand over a theorem-quality barrier/next consumer rather than raw brute force.

## Success criteria

Any one is sufficient:

- complete e=4 height-family emptiness and terminal-rank deletion;
- a substantially stronger exact finite contraction using a reusable compressed state representation;
- a theorem-quality lossless compression theorem that makes full replay tractable;
- a precise transfer barrier identifying the missing independent datum needed for closure.

Do not alter e=28,33,40,45 by analogy. Gate A, Gate B, whole-branch contradiction and global nontrivial-cycle exclusion remain open unless separately proved.
