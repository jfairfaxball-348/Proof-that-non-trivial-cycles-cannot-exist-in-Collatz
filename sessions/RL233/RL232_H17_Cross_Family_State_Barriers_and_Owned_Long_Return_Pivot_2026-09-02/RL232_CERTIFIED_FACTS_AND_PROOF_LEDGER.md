# RL232 certified facts and proof ledger

Date: 2026-09-02

## Promoted analytic mathematics

1. Conditional on the sole high branch and the inherited terminal-rank/K identity, the H17 K-compatible cores are:
   - A `[11,443,822,977,28,746,802,249]`;
   - B `[72,981,981,437,100,039,806,527]`.
2. Every H17-A terminal in that necessary core has `K>134,536,495,104`.
3. The H17-A exact owned-pair interface forbids four consecutive postterminal zero defects; immediate zero requires terminal difference `g=-1`.
4. The H17-B exact owned-pair interface forbids two consecutive postterminal zero defects; immediate zero requires terminal difference `g=+2` and the unique certified first interface.
5. On an ordinary zero-defect prefix, `C_(i+1)-C_i=-(1-alpha^-1)q_i`; either H17 zero prefix changes C by less than `2^-32`.
6. Disjoint H17 first-defect blocks alone imply only the coarse combined cap `floor(L/33)=4,167,516,524`.

## Exact finite certificates

1. Gap-free four-edge H17 return scan through terminal separation 1000: A→A, A→B, B→A, B→B all excluded by the carry-completed universal bound; combined H17 chronological spacing `>=1001`.
2. Exact rational K-corridor adjacent-boundary checks for the two H17 cores.
3. Complete depth-three owned-pair necessary-state graph for all 34 terminal height pairs in each H17 family.
4. Exact relaxed B→B return witness at separation `190,537`, rank displacement `-12,799`, and its scalar-capacity stress tests.
5. Exact rational moving-window saturation witness `Y_0=74,000,000,000`.

## Method barriers

1. Spacing `>=1001` alone gives incidence cap `137,390,654`, not `1,615`.
2. Family/rank/K plus generic universal error balls admit a very dense relaxed B-return chain.
3. Adding scalar K-speed and monodromy removes the near-full 721,792-event chain but still does not exclude a 655,001-event relaxed chain.
4. Local owned height/numerator propagation does not eliminate any of the 34 terminal height-pair classes through depth three.
5. Scalar positive moving-window state `(C,Y)` does not refine the inherited K corridor at the necessary-state level and changes only microscopically across the H17 zero prefix.

## Classification lock

- Necessary-state survivors are not physical realizations.
- Relaxed chains are method-barrier countermodels, not Collatz orbits.
- Mechanical terminal-rank order is not chronological order.
- Total variation is not prefix excursion.
- No new terminal rank is deleted.
- No sole-high-branch contradiction is claimed.
- Gate A and Gate B remain open.
- Global non-trivial-cycle exclusion remains open.

## Session classification

RL232: **Success C** under the exact RL232 success ladder.

Missing exact state/invariant:
`owned long-return endpoint/return-defect or higher-modulus/macro-reset state coupling successive H17 terminals`.

Successor:
`RL233_OWNED_LONG_RETURN_DEFECT_AND_HIGHER_MODULUS_TARGET.md`.
