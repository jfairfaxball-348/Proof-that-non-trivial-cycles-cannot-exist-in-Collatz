# RL283 closeout red-team note

Date: 2026-09-08

Result: **PASS**

This is a closeout proof-state/scope red team, not new research.

Checks:

1. **Gate scope.** RL283 does not claim Gate A. The authoritative residual remains `k>=25`, `k` odd, `H_can<k`.
2. **Preferred theorem scope.** `nu_2(J)<=H` for globally reachable positive even `d=1` checkpoints remains open. Finite `H<=22` survival is not promoted as a global theorem.
3. **Size-candidate status.** `J<=2^H` is recorded as falsified. It was only an incoming conjectural guide, so no inherited theorem is demoted.
4. **High-divisibility theorem.** The backward argument uses the cleared prefixes `P_i=2^iJ_i` and odd affine coefficient `c_i`; full `2^m` divisibility forces each earlier `2^i` divisibility. The conclusion is prefix integrality/parity legality only under the ordered depth-path hypotheses.
5. **Relaxation equivalence.** Ordered-rank high-divisibility searches are not represented as evidence from a broader dangerous class. A dangerous counterexample reconstructs a genuine legal path.
6. **Adjacent-swap barrier.** The exact witness `7706+486=8192` is used only to reject per-cell valuation-Lipschitz induction. It does not show Gate A is false.
7. **Zero-rank scalar dependency.** The integerized zero-rank identity is explicitly identified with `Q(J+1)=2S+D-12`, equivalent to the inherited `B+6=S+D/2`. It is not promoted as an independent invariant.
8. **Two-shadow scope.** `T=3^d C_x(-7)-C_y(-7)` is an exact affine representation. No monotone shadow potential or cycle exclusion is claimed.
9. **Terminal extension.** `X=x10^k`, `Y=y0^k1` has exactly one reversed final rank; the earlier ordered displacements sum to the inherited canonical height `H`. This is a reformulation, not an independent closure theorem.
10. **Bounded terminal regression.** The `379` exact terminals with extended length `L<=16` are regression evidence only. No residual contraction uses this bounded search.
11. **Noncrossing barrier.** The strengthened RL48 barrier is explicitly a theorem about a proof relaxation, not a counterexample to the real automaton.
12. **Boundary hazard.** The `Beta(n)` two-template formulation remains unproved as a global reachability bound and is not promoted as Gate-A progress beyond a reformulation.
13. **Inherited certificate economy.** The RL282 `H<=22` exact certificate is accepted as incoming authority and is not rerun by the compact RL283 verifier.
14. **No forbidden reopening.** Gate B, fifth selector, Radius 6+, boundary-cycle classification, and raw larger height-cap enumeration are not reopened.
15. **Successor isolation.** RL284 is an explicit user-directed exploratory pivot. It does not demote or overwrite the frozen Gate-A upstream programme.
16. **Prime overlay scope.** RL284 is instructed to drop primality cleanly if it provides no deterministic leverage beyond parity/valuation/factor structure.
17. **Global claim.** No global exclusion of non-trivial Collatz cycles is claimed at RL283 closeout.

No mathematical correction or demotion of inherited authoritative mathematics is required.

No promoted RL283 claim requires demotion.
