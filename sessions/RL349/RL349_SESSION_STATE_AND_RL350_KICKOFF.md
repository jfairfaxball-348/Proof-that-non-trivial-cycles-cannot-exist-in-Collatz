# RL349 session state and RL350 kickoff

Date: 2026-09-18
Status: RL349 CLOSED/FROZEN; RL350 prepared, not started.

## Frozen RL349 outcome

RL349 did not prove `O_75=empty`.

No Phase-4 residual class was eliminated and no new half-cycle numerical bound is promoted.

The useful durable result is a proof-architecture localization:

1. the live `L=ell` return is a matched contact pair, but applying a signed H-carry estimate to its
   physical separation requires a directed row/source/end identification that RL349 did not freeze;
2. the historical RL146 height-one exclusion cannot be imported without first proving its global
   binary-height ordinary-owner hypotheses;
3. the post-crossing over-half source route still needs one stronger genuine numerator/CRT estimate
   beyond `A=D0P-Xe>0`;
4. the sub-row deterministic decoder class remains unchanged.

The RL349 correction ledger explicitly demotes all live numerical claims that depended on the
missing orientation bridge.

Phase 4 remains OPEN.
Phase 5 remains NOT STARTED.
R1 remains OPEN at approximately 94% by theorem-level obligations.

## RL350 kickoff

RL350 is the unique next authoritative research session.

Its sole objective is:

**prove `O_75=empty` and close Phase 4.**

First, prove the directed half-cycle orientation bridge from the full two-row decorated walk:
for an exact `L=ell` return, identify the source and endpoint row identities and therefore the sign
of

`D=E-S`

relative to the matched defect

`Delta_i=P_i-Q_i`.

Do not assume the answer. Carry both possible row directions until the exact wrap convention fixes
them.

After that theorem:
- consume the H-carry telescope against the half-cycle class;
- then close the over-half crossing split;
- then close `75<=L<ell` through the deterministic decoder/bounded-signature interface;
- or merge the classes earlier if a stronger exact theorem emerges.

Do not begin Phase 5 in RL350 unless Phase 4 first closes.
