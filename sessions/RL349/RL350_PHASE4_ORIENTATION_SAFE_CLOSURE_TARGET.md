# RL350 target — orientation-safe Phase-4 closure

Status: UNIQUE LIVE TARGET FOR RL350; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent
`(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited
ownership/pruning, live high-carry scope where required, and external conditional least-state floor
`m>=2^71`.

## Absolute objective

Prove

`O_75 = empty`

and close Phase 4.

Nothing weaker closes Phase 4. Do not begin Phase 5 before this is achieved.

## Mandatory correction from RL349

Do NOT use as authority:
- the proposed 34-phase half-cycle mismatch bound;
- tentative half-cycle `D` windows near `25.357e9` or `22.046e9`;
- the tentative `n<27.558e9` consequence;
- the tentative `D>(n-1)/lambda` lower bound;
- the claim that the half-cycle necessarily contains height at least two;
- any automatic import of RL146 from a local height-one-looking return.

Those statements were live scratch and were explicitly demoted at RL349 closeout.

## First theorem — directed half-cycle orientation bridge

For an exact Phase-4 return with `L=ell`, inherited authority gives:
- matched contact endpoints;
- same decoration;
- `H=a`;
- physical source/end separation `D=E-S>0`;
- `2<=D<=2^36`, `D` even;
- terminal gap one;
- phase tag `1<=c<=80448749305`;
- parity mismatch within at most 36 ordinary phases.

At the matched rank `i`,

`d_i=0`,
`Delta_i=e_i=P_i-Q_i`.

Prove, from the actual full two-row directed decorated walk and wrap convention, exactly which of
`P_i,Q_i` is source and which is endpoint for each possible half-cycle direction. Therefore obtain
the correct signed relation between `D` and `Delta_i`.

This theorem must be row/wrap exact. Do not infer it merely from `E>S`.

## Half-cycle closure after the bridge

Once the sign is proved, immediately consume:
- the unique RL324 H-carry sign crossing;
- RL325 normalized telescope;
- q=0 contact geometry;
- RL348 terminal law;
- bounded predecessor/successor signatures;
- mixed 2/3-adic acceptance;
- the exact half-cycle affine identity
  `C=D0*S-Y*D=D0*E-X*D`;
- deterministic least-state descent where applicable.

The goal is elimination of the whole `L=ell` class, not a new numerical window.

Historical RL140--RL146 may be used only if their exact global hypotheses are proved for the live
object.

## Over-half closure

For `L>ell`, retain RL348 authority:
- all q=0 support lies on one strict-late complement `[s,e]` containing `k`;
- `R=e-s>=23135982580`;
- if `s<=j`, then `R>=61170756170`.

For `j<s<=k`, rederive the residual split at the lower/source endpoint. Positivity of

`A_s=D0P_s-Xe_s`

alone is not enough. Seek a genuine numerator estimate or bounded-signature/CRT relation strong
enough to reject the entire branch.

For `s<=j`, consume the full crossing-to-root ownership content rather than improving `R` again.

## Sub-row closure

For `75<=L<ell`, use the exact deterministic interface:
- decorated endpoints determine one physical length;
- total gap is forced;
- inverse-carry decoder gives at most one word;
- RL345 23/72 singleton constraints;
- RL346 bounded incoming signatures;
- RL348 terminal law;
- predecessor/successor CRT;
- q-record/profile, row/wrap, ownership, phase sign and descent acceptance.

Do not enumerate lengths or restart suffix-total ladders.

## Acceptance tests

Every closure must preserve:
- predecessor mixed 2-adic congruence and oddness;
- successor mixed 3-adic congruence;
- exact q-record/profile conditions;
- source/end row-phase decoration;
- early-row q=0 contact law;
- genuine two-row wrap orientation;
- physical ownership/pruning;
- exact phase-potential sign;
- deterministic descent below `2^71` where invoked.

## Stop rule

If `O_75=empty` is proved, enter closeout immediately and do not perform Phase-5 mathematics.
