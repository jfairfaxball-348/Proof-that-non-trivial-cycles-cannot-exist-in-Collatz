# RL349 proof ledger — Phase-4 closure attempt and orientation/ownership barrier

Date: 2026-09-18
Status: CLOSED/FROZEN
Incoming BASE_HEAD: `0580a98ca3319eb1eec00ba56582aee887de105f`
Successor: RL350

## Scope

All work remained only in the inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent at

`(a,ell)=(217976794617,137528045312)`,

the genuine full two-row physical cycle, exact inherited ownership/pruning, the inherited
high-carry scope where required, and the external conditional least-state floor `m>=2^71`.

R1 remains OPEN. Phase 4 remains OPEN. Phase 5 was not started.

RL349 was instructed to close Phase 4 by proving `O_75=empty`. It did not obtain a gap-free proof
of that statement. No residual length class is declared eliminated in this ledger.

## RL349.1 — exact scope barrier for historical height-one closure

RL146 proves exclusion of a primitive ordinary-owned **global height-one owner** of multiplicity
`g>1`. Its load-bearing hypotheses include a globally binary height word and the cyclic
ordinary-owned contact-carry system.

The live RL349 half-cycle residual supplies only a single complete `L=ell` q=0-to-q=0 return whose
endpoints are a matched contact, together with the inherited full two-row q-profile and H-carry
geometry. RL349 did not prove that the whole ordinary owner has global binary height one.

Therefore RL146 cannot be imported merely from a local or one-return "near height-one" conclusion.
The exact missing implication is itself part of the live Phase-4 problem.

Classification: exact dependency/scope barrier. This is not a new cycle exclusion.

## RL349.2 — exact orientation barrier for contact-defect numerics

For an `L=ell` return, RL347 gives matched contact endpoints with equal decoration, `H=a`, and for a
phase-potential nondecreasing directed return physical endpoint larger than physical source.

The H-carry variables at the matched rank are

`Delta_i=P_i-Q_i` and `e_i=Delta_i`

because `d_i=0`.

To turn the physical separation

`D=E-S>0`

into a signed statement `D=+e_i` or `D=-e_i`, one must identify which of the two matched physical
states `(P_i,Q_i)` is the source and which is the endpoint in the **directed decorated half-cycle
return**. The inherited contact theorem alone does not supply that signed identification uniformly
for both possible row directions.

RL349 explored several strong telescope inequalities after assigning such an orientation, but did
not freeze a complete directed-row proof covering the live object. Those numerical consequences are
therefore not promoted.

Classification: exact proof-architecture barrier / missing hypothesis localization.

## RL349.3 — over-half source-side route remains one ownership inequality short

RL348 already proves that an over-half return has all q=0 support on one strict-late complement
interval containing the least-root rank, and splits the complement according to whether its source
lies before or after the H-carry crossing.

RL349 re-examined the post-crossing source-side factorization suggested in RL348 scratch. The
available positive-numerator identity

`A_i=D0 P_i-X e_i>0`

provides the inherited corridor

`e_i/P_i < 1-1/lambda`.

That corridor by itself does not contradict the phase-potential constraint on a residual segment.
A successful source-side skip theorem still needs an additional genuine ownership estimate on the
source numerator/defect, or an exact bounded-signature theorem that supplies the missing strength.

No source-side residual inequality is promoted by RL349.

Classification: decisive route localization, not a theorem closing an over-half class.

## Exact arithmetic diagnostic retained

Using the inherited exact lower enclosure

`log 2 > 15757912/22733865`,

one has the exact upper bound

`ell/(12 log 2) < 32568166831634280/1969739
                 = 16534254960.496...`

and this is strictly below

`n0-1=20390252057`

by the exact positive margin

`7595307864868843/1969739`.

This arithmetic is certified by the RL349 verifier and red team. It becomes a contradiction only
if a future theorem legitimately derives the antecedent bound
`e_k < ell/(12 log 2)` in the live orientation. RL349 does not promote that antecedent.

## What RL349 does NOT prove

RL349 does not prove `O_75=empty`.

In particular it does not promote:
- elimination of `75<=L<ell`;
- elimination of `L=ell`;
- elimination of `ell<L<=251920108044`;
- a 34-phase half-cycle synchronization theorem;
- any new universal numerical bound on the half-cycle separation `D`;
- any signed lower bound such as `D>(n-1)/lambda`;
- a global height-one reduction;
- a source-defect contradiction for the post-crossing over-half branch.

All such live-session numerical/contact claims are explicitly demoted in
`RL349_CORRECTION_AND_DEMOTION_LEDGER.md`.

## Successor obligation

RL350 must continue Phase 4 without using the demoted RL349 numerics.

Its first mandatory theorem is the **directed half-cycle orientation bridge**: for an `L=ell`
decorated complete return, identify exactly the source/end row identities and the sign relation
between physical separation `D` and the matched H-carry defect `Delta_i`, including wrap direction.

Only after that bridge is proved may the H-carry telescope be consumed numerically against the
half-cycle class.

The other two Phase-4 residual classes remain exactly as inherited from RL348.

`PARENT_DIFFICULTY_DELTA = LATERAL`.
