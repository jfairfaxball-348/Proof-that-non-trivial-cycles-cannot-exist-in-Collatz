# RL197 Certified Facts and Proof Ledger

Date: 2026-08-31. Conditional physical branch remains `(37,0,23,-1)`.

## Newly proved analytic mathematics

- For every surviving H21 `{33,34,35}` co-owner, terminal rank `r` lies in
  `[23369453298,41775866136]` minus the inherited 14 deletions, and the exact mechanical
  digits at offsets `36,35,34,33` before the terminal are `1,2,1,2`.
- The relevant `tau=35,34,33` p-pair transitions are ordinary/common-mechanical on every
  surviving H21 rank. The only seam endpoint is terminal rank `41775866136`, already deleted.
- Let `(e35,e34,e33)` be the three common p-pair heights at the starts `tau=35,34,33`.
  Each lies in `{0,1}`. Positive actual acceleration exponents give exactly six triples:
  `000,010,011,100,110,111`.
- The middle chronological edge (`tau=34 -> tau=33`) and its p-shifted copy are both
  height-zero exactly for `000` and `100`. Equivalently, the desired H21-owned p-shift
  zero-edge bridge is exactly the selector `e34=0`.
- The inherited H21 normalized gaps are
  `Delta35=7*2^35`, `Delta34=21*2^33`, `Delta33=63*2^32`.
  Since `C_j=2^(e_j) Delta_j`, the allowed height scale changes `C` by a factor 2 while
  preserving the normalized gap and divisibility modulo 3. Therefore the inherited H21
  normalization and predecessor mod-3 sensor cannot decide `e34=0` versus `e34=1`.

## Exact certificate

`verification/verify_rl197_h21_height_scale.py` checks all displayed constants,
rank intervals, seam ownership, six height triples, two zero-edge triples, gap recurrence,
and scale-invariance.

## Inherited state retained

RL196's p-shift seam theorem, explicit anchored zero-edge pair and abundance barrier remain.
RL195's `J00>=9719139553`, `N0>=43742681439`; RL194 necessary-rank cardinality
`27057465824`, canonical terminal floors `190574/1826072`; RL191 spacing `>=1001`,
`N35<=7559400754`, ordinary flow `>480`, directional K variation `>80`; the complete-word
denominator equivalence; H21 core/deletions, canonical terminal floor 67, and the binding
`{33,34,35}` ownership/charging budget are unchanged.

## Open obligations

RL197 does not prove which H21 height scale occurs. No H21 budget release, atom
realization/exclusion, branch closure, Gate A/Gate B closure, non-trivial-cycle exclusion,
or global Collatz closure is claimed.
