# RL61 whole-tree audit handover — start here

Date: 2026-08-23

## Purpose

This bundle freezes the RL60 terminal-tail progress and deliberately changes the next session from **proof extension** to **whole-tree audit / review / roadmap reset**.

Do **not** begin by extending the terminal threshold from `K=39` to `K=41`, and do not spend the session trying to eliminate the sole safe-CF survivor. First reconstruct the global RL proof tree, classify every branch and dependency, and decide where the next unit of research effort has the highest expected value.

## Read first

1. `RL60_SESSION_FREEZE_AND_PROOF_STATE_2026-08-23.md`
2. `RL60_WHOLE_TREE_BRANCH_LEDGER_FOR_AUDIT.md`
3. `RL60_EXTERNAL_DEPENDENCY_AND_CERTIFICATE_LEDGER.md`
4. `RL61_WHOLE_TREE_AUDIT_AND_ROADMAP_KICKOFF_PROMPT_2026-08-23.md`
5. `verification/README_VERIFICATION_STATUS.md`

Then use `reference_snapshots/` for the RL48–RL59 proof-state chain and `historical_recovered/` for the recovered RL18–RL20 radius-3/global-bridge provenance.

The complete incoming RL59→RL60 handover is preserved unchanged under `inherited_current/` with its original sidecar checksum.

## Frozen headline state

The research does **not** prove Gate A, Gate B, RL, or Collatz.

The current terminal-tail object is the **sole survivor inside the safe continued-fraction stress regime**, not the sole open branch of RL globally.

The locally established radius-3 theorem remains an inherited closed/certified component, but no valid global RL→radius-3 bridge is presently proved.

The specific RL48 direct half-period radius-3 match was corrected in RL49 and must not be resurrected: its cyclic adjacent-transposition distance is even, so it does not meet the exact-radius-3 hypothesis used there.

Gate A — the uniform terminal-area inequality — remains open globally.

## RL60 session increment to preserve

The terminal shortcut thresholds were pushed beyond the RL59 handover:

- `N(31)=715,827,882`
- `N(33)=1,908,874,353`
- `N(35)=10,180,663,219`
- `N(37)=45,812,984,490`
- `N(39)=122,167,958,641`

The K39 threshold implies the internal survivor bound

`z >= 103,303,788,559`.

The exact survivor coupling remains

`K + z = q + 3`, with

`q = 45,446,975,257,190,057,863`.

An additional RL60 reduction, using Barina's externally published complete path-record envelope below `2^71`, gave the provisional external-certificate consequence

`25 <= K <= 129`, `K` odd,

or equivalently

`22 <= t=K-3 <= 126`, `t` even.

Combined with `K+z=q+3`, this would imply

`z >= 45,446,975,257,190,057,737`.

**Important:** preserve this as an external-certificate reduction pending a fresh audit of the path-record completeness statement and the exact envelope comparison. Do not silently promote it to an internal theorem.

## Freeze instruction

The next session should finish with a new whole-tree map and ranked roadmap before choosing whether to resume this survivor, attack the non-safe/global Gate-A regimes, or return to a new Gate-B/radius-3 bridge strategy.
