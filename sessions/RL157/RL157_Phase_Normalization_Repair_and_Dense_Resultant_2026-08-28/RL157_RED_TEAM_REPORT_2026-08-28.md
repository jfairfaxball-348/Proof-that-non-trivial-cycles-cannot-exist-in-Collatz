# RL157 red-team report

- The normalization follows directly from `rho^L=1/2`.
- The demoted `3T^L-2` formula is not retained as a theorem or certificate.
- `P_h(rho)=0` and resultant divisibility are necessary only; no converse or resultant-size exclusion is used.
- Primitivity uses a nonzero defect excursion and is not asserted at height zero.
