# Correction / demotion / promotion ledger through RL146

## Frozen earlier corrections

- RL143's old interval-based height-one exclusion remains demoted/retracted; interval support did not close arbitrary scattered height-one ownership.
- RL145 proved that the repaired local height-one CRT/run-fibre population coefficient is genuinely saturable below the required closure threshold.  More local congruence labels do not by themselves supply the missing coefficient.
- RL145's cyclic contact-carry equations are exact necessary arithmetic structure, not by themselves a contradiction.

## RL146 promotion

**PROMOTED — proved analytic mathematics within the frozen RL145 ordinary-owned height-one framework:** no primitive height-one owner exists for any multiplicity `g>1`.

Reason: the RL145 carry identity admits an integer residue ladder.  Binary height digits force strict order preservation between any pair of ladders.  A full block maps `d_t` to `d_{t+1}`, so cyclic shift preserves strict carry order.  This is impossible for a nonconstant finite cyclic carry vector.  Therefore all carries are equal and, by the inherited zero-sum law, zero.  Equal contact weights imply equal contact sets, forcing repeated blocks and contradicting primitivity for `g>1`.

## Scope lock

Do not promote RL146 beyond its hypotheses.  In particular it does not close `g=1`, mixed-height owners, negative-defect configurations, Gate A/B globally, all non-trivial Collatz cycles, or Collatz itself.
