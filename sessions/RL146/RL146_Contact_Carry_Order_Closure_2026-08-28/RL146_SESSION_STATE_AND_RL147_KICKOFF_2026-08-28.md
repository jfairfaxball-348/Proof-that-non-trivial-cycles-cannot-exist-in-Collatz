# RL146 session state / RL147 kickoff

## Frozen promotion

RL146 proves, inside the inherited ordinary-owned framework, that there is no primitive height-one owner of multiplicity `g>1`.

The new theorem is the contact-carry order closure:

- unroll the RL145 carry identity residue-by-residue;
- descend the forced `2^A` end divisibility to obtain integer normalized states `y_{t,r}`;
- use binary height digits to prove strict order preservation `y_{t,r}>y_{s,r} => y_{t,r+1}>y_{s,r+1}`;
- conclude `d_t>d_s => d_{t+1}>d_{s+1}`;
- cyclicity forces all `d_t` equal;
- `sum d_t=0` forces all carries zero;
- contact-set injectivity then makes every block identical, contradicting primitivity for `g>1`.

## Still open

- `g=1` is not closed by this argument.
- Mixed-height ownership is not closed.
- Genuine negative-defect configurations are not closed.
- No global Gate A/B, non-trivial-cycle, or Collatz closure is claimed.

The RL145 CRT/population saturation remains frozen as a method barrier; do not reopen it without a genuinely new resource.

## Verification economy

After the current bundle checksum, internal manifest, and fast verifier suite pass, accept this frozen proof-state ledger as authoritative. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers stop-and-repair.

## RL147

Read `RL147_LAYERWISE_CARRY_ORDER_OR_NEGATIVE_DEFECT_PIVOT_TARGET.md` and attack the mixed-height boundary first.  If the binary order mechanism cannot be lifted with a rigorous layerwise invariant, freeze the obstruction and pivot to genuine negative defect.
