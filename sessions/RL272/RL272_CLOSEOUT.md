# RL272 closeout

Date: 2026-09-07

Classification: **RADIUS5_KAPPA3_SECTOR_CLOSED**.

RL272 closes the sole remaining `|kappa|=3` Radius-5 topology: height-two mass-four-plus-one.

The load-bearing reduction is the exact normalized identity

`D | (15*3^v-2^u)`

together with its complementary representative and the uniform bound

`D <= 16*3^floor(A/2)`.

The determinant-three non-bracketing case is closed for `A>=45`. The bracketing case inherits the already-promoted RL266 LMN cutoff `A<=51389`; an exact integer size scan then leaves no possible `A>27`.

Gap-free reconstruction through `A=27` gives 149 canonical positive-domain states and zero full-`D` hits. An independent all-word/all-optimal-median replay through `A<=15` passes with exact orientation symmetry and zero full-`D` hits.

Together with RL271, every `|kappa|=3` topology is closed. RL270 already closed the full `|kappa|=1` sector.

Successor: **RL273**, prepared but not started, may now activate only the remaining `|kappa|=5` Radius-5 sector.

Frozen/unchanged: Gate A, Gate B, fifth retained selector, selector enumeration, general Radius-n programme, and global non-trivial-cycle exclusion. Radius 4 remains locally promoted. Radius 5 remains open until `|kappa|=5` is closed.
