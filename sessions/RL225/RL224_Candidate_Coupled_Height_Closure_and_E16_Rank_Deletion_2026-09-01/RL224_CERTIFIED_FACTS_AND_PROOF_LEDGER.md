# RL224 certified facts and proof ledger

Date: 2026-09-01.

## Inherited frozen state

All RL223 and earlier certified claims, corrections, demotions, scope locks and verification-economy rules remain binding except for the single e=16 rank now excluded below.

Incoming RL223 state:

- necessary terminal ranks: **13,415,865,871**;
- above-p sources: **7,091,831,284**; below-p sources: **6,324,034,587**;
- e=16 phase-51 family: **139,581,280** arithmetic candidates across **45,045** prefixes;
- e=16 terminal rank **34,124,151,203** live;
- state011 **90,749,885**, state111 **48,831,395**;
- all **469** inherited eta classes modulo 2187 represented;
- physical H21 incidence/charge unproved;
- Gate A, Gate B, branch contradiction, and global nontrivial-cycle exclusion open.

RL223's odd-modulus count-only saturation theorem remains valid and remains a relaxation-blindness lock. RL224 does not use a free fixed-count word language as a selector.

## RL224-T1 — exact candidate-coupled deterministic height state

**Classification: proved exact recurrence consequence using inherited H21 necessary conditions.**

For every exact e=16 candidate, the inherited affine coordinate

`eta=eta_*+3^17 k`,

`y_16=2^34 eta-1-3^16*2^13`, `h_16=1`

fixes the later accelerated odd trajectory uniquely. With

`a_i=v2(3y_i+1)`, `c_i=b_(i+1)-b_i`,

`y_(i+1)=(3y_i+1)/2^a_i`,

`h_(i+1)=c_i+h_i-a_i`,

every physical H21 continuation must satisfy `h_i>=0`. Therefore a state with `a_i>c_i+h_i` is an exact candidate-coupled impossibility: it forces the next height negative for that same fixed candidate. Unlike RL223's count-only language, the start is never varied independently of the candidate.

## RL224-C1 — gap-free full e=16 phase-200 certificate

**Classification: exact finite certificate.**

The scanner reconstructs the complete inherited e=16 two-sided finite prefix/k-window family, applies the inherited terminal-Hensel exclusions, omits the already-certified RL216 deleted `Q=43,013,953` prefix, and evaluates the deterministic necessary-height recurrence.

The source closeout was computed in nine contiguous non-overlapping chunks covering **indices 1..45,045 exactly once**. Promotion carries a smaller reconstructive certificate instead of those bulky TSV payloads: it regenerates the exact inherited prefix records (pinned SHA256) and reruns all **331,927,916** RL216-targeted candidates directly. This is a superset of, and therefore covers every one of, the **139,581,280** incoming RL223 phase-51 survivors.

Through transition index 200 it leaves exactly:

- **4,242** candidates;
- **4,054** prefixes;
- **40,991** complete-prefix deletions;
- state011 **2,782**, state111 **1,460**;
- mod18 counts `0:888`, `8:473`, `9:1,894`, `17:987`;
- **354** eta classes modulo 2187;
- prefix survivor multiplicities: **3,873** prefixes with one survivor, **174** with two, **7** with three.

Thus relative to the incoming RL223 family, phase 200 alone deletes **139,577,038** candidates.

## RL224-C2 — exact residual closure through transition 346

**Classification: exact finite certificate with full direct replay of the phase-200 remainder.**

Every one of the 4,242 phase-200 survivors is replayed from its exact `(eta_*,k)` coordinate. All **4,242 / 4,242** force a negative necessary height by transition index **346**. Equivalently, the final recorded failure has `a_346>c_346+h_346`, so `h_347<0`.

The last failure is the candidate

- prefix index `44,955`;
- `Q=153,134,585`;
- `k=31,480`.

No e=16 arithmetic candidate survives the necessary H21 height condition.

## RL224-T2 — e=16 terminal-rank exclusion

**Classification: proved scoped rank exclusion from the exhaustive necessary family.**

The inherited above-p small-offset map associates `e=16` with terminal rank

**34,124,151,203**.

RL215/RL216/RL217 provide the exhaustive necessary e=16 root family; RL224 proves that family empty under the mandatory nonnegative-height condition. Therefore terminal rank **34,124,151,203** is excluded.

Exact rank-count update:

- above-p necessary sources: **7,091,831,284 -> 7,091,831,283**;
- below-p necessary sources: unchanged **6,324,034,587**;
- total necessary terminal ranks: **13,415,865,871 -> 13,415,865,870**.

## Global scope

RL224 deletes **139,581,280 incoming candidates, all 45,045 incoming e=16 prefixes, and one necessary terminal rank**.

It does **not** prove physical H21 incidence or charge. It does not contradict the sole high branch as a whole. Gate A and Gate B remain globally open. Global nontrivial-cycle exclusion remains open.

Knowledge catalogues remain `stale/deferred` and are not proof-state authority.
