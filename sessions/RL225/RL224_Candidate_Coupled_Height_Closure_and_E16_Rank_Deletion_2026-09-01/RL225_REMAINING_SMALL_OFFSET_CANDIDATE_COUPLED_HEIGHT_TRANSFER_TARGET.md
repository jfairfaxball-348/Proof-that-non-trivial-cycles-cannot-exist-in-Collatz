# RL225 target — Remaining Small-Offset Candidate-Coupled Height Transfer / Rank Contraction

Date: 2026-09-01.

Status: successor handover target, **NOT STARTED**.

## Mission

Exploit RL224's successful candidate-coupled necessary-height mechanism on the remaining exact above-p small-offset alternatives while preserving type safety.

RL224 removes e=16. The inherited small-offset list below 56 still contains `e=4,28,33,40,45` unless independently removed elsewhere by a certified successor argument.

## Primary attack

Choose one remaining offset, preferably e=4 first because it already carries the inherited exact selector `eta=207 mod243` and state011, and:

1. derive the exact source/root coordinate and finite candidate parameterization for that offset from current authoritative definitions;
2. establish any required two-sided root window and terminal-Hensel/ownership filters at that exact scope;
3. prove the finite family is exhaustive for a physical realization of that offset;
4. couple each candidate to its deterministic accelerated odd trajectory and mandatory nonnegative-height recurrence;
5. use exact modular/range compression when useful, but preserve the fixed candidate identity;
6. report exact candidate, prefix, state, eta-class and terminal-rank effects;
7. provide a gap-free portable verifier and explicit survivor data whenever an empty-intersection claim fails.

## Type locks

Do not:

- transplant the e=16 affine `eta_*+3^17 k`, k-window or root-band formulas to another offset without a new derivation;
- reintroduce terminal rank 34,124,151,203 or any e=16 candidate;
- use free count-only odd-modulus words as actual continuations;
- turn necessary arithmetic candidates into physical H21 populations or charges;
- promote a remaining-offset result to whole-branch, Gate, or global closure without the explicit missing bridge.

## Success criteria

Produce at least one of:

1. another exact small-offset terminal-rank deletion;
2. a strict exact candidate selector with a materially smaller finite family and preserved rank bridge;
3. an exact contradiction for all currently remaining small offsets in one certified common construction;
4. a theorem-quality transfer barrier identifying the first exact datum absent for extending RL224.

No RL225 result exists until its start gate is completed.
