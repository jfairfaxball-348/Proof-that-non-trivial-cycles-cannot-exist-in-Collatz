# RL179 certified facts and proof ledger

## New proved analytic mathematics

- **RL179.1:** in the surviving high type `(v,H,J,d)=(37,0,23,-1)`, the exact value `F2=3(lambda-1)2^37` satisfies `F2>1/3`.  The bound is certified by rational interval bounds for `Delta=A ln2-L ln3` and `e^Delta-1>Delta`.
- **RL179.2:** every physical continuation of that high type requires at least two positive corrected-flow phases after phase 29; four of the ten exact phase-29 histories require at least three.  This uses the exact phase-24..29 budget plus the inherited fact that every individual positive corrected-flow term is `<1`.
- **RL179.4:** for a zero-height mismatch with odd part `w` and `c_(J+1)=1`, `v2(3^(J+2)w +/-1)=2` is impossible.  Valuation `1` gives the unique sign-preserving next transition; valuation at least `3` gives the unique zero return.  This removes one mod-8 odd class for each sign at ten exact mismatch indices.

## Exact finite / arithmetic certificates

- The surviving high state has exactly 10 necessary path histories at phase 29 under the RL178 pair-state law.
- Their cumulative corrected flow through phase 29 is always `< -7/8`; the exact best value is `-6734508720128/7625597484987`.
- Four exact phase-29 states have cumulative flow `< -5/3`: `(0,3,2144699292645)`, `(0,3,2144699292643)`, `(0,1,536174823161)`, `(0,2,1072349646321)`.
- **RL179.3:** the three positive phase-29 pair states are exactly `(1,0,536174823161)`, `(2,0,1072349646323)`, `(2,1,1072349646323)`.  None can return to zero at phase 30.  Only the first can reverse sign immediately; the other two remain positive.
- The `c_(J+1)=1` zero-height indices are exactly `1,3,6,8,11,13,15,18,20,23`.
- At odd `J+2`, the forbidden mod-8 classes are `w=1` for `d=+1` and `w=7` for `d=-1`; at even `J+2`, they are `w=3` and `w=5` respectively.

## Inherited correction / scope state

- Corrected `G_i=S_(p+i)-S_p-S_i` remains authoritative.
- Physical `F2=sum q_i(2^G_i-1)=3(lambda-1)g_p` remains authoritative.
- RL173's `3^(-G)` functional remains auxiliary only.
- RL175's sparse ownership resultant is not used as an independent gap obstruction.
- RL177/RL178 necessary automata remain one-way filters, not physical existence certificates.
- The external `m>=2^71` minimum is not used in any new RL179 theorem.

## Global status

The sole zero-height `v=37` negative sign survives, but now carries a mandatory multi-support compensation requirement.  Ten of fourteen zero-height mismatch indices acquire an exact one-class-per-sign mod-8 odd-part sieve.  The preferred `h_p=0` branch, the positive-height branch, Gate A, Gate B, global non-trivial-cycle exclusion, and Collatz remain open.
