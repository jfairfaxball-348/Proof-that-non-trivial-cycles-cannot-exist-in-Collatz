# RL236 correction / demotion ledger

RL236 introduces no demotion of valid RL235 mathematics.

Closeout correction of RL236 scratch work:

- tentative early charge `a≈7.80910717758 U` is **withdrawn / NOT PROMOTED**;
- tentative ordinary corrected-flow claim `>763` and derived signed/directional consequences are **withdrawn / NOT PROMOTED**;
- reason: the retained exact H20 cell `T=216803362095665397`, `H=20`, owners `{32,33,34}`, cell `[62456644959,96166487667]` exceeds its local certified budget before that tentative charge and had no invariant-specific occurrence cap;
- scratch H21 `T=283512088894331673`, owners `{33,34}` spacing `>=6251` is **NOT PROMOTED** in RL236 because it is not needed by the repaired theorem and is not part of the final portable verification surface.

The final promoted RL236 charge stops exactly when the uncapped H20 `T=216803362095665397` cell binds, yielding ordinary corrected flow `>742`.

Historical RL231 charging proof and old H17 `1,615` / `85,103,989` consequences remain demoted exactly as in RL235.

Gate A, Gate B, frontier, live-rank status, and global-exclusion status are unchanged.
