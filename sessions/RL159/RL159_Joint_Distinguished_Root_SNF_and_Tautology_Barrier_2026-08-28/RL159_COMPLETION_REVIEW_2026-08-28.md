# RL159 completion review

RL159 fully resolves the joint distinguished-root elimination target. The Sylvester row lattice for `2T^L-1` and `3T^(A-L)-2` is exactly the kernel of evaluation at the physical root modulo `d=|2^A-3^L|`. Hence its Smith normal form is `diag(1,...,1,d)`, and appending a dense primitive `P` gives maximal-minor divisor `gcd(d,P(rho))`.

This does repair the RL158 character-loss problem, but only by recovering the original physical evaluation exactly. Therefore the joint-resultant/SNF/subresultant direction is frozen as a **tautological method barrier**: it yields no independent size/factor/positivity leverage beyond `P_h(rho)=0 mod d`.

No global gate or singleton owner is closed. RL160 is deliberately redirected to a whole-history crossover audit across the completed session corpus.
