# RL169 session state and RL170 kickoff

RL169 proves a physical ordered-chain gap, then proves that direct chain
aggregation is capacity-slack even for all `L` phases. A successor must use a
non-chain correlation in the permutation between chronological and
lifted-defect order.

Start RL170 from `RL170_NONCHAIN_ORDER_PERMUTATION_TARGET.md`. Preserve the
ordinary `+1`, physical least-root, coprime `g=1`, corrected
phase-normalization, phase-order, and prior method-barrier qualifications.
