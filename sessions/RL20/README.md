# Collatz R# RL20 Global Bridge Handover

This bundle freezes the RL20 research state at the point where radius 3 is closed locally, the local-only bridge has been falsified sharply, and the next global target is the balanced gcd-block return.

Start with `START_HERE.md`.

The `inherited/` directory contains the complete RL19 radius-3 closure/global-packing handover. The `logs/` directory contains fresh outputs from all eight RL20 release verifiers.

No file in this bundle claims a proof of RL or of the Collatz conjecture.
