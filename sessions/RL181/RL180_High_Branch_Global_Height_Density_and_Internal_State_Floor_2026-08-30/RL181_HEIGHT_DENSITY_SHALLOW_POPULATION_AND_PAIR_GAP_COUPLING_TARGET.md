# RL181 target — height density, shallow population, and pair-gap coupling

Continue from the frozen RL180 high branch `(v,H,J,d)=(37,0,23,-1)`.

## Primary target

Seek an independent **physical** theorem that couples at least two of the following RL180 structures strongly enough to restrict or exclude the surviving high branch:

- `26,724,850,253 <= #{H_r>0} <= 88,981,261,496`;
- `#{H_r=0} >= 48,546,783,816`;
- the internally certified shallow populations through height four;
- `2^74 < m < 3*2^73`;
- the normalized physical pair-gap potential
  `K_(i+1)-K_i=(1/3)q_i(2^G_i-1)` with `K_0=2^37`;
- the exact RL180 delayed-support requirements in the early high interface.

Preferred consumers are simultaneous packing/spacing, divisibility, ownership, or physical state-gap constraints. A successful argument must use information beyond a reparameterization of the residue-height profile.

## Do not spend the session on

- a broad extension of the phase BFS without a new consumer;
- rank-only or monotone-chain packing already blocked by RL168-RL170;
- inverse-phase/rank coordinate identities already blocked by RL171;
- blind higher odd-modulus lifting after the RL180 mod-16/32 barrier;
- the demoted RL173 `3^-G` physical interpretation;
- recursive historical verification absent a failure or unresolved dependency.

## Scope discipline

Preserve the distinction between analytic proof, exact finite certificate, external computation, evidence, conjecture, and method barrier. Necessary automaton states are not physical existence certificates. The RL180 internal state floor applies to the surviving high branch only.

If the high branch is excluded, stop and reconcile the proof-state ledger before selecting the next surviving branch. Do not infer global Collatz closure from a local-type exclusion.
