# RL180 certified facts and proof ledger

## New proved analytic mathematics

- **RL180.2:** in the surviving `(37,0,23,-1)` high branch, with `W=R-Q`, RL178's residue deficit satisfies
  `D/(exp(s+Delta)-1) < W < D/(exp(s)-1)`.
- **RL180.6:** for `K_i=exp(s) z_(p+i)-z_i` in the carry-free p-shift window, `K_0=g_p` and `K_(i+1)-K_i=(1/3)q_i(2^G_i-1)`.

## Analytic results with exact finite / interval certificates

- **RL180.1:** all ten RL179 phase-29 high histories have cumulative corrected flow through phase 31 at most
  `64458869178368/205891132094649 < 1/3`. Every physical high continuation therefore needs positive corrected flow at index `>=32`. Six phase-29 roots need at least three positive phases at `>=32`; `(0,3,2144699292643)` needs at least four. That root further needs at least 4/3/2/1 positive phases from indices 34/36/38/40 onward respectively.
- **RL180.3:** the transported positive-height support obeys
  `26,724,850,253 <= #{r:H_r>0} <= 88,981,261,496`.
  Hence `#{r:H_r=0} >= 48,546,783,816`.
- **RL180.4:** internally in this high branch,
  `2^74 < m < 3*2^73`.
- **RL180.5:** internally in this high branch,
  `#{h<=1} >= 73,801,609,945`,
  `#{h<=2} >= 81,605,820,006`,
  `#{h<=3} >= 84,804,722,294`,
  `#{h<=4} >= 86,264,134,824`.

## Exact certificate outputs

- Mechanical loss: `23,493,381,795 < R-Q < 28,070,867,755`.
- Phase-31 global maximum: `64458869178368/205891132094649`.
- Phase-support and global-height verifier suite: PASS.

## Method barriers / negative results

- Mod-16/mod-32 continuation of the RL179 odd-part sieve refines valuation depth but yields no second immediate uniformly forbidden odd residue class at the tested lift. No stronger sieve is promoted.
- RL169's monotone-chain capacity barrier is not revived by the stronger high-branch state floor; its relevant scale cancels.
- RL170's universal order-permutation rank budget is not tightened by a larger `m`.

## Inherited correction / scope state

- Corrected `G_i=S_(p+i)-S_p-S_i` remains authoritative.
- Physical `F2=sum q_i(2^G_i-1)=3(lambda-1)g_p` remains authoritative.
- RL173's `3^-G` functional remains auxiliary only.
- RL175's sparse ownership resultant is not used as an independent gap obstruction.
- RL177/RL178 necessary automata remain one-way filters, not physical existence certificates.
- The new `m>2^74` statement is internal only to the surviving RL179 high branch; it is not promoted as a universal least-cycle computation.

## Global status

The sole zero-height `v=37` negative sign survives. The preferred `h_p=0` branch, the positive-height branch, Gate A, Gate B, global non-trivial-cycle exclusion, and Collatz remain open.
