# RL300 — physical imbalance / record-spike compression target

Date prepared: 2026-09-11
Status: PREPARED, NOT STARTED

## Incoming classification

RL299 closes as

`RESONANCE_RECORD_REDUCTION_PLUS_EXACT_PHYSICAL_FRONTIER_TO_A630138896_WITH_J_IMBALANCE_BARRIER`.

The unconditional selector frontier is eliminated through

`a=630138896`.

Gate A remains open. Gate B remains open. RL297 P-bottleneck work remains parked weak-green background.

## Frozen RL299 structure

Selector-envelope growth is no longer a bulk-selector problem. Put

`Delta=a ln2-ell ln3`.

The relaxed quotient ceiling

`Nhat=floor((79/9)/(exp(Delta)-1))+2`

is decreasing in `Delta`, so new worst envelopes are controlled by the sparse one-sided continued-fraction record sequence.

The certified upper records through the live frontier are

`U0=(301994,190537)`
`U1=(17087915,10781274)`
`U2=(102225496,64497107)`
`U3=(187363077,118212940)`
`U4=(272500658,171928773)`
`U5=(630138897,397573379)`.

Physical envelopes through U4 were exhaustively certified; their maxima culminate at 676 half-steps. U5 has relaxed ceiling

`Nhat(U5)=82931674943`,

which corresponds to `3,455,486,456` quotients / `6,910,972,912` starts under blind replay. Do not attack this by arbitrary volume unless a much smaller theorem-sized certificate first reduces the workload.

## New physical invariant

If a half-Collatz trajectory reaches 1 after `O` odd and `E` even half-steps, define

`J=O-E`.

RL299 proves

`2W(m) <= m+J+1`,

so the exact sufficient obstruction is

`2rho-m >= J+2`.

This is strictly sharper than using total stopping time. The principal RL300 objective is to control `J`, not to compute ever larger stopping envelopes.

## B-family ancestry and barrier

For `N=19+24h`,

`B0=81h+80`

has the exact four-odd-step ancestor

`16h+15 -> 24h+23 -> 36h+35 -> 54h+53 -> B0`.

But on `h==0 mod3`, `16h+15` is divisible by 3 and has no odd reverse predecessor; every earlier positive ancestor is a larger power-of-two multiple. Therefore reverse-ancestry refinement cannot uniformly improve the coefficient 16.

## Primary objective A — internal J compression

Seek a theorem that bounds `J=O-E` for the special physical A/B families at resonance records without enumerating all quotients. Prioritise:

- coupled A/B identities, shared suffixes or mergers;
- residue/transducer compression that is genuinely sublinear in the quotient envelope;
- scale/height arguments converting an extreme positive `J` into a contradiction with selector/full-phase geometry;
- exact record-to-record recurrences.

A bound need not be sharp: at large records the selector excess is enormous.

## Primary objective B — external finite certificates only if fully imported

RL299 found promising public external computations, but did not promote their later frontier extensions. RL300 may use such a certificate only if its exact provenance, checked range, quantitative statistic, and reproducibility are frozen in-repository. Convergence alone is not enough; the consumer needs a bound on stopping time or directly on `J`.

Do not treat a web-page status line as a promoted internal certificate.

## Primary objective C — decide whether to return to the Gate-A Bellman bottleneck

If physical J compression at U5 yields only another scaling wall, compare its burden against the parked RL297 Bellman/P shared-endpoint route. A decisive method barrier is a valid checkpoint; do not spend the session on blind quotient volume merely because U5 is finite.

## Success condition

RL300 succeeds with any one of:

- an analytic or compact exact certificate eliminating U5 without billions of direct trajectories;
- a reusable uniform/record-wise bound on `J`;
- a fully provenance-backed quantitative external certificate that legitimately eliminates a new record block;
- a decisive method barrier that makes a return to the Bellman/P route strategically superior.

## Scope

Gate A OPEN.
Gate B OPEN.
Radius 6+ frozen.
RO/divergent-orbit work out of scope.
Lean formalisation separate.
No global non-trivial-cycle exclusion is claimed.
