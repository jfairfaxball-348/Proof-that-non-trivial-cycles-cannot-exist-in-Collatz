# RL330 closeout — total-44 physical ownership and coefficient-43/2 contraction

Date: 2026-09-15
Status: CLOSED AND FROZEN
Incoming authoritative HEAD: `ed74f86795c59228e89548ec19296e4914b45210`
Successor: RL331

RL330 closes the exact obstruction named by its target: the conservative coefficient-22 total-44 cycles are not physically realizable in the self-consistent high-carry regime.

At `n>=32551214632`, exact reconstruction finds 13,556 total-44 singleton realizations but only 14 physical total-44 successor links, all directed from `(1,43)` to `(43,1)` or from `(2,42)` to `(42,2)`. Exact total-44-to-short reconstruction leaves 166 pair transitions. Combined with the retained large layer, every exact-owned singleton transition lies in a 274-state DAG with 21 edges and no path longer than one edge.

The resulting 323-state / 26,514-edge conservative automaton proves `2Z<=43K+129`. At `rho=60`, this forces `K>=6112357564`. The inherited exact residue-weighted telescope then gives `n<32551214631.602516...`, and the self-consistent authoritative cap becomes `n<=32551214631`, improving the incoming cap by 11,415,722.

Portable verifier `verification/verify_rl330_total44_ownership_density.py` and independent red team `verification/red_team_rl330_total44_ownership_density.py` are green.

The graph coefficient `43/2` is sharp because physical identity is still forgotten inside the total-43 short-singleton layer, represented by `N(21)<->N(22)`. This is a conservative graph obstruction only. RL331 must seek all-length phase ownership, owned-state drift, residue-support coupling, or a stronger consumer; blind threshold descent is not the programme.

RL330 does not close R1, Gate A, Gate B, the full `g=2` branch, or global positive non-trivial-cycle exclusion. RL324's local counterfamily remains binding. No root-aligned `G<2^35` bound is used. The external `2^71` least-state floor remains conditional.

`PARENT_DIFFICULTY_DELTA = EASIER`.
