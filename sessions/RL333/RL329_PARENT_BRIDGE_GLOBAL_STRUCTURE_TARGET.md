# RL329 — global structure beyond singleton-density contraction

Date prepared: 2026-09-15
Status: PREPARED, NOT STARTED

## GLOBAL PROOF ROADMAP

`CURRENT_STAGE = R1`
`CURRENT_STAGE_NAME = Parent Bridge`
`CURRENT_STAGE_PROGRESS = 72%`
`GLOBAL_PROOF_STATUS = OPEN`

## Absolute objective

Close or materially advance the remaining genuine ordered `g=2`, `Z0>0`, `K<0` parent branch under the promoted RL327 cap `1<=n<=32596612662`.

RL328 has proved that merely certifying progressively shorter singleton bridges inside the existing conservative graph and feeding their density into the current linear K-count telescope cannot plausibly close the high-carry regime.

## Primary theorem target

Prove the smallest theorem that introduces genuinely global information absent from the RL328 viability-barrier counterarchitecture.

Preferred targets, in order:

1. **Long-positive-run ownership/descent.** Show that sufficiently long positive-excess excursions cannot occur in a genuine owned crossing-to-canonical interval, or that such an excursion forces a strictly smaller genuinely owned balanced return.
2. **Global run/residue coupling.** Replace the worst-case `K`-smallest-distinct-residue consumer by a theorem tying positive-run geometry to actual residue positions strongly enough to defeat the RL328 barrier.
3. **Nonlinear/height-sensitive consumer.** Derive a telescope or energy inequality gaining materially more than the factor-two ceiling available from merely increasing individual `q_t` heights in the current linear recurrence.

A useful theorem must attack the >=3-positive-run obstruction or the consumer itself, not merely improve another singleton threshold.

## Binding strategic barrier

RL328 proves:

- the unchanged conservative graph contains a repeatable `N(49)->N(49)` >=3-positive cycle with `Z/K=49/3`;
- the current residue-min linear consumer needs `Z/K<0.217775` to reach the high-carry floor;
- even maximal factor-two per-positive height gain still needs `Z/K<1.135779`;
- therefore shorter-singleton density refinement alone is strategically insufficient.

Do not treat total-38, total-37, ... singleton enumeration as the default programme. Such enumeration is permitted only as a diagnostic subordinate to a new global theorem.

## Frozen scratch option

RL328 preserves a non-promoted shorter-singleton candidate down to `n<=32560139785`. It is not authoritative theorem state and must be fully reconstructed/red-teamed before reuse.

## Binding inherited barriers and scope

Preserve RL324's local-propagation barrier. Do not use root-aligned `G<2^35` in the late-row-root branch. External `2^71` least-state floor remains conditional.

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49547666544`.

Gate A OPEN. Gate B OPEN. R1 OPEN. `g=1` separate. Global positive non-trivial-cycle exclusion OPEN.
