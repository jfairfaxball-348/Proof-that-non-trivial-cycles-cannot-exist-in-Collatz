# RL77 SESSION STATE AND KICKOFF

Date: 2026-08-24

## 0. Authority and scope

**AUTHORITATIVE OUTGOING RL77 RESEARCH STATE.** Gate A remains open globally, Gate B remains open globally, and no RL/nontrivial-cycle/Collatz closure is claimed.

RL77 worked the RL76-selected deliberate alternative:

# **Product / growth / continued fraction + modern full phase**

The session obtained a modern half-cycle product window, a new de-scaling of the first RL73 convergent branch, an exact unimodular pump-coordinate theorem, and an exact closed-pump product/physical-scale law. The attack does not produce a global scale bound; the stop criterion is therefore triggered.

Detailed mathematics:

`RL77_PRODUCT_GROWTH_CF_FULL_PHASE_SCALE_COUPLING_AND_LATTICE_BARRIER.md`.

Selected next target:

`RL78_DQ_SENSITIVE_BOUNDED_RADIUS_GATE_B_RECONNAISSANCE_TARGET.md`.

## 1. Verification economy rule — retained

After the current bundle checksum, internal manifest and fast verifier suite pass, accept the frozen incoming proof-state ledger as authoritative. Do not recursively rerun historical expensive certificates unless a new load-bearing argument requires an unresolved definition, a verifier fails, or an apparent contradiction triggers stop-and-repair.

## 2. Incoming RL76 gate

The uploaded/current RL76 gate passed before RL77 mathematics:

- outer RL76 sidecar: **PASS**;
- freshly unpacked RL76 internal `SHA256SUMS.txt`: **PASS**;
- `bash verification/run_fast_rl76_verifiers.sh`: **PASS**.

Frozen RL76 fast counts retained:

- closed synchronized words checked: `26`;
- positive paired legality checks: `415`;
- scale-invariance checks: `416`;
- determinant-normal-form checks: `416`;
- fixed-context denominator-growth checks: `24`;
- `c=10` reciprocal-mass checks: `116`.

No expensive historical suite was recursively rerun.

## 3. Incoming RL76 state retained exactly

- radius-3 primitive/full-`D`: closed local obstruction;
- Gate A even `k`: impossible analytically;
- Gate A `k<=25`: closed by exact finite-certificate corollary;
- first globally open terminal exponent: odd `k>=27`;
- odd `27<=k<=165` hypothetical violations carry the RL73 giant macro with at least `40,249,491,324,522,944` aligned `00` columns;
- RL76 exact paired physical pump normal form retained;
- RL76 exact normalized-weight/physical-scale conservation retained;
- RL76 fixed-context invariant `m_q M_q=K_ctx` retained;
- RL76 `c=10` reciprocal/harmonic depth-pricing barrier retained;
- RL75 primitive proper-pump nondegeneracy and `N==19 mod24` retained.

## 4. New RL77 theorem A — exact full-half product budget and two-sided phase window

For every positive shortcut-Collatz segment `w` of length `n`, weight `s`, physical entry `X` and exit `Y`,

`Pi(w;X)=product_(odd bits)(1+1/(3X_i))=(2^n/3^s)(Y/X)`.

For the genuine RL64 full-phase half `v`, which sends `N+4` to `N`,

`boxed: Pi_v=zeta N/(N+4)=1+V/[3^ell(N+4)]`.

Using `V=19*3^r+8Q(y)` and the elementary bound

`Q(y)>=3^r-2^r`,

put

`epsilon_r=(8/27)(2/3)^r`.

Then

`boxed: [5-epsilon_r]/N <= zeta-1 < 79/[9(N-2)]`.

Equivalently,

`log(1+[5-epsilon_r]/N) <= a log 2-ell log 3 < 79/[9(N-2)]`.

Classification: **analytic synthesis** of RL19 product, RL64/RL65 full phase and RL73 phase squeeze.

## 5. New RL77 theorem B — reduced-CF successor window

Write

`a=gp`, `ell=gq`, `g=gcd(a,ell)`, `gcd(p,q)=1`,

and `beta=log_2 3`.

If

`q < 9g(N-2)log(2)/158`,

then `p/q` is a continued-fraction convergent of `beta`. If `q_+` is the next convergent denominator, then

`boxed: q_+ > 9g(N-2)log(2)/79 - q`,

and

`boxed: q_+ < g log(2)/log(1+[5-epsilon_r]/N)`.

Thus modern full phase controls the successor denominator relative to `gN`, but does not bound `gN` globally.

Classification: **analytic continued-fraction consequence / non-closing structural restriction**.

## 6. New RL77 theorem C — first RL73 convergent branch de-scales to `g=1`

Retain the RL73 exact reduced-denominator gate

`Q_gate=93,226,756,704,262,400,759`

and its unique surviving above-`log_2 3` convergent

`p0=123,139,092,617,126,647,266`,

`q0=77,692,117,359,936,589,403`.

Let

`lambda0=p0 log(2)-q0 log(3)`.

Using the exact rational atanh log intervals inherited in method from the audited RL49/RL73 verifier, RL77 verifies

`lambda0 < 79/[9(2^71-2)] < 2lambda0`.

If the reduced pair is `p0/q0`, then the full linear form is `g lambda0`, so the RL73 upper phase squeeze forces

`boxed: g=1`.

Hence the whole low-reduced-denominator branch is the single exact exponent pair

`boxed: (a,ell)=(p0,q0)`.

Every other full-phase datum has

`q>=93,226,756,704,262,400,760`.

For the exact pair,

`boxed: 2^71 <= N <= 2,578,333,030,765,879,156,036`,

with `N==19 mod24`.

Classification: **analytic corollary + exact rational log-interval certificate**.

## 7. New RL77 theorem D — canonical pumps form a unimodular exponent basis

Define

`Sigma=2ell-a`,

`Delta=2a-3ell`.

Then

`boxed: a=3Sigma+2Delta`,

`boxed: ell=2Sigma+Delta`,

`boxed: gcd(Sigma,Delta)=gcd(a,ell)`.

The canonical pumps have exponent vectors

- `101 -> (3,2)` and factor `8/9`;
- `10 -> (2,1)` and factor `4/3`.

Their determinant is `-1`, so they form a `Z`-basis of the exponent lattice and

`boxed: zeta=(8/9)^Sigma(4/3)^Delta`.

Therefore a generic “two-pump” continued-fraction attack on `8/9` versus `4/3` is only a GL(2,Z) reparametrization of the original `2^a/3^ell` near resonance unless a new theorem bounds the residual pump/context coordinates.

Classification: **analytic theorem / route diagnosis**.

## 8. New RL77 theorem E — exact closed-pump product/scale law

For a closed synchronized pump `c`, let

`P=2^|c|`, `R=3^wt(c)`, `C=Q(c)`,

and physical affine fixed point

`alpha=C/(P-R)`.

If `q` copies send physical state `x` to `y`, then

`boxed: Pi(c^q)-1 = alpha[(P/R)^q-1]/x`.

If the pump lies inside the genuine full half `v`, then the full-half product budget gives

`boxed:`

`x > [9(N-2)(N+4)/(43N+72)] |alpha| |(P/R)^q-1|`.

For `c=10`, this forces exponential physical entry scale as `q` grows. But RL76 supplies precisely such unbounded scale through its free integer `m`; the inequality becomes a lower bound on `m`, not a repeat-depth contradiction.

Classification: **analytic synthesis + exact method barrier**.

## 9. RL77 route decision

The RL77 stop criterion is satisfied.

The product theorem converts repeat depth into a physical-scale demand, but no inherited theorem upper-bounds that scale. The CF theorem converts near resonance into a successor-denominator demand, but `gN` remains unbounded. The canonical two-pump Diophantine formulation is not independent because the two pump exponent vectors are unimodular.

Therefore RL78 must **not** be another generic CF scan, Baker/two-log estimate with arbitrary context, endpoint/`Psi` attack, reciprocal packing re-run, or q-digit extension.

Pivot to:

# **`D|Q`-sensitive bounded-radius Gate-B reconnaissance**

The first requirement is to recover and use the exact RL20 radius-4 fake model as a negative control. Any candidate global bridge must distinguish genuine full-`D` ownership from that `D∤Q` local-grammar model.

## 10. RL77 correction/demotion ledger additions

Retain every RL72–RL76 correction and add:

- the RL73 first-convergent ray is now a single exact exponent pair; gcd multiples `g>=2` are excluded;
- generic `8/9` versus `4/3` two-pump Diophantine work is reclassified as an exponent-lattice coordinate change unless residual context is independently controlled;
- full-phase product bounds price closed-pump depth by physical state scale, not repeat count alone;
- RL76's free physical-scale parameter remains the unresolved obstruction;
- the reduced-CF successor window is not a finite global scan because `gN` is unbounded;
- the canonical-prefix negative-pump calculation is only a product-budget stress test, not a claimed full-phase completion.

## 11. Exact proof state after RL77

- radius-3 primitive/full-`D`: **closed local obstruction**;
- Gate A even `k`: **closed analytically**;
- Gate A `k<=25`: **closed by exact finite certificate corollary**;
- Gate A odd `27<=k<=165`: **open**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 12. RL77 verifier status

`python3 verification/verify_rl77_product_cf_scale.py`: **PASS**.

The current fast suite checks:

- inherited RL76 outer sidecar;
- exact segment product identities on legal bounded trajectories;
- bounded exhaustive `Q(y)>=3^r-2^r` cases;
- unimodular pump-coordinate identities and gcd preservation;
- exact `c=10` and `c=101` product-excess formulas;
- canonical-prefix product stress identities;
- exact rational log intervals proving `lambda0 < B < 2lambda0`;
- reproduction of the inherited RL73 reduced-denominator gate;
- the rigorous integer upper endpoint for `N` on the exact first pair.

The verifier is an audit/falsification certificate, not the proof of the infinite analytic statements.

---

# Self-contained kickoff for RL78

Continue the Collatz R-sharp / RL research from the authoritative RL77 bundle and matching `.sha256` sidecar.

First verify only the current RL77 gate:

1. outer RL77 sidecar;
2. freshly unpacked internal `SHA256SUMS.txt`;
3. `bash verification/run_fast_rl77_verifiers.sh`.

Apply the verification-economy rule after those pass.

The session target is:

# **RL78 — `D|Q`-sensitive bounded-radius Gate-B reconnaissance**

Use the closed radius-3 primitive/full-`D` theorem as the downstream consumer, but do **not** assume a global radius bound.

Recover selectively from GitHub the exact RL18/RL19 arbitrary-rotation/full-`D` interfaces and the exact RL20 radius-4 local-grammar fake model. The fake model is mandatory negative control: it satisfies the relevant local grammar but fails `D|Q`. Any proposed close-pair/radius bridge that cannot see this failure is not the missing theorem.

Seek a genuinely non-coboundary global relation that consumes full divisibility/ownership and forces either:

- two owned primitive full-`D` rotations at cyclic word radius `<=3`;
- an absolute owned radius `<=4`, making a finite radius-4 obstruction programme relevant; or
- a direct contradiction before local radius closure is needed.

Mandatory red-team rules:

- reject anything that collapses to `D*(state difference)`, the RL48 proper-factor identity, or the RL65 raw phase quotient;
- reject local-grammar-only arguments that also hold on the RL20 fake model;
- do not build a generic radius-4/5/6 case tree before a global theorem makes that radius unavoidable;
- do not return automatically to Gate-A digits or generic CF if the first candidate fails.

Use `RL78_DQ_SENSITIVE_BOUNDED_RADIUS_GATE_B_RECONNAISSANCE_TARGET.md` for the detailed candidate mechanisms and stop rule.

Before ending, freeze exact proofs, failed candidates, recovered definitions, dependencies and verifier status; create the next numbered authoritative bundle and sidecar; verify the internal manifest and fresh-unpack fast suite.
