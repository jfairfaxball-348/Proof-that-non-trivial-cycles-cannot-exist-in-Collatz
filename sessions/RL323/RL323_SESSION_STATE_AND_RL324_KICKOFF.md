# RL323 session state and RL324 kickoff

Date: 2026-09-14
RL323 status: CLOSED AND FROZEN
Successor: RL324
BASE_HEAD worked: `d060213279faecb865b0ef6566aa6aff35ec6972`

## State handed forward

RL323 found a genuine ordinary/full-`D` mechanism absent from the RL322 geometry-only model.

The phase-aligned physical `H`-carry

`D_t=3^(c_t)U_t-V_t`

starts negative, ends positive, and has positivity absorbing. Its unique first transition occurs at an actual matched physical rank.

At that crossing:

`r=u_j-v_j`,
`h=wt(v[v_j:u_j])>=2`,
`floor(Q/3^h)=P`,
`0<J<3^(h-1)`,
`0<eta<2^r/3`.

The complementary actual `ell`-odd prefix satisfies

`D0P<A<D0P+X/3`.

At the exact first external survivor, conditional on `m>=2^71`,

`q=p-ell>=3182833230`,
`r<=77265916075`.

The early same-rank cyclic-wrap scratch claim is withdrawn and must not be reused.

## RL324 instruction

Continue this progress rather than restarting another route by default.

The objective is not a smaller numerical constant. The objective is the missing parent-level link: convert the forced zero-carry crossing into contradiction, genuine owned descent, a bound on the original least-root canonical interface, or an already-covered bounded owned witness.

Prioritize connecting the unique crossing's global position to the least-root canonical rank using `D=-K` there and the genuine RL321/RL323 complementary ownership identities.

Do not enumerate the RL323 bound or drift back to fixed-depth/support grammar.

Read `RL324_ZERO_CARRY_TO_PARENT_OWNERSHIP_CLOSURE_TARGET.md` as the binding successor target.

Gate A OPEN. Gate B OPEN. Global positive non-trivial-cycle exclusion OPEN. `g=1` separate.
