# RL162 certified facts and proof ledger

## New proved analytic mathematics

- **RL162-A:** height-one phase-boundary compression uses the `A^{-1}` phase
  order, not chronological physical order.
- The explicit family `(A,L)=(4n,2n+1)` has two chronological boundaries and
  `2n` phase-residue boundaries for every `n>=2`.

## Exact finite certificate

- `RL162_CERTIFICATES/verify_rl162_phase_boundary_order.py` checks the full
  construction and phase identities for `2<=n<=64`.

## Method barrier

Chronological run information alone cannot sparsify the compressed phase
boundary polynomial.  A successor needs an actual-owner theorem about the
`A^{-1}`-step phase order or another non-homogeneous global resource.

## Status

No correction/demotion of an authoritative claim; no cycle or Gate exclusion.
The global proof status remains open.
