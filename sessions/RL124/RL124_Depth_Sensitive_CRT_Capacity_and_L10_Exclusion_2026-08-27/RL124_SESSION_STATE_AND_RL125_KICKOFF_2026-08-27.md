# RL124 session state and RL125 kickoff

## Completed RL number

`RL124`

## BASE_HEAD

`6f4b64fe4eba9984382677a5a4bb643b1307c512`

## Frozen promoted results

1. The RL123 depth-sensitive one-fibre/CRT capacity inequalities, exhaustively instantiated over ordered primitive `L=10` run profiles, exclude `91,625,515` of `92,555,670` primitive profiles.
2. The remaining `930,155` profiles yield `24,826,595` exact cyclic-root tests of `D|Q`; hits: `0`.
3. Consequently every hypothetical primitive nontrivial positive ordinary shortcut cycle has `L>=11`.

The counts in (1)--(2) are exact finite-certificate counts.  The capacity implication itself remains an inherited analytic physical-ownership theorem.

## Correction/demotion ledger

No theorem was demoted or repaired.

## Red-team state

- RL20, RL79, and RL81 carry forward unchanged.
- Physical representatives remain distinct from quotient representatives.
- Periodic profiles are tracked separately from primitive candidates.
- Raw/Farey scope and exact-certificate scope remain unrestricted only where their stated hypotheses permit.

## Verification economy

The RL123 incoming bundle checksum, internal manifest, supplied verifier, and independent fresh unpack all passed at this `BASE_HEAD`.  RL124's new complete verifier and certificate were then rerun; no historical expensive certificate was repeated.

## RL125 kickoff

Work on `RL125_L11_DEPTH_SENSITIVE_CAPACITY_TARGET.md`.  The first residual strip is `L=11`, `7<=Z<=43`; `Z>=44` is already analytically excluded by the zero-fibre width bound.  Compress via profile capacity before any ownership enumeration.
