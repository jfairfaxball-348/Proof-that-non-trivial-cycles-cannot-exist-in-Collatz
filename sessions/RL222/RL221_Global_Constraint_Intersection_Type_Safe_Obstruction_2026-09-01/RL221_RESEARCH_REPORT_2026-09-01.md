# RL221 research report — global constraint intersection / minimal contradiction core

Date: 2026-09-01.

## Question attacked

RL221 was instructed not to produce another catalogue.  It had to place a small set of strong modern restrictions on one common e=16/H21 candidate object and search for an actual empty intersection or, failing that, identify the exact missing bridge.

## Result

The selected five-family core is **not empty**.

A reconstructive verifier produced a concrete e=16 phase-51 candidate with
`Q16=43079489`, `k=28821`, `eta=3722043165201`,
and root `y0=24921895945404894117887`.
The same tuple satisfies:

1. H21 prefix/lift, state and orientation congruences;
2. the independent p-arc/complementary-arc root sandwich;
3. terminal Hensel/valuation;
4. RL217 phase-51 nonnegative mechanical height;
5. RL220's universal first-root `v2(3y0+1)=1`.

This is stronger than merely observing positive aggregate counts: it is an explicit common point on the exact shared tuple.

## Why later no-go results do not create a hidden contradiction

RL219 and RL220 proved useful no-go theorems for proposed certified-blue/backward constructions, but their frozen ledgers explicitly delete zero arithmetic candidates.  Their hypotheses live on construction parameters (seed/word/pump/star families), not automatically on the candidate tuple.

RL221 therefore records a type-safe rule: a route theorem becomes a contradiction-core constraint only after a proved bridge from every scoped physical candidate to that route's hypothesis.  No such bridge is currently inherited.

The historical RL197/RL198 zero-edge intersection is genuinely empty, but only for the special co-owned zero-edge consumer.  It is not a counterexample-family contradiction.

## Exact obstruction

The surviving candidate system lacks one independent full-period discriminator.  RL214 already exposed the canonical place: if `D=2^A-3^L` and `Qfull(d)` is the complete-word numerator, root integrality gives `Qfull(d)=0 modD`, but the quotient/root residue needs `Qfull(d) modD^2` or equivalent information.

RL221 promotes that observation from a loose open item to the next falsifiable intersection target: derive a candidate-wise full-period residue, translate it to k, and intersect it exactly with the finite phase-51 family.

## Scope

No candidate, prefix, state, eta class, or rank is deleted by RL221.  The frontier remains 13,415,865,871.  Gate A, Gate B, branch contradiction and global nontrivial-cycle exclusion remain open.
