# RL222 target — Full-Period Quotient-Residue / Candidate-Wise Return Coupling

Date: 2026-09-01.

## Mission

Continue from the frozen RL221 type-safe intersection result.  Do **not** perform another broad route survey and do not retry the RL219/RL220 finite-library, pump, bounded-star, one-step-blue, b38, or raw-depth screens.

The next task is to derive **one genuinely new necessary predicate on the same e=16/H21 candidate tuple** and intersect it exactly with the finite phase-51 family.

## Primary target

Let

`D = 2^A - 3^L`

and let `Qfull(d)` be the complete phase-aligned binary-word numerator, so that for a physical root

`y0 = Qfull(d) / D`.

RL214 proved the bridge but also proved the current information is insufficient:

`Qfull(d)=0 modD`

does not determine the quotient/root residue

`(Qfull(d)/D) modD`.

Attack the first missing level:

`Qfull(d) modD^2`

or an exactly equivalent independent full-period p/z-arc return numerator congruence.

## Required bridge to the finite candidate family

Every live prefix has

`y0 = y0_* + 3*2^58 k`

on a short exact finite k-window, already intersected with terminal Hensel and the RL217 phase-51 2-adic cylinders.

Any new residue must be translated explicitly into a predicate on `k` (or on the equivalent `eta`/candidate tuple) and then intersected with those existing windows.  Do not stop at a formal congruence that has no candidate-wise projection.

`D` is odd and coprime to `3*2^58`, so a root residue modulo D gives an exact k residue modulo D.  Because the current k-windows are tiny by comparison, a genuinely determined root residue is potentially a singleton or empty selector.

## Success criteria

A successful RL222 should produce at least one of:

1. an exact new candidate deletion, with the affected prefix/k tuple and a gap-free verifier;
2. a theorem-quality strict selector that reduces the current 139,581,280 family even if it does not delete a prefix/rank;
3. an empty intersection proving the scoped branch contradiction, with minimal assumptions identified;
4. if the quotient-residue route is itself structurally blind, a theorem proving that blindness and identifying the next **candidate-wise** missing return datum in falsifiable form.

## Falsification discipline

A claimed necessary residue theorem must be replayable on explicit candidate tuples.  If an exact tuple survives both the new predicate and all inherited RL221 conditions, it falsifies any claimed empty core.

## Frozen locks

- RL221 explicit witness proves the already-selected five-family core is nonempty.
- Route/construction no-go theorems are not candidate predicates without a physical-to-route bridge.
- Arithmetic candidates are not physical H21 incidences.
- `Qfull(d)=0 modD` is not the quotient residue.
- No Gate closure is inherited.

Keep the proof-state classifications and verification-economy rule unchanged.
