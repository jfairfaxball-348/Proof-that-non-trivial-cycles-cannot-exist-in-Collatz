# RL306 proof and scope ledger

Date: 2026-09-12

Final classification:

`SCALAR_WALL_HARD_PIVOT_AND_AFFINE_SHELL_OWNER_REDUCTION_PROVED`

## Promoted analytic / exact derived results

1. Conditional clipped wall-debt theorem under `Bcal(8)<=3`:
   `y=max(0,X-3)` satisfies `0<=y<=3` and exact normalized rewrites obey
   `y_source<=clamp(y_owner+w,0,3)`.

2. Direct wall-merger acyclicity under rank
   `r(L_D)=2D+1`, `r(RW_D)=2D`.

3. Universal gateway theorem for `S=(d,K)` and `O=(d+1,K+3^d)`:
   the `S0/O1` children coincide, while the complementary `O0` child is `P` composed with the `S1` child whenever physical.

4. Exact complementary leading-P siblings for all four inherited RL303 `D -> D-2` merger cylinders, completing their final-bit partitions.

5. Fixed-96 P/Q hard-pivot obstruction: the six-column zero-relative-cost cut leaves all twelve residual types at arbitrary stack depth with an unbounded load-bearing left context. In particular the `G_2` residual returns to the fixed-seed renewal factor and admits contexts of arbitrarily large Bellman threat. This meets the authoritative RL306 hard-pivot criterion.

6. Checkpoint-8 first-return corridor:
   `u_b=(J_b+1)/2 <= 3(3/2)^A` at first boundary return of cost `A`.

7. Even first returns from checkpoint 8 cannot violate the literal `+3` scalar ceiling.

8. Dangerous odd first-return branches from 8 satisfy the exact high-drift inequality and are forced onto the principal representative of their retained parity cylinder.

9. Minimal-scalar first-failure resonance gains two bits over the old excess-one first-failure setup: precision `2^(A'+d'+5)`.

10. Fixed-source shell identity:
    `Bcal(s)=sup_k(k-M_s(k))`; hence `Bcal(s)<=T` iff `M_s(k)>=k-T` for all `k`.

11. Arbitrary-source affine-ballot bijection with
    `g_S(u)=3^d(u+1)-K-1`, including the exact converse from ballot-positive paired ordinary Collatz trajectories to canonical futures.

12. Cascade contravariance:
    `g_(A o B)=g_B o g_A`.

13. Exact seven-source affine atlas:
    `27u-1`, `3u-7`, `27u+2`, `81u+26`, `9u+22`, `9u+89`, `27u+47` for the RL305 seven scalar sources.

14. Checkpoint-8 forced free affine launch to `(2,15)` and exact balanced numerator
    `2^N J=7*3^r+3Q_x-Q_y+2^N`, with its Ferrers expansion.

15. Exact shell P-source recurrence for `k>=2`:
    `M_P(k)=2+min(M_R3(k),M_8(k),M_D0(k))`.

16. Exact shell version of the RL292 five-state front door and the combined seven-shell identity
    `M_seed(k)=3+min(M_R3,M_8,M_D0,M_439,M_-17-2,M_-84-1,M_-28)(k)`.

17. Exact reduction: checkpoint-8 pointwise shell ownership over the six shifted competitors would imply
    `M_seed(k)=3+M_8(k)` and reduce Gate A to the single checkpoint-8 shell inequality `M_8(k)>=k-3`.

18. Common-right-shadow identity
    `g_seed(u)=g_8(u+7)`.

## Conditional / partial results

- The D0 odd F/S cylinders are scalarly safe under the inherited R3/8 scalar assumptions, but a complete D0 F/A prefix cover is not proved.
- The clipped wall-debt theorem assumes `Bcal(8)<=3`, which remains open.
- Complete final-bit partitions are proved only inside the four inherited prefinal wall macro cylinders, not for all wall prefixes.

## Exact finite certificates / regression evidence

- Fresh closeout structural verifier: GREEN; see `RL306_FRESH_VERIFICATION.txt`.
- Checkpoint-8 minimum-shell target certified by exact session closures through `k<=28`; next unresolved shell `k=29` requires `M_8(29)>=26`.
- Exact source-8 affine ballot enumeration through length 28 found no nonempty score above 1; this is evidence for the stronger excess-one conjecture only.
- Fixed-seed shell equality `M_seed(k)=3+M_8(k)` observed throughout the exact certified range `k<=21` available in the session closure; evidence only.

## Method barriers / falsifications

- Fixed-96/twelve-factor P/Q continuation has an irreducible unbounded context parameter with no decreasing credit; freeze/pivot is mandatory under RL306 policy.
- Per-Ferrers-cell valuation Lipschitz is false even in the checkpoint-8 source specialization.
- Naive local scalar prefix induction fails on legal states assigned fictitiously low ancestry credit.
- High retained drift alone does not force a large principal parity representative.
- The stronger rigidity `nu_2(J_end)>=A => J_end=8` is false; an exact length-194 cost-21 path reaches `2^21`.
- Generic cap extension is not a scalable proof route.

## Open

- checkpoint-8 shell-owner theorem / common-right-shadow exchange;
- `M_8(k)>=k-3` all depth;
- `Bcal(8)<=3`;
- `Bcal(P)<=1`;
- O1/O2/P8;
- the individual seven RL305 Bellman ceilings except insofar as they are reduced by the shell identity;
- Gate A;
- Gate B;
- global non-trivial-cycle exclusion.

## Frozen

- RL304 fixed-96/twelve-factor P/Q grammar, including all exact commutation identities;
- physical/resonance selector frontier `a=7354673373747273032`;
- Radius-4/local obstruction work;
- Radius 6+;
- separate Lean formalisation project.
