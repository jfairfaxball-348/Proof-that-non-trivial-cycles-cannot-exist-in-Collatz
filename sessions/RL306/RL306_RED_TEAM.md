# RL306 red team

Date: 2026-09-12

Result:

`RL306_RED_TEAM_PASS_FOR_PROMOTED_SCOPE`

## Claims explicitly rejected or narrowed

1. **No complete weighted wall transducer was claimed.**
   Direct mergers are acyclic and four macro cylinders have complete final-bit partitions, but global prefix coverage remains absent.

2. **No P/Q all-depth closure was claimed.**
   The fixed-96 cut was instead shown to retain an unavoidable unbounded context parameter with zero relative historical contraction. This triggered the mandated hard pivot.

3. **No generic P-insertion monotonicity was revived.**
   Inherited counterexamples remain binding.

4. **No checkpoint-8 excess-one theorem was claimed.**
   Large exact finite affine-tree tests support it, but the authoritative requirement remains only `Bcal(8)<=3`.

5. **No generic height-cap strategy is recommended.**
   Exact closures through shell `k<=28` are certificates/diagnostics only.

6. **No first-return theorem was silently promoted to the recursive Bellman theorem.**
   The corridor/principal-representative result controls only the first checkpoint-return macro from 8.

7. **No per-cell carry bound is used.**
   A source-8 one-cell example jumps from valuation 0 to 17 after substantial area has already been paid.

8. **No local scalar induction is used.**
   Legal states with fictitiously low assigned credit can violate the candidate local inequality. Minimum ancestry area is essential.

9. **No high-drift size theorem is claimed.**
   Long high-drift parity cylinders may have small principal representatives.

10. **No return-to-8 rigidity is claimed.**
    The exact cost-21 path to `2^21` disproves it.

11. **No shell-owner theorem is claimed.**
    RL306 proves the exact reduction showing why it would suffice and supplies finite evidence; RL307 must prove or falsify it.

12. **No coordinate collision is allowed.**
    RL302 canonical `K` is consistently `K=J+2^d-1`; the affine atlas is recorded in both `K` and `J` interpretations where needed.

## Scope check

Still OPEN: Gate A, Gate B, `Bcal(P)<=1`, `Bcal(8)<=3`, checkpoint-8 excess-one, O1/O2/P8, global cycle exclusion.

Frozen routes remain frozen unless the successor target explicitly reactivates them.
