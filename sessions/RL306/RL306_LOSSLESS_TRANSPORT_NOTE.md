# RL306 lossless transport note

The successor can resume without conversation history by reading:

1. `authoritative/START_HERE.md`;
2. `authoritative/RL307_CHECKPOINT8_SHELL_OWNER_AFFINE_BALLOT_GATE_A_TARGET.md`;
3. this session's `RL306_CLOSEOUT.md`;
4. `RL306_REPORT.md`;
5. `RL306_PROOF_AND_SCOPE.md`;
6. `RL306_RED_TEAM.md`;
7. `RL306_SCRATCH_FREEZE.md`;
8. `verification/verify_rl306_closeout.py` and `RL306_FRESH_VERIFICATION.txt`.

The fixed-96 P/Q work is intentionally frozen, not discarded. Its exact state is inherited from RL304 plus the RL306 hard-pivot diagnosis recorded here.

The primary live object is now the fixed-source shell minimum `M_s(k)` and the common-right-shadow identity `g_seed(u)=g_8(u+7)`.
