# RL128 session state and RL129 kickoff

## Completed RL number

`RL128`

## BASE_HEAD

`794a45d6b5225e5ab8267750db4337cab8c47275`

## Frozen promoted result

The complete positive `L=12` range is excluded. The inherited primitive ordinary frontier is now `L>=13`.

For `L=12`, positivity begins at `Z=8`. The inherited `H` floor analytically excludes every `Z>=41`; the exact corrected capacity/ownership certificate excludes the complete residual `8<=Z<=40` with zero `D|Q` hits.

New analytic compression: `D|Q` is invariant under one-bit cyclic rotation, hence under every cyclic root. One canonical-root test per primitive run-pair orbit is therefore ownership-complete for this necessary condition.

Exact RL128 certificate totals:

- capacity profiles: `1,559,151,385`;
- periodic: `8,839`;
- canonical primitive representatives/tests: `160,979,484`;
- orbit weight: `1,559,142,546`;
- divisibility hits: `0`.

## Correction/demotion ledger

RL126's empty-depth-fibre repair remains in force. No new inherited result is demoted.

The earlier pre-RL127 exploratory `L=12` chunks were never promoted; RL128 was rerun only after RL127 became committed `BASE_HEAD`.

## Frozen global scope

- primitive ordinary low-odd-count frontier: `L>=13`;
- Gate A: open;
- Gate B: open;
- global nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

## RL129 kickoff

Attack `L=13` with the inherited `H` floor and width ceiling first, then enumerate only the resulting finite residual using corrected nonempty-fibre CRT capacity. Reuse RL128 cyclic-root divisibility invariance and exact run-pair orbit compression. Preserve all RL20/RL79/RL81, primitivity, Raw/Farey, and certificate-scope red teams.
