# RL325 — remaining R1 global ownership bridge

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED

## GLOBAL PROOF ROADMAP

`CURRENT_STAGE = R1`
`CURRENT_STAGE_NAME = Parent Bridge`
`GLOBAL_PROOF_STATUS = OPEN`

The smallest theorem that advances the current roadmap stage is the remaining `K<0` parent bridge below.

## Absolute objective

Close the remaining R1 parent obstruction in the genuine ordered `g=2` late-row architecture.

At the conditional first external survivor, RL322 removes `Z0<0`, and RL324 bounds the original least-root canonical interface on `K>0`.

The only live canonical sign regime is therefore

`Z0>0`,
`K<0`,
`c0=-n<=-1`,
`M=m-n<m`,
`1<=n<2^35`.

RL324 also supplies a genuinely physical bounded adjacent crossing box:

- rank `j`: zero carry / bottom-third residual;
- rank `j+1`: unit-minus-one quotient / small complementary residual;
- both matched displacements are `<=77265916075` in externally conditional first-survivor scope;
- the original least-root canonical rank lies strictly after this transition.

## Primary theorem target

Using the genuine global full-`D0/H` ownership between the adjacent crossing box and the original least-root canonical cut, prove at least one uniformly in the remaining support:

1. contradiction;
2. a strictly smaller genuinely owned reduced balanced return;
3. an explicit absolute bound on the original canonical `(r,s,beta)` interface;
4. a genuinely owned bounded witness already excluded by a frozen theorem/certificate whose size is independent of the original support.

Any one, with correct scope, closes the current R1 blocker.

## First attack

Seek a telescoping/elimination identity across the entire matched-rank interval from `j+1` to the canonical rank, using the genuine complementary numerator identities and actual physical ordering.

The aim is to eliminate the individual unbounded odd-to-odd gaps rather than bound them one by one.

Useful frozen structure includes:

- `2^d A-C=-H Delta` at every genuine matched cut;
- the phase-aligned `H`-carry ownership identities from RL323;
- the unique linear sign transition from RL324;
- canonical `c0=-n`, `1<=n<2^35`;
- the global least-root/minimality location;
- the two-rank bounded crossing box.

## Binding barrier

RL324 proves an exact local physical-run counterfamily in which one next matched displacement can be arbitrarily large while the positive defect remains tiny.

Therefore do not make the primary attack:

- rank-by-rank displacement propagation;
- local residual sharpening;
- local least-state lower bounds alone;
- support enumeration;
- bare modulus escalation.

Any successful theorem must use a genuinely global ingredient absent from the RL324 counterfamily.

## Roadmap transition discipline

Do not declare R1 closed merely because another crossing identity, finite support bound on an extracted rank, or mismatch clock is obtained.

Advance `CURRENT_STAGE` to `R2` only if the parent bridge itself is closed at the required scope.

## Preserved scope

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49547666544`.
External `2^71` least-state floor remains conditional.
Gate A OPEN.
Gate B OPEN.
`g=1` separate.
Global positive non-trivial-cycle exclusion OPEN.
