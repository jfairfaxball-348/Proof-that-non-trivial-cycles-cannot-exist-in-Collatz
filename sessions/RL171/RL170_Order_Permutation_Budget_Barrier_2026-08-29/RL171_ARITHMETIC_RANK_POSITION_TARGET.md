# RL171 — arithmetic rank-position target

RL170 proves the universal full permutation rank budget is slack. Seek a
specific arithmetic correlation between chronological position and
lifted-defect rank that strengthens the universal `sum q_j r_j` bound.

Stay in the coprime `g=1`, ordinary-`+1`, physical least-root branch. Do not
reuse individual ranks, monotone chains, universal permutation sums,
counts/products, bare residues, fixed-radius conditions, phase-order
shortcuts, or rank-one closure.
