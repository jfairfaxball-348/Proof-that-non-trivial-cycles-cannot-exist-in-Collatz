# RL335 target — all-length owned-return closure

## Stage
R1 — Parent Bridge. Incoming progress: 90%. Global proof status: OPEN.

## Incoming exact state
Work in the ordered genuine `g=2`, `Z0>0`, `K<0` branch at `(a,ell)=(217976794617,137528045312)`, with the inherited conditional least-state floor and fixed-high-carry ownership regime.

RL334 proves:

- zero q-runs have length at most 37;
- refined ownership graph 385 states / 15,835 edges;
- `22(K-2H)-S<=44`, so `2H>=K-S/22-2`;
- all-rho carry cap `1<=n<=32546313237`;
- self-consistency at bootstrap `32546313238`.

## Unique live target
Close or materially advance the remaining **all-length >=4-positive owned-return tail**. The simplest q=23 projected obstruction is `N(29)->L(29,22)->N(29)`, with total positive defect 5 and slack 113. It is conservative because the return represents an arbitrary run of at least four positives.

Do not attack this by blind finite run-length enumeration. RL334 already confirms that excluding finite lengths merely exposes another long-run projected cycle.

Preferred theorem forms include any one of:

1. an all-length physical exclusion of the offending owned returns;
2. a support-uniform additional slack/charge theorem for every >=4-positive return strong enough to remove the q=23 obstruction or close the consumer directly;
3. an affine-constant or exact-state invariant proving a genuine owned descent/contradiction for such returns;
4. another theorem that closes the remaining ordered parent branch outright.

Preserve all RL334 exact certificates and do not weaken the q=22 theorem, zero-run theorem, all-rho consumer, or self-consistent cap.

## Binding barriers

- RL324 local propagation remains invalid.
- RL329 finite p-by-p enumeration is not an all-length closure route.
- Do not use cyclic-wrap arguments rejected in earlier correction ledgers.
- The external `2^71` floor remains conditional and must stay so labelled.

## Advance criterion
R1 closes only when the remaining ordered parent bridge is eliminated or replaced by a proved genuine owned descent/contradiction/absolute closure theorem. Otherwise report the exact residual all-length obstruction.
