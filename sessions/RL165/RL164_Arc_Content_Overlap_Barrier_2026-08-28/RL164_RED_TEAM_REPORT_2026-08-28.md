# RL164 red-team report

PASS.

- Verified `B_j>=p` only from actual positive exponents, not count data.
- Re-derived `rho_j>1/2`, `lambda<2`, and the strict `2^81` shallow-state bound with the inherited `g=1` hypotheses.
- Checked the two decompositions of the same `p+1` physical steps; both the exponent and affine-constant overlap identities include their endpoint terms.
- Did not claim that the overlap recurrence is the only possible constraint, or that residue compatibility implies full ownership.
- Retained RL104/RL108 inverse-cylinder barriers, RL162 phase order, and all `g>1` limitations.
