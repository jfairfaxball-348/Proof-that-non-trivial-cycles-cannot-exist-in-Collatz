# RL112 authoritative start — New Attacks Priority

Read in this order:

1. `RL111_TO_RL112_FREEZE_AND_FULL_AUDIT.md`;
2. `RL112_NEW_ATTACKS_PRIORITY_TARGET.md`;
3. `audit/LEMMA_AND_RESULT_REGISTRY.md`;
4. `verification/verify_rl112_audit.py`.

This package freezes the RL111 owned-sparse-support route and changes the live
task to a disciplined audit plus pre-tournament attack design.  It makes no
claim of a Gate closure, non-trivial-cycle exclusion, or Collatz closure.

The requested label `RL101` is already occupied by the frozen session under
`sessions/RL101/`.  Preservation of that history requires this new consecutive
handover to be labelled `RL112`; this is an explicit naming repair, not a
renumbering of historical work.
