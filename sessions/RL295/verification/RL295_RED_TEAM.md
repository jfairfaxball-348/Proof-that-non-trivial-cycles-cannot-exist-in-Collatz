# RL295 target-specific red team

Date: 2026-09-11

Result:

`RL295_RED_TEAM_PASS_FOR_PROMOTED_SCOPE`

## Promoted scope under test

- exact zero-depth wall normalization;
- double-E wall refactorization;
- all-depth nonexceptional `(4,39)` P-owner sector cut with unique residual `00 -> (5,191)`;
- exact P -> Q17 owner certificate;
- all-depth Q_d same-state owner splice;
- finite pre-Q17 `(6,807)` side-sector enumeration.

No complete `(4,39)` closure, no `Bcal(P)<=1`, and no Gate-A closure is claimed.

## Checks against inherited mandatory attacks

1. **Positive zero-height retention / trivial cycle.** The wall theorem is an identity of composite physical states. Zero-height boundary motion may be inserted or erased without changing the normalized wall debt. The Q17 proof uses endpoint equality, not a chosen boundary history.

2. **RL292 unbounded static pullbacks.** No global claim that all checkpoints are cheaply P-owned is made. The promoted owner statement concerns explicit states/families only.

3. **Cylinder isometry / finite-suffix transparency.** No bounded suffix is used as a selector. Parametric `(4,39)` sectors are solved by exact algebra, and Q_d by exact same-state splicing.

4. **Neutral `(101)` gauge.** Historical cost comparisons are attached to physical endpoints and exact words; inserting a neutral gauge cannot invalidate a cheaper witnessed route.

5. **Branch mergers / equal-area diamonds.** Wall normalization is state algebra, not history data. At mergers, min-plus ownership takes the minimum witnessed cost.

6. **Adjacent-swap valuation jumps.** No valuation-Lipschitz assertion is made.

7. **Homogeneous `(010)^R` danger towers.** Not excluded globally; irrelevant to an exact same-state owner splice after Q17.

8. **Shifted phase-reset / `10922->6146`.** No phase-only invariant is used.

9. **RL290 Bellman chains `2514->...->8192` and `1074->1364`.** The promoted statements do not replace the recursive Bellman operator with one-generation hazard.

10. **Locally legal but globally unreachable predecessor families.** Every P-owner claim used in the promoted scope is a forward legal replay from P.

11. **RL293 immediate-danger family.** No new assertion about that family.

12. **H<=60 owner exceptions J=2,3,6.** The promoted `(4,39)` family uses explicit owner words, not the relaxed H<=60 theorem. Q17 uses an explicit full certificate.

13. **Principal-cylinder restriction.** Not broadened.

14. **Cascade-specific wall gauges.**
   - `I=(0,0)` is removed immediately.
   - nonzero `W_u` is absorbed exactly and never evolved with artificial negative cost;
   - the cross-term is not used as a rejected-tube payment;
   - no residue-only debt scalar is introduced.

## Certificate integrity

The portable verifier replays the full P -> O17 -> B17 -> Q17 certificate and independently checks the canonical recurrence at every column. The 40-column B17 -> Q17 segment remains at depth >=2 after launch until its endpoint, so its stated 154 area is a genuine positive excursion with no hidden zero-height return.

The all-depth Q-tail theorem is analytic from the exact two-column recurrence `Q_d --10--> Q_(d+1)`; the verifier checks d=17..80 as regression.

## Deliberate nonpromotion

Finite search reductions from `(5,191)` through `(5,201)`, `(6,733)`, `(6,949)`, and `(6,807)` are excluded from the promoted proof state where their individual owner words were not retained. They are frozen as successor leads only.

This prevents a search transcript from being mistaken for an auditable theorem.
