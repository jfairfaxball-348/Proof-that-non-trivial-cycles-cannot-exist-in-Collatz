# Inherited state

The load-bearing inherited wide-tier certificate and original deep `r<=300,000` raw chunks are copied into `verification/raw/` for local fast aggregation. Historical proof-state outside this handover remains accepted under the verification-economy rule after the incoming checksum/manifest/fast-verifier gate passed.
