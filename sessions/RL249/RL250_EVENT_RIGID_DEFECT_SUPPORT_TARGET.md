# RL250 — beta=6 event/rigid defect-support classification target

Status: **PREPARED, NOT STARTED**.

## Frozen incoming frontier

RL249 closes as `R4_BRIDGE_REDUCED`.

On Branch C with `beta(P)=6`:

1. there are exactly six isolated negative roots, all `-1`;
2. total positive `P` mass is exactly 8 and `P_i in {-1,0,1,2}`;
3. `sum_i |P_i|=14`, so nonzero support has at most 14 roots;
4. `M_q=#{i:u_i!=u_(i+q)}=sum|Delta P_i|` and `12<=M_q<=28`, with equal mismatch orientations;
5. `a>=123`, at least 109 roots have `P_i=0`, and there is a disjoint length-7 factor satisfying `u[j..j+6]=u[j+q..j+q+6]`;
6. every ordinary zero run `0^L`, `L>=10`, forces a q-shifted `0^(L-9)` block;
7. `log_2(3)<a/ell<=65/41`;
8. RL248 remains frozen: at least four selected shifted singleton `10` signatures are wholly internal, `beta(E)>=16+H_I`, `H_I>=e_I`, exact event-versus-rigid arms and terminal selectors hold, every physical prefix scale exceeds `3^-8`, and three selected internal `01` gains have aggregate `>2/6561`.

Binding demotions from RL249 must be respected: no historical/current q identification, no quadratic desert theorem, no q-ordered B=13/14 theorem, no local-shape-to-global-Radius4 shortcut.

## Unique mission

Classify the beta=6 defect support against the **canonical event-versus-rigid dynamics**.

For the six negative singleton swap signatures and their compensating positive mismatch support, prove at least one of:

1. enough selected shifted `10` blocks must take canonical descent/event arms that their exact accumulated height/rank contribution closes Gate A on beta=6;
2. the rigid alternatives plus the at-most-28 mismatch support contradict exact full-phase terminal ownership `(d,J)=(1,2^k)`;
3. the constrained support produces an eligible primitive positive/full-D exact cyclic-distance-4 self-rotation satisfying every RL238/RL247 hypothesis line-by-line.

Use the mandatory q-shift agreement corridor and zero-run propagation only as exact support constraints. Use phase gains with exact order/ownership/residue information; the magnitude lower bound alone is known insufficient.

## Preferred attack order

1. Label every negative singleton by RL248's rigid/event arm and count exact event-height cost.
2. Couple the at-most-14 mismatches of each orientation to the compensating positive support; exploit that positive `P` mass totals only 8.
3. Intersect these labels with the length-7 q-shift agreement corridor and long ordinary zero runs.
4. Push surviving rigid cylinders backward/forward through the exact `(d,J)` terminal selectors and mod-3/mod-4/mod-8 fingerprints.
5. Only if a full-D exact-distance-4 pair is produced, audit every Radius-4 hypothesis before application.

Gate A: open.
Gate B: open.
Radius 5: inactive.
