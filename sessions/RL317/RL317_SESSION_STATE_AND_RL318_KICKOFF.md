# RL317 session state and RL318 kickoff

Date: 2026-09-14
Completed RL: RL317
Incoming RL: RL318
Incoming status: PREPARED, NOT STARTED

RL317 proves that the two full `g=2` shadow compositions have numerators
`D0(HR-epsilon)` and `D0(Hx+epsilon)`. Positive `H|epsilon` gives strict
descent to a smaller primitive `g=2` cycle. A nonzero residue gives exact
complementary full-D remainders and a forced integer wrong-bit phase.

After exact content reduction, that branch is a coprime-content `T_h` shadow
cycle paired with the content-`h` scaled physical cycle. Standalone
generalized-increment invariants therefore hit the inherited RL79 barrier;
only a non-homogeneous cross-content consumer can advance it.

The independently replayed first reduced fibre is `(301994,190537)`. Exact
certificates give `56theta<1<57theta`, state ceiling `710,220,447,737` through
`g<=56`, and—using RL131's stronger inherited descent floor—the global
first-fibre cap `g<=9,355,556` with state ceiling `1,311,372,708,449` at the
cap.

Surviving `g=2` branches:

1. `epsilon=0`: distinct rankwise ordered rows;
2. `epsilon>0`, `h>1`: non-homogeneous content/coprime-content `T_h` pairing.

Authoritative target:
`authoritative/RL318_NONHOMOGENEOUS_CROSS_CONTENT_AND_ORDERED_ROW_TARGET.md`.
