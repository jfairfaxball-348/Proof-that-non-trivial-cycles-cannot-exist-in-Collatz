# RL161 certified facts and proof ledger

## New proved analytic mathematics

- **RL161-A:** a global functional homogeneous in all physical states and the
  generalized odd increment is increment-blind at degree zero and only scales
  at positive degree.  It cannot alone force ordinary ownership `s=1`.

## Deterministic exact certificate

- `RL161_CERTIFICATES/verify_rl161_homogeneous_bridge.py` exhausts primitive
  binary words of lengths 2--10 with `2^A>3^L`, verifies their canonical
  generalized cycles, and checks odd scaling for normalized and raw pairwise
  perimeters.

## Inherited facts used

- RL79 canonical generalized-increment cycle theorem and odd scaling
  covariance.
- RL160 classification that the new bridge must be genuinely global and
  ownership-sensitive.

## Method barrier

- Homogeneous weighted-difference/perimeter and normalized
  population/packing arguments are not ownership discriminators unless an
  independent non-homogeneous ordinary-`s=1` consumer is added.

## Corrections/demotions

No inherited theorem is demoted.  This is a scope classification of the
priority bridge families.

## Open obligations

1. Find a non-homogeneous, ordinary-`+1` content/lattice, basin, or absolute
   capacity theorem.
2. Couple it to full-phase ownership without reducing it to full-return
   monodromy or a fixed-radius observable.
3. Apply it to every putative non-trivial cycle in its claimed class.

**Global status:** open.
