# RL161 red-team report

**Outcome: PASS.**

- The proof changes the generalized increment from `s` to `cs`; it never says
  an ordinary `s=1` orbit itself can be scaled.
- It concludes only that an argument with the stated homogeneity cannot
  distinguish ownership.  It does not declare all global invariants dead.
- Raw positive-degree perimeter is described as a scale demand, not as a
  contradiction.  An absolute ordinary physical cap remains a valid consumer.
- The certificate tests exact recurrences, parity, primitivity, normalized
  invariance, and raw linear scaling with integer arithmetic.
- RL87's non-homogeneous first-Farey physical cap is explicitly outside this
  barrier.
