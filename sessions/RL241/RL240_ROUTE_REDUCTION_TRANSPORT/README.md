# RL240 route-reduction transport

Completed generation: **RL240 — Radius-4 global bridge selected-counterflow reduction**.

Classification: **`R4_BRIDGE_REDUCED`**.

Promoted outcomes:

- selected Bezout root translated into genuine physical rotation flows;
- two determinant-one same-root flows coupled exactly by the half-period flow;
- analytic linear counterflow bound `B(G)+B(Ghat) >= a-t-5+H >= ell+H-5`;
- negative selected flow translated into an owned physical-scale consequence;
- RL76/RL77 identify the remaining concentration barrier;
- successor RL241 targets full-phase selected-counterflow anti-concentration.

The canonical RL240 ZIP is carried losslessly as four base64 text parts in this directory. Verify `PART_SHA256SUMS.txt`, reconstruct with `reconstruct_rl240_bundle.py`, then check the outer authoritative `.zip.sha256` sidecar.

Canonical ZIP SHA256:
`eba20702838128f09f0afec763103b58c9b7a2f1f37cf6f7dcc60d253b691ae9`.

Gate A, Gate B, and global non-trivial-cycle exclusion remain open. Radius 5 remains inactive. Knowledge catalogues remain stale/deferred.
