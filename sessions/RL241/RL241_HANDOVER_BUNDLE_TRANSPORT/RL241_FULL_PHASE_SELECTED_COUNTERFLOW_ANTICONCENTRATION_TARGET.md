# RL241 — full-phase selected-counterflow anti-concentration target

## Mission

Continue the active Radius-4 global bridge only. RL240 reduced the Gate-B bridge to a concentration problem. Prove a full-phase / ownership-sensitive theorem preventing the linear negative counterflow carried by the two canonical same-root selected rotations from concentrating into a bounded number of arbitrarily deep valleys or long even-descending owned segments.

Do **not** start Radius 5.

## Frozen RL240 input

Retain:

- Radius-4 local theorem is proved and RL239-audited; Gate B remains open.
- For `ap-mell=1`, `0<m<a`, define `G_i=p-W_i(m)` and `Ghat_i=p+ell-W_i(m+a)`.
- Both are genuine physical rotation flows with signed mass `2`.
- `Ghat_i-G_i=J_(i+m)`, where `J_i=ell-W_i(a)` is the half-period flow.
- With `Ahalf=a-t-3+H`, `sum |J_i|=2Ahalf`.
- Therefore `B(G)+B(Ghat) >= Ahalf-2 = a-t-5+H >= ell+H-5`.
- For either determinant-one selected rotation, `F_i=-k<0` implies `x_(i+shift) > (15/16) 3^k R#` under the retained near-resonant bound, with `R#` the genuine least phase state.
- RL76/RL77 show that reciprocal packing, generic product growth, generic continued fractions, and high-state existence alone do not uniformly price deep physical scale.

## Primary target theorem

Preferred: prove one eligible same-root selected rotation has exact cyclic adjacent-transposition distance `4`, with every audited Radius-4 hypothesis matched.

Otherwise prove a quantitative **full-phase selected-counterflow anti-concentration lemma** showing the `Omega(ell)` charge `B(G)+B(Ghat)` cannot be carried by only a small number of deep valleys / long even tails in a genuine full-`D` one-excursion cycle. The result must be strong enough to force exact Radius 4 or yield a direct contradiction.

An equivalent root-specific / phase-specific owned-return obstruction is acceptable.

## Required discipline and red teams

1. Use the `X-Y` full phase essentially; stress-test against the exact `(65,41)` proper-factor model.
2. Do not rely on bare-word geometry or a monotone radius-shell principle.
3. Do not substitute generic state packing; RL76 documents the physical-scale barrier.
4. Do not substitute generic CF/product growth; RL77 documents that barrier.
5. Preserve the correction that the RL43 Bezout coefficient is not automatically the RL19 physical step parameter; use the proven window-count flows directly.
6. Do not assume blanket positivity of the half-prefix determinant path.
7. Apply Radius 4 only after an exact eligible encounter is proved.
8. Keep Gate A logically separate.
9. Radius 5 remains fallback only.
10. Reject finite experiments unless an exhaustive physical reduction is proved.

## Checkpoint classifications

- `R4_BRIDGE_PROVED`
- `R4_BRIDGE_REDUCED`
- `R4_BRIDGE_BARRIER`

Only `R4_BRIDGE_PROVED` permits global application of the audited Radius-4 local theorem.
