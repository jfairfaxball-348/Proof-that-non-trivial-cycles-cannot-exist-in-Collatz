# RL48 Research Output — Start Here

Read `RL48_RESEARCH_PROGRESS_2026-08-22.md` first.

Then read:

1. `RL48_SAME_ROOT_SELECTOR_AND_PHASE_COLLAPSE.md`
2. `RL48_PHASE_TO_RANK_DEFECT_BRIDGE.md`
3. `RL48_RANK_RELAXATION_BARRIER.md`

Run:

```bash
sha256sum -c SHA256SUMS.txt
bash run_all_rl48_progress_verifiers.sh
```

The phase-to-rank-defect note is deliberately marked historical-definition-audit pending because the exact RL43/RL44 source bundle is not contained in the RL47->RL48 handover.
