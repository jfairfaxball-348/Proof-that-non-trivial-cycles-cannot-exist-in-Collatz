# RL276 — one-sided absolute ownership consumer: reconstruction and common-mode barriers

Date: 2026-09-07

## Classification

Primary:

`GATE_A_EXACT_BARRIER`

Promoted subordinate results:

- `ABSOLUTE_QUOTIENT_RECONSTRUCTION_IDENTIFIED`
- `ZERO_CARRY_PHYSICAL_BOX_INSUFFICIENT_FOR_PUMP_SCALE`

Gate A is **not** proved. Gate B is unchanged/open. The fifth selector was not scanned. Radius 6+ remains frozen. No global non-trivial-cycle exclusion is claimed.

## 1. Incoming target and scope

RL276 was activated from RL275's exact common-mode barrier.

The frozen Gate-A target remains

`H_can >= k=t+3`

for every retained genuine full-phase object, at the terminal canonical state

`d=1, J=2^k`,

with inherited odd `k`.

RL276 was required to seek a theorem that is one-sided and absolute: it must consume information invisible under addition of a common discrepancy component to the paired auxiliary flows.

The two prescribed routes were:

1. RL65/RL66 ordered phase digits coupled to the finite full-phase carry/canonical-state decomposition;
2. the repaired RL231--RL237 chronological owned-defect programme, preserving every correction/demotion and saturation barrier.

## 2. Completed phase-ladder reconstruction

Retain the RL65 exact quotient equality

`(N-2)M = 237*3^r - 12*mathcalD - 2^(a-k+1)`

with

`M=2^a-3^ell`, `ell=r+3`, `mathcalD>=0`.

Reducing modulo the **full** `3^ell` gives

`N == 2 + 2^(-a)*(237*3^r - 12*mathcalD - 2^(a-k+1)) (mod 3^ell)`.

This extends the ordered RL66 phase reconstruction through the final two 3-adic digits: the term

`237*3^r = 79*3^(r+1)`

has 3-adic valuation `r+1=ell-2`, so it is exactly the term first visible in those top two digits.

Classification: **analytic reconstruction consequence of the exact RL65 identity**.

This does not create a second full-phase equation.

## 3. Archimedean box and unique absolute CRT representative

The inherited phase quotient satisfies `N>0` and `N==3 (mod 8)`.

The exceptional value `N=3` is impossible for the required full-phase half-return: under the half-Collatz map,

`3 -> 5 -> 8 -> 4 -> 2 -> 1 -> 2 -> 1 -> ...`

so it cannot realize the required `N -> N+4`, namely `3 -> 7`.

Hence `N>=11`.

Since `mathcalD>=0` and `2^(a-k+1)>0`,

`(N-2)M < 237*3^r`.

Using `N-2>=9`,

`M < (79/3)*3^r`.

Because `3^ell=27*3^r`,

`M/3^ell < 79/81`.

Therefore

`3^ell < 2^a < (160/81)*3^ell < 2*3^ell`.

RL242 later proved zero canonical drift:

`N=R_u`, `S_u=N+4`,

with `R_u` the canonical first-half dyadic residue. Thus

`0 < N < 2^a`.

Combining the full `3^ell` residue above with `N==3 (mod 8)` gives one CRT class modulo `8*3^ell`. Since

`0 < N < 2^a < 2*3^ell < 8*3^ell`,

there is at most one positive representative of that CRT class in the canonical dyadic box. The genuine full-phase state is therefore exactly that unique representative.

Classification:

`ABSOLUTE_QUOTIENT_RECONSTRUCTION_IDENTIFIED`.

This is stronger than the old "infinitely many CRT lifts" situation: the absolute quotient is reconstructed, not merely constrained to a residue class.

## 4. Mandatory red team: reconstruction is not contradiction

The unique representative above is still obtained from the same exact full-phase quotient equality that defines the phase digits. RL242's zero-carry theorem identifies that representative with the canonical residue `R_u`.

Therefore the completed `3^ell` selector plus the dyadic box is a valid **absolute reconstruction lemma**, but not an independent ownership condition. It must not be promoted as a Gate-A contradiction merely because the CRT representative is unique.

The surviving obligation is compatibility of that unique absolute representative with a genuinely independent one-sided ownership quantity.

Classification: **exact method barrier**.

## 5. Cross-generation pump-scale red team

RL74--RL77 isolated the canonical positive synchronized pump `c=10`. RL77 proved that `q` copies inside a genuine full half-word force the physical pump-entry scale `x` to satisfy

`x > [9(N-2)(N+4)/(43N+72)] * ((4/3)^q - 1)`.

RL242 later proved zero carry and the full-cycle physical box

`x < 2^(2a)`.

This later theorem does **not** repair the RL77 scale obstruction.

Indeed,

`9(N-2)(N+4)/(43N+72) < N/2`.

A `q`-fold `c=10` pump occupies `2q` half-word columns, so `2q<=a`. Hence

`(4/3)^q <= (4/3)^(a/2) = (2/sqrt(3))^a < 2^a`.

Using zero carry `N<2^a`, the entire RL77 lower-scale demand lies below

`2^(2a-1)`,

while RL242 permits every physical state to be below `2^(2a)`.

Thus even the later zero-carry physical box has enough room to absorb the **maximum repeat depth allowed by word length**. It cannot by itself bound the common physical scale parameter introduced in RL76.

Classification:

`ZERO_CARRY_PHYSICAL_BOX_INSUFFICIENT_FOR_PUMP_SCALE`.

This is an analytic method barrier, not finite evidence.

## 6. Paired-half zero carry does not remove the common scale

Applying zero carry to both half-return identities does not create another scalar equation. Eliminating the common state reproduces the already-known proper-factor/full-phase relation

`Q(u)-Q(v)=4(2^a+3^ell)`.

Thus the paired absolute half-return equations do not eliminate RL76's free common physical-scale parameter; they repackage the existing full-phase compatibility.

No determinant-zero descent is revived: RL75 already proves genuine primitive proper pumps are nondegenerate.

Classification: **exact algebraic barrier**.

## 7. Chronological route audit

The prescribed fallback route was checked against the frozen RL231--RL237 programme.

RL231 already tested a genuinely fixed-origin sign-order route and proved a saturation barrier: even ideal ordering of the certified corrected-flow mass does not by itself force escape from the canonical corridor.

The later repaired programme strengthens **absolute total variation**, but preserves the explicit "variation, not excursion" scope and saturates on a specific high-height ownership cell. Therefore those results cannot be reclassified as a one-sided Gate-A prefix/excursion theorem.

No withdrawn charge, potential, or saturation estimate is revived.

Classification: **inherited exact method barrier confirmed for RL276 scope**.

## 8. Routes explicitly not promoted

RL276 does **not** promote:

- the unique CRT representative as a contradiction;
- a new phase-digit contradiction beyond the exact quotient equality;
- any bound from RL48's separable rank relaxation;
- any use of RL263's automatic prefix congruence as independent;
- any finite physical-magnitude obstruction contradicted by RL264's affine-ray theorem;
- any fixed-origin chronological excursion theorem not actually proved by RL231--RL237;
- any unfinished "rank-frontier/common-mode" estimate explored after the last checkpoint;
- any fifth-selector elimination;
- Radius 6 or higher enumeration.

## 9. Exact state after RL276

Gate A remains open:

`H_can >= k`

is not proved uniformly.

What RL276 does establish is a sharper localization of the missing theorem:

1. ordered phase data plus zero carry can reconstruct the **absolute** quotient `N`;
2. uniqueness of that absolute representative is still algebraically downstream of the existing full-phase equality;
3. the repaired chronological machinery supplies large total variation but not the required one-sided prefix excursion;
4. the later RL242 zero-carry physical box is quantitatively too weak to price canonical synchronized-pump depth.

The surviving theorem must constrain a **common-mode absolute ownership scale** itself. Useful forms include:

- a one-sided counterflow contradiction such as `beta(P)>z-k+4`;
- a genuinely independent boundary-coupled prefix/excursion theorem;
- an ordered-rank/canonical-state inequality that controls absolute rather than differential displacement;
- an equivalent direct contradiction to full-phase/full-`D` ownership.

## 10. Successor

RL277 should target only this remaining common-mode scale problem.

Do not repeat the completed CRT reconstruction, the paired subtraction identity, the coarse zero-carry physical box argument, or the repaired chronological total-variation route as though they were new.

The first task is to identify an exact bridge from canonical/full-phase origin data to one-sided counterflow or ordered absolute rank scale. The inherited RL248 zero-budget and RL253--RL262 window machinery may be used only with their exact promoted scopes; do not silently universalize Branch-C statements.

The fifth selector remains a controlled falsification laboratory only. Radius 6+ remains frozen.
