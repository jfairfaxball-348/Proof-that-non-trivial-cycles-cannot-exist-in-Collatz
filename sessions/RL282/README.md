# RL282 frozen handover

RL282 proves an exact terminal-backward 3-adic height-pricing theory for the cheap one-zero channel and, crucially, an exact local family showing why final-tail arithmetic plus the existing positive mass/`F` budgets cannot by themselves close Gate A.

Primary classification:

`TERMINAL_BACKWARD_HEIGHT_DIGIT_PRICING_AND_LOCAL_TAIL_BARRIER_PROVED`

The key positive theorem compresses any final positive boundary suffix to `(L,r,C_w)` and gives the exact valuation identity

`nu_3(2^(k+L+1)-2C_w+3^(r+1)) = r+nu_3(2J_0+3)`.

The key barrier theorem constructs, for every odd `k>=3` and every `h>=2`, infinitely many locally legal positive blocks

`J_in --0 1^h--> J_0 --0^q--> 2^k`

with total height `h` and arbitrarily long zero-height boundary tail.

Therefore Gate A remains open in the inherited authoritative residual:

`k>=5`, `k` odd, `H_can<k`.

The next target is upstream positive-checkpoint reachability versus accumulated height.

Start with:

- `RL282_TERMINAL_BACKWARD_HEIGHT_DIGIT_PRICING_AND_LOCAL_TAIL_BARRIER_2026-09-08.md`
- `RL282_CLOSEOUT.md`
- `RL283_UPSTREAM_2ADIC_CHECKPOINT_REACHABILITY_GATE_A_TARGET.md`
- `verification/verify_rl282_terminal_backward.py`
- `verification/RL282_FAST_VERIFIER_OUTPUT.txt`
