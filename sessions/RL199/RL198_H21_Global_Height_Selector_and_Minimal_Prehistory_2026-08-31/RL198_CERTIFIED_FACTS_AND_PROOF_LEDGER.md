# RL198 Certified Facts and Proof Ledger

Date: 2026-08-31. Conditional physical branch remains `(37,0,23,-1)`.

## Newly promoted analytic mathematics / exact synthesis

- RL186's physical zero-prefix law gives `v_2(C_start)=sum d_j>=tau`.
  Applying it to RL197's exact H21 starting numerators forces
  `e34=e33=1`.
- RL197's six local height states therefore collapse globally to exactly
  `011,111`.
- The RL197 canonical H21-owned p-shift zero-edge states `000,100` are globally excluded.
  In fact the whole co-owned `tau=35` zero-defect prefix contains no p-shift-compatible
  height-zero chronological edge.
- `C34=21*2^34` has valuation 34 and `C33=63*2^33` has valuation 33.
  Hence all 34 / 33 positive zero-prefix transition costs are exactly one.
- At `tau=35`, state `011` has all 35 costs one; state `111` has a leading cost two
  followed by the same 34 unit costs.
- One source before the H21 terminal, the mechanical rank lies in
  `[80448749305,98855162143]`, so its mechanical bit is 2.
  The common p-pair height there is exactly 20.
- The exact preterminal common owned numerator is
  `C_pre=14*3^34=233480543795331966`, with `C_pre mod 8=6`, and
  `3*C_pre/2=7*3^35`, the H21 terminal numerator.

## Exact certificate

`verification/verify_rl198_h21_global_height_selector.py` checks the selector arithmetic,
state intersection, valuation saturation, leading-cost dichotomy, preterminal rank/mechanical
bit, terminal height count, exact numerator and terminal identity.

## Inherited state retained

RL197's `1|212` local mechanics and six-state local classification remain correct at their
stated prehistory-free scope.  RL196's seam theorem and anchor, RL195's zero-incidence theorem,
RL194's global occupation/rank results, RL193's physical debt/carry incidence, RL191's
spacing/flow bounds, the H21 core/deletions, canonical terminal floor 67, and the binding
`{33,34,35}` ownership/charging budget remain inherited.

## Open obligations

The surviving global H21 states are `011,111`; RL198 does not decide `e35`.
The exact final preterminal numerator leaves one owned endpoint orientation/sign bit unresolved.
No H21 budget release, H21-family exclusion, sole-branch closure, Gate A/Gate B closure,
non-trivial-cycle exclusion, or global Collatz closure is claimed.
