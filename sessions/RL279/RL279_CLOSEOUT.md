# RL279 closeout

Date: 2026-09-08

Primary classification:

`SCALABLE_ZERO_RANK_RUN_COMPRESSION_PROVED`

Subordinate:

- `HEIGHT_BUDGET_ZERO_RANK_IDENTITY_PROVED`
- `UNIFORM_ZERO_MASS_CORRIDOR_PROVED`
- `BOUNDARY_CYCLE_SCALE_SIGN_THEOREM_PROVED`
- `ZERO_BUDGET_ONLY_HEIGHT_GROWTH_BARRIER_PROVED`
- `EXACT_SEVEN_ZERO_GATE_A_CONTRACTION_PROVED`

## Promoted frontier

RL279 converts the growing fixed-zero canonical search into a finite-dimensional structural description.

Promoted exact results include:

1. `K=J+2^d-1` gives a four-case affine transition law.
2. All-one runs compress by `nu_2(K)`; at `d>1` the macro has height charge `(nu_2(K)+1)(d-1)`.
3. The multiplicative prefix potential `P=2^(H+n)/3^r` changes by `2^d/3^x` per column and gives a constant Gate-A threshold on retained phase-box terminals.
4. Canonical height is exactly the total zero-rank lag:
   `H_can=sum(v_t-u_t)`.
5. The zero-rank identity decomposes into base zero mass plus a finite height-coupon budget.
6. The zero mass satisfies the uniform corridor
   `17/2<S<21`,
   giving a bounded exponential moment about the critical slope `log_(3/2)2`.
7. Zero-to-zero runs have scalar multiplier
   `2(2/3)^g`,
   and terminal scale depends on zero history through the final scalar weight plus the all-one tail.
8. The `d=1` neutral boundary subsystem is exactly accelerated `3n+1`.
9. Boundary endpoint potentials prove every positive neutral boundary cycle is scale-expanding and every negative neutral boundary cycle is scale-contracting, without classifying cycles.
10. An infinite exact retained family with `H_can=k=3` and unbounded `m0` proves that zero-budget-only height growth is impossible.
11. Supporting exact seven-zero replay proves
    `H_can<k => m0>=8 => z>=k+6`.

Gate A remains open. Gate B is unchanged/open and frozen. Fifth selector not scanned. Radius 6+ frozen. No global non-trivial-cycle exclusion is claimed.

## Verification

The portable RL279 verifier passes from the promoted recurrence and independently certifies the finite seven-zero claim plus bounded red-team checks of the structural identities.

The seven-zero search range is non-arbitrary: the exact six-zero sub-threshold gap yields `u7<=34`; the already-crossed branch gives `u7<=25`.

## Barrier / correction discipline

No inherited theorem was demoted.

RL279 does prove that one contemplated successor strategy is invalid: there is no uniform lower bound `H_can>=f(m0)` with `f(m0)->infinity` on the retained phase-box terminal class. The exact neutral-loop family lies on `H_can=k`, so this barrier does not weaken Gate A.

## Successor

RL280 should target the remaining height-positive excursion:

`d=1 -> d>1 -> d=1`.

The objective is a sharp scale/state-versus-height inequality compatible with the exact `K` macros, zero-weight multipliers, height coupons, and terminal scale formula. Neutral boundary cycles are already quotiented by their scalar sign law and should not be reclassified by solving Collatz cycles.

Further fixed-zero enumeration may be used only as supporting falsification evidence.
