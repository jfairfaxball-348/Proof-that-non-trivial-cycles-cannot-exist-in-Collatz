# RL255 — high-beta Branch-C parity/capacity target

Date prepared: 2026-09-05
Status: **PREPARED, NOT STARTED**

## Incoming state

RL254 proves for every simultaneous surviving Branch-C object:

`beta(P) >= 260`.

More precisely, with `H=19z-7a`, `n=19B-7q`:

- if `H,n` are both even, the 38-window relation factors through an exact 19-window identity and `beta(P)>=260`;
- otherwise `beta(P)>=355`.

Inherited consequences:

- `z>=287`;
- `a/ell<=149/94`;
- `beta(E)>=1032`.

## Authoritative target

Exploit the high-counterflow regime rather than returning to low-beta analysis.

Priority order:

1. Keep the halving/non-halving split explicit.
2. Combine `beta(P)` with the RL248 zero-budget inequality and exact scale/resonance arithmetic to contract the first surviving `(a,ell,z)` frontier.
3. Use Branch-C isolated-singleton geometry and 1-Lipschitz P-profiles to obtain an audited capacity bound linking `a`, `beta(P)`, and the positive mass `beta(P)+2`.
4. Feed the resulting scale/capacity floor into the genuine physical companion-envelope bound `beta(E)>=4(beta(P)-2)+H_I`.
5. Seek either a finite exact Gate-A contraction, an exact full-phase contradiction, or a fully eligible owned Radius-4 encounter. Do not infer Radius 4 from large beta(E) alone.

All RL249/RL250 demotions remain binding. RL251 Gabriel-horn equivalence remains frozen.

Gate A open. Gate B open. Radius 4 not invoked. Radius 5 inactive.
