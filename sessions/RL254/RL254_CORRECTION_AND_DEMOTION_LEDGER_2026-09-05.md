# RL254 correction and demotion ledger

Date: 2026-09-05

No inherited promoted theorem is demoted.

The RL253 exploratory statement `beta(P)>=118` is superseded: RL254 independently proves the stronger uniform `beta(P)>=260`, with `beta(P)>=355` in the non-halving branch.

Scope clarification promoted in RL254:
- RL249 stated the `65/41` rational sharpening inside its beta=6 session, but the proof ingredients used for that sharpening (`z>=46`, the retained scale window, and the coarse ratio corridor) are already general simultaneous Branch-C facts from RL248/inherited state. RL254 re-derives the comparison explicitly on the general Branch-C domain before using it.

Binding historical demotions remain unchanged:
- RL249 historical/current-q collision and downstream zero-desert/covering claims stay demoted;
- RL250 corrections stay binding;
- RL251 Gabriel-horn equivalence remains frozen;
- no shrinking-measure/cylinder-width emptiness inference is permitted;
- no Radius-4 invocation is made without exact ownership/distance/full-D eligibility.
