# RL109 session state and RL110 kickoff

Date: 2026-08-26

## 1. RL109 outcome

RL109 completed the requested post-basin global route re-audit. No Gate, RL/nontrivial-cycle, or Collatz closure is claimed.

The highest-ranked route is now a **global ordinary-`+1`, full-`D` owned sparse-rotation compression theorem**.

New analytic synthesis:

1. For every nonconstant binary word of length `A` and weight `L`,
   `dist_cyc(w,rot_1(w))=min(L,A-L)<A`.
2. Hence the globally closest pair of distinct rotations has cyclic radius `R_*<A`; every minimizing integer flow has a zero edge, so a common cut realizes the same distance as a linear adjacent-transposition path.
3. Each fixed-cut adjacent `10<->01` swap changes the ordinary unit word numerator by one signed monomial `+-2^i3^t`.
4. For actual ordinary owned cycle rotations, `Q(r)=D x_r`, so the closest-pair sparse transport is a **nonzero `R_*`-term signed multiple of `D`**.

The RL20 radius-4 fake is rejected at the premise because its exact `Q mod D` is nonzero. Generalized increment scaling gives only `D|sS`, so the unscaled `D|S` consumer is genuinely ordinary-`+1` sensitive.

## 2. Frozen global status

Retain exactly:

- primitive/full-`D` radius-3/small-radius engine: **closed local**;
- Gate A even terminal `k`: **closed analytic**;
- Gate A terminal `k<=25`: **closed exact-certificate corollary**;
- Gate A odd terminal `k>=27`: **open globally**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 3. Route ranking

1. full-`D` ordinary-owned sparse rotation compression;
2. global owned access to the closed radius-3 engine, viewed as the short-radius success branch of route 1;
3. Gate-A multiplicity/Farey coverage;
4. genuinely new direct full-phase ordinary-`+1` bypass.

Do not restart the frozen word-to-basin equality attack, raw Farey enumeration, normalized coboundary transport, or bulk cascade scanning.

## 4. RL110 target

Let `w` be a hypothetical primitive ordinary cycle word, `D=2^A-3^L`, and `R_*` the minimum cyclic rotation radius. Under the complement hypothesis `R_*>=4`, use the RL109 zero-flow cut and sparse transport

`S=Q(v)-Q(u)=sum epsilon_j 2^(a_j)3^(b_j)`

together with global ordinary ownership to prove

`0<|S|<D`

or another contradiction to `D|S`.

If instead the argument forces `R_*<=3`, hand the object to the inherited closed small-radius engine.

The first milestone is to derive the strongest globally valid upper bound on `|S|/D` and identify exactly which support/coefficient statistic prevents it from falling below `1`.

## 5. Mandatory red teams

- RL20 `D∤Q` fake;
- RL79 generalized-increment scaling;
- RL81 physical-vs-quotient separation;
- primitivity;
- raw multiplicity/Farey scope;
- external-input dependency;
- closure scope.

Barely rewriting `S=D(x_v-x_u)` is not progress. Any promoted inequality must bound the sparse side or physical side independently using new global structure.

## 6. Frozen cascade checkpoint

Retain without scanning:

- wide `(5,000,030,7,000,000)`;
- middle `(7,500,031,4,500,000)`;
- deep `(10,000,032,3,250,000)`;
- ultra `(15,000,035,2,005,000)`;
- `L_common=15,000,053`;
- active support `lambda=234375/3281264468752`, `mu=1/7000001`;
- first live odd raw-`g=1` first-Farey terminal `2,921,813,805`;
- branch upper endpoint `42,150,931,559`.

Verification economy and the sustained-attack rule remain in force.
