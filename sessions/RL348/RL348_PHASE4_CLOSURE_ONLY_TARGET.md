# RL348 target — PHASE 4 CLOSURE ONLY

Status: UNIQUE LIVE TARGET FOR RL348; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent
`(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited
ownership/pruning, live inherited high-carry scope where required, and external conditional
least-state floor `m>=2^71`.

## Sole objective

CLOSE PHASE 4.

Prove

`O_75 = empty`.

Equivalently, prove every exact physically owned complete q=0-to-q=0 return of length at least 75
which is phase-potential nondecreasing and satisfies all inherited predecessor/return/successor
mixed-adic, row/wrap, q-profile and least-state conditions is impossible or deterministically
descends below the inherited least-state floor.

This is the explicit and sole mathematical goal of RL348.

## Stop rule on success

If `O_75=empty` is proved, Phase 4 is CLOSED.

At that moment:
- stop mathematical research in RL348;
- enter closeout;
- freeze the Phase-4 closure theorem and verification;
- prepare a later successor for whatever comes next.

DO NOT begin Phase 5 in RL348, even after Phase 4 closes.

## Inherited RL347 contractions

RL348 may use the following promoted RL347 facts:

1. **Decorated rank decoder.** Exact q=0 phase tag plus row identity determines absolute physical
   rank in the `2ell` walk. Fixed decorated endpoints therefore determine one directed `L`, not two.
2. **Over-half endpoint geometry.** Any complete return with `L>ell` has strict late-row endpoints.
   Its complementary arc of length `R=2ell-L` contains every q=0 vertex.
3. **Extreme over-half exclusion in live high-carry scope.**
   `R>=23135982579`, hence `L<=251920108045`.
4. **Matched H-carry quotient.** After the unique crossing,
   `1/lambda<h_i<1`, with strict matched ranks decreasing `h` and contacts increasing `h`.
5. **Half-cycle contraction.** Any phase-nondecreasing `L=ell` return is a matched contact pair with
   even physical difference `D=E-S` satisfying `2<=D<=2^36`.

Read the RL347 correction ledger before using live-session scratch. In particular, do not use the
withdrawn claim that `L=ell` is impossible and do not use the withdrawn right-endpoint source-defect
estimate.

## Residual Phase-4 classes to close

The complete long-return residual is now organized as:

- `75<=L<ell`;
- `L=ell`: matched contact-to-contact, `D=E-S` even, `2<=D<=2^36`;
- `ell<L<=251920108045`: strict late-row endpoints, with every q=0 vertex on the short complement.

Exact decorated endpoints determine one `L` and exact `H`; RL346's inverse-carry decoder then leaves
at most one middle word.

## Preferred closure attacks

Use whichever exact route actually closes the residual, but prioritize:

1. **Half-cycle bounded-difference CRT closure.**
   Intersect `2<=D<=2^36`, contact decoration, exact `H=a`, predecessor/successor mixed 2/3-adic
   conditions and ownership/descent.
2. **Source-side H-carry/ownership skip theorem for over-half returns.**
   Combine the post-crossing quotient corridor with RL346 bounded incoming signatures and the genuine
   numerator identity; rederive inverse orientation from authority before using the unpromoted
   scratch candidate.
3. **Decorated deterministic decoder rejection for sub-row long returns.**
   Use the unique decorated rank/length plus terminal cone and mixed-adic acceptance tests to prove
   uniform rejection/descent without a length-by-length replay.
4. **Exact residual singleton**, only if a global theorem truly fails: isolate the first bounded
   physical signature that survives every mandatory test. This does not count as Phase-4 closure.

## Mandatory acceptance tests

Any Phase-4 closure must retain all of:

- predecessor mixed 2-adic congruence and oddness;
- successor mixed 3-adic congruence;
- exact q-record/profile conditions;
- exact source/end row-phase decoration;
- early-row q=0 contact law;
- genuine two-row wrap orientation;
- inherited physical ownership/pruning;
- exact phase-potential sign;
- deterministic least-state descent below `2^71` where used.

## Hard prohibitions

RL348 must not:

- work on Phase 5, even after Phase 4 closes;
- work on R1 post-Phase-4 assembly;
- start Gate A, Gate B, R2, or any later roadmap stage;
- restart `G_72` total-gap enumeration;
- use the invalid RL346 `G_72=76` scratch enumeration;
- restart sigma, coefficient, q-upgrade, or old linear-consumer ladders;
- import old P/Q conclusions without an exact bridge to the current physical q=0 interface;
- promote the withdrawn RL347 orientation claims.

## Success condition

The only success condition for RL348 is the theorem

`O_75=empty`.

Nothing short of that closes the target. Once obtained, finish RL348 immediately.
