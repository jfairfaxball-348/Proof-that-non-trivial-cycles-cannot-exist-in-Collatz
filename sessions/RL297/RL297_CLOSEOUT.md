# RL297 closeout

Date: 2026-09-11

RL297 closes as

`P_BOTTLENECK_RISK_AUDIT_WEAK_GREEN_SHARED_ENDPOINT_STRUCTURE_IDENTIFIED`.

## What RL297 established

The exact positive P cone was exhaustively checked through added area 27. Among 5,962,876 positive even checkpoints in scope, the maximum Bellman endpoint score was 1, attained uniquely by checkpoint 8 at added area 2. No score >=2 witness was found.

A large exact checkpoint-distance comparison also found

`m_P(E)=m_8(E)+2`

for every tested positive even checkpoint `E != 2`, with the relation holding across 36,207 nonexceptional common checkpoints through the larger comparison window.

The dual-coordinate identity

`N(P)=T(8)=7`

provides a concrete shared-endpoint mechanism for this phenomenon.

## What RL297 did not establish

RL297 does **not** prove:

- the universal P/8 shortest-path identity;
- `Bcal(8)<=3`;
- `Bcal(P)<=1`;
- complete non-P front-door closure;
- Gate A;
- Gate B;
- global non-trivial-cycle exclusion.

The Gate-A route therefore remains **weak green** rather than proved.

## Frozen scratch

The paired-state shared-endpoint rewrites and normalized `(delta,r)` transducer observations in `RL297_REPORT.md` are useful continuation material but are not promoted as an all-depth theorem.

## Strategic closeout

This session was an audit, not a selector session. No fifth-selector attack was undertaken.

The audit found no red result requiring abandonment of the current Gate-A route. Per user direction, the next authoritative research session should therefore return to the previously frozen selector/branch programme, beginning with the fifth retained selector after RL262's elimination of the fourth.

Gate A should remain parked at this weak-green audit checkpoint while that selector work proceeds, unless new mathematics later turns the audit assessment red.

## Scope

Gate B remains separate/open.
Radius 6+ remains frozen.
RO/divergent-orbit work remains out of scope.
The Lean formalisation project remains separate.
