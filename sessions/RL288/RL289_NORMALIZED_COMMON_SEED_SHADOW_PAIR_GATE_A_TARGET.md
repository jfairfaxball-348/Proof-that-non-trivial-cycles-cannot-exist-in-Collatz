# RL289 — normalized common-seed shadow-pair Gate-A target

Date prepared: 2026-09-09
Status: PREPARED, NOT STARTED

## Incoming classification

RL288 closed as

`FIXED_SEED_FIRST_DEVIATION_SYNCHRONIZATION_AND_LOCAL_PHASE_BARRIERS_PROVED`.

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

## Frozen RL288 conclusions

RL288 establishes:

1. the first positive state selected from the fixed seed satisfies the all-depth candidate `nu_2(K-1)<=H+d-1`;
2. simple `M=K-1` and shifted `T=J+2^d-3^d` magnitude barriers can first escape only through zero-height boundary amplification, but arbitrary safe reverse boundary rays prevent these local phase arguments from becoming global;
3. coupling `M` and `T` collapses to the already inherited two-template boundary hazard rather than producing a finite closed shifted-valuation invariant;
4. for the common seed `-7`, if a word first deviates from the genuine parity itinerary `(101)^infinity` at position `s`, then its shadow numerator has exact valuation `s`;
5. canonical paired shadows have the same first-deviation position;
6. after quotienting arbitrary initial neutral `(101)` repetitions, every nontrivial canonical trajectory departs through one of exactly three fixed zero-height roots:
   `(2,-6,0)`, `(2,-39,0)`, `(2,-3,0)`;
7. every fixed individual shifted-shadow valuation is degenerate after that departure.

## Mission

Attack Gate A using the **normalized joint common-seed shadow pair after synchronized departure**.

Let the common first-deviation position be `s`, prefix length `n>s`, and define

`R_x=U_x/2^s`,
`R_y=U_y/2^s`.

Both are odd. Exact canonical integrality gives

`R_y == 3^d R_x (mod 2^(n-s))`.

The successor should seek a genuinely joint fixed-seed theorem that couples the selected higher lift of `(R_x,R_y)` to prefix-dominance area / accumulated height and implies the preferred sufficient theorem

`J>0, K=J+2^d-1, A=H+d-1 => nu_2(K-1)<=A`,

or at least gives a material contraction of the exact Gate-A residual.

## Required starting point

1. Treat RL288's first-deviation synchronization and three-root reduction as fixed analytic input.
2. Preserve the common-seed representation; do not replace it by a variable local input.
3. Distinguish mandatory `2^(n-s)` paired-shadow integrality from any genuinely additional restriction on the higher lift.
4. Adversarially test proposed joint invariants against RL287's fixed-segment cylinder isometry and RL288's arbitrary safe reverse boundary rays.
5. Use the inherited `H<=22` certificate only as a falsification oracle, not as the principal route.

## Forbidden repeats

Do not make the principal route:

- another scalar reserve in `(S,D,Q,F,B)`;
- another local first-return carry/height bound;
- `M/T` magnitude safety or a finite shifted-valuation table;
- the conditional first-magnitude-escape mod-54 sieve without a new global selector;
- reverse local predecessor enumeration alone;
- boundary density or neutral-cycle deletion alone;
- larger raw height-cap enumeration;
- Gate B;
- the fifth selector;
- Radius 6+.

## Scope

Gate B remains separate/open/frozen. Fifth selector unscanned. Radius 6+ frozen.

No global non-trivial-cycle exclusion is claimed.

Stop at the next theorem-sized fixed-seed advance, decisive barrier, or material contraction, and report that checkpoint before doing further mathematics.
