# RL213 reduced H21 interface

Date: 2026-09-01. Compact successor interface.

## Constants and frontier

`A=217976794617`, `L=137528045312`, `B=80448749305`,
`p=65470613321`, `u=103768467013`, `z=L-p=72057431991`,
`Ap-uL=1`, `pB=1 modL`, `K0=2^37`.

Necessary terminal ranks remain **13,415,865,871**:
- above-p source: `7,091,831,284`;
- below-p source: `6,324,034,587`.

Global necessary eta classes remain `0,8,9,17 mod18`;
`011 <=> eta=0 mod9`, `111 <=> eta=8 mod9`.
Below-p constants remain corrected `37/60/97`.

## RL212 retained e=16 selector

At exact above-p offset e=16, 469 of the 486 H21-compatible eta residues modulo `3^7=2187` survive. The forbidden residues are

`[0, 53, 431, 891, 917, 972, 1160, 1295, 1458, 1493, 1565, 1620, 1701, 1862, 2060, 2088, 2106]`.

Both states survive. The root-unit lift is unique modulo `3^17` for each compatible exact prefix and preserves eta modulo2187. Ordinary terminal valuation is CRT-independent and supplies no e=16 rank deletion by itself.

## RL213 new endpoint-moment boundary

For e=16, RL210's matched p-shift prefix gives
`P_(p+16)=P_p+P_16/alpha` and `rho_(p+16)=rho_16/alpha`.
Substitution into the inherited RL201 absolute endpoint moment, followed by the exact local p-gap and root z-telescopes, reduces the moment identically to the already inherited root `0->p` telescope.

Therefore that existing endpoint formula is not an independent e=16 eta consumer. A successor needs genuinely new information restricting the full-word/root quotient or another global observable.

## RL213 new G56 boundary

RL210's large-offset system has `24<=m<=37` and smaller first-mismatch exponent 37. Across all 304 abstract `(m,larger exponent,orientation)` start cases, exact continuation through phase55 shows:

- the inherited `m=24` root-smaller orientation is the only infeasible case;
- all other **303** cases satisfy G56 with some strictly increasing bounded paired prefix.

Thus G56 plus monotonicity/mechanical exponent bounds alone cannot refine the remaining first-divergence cases.

## Retained e=4 selector and locks

RL211 e=4 remains `eta=207 mod243`, state011, with parity open.
Do not revive normalized-gap integrality, use the terminal valuation alone, promote abstract prefixes as physical, or generalize RL209's signed-successor root-window barrier beyond its certified scope.

No physical H21 incidence/charge, rank deletion, branch contradiction, Gate closure or global nontrivial-cycle exclusion is proved.
