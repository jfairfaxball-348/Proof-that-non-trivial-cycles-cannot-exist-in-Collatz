# RL321 session state and RL322 kickoff

Date: 2026-09-14
Status: RL321 CLOSED; RL322 PREPARED

## Frozen RL321 state

At the first external survivor, the late-row-root branch now has a canonical genuine split

`z=2^rM+eta`, `y=3^sM+J`,
`0<eta<2^r`, `0<J<3^s`,
`K=3^s(M-m)+J`.

The tail is physically localized between matched one-ranks of the ordered balanced rows:

`r=u_j-v_j`, `wt(beta)=s`.

The residual `J` follows the genuine second half for exactly `v2(M)<=75` more phases before its first mismatch.

Define

`Z0=XM-Ym`, `W0=Xm-YM`.

`W0>0` and `Z0` is odd.

If `Z0<0`, put `n=m-M`. Then

`2^29<n<=2^35` (conditional on external `m>=2^71`),
`m=floor(nX/D0)`,
`r>=41`, `s>=26`.

Tail-only congruence and corner-only dynamics are frozen as saturated routes.

## RL322 priority

Consume an actual degree of freedom from this canonical normal form.

Preferred success criteria:

- close `Z0>0` or `Z0<0`;
- bound `r` or `s` support-independently;
- reduce the negative bounded carry to a gap-free exact certificate using a licensed ordinary-owned condition;
- or connect the bounded post-antipode residual mismatch to a global ownership/crossing theorem.

If none of these occurs and the work only creates another residue parametrization, classify the route LATERAL and pivot to the inherited root-aligned `kappa=0` / actual reverse-crossing obligation.

Do not use the demoted height-38 claim. Do not import root-aligned `G<2^35` into the late-row-root branch.

Gate A OPEN. Gate B OPEN. Global exclusion OPEN. `g=1` separate.
