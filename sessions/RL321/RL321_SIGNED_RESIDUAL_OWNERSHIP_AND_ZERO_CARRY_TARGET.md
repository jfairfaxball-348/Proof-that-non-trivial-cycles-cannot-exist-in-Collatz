# RL321 — signed-residual ownership and zero-carry target

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED

## Absolute objective

Continue toward excluding positive non-trivial Collatz cycles.

## Primary target: ordinary consumer for the signed late-row residue

At the conditional first external survivor, the late-row-root branch now has

`K=3^sc+theta`, `|c|<=2^35`, `0<|theta|<3^s`,

and positive affine quantities

`Z=D0m+Xc`, `W=D0m-Yc`,

with

`U_s=3^sZ+Xtheta`, `V_s=W-(Y/3^s)theta`.

The surrogate `(Z,W)` is not ordinary-owned: the actual next half from `m`
has weight `ell+s`. Seek a theorem using the actual word support that does
one of:

1. bounds `s` or `theta` independently of support;
2. forces `theta=0` or `3|theta`;
3. transforms the residual through a genuinely parity-owned physical run;
4. supplies a gap-free finite certificate after the bounded carry `c` is
   fixed;
5. proves a sharper exact barrier identifying a new non-contact pivot.

Do not assume independent ownership of RL147's binary height layers. Do not
restart RL140--RL146 unless a height-one or support-localization hypothesis is
proved from ordinary dynamics.

## Secondary target: zero carry and crossing consumer

In the least-rooted nonzero-residue branch,

`E=kappa h+rho`, `0<rho<h`.

RL320 proves that `kappa>0` forces an interface among the three coupled paths
by phase 33, but not necessarily the physical/shadow mismatch. Either:

- connect that early interface to the inherited first reverse crossing and
  `3^Bq<=2^nE`; or
- attack `kappa=0`, where `E=rho` and the existing clock cap disappears.

Do not launch a generic fixed-depth prefix scan or promote an affine-ray
identity. Any consumer must remove a branch/parameter or create a complete
finite certificate.

## Scope

- Internal-only frontier: `ell>=190537`.
- External-certificate-conditional frontier: `ell>=49,547,666,544`.
- External `2^71` least-state floor remains conditional.
- Gate A: OPEN.
- Gate B: OPEN.
- Global positive non-trivial-cycle exclusion: OPEN.
- `g=1` remains separate.
