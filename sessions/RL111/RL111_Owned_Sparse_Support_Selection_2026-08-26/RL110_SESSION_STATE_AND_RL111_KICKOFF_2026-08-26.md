# RL110 session state and RL111 kickoff

Date: 2026-08-26

## RL110 outcome

RL110 attacked the global ordinary-owned sparse-rotation compression route from RL109. It did not prove `0<|S|<D` on `R_*>=4`; Gate A, Gate B, RL/nontrivial-cycle exclusion, and the Collatz conjecture remain open.

The promoted analytic result is a baseline obstruction:

`|S| <= min((2^(A-L)-1)(3^L-2^L), R_*2^(A-L-1)3^(L-1))`.

On `R_*>=4`, both upper envelopes exceed `D`, with their minimum greater than `27D/8`. A proof based only on fixed-content diameter or a termwise monomial maximum therefore cannot close the route.

The finite `8..17` primitive-necklace scan is an exact local red-team certificate only. It is not a global exclusion and must never be promoted as such.

## Inherited proof state

Retain:

- primitive/full-`D` radius-3 engine: closed local;
- Gate A even terminal `k`: closed analytic;
- Gate A terminal `k<=25`: closed exact-certificate corollary;
- Gate A odd terminal `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The RL20 `D∤Q` fake, RL79 generalized-increment barrier, RL81 physical-vs-quotient distinction, primitivity, raw/Farey scope, external-input, and closure-scope red teams remain mandatory.

## Provenance correction / exception

The incoming RL110 extracted tree passed its internal manifest and direct fast verifier. Its historical outer ZIP did not match its sidecar and was not readable by `unzip`; the supplied `rl_conveyor.py` also expected a top-level target while the established extracted package layout nests it. The user expressly directed the session to use the extracted `authoritative/` files as authoritative. RL110 records this exception; it does not rewrite history to call that broken incoming bundle verified.

The outgoing RL111 bundle is built afresh, has a matching sidecar, an internal manifest, and fresh-unpack verifier output.

## RL111 target

Seek a full ordinary-owned support-selection or physical-strip theorem for the RL109 canonical closest pair. A useful result must use more than `D|S`: it must consume full ordinary `D|Q` ownership, an actual physical-state inequality, or a support restriction provably unavailable to the RL20 fake and to generalized-increment scaling.

Do not restart generic radius-4/5 case ladders, local least-root/final-return grammar as a global bridge, normalized rotation coboundaries, raw first-Farey/multiplicity claims under global wording, or the numerical cascade without a structural trigger.
