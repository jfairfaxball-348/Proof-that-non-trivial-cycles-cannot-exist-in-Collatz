# RL210 correction / demotion ledger

**No new RL210 correction or demotion.**

**RL206-C1**, inherited and controlling: the corrected RL20 block increment is
`H_(j+1)-H_j=(z^j/Y)3^(-E_(j+1))Q(B_j)`; strictness requires a block containing
a 1. Do not reuse the omitted-normalization expression for raw-population bounds.

**RL206-C2**, inherited and controlling: at source scope the first necessary
below-p source offset is 37, root-prefix depth is at least 60, and first
root-normalization depth is at least 97. Historical 39 / 63 / 100 values are
invalid at that scope.

RL210 does not reflect C2 across p. Its p-shift theorem is derived in lifted
coordinates and locates the unique epsilon carry at `z=L-p`; every prefix used
in RL210 lies strictly before that carry.

No inherited theorem, certificate, physical-incidence statement, ownership rule,
branch qualification, Gate status, or global conclusion is repaired or demoted.
