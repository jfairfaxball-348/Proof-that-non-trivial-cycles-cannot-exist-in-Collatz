# RL210 research report — global p-shift prefix overlap

Date: 2026-08-31. Incoming authority RL209 at BASE_HEAD
`3412fc6b8fefa2a5eba4626dc3dd73a6322739b5`.

RL210 attacked the exact above-p cancellation datum requested by the handover.
The central advance is that positivity of every acceleration exponent turns the
56-bit global endpoint cancellation into a comparison of two *actual* p-separated
56-step cumulative-exponent prefixes. This supplies independent absolute overlap
information without reflecting the below-p 37/60/97 theorem.

For every above-p physical source `a=p+e` with `e>=56`, the two prefixes must
first diverge at an index `24<=m<=37`, and the smaller exponent at that divergence
is exactly 37. Before m the physical heights at phases t and p+t are equal. An
exact continuation certificate further rules out one of the two m=24
orientations; the only m=24 possibility left by this consumer has
`h_(p+24)=1`, `h_24=0`.

The small boundary `1<=e<56` was also exhausted against the current necessary
predicate. Exactly six ranks survive, at e=`4,16,28,33,40,45`. The endpoint
valuation forces complete p-separated prefix locking through the source for e=4
and e=16, hence a flat absolute K segment `K_0=...=K_(e+1)=2^37` in either case.
No comparable selector is promoted for the other four small offsets.

This session intentionally makes **no rank deletion**. The exact necessary
frontier stays 13,415,865,871. The new result is a global prefix/height selector
that constrains 7,091,831,278 current above-p necessary ranks plus two of the six
small-offset ranks more strongly. It does not select eta, state, terminal sign or
valuation and does not establish physical incidence/charge or any Gate/global
closure.

The successor should now couple these sharply reduced prefix cases to a genuinely
absolute complete-word K/height/closure relation. More unanchored reverse depth
or a longer free prefix enumeration would repeat the RL201 barrier rather than
use the new information.
