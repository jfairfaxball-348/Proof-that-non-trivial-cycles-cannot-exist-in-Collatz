# RL320 session state and RL321 handover

Date: 2026-09-14
RL320 status: CLOSED AND FROZEN
Successor: RL321

## State summary

RL320 proves two support-independent contractions at the conditional first
external survivor.

For a positive root-aligned cofactor carry,

`min(v2(E),v2(rho))<=33`,

with coincident clocks at most 32.

For the late-row scaled contact,

`K=3^sc+theta`,

where `|c|<=2^35`, `0<|theta|<3^s`, and a positive affine pair `(Z,W)` has
the exact balanced `D0/H` factor form. The pair is not an ordinary-owned
return; determinism forbids relabelling it as one. Geometry alone has
unbounded-height counterfamilies, and the unrestricted mixed-height contact
route is subject to RL147.

## RL321 instruction

Primary: consume the signed residual `theta` using a genuinely ordinary-owned
support-localization or physical-run theorem. Work with the actual
overweight/underweight words and equations

`U_s=3^sZ+Xtheta`, `V_s=W-(Y/3^s)theta`.

Do not relabel `(Z,W)` as a balanced return, assume independent ownership of
mixed-height layers, or invoke height-one contact results without proving
their hypotheses.

Secondary: isolate the `kappa=0` root-aligned branch or connect the positive-
carry three-path early interface to the actual physical/shadow reverse
crossing. The clock minimum alone is not a crossing-time bound.

Gate A/B, global exclusion, and `g=1` remain open.
