# RL290 closeout red team

Date: 2026-09-09
Result: PASS WITH SCOPE GUARDRAILS

## 1. Gate-A scope

PASS.

RL290 does not claim Gate A is closed.

The exact inherited residual remains:

`k>=25`, `k` odd, `H_can<k`.

No result in RL290 narrows that residual.

## 2. Seam normal form

PASS.

The identity

`B-C=2^(q-1)(K-1)`

is algebraic from RL289's normalized pair equation.

The promoted significance is a bridge/reparameterization only. It is not treated as an independent scalar reserve.

## 3. Gauge conclusions

PASS.

The neutral `(101)` prefix invariance is exact.

The positive boundary loop argument preserves physical state and height but changes seam scale.

The one-step merger and acyclic equal-area diamond are stronger barriers: they show chosen-history seam coordinates are not physical even after chronological loop erasure.

No claim is made that all possible history gauges have been classified.

## 4. Rational loop-invariant statement

PASS WITH NARROW SCOPE.

The statement concerns rational functions invariant under a fixed diagonal return action

`(C,P)->(3^r C,2^L P)`

with `L,r>0`.

Multiplicative independence of primes `2` and `3` prevents nontrivial rational monomial balance.

This is not a theorem excluding arbitrary discontinuous, valuation-based, combinatorial, or Bellman invariants.

## 5. Bellman envelope

PASS.

`Bcal` is a definition plus exact dynamic-programming identity.

The statement that any edge-Lipschitz endpoint-dominating dual must dominate `Bcal` is the standard path-unrolling consequence.

The equivalence `Bcal(1,-13)<=0` with Gate A is a reformulation, not a proof.

## 6. Height-one terminal family

PASS.

For odd `k>=3`, odd `q>=1`:

`J0=1+2^q(2^k-1)`

`Jin=(2^(q+2)(2^k-1)-2)/3`

satisfy the exact one-zero law at height one.

The shell condition

`nu_3(J0)=1 <=> q != k+2 (mod 6)`

leaves two of the three odd residue classes.

Scope guardrail: these are locally legal/admissible positive blocks. They are NOT all claimed globally reachable from the fixed seed.

The specific `k=13,q=1` launchpad `J=21842` is separately globally reachable inside the exact RL282 H<=22 certificate.

## 7. Stripped-unit renormalization

PASS.

For `t=nu_2(3J+2)>=3`, the suffix

`01 0^(t-2)`

has exact cost one and reaches

`(3J+2)/2^t + 1`.

The accelerated-Collatz interpretation follows from writing `J=2n`.

This is a future-successor identity, not an inductive potential.

## 8. Height-one checkpoint kernel

PASS.

A height-one first return has the unique off-boundary word `01`.

For odd return, all subsequent zero-height retained boundary motion and complementary exits are exactly RL283's `C/E` system.

Thus `Gamma_1` is exact.

No claim is made that higher-height kernels are classified.

## 9. Beta versus Bellman threat

PASS.

The frozen chain

`2514 -> 5378 -> 21842 -> 8192`

uses exact legal macro words and cost one per edge.

`Beta(2017)=5` is used only as a concrete one-generation comparison; the full Bellman threat can be larger because a low-valuation exit may itself have dangerous future successors.

This does not demote RL283's Beta formulation; it classifies it as one-generation rather than fully recursive.

## 10. Finite evidence

PASS.

The observed bound

`nested(J)<=H-2`

is explicitly marked unpromoted finite evidence.

It does not narrow the residual and is not installed as successor authority.

## 11. Old-result consistency

PASS.

RL290 strengthens or reinterprets, rather than contradicts:

- RL282 pure-zero local-tail barrier: now includes exact h=1;
- RL283 boundary hazard: identified as one-generation height-one Bellman projection;
- RL284 labelled accelerated bijection: identified inside stripped-unit renormalization;
- RL287 cylinder isometry and height-bounded skeleton: used as structural input;
- RL288 fixed-seed synchronization: retained;
- RL289 ballot bijection/rejected tube and mixed-state Bellman cost: retained.

No inherited promoted theorem is demoted.

## 12. Successor discipline

PASS.

The user explicitly requested the next session be a broad programme review/audit rather than automatic continuation of the current route.

The successor authority therefore requires an era-by-era audit and synthesis first.

The Bellman route is frozen as an important live option, not promoted to exclusive strategy.

## Final red-team result

`PASS`

No correction or demotion is required before promotion of the RL290 closeout package.
