# Authoritative start — RL264 two-trajectory low-area lift

Incoming state: **RL264**.

Completed incoming generation: **RL263 — physical-gap identity, ordered-prefix method barrier, and RL262 modulus correction**.

RL263 promotes:

- exact physical representation of the canonical internal path:
  `T_p=3^(d_p)A_p-B_p` for the two half-Collatz trajectories generated by the full-phase `N,N+4` swap;
- exact canonical parity coordinate
  `J_p=3^(d_p)(A_p+1)-(B_p+2^(d_p))`;
- exact ordered-prefix telescope
  `C-S_p=2^p*3^(rho-Y_p)*(B_p-3^(d_p-1)A_p)`;
- therefore the ordered dyadic prefix congruence is automatic on genuine physical full-phase pairs and cannot by itself force the proposed eventual low-density corridor;
- correction of the RL262 closeout modulus interpretation: in the inherited odd-`k` scope,
  `Dcal=C-2^m*(1+(N-2)*2^(k-1))/3`, so `Dcal==C (mod2^m)`;
- RL262's exact fourth-selector elimination is unaffected.

RL264 is **PREPARED, NOT STARTED**.

Read `RL264_TWO_TRAJECTORY_LOW_AREA_LIFT_TARGET.md` first.

RL264 must attack genuinely independent physical/canonical lift information under a hypothetical `H<k`; do not begin by resuming selector-by-selector enumeration. Gate A remains open. Gate B remains open. Radius 4 was not invoked by RL263. Radius 5 remains inactive.
