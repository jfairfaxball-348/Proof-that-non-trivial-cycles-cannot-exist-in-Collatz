# RL265 — Radius-5 upgrade and Radius-n feasibility target

Date prepared: 2026-09-06
Status: **PREPARED, NOT STARTED**

## User-directed pivot

Freeze the current Gate-A attack and the fifth retained arithmetic selector as future fallbacks. Do not resume either in RL265.

The new research direction is:

1. determine whether a literal **Radius-5 local theorem** can be obtained by a rigorous upgrade analogous in spirit to the successful Radius-3 -> Radius-4 programme;
2. if and only if the Radius-5 analysis is mathematically sound enough to support extrapolation, investigate the structure required for a general **Radius-n** theorem/programme.

The strategic motivation is to test whether extending the radius theorem itself could eventually subsume broad families of RL configurations, rather than proving a separate bridge from every RL survivor into a fixed small radius.

This motivation is a research hypothesis, not a promoted conclusion.

## Required startup

Recover the exact authoritative Radius-4 theorem and its dependency chain before attempting Radius 5.

In particular identify precisely:

- the Radius-3 theorem statement actually used by Radius 4;
- the literal Radius-4 theorem statement;
- primitive/full-D hypotheses;
- positive-domain/sign hypotheses;
- the exact cyclic adjacent-transposition distance notion;
- every topology family and arithmetic/continued-fraction/LMN dependency used in the Radius-4 proof;
- the RL247 scope audit and any later correction/demotion affecting Radius 4.

Do not infer Radius-5 hypotheses by analogy when an exact Radius-4 definition is available.

## Primary RL265 objective — Radius 5

Formulate the literal exact-distance-5 analogue of the promoted Radius-4 theorem in the same audited language.

Then perform a complete feasibility attack:

1. enumerate/classify the exact distance-5 transport/topology partitions without omitting orientation/cyclic cases;
2. determine which Radius-5 families reduce directly to the existing Radius-4 or Radius-3 theorem;
3. isolate genuinely new irreducible Radius-5 topology families;
4. for each new family, derive the exact arithmetic obstruction required;
5. use exact computation only as a gap-free certificate/falsification aid, not as a substitute for an infinite proof;
6. preserve primitive/full-D/positive-domain scope throughout;
7. run an explicit red-team audit against negative-D, nonprimitive, proper-factor, cyclic-distance, and reduced-denominator/multiple regressions.

Preferred strong outcome:

`RADIUS5_LOCAL_THEOREM_PROVED`

Acceptable meaningful outcomes include:

- `RADIUS5_REDUCED_TO_EXPLICIT_FINITE_NEW_FAMILIES`;
- `RADIUS5_EXACT_BARRIER_FOUND`;
- `RADIUS5_COUNTEREXAMPLE_OR_SCOPE_REPAIR_REQUIRED`.

Do not claim Radius 5 merely because all small numerical instances pass.

## Secondary objective — Radius n

Only after the Radius-5 structure is understood, test whether the R3->R4->R5 mechanism exposes a genuine induction or finite schema in `n`.

A rigorous Radius-n programme must answer at least:

1. What is the exact theorem statement for arbitrary radius `n`?
2. Which hypotheses remain uniform in `n`?
3. How are distance-n flow topologies parameterized? Integer partitions alone are not enough unless orientations/cyclic ownership are proved covered.
4. Is there an induction from radius `n` to `n+1`, or merely a growing finite case split?
5. Do new Diophantine approximation/LMN constants or exceptional families proliferate with `n`?
6. Does proving all finite radii actually imply the desired global RL exclusion, or can an RL survivor have unbounded/minimal self-rotation distance and therefore escape every fixed finite theorem?
7. What exact compactness/well-ordering/finite-distance argument would be needed for the phrase “all RL are in Radius n” to become a theorem rather than an intuition?

The session must be willing to falsify the Radius-n strategy if Radius 5 reveals nonuniform growth or if no uniform bridge from arbitrary RL data to finite exact cyclic distance can be proved.

## Frozen work

Do not resume:

- Gate A;
- the fifth retained selector;
- selector-by-selector enumeration.

Those are explicit future fallbacks.

Gate A remains open.
Gate B remains open.
Radius 4 remains promoted locally.
Radius 5 is the active target.
No Radius-n or global exclusion theorem is assumed.
