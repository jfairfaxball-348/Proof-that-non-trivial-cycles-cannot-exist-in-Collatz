# RL255 session state and RL256 kickoff

Date: 2026-09-05

RL255 status: COMPLETE.

Promoted:

- Branch-C capacity:
  `beta(P)+2 <= floor((a-3 beta(P)+1)^2/4)`;
- uniform floors:
  `a>=1100`, `z>=406`, `ell>=694`;
- refined resonance:
  `a/ell<=233/147`, `H/a<=3/233`;
- non-halving:
  `beta(P)>=356`, `a-36H>=1068`,
  and first non-halving selector at `(1986,1253,733,1352,853,25,17)`.

The immediate global first frontier is the halving selector

`(1100,694,406,317,200,14,4)`,

with `K=7`, `t=2`, `260<=beta(P)<=354`, `31<=k<=150`.

RL256 is PREPARED, NOT STARTED, and should attack this exact finite halving frontier.

Gate A/B open. Radius 4 not invoked. Radius 5 inactive.
