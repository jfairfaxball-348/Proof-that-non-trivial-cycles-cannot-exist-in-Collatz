# RL130 verification

Run `./run_fast_rl130_verifiers.sh`. The suite performs an exhaustive
small-word rotation/bound red-team, exact regeneration of all quotient ceilings
for `L=41..55`, and the exact induction descent certificate for every odd start
through `6,496,657,853`.
