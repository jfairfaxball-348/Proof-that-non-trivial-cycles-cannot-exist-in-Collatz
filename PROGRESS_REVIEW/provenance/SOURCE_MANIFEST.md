# Source manifest

Snapshot commit: `de5586d`  
Snapshot description: RL349 closeout / handover to RL350  
Package purpose: non-RL independent-review setup

Every copied source in this package is a verbatim convenience copy unless its filename is explicitly marked as a review-authored file. The original repository remains authoritative for the mathematical project; this directory is a frozen review representation.

The machine-readable manifest is `machine_readable/source_manifest.json`. It records the package path, original repository path, snapshot commit, SHA-256, Git blob ID where available, and copy class.

Copy classes:

- `authority`: current or load-bearing canonical source selected from the RL349 snapshot;
- `historical`: earlier source retained for logical provenance, barrier, correction, or route genealogy;
- `certificate`: verifier/data artifact retained for targeted reproduction;
- `review-authored`: new audit prose or metadata, not mathematical authority.

The current RL350 kickoff is retained only under `historical/` because it is post-boundary successor provenance. It is not a result included in the RL349 mathematical snapshot.
