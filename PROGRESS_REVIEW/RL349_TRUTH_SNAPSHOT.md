# RL349 truth snapshot

## Plain statement

At the committed end of RL349, the repository claims a substantial but incomplete reduction of one ordered genuine `g=2`, `Z0>0`, `K<0` parent branch to a residual obstruction. The parameters recorded for that branch are ` (a, ell) = (217976794617, 137528045312)`.

The project’s current roadmap stage is `R1 — Parent Bridge`, with an approximate repository estimate of 94% by theorem-level obligations. This percentage is a project metric, not a mathematical bound. The global proof status is explicitly `OPEN`.

## What the repository claims as retained mathematics

- The shortened Collatz map, parity-word and full-denominator language, and the ordinary `D = 2^A - 3^L` cycle encoding are foundational definitions/reductions.
- A primitive/full-`D` radius-3 obstruction is recorded as a closed local theorem, with explicit inherited hypotheses and finite/external dependencies. The RL20 exact word shows that the earlier local-grammar-only bridge to radius 3 is false.
- The arbitrary-radius orbit-sum identity and several later ownership, valuation, physical-state, and packing results are recorded as analytic theorems or exact finite certificates at their stated scopes.
- The current R1 branch has a full physical two-row bridge, q-profile/phase reductions, terminal-law and matched-row geometry, and exact finite certificates for selected bounded interfaces.
- RL343–RL348 reduce the live Phase-4 obstruction to exact residual classes summarized by `O_75`; they do not prove that `O_75` is empty.

## What RL349 specifically did

RL349 preserved an exact arithmetic inequality involving `ell/(12 log 2)` and its rational certificate. It did not prove the antecedent needed to use that inequality. Its closeout identified:

- the missing directed source/end orientation and sign bridge at an `L = ell` matched contact;
- the scope barrier preventing automatic use of RL146’s global height-one theorem;
- the fact that the over-half source-side route still lacks a genuine ownership/signature estimate;
- the continued openness of the three Phase-4 residual classes.

The live numerical claims that depended on an unproved orientation assignment were explicitly demoted. No new Phase-4 class elimination was promoted.

## Open chain to the final theorem

The current shortest recorded chain is:

`hypothetical positive non-trivial cycle`
→ primitive/full-`D` and ownership reductions
→ the ordered genuine `g=2` parent branch (one branch of the broader programme)
→ the full two-row canonical walk and exact obstruction `O_75`
→ prove `O_75 = empty` (still open)
→ close R1 / Parent Bridge
→ close R2 (`g=2`), R3 (`g>1` lift), R4 (scale), R5 (`g=1`), R6 (Gate reconciliation)
→ assemble and audit R7 end-to-end proof.

The chain is not yet a theorem because several arrows are conditional or branch-restricted, and the first live obstruction `O_75` is not eliminated.

## Boundaries and qualifications

The external least-state floor `m >= 2^71` is carried as a conditional inherited input in the current branch. Phase 5 short-return work remains scratch-only. Gate A and Gate B remain separate global obligations in the historical architecture. No current source claims that the Collatz conjecture, all positive non-trivial cycles, all `g=2` branches, or all `g>1` branches have been eliminated.
