# RL99 session state and RL100 kickoff

Date: 2026-08-25

## 1. RL99 closeout status

RL99 completed the requested global audit and did not run another coupled-cascade batch.

Incoming audit handover integrity:

- outer SHA-256: PASS;
- internal five-entry manifest: PASS;
- GitHub `authoritative/` provenance: matched the supplied RL99 audit handover.

No stop-and-repair event was triggered. The current frozen proof state contains historical repairs, but no load-bearing RL97 result inspected here was found to depend on a demoted claim.

## 2. Frozen global proof status

Retain exactly:

- primitive/full-`D` radius-3 obstruction: **closed local theorem**;
- Gate A even terminal `k`: **closed analytically**;
- Gate A terminal `k<=25`: **closed by exact finite-certificate corollary**;
- Gate A odd terminal `k>=27`: **open globally**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 3. RL99 strategic finding

The strongest recent route is the exact first-Farey/full-phase physical/2-adic cascade, but it remains a branch sieve.

The missing scope theorem is:

> **First-surplus Farey branch domination / coverage:** prove that every genuine full-phase Gate-A violation lies in the exact first-Farey branch, is no harder than that branch, or belongs to a finite explicitly exhaustible complementary branch set.

RL100 is assigned to this theorem.

## 4. Frozen first-Farey cascade state

Do not alter or forget:

- `k8=2,921,801,523`, exact current margin `-3,549`;
- middle `(D_m,R_m)=(7,500,032,4,350,000)`;
- deep `(D_d,R_d)=(10,000,032,3,000,000)`;
- ultra `(D_u,R_u)=(15,000,035,1,680,000)`;
- wide `(D_w,R_w)=(5,000,030,7,000,000)`;
- active support `lambda=4,000,000/52,500,238,500,033`, `mu=1/7,000,001`;
- feasible supports `A-C`, `C-D`, `D-E`;
- conservative common successor charge `L_common=15,000,053`;
- branch upper endpoint `42,150,931,559`, odd.

Exact scan restart instructions remain in:

`RL98_COUPLED_CASCADE_RESUME_AFTER_RL99.md`.

## 5. RL100 primary instructions

Read first:

1. `RL99_GLOBAL_AUDIT_SYNTHESIS_AND_RL100_ROADMAP.md`;
2. `RL99_PROOF_STATE_LEMMA_AND_CORRECTION_LEDGER.md`;
3. `RL99_CLOSURE_SCOPE_AND_ROUTE_MATRIX.md`;
4. `RL100_FAREY_BRANCH_DOMINATION_AND_GLOBAL_GATE_A_LIFT_TARGET.md`;
5. `RL98_COUPLED_CASCADE_RESUME_AFTER_RL99.md` only as deferred state.

Then recover selectively from GitHub:

- RL83 first-surplus product/Farey theorem;
- RL84 cylinder/extremal bridge;
- RL85 first-Farey physical scale;
- RL81 physical common-mode/lift;
- RL87 physical difference cap;
- RL79 generalized-increment barrier;
- RL20 fake/coboundary red teams.

Do not recursively replay historical expensive certificates unless a verifier/provenance problem, contradiction, or genuinely unresolved definition forces it.

## 6. RL100 exact target

For a general admissible reduced first-surplus pair `(p,q)`, derive branch-parameter versions of the physical maximum, block cap, terminal `k` cap, and reset capacity. Seek a theorem proving first-Farey exhaustivity, first-Farey worst-case domination, or a finite exhaustive complement.

Do not treat the fact that `(p*,q*)` is the smallest admissible pair as proof that every counterexample uses it.

Do not spend the session merely enumerating larger continued-fraction entries.

## 7. Success and pivot rules

**Success:** a universal complementary-branch theorem, finite explicit branch cover, or proven first-Farey domination.

**Pivot:** if only a larger `q` cutoff/admissible pair is obtained, or the general branch retains an uncontrolled physical context, stop this route and move to the highest ranked fallback in the RL99 roadmap.

Fallback order:

1. `D|Q`-sensitive Gate-B owned close-pair/strict-excursion theorem;
2. non-homogeneous ordinary-`+1` extremal/physical/basin bridge;
3. resume the exact coupled cascade only when a globality/acceleration trigger is met.

## 8. Verification economy rule

> After the current bundle checksum, internal manifest, and fast verifier suite pass, accept the frozen incoming proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize new mathematics on the stated live obstruction.

## 9. Sustained-attack rule

Work RL100 beyond the first minor lemma. Continue theorem shaping, red-team, and complementary-case analysis until context/compute makes further work unproductive. Do not close merely because one larger Farey denominator or one partial bound has been found.
