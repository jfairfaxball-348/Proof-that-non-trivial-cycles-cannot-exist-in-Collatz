# RL212 certified facts and proof ledger

Date: 2026-09-01. Canonical current classification record after completing RL212.
All physical conclusions remain conditional on the sole inherited high branch
`(37,0,23,-1)` and a physical H21 realization.

## New proved analytic mathematics

### RL212-T1 — exact e=16 absolute quotient reduction
RL210's e=16 prefix lock and flat K imply `h_16=1`, total root exponent 24 and
`y_(p+16)-y_16=3^16*2^13`. For every admissible root prefix with affine numerator
Q,

`eta = (2^24+Q)(2^58)^(-1) mod 3^16`.

### RL212-T2 — unique root-unit lift
For every H21-compatible exact e=16 prefix there is exactly one lift of eta from
modulo `3^16` to modulo `3^17` for which both root states `y_0,y_p` are units
modulo 3. The lift preserves eta modulo 2187 and does not select an H21 state or
terminal parity.

### RL212-T3 — terminal-valuation CRT boundary
The ordinary H21 terminal-valuation filter is a condition modulo `2^22`, whereas
the new e=16 holes are modulo `3^7`. Coprimality and CRT show that appending this
2-adic filter alone cannot delete a surviving ternary class or the e=16 rank.
A stronger consumer must correlate the quotient residue with additional global or
absolute data.

## New exact finite certificate

`verification/verify_rl212_e16_quotient_residue.py` exhausts the exact e=16 root
recurrence and certifies:

- 108,950 admissible arithmetic root prefixes;
- 45,046 survive the inherited H21 mod-9 state filter;
- state011: 29,286; state111: 15,760;
- all H21-compatible residue classes are reachable through `3^6=729`;
- at `3^7=2187`, 469 of 486 are reachable and exactly 17 are forbidden;
- forbidden residues: `[0, 53, 431, 891, 917, 972, 1160, 1295, 1458, 1493, 1565, 1620, 1701, 1862, 2060, 2088, 2106]`;
- the compressed phase-16 automaton has 4,950 `(height,Q mod2187)` states and
  only 1,243 height-one Q residues;
- the root-unit lift is unique for all 45,046 H21-compatible exact prefixes.

The raw prefix list is not an authoritative artifact and is not a physical
population.

## Inherited state retained

The necessary terminal rank count remains **13,415,865,871**:
- above-p source: **7,091,831,284**;
- below-p source: **6,324,034,587**.

The e=16 terminal rank remains `34,124,151,203`. RL211's exact e=4 selector
`eta=207 mod243`, state011 remains unchanged. RL210's large-offset first-divergence
selector `24<=m<=37`, smaller exponent 37, remains unchanged.

All RL206 corrections, below-p `37/60/97`, p-shift carry, physical/charge/Gate/
global locks and the flat-K denominator barrier remain binding.

## Open obligations

The strongest next consumers are:
1. couple the 17 e=16 ternary holes to the exact absolute endpoint/complete-word
   moment strongly enough to select eta modulo 2187 or delete the e=16 rank;
2. if that cannot be done, transport quotient-state residue/orientation into the
   RL210 `24<=m<=37` first-divergence system without collapsing it to the inert
   dyadic denominator test;
3. treat e=28,33,40,45 only with a proved common consumer or separately.

No physical H21 incidence, H21 charge, rank deletion, branch contradiction, Gate
closure or global nontrivial-cycle exclusion is proved.
