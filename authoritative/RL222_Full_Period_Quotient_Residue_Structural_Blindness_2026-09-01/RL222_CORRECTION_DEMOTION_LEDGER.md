# RL222 correction / demotion ledger

Date: 2026-09-01.

No inherited mathematical claim requires correction or demotion in RL222.

## Scope protections recorded by RL222

1. The denominator-separation theorem applies to the current bounded arithmetic root family.  It does not turn an arithmetic candidate into a physical H21 realization.
2. RL214's statement that bare ownership `Qfull=0 mod D` does not determine a quotient residue remains correct.  RL222 adds the independent inherited root-size input `y0<D` and proves that, on this bounded family, an ownership-derived `Qfull mod D^2` has no wrap and therefore no new information.
3. `Qfull mod D^2 = D*y0` is a structural-blindness theorem for a residue recovered through the ownership identity.  It does not say that an independently computed full-word return observable is uninformative.
4. The exact tail target is a necessary equality only for a physical full return.  It is not asserted for arbitrary arithmetic candidates absent a legal physical continuation.
5. The local `2^m` tail-prefix congruence is automatic from the affine recurrence and cannot be counted as a candidate deletion or return certificate.
6. No candidate, prefix, terminal rank, state, eta class, physical H21 incidence, charge theorem, branch contradiction, Gate A closure, Gate B closure, or global Collatz proof is added.

Knowledge catalogues remain `stale/deferred` and are not proof-state authority.
