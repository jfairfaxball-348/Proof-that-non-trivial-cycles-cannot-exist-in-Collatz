# RL78 target — `D|Q`-sensitive bounded-radius Gate-B reconnaissance

Date: 2026-08-24

## Purpose

RL77 completed a serious product/growth/continued-fraction attack. It obtained a modern full-phase product window, collapsed the first reduced-CF branch to one exact exponent pair, and proved that the canonical `8/9` versus `4/3` two-pump Diophantine formulation is a unimodular rewrite of the original exponent lattice unless the residual context is independently bounded.

The physical scale and complementary context remain uncontrolled. Per the RL75/RL77 stop criterion, the next deliberate route is now:

# **Gate B — genuine `D|Q`-sensitive bounded-radius / owned-rotation reconnaissance**

This is reconnaissance for the missing global bridge. It is **not** permission to launch a generic radius-4/5/6 local case-tree campaign.

## Frozen starting point

Retain:

- the exact primitive/full-`D` radius-3 local obstruction is closed;
- RL20 has an exact radius-4 packed local-grammar fake model, but it is not genuine because `D∤Q`;
- therefore local root/final-return grammar alone cannot force radius `<=3`;
- RL49 permanently kills the old half-period-radius-3 shortcut via the even-distance identity;
- RL72 shows the raw RL19 weighted difference on the RL48 half-rotation pair collapses to the proper-factor identity;
- RL75 `N==19 mod24` and RL64 full phase provide genuine ownership information;
- RL77 product/CF does not supply a global scale cap.

## Primary theorem target

Find a genuinely nonlocal `D|Q`-sensitive invariant or packing statement that distinguishes a true RL object from the RL20 fake model and forces one of:

1. two owned primitive full-`D` rotations at cyclic adjacent-transposition radius `<=3`; or
2. an absolute owned radius bound `<=4` together with a sharply identified finite radius-4 obstruction programme; or
3. a direct contradiction before a local radius theorem is needed.

The highest-value first theorem would have the form:

> **Owned close-pair theorem.** In every genuine primitive full-`D` RL object, global divisibility/ownership forces two admissible owned rotations whose cyclic word distance is bounded by an absolute constant, and the proof explicitly fails on the RL20 `D∤Q` radius-4 fake model.

## Required interfaces to recover selectively

Use GitHub only as needed for exact load-bearing definitions:

- RL18/RL19 arbitrary-rotation `D|Q` and weighted-difference identities;
- RL19 weighted populations/state packing;
- RL20 exact radius-4 fake model and its precise failure `D∤Q`;
- RL20 balanced-return / strict-excursion programme;
- RL48/RL64 full-phase ownership and the four-swap pair;
- RL72 collapse diagnosis for the raw half-rotation splice;
- RL75 tournament's radius-route assessment.

Do not recursively re-audit expensive historical certificates.

## Mandatory negative control

Every candidate bridge must be tested against the exact RL20 fake model.

Reject the candidate immediately if it uses only properties also satisfied by that model, such as local slope/root/final-return envelopes, without consuming the missing global `D|Q` ownership.

A successful invariant should make the fake model fail for the same mathematically visible reason it is not genuine.

## Candidate mechanisms worth testing

- subtract the known state/proper-factor coboundary from the RL19 arbitrary-rotation weighted difference and inspect the remaining `D|Q` residual;
- compare two or more owned rotations rather than only the RL48 half-period pair;
- use global numerator divisibility to constrain the cyclic distribution of close rotations;
- strict-excursion packing with an explicit full-`D` residue/quotient consumer;
- a radius-4-specific ownership obstruction only **after** a global theorem makes radius 4 unavoidable.

## Red-team / stop rules

Do not continue a candidate if:

- it collapses algebraically to `D*(state difference)` or the RL48 proper-factor identity;
- its quotient is merely an already-known phase/state integer;
- it cannot distinguish the RL20 `D∤Q` fake model;
- it assumes an absolute radius bound instead of proving one;
- it starts building a large radius-4 case tree before a genuine global bridge exists.

If no non-coboundary `D|Q`-sensitive close-pair invariant survives a serious reconnaissance, freeze that failure explicitly and return to the RL75 route map rather than defaulting to more local digits.

## Close-out requirement

Freeze exact proofs, failed candidates, recovered interfaces, correction ledger and verifier status; create the next numbered authoritative bundle and sidecar; verify the internal manifest and fresh-unpack fast suite.
