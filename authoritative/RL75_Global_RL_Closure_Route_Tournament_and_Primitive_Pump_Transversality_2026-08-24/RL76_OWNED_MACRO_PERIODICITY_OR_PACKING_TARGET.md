# RL76 target — Owned synchronized-macro periodicity or packing

Date: 2026-08-24

## Purpose

RL76 should attack the tournament winner from RL75 rather than returning to automatic local q-digit extension.

The target is the first globally open Gate-A band:

`27<=k<=165`, `k` odd, hypothetical violation `H<k`.

RL73 guarantees a post-first-mismatch maximal positive height-one synchronized block containing at least

`40,249,491,324,522,944`

aligned `00` columns.

RL74 proves that count/endpoints/`Jg`/`Psi` alone cannot contradict such a block, but a fixed repeated pump is visible to full phase through an exact Möbius law.

RL75 proves that the Möbius determinant-zero case for a nonempty proper physical pump is incompatible with a primitive cycle: determinant zero is exactly physical pump closure. Thus the genuine repeated-pump branch is nondegenerate.

## Exact theorem target

Prove an ownership-sensitive theorem giving

`Z < 40,249,491,324,522,944`

for the aligned-`00` count `Z` of every RL73 maximal block in a genuine RL64 full-phase completion with odd `27<=k<=165` and `H<k`.

Do so through an exhaustive **periodicity-or-packing dichotomy**.

### Branch P — periodic/repetitive macro

1. Extract a nonempty proper closed synchronized factor `c` with quantitatively significant repeat structure.
2. Use RL74's exact Möbius law for `v_q=A c^q B`.
3. Use RL75 primitive nondegeneracy `D_*!=0`; do not use determinant-zero deletion as a route.
4. Bring RL65–RL71 terminal/rank information into the context coefficients or physical pump-entry/exit states.
5. Prove a repeat-depth bound uniform over all genuine low-`k` completions.

A useful partial theorem would close the canonical positive `c=10` repeat branch uniformly in the low-`k` window.

### Branch A — aperiodic/high-complexity macro

If no usable deep repeated closed factor exists, force a global cost from the many distinct contexts/states. Candidate consumers:

- RL19 exact weighted populations and least-state state packing;
- RL20 coprime-6 and strict-excursion packing;
- RL50 defect/rank common-displacement structure;
- RL64 full-phase ownership and full-word denominator;
- RL71 exact low-`k` terminal selector information.

The result must be a real global budget, not local grammar or endpoint mass.

## Mandatory red-team tests

Any proposed theorem must be tested against:

1. RL74's canonical fixed-area shrink/growth pump family. If the theorem does not use a condition absent from this family, it is false as a Gate-A theorem.
2. RL20's exact radius-4-packed local-grammar word. If the theorem cannot distinguish the missing `D|Q`, it is not a genuine ownership/packing theorem.

## Reusable RL75 theorem

For `v_q=A c^q B`, physical pump entry `x`, pump exit `y`, and RL74 notation,

`D_*=2^|A| 3^wt(B) (delta x+C) M_q/P^q`

`=2^|A| 3^wt(B) (delta y+C) M_q/R^q`,

and

`D_*=0 <=> F_c(x)=x`.

In a primitive cycle with `c` proper, `D_*!=0`.

Also, for genuine nontrivial full phase,

`N==19 mod24`.

## Strategic alternatives if the target stalls

First alternative: product/growth/continued-fraction route, but only after deriving a new RL-specific full-phase restriction on reduced `(A,L)` data.

Second alternative: `D|Q`-sensitive bounded-radius Gate B reconnaissance, with `R0=4` only as the smallest experimental candidate. Do not build a generic radius ladder first.

Do not pivot to q=6/q=7 compatibility digits, raw `(2-N)M`, endpoint `Psi`, or determinant-zero descent.

## Session close-out requirement

Freeze the exact result, failures, corrections, verifier state and next theorem target. Create the next numbered authoritative bundle and matching sidecar after fresh-manifest and fast-verifier checks.
