# RL322 closeout — negative canonical branch elimination and strategic pivot to all-scale ownership

Date: 2026-09-14
Status: CLOSED AND FROZEN
Incoming BASE_HEAD: `5473c663f2891909c3cece93c4263b4e24715536`
Successor: RL323

## Executive conclusion

RL322 achieves one genuine branch elimination and one decisive method barrier.

In the canonical negative branch `Z0<0`, the tiny owned-prefix defect forces the first 66 physical shortcut bits to be odd. Hence `m==-1 (mod 2^66)`. Combined with RL321's exact Beatty identity `m=floor(nX/D0)` and the inherited `2^71<=m<2^75` window, only 480 exact unit-interval tests remain. A gap-free exact rational certificate excludes every one. Therefore `Z0<0` is impossible at the conditional first external survivor, uniformly over all remaining support/height.

In the positive branch `Z0>0`, an exact first-survivor combinatorial countermodel satisfies nonnegative least-root defect, ordered balanced rows, matched-rank localization, and the canonical tail residual dynamics while producing a positive affine quantity below the universal minimum ordinary balanced-row numerator. Thus positivity plus the current geometry does not promote `Z0` to ordinary ownership.

No Gate A, Gate B, global positive non-trivial-cycle exclusion, or `g=1` result is claimed.

`PARENT_DIFFICULTY_DELTA = LATERAL`.

The negative branch removal is real support-uniform progress inside its scope, but the main project-level missing all-scale ordinary/full-`D` ownership consumer is unchanged on the surviving branch.

## Strategic assessment

The explicit audit in `RL322_STRATEGIC_ASSESSMENT_RL305_TO_RL322.md` answers the requested programme-level question.

Verdict: RL315–RL322 genuinely improved the architecture and RL322 paid off part of that normalization by eliminating one whole sign branch. However, the positive-branch countermodel marks a strategic stop point. Further sharpening of carries, residues, constants, height lower bounds, or support classifications would now risk reverting to the local-grammar behaviour RL305/RL309 were intended to prevent.

The next authoritative session therefore does **not** inherit “continue the current local attack”. RL323 targets an all-scale ordinary/full-`D` ownership coercivity/extraction theorem.

## What survives arbitrary support?

The RL322 negative-branch contradiction does: it imposes no upper bound on `r`, `s`, or tail support before excluding the branch.

The surviving positive-branch geometry does not. RL322 gives an exact barrier showing that the existing local structural ingredients do not themselves become coercive at large support.

## Verification

Portable verifier:

`verification/verify_rl322_negative_branch_and_positive_barrier.py`

Frozen output records:

- exact rational log/exponential enclosure PASS;
- forced physical prefix length 66;
- exactly 480 Branch-B cases;
- minimum certified integer-boundary margin `>0.000775775058362`;
- exact Branch-A geometry barrier PASS.

## Scope and frozen obligations

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49,547,666,544`.
External `2^71` least-state floor remains conditional.

Gate A OPEN.
Gate B OPEN.
Global positive non-trivial-cycle exclusion OPEN.
`g=1` remains separate.
Root-aligned `kappa=0` / actual-crossing work remains frozen and available, not automatic priority.

## Successor

RL323 primary target: prove a support-uniform ordinary/full-`D` owned-interface consumer. The smallest accepted first theorem is an owned positive-interface extraction lemma giving either an absolute support-independent bound or a genuinely owned strict descent object, uniformly in the remaining matched-rank tail support.

Do not restart fixed-depth scans, support-by-support grammar, bare modulus escalation, recurrence-only reformulations, or any already frozen barrier route.

Knowledge catalogues: `stale/deferred` for connector closeout.
Lean formalisation: separate.
