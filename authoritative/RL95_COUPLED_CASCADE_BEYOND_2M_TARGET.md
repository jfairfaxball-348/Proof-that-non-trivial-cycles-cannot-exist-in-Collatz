# RL95 target — coupled four-tier cascade beyond deep radius 2,000,000

Date: 2026-08-25

## Authority

Continue from the frozen RL94 state.

RL94 eliminated every odd exact first-Farey/full-phase terminal value through

`2,921,758,703`.

The first surviving odd candidate for this route is

`k5=2,921,758,705`.

Do not restart generic quotient packing, independent midpoint congruence accumulation, or direct enumeration of odd cofactors.

## Frozen constants

- `C=42,150,931,628`;
- `S_*=26,594,276,905`;
- wide `(D_w,R_w,L_w)=(5,000,030,7,000,000,5,000,053)`;
- middle `(D_m,R_m,L_m)=(7,500,032,3,675,000,7,500,053)`;
- deep `(D_d,R_d,L_d)=(10,000,032,2,000,000,10,000,053)`;
- ultra `(D_u,R_u,L_u)=(15,000,038,335,000,15,000,053)`;
- current support `lambda=5,000,000/52,500,238,500,033`, `mu=1/7,000,001`;
- corrected conservative common successor `L_common=15,000,053`;
- first-Farey odd upper bound `42,150,931,559`.

At `k5`, `b=k-1` remains the worst stratum. The frozen RL94 margin is `-4,350`.

## 1. Immediate coupled batch

For `R_d=2,025,000`, unchanged-depth `A-C` geometry requires

- `R_m>=3,683,328`;
- `R_u>=366,674`.

Therefore a natural completed batch is

- ultra `r=335,001..370,000`;
- middle `r=3,675,001..3,700,000`;
- deep `r=2,000,001..2,025,000`.

Record every new global bit-length minimum and adjust safe depths immediately if a floor falls.

## 2. Re-optimize the exact support, do not inherit it blindly

At the RL94 frontier, feasible pair supports are `A-C`, `C-D`, and `D-E`, and `A-C` is optimal in the live aggregate direction.

After every material frontier move:

1. rebuild the staircase-complement corners;
2. enumerate exact feasible nonnegative supporting edges;
3. choose the support minimizing the live weighted aggregate bound;
4. prove that the chosen support is valid over all corners;
5. check whether an edge crossover has occurred.

Do not assume `A-C` remains optimal merely because it remains feasible.

## 3. Exact interval propagation

After each coupled improvement:

1. verify `floor(lambda*C)>2` or redo the block-count optimization;
2. retain the weakest load-bearing common successor ceiling (`15,000,053` unless improved rigorously);
3. evaluate the exact worst-stratum margin at `k5`;
4. if positive, locate the exact final consecutive odd exclusion and next failure;
5. continue the coupled cascade while productive.

## 4. Watch the long-range transitions

Under unchanged certified depths, the ultra depth-axis is nonbinding through deep radius floor about `3,499,993`. Before that point the principal burden is replenishing ultra radius, with periodic middle-radius extensions.

The `A-C` coefficient is weakening as `R_d` rises: `floor(lambda*C)` fell from `4,195` at RL93 to `4,014` at RL94. Track this exactly. The top-stratum reduction remains extremely safe now, but the trend is strategically relevant.

## 5. Optional stronger route: tier-specific charging

Do not assume a stronger mixed successor charge. However, if a rigorous lower bound on the population covered by the stronger wide/middle/deep tiers can be derived from the exact dual weights, test whether it materially improves interval propagation. Any such theorem must preserve physical-representative separation and may not count quotient addresses as physical blocks.

## 6. Red teams

Every promoted theorem must pass:

1. RL81 common-mode freedom;
2. RL79 generalized-increment homogeneity;
3. RL20 physical representative/packing separation;
4. primitivity;
5. first-Farey scope;
6. RL88 arbitrary-reset family;
7. verification economy;
8. load-bearing successor-charge consistency;
9. support-edge re-optimization.

## 7. Sustained attack protocol

A successful 25,000-step deep move is a checkpoint, not a close trigger. Continue through adjacent coupled batches, exact support re-optimization, interval propagation, and any nearby edge crossover until compute/context pressure genuinely threatens reliability or the route is exhausted.
