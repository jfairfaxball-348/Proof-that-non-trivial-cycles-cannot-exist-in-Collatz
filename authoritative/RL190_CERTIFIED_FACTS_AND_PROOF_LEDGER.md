# RL190 certified facts and proof ledger

Date: 2026-08-30

Scope: all new statements remain internal to `(v,H,J,d)=(37,0,23,-1)` and the inherited RL184 clean shallow-start family.

## Inherited frozen facts

- Extremal triple terminal necessary core: `E=[72797034370,103818202602]`.
- Extremal triple terminal normalized gap: `3^37/2^21`.
- `tau=37` triple-start normalized gap: exactly `2^37` or `2^38`.
- Extremal triple block span: exactly 38.
- RL189 terminal spacing: at least 43.
- RL189 `N_35<=9594979905`; at least `480194594` clean starts have `tau<=34`.
- RL187 `N_36<=7238318174`, `N_37<=7052720272`.
- RL187 ordinary absolute corrected-flow: `>2787212689/6291456>443`.
- RL183 global common-mechanical law: `2^c delta'=3delta+epsilon`, `|epsilon|<1`, away from source ranks `R-1` and `L-1`.
- RL182 first-ternary-digit sensor: `3|C_i` iff predecessor defect `G_(i-1)` is even on an ordinary p-edge.

## RL190.1 — separation 43 exclusion and spacing at least 46

**Class:** proved analytic mathematics + exact finite integer/rational certificate.

At separation 43 the simultaneous terminal-rank overlap is

`[72797034370,82723115287]`.

Across it the six required mechanical bits are exactly `212122`. None of the six pair-transition source ranks hits `R-1=57079296006` or `L-1=137528045311`.

Six-step affine propagation from `3^37/2^21` has center

`3^43/2^31`

and strict radius

`1519/1024`.

The ball misses both permitted next triple-start gaps `2^37,2^38`, so separation 43 is impossible.

Separations 44 and 45 have empty terminal-rank overlap. Therefore consecutive extremal triple terminals are separated by at least 46 phases.

## RL190.2 — separation 46 finite exceptional seam

**Class:** proved analytic mathematics + exact finite integer/rational certificate.

At separation 46 the exact rank overlap is

`[85411789764,103818202602]`.

Every rank except

`90789138715`, `101129528126`

lies in one of three ordinary/common-mechanical intervals with words

`212122121`, `212212121`, `212212122`.

The corresponding nine-step affine balls miss both `2^37` and `2^38`.

Thus any hypothetical separation-46 pair must begin at one of the two displayed terminal ranks. They are unresolved because the required nine-step path hits a switch/carry exception. No realization claim is made.

## RL190.3 — `N_35` density `1/15`

**Class:** proved analytic combinatorics + exact integer certificate.

Spacing `>=46`, triple span 38, and the inherited nontriple capacity `K<=floor(2S/37)` imply every triple-plus-following-nontriple group has `N_35` density at most `1/15`.

The bound is exact within this grouping inequality, attained by the arithmetic case `S=37,K=2`.

Therefore

`N_35<=floor(L/15)=9168536354`

and at least

`906638145`

clean starts have `tau<=34`.

## RL190.4 — dangerous H21 phase core and exact span

**Class:** proved analytic mathematics + exact finite integer/rational certificate.

The binding height-21 `{33,34,35}` co-owner has common terminal invariant

`(T,H)=(350220815692997949,21)`.

Full zero-prefix physical-gap compatibility refines the simultaneous terminal rank to

`D=[23369453298,41775866136]`.

This is entirely in the early mechanical compartment and disjoint from `E`.

Its `tau=35` starting numerator is `7*2^35` (or the height-one copy `7*2^36`), hence is not divisible by 3. RL182's sensor forces the immediately preceding defect to be odd/nonzero. The dangerous block therefore has exact span 36.

## Charging status

**Class:** exact retained obstruction.

With the strengthened `N_35` cap the monotone two-level split still has positive objective slope toward the `tau=35` weight under the binding budget `2x+y<=2^-22`, `x>=y`. Its optimum remains

`x=y=1/(3*2^22)`.

Thus no corrected-flow improvement beyond the inherited `>443` is promoted.

## Closure status

The isolated triple, separation-46 exceptional seam, sole high branch, preferred branch, Gate A, Gate B, non-trivial-cycle exclusion, and Collatz remain open.
