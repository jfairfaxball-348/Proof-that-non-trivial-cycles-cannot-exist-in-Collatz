# RL189 correction and demotion ledger

Date: 2026-08-30

## New corrections / demotions

None to inherited mathematical claims.

## Session bookkeeping correction

During exploration, an intermediate note considered whether a `tau=34` companion corridor might be needed to push the spacing argument. The promoted proof does **not** use that assumption. It uses the already co-owned clean `tau=35` 40-edge corridor, which supplies the required four post-terminal transitions for separation 41.

## Frozen scope locks retained

1. The physical corrected defect is `G_i=S_(i+p)-S_p-S_i`.
2. The corrected physical flow uses `2^G`; RL173's `3^-G` functional remains auxiliary only.
3. Necessary rank intervals and affine candidate images are one-way filters, not realization claims.
4. RL168-RL171 generic rank/lattice/inverse-rank barriers remain in force.
5. All RL180-RL189 statements here remain internal to `(37,0,23,-1)`.

## RL189-specific scope locks

- Spacing `>=43` concerns consecutive extremal `{35,36,37}` triple terminals only.
- The separation-41 proof uses four post-terminal ordinary/common-mechanical transitions from the clean `tau=35` corridor.
- `N_35<=9594979905` counts the inherited RL184 clean-start population.
- The charging result excludes only the stated two-level monotone `tau<=34` / `tau=35` split; it is not a universal no-go theorem for all phase-sensitive charges.
- The inherited ordinary absolute corrected-flow floor remains `>443`.
- No branch, Gate A/B, non-trivial-cycle, or Collatz closure claim is promoted.
