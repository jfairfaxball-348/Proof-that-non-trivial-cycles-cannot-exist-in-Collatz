# RL180 Multi-Support Residue Deficit and Odd-Part Lift Target

## Main objective

Continue the preferred `h_p=0` attack from RL179 without reopening dead routes.  The surviving high zero-height type is still

`(v,H,J,d)=(37,0,23,-1)`.

RL179 now proves that every necessary high continuation has cumulative corrected flow `<-7/8` through phase 29, `F2>1/3`, and therefore requires at least two positive corrected-flow phases after phase 29; four phase-29 histories require at least three.  Each positive term is `<1` by the residue-ordered mechanical envelope.

## Primary attack A: multi-support versus residue deficit

Use RL178's exact identity

`F2=rho_t-D`,
`D=sum_(r=1)^(L-1)(omega_(r-1)-omega_r)(1-2^-H_r)`

with RL179's forced multiplicity of later positive `G_i` phases.  Translate each mandatory positive phase into exact positive-height residue support and seek a quantitative aggregate lower bound on `D` strong enough to contradict the high value of `F2`, or else prove a strict location/order restriction on where the required supports can lie.

Do not replace exact residue weights by an unjustified uniform average.  Interval/rational bounds are required for any quantitative comparison.

## Primary attack B: exact phase-30 return structure

Start from the three RL179 phase-29 positive pair states and their complete phase-30 images.  Continue normalized integer relations only as far as needed to constrain the mandatory later positive supports.  Prefer valuation/congruence theorems or bounded exact interfaces over broad necessary-state BFS.

A surviving necessary state is not physical existence.

## Primary attack C: odd-part lifting

For the ten `c_(J+1)=1` zero-height indices, lift RL179's mod-8 sieve to mod 16 / mod 32 where this removes additional odd-part classes or forces early zero return.

Treat separately the four `c_(J+1)=2` indices

`J in {5,10,17,22}`.

Classify `v2(3^(J+2)w +/-1)` and the resulting exact pair-state transition by congruence classes of odd `w`.  Do not enumerate all admissible odd gaps individually.

## Avoid

- no RL173 `3^(-G)` resurrection;
- no RL175 sparse resultant as an independent gap obstruction;
- no silent `m>=2^71`;
- no physical-existence inference from necessary states;
- no uncontrolled enlargement of the RL177 height-only automaton;
- no move to `h_p>=1` until this multi-support / odd-part mechanism is worked as far as productive.

## Success condition

Preferred: exclude `(37,0,23,-1)` and therefore eliminate zero-height `v=37` entirely.

Strong partial success: prove a quantitative multi-support residue-deficit theorem, a strict support-location theorem, or a higher-modulus odd-part sieve that materially removes remaining zero-height classes.
