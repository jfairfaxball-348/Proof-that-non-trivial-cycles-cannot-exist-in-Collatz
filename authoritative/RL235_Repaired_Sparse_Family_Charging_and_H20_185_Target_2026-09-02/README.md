# RL235 — repaired sparse-family charging and H20 successor target

Date: 2026-09-02

Outcome: **Success B**.

RL235 repairs the coverage defect identified by RL234 in RL231's sparse-family charging theorem. The repair enumerates all 7,531 retained full-prefix simultaneous-owner atomic cells exposed to the repaired charge schedule, uses generic local defect budgets first, applies the preserved RL234 K-priced defect lemma only where needed, and uses the preserved RL231 chronological spacing theorem only for the exact H21 `{33,34,35}` terminal invariant to which it applies.

The resulting ordinary absolute corrected-flow floor is rigorously `>712`. The inherited signed-total/carry conversion gives each signed corrected-flow mass `>356` and each directional K variation `>118`. These remain total-variation statements, not prefix-excursion statements.

The former RL231 `>665` proof remains historically invalid as written; RL235 supersedes it with a new gap-free proof. The former `N17<=1,615` and `85,103,989` H17 scale remain demoted and are not restored.

The repaired next bottleneck is the exact H20 terminal cell

`T=5*3^35`, `H=20`, owners `{32,33,34,35}`, full-prefix core `[96166487668,98855162143]`.

A chronological occurrence cap `N20<=746,997,478` makes the next early-charge lift strictly profitable. A direct spacing theorem `>=185` suffices; spacing `184` does not.

No terminal rank is deleted. Gate A, Gate B and global non-trivial-cycle exclusion remain open.
