# RL202 certified facts and proof ledger

Date: 2026-08-31. Canonical status-bearing ledger. All physical implications
remain conditional on the sole high branch `(37,0,23,-1)` and every inherited
H21 ownership/Gate/global scope lock.

## Proved analytic mathematics

- Let `N=2^24`. On both chronological root windows
  `[0,N)` and `[L-N,L)`, the unique p-shift carry source `z=L-p` is absent.
  The inherited exact noncarry drift
  `3(K_(i+1)-K_i)=rho_i(2^(-h_(i+p))-2^(-h_i))` therefore gives
  `|K_(i+1)-K_i|<1/3` throughout both windows.
- With exact root anchor `K_0=2^37`, lifted `K_L=lambda K_0`, and inherited
  `0<delta=ln(lambda)<2^-40`, one has `0<K_L-K_0<1`. Hence every physical
  H21 terminal whose canonical terminal phase lies in either root window must
  satisfy
  `K_0-N/3 < K_H21(r) < K_0+1+N/3`.
- Using the inherited exact terminal law `K_H21(r)=rho_(I(r))*T/2^21` and its
  strict decrease in rank, exact logarithmic boundary certificates reduce all
  such ranks to `[38643145224,38659291956]`.
- Exact modular floor-sum counting removes 1,973,384 previously surviving
  necessary ranks from the forward root window and 1,973,397 from the backward
  root window, for 3,946,781 new root-anchor rank exclusions after subtracting
  seven already-inherited deletions.
- The H21 tau34 source has height one and the next 33 acceleration exponents are
  exactly one. Since every mechanical bit is at least one, all 34 corresponding
  heights are positive. They therefore cannot coincide with inherited global
  zero-height anchors `0,1,p,p+1`. Exhaustive modular collision algebra adds four
  new rank exclusions:
  `26058127775,28746802251,36398517186,39087191662`.
- The root-window exclusions and four anchor-collision exclusions are disjoint.
  Combined with the inherited RL201 necessary count 16,192,674,019, the new
  necessary-rank count is exactly **16,188,727,234**.

## Exact finite certificate

- `verification/verify_rl202_absolute_anchor.py` uses integer/Fraction arithmetic,
  sign-correct logarithm enclosures, four exact rank-boundary comparisons, exact
  AtCoder-style floor sums for the two complete phase windows, and exhaustive
  4-anchor x 34-offset collision enumeration.
- The certificate does not materialize or hash a 3.9M-entry exclusion list; the
  promoted set is defined exactly by compact rank/phase predicates and counted
  without an ordering-sensitive digest.

## Externally inherited certificate/state

Incoming RL201 is accepted under verification economy after its promoted bundle,
manifest, fresh-unpack review and portable fast suite. Its four eta classes
`0,8,9,17 mod18`, the first-rank positive-nu21 lift cut, all nu>=22 Hensel cuts,
the terminal K law, exact endpoint moment identities, denominator theorem,
coupled-local barrier and support correction remain binding.

## Method barriers and open obligations

The new restrictions are rank/absolute-anchor restrictions; they do not select
eta class, state e35, terminal sign or valuation. The root speed cone is proved
only on the two stated carry-free chronological windows and must not be extended
through the carry without a separate bound. The four zero-height collisions use
only the inherited exact 34-step H21 positive tail and named global anchors.
No physical H21 occurrence, H21 charging/ownership release, sole-branch closure,
Gate A/B closure, nontrivial-cycle exclusion or global Collatz conclusion follows.
