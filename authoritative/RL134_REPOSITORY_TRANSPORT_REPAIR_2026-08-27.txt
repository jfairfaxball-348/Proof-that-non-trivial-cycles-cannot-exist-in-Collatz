RL134 repository transport repair
Date: 2026-08-27

Affected promotion commit: 90849d0d268994dfdb3fde70df2d2e1c3e74fdfe
Parent authoritative commit: 196ee7fe627c09889664f3508962b7a1e12025bf

Defect discovered by post-commit sanity check:
- the extracted RL134 authoritative tree, sidecar, fresh-unpack report, and verifier files were intact;
- the direct binary RL134 ZIP stored by the GitHub connector was truncated to 3,030 bytes;
- therefore the original promotion commit was not accepted as a clean completed handover.

Repair:
- remove the corrupt direct ZIP;
- preserve its valid SHA-256 sidecar, which authenticates the original verified 16,940-byte ZIP;
- store a lossless eight-part base64 representation under authoritative/RL134_BUNDLE_TRANSPORT/;
- provide a deterministic reconstruction script that requires exact size 16,940, exact SHA-256 6ff26609ee5414dc7a2434b6bb3b7993577936894eb7c392395edf36ab6b21ec, and a clean ZIP CRC test;
- preserve all extracted RL134 mathematical/verification files unchanged.

Classification: repository transport repair only. No mathematical theorem, certificate scope, proof-state classification, frontier, correction/demotion ledger, or RL135 target changes.

After the repair commit passes the repository sanity check, the repaired RL134 handover may be treated as the authoritative incoming RL135 state. The history is not rewritten; the repair is explicit because the defect was discovered only after the original promotion commit was published.
