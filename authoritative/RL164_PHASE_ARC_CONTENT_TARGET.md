# RL164 — phase-arc content target

Start from the fully verified RL163 package. In the coprime `g=1` first-survivor branch, determine whether the actual positive odd arc numerators

`2^(B_j)y_(j+p)=3^p y_j+C_j`, `p=A^(-1) mod L`,

can be constrained by least-state ownership, predecessor content, or another ordinary-Collatz invariant beyond the telescoping total-count identity.

Do not use only the aggregate phase-step exponent identity; RL163 proves that its sum is tautological. Do not identify phase order with chronological order, and preserve the `g=1` / generalized-increment / physical-state / primitivity red teams. A negative result must state the exact non-consumption barrier rather than imply an exclusion.
