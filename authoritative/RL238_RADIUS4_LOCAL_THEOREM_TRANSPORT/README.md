# RL238 Radius-4 local-theorem transport

Completed generation: **RL238 — USER-DIRECTED PARTIAL TARGET CLOSEOUT / LOCAL-THEOREM SUCCESS**.

The canonical RL238 bundle is carried losslessly as base64 text parts in this directory. Reconstruct it with `reconstruct_rl238_bundle.py`, verify `PART_SHA256SUMS.txt`, then verify the reconstructed ZIP against the outer authoritative `.zip.sha256` sidecar.

Promoted local result: the literal inherited exact-distance-4 Radius-4 theorem is proved for primitive full-`D` self-rotations. This is a local theorem only. Gate A, Gate B, and global exclusion remain open.

The bundle contains the exact Radius-3 provenance used by RL238, the Radius-4 proof/correction ledgers, audited LMN dependency, mandatory red team, the full checksum-locked portable verifier suite, fresh-unpack record, and the RL239 target.

Knowledge catalogues remain stale/deferred.
