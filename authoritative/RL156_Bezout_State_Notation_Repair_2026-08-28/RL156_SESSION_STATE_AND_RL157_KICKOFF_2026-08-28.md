# RL156 session state / RL157 kickoff

RL156 repaired the state/Bézout symbol collision in RL155 without changing
the mathematics. RL157 resumes the dense full-modulus singleton phase attack
with `M` reserved for the least state and `u` reserved for the Bézout
coefficient in `Ap-uL=1`.

The live task is still an arithmetic discriminator for the coupled dense
positive phase remainder, or an explicitly new branch resource. Do not
reuse `F_1`, individual-level congruences, a small-prime sieve, sparse-phase
cancellation, or the legacy `g=2` one-excursion resultant route.
