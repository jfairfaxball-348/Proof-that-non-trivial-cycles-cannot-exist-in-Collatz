# RL156 red-team report

- `M` and `u` are distinct by definition; no equation identifies them.
- The full denominator remains `D=2^A-3^L`; no `F_1` replacement occurs.
- The verifier checks only retained algebra on exact toy paths, not a cycle
  converse or an exclusion.
- Positivity after binomial reduction is not treated as a contradiction.
- All global Gates and the singleton owner remain open.
