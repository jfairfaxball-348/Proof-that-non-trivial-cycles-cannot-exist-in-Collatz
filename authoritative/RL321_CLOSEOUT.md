# RL321 closeout — ordinary residual ownership, matched-rank localization, and Beatty carry collapse

Date: 2026-09-14
Status: CLOSED AND FROZEN
Incoming BASE_HEAD: `7af6550c25b6c900f097d207261d20682c42c0cf`
Successor: RL322

## Executive conclusion

RL321 meets the primary RL320 successor target structurally: the signed late-row residue is transformed through a genuinely parity-owned physical tail and exact physical prefix junction. The unbounded support residue is replaced by a canonical positive remainder and common quotient.

The session does not close Gate A, Gate B, `g=1`, or global positive non-trivial-cycle exclusion.

The main promoted advances are:

1. a genuine `ell`-odd prefix / `s`-odd tail split with exact valuation `v2(A-Z)=p`;
2. an exact tail-residue saturation theorem showing why suffix congruence alone cannot close the branch;
3. a canonical ordinary-owned Euclidean decomposition
   `K=3^s c0+J`, `0<J<3^s`, with common quotient
   `M=floor(z/2^r)=floor(y/3^s)` and `|c0|<=2^35`;
4. two genuine complementary `ell`-odd proper-factor identities carrying both `D0` and `H` without relabelling RL320's surrogate;
5. exact matched-rank localization `r=u_j-v_j` inside the inherited ordered physical rows;
6. a support-independent post-antipode residual exit clock `v2(M)<=75` (<=74 in the negative branch), explicitly not a descent theorem;
7. an exact `3n-1` dual residual and an all-depth saturation barrier for tail-only corner attacks;
8. in the negative canonical branch, a bounded-carry Beatty collapse
   `m=floor(nX/D0)` with externally conditional `2^29<n<=2^35`, together with `r>=41`, `s>=26`.

`PARENT_DIFFICULTY_DELTA = EASIER`.

The reason is concrete: the late-row branch no longer contains an arbitrary signed support residue plus an independent physical least-state scale. It now has an ordinary-owned residual, a matched physical rank interval, and in the negative branch one bounded carry that determines `m` exactly.

## Corrections / barriers

- Tail-only residue and least-state arguments are exactly saturated by a local physical counterfamily.
- The negative corner is also saturated at unbounded height by a `3n-1` dual family.
- The <=75-phase residual mismatch is not a crossing/descent contradiction; the physical trajectory remains above the residual at the first mismatch.
- A proposed all-rank/height-38 bound is DEMOTED because it incorrectly used RL319's root-aligned `G<2^35` estimate in the late-row-root branch.
- The bounded carry is not yet a complete finite certificate and does not license a generic support scan.

## Strategic audit requested at closeout

`RL321_STRATEGIC_AUDIT_RL305_TO_RL321.md` reviews RL305's pivot objectives against RL306–RL321.

Verdict: the programme has stayed true to the audit discipline and is genuinely materially closer in architecture and parameter control. The repeated EASIER labels do not compound arithmetically; the proof remains one major ownership-sensitive consumer short of turning the modern normal form into contradiction, and several global branches remain open.

## Verification

Portable verifier:

`verification/verify_rl321_canonical_residual.py`

The verifier checks small exhaustive ordinary-tail quotient transport, residue saturation, matched-rank localization, exact elementary constant inequalities, negative-corner dual saturation, and the first-mismatch gap comparison. The unrestricted results are analytic; bounded loops are regression evidence only.

## Successor

RL322 should consume the **canonical ordinary residual / bounded-carry normal form**, not restart tail-residue congruence, fixed-depth scans, support `3,4,5,...` grammar, or fixed-96 P/Q.

Primary alternatives already frozen by RL321:

- `Z0>0`: obtain a genuinely ordinary-owned consumer for the positive canonical surrogate/residual coupling;
- `Z0<0`: use `m=floor(nX/D0)`, `2^29<n<=2^35`, matched-rank localization, or the <=74 post-antipode clock to remove a parameter or create a complete finite certificate;
- if neither gives a parent-near contraction, return to the root-aligned `kappa=0` / actual-crossing obligation rather than refining tail residues indefinitely.

Knowledge catalogues: `stale/deferred`.
Lean formalisation: separate.
