# RL324 closeout — adjacent parent bridge and canonical half-branch contraction

Date: 2026-09-14
Status: CLOSED CANDIDATE FOR ATOMIC PROMOTION
Incoming BASE_HEAD: `71cd114d91e940a22fd6380abdb41f0a7a8d0e05`
Successor: RL325

## Executive conclusion

RL324 materially advances the missing parent bridge but does not close it.

The preserved RL323 late scratch was rederived and red-teamed successfully. The zero-carry matched rank is strictly interior, and the very next matched rank has exact unit-minus-one Euclidean quotient with a small complementary residual.

This adjacent pair is linked through genuine complementary full-`H` ordinary ownership, producing a real sign flip rather than another affine surrogate.

The matched-rank defect sign is then synchronized with the original RL321 least-root canonical carry sign. This yields the first direct parent-level payoff:

> on the entire `K>0` sign half of the original least-root canonical interface, the original canonical displacement and height are absolutely bounded:
>
> `r_can<=77265916075`, `s_can<=r_can`
>
> at the conditional first external survivor.

The adjacent post-crossing matched rank retains the same displacement bound, giving a two-rank bounded physical box.

However, RL324 also proves an exact local physical-run counterfamily showing that this bounded box cannot be propagated to later matched ranks using only the local defect recurrence, positivity, unit-minus-one residual geometry, and local least-state lower bounds. The remaining bridge must therefore consume genuinely global full-return ownership/minimality structure.

RL322 already excludes `Z0<0`. The unresolved canonical parent regime is now sharply isolated as

`Z0>0`,
`K<0`,
`c0=-n<=-1`,
`M=m-n<m`,
`1<=n<2^35`.

No Gate A, Gate B, all-`g=2`, all-`g>1`, `g=1`, or global positive non-trivial-cycle result is claimed.

`PARENT_DIFFICULTY_DELTA = EASIER`.

## Main promoted results

1. Validated adjacent zero-carry -> unit-minus-one matched-rank theorem.
2. Genuine full-ownership sign flip across the adjacent pair.
3. Unique linear matched-rank defect sign transition.
4. Exact synchronization of least-root canonical `K` sign with position relative to the crossing.
5. Absolute original-canonical displacement/height bound on the `K>0` half-branch.
6. Same conditional displacement bound at the immediate post-crossing matched rank.
7. Exact local physical-run counterfamily blocking rankwise propagation to arbitrary later matched ranks.
8. Remaining parent branch reduced to `Z0>0, K<0`.

## Verification

Portable verifier:

`verification/verify_rl324_parent_bridge.py`

It checks:

- the frozen RL323 rational-log boundary calculation;
- the new lower-side loss margin at `q=3182833229`;
- small exhaustive instances of the valid interior defect recurrence;
- the unit-minus-one orientation;
- exact local propagation-barrier families over representative large 2-adic gaps.

The unrestricted mathematical statements are analytic; bounded loops are regression evidence only.

## GLOBAL PROOF ROADMAP STATUS

1. **CURRENT_STAGE** — `R1`
2. **CURRENT_STAGE_NAME** — `Parent Bridge`
3. **CURRENT_STAGE_PROGRESS** — `50%`
4. **THIS_SESSION_ADVANCE** — RL324 connected the forced crossing to the original canonical sign, bounded the full `K>0` canonical half-branch, and isolated a precise global blocker for `K<0`.
5. **CURRENT_STAGE_BLOCKER** — A global full-ownership bridge closing or absolutely bounding the remaining `Z0>0, K<0` canonical parent branch.
6. **ADVANCE_CRITERION** — A theorem-level closure of the complete R1 parent bridge at the required parent scope, including `K<0`.
7. **REMAINING_STAGES** — `R2 — g=2 Closure`; `R3 — g>1 Lift`; `R4 — Scale Closure`; `R5 — g=1 Closure`; `R6 — Gate Reconciliation`; `R7 — End-to-End Proof`.
8. **ROADMAP_DELTA** — `ADVANCE`
9. **GLOBAL_PROOF_STATUS** — `OPEN`

## Successor

RL325 must work on the smallest theorem that advances R1:

> consume the genuine global full-`D0/H` ownership between the bounded zero-carry/unit-minus-one box and the original least-root canonical cut in the remaining `Z0>0, K<0` regime, yielding contradiction, genuinely owned descent, or an absolute original-canonical interface bound.

Do not try to propagate the bound one matched rank at a time; RL324 proves that route false without an additional global ingredient.

Knowledge catalogues: `stale/deferred`.
Lean formalisation: separate.
