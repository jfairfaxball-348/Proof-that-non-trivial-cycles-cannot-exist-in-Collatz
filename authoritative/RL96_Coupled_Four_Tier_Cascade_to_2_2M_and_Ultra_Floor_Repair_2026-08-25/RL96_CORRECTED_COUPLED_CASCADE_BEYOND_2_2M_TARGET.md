# RL96 target — corrected coupled cascade beyond deep radius 2,200,000

Date: 2026-08-25

## Authority

Continue from the frozen RL95 state.

RL95 eliminated every odd exact first-Farey/full-phase terminal value through

`2,921,766,549`.

The first surviving odd candidate for this route is

`k6=2,921,766,551`.

Do not restart generic quotient packing, independent midpoint congruence accumulation, or direct enumeration of odd cofactors.

## Frozen constants

- `C=42,150,931,628`;
- `S_*=26,594,276,905`;
- wide `(D_w,R_w,L_w)=(5,000,030,7,000,000,5,000,053)`;
- middle `(D_m,R_m,L_m)=(7,500,032,3,800,000,7,500,053)`;
- deep `(D_d,R_d,L_d)=(10,000,032,2,200,000,10,000,053)`;
- corrected ultra `(D_u,R_u,L_u)=(15,000,035,605,000,15,000,053)`;
- current support `lambda=1,600,000/17,500,079,500,011`, `mu=1/7,000,001`;
- conservative common successor `L_common=15,000,053`;
- first-Farey odd upper bound `42,150,931,559`.

At `k6`, `b=k-1` remains the worst stratum. The frozen RL95 margin is `-12,798`.

## 1. Immediate corrected coupled batch

For `R_d=2,225,000`, unchanged middle/deep depth geometry under the current `A-C` support requires

- `R_m>=3,816,661`;
- `R_u>=633,341`.

Therefore a natural completed batch is

- ultra `r=605,001..635,000`;
- middle `r=3,800,001..3,825,000`;
- deep `r=2,200,001..2,225,000`.

Use completed exact scans only.

The ultra floor has already fallen once. Record every new global ultra minimum and update

`D_u=B_u-2`

before rebuilding geometry.

## 2. Treat ultra floor loss as a live variable

The RL95 extension found

`B_u=15,000,037 at r=575,974`

and therefore

`D_u=15,000,035`.

Do not assume the ultra depth remains fixed during further radius extension.

However, the current `A-C` support has substantial E-axis depth reserve:

`D_u>=10,937,549`

is enough for the `E` corner alone.

Current reserve:

`4,062,486`.

Use this only as a sensitivity bound. It does not waive the requirement to update exact tier depths after every new global floor.

## 3. Re-optimize the exact support after every material move

At the RL95 frontier, feasible pair supports are

`A-C`, `C-D`, `D-E`,

and `A-C` is optimal in the live aggregate direction.

After every material frontier or depth move:

1. rebuild all staircase-complement corners using actual certified depths and radii;
2. enumerate exact feasible nonnegative supporting edges;
3. choose the support minimizing the live weighted aggregate;
4. prove it is valid over every corner;
5. check whether an edge crossover has occurred.

Do not inherit `A-C` blindly.

## 4. Exact interval propagation

After each coupled improvement:

1. verify `floor(lambda*C)>2` or redo the block-count optimization;
2. retain `L_common=15,000,053` unless a rigorous stronger population theorem is proved;
3. evaluate the exact worst-stratum margin at `k6`;
4. if positive, locate the exact final consecutive odd exclusion and first failure;
5. continue the coupled cascade while productive.

## 5. Long-range geometry

At the corrected RL95 state:

- current `floor(lambda*C)=3,853`;
- corrected ultra depth-axis remains nonbinding through deep radius floor `3,499,992`;
- the next deep move has only five units of inherited middle-radius slack, so middle replenishment is immediately mandatory.

Track the decline in `floor(lambda*C)` exactly. The top-stratum reduction is still extremely safe, but the trend is strategically relevant.

## 6. Optional stronger route: tier-specific charging

Do not assume a stronger mixed successor charge.

If a rigorous lower bound on the population covered by stronger wide/middle/deep tiers can be derived from the exact dual weights, test whether it materially improves interval propagation.

Any such theorem must preserve physical-representative separation and may not count quotient addresses as physical blocks.

## 7. Red teams

Every promoted theorem must pass:

1. RL81 common-mode freedom;
2. RL79 generalized-increment homogeneity;
3. RL20 physical representative/packing separation;
4. primitivity;
5. first-Farey scope;
6. RL88 arbitrary-reset family;
7. verification economy;
8. load-bearing successor-charge consistency;
9. support-edge re-optimization;
10. live bit-length floor update before geometry.

## 8. Sustained attack protocol

A successful 25,000-step deep move is a checkpoint, not a close trigger.

Continue through adjacent coupled batches, exact support re-optimization, interval propagation, floor changes, and nearby edge crossovers until compute/context pressure genuinely threatens reliability or the route is exhausted.
