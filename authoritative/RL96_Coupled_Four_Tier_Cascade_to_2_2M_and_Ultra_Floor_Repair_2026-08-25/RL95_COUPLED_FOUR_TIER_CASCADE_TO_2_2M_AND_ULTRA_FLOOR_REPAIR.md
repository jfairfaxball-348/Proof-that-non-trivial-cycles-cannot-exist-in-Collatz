# RL95 — coupled four-tier cascade to deep radius 2,200,000 and ultra-floor repair

Date: 2026-08-25

## 0. Executive outcome

RL95 continued the frozen RL94 coupled four-tier target under the verification-economy and sustained-attack rules.

**No Gate A global, Gate B, RL/nontrivial-cycle, or Collatz closure is claimed.** Every terminal exclusion below remains conditional on the inherited exact first-Farey/full-phase Gate-A branch.

Incoming first route survivor:

`k5 = 2,921,758,705`, with frozen RL94 margin `-4,350`.

RL95 did not stop at the requested first 25,000-step deep move. It completed a sustained coupled cascade advancing the exact certified radii from

- middle `R_m=3,675,000` to `3,800,000`;
- deep `R_d=2,000,000` to `2,200,000`;
- ultra `R_u=335,000` to `605,000`.

The middle and deep global bit-length floors remain unchanged, but the ultra extension discovered a new lower global minimum:

`15,000,037 at r=575,974`.

Accordingly the effective ultra safe depth for the full extended radius is repaired from

`D_u=15,000,038`

to

`D_u=15,000,035`.

This is a frontier-extension floor update, not a demotion of the frozen RL94 theorem on `r<=335,000`.

The final RL95 four-tier staircase is therefore

- wide `(D,R,L)=(5,000,030,7,000,000,5,000,053)`;
- middle `(7,500,032,3,800,000,7,500,053)`;
- deep `(10,000,032,2,200,000,10,000,053)`;
- ultra `(15,000,035,605,000,15,000,053)`.

The exact final `A-C` support is

`mu = 1/7,000,001`,

`lambda = 1,600,000 / 17,500,079,500,011`.

The weighted decrement floor remains very large:

`floor(lambda*C)=3,853 > 2`,

so `b=k-1` remains the worst block-count stratum.

An exact support-edge audit finds precisely the feasible two-corner supports

`A-C`, `C-D`, `D-E`.

`A-C` is optimal for the live terminal aggregate throughout the promoted RL95 interval and at the first failure.

Using the conservative load-bearing common successor ceiling

`L_common=15,000,053`,

the exact rational interval verifier excludes all `3,923` consecutive odd values

`2,921,758,705 <= k <= 2,921,766,549`.

At the final eliminated odd value the contradiction margin is `+2,508`; at the next odd

`k=2,921,766,551`

the margin is `-12,798`.

Therefore the updated branch-specific surviving odd window is

`2,921,766,551 <= k <= 42,150,931,559`, `k` odd.

## 1. Incoming verification and GitHub provenance

The supplied RL95 handover passed:

- outer SHA-256 sidecar;
- all 89 internal manifest entries;
- supplied fresh-unpack verification;
- complete RL94 fast verifier suite.

Verification economy was applied: inherited expensive scans were accepted after this gate and were not recursively replayed.

Read-only GitHub provenance found authoritative incoming commit

`5d4bf0125bd31c21191d1a09dd93f9ba395d193b`

with message

`RL95_Coupled_Four_Tier_Cascade_Beyond_2M_2026-08-25`.

No repository write was performed during the research attack.

## 2. Sustained cascade progression

The requested first coupled batch was

- ultra to at least `370,000`;
- middle to `3,700,000`;
- deep to `2,025,000`.

RL95 completed that batch and continued through additional coupled checkpoints instead of closing at the first success.

Representative exact endpoint progression:

| final deep radius | middle radius | ultra radius | ultra safe depth | `floor(lambda*C)` | odd values excluded from `k5` |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2,025,000 | 3,700,000 | 370,000 | 15,000,038 | 3,994 | 482 |
| 2,075,000 | 3,725,000 | 435,000 | 15,000,038 | 3,954 | 1,452 |
| 2,100,000 | 3,750,000 | 470,000 | 15,000,038 | 3,934 | 1,941 |
| 2,125,000 | 3,750,000 | 505,000 | 15,000,038 | 3,913 | 2,433 |
| 2,150,000 | 3,775,000 | 535,000 | 15,000,038 | 3,893 | 2,927 |
| 2,200,000 | 3,800,000 | 605,000 | 15,000,035 | 3,853 | 3,923 |

The final row is the promoted RL95 state. Intermediate rows are checkpoints from exact completed scans and exact rational recomputation; only the final row is load-bearing for the final theorem.

## 3. Exact ultra-radius extension and floor event

RL95 completed exact GMP scans modulo `2^15,000,056` over the gap-free range

`335,001 <= r <= 605,000`.

This covers `270,000` new ultra-radius values.

The decisive new minimum is

`15,000,037 at r=575,974`.

The inherited minimal-reset interface states that a balanced inverse-residue bit-length floor `B` certifies safe depth `D=B-2`. Therefore the full extended ultra tier becomes

`d<=15,000,035 and r<=605,000 => n_next<=15,000,053`.

Classification: **new exact finite certificate + inherited-interface analytic consequence + safe-depth floor update**.

The floor event was handled immediately. No subsequent geometry or interval result uses the old `D_u=15,000,038` at radii beyond its frozen RL94 domain.

## 4. Exact middle-frontier extension

RL95 completed five exact 25,000-step scans modulo `2^7,500,056` over

`3,675,001 <= r <= 3,800,000`.

The new-band minimum is

`7,500,039 at r=3,785,526`.

This is above the inherited combined minimum

`7,500,034 at r=1,635,300`.

Therefore the middle depth floor remains unchanged and the exact effective tier extends to

`d<=7,500,032 and r<=3,800,000 => n_next<=7,500,053`.

Classification: **new exact finite certificate + inherited-interface analytic consequence**.

## 5. Exact deep-frontier extension

RL95 completed eight exact 25,000-step scans modulo `2^10,000,056` over

`2,000,001 <= r <= 2,200,000`.

The new-band minimum is

`10,000,040 at r=2,090,420`.

This remains above the inherited combined minimum

`10,000,034 at r=378,722`.

Hence the exact deep tier extends to

`d<=10,000,032 and r<=2,200,000 => n_next<=10,000,053`.

Classification: **new exact finite certificate + inherited-interface analytic consequence**.

## 6. Final corrected four-tier geometry

The final staircase-complement corners are

`A=(0,7,000,001)`,

`B=(5,000,031,3,800,001)`,

`C=(7,500,033,2,200,001)`,

`D=(10,000,033,605,001)`,

`E=(15,000,036,0)`.

For

`mu=1/7,000,001`,

`lambda=1,600,000/17,500,079,500,011`,

`A` and `C` lie exactly on

`lambda*d + mu*r = 1`.

The other final corner values are

- `B = 1,944,454,877,779 / 1,944,453,277,779 > 1`;
- `D = 17,512,561,955,011 / 17,500,079,500,011 > 1`;
- `E = 8,000,019,200,000 / 5,833,359,833,337 > 1`.

Therefore every integer point outside the certified four-tier union satisfies

`lambda*d + mu*r >= 1`.

At `R_d=2,200,000`, this support requires at least

- `R_m >= 3,799,995`; actual `3,800,000`, slack `5`;
- `R_u >= 600,008`; actual `605,000`, slack `4,992`.

For the next 25,000-step deep move to `R_d=2,225,000`, unchanged middle/deep depths and current active `A-C` geometry require

- `R_m >= 3,816,661`;
- `R_u >= 633,341`.

Thus a natural next completed batch is middle to `3,825,000`, ultra to at least `635,000`, and deep to `2,225,000`, subject to rechecking all bit-length floors.

The corrected ultra depth-axis remains nonbinding through deep radius floor

`3,499,992`.

Classification: **updated exact four-tier supporting-line theorem + exact coupling thresholds**.

## 7. Ultra-depth robustness reserve

The new ultra minimum lowers `D_u` by three, but the active `A-C` support does not use corner `E` as an equality corner.

At the final support, the `E` constraint is simply

`lambda*(D_u+1) >= 1`.

The exact minimum ultra depth compatible with this `E` constraint is

`D_u >= 10,937,549`.

The corrected current ultra depth is

`D_u=15,000,035`,

leaving an `E`-axis reserve of

`4,062,486`.

Thus the three-bit floor loss is important for the exact tier certificate but is not geometrically close to dislodging the active `A-C` support. Future ultra-floor losses must still be applied immediately, but small losses of this scale are not themselves a route failure.

Classification: **new exact support-sensitivity observation**.

## 8. Supporting-edge re-optimization audit

At the final corrected staircase, exact enumeration of all two-corner nonnegative supporting lines gives precisely

- `A-C`;
- `C-D`;
- `D-E`.

The exact interval verifier compares their weighted aggregate values at every promoted odd `k` and at the first failure.

`A-C` is no worse than either alternative throughout.

At the incoming `k5`, the contradiction margins under the three supports are respectively

- `A-C`: `+60,032,360`;
- `C-D`: `+58,402,443`;
- `D-E`: negative by billions.

At the final promoted odd `k=2,921,766,549`, `A-C` remains strictly superior; `C-D` is already negative there.

Classification: **updated exact convex-envelope audit**.

## 9. Top-stratum reduction

At the final support,

`floor(lambda*C)=3,853`.

Increasing `b` by one raises the weighted bad-block floor by at least `3,853`, while the explicit `2b` contribution can improve the contradiction margin by only `2`, and short-successor capacity is nondecreasing.

Therefore the contradiction margin strictly decreases with `b`.

It remains sufficient to test

`b=k-1`.

Classification: **updated analytic monotonicity theorem**.

## 10. Exact RL95 odd-interval propagation

The final verifier evaluates every odd

`2,921,758,705 <= k <= 2,921,766,549`

at `b=k-1` with exact rational arithmetic, the corrected final `A-C` support, and

`L_common=15,000,053`.

All `3,923` values violate the necessary condition.

At `k=2,921,766,549`:

- `Q_min=123,139,091,657,859,702,260`;
- `D_b=16,090,339,865,877,884`;
- `R_b^max=10,151,905,930,914,660`;
- weighted bad-block bound `2,921,382,173`;
- forced multiscale-good blocks `384,375`;
- short-successor capacity `381,867`;
- contradiction margin `+2,508`.

At the next odd `k=2,921,766,551`:

- `Q_min=123,139,091,657,859,702,113`;
- `D_b=16,090,424,167,741,287`;
- `R_b^max=10,151,959,119,468,566`;
- weighted bad-block bound `2,921,397,479`;
- forced multiscale-good blocks `369,071`;
- short-successor capacity `381,869`;
- contradiction margin `-12,798`.

### Theorem 10.1 — RL95 branch-specific odd-interval exclusion

No exact first-Farey/full-phase Gate-A survivor has odd

`2,921,758,705 <= k <= 2,921,766,549`.

The updated branch-specific odd window is

`2,921,766,551 <= k <= 42,150,931,559`, `k` odd.

Classification: **new analytic theorem + exact finite 2-adic certificates + exact rational interval audit**.

## 11. Red-team audit

### RL81 common-mode freedom
Passed. No quotient state is promoted to a physical state.

### RL79 generalized-increment homogeneity
Passed. The ordinary `+1` minimal-reset constant remains essential.

### RL20 physical representative/packing separation
Passed. Weighted cover counts synchronized physical blocks, not quotient-address multiplicity.

### Primitivity
Passed. Residue/cofactor repetition is not interpreted as physical cycle-state repetition.

### First-Farey scope
Preserved. Every new exclusion is branch-specific.

### RL88 arbitrary-reset family
Passed. High reset valuation is excluded only within certified `(d,r)` regions.

### Verification economy
Passed. Only new RL95 bands were scanned; inherited expensive certificates were not replayed.

### Load-bearing successor charge
Passed. `L_common=15,000,053` is retained because the ultra tier remains load-bearing. The ultra safe-depth repair changes `D_u`, not the modulus-derived successor ceiling.

### Convex-envelope re-optimization
Passed. Final feasible supporting edges were explicitly enumerated and compared after the ultra depth repair.

### Floor-loss stop-and-repair
Passed. The new ultra global minimum was applied before any final theorem was promoted.

## 12. Proof-state ledger additions

### New proved analytic mathematics

1. Updated corrected four-tier support at `(R_m,R_d,R_u)=(3,800,000,2,200,000,605,000)` with `D_u=15,000,035`.
2. Exact final feasible-support enumeration and terminal-direction optimality of `A-C`.
3. Updated top-stratum monotonicity with decrement floor `3,853`.
4. Exact `A-C` E-axis robustness reserve of `4,062,486` ultra-depth units.
5. Elimination of `3,923` additional consecutive odd first-Farey/full-phase terminal values.

### New exact finite certificates

1. Ultra: `r=335,001..605,000`, gap-free; new/global minimum `15,000,037 at r=575,974`; updated `D_u=15,000,035`.
2. Middle: `r=3,675,001..3,800,000`; new-band minimum `7,500,039 at r=3,785,526`; combined minimum unchanged.
3. Deep: `r=2,000,001..2,200,000`; new-band minimum `10,000,040 at r=2,090,420`; combined minimum unchanged.

### Correction/demotion ledger

No frozen RL94 theorem is demoted.

RL95 extends the ultra radius beyond the RL94 domain and discovers a lower bit-length floor. The promoted full-radius ultra tier is therefore repaired to `D_u=15,000,035`. Any provisional geometry computed before that floor event was treated only as an intermediate checkpoint and is not load-bearing.

RL93's `15M` common-successor repair remains in force.

### Route limitation

The final margin at `k=2,921,766,551` is `-12,798`.

The active-support coefficient continues its expected decline:

- RL93: `floor(lambda*C)=4,195`;
- RL94: `4,014`;
- RL95 final: `3,853`.

This is still far above the top-stratum threshold `2`, but the trend remains strategically relevant.

The final `A-C` geometry has only `5` units of middle-radius slack, so the next deep move requires a middle extension as well as ultra replenishment.

## 13. Why RL95 closes here

RL95 did not stop at the first requested deep move. It completed:

- `270,000` new ultra-radius scans;
- `125,000` new middle-radius scans;
- `200,000` new deep-radius scans;
- eight completed 25,000-step deep chunks;
- repeated exact geometry re-optimization and interval propagation;
- a live ultra-floor stop-and-repair event.

Attempts to extend the ultra scan beyond `r=605,000` encountered repeated execution-time pressure before a complete next certificate was produced. Those incomplete attempts produced no promoted data and are excluded from the proof state.

Closing at `R_u=605,000`, `R_m=3,800,000`, `R_d=2,200,000` preserves a fully gap-free, independently verified frontier.

## 14. RL96 route selection

Continue at

`k6=2,921,766,551`.

Immediate corrected coupled target:

1. scan ultra `605,001..635,000` in completed exact chunks, watching for any further global floor loss;
2. extend middle `3,800,001..3,825,000`;
3. extend deep `2,200,001..2,225,000`;
4. recompute global minima and safe depths before geometry;
5. rebuild all staircase corners using the actual updated `D_u`;
6. re-enumerate all feasible supports and optimize in the live aggregate direction;
7. retain `L_common=15,000,053` unless a rigorous mixed-charge theorem is proved;
8. propagate the exact odd interval immediately;
9. continue through adjacent coupled batches while the route remains productive.

The sustained-attack and verification-economy rules remain mandatory.
