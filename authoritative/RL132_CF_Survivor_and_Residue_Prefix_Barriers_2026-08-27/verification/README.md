# RL132 verification

Run `./run_fast_rl132_verifiers.sh`. The suite reruns the inherited CF gate, checks the first surviving above-side convergent with rigorous intervals, and verifies the fixed-prefix residue barrier diagnostic.
