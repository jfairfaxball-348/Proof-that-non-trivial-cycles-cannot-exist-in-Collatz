# START HERE — RL132 barrier handover

Run `verification/run_fast_rl132_verifiers.sh`, then read the session state and RL133 target.

The conditional external-floor frontier remains `L>=49,547,666,544`. RL132 adds method barriers only; it closes no global gate.
