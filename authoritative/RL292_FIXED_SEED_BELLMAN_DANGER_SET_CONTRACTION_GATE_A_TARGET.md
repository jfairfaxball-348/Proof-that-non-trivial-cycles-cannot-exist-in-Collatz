# RL292 — fixed-seed Bellman danger-set contraction Gate-A target

Date prepared: 2026-09-10
Status: PREPARED, NOT STARTED

## Incoming classification

RL291 closed as

`PROGRAMME_CONSOLIDATED_GLOBAL_REACHABILITY_OBSTRUCTION_AND_FIXED_SEED_BELLMAN_COMBINATION_FAST_TRACK_RECOMMENDED`.

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

Gate B remains separate/open/frozen. Fifth selector remains unscanned. Radius 6+ remains frozen.

No global non-trivial-cycle exclusion is claimed.

## Why this target is first

RL291 audited the programme from the pre-RL100 Farey/cascade era through RL290 and found no dormant theorem that already closes Gate A.

The repeated missing resource is one global, physical, gauge-invariant reachability/ownership control. Earlier programmes saw it as branch coverage, non-homogeneous ownership, global prefix/cancellation, physical incidence, common-mode scale, upstream checkpoint reachability, all-prefix 2-adic dominance, or fixed-seed historical area versus rejected-tube depth.

RL292 must therefore combine the surviving structures rather than start another independent local-charge programme.

## Primary sufficient theorem

Use RL290's gauge-free future-threat envelope `Bcal` on physical states.

The primary target is

`Bcal(1,-13) <= 0`.

Equivalently, under a consistent root normalization, prove a fixed-seed Bellman domination statement of the form:

> for every positive-even physical checkpoint `s` reachable from the RL fixed seed with accumulated canonical height `H(s)`, the future threat of `s` is no greater than the historical cost already paid to reach it.

An equivalent backward danger-set statement is acceptable:

> every checkpoint of future threat at least `r` requires at least `r` units of genuine fixed-seed historical area/height.

Either form must imply the exact Gate-A target, not merely a bounded-height certificate.

## Required inherited inputs

Treat the following as the compact live kernel recovered by RL291.

1. **RL279 scalable cost structure**
   - zero-rank/run compression;
   - `H_can` as exact zero-rank lag;
   - height-coupon decomposition;
   - `17/2<S<21` zero-mass corridor;
   - exact boundary scale sign law.

2. **RL282 terminal-backward barrier and repaired finite authority**
   - affine terminal-tail compression;
   - local terminal families showing tail thinning alone is insufficient;
   - repaired exact `H<=22` certificate as falsification support only.

3. **RL283 global reachability equivalence**
   - dangerous high divisibility reconstructs prefix legality;
   - the checkpoint theorem `nu_2(J)<=H` is a legitimate sufficient target;
   - `Beta` is a one-generation boundary-hazard projection, not the full recursive threat.

4. **RL284 labelled accelerated geometry**
   - exact labelled bijection and predecessor rays;
   - local reversibility barriers.

5. **RL285/RL286/RL287 cost bridges**
   - all-prefix 2-adic/Ferrers reformulation;
   - excursion carry equals normalized coupon defect;
   - zero-height boundary quotient, cylinder isometry, and height-bounded non-boundary skeleton;
   - state-free/local scalar reserve barriers.

6. **RL288/RL289 fixed-seed ancestry**
   - synchronized first deviation;
   - exact affine ballot bijection;
   - rejected-tube interpretation of checkpoint valuation;
   - neutral-prefix gauge correction.

7. **RL290 physical Bellman quotient**
   - seam gauge, merger, and equal-area-diamond barriers;
   - physical-state future-threat envelope `Bcal` and checkpoint Bellman operator;
   - exact positive-even macro graph with positive costs;
   - complete height-one kernel;
   - universal exact height-one terminal launchpads;
   - stripped-unit / labelled-accelerated renormalization;
   - genuine chain `2514 -> 5378 -> 21842 -> 8192`.

## First attack: exact height-one kernel under fixed-seed ancestry

Start with the fully classified height-one Bellman kernel.

Seek a gauge-invariant potential, backward danger-set rule, or ancestry restriction that is simultaneously:

- defined on physical checkpoint states or a proved fixed-seed quotient;
- invariant under arbitrary retained positive zero-height boundary motion;
- invariant under branch mergers and equal-area diamonds;
- invariant under neutral `(101)` seed-prefix insertion;
- charged exactly by genuine height/coupon cost;
- strong enough to dominate endpoint valuation / rejected-tube depth.

The preferred positive outcome is a Bellman supersolution `Phi` whose macro-edge decrement is paid by exact height and whose root value proves `Bcal(1,-13)<=0`.

A backward formulation is equally good if it proves that the fixed-seed predecessor set of threat level `r` lies outside every historical-area ball of radius `<r`.

## Mandatory red-team tests before promotion

Every proposed invariant or potential must be tested against all of:

1. arbitrary positive boundary retention / zero-cost phase reset;
2. RL290 fixed-seed branch mergers;
3. RL290 loop-free equal-area diamonds;
4. neutral `(101)` seed-prefix gauge;
5. RL287 legal-segment cylinder isometry;
6. RL283 adjacent-swap valuation jumps;
7. RL289 finite-suffix transparency and rejected-tube interpretation;
8. the recursive threat chain
   `2514 -> 5378 -> 21842 -> 8192`;
9. RL290's nested jump `1074 -> 1364`;
10. generalized/fake-family discrimination where relevant.

If a candidate fails one of these, record the exact barrier and do not repackage it as a new coordinate.

## Success condition

RL292 succeeds strongly if it proves any one of:

- `Bcal(1,-13)<=0`;
- the equivalent global fixed-seed checkpoint theorem `nu_2(J)<=H` for every reachable positive-even balanced checkpoint;
- an exact backward danger-set contraction that immediately implies either theorem;
- a strictly weaker but analytic theorem that materially contracts the exact residual `k>=25`, odd, `H_can<k` and exposes a finite or theorem-sized final complement.

## Controlled pivot condition

If the exact height-one kernel does not contract after fixed-seed ancestry is imposed, do not reopen raw enumeration.

Instead:

1. freeze the smallest exact counterexample/failure mechanism;
2. identify the smallest excursion height `h>1` whose kernel is required to repair it;
3. extend the Bellman operator only to that minimal additional kernel;
4. preserve the same fixed-seed, gauge, merger, and cost tests.

If the Bellman formulation itself proves technically obstructive while the consolidated cost architecture remains sound, pivot to the equivalent direct checkpoint theorem

`globally reachable positive even d=1 checkpoint (J,H) => nu_2(J)<=H`

using RL279 + RL286 + RL287 + RL288/RL289 directly.

## Forbidden principal routes

Do not make the principal programme any of:

- fixed-prime p-adic forcing from `D|Q` alone;
- ordinary arc-content as an independent second constraint;
- uncovered first-Farey or other branch cascades;
- bare modulus escalation;
- phase/precision-only cylinder merging;
- reflected H21/root-window arguments without the corrected global prefix datum;
- the demoted RL231 sparse-family charging theorem as previously stated;
- deeper raw H21/e=4 stratification;
- repeated fixed-zero level enumeration;
- CRT/absolute quotient reconstruction as a contradiction;
- final-tail 3-adic thinning/mass/F ceilings alone;
- state-free carry/height budgets;
- local primality/irreversibility/valuation-only charges;
- another local scalar reserve vulnerable to phase reset;
- finite shifted-valuation tables as proof;
- absolute signed rank;
- bounded recent suffix;
- cycle rotation;
- chosen-history seam charges;
- larger raw height-cap enumeration;
- Gate B;
- fifth selector;
- Radius 6+.

## Finite evidence policy

The repaired RL282/RL285 bounded certificates and RL290 nested-valuation regressions may be used only as falsification or pattern-discovery oracles.

No finite observation becomes a global theorem without an analytic bridge.

## Session discipline

RL292 is a theorem attack, not another programme-wide audit and not a route tournament.

Work the Rank-1 synthesis far enough to obtain a theorem, a precise barrier, or a minimal controlled kernel extension. Do not proliferate new coordinates without first checking whether they are algebraically independent, gauge invariant, merger invariant, boundary invariant, and genuinely fixed-seed/global.

Close with a promoted theorem/barrier ledger, exact residual status, red-team result, and one unique successor target.
