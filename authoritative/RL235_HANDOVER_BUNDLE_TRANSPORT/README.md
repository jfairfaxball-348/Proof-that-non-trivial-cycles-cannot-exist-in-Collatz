# RL235 handover bundle transport

The completed RL234 package is stored directly in Git with tree SHA:

`2eb4a804304dba82ee98ce778c31c10cc031322b`.

Run `reconstruct_rl234_bundle.py` inside the completed package to reproduce the deterministic canonical ZIP, then compare it with the outer `.zip.sha256` sidecar.

Canonical ZIP SHA256: `ca4e61c9404e565957f67d4d11d63d9a28fa0de4c1b5af29277dc5b45624435d`.

RL235 is prepared but **NOT STARTED**. Knowledge catalogues remain `stale/deferred`.
