# RL290 — gauge-invariant fixed-seed seam / rejected-tube Gate-A target

Date prepared: 2026-09-09
Status: PREPARED, NOT STARTED

## Incoming classification

RL289 closed as

`FIXED_SEED_BALLOT_BIJECTION_REJECTED_TUBE_AND_SEAM_BARRIERS_PROVED`.

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

## Core inherited RL289 conclusions

RL289 proves:

1. exact fixed-seed parity-cylinder affine relation
   `zeta_y == 3 zeta_x + 14 (mod 2^n)`;
2. exact converse:
   canonical prefixes from `J=-13` are precisely residues `z` for which the parity-count difference between `A(z)=3z+14` and `z` never goes negative;
3. at a balanced state the current section is
   `g_J(u)=3u+1-J`,
   so `nu_2(J)=k` is depth-`k` agreement with the reference `h(u)=3u+1`;
4. for a positive even balanced state, `nu_2(J)=k` is exactly the depth of the complete formal `d=0` rejected sibling tube adjacent to the legal ballot path;
5. the old RL283 terminal extension is one branch of that full rejected tube;
6. any fixed finite recent suffix is transparent to arbitrarily deep future boundary phase;
7. rational final-pair common-phase invariants collapse to the inherited defect;
8. pure `3`-adic endpoint-code/rank data admit arbitrarily deep zero-area fake terminal families;
9. the mixed endpoint-code/carry representation is exact, but accumulated `H` is a path cost and genuine histories can merge;
10. signed defects are finite `3`-adic truncations of the inherited fixed-seed shadows; apparent high **absolute** rank rigidity is a neutral-prefix gauge artefact and is not a Gate-A contraction;
11. all ballot-restoring cycle-lemma rotations of a terminal formal extension have fixed-seed seam valuation at most `2`, so reflection/rotation repairs ballot order while destroying fixed-seed divisibility.

## Mission

Attack Gate A using a **gauge-invariant post-departure fixed-seed seam/history constraint**.

The conceptual target is now:

`past fixed-seed legal ancestry / accumulated ballot area`

versus

`depth of the adjacent complete rejected d=0 tube`

or equivalently

`agreement depth of the balanced section g_J with h(u)=3u+1`.

Seek a theorem which uses the full fixed seed `-7` / affine map `A(z)=3z+14` and is invariant under insertion/deletion of the neutral `(101)` seed prefix.

A successful result should imply

`J>0, K=J+2^d-1, A=H+d-1 => nu_2(K-1)<=A`

or materially contract the exact residual.

## Required starting point

1. Treat RL289's affine ballot bijection and rejected-tube theorem as fixed analytic input.
2. Work only with variables/invariants unchanged by arbitrary neutral `(101)` prefix insertion.
3. Preserve the distinction between the legal canonical path and the adjacent formal rejected `d=0` tube.
4. Preserve fixed-seed arithmetic across any cut/seam; RL289 proves cycle-lemma rotations lose all but at most two powers of `2`.
5. Adversarially test every proposed local/history invariant against:
   - RL287 fixed-segment cylinder isometry;
   - RL289 finite-suffix transparency;
   - the positive zero-height boundary hazards;
   - the signed-defect gauge correction.
6. Use the inherited `H<=22` certificate only as a falsification oracle.

## Preferred forms of progress

Prefer one of:

- a gauge-invariant section-tree distance transported from root `A(z)=3z+14` to the balanced reference `h(z)=3z+1`;
- an injective or irreversible seam charge from levels of the complete rejected tube to genuinely historical ballot-area events;
- a fixed-root theorem showing that deep agreement with `h` forces at least as much accumulated area;
- a material arithmetic contraction of possible seam configurations for the residual odd `k>=25`.

A decisive impossibility/barrier theorem for this entire gauge-invariant seam route is also a valid checkpoint.

## Forbidden principal routes

Do not make the principal route:

- absolute signed-defect rank or the demoted high-rank discrete-log programme;
- pure `3`-adic endpoint codes / reverse ranks without the fixed-seed `2`-adic coupling;
- cyclic rotation / cycle-lemma ballot repair;
- another bounded recent-suffix argument;
- another scalar reserve in `(S,D,Q,F,B)`;
- local first-return carry/height bounds;
- `M/T` magnitude safety or finite shifted-valuation tables;
- reverse predecessor enumeration alone;
- boundary density / neutral-cycle deletion alone;
- larger raw height-cap enumeration;
- Gate B;
- fifth selector;
- Radius 6+.

## Scope

Gate B remains separate/open/frozen. Fifth selector unscanned. Radius 6+ frozen.

No global non-trivial-cycle exclusion is claimed.
