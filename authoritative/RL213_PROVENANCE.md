# RL213 provenance and authority record

Date: 2026-09-01.

Incoming remote/default-branch head: `3120105163bbcad7f14186db4499514dfd216cab`.
Incoming authoritative tree: `0eabe22e53e170eaaa8b6ff8ef2149f17356f739`.
Incoming job: RL213. Successor job: RL214.

RL213 uses no external mathematics or external certificate. The analytic endpoint-collapse argument uses only the frozen RL201 endpoint moment and the inherited RL210/RL212 e=16/root identities. The finite G56 saturation certificate uses standard-library Python and exact integer modular arithmetic.

The incoming RL212 authority is frozen by exact Git tree identity. Connector closeout uses lossless Git-object transport: unchanged inherited Git objects are reused exactly, while the RL213 overlay is sealed by an internal SHA256 manifest, deterministic ZIP reconstruction, outer SHA256 sidecar, clean-unpack verifier replay and proof-state guard before one atomic ref advance.

The exact incoming authority is frozen under `sessions/RL212`; successor authority is completed RL213 with one RL214 target.

Knowledge catalogues remain stale/deferred and are not part of the proof-state safety boundary.
