# RL155 session state / RL156 kickoff

RL155 freezes the full-modulus singleton identity as a dense positive
remainder.  RL156 must find an arithmetic discriminator for that remainder,
or change branch with an explicitly new resource.  Do not reuse sparse phase
or `g>1` factor arguments.

