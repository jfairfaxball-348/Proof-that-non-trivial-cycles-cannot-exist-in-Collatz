# RL316 — reduced-shadow interface full-ownership target

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED
Session type: SUPPORT-INDEPENDENT FULL-D OWNERSHIP ATTACK AFTER RL315

## Absolute objective

Continue toward proving that a positive non-trivial Collatz cycle cannot exist.

RL315 materially changed the architecture.  Do not restart the old literal
controlled-branch repeated-level target.

## Authoritative starting point

Read:

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `sessions/RL315/RL315_CLOSEOUT.md`
5. `sessions/RL315/RL315_RESEARCH_LEDGER.md`
6. RL206, RL233, RL310-RL313 only as needed.

Treat the repository as authoritative.

## Exact inherited object

For every `g>1`, write

`A=ga`, `L=g ell`, `gcd(a,ell)=1`.

RL315 supplies a canonical reduced word `tau` of counts `(a,ell)` and a
nonnegative integer interface `f` satisfying

`d_i=tau_(i mod a)+f_(i+1)-f_i`.

Every interface column has a zero.

The universal shift `a` has a genuine zero-flow window, so a proper reduced
balanced return always exists for `g>1`.

With

`X=2^a`, `Y=3^ell`, `D0=X-Y`,
`H=(X^g-Y^g)/(X-Y)`,

and a phase normalized by `f_0=0`, full ownership implies

`sum_i 2^i 3^(L-C_i^v-f_i)(3^(f_i)-1)=4 n H`

for an integer `n>0`.

The reduced rational shadow strictly dominates an actual physical state in
every reduced phase.

## Primary target

Prove a support-independent ownership theorem of the form:

> A nonzero canonical interface `f` cannot satisfy genuine full `D0H`
> ownership and the binary path constraints of a positive primitive cycle.

Equivalent acceptable outcomes:

- force `f=0`, contradicting primitivity for `g>1`;
- force a universally bounded interface certificate already excluded;
- reduce every nonzero interface to a finite exact arithmetic certificate whose
  size is independent of an unbounded support parameter.

## First attack order

1. Rewrite the ownership energy by rows/layers and seek a small-coefficient or
   no-carry theorem using both `D0` and `H`.
2. Exploit binary path legality:
   - rises only on `tau=0`;
   - falls only on `tau=1`;
   - layers are order-preserving transports of odd events.
3. Attack `g=2` separately:
   - fully-active branch is impossible;
   - balanced rows obey
     `q_0-q_1=(X+Y)(x_1-x_0)`;
   - fixed-weight `Q(w) mod 2^a` decoding makes the outgoing row a deterministic
     function of the boundary state.
4. If the analytic ownership route stalls, independently replay RL315's first
   reduced-fibre numerical scratch with a portable exact verifier before using
   any of its constants.

## Binding red teams

1. `H | Q(d)-Q(tau^g)` alone is insufficient; nonrepeated counterexamples
   exist.
2. Do not infer local denominator ownership.
3. Do not count recurrence/gauge identities as independent constraints.
4. Respect RL206 additive compatibility classification.
5. Respect RL233 finite-modulus barrier.
6. Do not replace the interface route with support `3,4,5,...` grammar.
7. Do not equate canonical flow mass with optimal transport radius.
8. Do not silently promote RL315's unreplayed large numerical scans.
9. Do not treat the reduced rational shadow as an integer cycle.
10. Keep `g=1` separate; there is no proper reduced balanced return there.

## Secondary numerical target

RL315 preserved promising scratch suggesting the first internally surviving
reduced fibre may have both finite multiplicity and finite least-state scale.

If re-entered:

- regenerate every constant from source;
- produce a portable exact verifier;
- use stopping-to-smaller induction only with contiguous audited coverage;
- record checksums/range endpoints;
- do not promote conversational scan claims without replay.

## Fallback ranking

If the interface ownership target hits a precise structural barrier:

1. `g=2` deterministic row-decoding / terminal ownership;
2. reproducible first-fibre finite closure;
3. mature `g=1` Bezout / ordinary-`+1` order-sensitive route;
4. only then reconsider other RL314-ranked frozen routes.

## Scope

RL315 is closed and frozen.
Gate A remains open.
Gate B remains open.
Global positive non-trivial-cycle exclusion remains open.
No Collatz conjecture claim is made.
Lean formalisation remains separate.
Knowledge catalogues remain stale/deferred.
