# RL108 target — word-sensitive physical equality bridge to the ordinary basin

Use the frozen RL107 collision classification and equality-barrier analysis as
negative constraints.  Work with the actual maximum-rooted first-Farey word
and actual physical boundary states.  Seek a theorem that forces an **exact
integer identity** between one such physical state (or an explicitly owned
roof predecessor) and a certified ordinary `+1` basin node.

A successful bridge must be genuinely word-sensitive: it must use ordering,
prefix geometry, physical extremality, or another full-ownership property not
reducible to raw counts, residue compatibility, bare inverse-cylinder
minimality, CRT matching, quotient proximity, or generalized-increment
population estimates.

Priority tests:

1. determine whether exact prefix ordering/extremality can force a unique
   least physical representative stronger than a local cylinder minimum;
2. test whether a boundary/roof state can be identified exactly with an LTE
   blue-comb or another independently certified ordinary basin family;
3. if equality cannot be forced, isolate the sharp word-sensitive obstruction
   and freeze it rather than reverting to already-bounded count/collision
   routes.

Any promoted claim must retain actual physical-state ownership, identify its
ordinary `s=1` discriminator, and pass RL20 (denominator ownership), RL79
(generalized-increment scaling), RL81 (physical vs quotient state),
primitivity, raw-multiplicity, scope, and external-input red teams.

Do not use the interrupted RL107 leading-`11` enumeration as evidence unless a
new exact, gap-free run is independently completed and verified.
