# RL144 completion review

RL144 is a stop-and-repair closeout.

- **Demotion:** RL143's claimed height-one exclusion for
  `303,279,262,681 <= g <= 771,316,334,039` is retracted because it equated
  the RL123 depth-2 boundary count `C_(2,1)` with the larger two-odd-window
  count `O_2`.
- **New analytic theorem:** height-one ordinary odd runs have length at most 4.
- **New corrected floor:**
  `W >= (162/13)(g(2L-A)-3)`.
- **Exact method barrier:** even with the stronger RL137 state constant and the
  carried RL143 upper-width estimate, the corrected lower/upper ratio stays
  below `0.896241` throughout the inherited one-defect range.

No multiplicity, Gate, or global Collatz status is closed by RL144.
