# RL97 SESSION STATE AND RL98 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Frozen RL97 exact tiers

### Wide inherited tier

`d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`.

### Middle RL97 tier

New exact scan `r=3,950,001..4,350,000` has minimum

`7,500,036 at r=4,084,218`.

The inherited global minimum remains

`7,500,034 at r=1,635,300`.

Effective tier:

`d<=7,500,032`, `r<=4,350,000` => successor `<=7,500,053`.

### Deep RL97 tier

New exact scan `r=2,400,001..3,000,000` has minimum

`10,000,036 at r=2,955,163`.

The inherited global minimum remains

`10,000,034 at r=378,722`.

Effective tier:

`d<=10,000,032`, `r<=3,000,000` => successor `<=10,000,053`.

### Ultra RL97 tier

New exact scan `r=870,001..1,680,000` has minimum

`15,000,037 at r=905,728`,

with a second tie at `r=1,368,912`.

The inherited global minimum remains the same value

`15,000,037 at r=575,974`.

Effective tier:

`d<=15,000,035`, `r<=1,680,000` => successor `<=15,000,053`.

No RL97 depth-floor repair is required.

## Frozen final four-tier support

`mu=1/7,000,001`

`lambda=4,000,000/52,500,238,500,033`

Active corners:

`A=(0,7,000,001)`

`C=(7,500,033,3,000,001)`.

Other corners:

`B=(5,000,031,4,350,001)`

`D=(10,000,033,1,680,001)`

`E=(15,000,036,0)`.

Exact feasible pair supports remain `A-C`, `C-D`, `D-E`, and `A-C` is optimal over the full promoted RL97 interval and at the first failure.

`floor(lambda*C)=3,211`, so `b=k-1` remains the worst block-count stratum.

## Frozen RL97 interval result

Every odd

`2,921,774,731 <= k <= 2,921,801,521`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `13,396` additional consecutive odd values.

At `k=2,921,801,521`, margin is `+10,473`.

At

`k8=2,921,801,523`,

margin is `-3,549`.

Updated branch-specific surviving odd window:

`2,921,801,523 <= k <= 42,150,931,559`, `k` odd.

## Frozen geometry/coupling state

At `R_d=3,000,000`, the final `A-C` support requires

- `R_m>=4,333,329`; actual `4,350,000`;
- `R_u>=1,666,673`; actual `1,680,000`.

For `R_d=3,025,000`, it requires

- `R_m>=4,349,996`; actual already `4,350,000`;
- `R_u>=1,700,006`.

For `R_d=3,050,000`, it requires

- `R_m>=4,366,662`;
- `R_u>=1,733,340`.

The ultra depth-axis remains nonbinding through deep radius floor `3,499,992` while current depth floors hold.

At the final support the `E`-axis ultra-depth minimum is `13,125,059`, leaving reserve `1,874,976`.

## Conservative successor charge

Retain

`L_common=15,000,053`.

No tier-specific population theorem was proved in RL97.

## Authoritative RL98 live obligation

Attack `k8=2,921,801,523` with the corrected coupled four-tier cascade beyond deep radius `3,000,000`.

Immediate priority:

1. extend ultra `1,680,001..1,705,000` or slightly beyond;
2. recompute the ultra global minimum immediately;
3. extend deep `3,000,001..3,025,000` (no middle extension is geometrically required for this single step if floors hold);
4. rebuild and re-optimize every exact feasible support;
5. propagate the odd interval immediately;
6. continue to the next coupled checkpoint: middle at least `4,375,000`, ultra at least `1,740,000`, deep `3,050,000`;
7. continue through adjacent 25,000-step deep batches while productive, with special attention to the deep near-floor behavior and the approach to the `3,499,992` ultra depth-axis threshold.

## Sustained attack rule

A theorem, endpoint elimination, floor event, support crossover, or improved bound is a checkpoint rather than a close trigger. Continue through coupled frontier propagation, exact support re-optimization, interval propagation, floor changes, adjacent regimes, and red-team while the route remains productive and reliability is not threatened by compute/context pressure.

## Verification economy rule

After the current bundle checksum, internal manifest, and fast verifier suite pass, accept this frozen proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize new mathematics on the stated live obstruction.
