# RL328 route-viability provenance

Date: 2026-09-15
Status: VERIFIED PROVENANCE FOR RL328 CLOSEOUT

RL328 depends on RL327's verified ordered `g=2`, `Z0>0`, `K<0` branch; `(a,ell)=(217976794617,137528045312)`; high-carry ownership threshold `20390252058`; carry cap `32596612662`; mechanical excess sequence with `Z+K=T+1`; conservative automaton convention admitting all >=3-positive transitions and zero-run lengths through 49; and the residue-min weighted linear telescope with exact rational/logarithmic enclosure.

RL324's local-propagation barrier remains binding.

The new promoted object is a **method viability barrier** combining one explicit repeatable edge family already admitted by the inherited conservative graph with the inherited exact telescope consumer. Because it is a barrier about what the current method can certify, it does not depend on the shorter-singleton scratch enumeration.

No physical trajectory is asserted to realize the repeatable graph cycle; the graph is a conservative superset and the cycle is used only to show that a potential theorem on that unchanged graph cannot have asymptotic coefficient below `49/3`.

The optimistic carry values are method diagnostics, not new physical carry caps. The internal/external scope split and externally conditional `2^71` least-state floor are unchanged.
