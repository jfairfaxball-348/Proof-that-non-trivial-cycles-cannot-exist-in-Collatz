# RL215 target — consume the finite e=16 ownership-root family

Date: 2026-09-01.

Accept all RL214 results and inherited scope locks.

## Primary target

RL214 has reduced every physical e=16 H21 realization to one of **1,629,818,931** bounded arithmetic root candidates after the ordinary terminal filter, while retaining all 45,046 H21 prefixes.

The next target is to add one genuinely independent full-word digit or residue.

1. Starting from the exact bridge `y_0=Q(d)/D`, seek a rigorous restriction on `Q(d) modD^2`, equivalently `(Q(d)/D) modD`, from complete-word ownership, least-state/global structure, a second owned cut, or another exact invariant.
2. Equivalently, use the exact positive p-arc identity
   `2^uK_0=(3^p-2^u)y_0+P`
   to derive an independently anchored congruence, valuation, residue class or sharper interval for the full p-arc numerator `P`.
3. Project that new datum onto the structured RL214 candidate family. Try to delete one or more e=16 prefixes, an H21 state, a mod18 class, or the terminal rank `34,124,151,203`.
4. Exploit the arithmetic progression structure; do not enumerate 1.63 billion candidates naively when modular counting can be exact.

## Secondary target

If no independent D-adic or p-arc information can be proved, state the exact no-go scope. Only then consider signed successor-K, and only after proving an independent anchor at the e=16 terminal/successor phase (`p+50` / successor). RL209's backward-root anchor is not available there by inheritance.

## Locks

Do not treat `Q modD=0` as a quotient-residue condition.
Do not repeat the RL214 positivity cap without new information.
Do not revive the collapsed RL201 e=16 endpoint moment, saturated G56 family, flat-K normalized-gap integrality, or terminal valuation alone.
Do not promote bounded arithmetic candidates as physical words or populations.
Preserve all rank counts, RL206 corrections, below-p `37/60/97`, p-shift carry and physical/charge/Gate/global locks unless an exact new proof changes them.
