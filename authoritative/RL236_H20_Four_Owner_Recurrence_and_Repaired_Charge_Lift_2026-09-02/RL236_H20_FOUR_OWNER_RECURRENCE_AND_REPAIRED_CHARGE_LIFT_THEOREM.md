# RL236 — H20 four-owner recurrence and repaired charge lift

## 1. Incoming target

RL236 starts from the gap-free RL235 repaired charging state.  The exact successor blocker is

- `T = 5*3^35 = 250157725494998535`,
- `H = 20`,
- owners `{32,33,34,35}`,
- full-prefix core `[96166487668,98855162143]`.

RL235 showed that a chronological spacing theorem `>=185` for this exact invariant would suffice for a strict further early-band charge improvement.

## 2. Exact recurrence theorem

The inherited carry-completed universal return transport is applied only to the exact invariant above.

For every chronological self-return separation `36 <= s <= 3031`, the overlap of the full-prefix core with its translated copy is partitioned into carry-word atoms.  On every atom the exact universal return ball is disjoint from the exact terminal target.

The exhaustive certificate contains:

- `1829` carry-word atoms,
- `118` separations with nonempty translated-core overlap,
- no surviving exact return state through separation `3031`.

Therefore consecutive occurrences of this exact H20 invariant have chronological spacing

`>=3032`.

Its period occurrence cap is consequently

`floor(137528045312 / 3032) = 45358853`.

This is invariant-specific.  It is not transferred to any other H20 numerator or owner subset.

## 3. Safe repaired-charge lift

Keep the RL235 middle/late levels fixed and increase only the early-band charge.

The first previously uncapped retained cell that becomes binding is the distinct exact H20 cell

- `T = 216803362095665397`,
- `H = 20`,
- owners `{32,33,34}`,
- full-prefix cell `[62456644959,96166487667]`.

Set the early charge exactly at this cell's certified local rank-endpoint K budget divided by three.  Numerically,

`a = 6.607706073334... U`, where `U=2^-25`.

The middle/late level remains

`b=c=1.137969958290... U`.

At this charge:

- the new H20 `{32,33,34,35}` excess is paid only at its new exact cap `45358853`;
- the inherited H21 `{33,34,35}` and H21 `{34,35,36,37}` excesses are paid only at their preserved exact spacing-1001 caps;
- every other retained atomic cell is within its local certified budget;
- the distinct H20 `T=216803362095665397`, owners `{32,33,34}` cell is exactly binding and receives no occurrence assumption.

The resulting exact rational lower bound is

`ordinary absolute corrected flow > 742.087526419412... > 742`.

Hence the preserved signed-total/carry conversion gives:

- each signed corrected-flow mass `>371`;
- each directional K variation `>123`.

These remain total-variation statements, not prefix-excursion statements.

## 4. Next exact bottleneck

At the binding early charge, the remaining positive early-band slope before charging the new H20 `{32,33,34}` cell is

`1967525224`.

If further early-band improvement is attempted without changing the other bands, the exact cell

`T=216803362095665397`, `H=20`, owners `{32,33,34}`

would require occurrence count

`N <= 655841741`.

A direct spacing theorem `>=210` suffices, while `209` does not.

This is the unique immediate one-dimensional repaired-charge bottleneck.  A successor may instead seek a genuinely coupled charge/state direction, but it may not transfer recurrence caps across distinct invariants.

## 5. Closeout repair of scratch checkpoint

During RL236 scratch work a later charge `a≈7.8091U` and flow `>763` were tentatively reported.  Mandatory closeout red-team found that the distinct retained H20 `T=216803362095665397`, owners `{32,33,34}` cell would already exceed its local budget there without an invariant-specific occurrence theorem.

Therefore the later `>763` charge lift and all consequences derived from it are **withdrawn / NOT PROMOTED**.

A scratch claim of H21 `{33,34}` spacing `>=6251` is also **NOT PROMOTED** in RL236: it is unnecessary for the repaired certified result and was not independently reproduced by the final portable fast verifier.

No RL235 theorem is weakened by this closeout repair.
