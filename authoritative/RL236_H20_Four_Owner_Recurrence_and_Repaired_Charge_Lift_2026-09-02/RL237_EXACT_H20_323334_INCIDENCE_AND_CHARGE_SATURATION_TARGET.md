# RL237 target — exact H20 `{32,33,34}` incidence and repaired-charge saturation

## Mission

Continue from the repaired RL236 Success B state.

The immediate exact blocker is

- `T=216803362095665397`,
- `H=20`,
- owners `{32,33,34}`,
- full-prefix cell `[62456644959,96166487667]`.

Under the one-dimensional early-band continuation, strict improvement requires occurrence count `<=655841741`.  A direct chronological spacing theorem `>=210` suffices; `209` does not.

## Binding preserved state

- exact H20 `T=5*3^35`, owners `{32,33,34,35}` spacing `>=3032`, cap `45358853`;
- repaired ordinary absolute corrected flow `>742`;
- each signed corrected-flow mass `>371`;
- each directional K variation `>123`;
- exact RL235 7,531-cell charging state space and local K-pricing framework;
- inherited H21 exact spacing-1001 certificates used by RL236;
- sole high branch `(37,0,23,-1)`;
- K corridor `128081997553 < K < 146795909391`;
- frontier `13415865870`;
- live e=4 rank `31435476727` with `3856660232` survivors through transition 43;
- Gate A and Gate B remain open.

## Required attack

1. Reconstruct the exact H20 blocker and its full-prefix cell from the frozen state.
2. Apply exact chronological return transport through separation `209`.
3. If all returns through `209` are excluded, certify spacing `>=210`, attach the cap only to this exact invariant, and re-optimize the repaired schedule.
4. If the direct recurrence route fails, classify exact survivors and identify the smallest stronger owned-return state.
5. In parallel only if justified by exact charge geometry, test a genuinely coupled band direction; do not reuse a cap from another invariant.
6. Preserve RL236 closeout correction: the withdrawn `>763` scratch lift and unpromoted H21-6251 scratch claim are not inherited theorems.

## Success ladder

### A
The repaired charge lift plus preserved results contradicts the sole high branch.

### B
Certify the exact H20 incidence bound or an equally rigorous coupled-charge bypass, obtain a materially stronger repaired consequence, and isolate the next bottleneck.

### C
Prove a sharp exact barrier and identify the missing state without weakening RL236 coverage.

## Mandatory red teams

- necessary cells are not physical realizations;
- occurrence caps are exact-invariant specific;
- K-pricing is local budget, not incidence;
- every newly activated retained cell must be covered;
- total variation is not prefix excursion;
- chronological order is not p-rank order;
- Gate A and Gate B remain open unless separately proved.
