# RL189 certified facts and proof ledger

Date: 2026-08-30

Scope: all new statements remain internal to `(v,H,J,d)=(37,0,23,-1)` and the RL184 clean shallow-start family.

## Inherited frozen facts

- RL188 terminal-rank core: `E=[72797034370,103818202602]`.
- RL188 triple-start gaps: exactly `2^37` or `2^38`.
- RL188 consecutive extremal triple-terminal spacing: at least 41.
- RL188 `N_35<=10063027705` and at least `12146794` clean starts with `tau<=34`.
- RL187 ordinary absolute corrected-flow floor: `>2787212689/6291456>443`.
- RL183 common-mechanical affine law: `2^c delta'=3delta+epsilon`, `|epsilon|<1`.

## RL189.1 — terminal spacing at least 43

**Class:** proved analytic mathematics + exact finite integer/rational certificate.

At separation 41 the simultaneous terminal-rank overlap is exactly

`[75071400353,103818202602]`.

The co-owned clean `tau=35` corridor supplies the four post-terminal transitions needed to reach a hypothetical second triple's `tau=37` start. The exact four-bit words are only `2121` and `2122`; their affine images lie respectively within `119/64` of `3^41/2^27` and within `119/128` of `3^41/2^28`, and neither can equal `2^37` or `2^38`.

Separation 42 has empty terminal-rank overlap.

Therefore consecutive extremal triple terminals are separated by at least 43 phases.

## RL189.2 — strengthened N35 crossover

**Class:** proved analytic combinatorics + exact integer certificate.

The extremal-triple density falls to at most `3/43`, hence

`N_35<=9594979905`.

Therefore at least

`480194594`

clean starts have `tau<=34`.

## RL189.3 — two-level charging obstruction

**Class:** proved exact linear/rational obstruction.

For any monotone split assigning weight `x` to `tau<=34` and `y` to `tau=35`, the inherited height-21 family `{33,34,35}` forces

`2x+y<=2^-22`, `x>=y`.

With the strengthened `N_35` cap, the guaranteed short populations are

`a=480194594`, `b=2356661731`,

and `b-a/2=2116564434>0`. The lower-bound objective is therefore maximized at

`x=y=1/(3*2^22)`,

the old RL187 flat short weight.

Thus the strengthened crossover alone does not improve the inherited `>443` ordinary corrected-flow floor within this natural two-level monotone charging class.

## Closure status

The isolated triple, sole high branch, preferred branch, Gate A, Gate B, non-trivial-cycle exclusion, and Collatz remain open.
