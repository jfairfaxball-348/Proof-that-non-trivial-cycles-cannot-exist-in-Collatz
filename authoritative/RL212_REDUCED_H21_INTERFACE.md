# RL212 reduced H21 interface

Date: 2026-09-01. Compact successor interface.

## Constants and frontier

`A=217976794617`, `L=137528045312`, `B=80448749305`,
`p=65470613321`, `u=103768467013`, `z=L-p=72057431991`,
`Ap-uL=1`, `pB=1 modL`, `K0=2^37`.

Necessary terminal ranks remain **13,415,865,871**:
- above-p source: `7,091,831,284`;
- below-p source: `6,324,034,587`.

Global necessary eta classes remain `0,8,9,17 mod18`;
`011 <=> eta=0 mod9`, `111 <=> eta=8 mod9`.
Below-p constants remain corrected `37/60/97`.

## RL211 retained e=4 selector

A physical e=4 H21 realization must have root heights `00001`,
accelerations `1211`, `eta=207 mod243`, and state011.
No physical realization or terminal parity is asserted.

## RL212 new e=16 quotient selector

RL210 supplies p-separated height equality through e=16 and
`K_0=...=K_17=2^37`. The root recurrence has 108,950 arithmetic prefixes;
45,046 satisfy the inherited H21 mod-9 state law.

The exact quotient-state automaton is saturated on H21 classes through modulus
`3^6=729`. At the first informative level `3^7=2187`, exactly 469 of the 486
H21-compatible eta residues remain. The 17 forbidden residues are:

`[0, 53, 431, 891, 917, 972, 1160, 1295, 1458, 1493, 1565, 1620, 1701, 1862, 2060, 2088, 2106]`.

Both H21 states remain. The exact root p-gap unit condition gives one lift modulo
`3^17` for each compatible exact prefix and creates no further mod-2187 deletion.
Terminal parity remains open.

The standard H21 terminal-valuation filter is modulo `2^22` and is CRT-independent
of the mod-2187 selector. It cannot by itself delete the e=16 terminal rank
`34,124,151,203`.

## RL210 large-offset selector retained

For every above-p necessary source with `e>=56`, first p-separated mismatch m
satisfies `24<=m<=37` and `min(S_m,S'_m)=37`. At m=24 the root-smaller
orientation is excluded; the surviving orientation has `h_(p+24)=1,h_24=0`.

## Scope locks

No physical H21 incidence/charge, rank deletion, branch contradiction, Gate
closure or global nontrivial-cycle exclusion is proved. Preserve the p-shift carry,
RL206 corrections, all ownership/global locks and the RL211 flat-K denominator
barrier. A successor must use an independent absolute/complete-word consumer, not
the terminal 2-adic test alone.
