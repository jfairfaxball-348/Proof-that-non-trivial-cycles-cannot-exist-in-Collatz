# RL190 target — phase-43 boundary seam and phase-resolved early-defect charging

Date: 2026-08-30

## Authoritative objective

Continue the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from RL189.

RL189 proves:

- consecutive extremal `{35,36,37}` triple terminals are separated by at least 43 phases;
- `N_35<=9594979905`;
- at least `480194594` clean starts have `tau<=34`;
- a monotone two-level `tau<=34` / `tau=35` charge split cannot improve the RL187 `>443` ordinary corrected-flow floor because the height-21 family `{33,34,35}` forces the flat optimum.

## Preferred theorem

Attack the first unresolved spacing-43 seam.

If terminals occur at `t` and `t+43`, the later `tau=37` start is at `t+6`. RL189's clean `tau=35` boundary argument is no longer sufficient by the same indexing alone. Seek, in order:

1. an exactly owned additional physical transition/corridor that reaches `t+6`;
2. a terminal-rank or mechanical-phase refinement excluding the separation-43 overlap;
3. a sign/phase affine obstruction at the boundary;
4. a phase-resolved height-21 ownership theorem that separates `{33,34}` from `35` strongly enough to improve charging.

A valid charging advance must certify physical safety against all co-owned terminal families and must recompute the guaranteed corrected-flow floor exactly.

## Do not

- assume a `tau<=34` companion corridor without proving ownership/cleanliness;
- treat the necessary terminal-rank core as an existence set;
- replace physical `2^G` corrected flow by the auxiliary RL173 `3^-G` quantity;
- turn total variation into chronological K excursion without a sign-order theorem;
- claim isolated-triple, branch, Gate A/B, non-trivial-cycle, or Collatz closure without a genuine contradiction.
