# RL349 target — AGGRESSIVE ONE-SESSION PHASE-4 CLOSURE

Status: UNIQUE LIVE TARGET FOR RL349; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent
`(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited
ownership/pruning, live high-carry scope where required, and external conditional least-state floor
`m>=2^71`.

## Absolute objective

**Close Phase 4 in this research session.**

Prove

`O_75 = empty`.

Nothing weaker counts as success.

Do not begin Phase 5, R1 post-Phase-4 assembly, Gate A, Gate B, R2 or any later roadmap stage.

## Session-level execution mandate

RL348 accumulated enough contractions. RL349 is not a further contraction session.

Work aggressively toward elimination of the complete residual, consuming all three classes in one
integrated proof programme:

- `75<=L<ell`;
- `L=ell`;
- `ell<L<=251920108044`.

Do not schedule a future RL merely to improve another local constant, shave another decoder depth,
or narrow another endpoint window. A new local lemma is useful only insofar as it is immediately
consumed into class elimination.

The binding connector resumability protocol remains in force: bounded continuation work units may
be used to avoid technical stalls. However, internal/user-facing checkpoints should correspond to:
- elimination of an entire residual class;
- a theorem merging two or three residual classes into one closure mechanism;
- a material correction/demotion;
- or a decisive proved architecture barrier showing why the present exact interface cannot close.

A minor numerical tightening is not a sufficient research checkpoint for RL349.

## Mandatory inherited starting point

Read:
- `START_HERE.md`;
- `RL348_PROOF_LEDGER.md`;
- `RL348_EXACT_CERTIFICATE.md`;
- `RL348_CORRECTION_AND_DEMOTION_LEDGER.md`;
- `RL348_SCRATCH_FRONTIER.md`;
- `RL348_GLOBAL_PROOF_ROADMAP_STATUS.md`;
- `RL348_SESSION_STATE_AND_RL349_KICKOFF.md`;
- this target.

Then use RL347/RL346/RL345/RL344/RL343 exact authority only as needed.

Promoted RL348 facts include:
1. universal terminal law:
   `g_1=1`, `q_(L-1)=1`, `1<=c_end<=80448749305`, endpoint `==3 mod 4`;
2. half-cycle parity mismatch within at most 36 ordinary phases;
3. over-half q=0 support is entirely strict late-row;
4. its complement is one strict-late interval containing `k`;
5. `R>=23135982580`, `L<=251920108044`;
6. if the complement begins on/before `j`, `R>=61170756170`.

Read the correction ledger before using RL348 live scratch. In particular, do not assume
`D<=2^36-2`, do not turn contact preservation into q=0 preservation, and do not use an unverified
source-side residual inequality without rederiving its orientation.

## Closure attack order

### A. Eliminate the half-cycle class, not merely tighten it

Use simultaneously:
- matched contact endpoints;
- same decoration and `H=a`;
- `2<=D<=2^36`, even;
- endpoint terminal gap one and `c<=a-ell`;
- forced parity mismatch within 36 binary phases;
- RL345/RL346 bounded predecessor/successor signatures;
- exact mixed 2/3-adic endpoint acceptance;
- genuine proper-factor ownership identities.

The desired output is a direct contradiction or deterministic descent for every half-cycle
candidate.

Do not import historical height-one exclusions unless their exact hypotheses are first proved for
the live object.

### B. Close the over-half class by crossing split

For `L>ell`, use the strict-late complement interval `[s,e]` containing `k`.

If `s<=j`, exploit the very long forced crossing-to-root content
`R>=61170756170` together with the ownership telescope/phase-potential geometry; do not settle for
another lower bound on `R`.

If `j<s<=k`, rederive the source-oriented matched-factor split once, then combine:
- `k-s>=23135982580`;
- post-crossing `1/lambda<h<1`;
- genuine numerator identity `A=D0P-Xe`;
- exact phase-potential nondecrease;
- bounded incoming source signature;
- terminal law;
- predecessor/successor mixed-adic acceptance.

The desired output is a source-side skip theorem that rejects the entire post-crossing branch.

### C. Consume `75<=L<ell` through the deterministic decoder

This class must not be attacked by length enumeration.

Use:
- exact decorated endpoints -> one physical length;
- forced exact total gap;
- deterministic inverse-carry word reconstruction;
- RL345 23/72 singleton interface;
- RL346 bounded incoming signatures;
- RL348 terminal gap/tag law;
- predecessor 2-adic and successor 3-adic CRT;
- q-record/profile test;
- row/contact/wrap acceptance;
- ownership/pruning;
- phase-potential sign;
- deterministic least-state descent.

Seek an all-length rejection/ranking theorem over the decoder. If the three classes can be merged
earlier by a stronger physical theorem, do that instead.

## Mandatory acceptance tests

No closure is valid without all applicable:
- predecessor mixed 2-adic congruence and oddness;
- successor mixed 3-adic congruence;
- exact q-record/profile conditions;
- source/end row-phase decoration;
- early-row q=0 contact law;
- genuine two-row wrap orientation;
- physical ownership/pruning;
- exact phase-potential sign;
- deterministic descent below `2^71` where used.

## Hard prohibitions

Do not:
- work on Phase 5;
- restart total-gap (`G_72`) enumeration;
- use the invalid RL346 `G_72=76` scratch enumeration;
- restart sigma/coefficient/q-upgrade/old linear-consumer ladders;
- enumerate `L` one-by-one;
- silently promote RL348 scratch;
- use RL140--RL147 historical conclusions outside their exact hypotheses;
- call Phase 4 closed without a gap-free `O_75=empty` theorem.

## Stop rule

The successful endpoint is exactly:

`O_75=empty`.

Then Phase 4 is CLOSED. Enter closeout immediately and do not perform Phase-5 mathematics.
