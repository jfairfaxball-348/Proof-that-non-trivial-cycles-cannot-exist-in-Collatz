# RL154 red-team report

- The transfer equations retain the ordinary affine `+1` numerator.
- The prefix/suffix residues are necessary consequences only, not cycle
  constructions.
- The terminal-height witness is explicitly a local grammar witness, never a
  physical cycle.
- No `g>1` factor argument is applied at `g=1`.
- No arbitrary finite cutoff is promoted.

