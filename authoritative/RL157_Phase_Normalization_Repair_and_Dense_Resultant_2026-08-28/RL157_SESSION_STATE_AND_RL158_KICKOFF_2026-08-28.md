# RL157 session state / RL158 kickoff

RL157 corrects the phase polynomial to `2T^L-1`. The live object is `P_h(T)=sum_(j<L) 2^(H-h_j)T^(Aj mod L)`, whose nonzero resultant with `2T^L-1` is divisible by the full modulus `D`. RL158 must seek an arithmetic discriminator of that resultant or an explicitly new branch resource. Do not revive the demoted `3T^L-2` formula, sparse phase, `F_1`, individual-level ownership, a small-prime sieve, or the legacy `g=2` one-excursion resultant route.
