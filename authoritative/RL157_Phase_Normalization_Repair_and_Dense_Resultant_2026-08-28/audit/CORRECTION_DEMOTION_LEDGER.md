# Correction / demotion ledger through RL156

- RL156: **notation-only correction**. RL155 overloaded `m` for both the
  least odd state and a Bézout coefficient. The state is now `M` and the
  Bézout coefficient is `u` in `Ap-uL=1`. No equation, certificate, scope,
  or conclusion is demoted.
- RL157: **formula demotion and repair**. RL155/RL156's `3T^L-2` reduction
  and `2^h_j3^(H-h_j)` coefficient conflict with `rho^L=1/2`. They are
  replaced by `2T^L-1` and `2^(H-h_j)`. The phase/numerator equivalence and
  dense one-per-residue conclusion survive; no cycle result is demoted.
