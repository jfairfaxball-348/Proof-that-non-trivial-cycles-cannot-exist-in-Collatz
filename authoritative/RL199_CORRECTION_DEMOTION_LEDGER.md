# RL199 Correction / Demotion Ledger

Date: 2026-08-31.

No inherited theorem or exact finite certificate is demoted.

Scope locks:

- `eta` parameterizes an assumed physical H21 co-owner. The arithmetic survival of an `eta`
  residue is not a claim that a global physical cycle realizes it.
- `011 <=> eta=0 mod 9` and `111 <=> eta=8 mod 9` are necessary within the inherited H21
  co-owner hypotheses. RL199 does not prove either state occurs.
- The mod-18 table determines terminal orientation **conditional on `eta`**; it does not supply
  the missing absolute value of `eta mod 18`.
- The Hensel filter removes one sparse lift residue within each mod-18 class. It does not exclude
  any of the four state/sign classes.
- The information-loss theorem is limited to consumers factoring through the listed
  `tau=34`-or-later unoriented common heights, common pair numerators and mechanical bits.
  Complete-word, absolute endpoint, signed K/rho, phase/prehistory and other oriented constraints
  are not declared blind.
- The signed K increment is a bridge identity, not a variation-to-excursion inference or a
  certified sign contradiction.
- Necessary H21 terminal ranks remain necessary ranks, not physical populations.
- All prior finite-cutoff, canonical-lift, physical-versus-necessary, rank-versus-time and
  variation-versus-excursion guardrails remain in force.
