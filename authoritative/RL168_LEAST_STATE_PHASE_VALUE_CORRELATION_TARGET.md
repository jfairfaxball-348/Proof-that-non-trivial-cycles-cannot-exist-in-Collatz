# RL168 — least-state phase-value correlation target

RL167 shows that all full affine phase closures are one condition.  Seek a
new quantitative theorem coupling the actual ordered values
`y_j=N_j/(2^A-3^L)` to the defect heights `h_j`, beyond the local recurrence,
the one-sided product/population bounds, and the dense closure congruence.

The theorem must use genuine least-state ordering and remain in the inherited
coprime `g=1`, ordinary-`+1`, physical-state first-survivor scope.  Do not
reopen fixed-radius, bare inverse-cylinder, count-only, product-only,
phase-order, or same-root resultant/SNF routes.
