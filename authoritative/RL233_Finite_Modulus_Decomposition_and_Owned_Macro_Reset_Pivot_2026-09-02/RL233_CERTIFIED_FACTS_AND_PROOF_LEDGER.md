# RL233 certified facts and proof ledger

Date: 2026-09-02

## Promoted analytic mathematics

1. **Owned-return concatenation telescope.** For every legal owned path, `Q_n=3^(n-k)Q_k+2^D_k T_(k,n)` with the definitions in the main report.
2. **Dyadic quotient-rebasing theorem.** Prefix-forced powers of two do not create an independent endpoint digit; after division the identical suffix-return problem is re-based at the exact current owned state.
3. **3-adic finite source-term memory theorem.** At precision `3^b`, after at least `b` transitions the explicit source-numerator term vanishes and the residue is determined by the final `b` transition labels. This is residue-state memory only; legal labels may carry extra history.
4. **Unit-modulus reversibility theorem.** For `gcd(q,6)=1`, fixed legal transition labels induce an affine bijection of numerator residues modulo q. Bare q-residues have no intrinsic depletion/reset-cost semantics.
5. **Complete finite-modulus decomposition.** By CRT, every bare finite endpoint modulus is a product of the preceding three behaviours. Modulus escalation alone cannot manufacture the missing H17 macro reset cost.
6. **Scale consequence.** A standalone 3-adic source-term coordinate intended to retain source information across the spacing-only scale sufficient for `N17<=1615` requires `b>=85,103,989`.

## Exact finite regression certificate

The portable verifier exhaustively replays all RL232 depth-three owned-pair necessary paths:

- A: 14,244 paths;
- B: 16,976 paths;
- total: 31,220 paths.

It checks the exact telescope and the three modular components on every path. This is regression support, not the logical basis of the arbitrary-depth analytic theorem.

## Method barrier / route retirement

Retired as a primary route at current scope: treating a larger finite endpoint modulus, successive dyadic quotient digits, a standalone finite 3-adic residue, or a bare prime-to-6 endpoint residue as the consumed long-range H17 resource.

Still live: a separately restricted physical legal-tail automaton or a noncongruential owned macro resource.

## Classification lock

- Necessary residue/path states are not physical realizations.
- Reversibility modulo q is conditional on fixed legal labels and does not prove residue saturation over the physical language.
- 3-adic finite memory concerns the standalone residue coordinate; it does not assert that the final transition labels are source-independent.
- The `b>=85,103,989` threshold is a scale requirement for a standalone source-memory coordinate, not a proof that such a huge modulus would close H17 incidence.
- No new terminal rank is deleted.
- No H17 incidence `<=1,615` theorem is claimed.
- No sole-high-branch contradiction is claimed.
- Gate A and Gate B remain open.
- Global non-trivial-cycle exclusion remains open.

## Session classification

RL233: **Success C** under the exact RL233 success ladder.

Successor: `RL234_OWNED_MACRO_RESET_COST_AND_LEGAL_TAIL_CONSUMER_TARGET.md`.
