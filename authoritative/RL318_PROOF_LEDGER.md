# RL318 proof ledger — external-frontier contraction and first-survivor splice audit

Date: 2026-09-14
Status: FROZEN WITH RL318 CLOSEOUT

## A. Promoted external finite computational dependency

RL318 freezes the peer-reviewed Barina convergence verification through `2^71` as an **external finite computational certificate dependency**.  The exact provenance and scope are recorded in `RL318_EXTERNAL_2P71_DEPENDENCY.md`.

The external exhaustive computation is not rerun.  Only its exact finite statement is consumed:

`every positive n < 2^71 converges to 1`

for the same shortcut Collatz map used in this repository.

## B. Exact conditional frontier contraction

Using the frozen external floor together with the inherited RL131 continued-fraction consumer and RL315 reduced-shadow transfer gives

`boxed: ell >= 49,547,666,544`

for every `g>1` survivor, conditional on the external dependency.

This is much stronger than the internal-only reduced frontier

`ell >= 190,537`.

RL317's first internal fibre is eliminated immediately because its exact state ceiling

`1,311,372,708,449`

is strictly below

`2^71 = 2,361,183,241,434,822,606,848`.

Thus the external splice removes a huge contiguous reduced-count range; it does not merely kill the single RL317 first fibre.

## C. First conditional survivor

The inherited exact continued-fraction chain places the first above-resonance survivor beyond the `2^71` denominator wall at

`boxed: (a,ell)=(217,976,794,617, 137,528,045,312)`.

The previous convergent beyond the denominator wall is below resonance, so it cannot have `2^a>3^ell`.

This pair is the historical RL132 first survivor.  RL318 therefore does not restart bare CF, residue-prefix, or generic count/gcd attacks at this scale.

## D. Exact generic-packing red team

The RL318 consumer verifier combines the rigorous logarithm interval for the survivor with the external floor `R0=2^71` and the RL310 generic packing inequality.

Using `log(1+x)>=x/(1+x)`, the packing right-hand side is rigorously more than `21.6` times the required `2 Delta` left-hand side.

Therefore generic RL310 physical segment packing does not eliminate the first external survivor.  This is a method barrier, not evidence for a cycle.

## E. Historical first-survivor structure recovered for reuse

The following already-promoted results at the same pair are explicitly reactivated as available resources:

1. RL134/RL135: for `g<=6`, every proper physical prefix has nonnegative mechanical defect `h_j>=0`.
2. RL135: for `g<=6`, the global least odd state satisfies `m<2^75`.
3. RL136/RL137: broader defect/excursion and no-negative branch state ceilings exist, but they do not by themselves exclude a multiplicity.
4. RL139--RL142: ordinary full-ownership contact-polynomial obstructions exclude compressed, one-deviant, bounded consecutive, and cyclic bounded-interface height-one patterns, but do not cover arbitrary `g=2` contact geometry.

These results predate RL316--RL317's dual-shadow factorization and remain legitimate splice inputs.

## F. New scratch observations preserved but NOT promoted as closure theorems

1. **Cross-content sign reversal.**  In the RL317 nonzero-residue branch, after content reduction the content-`h` physical copy and the coprime-content `T_h` shadow have boundary separations `+E` at the lower boundary and `-E` at the upper boundary.  Hence their relative order must cross within the row.

2. **First-crossing orientation.**  Same-parity steps only rescale the signed gap.  An initial physical-odd/shadow-even mismatch does not produce the required first sign crossing; a later reverse mismatch is necessary.  This did not yield a contradiction and is retained only as structural scratch.

3. **Row affine carry trichotomy.**  For `epsilon=kH+r`, `0<r<H`, with `M=R-k`, `N=x+k`,

   `F_tau(M)=N+r/X`,
   `F_sigma(N)=M-r/X`.

   Since `0<r<X+Y<2X`, the row endpoint rounding defect has only the one/two-unit trichotomy `r<X`, `r=X`, `r>X`.  Actual integer trajectories cease to follow the prescribed shadow after the mismatch clock, so these rounded affine endpoints cannot be identified with physical endpoints without a new theorem.

4. **First-survivor cut-local mismatch idea.**  If one could identify an RL135 canonical contact with the RL317 balanced-row cut, the RL135 state window would force the physical boundary gap below `2^35`, hence force the two physical rows to disagree within the first 35 half-steps.  RL318 did **not** prove that cut identification.  This implication is therefore conditional only and must not be used as a live theorem.

## G. Explicit correction / rejected splice

RL318 briefly attempted to combine the global least-state bound `m<2^75` with the arbitrary universal balanced cut from RL315/RL317.  This is not justified: the balanced cut is existential between genuine rotations, but is not automatically the minimum-root canonical cut used by RL135.

Accordingly:

- no unconditional `G<2^35` theorem is promoted for the RL317 rows;
- no unconditional early-35-bit certificate is promoted;
- no least-state prefix congruence is transferred to that arbitrary cut.

This correction is binding for successors.

## H. Surviving RL317 branches

RL318 does not close either exact `g=2` branch:

1. `epsilon=0`: ordered distinct physical rows with genuine `D0` ownership;
2. `epsilon>0`, `H not| epsilon`: non-homogeneous content-`h` / coprime-content `T_h` pair.

The direct order-crossing argument in branch 2 is insufficient by itself.  The historical contact-polynomial family does not automatically cover multiplicity two.  Generic packing also fails at the first external survivor.

## I. Successor direction

The safest next splice is **cut-safe**:

- work with the full `g=2` ordinary cycle rooted at its true least state;
- use RL135.2's global `h_j>=0` theorem at the first external survivor;
- combine that full-word nonnegative-defect geometry with RL316--RL317's genuine full-`D` ownership identities;
- only transfer a bound or contact statement to a balanced row cut after proving the cut-identification/transport lemma explicitly.

A second legitimate route is to derive a truly non-homogeneous signed-gap invariant for the content/coprime-content pair that survives the RL79 homogeneous barrier.

## J. Scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
`g=1` remains separate.
Internal-only and external-conditional frontiers remain explicitly distinguished.

`PARENT_DIFFICULTY_DELTA = EASIER`

Reason: the external `2^71` splice conditionally moves the reduced frontier from `190,537` to `49,547,666,544` and identifies a single first external survivor scale, even though the local `g=2` branch itself remains unclosed.
