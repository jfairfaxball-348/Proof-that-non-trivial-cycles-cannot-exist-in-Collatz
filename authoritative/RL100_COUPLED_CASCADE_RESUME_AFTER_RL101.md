# RL100 — exact coupled-cascade resume point after RL101

Date: 2026-08-25

## Status

The numerical cascade is **paused, not abandoned**.  RL101 is assigned to the higher-leverage raw-multiplicity/cylinder ownership theorem first.

This file preserves the exact repaired restart state.

## 1. Frozen branch scope

The cascade applies only to the

**exact first-Farey reduced slope, raw `g=1`, full-phase Gate-A branch**.

Do not promote its interval exclusions to global Gate A without a separate multiplicity/Farey lifting theorem.

## 2. Exact tier state

- wide `(D_w,R_w,L_w)=(5,000,030,7,000,000,5,000,053)`;
- middle `(D_m,R_m,L_m)=(7,500,031,4,500,000,7,500,053)`;
- deep `(D_d,R_d,L_d)=(10,000,032,3,250,000,10,000,053)`;
- ultra `(D_u,R_u,L_u)=(15,000,035,2,005,000,15,000,053)`.

Middle was repaired in RL100 after the exact new balanced-residue minimum

`7,500,033 at r=4,478,962`.

The other inherited global minima remain

- deep `10,000,034 at r=378,722`;
- ultra `15,000,037 at r=575,974`, with an RL100 tie at `r=1,919,735`.

## 3. Final geometry

Corners:

`A=(0,7,000,001)`

`B=(5,000,031,4,500,001)`

`C=(7,500,032,3,250,001)`

`D=(10,000,033,2,005,001)`

`E=(15,000,036,0)`.

Feasible exact nonnegative supports:

- `A-C`;
- `C-D`;
- `D-E`.

Active terminal support:

`lambda=234375/3281264468752`,

`mu=1/7000001`.

`floor(lambda*C)=3010>2`.

Common successor charge:

`L_common=15,000,053`.

## 4. Live terminal

First live odd:

`k=2,921,813,805`.

Exact margin there:

`-3,297`.

Previous odd:

`2,921,813,803`, margin `+10,323`.

Branch upper endpoint:

`42,150,931,559`, odd.

## 5. Scan coverage already completed

Do not rescan unless a verifier/provenance problem requires it:

- ultra through `r=2,005,000` modulo `2^15,000,056`;
- middle through `r=4,500,000` modulo `2^7,500,056`;
- deep through `r=3,250,000` modulo `2^10,000,056`.

No gap may be skipped on resumption.

## 6. Resumption procedure

When deliberately resuming:

1. choose the next deep radius checkpoint;
2. solve the exact staircase support thresholds for the middle/ultra radii using the **repaired** `D_m=7,500,031`;
3. extend every required 2-adic band gap-free;
4. compare every completed band minimum with the live global minimum immediately;
5. if a new minimum appears, repair the relevant depth before geometry promotion;
6. rebuild all corners from actual depths/radii;
7. enumerate every feasible pair support exactly;
8. verify the terminal-direction optimal edge;
9. verify `floor(lambda*C)>2` or redo block-count optimization;
10. propagate only the consecutive odd interval for which the exact margin is positive.

## 7. Strategic warning

RL100 added 250,000 deep-radius units and eliminated 6,141 further odd terminals.  This confirms continued local productivity but poor global economics.

Do not run long blind scans unless the structural programme has again hit a well-recorded barrier or a new theorem materially accelerates the cascade.
