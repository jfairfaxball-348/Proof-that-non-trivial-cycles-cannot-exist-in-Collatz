# RL294 — first-d=1 minimum ownership and tight P-source static danger-tree Gate-A target

Date prepared: 2026-09-10
Status: PREPARED, NOT STARTED

## Incoming classification

RL293 closes as

`FIXED_SEED_MINPLUS_OWNER_DANGER_TREE_REDUCTION_AND_K29_CONTRACTION_PROVED`.

Gate A remains open with exact residual

`k>=31`, `k` odd, `H_can<k`.

Gate B remains separate/open/frozen. Fifth selector remains unscanned. Radius 6+ remains frozen. No global non-trivial-cycle exclusion is claimed.

## Why this target is next

RL293 proves that the infinite boundary-danger problem is best attacked after minimum historical ownership and first-boundary compression, not by finite shifted valuations or raw height caps.

Three new facts are load-bearing:

1. every RL292 static hazard ball pulls back isometrically through a fixed legal segment, so local phase precision is not lost;
2. a hypothetical dangerous first checkpoint must have high signed boundary drift and, from historical height nine onward, must start at the least representative of its own parity cylinder rather than an arbitrary higher lift;
3. through historical height 60, every genuine first positive `d=1` state is minimum-owned by `P=(d,J)=(2,3)` in the sense `m_P(B)<=H(B)-1`.

RL293 also closes `k=29` using a compact first-checkpoint splice plus an exact P-source certificate. Do not simply extend that large source cap to `k=31` as the principal programme.

## Primary theorem target A — all-depth first-d=1 owner theorem

Prove, for every genuine fixed-seed trajectory, if `B` is its first positive `d=1` physical state at historical height `H(B)`, then

`boxed: m_P(B)<=H(B)-1`,

where `P=(2,3)` and `m_P(B)` is the minimum positive cost from P to the identical physical state B.

Equivalent first-checkpoint or cutset formulations are acceptable if they are strong enough to splice every later positive future without increasing historical cost.

The exact H<=60 certificate is evidence/certificate for the finite range, not an all-depth proof. Its only relaxed exceptions are `J=2,3,6`, all repaired by genuine fixed-seed lower bounds.

If the theorem is false, find the first exact genuine counterexample and identify the minimal additional ancestry label needed for a correct finite-state/min-plus owner relation. Do not replace it with an unrelated scalar potential.

## Primary theorem target B — collapse the Bellman front door to P

RL293 proves the conditional reduction:

if each first positive `d=1` state on a front-door future is P-owned within the relevant historical credit, then

`Bcal(s)<=Bcal(P)+T_s-1`.

Therefore an all-depth owner theorem should reduce RL292's five inequalities to the single tight obligation

`boxed: Bcal(2,3)<=1`.

Make this implication explicit and verify every front-door threshold. Do not assume the reduction before the owner hypothesis is proved on the needed scope.

## Tight P-source danger-tree attack

Use RL292's static boundary hazard

`Beta(n)=sup_(w,alpha)[nu_2(n-rho_(w,alpha))-|w|]`,

with roots `alpha=-1,-2/3`, together with RL293's new restrictions.

For a hypothetical first checkpoint failure at historical height H after first positivity at H0:

- normalized-K corridor:
  `J_b+1 <= 3(3/2)^(H-H0)`;
- loop-erased boundary word with r one-steps and z zero-steps must satisfy
  `r-z+2 > H0 + lambda H`,
  `lambda=log_(3/2)(4/3)`;
- for `H>=9`, the boundary origin is the principal parity-cylinder representative `n_b=zeta_w`;
- the endpoint code satisfies `0<=e_w<3^r`;
- terminal hazard `2^k` forces either
  `e_w=2^k-1` (`alpha=-1`) or
  `e_w=(2^k-2)/3` (`alpha=-2/3`).

The `alpha=-2/3` endpoint is exactly the labelled height-one predecessor ray. Retaining instead of taking the hazardous exit gives the deterministic zero-height reduction `01 0^(R-2)` to checkpoint `q+1` when `3e+2=2^R q`.

Investigate the two roots separately if useful. The missing theorem must price fixed-seed ancestry/minimum ownership; numerical predecessor equality is not reverse reachability.

## Checkpoint-8 subbranch

RL293 proves the checkpoint-8 excess-one bound on the complete nonempty balanced region `A<=7` and proves the entire area-two equality class. The first unresolved checkpoint-8 counterexample, if any, has added area at least eight.

Checkpoint `8` is not a universal gateway. Use it only as a sharp local Bellman branch or equality model.

## Mandatory red teams

Any proposed all-depth owner/danger theorem must explicitly survive:

1. arbitrary positive zero-height boundary retention and the positive trivial cycle;
2. RL292 all-zero static-tree pullbacks with unbounded Beta;
3. RL287 cylinder isometry and RL289 finite-suffix transparency;
4. neutral `(101)` fixed-seed gauge;
5. physical branch mergers/equal-area diamonds;
6. RL283 adjacent-swap valuation jumps;
7. homogeneous `(010)^R` Rank-1 danger towers;
8. shifted phase-reset family and genuine `10922->6146`;
9. RL290 chains `2514->5378->21842->8192` and `1074->1364`;
10. locally legal terminal predecessor families which are not globally reachable;
11. the RL293 immediate-danger family excluded by the normalized-K corridor;
12. the three relaxed H<=60 owner exceptions `J=2,3,6` and their genuine repairs;
13. principal-cylinder reduction scope: it applies to dangerous first-checkpoint geometry, not arbitrary later boundary visits.

## Forbidden principal routes

Do not restart as the principal programme:

- finite shifted-valuation/affine-template maxima;
- endpoint-only locally bounded 2-adic potentials;
- support-count or excursion-count charging;
- per-cell valuation Lipschitz induction;
- chosen-history seam charges;
- raw larger height-cap expansion;
- generic Collatz-orbit termination assumptions;
- Gate B, fifth selector, or Radius 6+.

Finite computation is acceptable as a falsification oracle, compact cutset certificate, or regression for an analytic reduction. A k=31 finite contraction may be recorded if it comes cheaply from a new theorem-sized compression, but it should not displace the all-depth owner/P-source objective.

## Success condition

RL294 succeeds strongly if it proves any of:

- the all-depth first-d=1 P-owner theorem;
- an exact finite augmentation of P-ownership that covers all genuine first-positive d=1 states;
- collapse of the five-state front door to `Bcal(2,3)<=1`;
- `Bcal(2,3)<=1` itself;
- a static principal-cylinder/high-drift separation theorem strong enough to imply Gate A;
- the global checkpoint theorem `nu_2(J)<=H`;
- `Bcal(1,-13)<=0`.

If none closes, freeze the first exact obstruction with its minimal ancestry coordinate and leave a single precise successor target.
