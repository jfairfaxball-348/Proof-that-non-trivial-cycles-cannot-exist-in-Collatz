# RL234 verification

Portable fast suite:

```sh
bash run_fast_rl234_verifiers.sh
```

The charging-gap verifier uses Python standard-library integer/Fraction arithmetic only. It reconstructs the exact retained H20 cell and proves K-corridor compatibility with rational log/exp enclosures.
