# RL232 — H17 cross-family state barriers and owned long-return pivot

Date: 2026-09-02

## 0. Outcome and scope

RL232 continues the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from the verified RL231 state.

**Outcome: Success C.**

RL232 does not prove the desired combined H17 incidence bound `N17<=1,615`. Instead it closes the exact target's pivot criterion: progressively richer cross-family boundary states still permit necessary-state incidence far above 1,615, and the first missing information is isolated as a genuinely long-range owned endpoint/return-defect or macro-reset invariant coupling one H17 terminal to the next.

No necessary terminal rank is deleted. The frozen frontier remains `13,415,865,870`; e=4 terminal rank `31,435,476,727` remains live with `3,856,660,232` combined arithmetic survivors through transition 43. The sole high branch, Gate A, Gate B, and global non-trivial-cycle exclusion remain open.

## 1. Exact combined H17 return exclusion through separation 1000

RL231 resolved H17 into two exact maximal families:

- H17-A offsets `{26,...,32}`, `(T,H)=(11*3^32,17)`, raw terminal core `[5,377,348,952,28,746,802,249]`;
- H17-B offsets `{27,...,33}`, `(T,H)=(5*3^33,17)`, raw terminal core `[72,797,034,370,103,818,202,602]`.

Using the inherited carry-completed universal gap transport and exact mechanical atoms, RL232 checks all four directed return classes jointly through terminal separation 1000:

| edge | atoms | nonempty separations | minimum safety margin |
|---|---:|---:|---:|
| A→A | 13,823 | 329 | `>8,249,443` |
| A→B | 18,149 | 383 | `>85,336,126` |
| B→A | 18,241 | 383 | `>77,331,112` |
| B→B | 24,179 | 437 | `>7,499,467` |

There are no universal-return survivors. Hence the **combined chronological H17 event sequence has spacing at least 1001**.

Classification: exact finite certificate supporting a conditional chronological non-recurrence theorem. This is not an incidence theorem at the required global scale: `floor(L/1001)=137,390,654 >> 1,615`.

## 2. Exact H17 K-compatible terminal cores

For a fixed terminal invariant, the inherited exact rank/K formula makes terminal K strictly decreasing with mechanical terminal rank. Exact rational logarithm/exponential bounds cut the raw H17 cores to the necessary physical K corridor:

- H17-A: `[11,443,822,977,28,746,802,249]`;
- H17-B: `[72,981,981,437,100,039,806,527]`.

Moreover every H17-A terminal in its K-compatible core satisfies

`K > 134,536,495,104`.

Classification: analytic consequence of inherited rank/K monotonicity plus exact rational boundary certificate.

No rank in either interval is asserted physically realized.

## 3. Relaxed cross-return graph barrier

The universal carry-completed error-ball state admits a concrete relaxed H17-B→H17-B return with:

- chronological separation `190,537`;
- terminal-rank displacement `-12,799`;
- zero-error target miss `<16,000`;
- universal relaxed radius `>47,626`.

This edge can be chained `721,792` relaxed events in less than one period while staying in the K-compatible rank core at terminal endpoints. This is not a physical orbit: the relaxed error variables are not required to arise from a single physical height sequence.

Adding full-period scalar K motion excludes that near-full chain, but still fails decisively. A shorter chain with `655,001` relaxed H17-B events lies entirely in the exact B K-compatible rank core and leaves enough remaining scalar K-motion capacity to meet the full-period endpoint requirement.

Classification: exact method barrier. It proves that family/rank/K plus generic per-phase scalar speed and monodromy are insufficient constraints; it does not prove existence of a physical high-incidence chain.

## 4. Owned H17 local prefix graph

RL232 specializes the inherited exact owned-pair transition law to H17.

After the K-core cuts, the mechanical words are common across the whole relevant cores:

- H17-A first four bits: `1212`;
- H17-B first three bits: `212`.

The complete terminal height-pair domain has 34 ordered unequal pairs with maximum height 17.

Exact depth-three necessary-state counts are:

| family | depth 1 | depth 2 | depth 3 |
|---|---:|---:|---:|
| A states | 329 | 2,301 | 10,451 |
| A edges | 329 | 2,555 | 12,436 |
| B states | 370 | 2,349 | 12,471 |
| B edges | 370 | 2,595 | 14,904 |

Every one of the 34 A input pairs and every one of the 34 B input pairs has a necessary survivor through depth three.

Two local physical restrictions are nevertheless proved:

1. H17-A can have an immediate postterminal zero defect only from terminal difference `g=-1`, and four consecutive postterminal zero defects are impossible.
2. H17-B can have an immediate postterminal zero defect only from terminal difference `g=+2`, through one exact interface, and two consecutive postterminal zero defects are impossible.

Classification: analytic local zero-interface consequences plus exact finite necessary-state certificate; also a method barrier because all 34 terminal pair classes survive depth three.

## 5. Positive moving-window saturation barrier

The inherited physical window identity is

`3K_i = alpha C_i + beta Y_i`,

with `0<C_i<Y_i` and positive coefficients.

RL232 tests whether carrying `(C,Y)` adds the missing long-range scalar state. It does not.

The inherited canonical full-period mass enclosure contains the algebraic witness

`Y_0=74,000,000,000`.

Exact outward-rounded bounds show that, throughout the corresponding admissible Y-band, **every K in the full inherited K corridor has a compatible positive C with `C<Y`**. A conservative compatible range spans roughly

`4.45e9 < C < 6.06e10`.

This is an algebraic necessary-state witness, not a physical q-sequence. Its purpose is to prove that scalar window positivity alone cannot refine the K corridor.

On an H17 zero-defect prefix the exact relation gives

`C_(i+1)-C_i = -(1-alpha^-1) q_i`.

Across either complete H17 zero prefix, the total internal C drop is strictly less than `2^-32`, microscopic compared with the scalar admissible C reservoir.

Finally, disjoint first-defect blocks alone give at best the uniform combined packing cap

`floor(L/33)=4,167,516,524`,

still about 2.58 million times the required `1,615`.

Classification: analytic scale consequence + exact rational barrier certificate.

## 6. Success-C conclusion and pivot

The available states tested in RL232 are now:

`family`
→ `cross-family terminal rank`
→ `K`
→ `generic scalar flow/monodromy`
→ `owned local height/numerator prefix`
→ `positive scalar moving windows`.

They do not deliver `N17<=1,615`; exact necessary-state witnesses remain vastly above the threshold.

The missing object is therefore not another modest recurrence cutoff or another scalar window. The successor should attack an **owned long-return endpoint defect**, higher owned quotient/modulus state (for example modulo-`D^2` where legitimately derived), or an equivalent macro reset cost that must be transported from one H17 terminal to the next.

This is the RL227 F1-style fallback. Raw additive recurrence residues, endpoint-oblivious scalars, and local-prefix deepening without a new cross-return state are retired as primary RL233 routes at the present scope.

## 7. Preserved open state

Unchanged:

- necessary terminal-rank frontier `13,415,865,870`;
- e=4 terminal rank `31,435,476,727` live;
- e=4 combined arithmetic survivors through transition 43 `3,856,660,232`;
- combined physical H17 incidence `<=1,615`: **not proved**;
- sole high branch: **open**;
- Gate A: **open**;
- Gate B: **open**;
- global non-trivial-cycle exclusion: **open**.

No necessary-state witness in RL232 is promoted as a physical realization.
