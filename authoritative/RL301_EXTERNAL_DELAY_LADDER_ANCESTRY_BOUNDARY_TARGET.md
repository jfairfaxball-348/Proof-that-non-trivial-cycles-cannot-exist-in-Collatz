# RL301 — external delay ladder / ancestry boundary decision target

Date prepared: 2026-09-11
Status: PREPARED, NOT STARTED

## Incoming classification

RL300 closes as

`EXTERNAL_DELAY_RECORD_CERTIFICATE_PLUS_EXACT_RESONANCE_FRONTIER_TO_A206745572560704146_WITH_DIRECT_START_BOUNDARY`.

There are now two deliberately distinguished frontiers:

- internally self-contained RL299 frontier: `a=630138896`;
- promoted frontier using the frozen external finite-delay certificate:
  `a=206745572560704146`.

Gate A remains open. Gate B remains open. RL297 P-bottleneck work remains parked weak-green background.

## Frozen RL300 certificate

The only external quantitative statement consumed is

`D(n)<=2334 for every positive n<4761963248413673697`,

derived from the frozen consecutive delay-record pair

`3571472436310255273`,
`4761963248413673697`

with local replay delays `2334`, `2337`.

For every physical start in the certified direct-start blocks,

`J<=half_stopping<=D<=2334`.

The exact rational resonance verifier certifies 51 records from U5 to the first direct-start boundary.

Final safe controlling record:

`(196864044717151823,124207383220472977)`

with

`Nhat=1250146787826445598`,
`Bmax=4219245408914253845`.

First unresolved direct-start record:

`(206745572560704147,130441933147714940)`

with

`Nhat=4894841535700125438`,
`Bmax=16520090182987923332`.

## Primary objective A — cross the boundary using inherited physical compression

Before importing a larger external ladder, test whether the inherited RL299 B-family ancestry

`16h+15 -> 24h+23 -> 36h+35 -> 54h+53 -> B0`

or a coupled A/B suffix/merger argument places the boundary record back inside the already-frozen quantitative delay certificate.

This must be done with exact integer envelopes and the same proof-state discipline. Do not infer a global stopping theorem.

## Primary objective B — larger external finite certificates only with the RL300 provenance standard

If another delay-record interval is required, freeze:

- exact source URLs / provenance;
- exact consecutive record values;
- exact locally replayed delays;
- exact checked range consumed;
- exact consumer conversion to a `J` or stopping bound.

Do not use a convergence-only range, a status-line claim, or an unlabelled external database as a theorem.

## Primary objective C — strategic Gate-A comparison

If the external-certificate route produces only another finite frontier rather than structural compression, compare the next cost directly against the parked RL297 Bellman/P shared-endpoint route.

A method decision/barrier is a valid RL301 result. Do not spend the session on blind trajectory volume.

## Success condition

RL301 succeeds with any one of:

- a compact ancestry/coupled-family certificate crossing the first RL300 direct-start boundary;
- a further provenance-backed quantitative finite certificate with an exact new resonance frontier;
- a reusable structural theorem that reduces dependence on external delay intervals;
- a decisive comparison showing that returning to the Bellman/P route is strategically superior.

## Scope

Gate A OPEN.
Gate B OPEN.
RL297 P work parked weak-green.
Radius 6+ frozen.
RO/divergent-orbit work out of scope.
Lean formalisation separate.
No global non-trivial-cycle exclusion is claimed.
