# RL210 target — H21 above-p prefix cancellation and global selector

Incoming job RL210 follows completed RL209.  Apply RL206-C1/C2, the complete
RL208 predicate, and the RL209 pointwise exclusion before any count.  The exact
current necessary-terminal frontier is **13,415,865,871**, split as
**7,091,831,284 above p** and **6,324,034,587 below p**.

## Primary task

Attack the remaining canonical `a>p` frontier through genuinely independent
global information.  For `a=p+e`, use the exact RL209 decomposition

`E_a=3*2^(u+37)-2^u P_p+(3^p-2^u)R_(p,a)`,
`S_a=u+floor((Ae+1)/L)-1>=u`.

The goal is to control the cancellation term at the physical complete-word
hypotheses: obtain an independently certified residue, interval, overlap/closure
identity, anchored height, or equivalent restriction on `P_p`, `R_(p,a)`, or the
joint absolute prefix moment that is not already the endpoint identity in new
notation.  If such data determine eta at Hensel level, prove the full 56-bit
precision at the exact physical source before consuming it.

Derive every p-shift in lifted coordinates.  A canonical wrap after `a+p>=L` is
not a second below-p tau34 source; the unique epsilon carry remains phase `L-p`.
Do not import or reflect the below-p 37/60/97 theorem.

## Secondary opportunities

A nonzero exact deletion, independent eta/state/sign/valuation selector, physical
H21 incidence/charge, or contradiction of `(37,0,23,-1)` is preferred.  A sharp
obstruction to a genuinely new global consumer may be retained at exact scope.

RL209's 4,096-way root stratification is not asserted to exhaust the mathematical
pointwise cone, but merely increasing subdivision is lower priority and must not
be presented as new independent information.  The immediate signed-successor
root test is already exhausted on the named 986-rank old-root survivor set.

## Required red teams

Preserve RL195 denominator equivalence and RL201 real-mass/coupled-reverse
barriers; freeze canonical positions before eta bits; never relabel orientation,
state, parity or valuation laws as new selectors; distinguish necessary ranks
from physical realizations and charges; and do not turn a branch-scoped result
into Gate A/B or global nontrivial-cycle exclusion.

Unfinished work is NOT PROMOTED.  Preserve exact uncovered ranges and stop at the
first invalid live dependency if a repair is required.
