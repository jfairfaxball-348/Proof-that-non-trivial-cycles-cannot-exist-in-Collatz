# RL129 target — `L=13` tail cut and corrected canonical ownership

Start from the RL128 frontier `L>=13`.

For `L=13`:

1. determine the exact positivity threshold `D=2^(13+Z)-3^13>0`;
2. derive and prove the smallest `Z` at which the inherited `H(13,Z)` floor contradicts the physical width ceiling;
3. enumerate the resulting finite residual strip gap-free using only the corrected nonempty-fibre CRT capacity predicate;
4. retain RL128's proved cyclic-root invariance of `D|Q`, pair-primitivity equivalence, canonical pair-orbit compression, and exact orbit-weight invariant;
5. treat any divisibility hit only as a necessary-condition event requiring analysis, never as a physical cycle.

All inherited ownership, scaling, physical-state, primitivity, Raw/Farey, finite-certificate, correction/demotion, verification-economy, and sustained-attack rules remain mandatory. No global closure is implied unless its independent obligations are actually discharged.
