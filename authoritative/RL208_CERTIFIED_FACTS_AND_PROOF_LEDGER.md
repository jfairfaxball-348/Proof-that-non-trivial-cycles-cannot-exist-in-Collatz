# RL208 certified facts and proof ledger

Date: 2026-08-31. Canonical current classification record.

## New proved analytic mathematics

**RL208-T1:** for every integer `1<=N<=2^35`, the inherited noncarry H21 speed
bound and lifted endpoint defect imply

`K_0-N/3 < K_i < K_0+1+N/3`

for terminal phases in `[0,N)` or `[L-N,L)`.  The selected paths are carry-free
because `2^35<p-1<z+1`.  Proof and exact scope are in
`RL208_LAYERED_ROOT_CONE_CERTIFICATE.md`.  This is not a bound across the carry.

## New exact finite certificate

**RL208-CERT1:** a 704-layer exact modular count over radii from `2^24` through
`2^35`, with exact rational boundary signs and an independent coordinate-swapped
rectangle recount, deletes **2,765,120,323** previously surviving necessary
terminal ranks.  Forward layers contribute 1,382,560,101 and backward layers
1,382,560,222.  There is no overlap with the inherited root-window, isolated or
anchor deletions.  The necessary-terminal count is therefore exactly
**13,423,606,911**.

At source scope `a=i-34 modL`, the reduced frontier contains exactly
**7,099,572,324** necessary ranks with `a>p` and **6,324,034,587** with `a<p`.
The certificate and its exact verifier are
`RL208_LAYERED_ROOT_CONE_CERTIFICATE.md` and
`verification/verify_rl208_layered_root_cone.py`.

## Inherited proved mathematics and certificates

RL206-T1/T2/T3, all inherited H21 identities, the RL202 root-anchor theorem,
RL206-C1 and RL206-C2 retain their recorded classifications and scopes.  The
corrected below-p constants remain 37 / 60 / 97.  Eta classes remain
`0,8,9,17 mod18`.  The original pre-RL208 predicate/count 16,188,727,234 is an
inherited input; RL208-CERT1 is the only change to that rank frontier.

The three unchanged RL206 mathematical verifiers and their unchanged outputs are
accepted under verification economy by exact Git blob identity against the
already-passing RL207 handover.  They are not relabelled new RL208 work.

## Barriers and open obligations

The conservative common-radius root-cone consumer saturates at `N=2^35`, where
its safe rank band is the whole inherited core.  This is only a method-scope
barrier; a sharper pointwise root cone remains possible.

The canonical `a>p` independent-information target remains open on its reduced
7,099,572,324-rank frontier.  No reflected below-p theorem is imported.  The
endpoint moment, forced tau34 tail, orientation law, RL195 denominator
equivalence, and RL201 real-mass/coupled-reverse barriers remain controlling.

No independent eta/state/sign/valuation selection is proved.  No physical H21
incidence or charge is proved.  No contradiction of `(37,0,23,-1)` is proved.
Gate A globally open; Gate B globally open; nontrivial-cycle exclusion open.
