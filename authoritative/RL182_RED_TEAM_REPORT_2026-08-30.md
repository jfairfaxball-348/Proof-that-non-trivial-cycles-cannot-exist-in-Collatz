# RL182 red-team report

Date: 2026-08-30

- **Incoming authority:** PASS. Frozen at commit `0e4899aed34fe94773245cdd0963cffa9f61340c`, root tree `40af4b362cf198816b0aef619d49d55bbc825bcb`, authoritative tree `8af77869b2e2e68f687d100f1afb80ac55bb3324`.
- **Incoming fast verification:** PASS. The authoritative RL181 verifier logic was independently replayed with exact `Fraction` arithmetic after the frozen tree and prior clean-candidate verification records were re-read.
- **Dyadic/integer distinction:** PASS. `U_i` is a dyadic physical difference; only the rescaled ordinary-edge `C_i` is called the physical integer numerator.
- **Ternary reduction:** PASS. Negative powers of two are units modulo `3^n`; the inherited `3^n` term vanishes exactly.
- **Numerator uniqueness:** PASS. Uniqueness is claimed only after the strict physical shallow numerator ceiling is proved below the relevant `3^n`.
- **Carry:** PASS. Physical numerator claims exclude the unique carry edge; periodic algebra may cross it without reclassifying it.
- **Necessary versus physical:** PASS. No arbitrary residue, suffix word, or necessary automaton state is promoted as physically realized.
- **p-window complement:** PASS. `q<=rho` and the arbitrary-subset mechanical envelope are used. A wrapped periodic q-term is larger by `lambda`, so base-period replacement is conservative for a lower bound.
- **Refined m/Q scope:** PASS. These are recomputed from frozen RL180 identities and remain internal to the surviving high branch.
- **Affine-tail occupancy:** PASS. The rank envelope is monotone and minimized over arbitrary selected sources before inserting the shallow population floors.
- **Repeated defect classes:** PASS. The possible carry edge is removed before pigeonholing; a possible unique carry successor is explicitly exempted from the mod-3 prediction.
- **Historical barriers:** PASS. No blocked generic packing/rank/inverse-rank/higher-modulus route is silently revived.
- **No false closure:** PASS. The `(37,0,23,-1)` high type and all global gates remain open.

Fast verifier: PASS before candidate packaging.
