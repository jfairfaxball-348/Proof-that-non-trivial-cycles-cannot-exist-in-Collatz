# RL191 Correction / Demotion Ledger

Date: 2026-08-30

## New corrections or demotions

**None.**

RL191 did not discover a false inherited theorem, verifier failure, or contradiction requiring repair.

## Scope clarifications recorded at closeout

These are guardrails, not corrections:

1. The universal transport law removes the mechanical-switch/carry exception by changing to the carry-completed `K/rho` gap.  It does not assert the old ordinary pair-gap formula held at the exceptional sources.
2. The finite resonance theorem excludes separations only through 1000.  The certified consequence is spacing `>=1001`, not exclusion of every larger separation.
3. The arithmetic equality case `S=999,K=54` in the density calculation is not claimed physically realized.
4. The strengthened `>80` directional `K` result is total directional variation, not a chronological excursion.
5. The H21 core `D` remains open; its `{33,34,35}` family is still binding and no H21 budget relaxation is claimed.
6. Necessary terminal ranks and overlap atoms are certificate state-space objects; no arbitrary rank is promoted to a realized Collatz state.
