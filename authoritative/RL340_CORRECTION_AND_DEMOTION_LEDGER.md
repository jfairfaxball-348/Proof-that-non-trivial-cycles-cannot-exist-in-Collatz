# RL340 correction and demotion ledger

Date: 2026-09-16. Status: CLOSED AND FROZEN.

No inherited authoritative theorem is corrected or demoted.

The RL340 research-session finite-potential calculation leading to `35(K-2H)-S <= -(5/13)K + 538/13` and `2H >= (92/91)K - S/35 - 538/455` is **not promoted** at closeout. Its arithmetic equivalence is checked, and its final `(23,23,p=6)` extremal interface is physically excluded, but the complete finite edge set and potential values were not packaged into a standalone replayable certificate before CLOSEOUT_LOCK. It is frozen as an unpromoted theorem candidate for RL341 verification.

The exact `(23,23,p=6)` escape certificate and the linear-consumer arithmetic barrier are promoted. All results remain conditional on the external least-state floor `m>=2^71`; no floor discharge, R1 closure, Gate A closure, Gate B closure, or global theorem is claimed.
