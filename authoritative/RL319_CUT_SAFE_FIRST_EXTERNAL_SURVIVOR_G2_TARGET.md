# RL319 — cut-safe first-external-survivor `g=2` target

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED
Session type: LEAST-ROOT / BALANCED-CUT TRANSPORT WITH ORDINARY FULL OWNERSHIP

## Absolute objective

Continue toward proving that a positive non-trivial Collatz cycle cannot exist, without losing the RL316--RL318 dual-shadow progress and without importing an unjustified least-root bound to an arbitrary balanced cut.

## Authoritative starting point

Read first:

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `RL318_PROOF_LEDGER.md`
5. `RL318_RED_TEAM_REPORT.md`
6. `RL318_EXTERNAL_2P71_DEPENDENCY.md`
7. `sessions/RL318/RL318_CLOSEOUT.md`

The repository is authoritative.  Conversation memory is not.

## Conditional first-survivor scale

When the frozen Barina external finite certificate is admitted, every `g>1` reduced survivor has

`ell >= 49,547,666,544`.

The first above-resonance continued-fraction survivor is

`(a,ell)=(217,976,794,617, 137,528,045,312)`.

For `g=2`, the full counts are `(2a,2ell)`.

RL135.2 is available at this same reduced pair and proves, in the true least-root physical normalization, that every proper prefix has mechanical defect

`h_j>=0`.

RL316--RL317 are also available: every genuine balanced two-row cut has the exact ordered-row / cross-content dual-shadow dichotomy and genuine full-`D` factor identities.

The missing bridge is that these two coordinate systems have not been proved to coincide.

## Primary target

Prove one of the following cut-safe statements.

### Target A — balanced-cut transport

Show that for `g=2` at the first external survivor, the global least-root nonnegative-defect theorem forces the existence of a balanced length-`a` cut whose endpoints inherit a quantitative least-root/contact bound strong enough to constrain the RL317 row gap `G=x-R`.

A particularly strong acceptable form would be a proof that some RL317 balanced cut satisfies an absolute bound `G<2^C` for a fixed small `C`, because then the physical row first-disagreement clock is bounded independently of `a`.

Do **not** assume this transport; prove it.

### Target B — no-transport full-word ownership consumer

Avoid moving the least-root estimate to a balanced cut.  Instead, use `h_j>=0` on the entire true least-root `g=2` word to derive an ordinary full-`D` numerator/factor restriction that, when translated algebraically, eliminates either

- the RL317 `epsilon=0` ordered-row branch, or
- the RL317 `epsilon>0` cross-content branch.

This route is preferable if the balanced-cut transport is false or too weak.

### Target C — genuinely non-homogeneous crossing theorem

For the `epsilon>0` branch, strengthen the signed-gap reversal between the content-`h` physical copy and the coprime-content `T_h` shadow into a theorem that uses the ordinary `+1` origin of the content trajectory and cannot be reduced to an RL79 homogeneous invariant.

A mere proof that the trajectories cross is insufficient.

## First attack order

1. Write the `g=2` full word in the true least-root RL135 coordinates and characterize every length-`a`, weight-`ell` window in terms of the nonnegative defect path.
2. Determine whether the integer 1-Lipschitz balanced-window count theorem plus `h_j>=0` forces a canonical contact, bounded contact displacement, or a constrained pair of block contact sets.
3. If a bounded interface is obtained, test it against RL139--RL142 full-ownership contact-polynomial obstructions and RL316--RL317 dual-shadow identities.
4. If transport fails, derive the exact obstruction and switch immediately to Target B rather than enumerating supports.
5. Preserve the external/internal distinction throughout; the same structural theorem should be stated internally whenever it does not actually use `2^71`.

## Binding red teams

- The RL315 universal balanced cut is not automatically the global least-root cut.
- No unconditional `G<2^35` or 35-step mismatch theorem was proved in RL318.
- Do not infer local denominator ownership for a proper balanced segment.
- Standalone `T_h` invariants remain blocked by RL79.
- Generic RL310 packing already fails by a factor greater than `21.6` at this survivor.
- RL140--RL142 do not cover arbitrary `g=2` contact geometry without their stated interface hypotheses.
- Finite-prefix parity/gap identities may be tautological along physical affine rays (RL263--RL264).
- Do not restart bare CF/residue-prefix work, support-by-support grammar, or the old `a<=22` scan.
- `g=1` remains separate.

## Scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
Lean formalisation remains separate.

The correct parent difficulty assessment inherited from RL318 is `EASIER` conditionally because the external frontier moved enormously, while the local first-survivor `g=2` bridge remains a hard open obligation.
