# RL259 — post-first-frontier simultaneous Gate-A/Gate-B arithmetic target

Date prepared: 2026-09-05
Status: **PREPARED, NOT STARTED**

## Incoming promoted state

RL258 proves a targeted Gate-A certificate at the unique first halving
selector

`(a,ell,z,q,r,H_sel,n)=(1100,694,406,317,200,14,4)`:

- RL257 already eliminates `k=33`;
- RL258 proves every `k=31` genuine completion has `H_can>=31`.

Therefore this first halving selector contributes no object that is
simultaneously unresolved by Gate A and Gate B.

## Unique target

Re-enter the exact promoted arithmetic selector/frontier machinery from
RL254-RL257 with the first halving selector removed.

1. Determine the next arithmetic selector(s) not already excluded by the
   promoted resonance, determinant, beta/capacity, zero-budget, parity,
   halving/non-halving, and terminal constraints.
2. Preserve the distinction between Gate A and Gate B.
3. For each next simultaneous survivor, identify its terminal-exponent range
   and the cheapest exact route to either:
   - a targeted Gate-A certificate `H_can>=k`, or
   - the active Gate-B/Radius-4 bridge.
4. If the next frontier is finite, contract it with exact reproducible
   certificates.  If it jumps to a substantially larger scale, state the
   exact new floor/frontier and hand over the smallest live branch.

## Binding safeguards

- Do not identify `H_sel` with `H_can`.
- Use the corrected internal endpoint `J=2^k` before the omitted terminal 10.
- Do not resurrect demoted RL256 backward-tree or relaxed MILP claims.
- Do not apply Radius 4 without every audited primitive/full-D/exact-distance
  hypothesis.
- Radius 5 remains inactive unless a later authoritative session activates it.

Gate A remains open uniformly.
Gate B remains open.
The first halving selector is now Gate-A safe and removed from the simultaneous
unresolved branch.
