# RL343 target — aggressive R1 closure via canonical CRT phase carriers

Status: UNIQUE LIVE TARGET FOR RL343; prepared, not started.

Work only in the ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, retaining the external least-state-floor qualification `m>=2^71`, q=0 physical band, `z<=35`, exact ownership/pruning, RL337 fixed-depth right-suffix compression, RL341 exact shared-state recurrence machinery, and RL342 mixed-adic CRT / phase-potential results.

## Absolute objective

**Close R1 — Parent Bridge in this session if mathematically possible.**

The target is a parent-level contradiction, not another quantitative improvement and not another finite defect threshold.

## Primary closure theorem

Prove the smallest all-length theorem strong enough to establish:

> Every exact physical recurrence carrier that is nondecreasing for the canonical phase-adjusted potential either has no admissible physical lift or forces descent below the inherited least state.

RL342 proves that, once the parent recurrence is represented as a closed walk of canonical decorated physical states, at least one carrier must be phase-potential nondecreasing. Therefore eliminating **only that subclass** is sufficient; there is no need to prove descent for every carrier.

## Required route

Use RL337's fixed-depth right-suffix compression aggressively. For fixed suffix depth `s`, the last `s` profile heights lie in a finite set independent of the total positive-run length. Combine that finite suffix with:

- RL342's predecessor power-of-two compatibility condition;
- RL342's successor power-of-three compatibility condition;
- their exact CRT progression intersection;
- exact q=0 band/ownership information;
- the phase-nondecreasing inequality;
- deterministic least-state descent when a physical progression survives.

Seek a suffix depth at which the arbitrary left prefix becomes irrelevant, monotone, or represented by a finite boundary datum. Then prove every resulting nondecreasing canonical class is empty or descending.

The intended closure chain is

`hypothetical parent cycle`
`=> closed sequence of exact canonical physical recurrence carriers`
`=> at least one phase-potential nondecreasing carrier`
`=> one finite fixed-depth suffix/CRT class`
`=> impossible physical lift or forced descent below m`
`=> contradiction`
`=> R1 CLOSED`.

## Aggressive route discipline

Do **not** make any of the following the main programme:

- `sigma=27,28,...` threshold enumeration;
- completion of the whole `sigma=26` layer merely for its own sake;
- `c=18,c=19,...` coefficient climbing;
- another q-upgrade;
- another endpoint/phase-weight refinement;
- another ordinary linear telescope consumer.

The frozen `sigma=26` material is a diagnostic corpus for falsifying candidate suffix criteria. The promoted 238,329-member `(15,1)->(1,16)->(16,1)` family is a model of the desired unit of elimination: one exact CRT class removed wholesale.

## Success criterion

Declare R1 CLOSED only if the current ordered parent branch is genuinely impossible under the inherited assumptions.

If full closure fails, the required non-closure outcome is **one exact quantified canonical suffix/CRT obstruction** whose elimination would close R1. Do not hand over a mere next defect threshold.

Preserve the external least-state-floor qualification exactly. Do not mix this research session with the separate Lean formalisation project.
