# RL276 — one-sided absolute ownership consumer target

Date prepared: 2026-09-07
Status: PREPARED, NOT STARTED

## Incoming classification

RL275 closed as

`OWNED_DISCREPANCY_COMPRESSION_IDENTIFIED + GATE_A_EXACT_BARRIER`.

It proved the exact paired determinant telescope but also proved, by direct comparison with the bit/height derivative, that the telescope is an identity rather than an independent Gate-A constraint.

## Fixed target

Gate A remains

`H_can >= k=t+3`

for every retained genuine full-phase object, at terminal canonical state

`d=1, J=2^k`,

with inherited odd `k`.

The next theorem must be **one-sided and absolute**. It must not be invariant under adding the same common discrepancy component to both auxiliary half-word flows.

## Required inherited state

Retain exactly:

- RL64 full-phase extendability / absolute canonical ownership;
- RL65 `H_can=sum delta_j`, exact phase quotient identity, and quotient `N`;
- RL66 odd terminal `k`, ordered rank-defect recursion and phase-digit ladder;
- RL67+ canonical absolute-state/interface restrictions as promoted;
- RL231--RL237 chronological owned-defect/charging results together with every stop-and-repair demotion and the frozen saturation barrier;
- RL241 finite full-phase carry decomposition for `N`;
- RL243 `Q-P=h-h_shift` only as a differential identity;
- RL253--RL262 exact selector/counterflow eliminations;
- RL263 physical-telescope barrier;
- RL264 affine-ray/physical-lift barrier;
- RL274 determinant/counterflow discrepancy identity;
- RL275 common-mode barrier.

Do not reopen Radius 4 or Radius 5 local proofs.

## Mission

Prove one of the following, uniformly under the genuine full-phase hypotheses and hypothetical `H_can<k`:

1. a one-sided counterflow contradiction such as
   `beta(P)>z-k+4`;
2. an impossible Archimedean/order range for the full-phase quotient `N`;
3. an absolute canonical-state contradiction in `(d,T,J,H_can)`;
4. a chronological owned recurrence whose cumulative contribution grows with scale and cannot cancel between the two halfwords;
5. an equivalent direct contradiction to full-phase/full-`D` ownership.

## Preferred attack order

### A. Phase-digit ladder x finite carry decomposition

Recover the exact RL241 statement before using it. Couple the RL65/RL66 ordered `3`-adic phase-digit ladder to the finite carry decomposition of `N`.

The target is not another CRT residue. Seek a size/order statement that reduces the compatible quotient representatives enough to contradict positivity, the exact quotient equality, or full-phase carry geometry.

Use the fact that `H_can<k` makes the rank defect sparse in **positive displacement**, but preserve rank order and the deterministic canonical state. Do not fall back to the rejected RL48 separable cap/room relaxation.

### B. Chronological absolute consumer

If route A does not close, inspect the frozen RL231--RL237 chronological owned-defect/recurrence programme as a possible absolute consumer. Preserve the exact repaired coverage ledger and saturation boundary. Do not resurrect any withdrawn charge bound.

The question is whether a fixed-origin or boundary-coupled chronological theorem can consume the common discrepancy mode that RL275 shows is invisible to paired subtraction.

### C. Fifth selector only as a laboratory

The fifth retained selector may be used to falsify or verify a proposed one-sided theorem. Do not resume an open-ended selector conveyor belt unless the theorem itself requires a single exact test case.

## Explicitly forbidden routes

Do not:

- subtract the paired determinant identities and treat the result as new;
- use only `Q-P=h-h_shift` to bound `beta(P)`;
- revive the false RL59 terminal potential or descendants;
- revive the RL48 separable rank relaxation;
- treat finite low-area scans as a uniform theorem;
- use RL263's automatic ordered-prefix congruence as an independent restriction;
- use finite physical magnitude after RL264's affine-ray theorem;
- start Radius 6+ enumeration;
- merge Gate B into Gate A without a proved coupling theorem.

## Preferred classifications

A successful RL276 should end in one of:

- `GATE_A_SCALE_GROWTH_PROVED`;
- `GATE_A_REDUCED_TO_EXACT_TARGET`;
- `ONE_SIDED_OWNERSHIP_CONSUMER_IDENTIFIED`;
- `GATE_A_EXACT_BARRIER`.

No global non-trivial-cycle exclusion may be claimed unless all remaining implications are actually closed.
