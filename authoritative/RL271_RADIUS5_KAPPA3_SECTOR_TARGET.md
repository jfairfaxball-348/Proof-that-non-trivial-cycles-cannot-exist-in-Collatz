# RL271 — Radius-5 `|kappa|=3` sector activation

Date prepared: 2026-09-07
Status: **PREPARED, NOT STARTED**

Incoming classification: `RADIUS5_KAPPA1_SECTOR_CLOSED`.

## Inherited exact state

RL270 closes the final determinant-one `[1,1,1,1,1]` leaf. Together with RL266-RL268, the complete Radius-5 `|kappa|=1` determinant-one sector is closed.

RL265 remains authoritative for the global Radius-5 structural classification:
- every exact distance-5 optimal flow has `|kappa| in {1,3,5}`;
- `qA-mL=kappa`;
- `gcd(A,L)` divides `kappa`;
- the nine topology families are seven flat families plus the two height-two families.

For `|kappa|=3`, the topology-specific allowed families inherited from RL265 are:
- flat `[4,1]`;
- flat `[3,1,1]`;
- flat `[2,2,1]`;
- flat `[2,1,1,1]`;
- flat `[1,1,1,1,1]`;
- height-two mass-four-plus-one.

The families `[5]`, `[3,2]`, and height-two connected mass five do not carry skew three in the inherited classification.

## Primary target

Establish the first authoritative `|kappa|=3` arithmetic reduction/closure.

Required workflow:
1. orient to `kappa=+3` only after recording the exact source/target reversal for `-3`;
2. derive the exact consequences of `qA-mL=3` for each relevant gcd sector rather than importing determinant-one identities unchanged;
3. preserve all six inherited `|kappa|=3` topology families until an exact argument removes one;
4. derive topology-appropriate zero-flow cuts, edge identities and support bounds from the promoted Radius-5 framework;
5. retain divisibility by the complete positive `D=2^A-3^L`; never replace it by a proper factor;
6. use infinite-to-finite reduction before any large enumeration;
7. preserve negative-`D`, proper-factor, nonprimitive, cyclic-wrap, sign/order and numerator-index red teams.

Preferred outcomes:
- a closed `|kappa|=3` topology leaf;
- an explicit exact finite reduction for a `|kappa|=3` leaf;
- or a sharply stated exact barrier that identifies the missing arithmetic lemma.

Do not begin `|kappa|=5` in RL271 unless a promoted `|kappa|=3` checkpoint explicitly authorizes it.

Frozen:
- `|kappa|=5`;
- Gate A;
- fifth retained selector;
- selector enumeration;
- general Radius-n programme.

Radius 4 remains promoted locally. Gate B, Radius 5 and global exclusion remain open.
