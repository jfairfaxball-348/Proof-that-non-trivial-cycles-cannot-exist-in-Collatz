# RL136 target — owned defect excursions and contact packing

Date: 2026-08-27

Start from the RL135 defect-path constraints at the first reduced survivor

`(A,L)=(217,976,794,617,137,528,045,312)`.

## Priority A — `g<=6` zero-contact versus no-contact dichotomy

RL135 proves `h_j>=0` for every proper prefix when `g<=6`.

Treat separately:

1. **canonical-contact branch:** one or more internal phases have `h=0` at
   `(S,j)=(kA,kL)`;
2. **no-contact branch:** every internal canonical phase marker has positive
   defect.

For the contact branch, seek an actual owned CRT/fibre, state-spacing,
block-numerator, or repeated-state obstruction that uses more than
`S=D(x'-x)`. The RL135 absolute contact gap is available.

For the no-contact branch, exploit the repeated mechanical phase across the
`g` nominal survivor blocks. Seek a forced weighted deficit, first-return
structure, or incompatible accumulation of positive defect.

Do not infer a contact exists.

## Priority B — sparse `h=-1` excursions for `7<=g<=16`

The only negative-defect candidates are exactly the positive determinant
shells:

- `+1` beginning at multiplicity seven;
- `+2` beginning at multiplicity twelve.

Use their exact phase locations and translate spacing. Couple those phases to
actual state congruences, run/depth fibres, or ownership. A useful theorem
must distinguish candidate lattice positions from physically realized
prefixes.

## Priority C — global sliding-window bridge only with dispersion control

RL123.5 remains available:

`dist_cyc(w,rot_s(w))=min_c sum_i |h_i^(s)-c|`.

A single canonical mean window is insufficient. Pursue this route only if
RL135's all-prefix defect constraints can be converted into a **global**
bound on the sliding-window count dispersion. If successful, test whether it
forces radius at most three or another owned packing contradiction.

## Priority D — consume the mesoscopic state ceilings

Internally:

- `m<2^75` through `g=28,000,000,000`;
- `m<2^76` through `g=771,316,334,039`.

Conditional on inherited external `m>=2^71`, RL135 also gives large actual
state populations below `2^79` and `2^80` through `g=16`.

Use these only with structured ownership/CRT/fibre spacing. Do not replace
the target by an unstructured scan to an absolute bound.

## Mandatory scope and red teams

- external `R#>=2^71` remains externally sourced;
- internal frontier remains `L>=190,537` unless independently advanced;
- conditional external frontier remains `L>=49,547,666,544`;
- no multiplicity upper bound is inherited;
- no Gate A/B or global Collatz closure without discharging the actual
  obligations;
- preserve RL20, RL79, RL81, primitivity, physical-versus-quotient,
  correction/demotion, verification economy, and sustained attack;
- freeze any canonical-contact argument that reduces only to
  `S=D(x'-x)`;
- do not continue determinant-shell enumeration unless the new shell feeds an
  owned physical obstruction.
