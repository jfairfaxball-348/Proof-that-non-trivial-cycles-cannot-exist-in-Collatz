# RL243 handover bundle transport

Incoming state: **RL243**.

RL242 completed as `R4_BRIDGE_REDUCED`.

Unique target: eliminate the simultaneous double-deep-counterflow survivor `beta(G)>=2` and `beta(Ghat)>=2`, or force an eligible exact Radius-4 encounter from it.

RL243 is prepared but NOT STARTED. Radius 5 remains inactive. Gate A remains separate.
