# RL323 closeout — physical H-carry crossing and zero-carry owned interface

Date: 2026-09-14
Status: CLOSED AND FROZEN
Incoming BASE_HEAD: `d060213279faecb865b0ef6566aa6aff35ec6972`
Incoming authoritative tree: `b6dc892b587618b6327aa67375a40f84042ec58b`
Successor: RL324

## Executive conclusion

RL323 directly attacked the project-level missing ordinary/full-`D` ownership consumer requested by RL322.

The session does not close Gate A, Gate B, `g=1`, or global positive non-trivial-cycle exclusion. It also does not yet prove the literal smallest incoming target for the original least-root canonical interface.

It does, however, produce the first support-uniform physical crossing mechanism on the surviving ordered branch.

For the genuine ordered balanced rows, the phase-aligned quantity

`D_t=3^(c_t)U_t-V_t`

has an exact ordinary `+1` recurrence with positivity absorbing, while

`D_0<0<D_a`.

Thus there is a unique transition into the positive region. At that transition the full `D0/H` ownership identities are genuine, and the crossing occurs at an actual matched physical rank.

The resulting matched-rank tail has exact zero integer carry:

`floor(Q/3^h)=P`.

More sharply,

`0<J<3^(h-1)`,
`h>=2`,
`0<eta<2^r/3`.

The complementary genuine `ell`-odd prefix numerator satisfies the narrow ownership window

`D0P<A<D0P+X/3`.

Conditional on the inherited external least-state floor at the exact first external survivor, ordinary state growth plus exact rational logarithmic bounds force

`q=p-ell>=3182833230`

and therefore

`r=u_j-v_j<=77265916075`,
`2<=h<=r`.

This is an absolute support-independent bound on a forced genuinely owned matched-rank interface. It is not a support enumeration and not a complete certificate.

`PARENT_DIFFICULTY_DELTA = EASIER`.

## Correction carried into the freeze

An early unpromoted matched-rank defect route used an invalid same-row cyclic wrap. The wrap and its derived one-crossing claim are withdrawn.

A later checkpoint also labelled a post-crossing tail bound as a matched-rank displacement before that identification had been proved. The final RL323 theorem repairs this at the actual first `H`-carry transition.

No earlier authoritative RL result is changed.

## Strategic meaning

RL322 established that geometry plus positivity is insufficient. RL323 supplies exactly the missing ingredient that countermodel lacked: an ordinary-physical `+1` recurrence whose sign is simultaneously tied to the genuine `H` ownership factor.

The next session should therefore continue this route at the parent level, not retreat to support grammar or merely improve the numerical constant.

The main unresolved bridge is:

> convert the forced zero-carry matched-rank crossing into closure of the surviving parent branch — by contradiction, a genuinely smaller owned reduced balanced return, or an absolute bound on the original least-root canonical interface.

A second acceptable closure is to prove that the bounded zero-carry owned witness necessarily falls under an already-proved obstruction, without enumerating support up to the RL323 bound.

## Scope and barriers retained

- Internal-only frontier: `ell>=190537`.
- External-certificate-conditional frontier: `ell>=49547666544`.
- External `2^71` least-state floor remains conditional.
- No fixed-depth prefix scan.
- No support-by-support grammar.
- No bare modulus escalation.
- No tail-residue-only or corner-smallness-only attack.
- No geometry-only promotion of affine factors.
- No independent ownership of frozen RL147 mixed-height layers.
- No reuse of the demoted RL321 height-38 transfer.
- Do not enumerate `r<=77265916075`.

## Verification

Portable verifier:

`verification/verify_rl323_zero_carry_crossing.py`

It checks the exact one-step `D` recurrences on exhaustive small parity-compatible states, first-crossing inequalities, the rational-log enclosure, the elementary growth-factor bound, and the exact boundary integer for the conditional zero-count/displacement theorem.

The unrestricted mathematical results are analytic. Finite loops in the verifier are regression evidence only.

Knowledge catalogues: `stale/deferred`.
Lean formalisation: separate.
