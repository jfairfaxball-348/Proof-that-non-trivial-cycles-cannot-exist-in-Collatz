# RL220 reduced H21 / certified-blue interface

Date: 2026-09-01

## Frozen arithmetic family

- root lattice per prefix: `y(k)=y_*+3*2^58 k`;
- exact root band:
  `R_MIN=24913843845551577787381`,
  `R_MAX=31285589992934194300574`;
- 45,045 live prefixes;
- 139,581,280 phase-51 survivors;
- 3,132,617 disjoint phase-51 2-adic cylinders, maximum precision 25 bits.

## Backward-word algebra

For a raw word of length `n` with `h` odd inverses,

`F_w(s)=(2^n s-C_w)/3^h`.

For odd-only gap exponents,

`Q_0=0`,
`Q_j=2^(e_j)Q_(j-1)+3^(j-1)`,
`3^h y=2^E s-Q_h`.

## RL220 rigidity lock

If a fixed block with `h>0` is repeated indefinitely from one fixed integer
boundary and all iterates are integral, that boundary is the block's fixed
point. Therefore:

- an O-containing star in a rectangular bounded-star family is idle;
- a D-only star is dyadic variation;
- after simplification, bounded-star families have fixed odd count and affine
  dyadic gap exponents.

## Exact surviving-family pullback

For affine gap exponents `e_j=b_j+sum_l a_(j,l)m_l`, substitute the unrolled
`Q_h` into

`3^h(y_*+3*2^58 k)=2^E s-Q_h`.

This is the exact finite-term exponential/S-unit equation to be solved. Every
parameter tuple supplies at most one proposed `k`.

## New exact negative screens

- every live prefix root progression is `y == 3 (mod 4)`, hence
  `v2(3y+1)=1` throughout that progression;
- the one-step bridge to the inherited `[1,2^71]` certified interval is empty;
- `b_38=25185954575304774473045` lies in the band but matches zero live inherited root windows
  before phase 51.

## RL221 handoff lock

Do not spend RL221 on:

- finite fixed-word/dyadic libraries;
- one-star or rectangular bounded-star families already covered here;
- the RL80 LTE comb;
- seed-1 reverse-tree depth extension;
- the one-step `[1,2^71]` bridge.

The next family must be genuinely aperiodic/growing-odd-count or use
correlated nonrectangular parameters, unless it supplies an independent theorem
that solves the RL220 S-unit image exactly.
