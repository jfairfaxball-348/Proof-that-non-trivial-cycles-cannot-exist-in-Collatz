# RL220 certified facts and proof ledger

Date: 2026-09-01

## Inherited authoritative state

Accepted under the verification-economy rule after the RL219 checksum,
fresh-unpack, manifest, and fast-verifier gate:

- necessary terminal ranks: **13,415,865,871**;
- above-p source necessary count: **7,091,831,284**;
- below-p source necessary count: **6,324,034,587**;
- e=16 phase-51 survivors: **139,581,280**;
- live e=16 prefixes: **45,045**;
- phase-51 selector: **3,132,617** disjoint 2-adic cylinders, maximum
  **25 bits**;
- state 011: **90,749,885**;
- state 111: **48,831,395**;
- mod18 populations: 0 -> **35,622,831**, 8 -> **19,167,422**,
  9 -> **55,127,054**, 17 -> **29,663,973**;
- all **469** reachable eta mod 2187 classes remain represented;
- e=16 terminal rank **34,124,151,203** remains live;
- Gate A open globally; Gate B open globally; global nontrivial-cycle
  exclusion open.

## RL220-T1 — fixed-block infinite-integrality rigidity

**Classification: proved analytic mathematics.**

For a fixed nonempty raw backward block
`F(x)=(2^n x-C)/3^h` with `h>0`, if `F^m(x)` is integral for every `m>=0`,
then `(2^n-3^h)x=C` and `F(x)=x`.

The complete proof is in
`proofs/RL220_BACKWARD_PUMP_RIGIDITY_AND_BOUNDED_STAR_REDUCTION.md`.

## RL220-T2/T3 — one-star and bounded-star collapse

**Classification: proved analytic mathematics / method barrier.**

For a fixed-seed one-star family, a starred block containing an odd inverse is
constant at its fixed point; a D-only star is a dyadic ray and is already in
RL219-T2's architecture.

More generally, in a fixed finite bounded-star expression that is legal for
every independent parameter tuple in `N^r`, every non-dyadic star is
algebraically idle. The genuinely varying parameters can occur only in pure-D
gaps around a fixed number of odd inverses.

Scope lock: this theorem requires a rectangular all-tuples legality domain. It
does **not** cover correlated parameter sets, growing odd count, or genuinely
aperiodic/recursive word generators.

## RL220-T4 — S-unit/exponential lattice reduction

**Classification: proved analytic mathematics / reduction theorem.**

After the bounded-star collapse, the RL218 recurrence unrolls the endpoint as
a finite signed exponential expression in powers of 2 with fixed powers of 3.
Substitution of `y=y_*+3*2^58 k` gives an exact S-unit-type Diophantine
equality. Each parameter tuple proposes at most one exact `k`; the
parameterization alone does not certify a non-singleton blue k-class.

No finiteness theorem for all solutions of the resulting correlated
multi-parameter equation is claimed.

## RL220-C1 — current-root first accelerated-step screen

**Classification: exact finite certificate.**

The portable verifier reconstructs all 45,045 inherited live root prefix
windows and proves every base is `3 mod 4`, hence every root in those
progressions has `v2(3y+1)=1`. The one-accelerated-step route into the stable
external `[1,2^71]` interval therefore gives zero deletions.

## RL220-C2 — b_38 equality screen

**Classification: exact finite certificate / route sanity check.**

For `b_n=(4^n-1)/3`, `b_38=25185954575304774473045` lies in the exact root band. The verifier
checks every one of the 45,045 live inherited root windows and finds zero exact
same-integer matches before phase 51. Independently, this family is a fixed
`O` inverse applied to a dyadic seed ray and is already within RL219-T2.

## Frozen numerical outcome

- new candidate deletions: **0**;
- new prefix deletions: **0**;
- new rank exclusions: **0**;
- necessary-rank frontier: **13,415,865,871** unchanged;
- e=16 phase-51 survivors: **139,581,280** unchanged;
- Gate A, Gate B, and global exclusion remain open.
