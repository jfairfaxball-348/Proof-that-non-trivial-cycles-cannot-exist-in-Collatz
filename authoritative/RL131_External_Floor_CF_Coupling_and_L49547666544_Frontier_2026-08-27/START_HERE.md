# START HERE — RL131 external-floor handover

Run `verification/run_fast_rl131_verifiers.sh`, then read the session state and RL132 target.

Frozen scope: conditional on the inherited external least-state floor `R#>=2^71`, primitive positive ordinary candidates require `L>=49,547,666,544`. Gate A/B and global closure remain open.
