# RL131 — external-floor continued-fraction coupling

## Result

RL131 applies the accepted external least-state floor `R#>=2^71` to the inherited RL19 odd-step product identity and RL20 continued-fraction gate. The independently rerun exact rational-interval verifier proves that every possible reduced odd-count denominator satisfies

`L/gcd(A,L) >= 49,547,666,544`.

Thus `L>=49,547,666,544`. This strictly subsumes the reconciled internal RL130 frontier `L>=190,537` for the stated primitive positive ordinary scope.

## Scope and classification

This is an **exact finite arithmetic certificate conditional on an inherited external computational input**, not an analytic internal theorem. The `2^71` input is preserved as external; no stronger demoted floor is used. Gate A, Gate B, global nontrivial-cycle exclusion, and the Collatz conjecture remain open.

The verifier constructs rigorous rational intervals for `log(2)` and `log(3)`, reconstructs the relevant continued-fraction prefix, and excludes every above-side convergent through the exact denominator gate. The first convergent denominator beyond the gate is recorded, not excluded.
