# RL98 SESSION STATE AND RL99 KICKOFF

Date: 2026-08-25

## 0. Authoritative purpose

RL99 is the **global audit / review / synthesis / correction-ledger / route-assessment / RL100+ roadmap-setting session**.

Do not continue the deferred RL98 coupled-cascade attack as the main task during RL99.

Instead, preserve its exact restart point and audit the entire research programme that has led to session 99.

The audit must be broad enough to answer where the project genuinely stands at the threshold of RL100, what has accumulated, what has been ruled out, what remains branch-specific, and which route deserves the next sustained attack.

## 1. Frozen global theorem status

Retain unless the audit proves a correction or a new corollary:

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

## 2. Frozen latest branch state

The current exact first-Farey/full-phase Gate-A branch has:

- wide `d<=5,000,030`, `r<=7,000,000`;
- middle `d<=7,500,032`, `r<=4,350,000`;
- deep `d<=10,000,032`, `r<=3,000,000`;
- ultra `d<=15,000,035`, `r<=1,680,000`;
- feasible supports `A-C`, `C-D`, `D-E`;
- live support `lambda=4,000,000/52,500,238,500,033`, `mu=1/7,000,001`;
- `floor(lambda*C)=3,211`;
- conservative `L_common=15,000,053`;
- all odd branch values through `2,921,801,521` excluded;
- first surviving branch candidate `k8=2,921,801,523`;
- frozen margin there `-3,549`.

The full resume instruction is `RL98_COUPLED_CASCADE_RESUME_AFTER_RL99.md`.

## 3. Audit baseline and historical checkpoints

RL99 should use the repository itself as the research corpus.

Do not recursively re-prove every session from scratch. Use the following as anchor audits / strategic checkpoints, then selectively recover source sessions when a dependency, scope question, correction, or route comparison requires it:

1. **RL18–RL20** — repaired radius-3/global identities, final local closure, physical packing separation, radius-4 fake/countermodel, balanced-return and strict-excursion global route framing.
2. **RL43–RL62** — one-excursion / Gate-A valuation, rank/phase/defect programme, safe-CF restrictions, whole-tree audit, and the RL59 sign-error stop-and-repair culminating in the exact `H<=24` finite certificate.
3. **RL63–RL71** — synchronized dangerous cylinders, full-phase ownership, rank-tail/backward-digit ladder, even-`k` elimination, nested residue floors.
4. **RL72** — first major global audit, lemma catalogue, correction/demotion ledger, closure matrix, and roadmap. Treat this as the main pre-RL73 baseline.
5. **RL73–RL75** — giant owned macro, full-phase pump/Möbius structure, global route tournament, primitivity/transversality synthesis.
6. **RL76–RL79** — physical-scale/packing/product/CF route stress tests, common barrier synthesis, generalized-increment theorem, and the conclusion that a successful route must consume the absolute unit-increment fact `s=1` in a non-homogeneous way.
7. **RL80 onward** — the proved-blue dyadic funnel / certified basin pivot and its descendants.
8. **RL81–RL88** — common-mode, 2-adic / valuation / reset / physical packing developments, including the arbitrary-reset family constraints.
9. **RL89–RL93** — reset-budget, 2-adic frontier, banded rectangle, multiscale/weighted tier, and deep-frontier development.
10. **RL94–RL97** — coupled four-tier geometry, exact support-edge optimization, ultra/deep/middle gap-free scans, and sustained exact interval propagation.
11. **RL98** — strategic checkpoint only; the RL97 state is preserved and the beyond-3M cascade is deferred for post-audit resumption.

RL99 must also look earlier than RL18 when needed to understand the origin or exact hypotheses of the radius-3 theorem, RL reduction, or inherited external dependencies. Do not assume the RL72 catalogue makes pre-RL18 provenance irrelevant.

## 4. Verification economy rule

Apply:

> After the current bundle checksum, internal manifest, and fast verifier suite pass, accept the frozen incoming proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize logical audit, synthesis, and new strategically decisive consequences.

For the global audit:

- verify current handover integrity;
- inspect historical reports, ledgers, and exact theorem interfaces;
- rerun only cheap/fast verifiers when useful;
- do not replay massive scans merely for reassurance;
- explicitly mark any result whose exact certificate is inherited rather than rerun.

## 5. RL99 mandatory deliverables

RL99 should produce, at minimum:

### A. Updated global closure tree

A concise tree/matrix showing exactly what is:

- globally closed;
- locally/branch closed;
- open;
- dead by countermodel;
- blocked by a known method barrier;
- dependent on an external certificate;
- dependent on an exact finite certificate.

### B. Full proof-state / lemma catalogue

Update RL72's catalogue through RL98.

For every load-bearing reusable result record:

- session/source;
- statement/interface;
- hypotheses/scope;
- classification;
- downstream consumers;
- whether it distinguishes genuine `s=1` ownership from generalized-increment fakes;
- whether it is branch-specific;
- whether a correction/demotion affects it.

### C. Correction / demotion ledger

Consolidate all known repairs, not just RL59.

Identify:

- false claims;
- over-strong interpretations;
- route collapses to coboundaries/tautologies;
- fixed-survivor facts that were once tempting to read uniformly;
- countermodel-killed bridges;
- scope restrictions that must remain explicit.

Then audit downstream dependencies for surviving contamination.

### D. Route genealogy

Explain how the programme moved through:

- radius-3 obstruction;
- Gate A / Gate B split;
- valuation/rank/phase/ownership;
- route tournament and generalized-increment barrier;
- blue/dyadic funnel;
- 2-adic frontier / reset / packing;
- multiscale tiers;
- coupled four-tier cascade and interval propagation.

The goal is to distinguish genuine cumulative progress from repeated reformulation of the same missing global bridge.

### E. Branch-versus-global map

For every major late-stage result, state exactly which restrictions it assumes:

- safe-CF;
- one-excursion;
- first-Farey;
- full-phase;
- terminal-owned;
- minimal-reset or arbitrary-reset;
- physical-representative separation;
- other inherited selectors.

Then answer whether there is a proved mechanism that every hypothetical RL counterexample must enter that branch.

If not, name the missing lifting theorem precisely.

### F. Strategic value of the recent cascade

Assess the RL89–RL97 cascade as a research asset.

Quantify what it has proved, but also ask:

- Is the exclusion engine scalable in principle?
- What happens near the `3.5M` geometry threshold?
- Is there a foreseeable support/floor exhaustion point?
- Does the branch lower endpoint movement materially approach the finite upper window?
- Can the same exact certificates be used to prove a branch-to-global covering theorem?
- Is the correct next move more scanning, a new tier, stronger successor charging, or lifting the branch hypotheses?

### G. Gate A / Gate B / direct-closure portfolio

Re-rank all serious routes, including ones deprioritized in RL75/RL79, under the evidence accumulated since.

Do not assume the current Gate-A branch must win.

For each route give:

- desired final contradiction;
- strongest existing ingredients;
- precise missing theorem;
- known countermodels/barriers;
- expected value of another session;
- expected compute burden;
- whether progress would be global or branch-specific.

### H. RL100+ roadmap

End with:

1. one **primary RL100 target**;
2. up to three ranked fallback/parallel routes;
3. a reasoned trigger for when to resume the deferred coupled cascade;
4. concrete stop/pivot conditions;
5. the exact artifacts and frozen states the next session should inherit.

Do not give a vague “continue research” handover. RL100 must have a theorem-shaped or certificate-shaped target.

## 6. Audit standards

Preserve the project distinction between:

- proved analytic mathematics;
- exact finite certificates;
- externally inherited certificates;
- computational evidence;
- conjectures;
- method barriers / dead routes;
- repaired or demoted claims.

Do not use numerical success, repeated verification, or large scanned ranges as a substitute for global logical scope.

Conversely, do not dismiss an exact finite/certificate route merely because it is computational: assess whether it can be made exhaustive over a rigorously bounded remaining domain.

## 7. New deductions during RL99

RL99 may prove new corollaries if they follow cleanly from already certified interfaces.

If a genuinely new theorem appears during synthesis:

- state it separately;
- prove it with explicit dependencies;
- classify it;
- red-team it;
- update the closure matrix only to the exact scope justified.

Do not let incidental new mathematics derail the main whole-program audit.

## 8. Required closeout

At RL99 closeout, create:

- a global audit / synthesis report;
- updated proof-state and correction/demotion ledgers;
- route/closure matrix;
- RL100+ roadmap;
- `RL99_SESSION_STATE_AND_RL100_KICKOFF_2026-08-25.md`;
- one authoritative RL100 target;
- a handover bundle with checksum and fast/fresh verification.

If the audit concludes the coupled cascade should resume immediately, RL100 should restart from `RL98_COUPLED_CASCADE_RESUME_AFTER_RL99.md`, not from a reconstructed or approximated state.
