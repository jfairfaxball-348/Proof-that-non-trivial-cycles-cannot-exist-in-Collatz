# RL306 — scalar max-plus wall/transducer Gate-A target

Date prepared: 2026-09-12
Status: PREPARED, NOT STARTED

## Incoming classification

RL305 closes as

`GATE_A_MINIMAL_SCALAR_DEPENDENCY_AUDIT_AND_MAXPLUS_WALL_FAST_TRACK_IDENTIFIED`.

Gate A remains open. O1, O2, P/8, `Bcal(P)<=1`, checkpoint-8 scalar ceiling, and all non-P front-door obligations remain open.

The key RL305 correction is architectural:

- O1/O2/P8 are stronger sufficient routes, not necessary Gate-A dependencies;
- checkpoint-8 excess-one `nu_2(E)<=A+1` is stronger than the literal minimal ceiling `Bcal(8)<=3`;
- the actual P-source target is scalar and should be attacked with normalized Bellman credits.

## Absolute objective

Test whether the exact RL302–RL304 wall/cascade/PQ machinery becomes a genuinely convergent all-depth proof after normalization by the minimal Bellman scalar credits.

The primary target is

`Bcal(P)<=1`.

By RL305 this is exactly equivalent to

- `Bcal(R3_tower)<=3`;
- `Bcal(8)<=3`;
- `Bcal(D0)<=3`.

Do not make O1, O2, or universal P/8 identity the default target.

## Primary normalized family target

For odd `D>=3` define

`X_L(D)=Bcal(L_D)-B_L(D)`,
`X_R(D)=Bcal(RW_D)-B_R(D)`,

with

`B_L(D)=(D^2+D-6)/2`,
`B_R(D)=(D^2+3D-4)/2`.

For `d>=3` define

`X_A(d)=Bcal(A_d)-C_A(d)`,
`X_F(d)=Bcal(F_d)-C_F(d)`,

with

`C_A(d)=d^2-2d-1`,
`C_F(d)=d^2-d-2`.

The desired scalar ceilings are

`Bcal(8)<=3`,
`X_L(D)<=3`,
`X_R(D)<=3`,
`X_A(d)<=3`,
`X_F(d)<=3`.

The shallow R3/checkpoint-2 renewal must be handled exactly from the inherited RL302 recurrence.

## Mandatory first experiment

Construct the complete weighted max-plus quotient generated by the relevant RL302–RL304 exact identities.

At minimum include and verify the exact inherited weighted branches:

- `L_D -> RW_(D-2)`: weight `k-D-4`;
- `L_D -> L_(D-2)`: weight `k-2D-5`;
- `RW_D -> L_(D-2)`: weight `k-3D-2`;
- `RW_D -> RW_(D-2)`: weight `k-2D-8`;
- adjacent-wall `L_D -> RW_D`: weight `D-k-1`.

Then incorporate the leading-P siblings, D0 F/A families, wall normalization, and only the portions of the RL304 fixed-96/PQ grammar that are actually needed for complete coverage.

## Success criterion

A theorem-sized success is one of:

1. a finite complete transducer with every legal future branch covered and all sinks already scalar-safe;
2. an explicit potential `phi` with
   `weight+phi(next)-phi(current)<=0`
   on every edge and strict decrease on every nontrivial cycle after exact renewal quotienting;
3. an equivalent negative-cycle/longest-path certificate proving all normalized excesses <=3;
4. a strict reduction of the scalar family set to a smaller finite residual with a proved decreasing parameter.

Syntactic termination of every rewrite is not required if the weighted Bellman excess decreases.

## Hard pivot criterion

Do not continue producing additional unweighted commutation identities indefinitely.

Immediately freeze and pivot if a correct complete quotient exhibits:

- a legally repeatable nonnegative normalized cycle not absorbed by an already-safe sink;
- an unavoidable unbounded state parameter with no decreasing credit;
- or equivalently a return to unrestricted checkpoint futures/circular checkpoint-2 grammar.

If this occurs, the next principal route is the direct fixed-seed ancestry/static-danger separation using RL289/RL292/RL293, but only for the seven scalar Gate-A obligations.

## Checkpoint-8 discipline

The minimal required theorem is

`Bcal(8)<=3`.

Do not silently replace it by the stronger excess-one conjecture

`nu_2(E)<=A+1`.

The excess-one theorem may still be used if independently proved, but it is not required by the scalar dependency graph.

## Gate-A dependency discipline

Even after `Bcal(P)<=1` is proved, Gate A still requires:

- `Bcal(4,39)<=3`;
- `Bcal(2,-17)<=1`;
- `Bcal(2,-84)<=2`;
- `Bcal(3,-28)<=3`.

RL296's `(4,39)` residuals remain exact and frozen.

If the P-source weighted method succeeds, apply the same scalar normalization directly to these front-door residuals rather than reconstructing universal ownership.

## Frozen routes

Preserve without consuming:

- RL304 fixed-96/twelve-factor P/Q frontier;
- physical/resonance external selector frontier `a=7354673373747273032`;
- Radius-4/local obstruction results;
- Radius 6+ freeze;
- separate Lean formalisation project.

## Prohibited substitutions

Do not replace this target by:

- raw height-cap expansion;
- a larger finite P cone;
- another coordinate system without a weighted decrease theorem;
- generic linear wall-debt amortisation (refuted by RL303);
- blanket P-insertion monotonicity (refuted by RL302);
- unweighted continuation of P/Q grammar merely because it is current.

## Deliverables

Produce:

1. exact complete weighted transition grammar or the first rigorous obstruction to one;
2. coverage proof/prefix partition;
3. cycle-weight or potential analysis;
4. explicit status of `Bcal(8)<=3`;
5. exact contraction, closure, or pivot recommendation;
6. preserved scratch and verifier material sufficient to resume without conversation history.

## Scope

Gate A OPEN.
Gate B OPEN/frozen.
O1 OPEN.
O2 OPEN.
P/8 OPEN.
`Bcal(P)<=1` OPEN.
`Bcal(8)<=3` OPEN.
Checkpoint-8 excess-one OPEN.
Physical/resonance frozen at `a=7354673373747273032`.
Radius 6+ frozen.
Lean formalisation separate.
No global non-trivial-cycle exclusion is claimed.
