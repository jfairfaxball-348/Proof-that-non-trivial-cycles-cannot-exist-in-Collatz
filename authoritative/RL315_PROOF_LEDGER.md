# RL315 proof ledger — promoted mathematics, scratch, and barriers

Date: 2026-09-14
Status: FROZEN WITH RL315 CLOSEOUT
Provenance: mechanically copied from the frozen
`sessions/RL315/RL315_RESEARCH_LEDGER.md` for active-package discovery.  This
copy changes no mathematical classification or scope.

This ledger separates theorem-grade results from preserved but unreplayed
numerical claims so RL316 can recover the inherited state without silently
upgrading scratch to authority.

## A. Theorem-grade results frozen by RL315

1. Universal cyclic length-`a` / weight-`ell` window for every `g>1`.
2. RL311 physical endpoint ratio `<lambda<3` for that reduced balanced return.
3. Canonical multi-layer reduced-shadow/interface normal form.
4. Strict transformed physical state domination by the reduced rational shadow.
5. Odd-shadow-state majorization by distinct genuine odd cycle states.
6. Internal reduced-denominator upgrade `ell>=190537` via RL130's
   positivity/product minimum-element certificate.
7. Exact positive `H`-multiple interface-energy identity.
8. Complete height-one/binary-interface classification.
9. `g=2` cannot be fully active at shift `a`; exact two-row spacing identity.
10. Canonical block level / physical state order-isomorphism in `lambda<3`.
11. Front-loaded fixed-count block numerator/product lower bound.
12. Constant-coefficient block compression and generalized RL206 cokernel
    barrier.
13. Fixed-weight row numerator injectivity modulo `2^a`.

## B. Useful algebra/scratch preserved

- row-energy form of the cofactor identity;
- superlevel/layer decomposition of interface energy;
- order-preserving interpretation: interface rises on shadow-zero bits and
  falls on shadow-one bits, corresponding to moving odd events earlier;
- equality-layer estimate placing reduced shadow and physical minimum in the
  same coarse factor-three scale;
- small-word tests showing bare `H` divisibility has nonrepeated counterexamples;
- small-word tests supporting the interface decomposition and row decoding;
- exploratory numerator upper bounds, cyclotomic-factor projections, and
  3-adic leading-term calculations that did not independently close ownership.

## C. Numerical scratch requiring independent replay

Preserve, but do not cite as authoritative until replayed:

- first reduced internal survivor `(301994,190537)`;
- candidate fibre caps `10,199,842` and later `9,728,182`;
- `56 theta<1<57 theta`;
- low-multiplicity ceiling `710,220,447,737`;
- whole-fibre ceiling about `1.365e12`;
- claimed internal stopping-to-smaller extension to `19,671,092,983`;
- claimed two one-billion-odd-start scan blocks and their stopping-time /
  intermediate-value maxima.

## D. Explicit non-routes / counterexamples

- bare cofactor divisibility does not force repeated reduced word;
- recurrence/gauge compatibility is not an independent ownership consumer;
- bare finite modulus is insufficient;
- canonical flow mass is not optimal transport radius;
- support 3/4/5 grammar should not be resumed;
- fixed-depth residue descent has inherited all-odd-prefix survivors;
- rational fully-active models survive ownership-blind geometry;
- proper-segment denominator ownership remains invalid.

## E. Most promising re-entry points

1. Full `D0H` ownership of the nonnegative interface, not `H` alone.
2. Row-energy small-coefficient/no-carry theorem.
3. `g=2` row reconstruction from `R mod 2^a`.
4. Reproducible finite first-fibre verifier.
5. Separate `g=1` ordinary-`+1` / Bezout route only if interface route stalls.
