# RL166 red-team report

- The `2^71` input retains its inherited conditional external label.
- The witness is a local inequality grammar, never an actual cycle/state model.
- Phase disjointness uses the actual inverse step `p`, not chronological order.
- The result does not replace the full affine closing equality with its one-sided product consequence.
