# RL208 research report — layered root-cone consumption

RL208 resumed the unfinished H21 independent-information task from the exact
RL207 frontier.  The above-p carry algebra remained available, but the root-cone
route was selected first because the inherited noncarry speed law and unique
carry position already supplied a candidate independent geometric consumer.

The carry-free root cone was re-derived from inherited identities rather than
promoting RL207 scratch.  It extends the scoped common-radius inequality from the
old `2^24` windows through `2^35`.  A 64-subdivision dyadic layer partition then
turns the monotone terminal law `K_H21(r)` into exact, disjoint rank exclusions.
Exact rational log signs establish every safe-band boundary; floor-sum modular
counts are independently repeated in the inverse rank-to-phase coordinate.

The resulting exact finite certificate deletes **2,765,120,323** previously
surviving necessary ranks and leaves **13,423,606,911**.  The reduced above-p
source frontier is **7,099,572,324**.  The conservative common-radius consumer is
now exhausted at `2^35`; its safe band there already covers the entire inherited
rank core.

This is strong finite narrowing, not H21 physical incidence and not Gate/global
closure.  No eta class or terminal state/sign/valuation is selected.  The next
best target is the genuine above-p independent consumer on the reduced frontier,
with sharper pointwise root geometry as a secondary route rather than repeating
the saturated common-radius calculation.
