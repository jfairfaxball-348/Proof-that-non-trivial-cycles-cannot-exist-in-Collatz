# RL229 research report

Date: 2026-09-01.

RL229 attacked the physical H21 incidence / ownership / exhaustive-charge bridge rather than
continuing the e=4 endpoint traversal.

The historical physical side is stronger than the recent arithmetic handovers alone make obvious:
RL184 already forces a huge population of physical nonzero defects, and RL187 already solves the
first-defect ownership/multiplicity problem for clean starts. The obstruction occurs at the next
interface. The binding height-21 `{33,34,35}` charge condition is independent of terminal rank.

This yields a new exact route theorem: the current H21 charging polytope contains duplicate copies
of the same inequality `2x+y<=2^-22`. Deleting isolated admissible H21 ranks cannot change that
polytope until the last dangerous rank is removed. The current e=4 terminal rank is still inside
the dangerous rank core and has a nonempty arithmetic candidate family, so the inherited charge
plateau persists exactly.

This makes the missing bridge precise. Future H21 work must either attach quantitative physical
mass to rank classes, eliminate the whole dangerous H21 support family, or be bypassed. Merely
proving another terminal rank impossible is mathematically useful but gives zero leverage to the
current physical charging LP.

Per the RL227 fallback rule, RL230 therefore pivots to a direct owned-macro / chronological
strict-excursion high-branch consumer using the already-forced physical defect blocks.

No frontier or closure status changes.
