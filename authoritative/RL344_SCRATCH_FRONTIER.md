# RL344 scratch frontier

Date: 2026-09-17
Status: FROZEN NON-AUTHORITATIVE SUPPORTING FRONTIER

## Endpoint-cylinder scans

Using the exact last-defect identity
`E=2^(t+1)j-1`, `j` odd, deterministic accelerated-Collatz scans were run for successive terminal-tail cylinders below the inherited terminal-60 class.

Scratch results reached through `t=50` with no unresolved endpoint in those scanned cylinders. Several ranges had digest or independent implementation checks during the interactive session, but the complete aggregate did not receive a single closeout-grade independent red team. Therefore the contraction `t<=49` is NOT PROMOTED.

RL345 may reconstruct and red-team this material if useful, but Phase 4 has higher priority.

## Phase-5 short-return scratch

A record-margin quotient was derived for complete returns with `L<75`. For endpoint-forward valuations `d_r` and `G_r=sum d_i`, candidate words satisfy explicit Beatty-prefix ceilings.

Small-length endpoint-forward gap words found:
- L=2: `(1,2)`, `(1,3)`
- L=3: `(1,1,3)`, `(1,2,2)`
- L=4: `(1,1,1,3)`, `(1,1,1,4)`, `(1,1,2,2)`, `(1,1,2,3)`, `(1,2,1,3)`

A small-state DP was also used to represent the full short grammar, and lengths 48..74 were observed to have total gap at least 76, implying fixed-word band-endpoint uniqueness by the RL343 band width.

These short-return calculations were done before the user corrected the required phase ordering. They are intentionally frozen as scratch and must not be treated as an RL344 promoted certificate.

## Strategic note

Do not resume raw terminal-cylinder doubling or short-return counting before finishing Phase 4. The strongest current mechanism is the Phase-4 two-ended singleton interface:
75-gap endpoint suffix + 24-gap source prefix + exact decorated boundary tags -> at most one physical middle lift.
