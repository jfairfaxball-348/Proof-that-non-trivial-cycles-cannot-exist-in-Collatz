# RL312 — short owned balanced-return consumer / controlled-multiplicity closure target

Date prepared: 2026-09-13
Status: PREPARED, NOT STARTED
Session type: MATHEMATICAL EXECUTION CONTINUING RL311 GLOBAL COMPRESSION ROUTE

## Absolute objective

Continue toward the actual theorem:

> Prove that a positive non-trivial Collatz cycle cannot exist.

RL312 starts from the strongest exact RL311 compression result. Do not reset automatically to an older local programme.

## Authoritative incoming state

Read in order:

1. `AGENTS.md`;
2. `docs/RL_RESEARCH_PROTOCOL.md`;
3. `docs/RL_STATE_MACHINE.md`;
4. `sessions/RL311/RL311_CLOSEOUT.md`;
5. `sessions/RL311/verify_rl311_closeout.py`;
6. exact historical sources named by RL311 only as needed.

Treat the repository as authoritative. Preserve every RL311 correction/demotion and scope qualification.

## Primary inherited object

For every hypothetical primitive positive cycle in the active one-sided sector `lambda<3`, let

- `R=min C`, `M=max C`;
- `A,L` be the full shortcut length and odd count;
- `A=ga`, `L=g ell`, `g=gcd(A,L)`;
- `z=2^a/3^ell`, so `z^g=lambda`;
- `d,o` be the physical `R->M` arc length and odd count;
- `kappa_ext=Ao-dL`;
- `Q_ext=(4M+1)/(4R+1)`;
- `h=ceil(log_3 Q_ext)`.

RL311 proves:

`0<=E_j<=h`

for every canonical gcd-block level

`E_j=K_j-j ell`.

Therefore:

`boxed: g<=h+1`

or there exists a proper pair of canonical block cuts `j<k`, with `m=k-j<=h+1`, such that

`E_j=E_k`.

For that pair:

- the physical segment has exact counts `(ma,m ell)`, hence exact reduced global slope;
- both endpoints are genuine rotations of the same primitive full-D cycle;
- no local-denominator ownership is asserted;
- the endpoint states satisfy
  `1/lambda < (4x_k+1)/(4x_j+1) < lambda < 3`.

RL311 also proves, in the same sector:

- `0<=P_i<=2h` for the canonical extremal one-sided profile;
- `h-2 < kappa_ext/A < h+1`;
- `g|kappa_ext`;
- `z-1 >= 1/[3^h(4R+1)]`.

These are supporting constraints, not closure by themselves.

## Primary target

Consume the RL311 dichotomy by proving one of the following genuinely parent-near statements.

### A. Short owned balanced-return contradiction

For the proper equal-level pair, use genuine full-D ownership, quotient-layer integrality, weighted rotation difference, or another independent physical invariant to prove a contradiction or a bounded owned witness already excluded by Radius 3/4/5.

The consumer must use information not contained in the RL20 block coboundary or RL206 recurrence-compatibility class.

### B. Controlled-multiplicity closure

Prove that the complementary branch

`g<=h+1`

is impossible, finite after an independent arithmetic/global restriction, or forces a bounded owned witness.

Do not mistake `g<=h+1` alone for finiteness because `h` remains unbounded.

### C. Unified consumer

Find one ownership-sensitive inequality that applies both to the short balanced pair and to the controlled-multiplicity branch and yields a direct contradiction or exhaustive bounded reduction.

This is preferable to developing two unrelated local grammars.

## Required red teams

Every candidate must pass all of these:

1. **No proper-prefix ownership leap.** Global `D|Q` does not imply ownership modulo the denominator of the balanced segment.
2. **No recurrence/coboundary repackaging.** RL206 classifies broad additive compatibility and fixed-arc bounded rational residual families as insufficient.
3. **No branch-to-global promotion without coverage.** The RL311 dichotomy is the coverage statement; consumers must handle both outputs or explicitly close one.
4. **No fixed-radius escalation without an encounter theorem.** Radius 6+ remains frozen.
5. **No finite-depth grammar as a substitute for a uniform lift.** If a bounded search is used, prove why the RL311 `h`-dependent family reduces to that bound.
6. **Preserve checkpoint-9 correction.** RL311 does not prove `E_j<=floor(kappa_ext/A)`; only the corrected bound and the independent `E_j<=h` may be used.
7. **Do not resurrect the demoted block strip-width lower bound.** A canonical block need not contain a `1`.

## High-value exact interfaces

Legitimate tools, when hypotheses match exactly:

- RL19 arbitrary-rotation weighted difference and population/state packing;
- full-D cyclic numerator ownership and quotient-layer ordinary `+1` physicality;
- RL20 balanced-return / block-coboundary formulas, with RL206 correction applied;
- RL274 determinant/discrepancy identities;
- established Radius-3/4/5 local theorems only as consumers of a proved eligible owned witness;
- RL311 `U_i=q_i(4x_i+1)` monotone coordinate, determinant-density identity, and profile/block-height bounds.

## Frozen routes

Do not automatically resume:

- complementary `S_RM>=ln3` skew-amplification route;
- Gate-A `Y=(6,504)` / high-index A/F ancestry route;
- fixed-96 P/Q commutation;
- H21;
- Radius 6+;
- raw finite Farey enumeration;
- quotient gcd/content or fixed-prime residue programmes;
- selector scans;
- proper-prefix ownership from factor divisibility.

They remain frozen resources/barriers unless RL312 derives an exact new splice.

## Checkpoint discipline

At every meaningful checkpoint state:

`PARENT_DIFFICULTY_DELTA = EASIER | LATERAL | HARDER`.

`EASIER` requires a real contraction of the parent global arrow: closure of one RL311 branch, a genuinely independent consumer, a bounded owned extraction, or another exhaustive all-scale reduction.

If two consecutive meaningful RL312 checkpoints are LATERAL/HARDER, freeze the balanced-return route and step back to the universal parent obstruction instead of growing a local grammar.

## Success ladder

Full success:
- contradiction for every hypothetical positive primitive non-trivial cycle, or a universal owned witness already excluded.

Strong partial success:
- eliminate one side of the RL311 dichotomy with an independent theorem while strictly simplifying the other;
- or prove an exhaustive finite/controlled reduction independent of `h`.

Insufficient success:
- another lower bound on discrepancy;
- a selected balanced pair with no consumer;
- fixed-depth cases whose depth grows with `h`;
- a recurrence identity in new coordinates.

## Scope

Gate A remains open and frozen as fallback.
Gate B remains open.
Global non-trivial-cycle exclusion remains open.
No Collatz conjecture claim is made.
Lean formalisation remains a separate project.
