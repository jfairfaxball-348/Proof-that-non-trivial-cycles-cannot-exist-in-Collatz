# RL96 SESSION STATE AND RL97 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open globally;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

The first-Farey/full-phase branch is narrowed further but remains branch-specific.

## Incoming RL96 gate

- outer RL96 SHA-256: PASS;
- incoming internal manifest: 69/69 PASS;
- inherited RL95 fast verifier suite: PASS;
- GitHub incoming provenance commit: `34cac3e8a47435a6b0014bc57e46a4e8e3ab3626`.

Verification economy applied. No recursive historical expensive verification was performed.

## Frozen RL96 exact tiers

### Wide inherited tier

`d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`.

### Middle RL96 tier

New exact scan `r=3,800,001..3,950,000` has minimum

`7,500,036 at r=3,871,407`.

The inherited combined minimum remains

`7,500,034 at r=1,635,300`.

Effective tier:

`d<=7,500,032`, `r<=3,950,000` => successor `<=7,500,053`.

### Deep RL96 tier

New exact scan `r=2,200,001..2,400,000` has minimum

`10,000,037 at r=2,216,053`.

The inherited combined minimum remains

`10,000,034 at r=378,722`.

Effective tier:

`d<=10,000,032`, `r<=2,400,000` => successor `<=10,000,053`.

### Ultra RL96 tier

New exact scan `r=605,001..870,000` has minimum

`15,000,038 at r=868,107`.

The inherited global minimum remains

`15,000,037 at r=575,974`.

Effective tier:

`d<=15,000,035`, `r<=870,000` => successor `<=15,000,053`.

No depth-floor repair is required in RL96.

## Frozen final four-tier support

`mu=1/7,000,001`

`lambda=4,600,000/52,500,238,500,033`

Active corners remain

`A=(0,7,000,001)`

and

`C=(7,500,033,2,400,001)`.

Other corners:

`B=(5,000,031,3,950,001)`

`D=(10,000,033,870,001)`

`E=(15,000,036,0)`.

Exact feasible pair supports remain `A-C`, `C-D`, `D-E`, and `A-C` is optimal over the promoted RL96 interval and at the first failure.

`floor(lambda*C)=3,693`, so `b=k-1` remains the worst block-count stratum.

## Frozen RL96 interval result

Every odd

`2,921,766,551 <= k <= 2,921,774,729`

is impossible on the exact first-Farey/full-phase Gate-A branch.

This removes `4,090` additional consecutive odd values.

At `k=2,921,774,729` margin is `+11,013`.

At

`k7=2,921,774,731`

margin is `-3,972`.

Updated branch-specific surviving odd window:

`2,921,774,731 <= k <= 42,150,931,559`, `k` odd.

## Frozen geometry/coupling state

At `R_d=2,400,000`, the final `A-C` support requires

- `R_m>=3,933,328`; actual `3,950,000`;
- `R_u>=866,674`; actual `870,000`.

For `R_d=2,425,000`, it requires

- `R_m>=3,949,995`; actual already `3,950,000`;
- `R_u>=900,007`.

For `R_d=2,450,000`, it requires

- `R_m>=3,966,662`;
- `R_u>=933,341`.

The ultra depth-axis remains nonbinding through deep radius floor `3,499,992`.

At the final support the `E`-axis ultra-depth minimum is `11,413,095`, leaving reserve `3,586,940`.

## Conservative successor charge

Retain

`L_common=15,000,053`.

No tier-specific population theorem was proved in RL96.

## Authoritative RL97 live obligation

Attack `k7=2,921,774,731` with the corrected coupled four-tier cascade beyond deep radius `2,400,000`.

Immediate priority:

1. extend ultra `870,001..905,000`;
2. recompute the ultra global minimum immediately;
3. extend deep `2,400,001..2,425,000` (no middle extension is geometrically required for this single step if floors hold);
4. rebuild and re-optimize all exact supports;
5. propagate the odd interval immediately;
6. continue to the next coupled checkpoint: middle at least `3,975,000`, ultra at least `935,000`, deep `2,450,000`;
7. continue through adjacent batches while productive, with live floor and edge-crossover checks after every material move.

## Sustained attack rule

A theorem, endpoint elimination, floor event, or improved bound is a checkpoint rather than a close trigger. Continue through coupled frontier propagation, support-edge changes, floor changes, constant sharpening, adjacent regimes, and red-team while the route remains productive and reliability is not threatened by compute/context pressure.

## Verification economy rule

After the current bundle checksum, internal manifest, and fast verifier suite pass, accept this frozen proof-state ledger as the authoritative inherited state. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event. Prioritize new mathematics on the stated live obstruction.
