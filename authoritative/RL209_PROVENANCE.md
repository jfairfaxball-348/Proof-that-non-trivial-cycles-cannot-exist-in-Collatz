# RL209 provenance and verification economy

BASE_HEAD at RL209 startup/closeout:
`a080622ce3d020c249b71c8b12552361601810de`.
Incoming authoritative tree:
`159e625b25c7cd9aadf6f53f03e0a63643906c32`.

The RL208 handover records passed transport reconstruction, outer checksum,
internal manifest, clean-unpack verification, exact layered-root certificate and
proof-state guard.  RL209 accepts unchanged RL208/RL206 inherited mathematics by
exact Git identity under verification economy.  No recursive historical replay
was needed; the only targeted provenance reads were the controlling RL201/RL203
endpoint/barrier texts already preserved under authoritative provenance.

The new RL209 verifier uses rigorous fixed-point logarithm enclosures, exact
modular floor sums, and no floating-point sign decision.  It certifies the
7,741,040 disjoint rank deletion, the exact 13,415,865,871 remainder, and the
41,412-case scoped signed-successor barrier.  Decimal arithmetic is used only to
propose monotone crossing candidates; every crossing used by the certificate is
confirmed by outward integer log intervals.

Connector closeout uses documented lossless transport.  The immutable incoming
state is pinned by BASE_HEAD and the full incoming authoritative tree in
`transport/INHERITED_GIT_OBJECTS.json`; inherited subtrees/blobs retain their Git
identities.  The new RL209 overlay is ZIP-packed with an internal SHA256 manifest
and outer sidecar, fresh-unpacked locally, and the current verifier/proof-state
guard rerun there.  Repository transport stores that exact ZIP as numbered
base64 parts plus a reconstruction script and per-part hashes.

The final successor authority is assembled from the pinned inherited objects and
the verified RL209 overlay in one Git-object tree/ref transition.  Knowledge
catalogues remain unchanged and are **stale/deferred**.
