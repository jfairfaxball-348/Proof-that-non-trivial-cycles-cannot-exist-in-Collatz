# RL175 — corrected p-shift flow consumer target

Start from the verified RL174 repair and quantitative physical floor.

For the coprime `g=1` first-survivor branch, the physical accelerated p-shift
functional is

`F2=sum_i q_i(2^(G_i)-1)=3(lambda-1)(y_p-y_0)`,

not the superseded `sum q_i(3^(-G_i)-1)` functional.

Seek an exact upper bound, decomposition, or incompatibility for `F2` from the
transported height profile, p-arc content, chronological affine structure, or
the single dense closure condition that is genuinely independent of the
identity itself.

Priority split:

1. `h_p=0`: sharpen the inherited internal window `F2<2/3` toward the internal
   physical floor, or prove a precise barrier.
2. `h_p>=1`: consume the forced lifted defect `E_p=1+Lh_p` using population,
   rank, or p-arc information without falling back to the already closed
   individual/chain-capacity routes.
3. Separately test the stronger conditional floor `F2>1/176` under the
   inherited external `m>=2^71`, clearly retaining its external status.

Do not reopen fixed-radius stacking, unordered valuation statistics,
same-root elimination without independence, or RL173's corrected-away base
mismatch. No global closure may be claimed unless all inherited obligations
are discharged.
