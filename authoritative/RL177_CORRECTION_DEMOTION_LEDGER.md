# RL177 correction / demotion ledger

No inherited theorem is newly demoted or repaired in RL177.

Retained corrections and scope locks:

- RL173's accelerated interpretation of the `3^(-G)` weighted functional
  remains demoted to an auxiliary functional only.
- RL174's corrected physical p-shift
  `G_i=S_{p+i}-S_p-S_i` remains authoritative.
- The physical identity remains
  `F2=sum q_i(2^(G_i)-1)=3(lambda-1)(y_p-y_0)`.
- RL177's exact local automaton certifies only feasibility under the listed
  local rules; it is **not** promoted as construction or existence of a
  physical cycle type.
- The external `m>=2^71` minimum remains externally qualified and is not used
  in any new core RL177 theorem.
