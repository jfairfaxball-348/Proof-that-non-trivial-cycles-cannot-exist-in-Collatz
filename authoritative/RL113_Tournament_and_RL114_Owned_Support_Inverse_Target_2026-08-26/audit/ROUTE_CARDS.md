# RL113 route cards — required tournament record

All cards use the gap-free inherited primitive-necklace range `A=8..17` (1,580 radius-four necklaces; 27,866 closest pairs; zero `D`-divisible pairs) only as a geometry/ownership red-team baseline. There is no observed nontrivial owned input in that range, so no card treats it as a positive test or theorem.

| Attack | Theorem target and registry input | Mixed-base / universality analysis | RL20 and RL79 outcome | Primitive test and one proof obligation |
|---|---|---|---|---|
| Owned inverse | Classify selected two-/three-term near-extremizers to force `R<=3` or `0<|S|<D`; uses RL109.3 and RL110 barrier. | Uses ordered `2^a3^b` support and ordinary full numerator divisibility, not a generalized-map decision method. | RL20 fails `D|Q`; retain unscaled `D|S`, so RL79 does not supply the premise at `s!=1`. | `A=8..17` baseline plus RL20; prove the canonical ordered-support congruence. |
| Fourier discrepancy | Character bound for selected owned support with strict mass loss; uses RL109.3/RL110. | Characters must see simultaneous dyadic/triadic exponents and full ownership; no claim for arbitrary maps. | Must reject RL20 at ownership; must not divide generalized `s`. | Same baseline; define the ownership-sensitive character statistic. |
| Finite transducer | Sound finite quotient with synchronizing defect excluding owned `R>=4`; uses parity-word identity and RL109. | Carries binary position, ternary rank, and residue data, special to ordinary numerator arithmetic. | RL20 must enter a non-owned reject state; RL79 has no transition-preserving cancellation. | Same baseline; prove state space stays finite with variable `D`. |
| S-unit legality | Explicit legal three-monomial gap beyond a slope bound; uses RL109.3 and RL49/RL100 only if scoped. | Couples `2^a3^b` legal coordinates, not bare `log 2/log 3` approximation. | RL20 must fail the legal owned premise; retain unit increment in divisibility. | Same baseline; isolate a coordinate restriction absent from generic S-unit estimates. |
| Modular expander | Selected-support expansion modulo chosen primes conflicts with `D|S`; uses RL109.3. | Simultaneous orders of 2 and 3 are the contact; selected ownership keeps it non-universal. | RL20 fails before collapse; generalized `s` changes the congruence and is not cancelled. | Same baseline; prove deterministic expansion for rotation-selected supports. |
| p-adic transfer | Finite conditioned owned-fibre operator has dangerous-cone escape; uses full ownership and RL109. | The fibre must condition on ordinary `D|Q`, not random parity or a generalized map. | RL20 excluded from fibre; RL79 has a different `s`-fibre. | Same baseline; define finite-level operator and stationary law exactly. |
| Entropy rigidity | Owned `R>=4` event gives entropy deficit; uses RL109 only after new ownership input. | Radius alone is binary geometry, so mixed-base contact is currently missing. | As stated it risks accepting RL20; RL79 also has no unit-sensitive consumer. | Same baseline shows no owned evidence; first prove ownership creates entropy loss. |
| Geometry transference | Exact lattice forces forbidden second relation; uses RL109.3 and may consult RL77 barrier. | Must encode exponents and full numerator ownership, not the ordinary slope lattice. | RL20 must violate a lattice premise; no normalized `s` identity allowed. | Same baseline; construct the exact ownership-bearing lattice. |
| `+1` cocycle | Non-homogeneous identity bounds selected support; uses RL79 barrier and RL109. | Uses absolute affine increment and dyadic/triadic numerator relation, special to ordinary map. | RL20 must fail ownership; expression must be false or quantitatively different for `s!=1`. | Same baseline; exhibit the identity and independent size force. |
| Symbolic specification | Owned zero-flow blocks cannot extend primitively; uses RL109.2. | Presently local symbolic data lacks mixed-base/full-ownership contact. | A local version accepts RL20 and survives RL79, so it is demoted. | Same baseline; derive a `D|Q`-dependent extendability condition. |
| Mahler/automatic | Rotation family functional equation plus 3-relation forces periodicity; uses full numerator identity. | Must retain ordinary numerator ownership; automaticity alone is base-2 only. | RL20 must fail the full equation’s ownership side; `s` must not factor out. | Same baseline; write the genuine ownership-preserving functional equation. |
| CEGIS strip | Bounded invariant discovered computationally then proved; uses RL110 physical-strip need. | Candidate must encode `2`/`3` data and ordinary ownership; solver output is never theorem evidence. | Require explicit RL20 reject and `s!=1` failure. | State exact range before scan; prove inductive invariant symbolically. |

The source registry labels are used only at their carried scope. In particular, no card converts a finite test, an automatic construction, a transfer heuristic, or a Raw/Farey statement into a global theorem.
