# RL114 target — owned support inverse theorem

## Objective

Work only on the top-ranked surviving RL113 route. Let `u,v` be the canonical zero-flow-cut closest owned rotations from RL109, with `R_*=R>=4`, and write `S=Q(v)-Q(u)=sum_(j=1)^R epsilon_j 2^(a_j)3^(b_j)`, where `D=2^A-3^L`, `D|Q(u)`, `D|Q(v)`, and hence `0!=S`, `D|S`.

Prove a genuinely owned support-selection statement. Full success forces either `R<=3` or `0<|S|<D`. A useful intermediate result is a rigorous classification of two- and three-term near-extremal subconfigurations of the ordered canonical support that forces a repeated rotation, forbidden period, or another primitivity violation.

## First mandatory lemma

Define a near-extremal subconfiguration precisely, including signs, ordered swap coordinates, and scale relative to `D`. Prove: if such a two- or three-term configuration occurs in an owned canonical support and accounts for the largest permitted coefficient mass, then `D|Q(u)` and `D|Q(v)` force a specified support congruence incompatible with distinct primitive rotations.

The thresholds and conclusion are not inherited. RL114 must establish them. A statement involving only `D|S` is inadequate unless canonical support order supplies an argument unavailable to arbitrary legal sparse sums.

## Mandatory red teams

1. RL20: state where `D∤Q` prevents the argument; a conclusion also true for the local fake is rejected.
2. RL79: retain unscaled ordinary divisibility and never cancel a general increment factor.
3. RL81: identify actual physical rotations before treating `Q/D` as a state.
4. Primitivity: state exactly where distinctness/non-periodicity is used.
5. Scope: label any Raw/Farey restriction as branch-specific.
6. Finite work: label each scan with an exact complete range and never promote it to the inverse theorem.

## Stop and handoff

Record a barrier and stop this route if the strongest proposed lemma follows from RL110 all-legal support bounds, the equality `S=D(x_v-x_u)`, or a normalized generalized-increment identity. On that outcome, promote no theorem and begin the next session with tournament route 2, dyadic–triadic Fourier discrepancy. If all retained routes stop, follow the RL113 return policy rather than reviving a retired route unchanged.
