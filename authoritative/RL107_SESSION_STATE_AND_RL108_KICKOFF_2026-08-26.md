# RL107 session state and RL108 kickoff

RL107 closes the odd-core-collision ownership candidate as an exact physical
classification and route barrier.  For maximum-rooted internal first-Farey
boundary states, equal odd cores are equivalent to an aligned all-even block
run.  RL106 therefore bounds each collision class, but the guaranteed number
of distinct boundary odd cores is only `1` for `g=2,3` and `2` for `g>=4`;
there is no growing odd-product population.

The closeout also freezes a secondary ordinary equality interface: a dyadic
physical maximum makes the two-odd-step roof predecessor an LTE blue-comb
basin node, but roof grammar, raw counts, and bare inverse-cylinder minimality
do not force that equality.  Same-count local cylinders with leading `11`
have least even representatives `512` and `1106`.

Two unpromoted checkpoint-draft defects were repaired before promotion: the
`g=3` class-count endpoint and the missing maximum-rooted argument identifying
the all-even cyclic arc.  No inherited authoritative theorem was demoted.  An
interrupted leading-`11` enumeration through length 18 remains NOT PROMOTED.

RL108 must attack the remaining concrete interface: derive a genuinely
word-sensitive **exact physical equality** from the actual first-Farey
maximum-rooted word/boundary structure to a certified ordinary basin node.
The bridge must use information beyond roof grammar, raw counts, residues,
bare cylinder minimality, or quotient proximity, and must pass the RL20,
RL79, RL81, primitivity, raw-multiplicity, scope, and external-input red teams.

Verification economy and the sustained-attack rule remain in force.
