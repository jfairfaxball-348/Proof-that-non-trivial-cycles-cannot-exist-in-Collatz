# RL207 correction / demotion ledger

**No new RL207 correction or demotion.** The mathematical frontier is carried
forward unchanged. Closing an unfinished target is not proof-state demotion.

**RL206-C1**, inherited: first invalid dependency was R20G.12 in the RL20
near-resonant geometry note. The correct increment is
`H_(j+1)-H_j=(z^j/Y)3^(-E_(j+1))Q(B_j)`; strictness requires a block containing
a 1, not merely positive block length. Height geometry, nonnegative increments,
endpoints and total strip width survive. The missing normalization must not
be reused for raw-population bounds. Complete correction is unchanged in
`corrections/RL20_INCREMENT_NORMALIZATION_REPAIR.md`.

**RL206-C2**, inherited: first invalid dependency was section 4 of the RL203
dyadic-prefix proof at commit `51daff87f4661a7f206a60df62492bc3d2d19f57`.
It tested a source rank against a terminal core. Terminal phase is `a+34`.
Replace the invalid 39 / 63 / 100 bounds with **37 / 60 / 97** at the corrected
below-p source scope. Both precision depths still exceed 56; the qualitative
one-sided boundary survives. Source `a=p-37` gives necessary terminal rank
33709842710, not a physical realization. No ranks are restored or deleted.
Complete correction and its independent review are unchanged in
`corrections/RL203_SOURCE_TERMINAL_INDEX_REPAIR.md` and
`reviews/INDEPENDENT_H21_COORDINATE_REPAIR.md`.

Original uncorrected RL203 sources remain immutable provenance. The correction
overlay takes precedence. The original RL206 correction ledger is preserved
at `provenance/RL206/RL206_CORRECTION_DEMOTION_LEDGER.md.txt`.
No mechanical closeout event changes a mathematical classification.
