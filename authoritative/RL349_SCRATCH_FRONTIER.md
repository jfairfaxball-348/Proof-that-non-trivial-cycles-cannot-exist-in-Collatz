# RL349 scratch frontier

Date: 2026-09-18
Status: FROZEN, NON-AUTHORITATIVE

These items are preserved so RL350 can resume without reconstructing the session. None is a promoted
Phase-4 theorem unless explicitly inherited from RL348 or earlier authority.

## A. Exact half-cycle affine identity

For an `L=ell` complete return, inherited authority gives `H=a`. Let directed physical source be
`S`, endpoint be `E`, and for a nondecreasing half-cycle put `D=E-S>0`.

With `X=2^a`, `Y=3^ell`, `D0=X-Y`, the exact affine return equation

`Y E = X S - C`

gives both equivalent identities

`C=D0*S-Y*D`

and

`C=D0*E-X*D`.

Hence

`C/X = (1-1/lambda)E-D`.

This is exact algebra and orientation-safe because it uses only the directed return variables
`S,E`. It does not identify `D` with the signed matched defect `Delta_i`.

## B. Missing directed-contact sign bridge

At a matched contact rank `i`, `d_i=0`, so

`Delta_i=e_i=P_i-Q_i`.

The live missing theorem is to determine, for each possible directed `L=ell` decorated return,
whether

`(S,E)=(Q_i,P_i)` or `(P_i,Q_i)`,

with the exact row/wrap conventions. Only then is `D` equal to `+e_i` or `-e_i`.

Do not infer this from `E>S` alone.

## C. Conditional height-one/telescope diagnostic

A live route tried to combine a height-one interior q-profile with

`q_v(i)=q_u(i)+d_i`

to obtain `d_i<=1` on the crossing-to-root interval, and then consume

`2^(a_i)e_(i+1)=3e_i+1-2^(-d_i)`.

If the required row orientation really does give `d_i<=1` throughout that inherited linear
interval, the additive terms are at most `1/2`, yielding the candidate ceiling

`e_k < ell/(12 log 2)`.

The exact arithmetic certificate shows this would contradict the high-carry floor. The implication
from the live half-cycle profile to the needed rowwise `d_i<=1` statement was not frozen and is the
reason this route is NOT PROMOTED.

## D. Conditional post-root contact lower bound

A live derivation suggested that, once a post-root matched contact is oriented so that `D=e_i>0`,
the positive normalized H-carry telescope from the least-root rank could force a lower bound of the
shape

`D > coefficient * e_k`

with a mechanical coefficient near one, potentially yielding

`D>(n-1)/lambda`.

The exact exponent/row orientation at the contact was not frozen at closeout. Treat the displayed
bound only as a target to rederive after the directed-contact sign bridge.

## E. Historical height-one route

RL146 is available only if RL350 proves its exact global hypotheses:
- ordinary-owned primitive owner;
- multiplicity `g>1`;
- globally binary height word;
- cyclic contact-carry system.

Do not use RL140--RL146 merely from a local half-cycle profile.

## F. Post-crossing over-half source route

Inherited exact facts:
- q=0 support is one strict-late complement interval `[s,e]`;
- `s<=k<=e`;
- `R>=23135982580`;
- if `s<=j`, `R>=61170756170`.

For `j<s<=k`, rederive the residual split at the **lower/source endpoint**. The useful exact owned
identity remains

`A_s=D0 P_s-Xe_s>0`.

RL349 found no contradiction from this positivity corridor alone. A successor needs either:
1. a stronger owned lower/upper estimate for `A_s`; or
2. a bounded-signature theorem tying `A_s,e_s` to predecessor/successor CRT data.

## G. Sub-row class unchanged

For `75<=L<ell`, continue to use:
- RL346 bounded incoming signatures;
- RL347 exact decorated rank/length decoder;
- exact `(L,H)` inverse-carry uniqueness;
- RL348 terminal law;
- RL345 23/72 singleton constraints;
- predecessor 2-adic and successor 3-adic acceptance;
- exact q-record/profile, row/wrap, ownership and descent tests.

Do not enumerate `L`, total gap, or suffix totals.

## H. RL350 first operation

Prove or refute the directed half-cycle orientation bridge in Section B directly from the full
two-row decorated-rank conventions. Keep both directions until the row identity settles them.

If the bridge is proved, immediately retest the conditional telescope routes C and D. If it is not,
freeze the exact two-orientation alternative and seek a sign-free carry contradiction.
