# RL167 session state and RL168 kickoff

RL167 proves that full affine closure at all physical phases propagates from
one numerator divisibility.  In the `g=1` defect branch this is precisely the
already known corrected dense phase condition.  Therefore neither repeated
phase closure nor phase-by-phase integrality supplies the missing
phase-distribution theorem.

Start RL168 from `RL168_LEAST_STATE_PHASE_VALUE_CORRELATION_TARGET.md`.
Preserve all correction/demotion ledgers, the `rho^L=1/2` normalization,
ordinary `+1`, physical/quotient separation, `g=1` scope, external-floor
qualification, RL162 phase order, and the existing barriers to local,
count/product, and same-root-elimination methods.
