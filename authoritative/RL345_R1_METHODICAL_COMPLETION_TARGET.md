# RL345 target — methodically finish the residual-return programme and close R1

Status: UNIQUE LIVE TARGET FOR RL345; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited ownership/pruning, and external conditional least-state floor `m>=2^71`.

## Objective

Work through the remaining RL344 route in strict phase order and prove the exact owned phase-nondecreasing return obstruction `O_75` empty. If successful, the RL343 closed-walk bridge contradicts the parent and R1 closes.

Phases 1–3 are closed methodological stages and should not be reopened absent a concrete contradiction.

## Phase 4 — active and first priority

Use the promoted RL344 singleton-interface theorem:

`0 < P - 3^L E/2^H < 2^37`

for every phase-nondecreasing complete return, together with
`3^23<2^37<3^24`.

A fixed first 24 inverse gaps therefore fixes at most one compatible source `P` once the endpoint `E`, length `L`, total gap `H`, and exact decorated source/end row-phase tags are fixed. RL343's final-75-gap theorem gives at most one endpoint for a fixed surviving suffix. Thus a fixed decorated `(24-prefix,75-suffix,boundary-tags)` interface has at most one middle physical lift.

Finish Phase 4 by:
- constructing the exact finite/compressed boundary-signature representation;
- reconstructing the unique middle lift when it exists;
- applying predecessor and successor mixed 2/3-adic CRT as accept/reject conditions;
- preserving exact row/contact and wrap orientation;
- enforcing physical ownership/pruning;
- iterating the deterministic physical trajectory and excluding any candidate that falls below `2^71`;
- retaining the exact phase-potential sign.

Do not call Phase 4 closed until every long-return interface is eliminated/descending or the first exact unresolved singleton class is recorded with all conditions.

## Phase 5 — only after Phase 4 closes

Treat all complete returns with fewer than 75 inverse gaps.

RL344 contains scratch suggesting an exact Beatty/lattice-path quotient and fixed-word endpoint uniqueness for lengths 48..74. Reconstruct and independently verify before promotion. Then eliminate the entire short class by exact no-lift, phase failure, ownership failure, or deterministic descent.

## Phase 6 — only if still necessary

The RL344 all-depth record theorem already removes much arbitrary-prefix freedom. If a genuine residual remains after Phases 4–5, build the exact repeatable interface and prove the required pumping/carry-drop closure without lossy merging. Otherwise record that Phase 6 is subsumed.

## Success condition

Prove `O_75=empty` under the inherited parent assumptions. Run an independent red team over every finite certificate and all boundary cases. Then close R1.

If closure fails, freeze the first exact unresolved physical class, not a vague profile family or threshold.
