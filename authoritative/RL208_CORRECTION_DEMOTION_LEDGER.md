# RL208 correction / demotion ledger

**No new RL208 correction or demotion.**

**RL206-C1**, inherited and controlling: the corrected RL20 block increment is
`H_(j+1)-H_j=(z^j/Y)3^(-E_(j+1))Q(B_j)`; strictness requires a block containing
a 1.  Do not reuse the omitted-normalization expression for raw-population
bounds.

**RL206-C2**, inherited and controlling: the RL203 source/terminal indexing is
corrected at source scope.  The first necessary below-p source offset is 37,
root-prefix depth is at least 60, and first root-normalization depth is at least
97.  The historical 39 / 63 / 100 values are invalid at that scope.

RL208 does not repair, demote, or reinterpret any inherited theorem or
certificate.  Its new layered root-cone result uses terminal phase
`i=I(r)=pr mod L`; source phase is `a=i-34 mod L` only when reporting the source
split.  Necessary ranks are not physical realizations or charges.
