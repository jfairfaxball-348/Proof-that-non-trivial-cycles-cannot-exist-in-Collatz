# RL237 completed package

Classification: **Success B — coupled recurrence/charge progress, local saturation, strategic freeze**.

RL237 materially advances the repaired sparse-family programme:

- exact H20 `{32,33,34}` spacing `>=210`, cap `654895453`;
- exact H20 `{31,...,36}` spacing strengthened to `>=5597`, cap `24571742`;
- exhaustive 7,531-cell coupled charge lift to ordinary corrected flow `>742.4232`;
- exact identification of the next uncapped H21 `{34,35}` saturation cell;
- a final returnability certificate for that H21 cell: spacing `>=3032`.

The programme is deliberately frozen rather than abandoned. Further charge improvement would require the H21 `{34,35}` occurrence cap `<=6182`, equivalent to direct spacing `>=22242932`; the existing spacing `>=3032` is far short, but its successful recurrence replay demonstrates that the method remains coherent and returnable.

RL238 is **prepared but NOT STARTED**. It suspends this charging programme and pivots to Gate B: reconstruct the already-proved radius-3 theorem as inherited baseline, build/prove the exact radius-4 analogue, and test whether radius 4 admits a materially easier global bridge.

See the theorem, proof ledger, correction ledger, red team, proof state, and `RL238_RADIUS4_GATE_B_PIVOT_TARGET.md`.
