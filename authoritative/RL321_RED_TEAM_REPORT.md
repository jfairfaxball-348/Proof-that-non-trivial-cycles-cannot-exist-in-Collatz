# RL321 red-team report

Date: 2026-09-14
Status: FROZEN WITH RL321 CLOSEOUT

## Ownership

PASS. `alpha`, `beta`, the cut state `z`, antipode `y`, and the complementary return segment are genuine pieces of the assumed ordinary cycle. The canonical remainder `eta` is proved to follow `beta` by integer parity synchronization; `J` is therefore an ordinary integer trajectory endpoint. No RL320 affine surrogate is relabelled as an owned balanced row.

## RL147 mixed-height barrier

PASS. RL321 never assigns independent ownership to RL147's binary height layers. The new proper-factor identities use one genuine prefix and one genuine complementary segment.

## Tail-only residue consumer

BARRIER. The family `beta=1^s0^t` realizes every unit residue modulo `3^s`; the least-state floor does not remove the local family. Therefore `theta mod 3^s`, tail ownership, or tail leastness alone cannot close the branch.

## Negative-corner tail dynamics

BARRIER. The exact `3n-1` dual family with `beta=1^h`, `nu=D=1` survives for all `h>=40`. Corner smallness is not an all-depth height bound.

## Residual mismatch clock

QUALIFIED PASS. `t0=v2(M)<=75` is an ordinary-owned support-independent exit clock. It is not promoted as descent or crossing: a rigorous gap-ratio estimate shows the physical trajectory remains above the residual at the first mismatch.

## External provenance

PASS. Uses of `m>=2^71` remain explicitly conditional. The internal-only and external-certificate-conditional frontiers remain separate.

## Beatty bounded-carry branch

PASS WITH SCOPE. In `Z0<0`, `m=floor(nX/D0)` with `2^29<n<=2^35` under the external floor. This is a bounded arithmetic coordinate, not a complete finite certificate and not permission for a generic support scan.

## Scope correction

PASS AFTER DEMOTION. A proposed all-rank/height-38 bound imported RL319's root-aligned `G<2^35` into the late-row-root alternative. The claim and every consequence are removed from the promoted result set.

## Global claim discipline

PASS. Gate A OPEN; Gate B OPEN; global positive non-trivial-cycle exclusion OPEN; `g=1` separate.
