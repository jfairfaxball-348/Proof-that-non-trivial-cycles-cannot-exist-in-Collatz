# RL175 certified facts and proof ledger

## New proved analytic mathematics

- **RL175.1:** `F2=(q_p^(-1)-1)Q+((lambda-1)/q_p)Q0`.
- **RL175.2:** if `h_p=0`, then `E_p=1` and `y_p` is the unique second-smallest
  physical state.
- **RL175.3:** a physical `h_p=0` profile has neither zero nor exactly one
  positive-height phase.
- **RL175.4:** in the physical `h_p=0` branch, `R-Q>5/8`.
- **RL175.5:** in that branch, `F2<1/2-Delta/16`.
- **RL175.6:** conditional on inherited `m>=2^71`, if `H=h_p>=1`, then
  `F2>6,365,000,000(2^H-1)`.

## Exact finite / interval certificates

- `A*p-L*u=1`.
- `5Delta < -d_- < 6Delta`.
- `5theta<1`.
- `Delta>1/1,116,000,000,000`.
- `6^(A-L)<D=2^A-3^L` is certified by exact rational logarithm bounds.
- `3*2^71*Delta>6,365,000,000`.
- Explicit internal display:
  `F2<1/2-1/17,856,000,000,000` when `h_p=0`.

## Inherited correction/demotion state

- RL173's `sum q_i(3^(-G_i)-1)` remains auxiliary only.
- The physical accelerated p-shift functional is the RL174 corrected
  `sum q_i(2^(G_i)-1)`.

## External qualification

- Only RL175.6 uses the inherited external computational minimum `m>=2^71`.

## Global status

No cycle construction or exclusion. Gate A and Gate B remain open. Global
non-trivial-cycle exclusion and Collatz remain open.
