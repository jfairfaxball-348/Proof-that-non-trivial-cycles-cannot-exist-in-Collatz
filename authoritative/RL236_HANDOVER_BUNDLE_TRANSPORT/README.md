# RL236 handover bundle transport

The completed RL235 package is stored directly in Git with tree SHA:

`5c6875cf6ed924627dbaa03f19fbc1a0ef5746a2`.

Run `reconstruct_rl235_bundle.py` inside the completed package to reproduce the deterministic canonical ZIP, then compare it with the outer `.zip.sha256` sidecar.

Canonical ZIP SHA256: `94ba303becdc2c7a81db081f7e1968861e230f5cc3df28c8c88ed631440297e2`.

RL236 is prepared but **NOT STARTED**. Knowledge catalogues remain `stale/deferred`.
