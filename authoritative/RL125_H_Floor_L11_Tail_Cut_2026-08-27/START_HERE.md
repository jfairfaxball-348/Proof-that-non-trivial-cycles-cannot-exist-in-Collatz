# START HERE — RL125 handover

Run `verification/run_fast_rl125_verifiers.sh`, then read the session state and continue with the RL126 target.

Frozen frontier: `L>=11` globally within the inherited primitive ordinary framework; at `L=11`, only `7<=Z<=33` remains.  Gate A/B and Collatz remain open.
