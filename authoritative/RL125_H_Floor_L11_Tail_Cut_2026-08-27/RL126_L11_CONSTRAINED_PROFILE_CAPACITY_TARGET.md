# RL126 target — constrained profile capacity for `L=11`

The only live `L=11` strip is `7<=Z<=33`.  Generate only profiles that can satisfy every one-fibre and CRT capacity inequality, but make the recursion/provenance sufficient to establish gap-free coverage.  Then assess whether the residual supports a compressed exact `D|Q` scan.

Retain the RL20/RL79/RL81, primitivity, Raw/Farey, and exact-certificate red teams.  No global closure is implied by this finite strip.
