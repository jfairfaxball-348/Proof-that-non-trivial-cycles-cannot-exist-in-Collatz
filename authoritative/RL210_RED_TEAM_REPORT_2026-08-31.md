# RL210 red-team report

Date: 2026-08-31. Result: **PASS at the promoted scopes below**.

1. **Lift/carry.** `b_(p+t)=u+b_t` is used only for `0<=t<z=L-p`. The proof
   derives the unique carry from `tB=L-1 modL`; it is not confused with a
   canonical wrap.
2. **No reflected below-p theorem.** No 37/60/97 vanishing argument is moved
   across p. RL210 instead uses the exact above-p endpoint plus p-separated
   actual-word prefixes.
3. **Independence from endpoint relabelling.** The new input beyond the endpoint
   equality is strict growth `S_(i+1)-S_i=a_i>=1`, which truncates the global
   cancellation to 56 anchored prefix positions and yields the first-divergence
   valuation selector.
4. **RL201 barrier.** RL201's local coupled translation left absolute anchors,
   global vertex identifications, closure and full-word moments outside scope.
   RL210's p-separated absolute-prefix equality is in that permitted global
   scope and does not claim the local barrier false.
5. **First-divergence valuation.** At the first mismatch, the smaller power of two
   occurs exactly once because each exponent sequence is strictly increasing;
   later terms have larger valuation. No cancellation can raise it.
6. **Index range.** `b_23=36`, `b_24=38` and strict growth from zero give exactly
   `24<=m<=37` when the smaller first-mismatch exponent is 37.
7. **m=24 certificate.** The verifier exhausts every continuation compatible with
   strict increase and `S_t,S'_t<=b_t` modulo `2^40`; it excludes only the named
   root-smaller orientation. The surviving orientation is not promoted as a
   physical realization.
8. **Small-offset completeness.** All 55 offsets are transported to terminal
   rank. Their phases lie outside RL208/RL209 root-layer regions, and the full
   inherited interval plus isolated/anchor cuts leaves exactly six. The lock
   criterion applies exactly at e=4,16.
9. **No hidden rank deletion.** Prefix restrictions are necessary conditions on a
   hypothetical physical realization, not proven absence of a necessary rank.
   The rank frontier and above/below split are unchanged.
10. **Physical/Gate locks.** No eta/state/sign/valuation selector, physical H21
    incidence, charge, high-branch contradiction, Gate A/B closure or global
    nontrivial-cycle exclusion is claimed.

No new correction or demotion was triggered.
