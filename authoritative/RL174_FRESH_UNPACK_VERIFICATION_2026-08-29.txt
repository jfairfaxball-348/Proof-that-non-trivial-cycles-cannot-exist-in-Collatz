RL174 fresh-unpack verification record

The final outer ZIP sidecar is checked before a clean unpack. In the clean
unpack, every entry in SHA256SUMS.txt must pass and
`python3 verify_rl174_report.py .` must pass.

Expected verifier tail:
RL174 p-shift correction/gap verifier: PASS
RL174 report verifier: PASS

The final bundle was rebuilt before this clean verification; no outer ZIP
digest is self-referenced inside the bundle.
