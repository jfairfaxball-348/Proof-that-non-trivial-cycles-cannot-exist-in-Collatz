# RL122 target — genuinely new global bridge generation

RL121 found no established registry theorem that satisfies the RL113 return policy. RL122 is therefore authorized to search for a **new theorem**, not a renamed formulation of RL114–RL120.

Work in this strict order.

## Priority A — global owned radius-three producer

Seek a fully quantified theorem for an arbitrary hypothetical primitive ordinary cycle that forces `R_*<=3` for an owned pair, or directly manufactures every hypothesis required by the closed primitive/full-`D` radius-three engine.

A valid candidate must use a new ordinary-owned structural input beyond RL109's `D|S` and beyond RL110's unrestricted support geometry. It must fail for the RL20 `D∤Q` fake for the correct ownership reason and must not survive generalized-increment scaling unchanged.

## Priority B — universal Gate-A branch consumer

If Priority A yields no viable theorem, seek a global theorem that either:

- forces the first-surplus data into the exact first-Farey/raw-`g=1` branch;
- reduces all complementary multiplicities/Farey branches to a finite explicitly parameterized set; or
- proves a monotone domination theorem strong enough that the existing branch certificates cover every Gate-A violation.

Do not enumerate more Farey ratios or extend the cascade unless such a universal structural theorem supplies the trigger.

## Priority C — structural physical-strip / full-ownership theorem

If A and B remain blocked, formulate a new global ordinary-owned inequality for actual selected cycle states or selected sparse support. It must give an independent quantitative resource (strip width, absolute size, support mass loss, valuation restriction, or equivalent) that is not merely `S=D(x_v-x_u)`, rotation transport, a normalized coboundary, or generalized-increment covariance.

## Candidate admission rule

Before sustained work, state for each candidate:

1. exact quantified theorem and scope;
2. genuinely new input not present in the RL112 registry or RL114–RL120 stopped formulations;
3. the exact RL20 discriminator;
4. the exact RL79 `s=1` discriminator;
5. physical-state ownership under RL81;
6. primitivity use;
7. Raw/Farey/finite scope;
8. the shortest falsification or proof obligation.

If no candidate introduces a new mathematical input, record that as a frontier barrier rather than relabelling a stopped route.
