# RL324 — zero-carry crossing to parent-level ownership closure

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED

## Absolute objective

Continue the RL323 all-scale ordinary/full-`D` breakthrough all the way to the missing **parent-level ownership link**.

Do not spend RL324 merely sharpening the `77265916075` displacement constant, cataloguing supports below it, or developing another local residual grammar.

The goal is to close the hardest surviving upstream obstruction: turn the forced physical zero-carry crossing into a theorem that removes the surviving ordered `g=2` parent branch, or reduces it to an already-covered genuinely bounded/owned case.

## Frozen RL323 input

For the genuine ordered balanced rows, RL323 proves an actual phase-aligned `H`-carry

`D_t=3^(c_t)U_t-V_t`

with

`D_0<0<D_a`

and positivity absorbing.

At the unique first transition into `D>0`, there is an actual matched rank `j` with

`beta=v[v_j:u_j]`,
`r=u_j-v_j`,
`h=wt(beta)>=2`.

If `P=U_(u_j)` and `Q=V_(u_j)`, the actual tail has

`floor(Q/3^h)=P`,
`0<J<3^(h-1)`,

and with

`z=2^rP+eta`

one has

`0<eta<2^r/3`.

For the complementary genuine `ell`-odd prefix `alpha`, length `p=a-r`, numerator `A`,

`A=D0P+2^p eta`,
`D0P<A<D0P+X/3`.

At the exact first external survivor, conditional on the inherited external least-state floor,

`q=p-ell>=3182833230`,
`r<=77265916075`,
`2<=h<=r`.

The full aligned ownership identities remain

`A_t+3^(c_t)B_t=D0(3^(c_t)U_t+V_t)`,
`3^(c_t)B_t-A_t=H D_t`.

## Primary theorem target

Prove a parent-level closure theorem for every surviving ordered `g=2` configuration:

> the RL323 zero-carry crossing forces at least one of:
>
> 1. contradiction;
> 2. a strictly smaller genuinely owned reduced balanced return;
> 3. an explicit absolute bound on the **original least-root canonical interface** `(r,s,beta)`, not merely on another extracted crossing;
> 4. a genuinely owned bounded witness that is already excluded by a frozen theorem/certificate whose size is independent of the original support.

A proof of any one alternative uniformly in the original support is the intended RL324 success criterion.

## First attack to prioritize

Use the *global position* of the unique `H`-carry crossing relative to the least-root canonical rank.

At the least-root phase the aligned carry specializes to `D=-K`. The crossing is unique. Combine that sign ordering with:

- the surviving RL322 positive canonical branch;
- the RL321 canonical quotient/residual and matched-rank localization;
- the RL323 zero-carry crossing;
- the two genuine complementary `ell`-odd `D0/H` identities;
- least-root minimality and actual physical order.

The desired outcome is a bridge from the extracted crossing back to the canonical least-root interface, or a second genuine reduced return that is strictly smaller.

If that bridge fails for a structural reason, prove the exact barrier and immediately test whether the zero-carry prefix window itself supplies an owned descent/full-`D` factor obstruction.

## What not to do

Do not:

- enumerate `r` or `h` up to the RL323 bound;
- improve the bound for its own sake;
- restart support `3,4,5,...` grammar;
- restart fixed-depth prefix scans;
- use tail residues without the full complementary ownership identities;
- promote `Z0`, `W0`, or any affine factor to a physical row;
- rely on the withdrawn RL323 odd-rank cyclic-wrap claim;
- use the demoted RL321 height-38 transfer;
- reopen geometry-only routes already killed by the RL322 countermodel;
- divert into the separate Lean formalisation project.

## Strategic stop rule

RL324 should stay on the parent-level missing link. If the zero-carry route cannot be promoted to parent closure, freeze the exact obstruction and compare it only against the other frozen parent-near alternatives (root-aligned `kappa=0` / actual crossing, full cofactor ownership, or another genuinely ordinary-owned global mechanism).

Do not allow a failed bridge to turn into an open-ended local parameter programme.

## Scope

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49547666544`.
External `2^71` least-state floor remains conditional.
Gate A OPEN. Gate B OPEN. Global positive non-trivial-cycle exclusion OPEN. `g=1` remains separate.
