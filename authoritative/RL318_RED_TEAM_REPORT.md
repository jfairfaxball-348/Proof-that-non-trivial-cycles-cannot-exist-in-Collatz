# RL318 red-team report

Date: 2026-09-14
Status: FROZEN WITH RL318 CLOSEOUT

## 1. External certificate scope

The Barina computation is an **external finite computational dependency**, not an internally reproduced theorem.  Successors may consume only the frozen static statement `n<2^71 => convergence to 1` under the shortcut Collatz map.

Do not silently replace it by a later mutable website limit or claim the exhaustive run was reproduced locally.

## 2. Internal versus conditional frontier

Keep both frontiers visible:

- internal-only reduced frontier: `ell>=190,537`;
- conditional-on-Barina reduced frontier: `ell>=49,547,666,544`.

The stronger frontier must never be stated as unconditional repository mathematics.

## 3. First external survivor is not excluded by generic packing

At `(a,ell)=(217,976,794,617,137,528,045,312)`, RL310's generic packing inequality has more than a `21.6x` safety margin in the wrong direction.  Do not spend a successor session merely tightening the same generic inequality unless a genuinely new ordinary-owned term is introduced.

## 4. Balanced-cut / minimum-root distinction

The RL315 universal balanced cut is existential among genuine rotations.  RL135's `m<2^75` and canonical-contact gap bounds are rooted at the global least odd state.

No theorem in RL318 identifies those cuts.

Therefore the tempting consequence

`balanced boundary gap G < 2^35`

and the resulting `first row disagreement <=34`

are **not promoted**.  They become valid only after a new cut-identification or transport theorem is proved.

## 5. Cross-content order crossing is not enough

In the RL317 `epsilon>0` branch, the content-`h` physical copy and coprime-content shadow reverse signed order across one balanced row.  A reverse parity mismatch can flip the sign without contradicting integrality or coprimality.

Thus "the two trajectories cross" is structure, not closure.

## 6. RL79 barrier remains binding

Do not use standalone `T_h` moments, products, normalized states, permutation identities, or homogeneous invariants to force `h=1`.  RL317 already identifies these as the inherited RL79 barrier.

Any successor cross-content theorem must use the fact that one trajectory is the scaled ordinary `+1` physical orbit while the other is coprime-content.

## 7. Contact-polynomial scope

RL139--RL142 are valid ordinary-owned obstructions, but their hypotheses are specialized.  In particular, RL140's `g=2` theorem requires a height-one one-deviant-block pattern with at most three changed contact residues.  RL142's consecutive-interface theorem requires enough untouched blocks and does not specialize to arbitrary `g=2` geometry.

Do not claim these historical results already close the RL317 ordered-row branch.

## 8. No local denominator ownership

A balanced proper segment remains a genuine physical return but does not inherit its own local denominator divisibility.  Continue to distinguish full `D` ownership from `D0` and `H` factors exactly as in RL316--RL317.

## 9. Finite-prefix physical lift warning

RL263--RL264 show that many finite-prefix parity/gap congruences are automatic identities along physical affine rays.  A finite mismatch clock or local signed-gap recurrence is not useful unless coupled to an independent global ownership, least-state, packing, or physical-run constraint.

## 10. Successor discipline

Preferred successor route:

1. root the full `g=2` object at the true least state;
2. apply RL135.2 globally (`h_j>=0` at the first external survivor);
3. derive a full-word consequence that can be consumed by RL316--RL317 ownership;
4. only then move information to the balanced cut through a proved transport lemma.

Keep `g=1` separate.  Do not restart support-by-support grammar, the old `a<=22` scan, raw CF arithmetic, or generalized-increment standalone invariants.

Gate A/B remain open; no global Collatz claim.
