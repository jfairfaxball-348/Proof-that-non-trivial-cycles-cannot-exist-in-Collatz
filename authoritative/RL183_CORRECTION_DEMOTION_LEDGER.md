# RL183 correction / demotion ledger

No inherited theorem required correction or demotion during RL183.

Retain all inherited scope locks:

1. `G_i=S_(p+i)-S_p-S_i` is the corrected accelerated p-shift defect.
2. The physical corrected-flow functional uses `2^G`; RL173's `3^-G` functional remains auxiliary only.
3. RL175's sparse ownership resultant is not used as a generic independent gap obstruction.
4. Necessary automata and the new RL183 local height/mechanical templates are one-way filters only.
5. RL168-RL171 generic rank, monotone-chain, universal permutation-budget, and inverse-rank routes are not revived.
6. RL180-RL183 high-branch floors, m bounds, pair-gap facts, suffix ownership, corridor counts, map repetition, and phase-location facts remain internal to `(37,0,23,-1)`.

New route barriers recorded:

- Forward height rise does not bound the complete *preceding* suffix vocabulary because backward height drops are not correspondingly bounded.
- Repeated use of a necessary short affine numerator map is not by itself contradictory.
- Extending the necessary local-template DP to long windows is not a closure-grade substitute for a global physical consumer.
- The mechanical phase split is strong localization but raw compartment capacity still exceeds the inherited shallow quotas.

No frontier, Gate A/B, non-trivial-cycle, or Collatz closure claim is changed.
