# RL295 — canonical cascade wall-debt and P-owner Gate-A target

Date prepared: 2026-09-10
Status: PREPARED, NOT STARTED

## Incoming classification

RL294 closes as

`CANONICAL_CASCADE_ALGEBRA_AND_ALL_DEPTH_CHECKPOINT2_TO_P_BELLMAN_DOMINATION_PROVED`.

Gate A remains open with exact residual

`k>=31`, `k` odd, `H_can<k`.

Gate B remains separate/open/frozen. Fifth selector remains unscanned. Radius 6+ remains frozen. No global non-trivial-cycle exclusion is claimed.

## Why this target is next

RL294 discovered an associative cascade product on canonical K-states,

`(a,A) o (b,B)=(a+b,3^b A+B)`,

which intertwines propagated canonical columns and has the exact area ledger

`DeltaH_composite=sum DeltaH_factors+(m-1)L`.

The RL292 front-door states factor exactly through P and the distinguished seed-boundary/departure states. In particular

`(4,39)=P o R3=P o Z o Z`.

RL294 also proves globally

`Bcal(1,2)<=Bcal(P)+1`.

The remaining obstruction is no longer an undefined owner cone. It is the exact event where a cascade component hits the ballot wall and enters the formal `d=0` rejected tube while another factor keeps the composite trajectory legal.

RL294 scratch contains a promising but unpromoted contraction of `(4,39)` to `(5,191)`, followed by a finite P×P preperiod and an eventual small coefficient tail. RL295 must either prove that structure cleanly or identify its first failure.

## Primary theorem target A — wall-refactorisation calculus

Build an exact finite/min-plus state description for an ordered cascade when a factor reaches formal depth zero.

Requirements:

1. identity factor `I=(0,0)` is quotiented immediately;
2. nonzero formal `d=0` K is retained as rejected-tube/wall debt, not discarded;
3. the transition/refactorisation preserves the exact composite physical state;
4. every debt transfer has an exact historical-area accounting;
5. the construction is invariant under branch mergers and zero-height boundary gauges.

Do not replace the debt with a residue-only scalar potential: RL294 proves that the aligned base-9 carry cannot be absorbed by such a correction.

## Primary theorem target B — close the hard `(4,39)` owner branch

Independently verify the RL294 scratch candidate:

- all nonexceptional propagated-output sectors of `(4,39)=P o Z o Z` are P-owned within the RL292 threshold;
- the unique residual early prefix is `00`, reaching `(5,191)` at source cost 6.

Then analyze

`(5,191)=(3,K=24) o P`.

The scratch candidate isolates a finite preperiod containing

`(5,201)`, `(6,733)`, `(6,949)`, `(6,1273)`

and an eventual coefficient pattern

`... -> 3 -> 2 -> 3 -> 2 -> ...`.

Prove a parametric P-domination theorem for the eventual tail, leaving at most a finite explicit list of wall sectors.

Success here should establish the needed all-depth owner/Bellman inequality for `(4,39)` without a raw height cap.

## Primary theorem target C — propagate across the rest of the front door

Use RL294's exact factorizations

`(2,-17)=B_-4 o B_-6`,
`(2,-84)=B_-19 o B_-28`,
`(3,-28)=B_-4 o P = R6 o B_-13`

and the RL292 thresholds.

Attempt to close each state through the same cascade/wall calculus. Use the new all-depth checkpoint-2-to-P theorem as a terminal sink wherever wall reduction reaches checkpoint 2.

If all four non-P obligations close, make the RL293 conditional implication explicit and reduce the global front door to

`Bcal(P)<=1`.

## Tight P-source target after front-door collapse

Only after the owner/front-door work succeeds, return to the inherited P-source static danger tree.

Use:

- RL293 normalized-K first-boundary corridor;
- high-signed-drift necessary condition;
- dangerous first-checkpoint principal parity-cylinder representative theorem;
- RL292 two static roots `alpha=-1,-2/3`;
- the exact alignment of `alpha=-2/3` with the labelled height-one predecessor ray.

The target remains

`boxed: Bcal(2,3)<=1`.

## Mandatory red teams

Any proposed global owner/wall theorem must explicitly survive:

1. arbitrary positive zero-height boundary retention and the positive trivial cycle;
2. RL292 all-zero static-tree pullbacks with unbounded Beta;
3. RL287 cylinder isometry and RL289 finite-suffix transparency;
4. neutral `(101)` fixed-seed gauge;
5. physical branch mergers/equal-area diamonds;
6. RL283 adjacent-swap valuation jumps;
7. homogeneous `(010)^R` Rank-1 danger towers;
8. shifted phase-reset family and genuine `10922->6146`;
9. RL290 chains `2514->5378->21842->8192` and `1074->1364`;
10. locally legal terminal predecessor families not globally reachable;
11. RL293 immediate-danger family excluded by normalized-K corridor;
12. H<=60 relaxed owner exceptions J=2,3,6 and their genuine repairs;
13. principal-cylinder scope restriction;
14. new RL294 cascade-specific wall gauges:
   - formal identity factors must not accumulate artificial negative area;
   - composition cross-term alone does not pay rejected-tube depth;
   - residue-only scalar carry corrections are impossible.

## Forbidden principal routes

Do not restart as the principal programme:

- raw larger H/k caps;
- larger unstructured P cones;
- finite shifted-valuation/affine-template maxima;
- endpoint-only locally bounded 2-adic potentials;
- residue-only scalar corrections to the cascade;
- support-count or excursion-count charging;
- chosen-history seam charges;
- generic Collatz termination assumptions;
- Gate B, fifth selector, or Radius 6+.

Finite computation is acceptable only as falsification, exact small cut certification, or regression for an analytic cascade reduction.

## Success condition

RL295 succeeds strongly if it proves any of:

- a correct all-depth cascade wall-refactorisation/min-plus theorem;
- the all-depth `(4,39)` P-owner/Bellman domination inequality;
- an exact finite augmentation covering all five RL292 front-door states;
- collapse of the five-state front door to `Bcal(P)<=1`;
- the all-depth first-positive first-d=1 P-owner theorem;
- `Bcal(P)<=1`;
- Gate A.

If the cascade route fails, freeze the first exact counterexample/refactorisation obstruction and the minimal additional ancestry coordinate required.
