# RL161 fresh-unpack review

The transport package must be unpacked into an empty directory.  Verify its
internal SHA-256 manifest and run:

```bash
python3 verify_rl161_report.py .
```

Review the scope controls: generalized scaling must not be mistaken for
ordinary scaling; the barrier applies only to homogeneous interfaces; and no
global cycle-exclusion claim may appear.
