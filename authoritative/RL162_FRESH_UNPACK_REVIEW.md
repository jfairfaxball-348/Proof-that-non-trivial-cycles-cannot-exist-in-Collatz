# RL162 fresh-unpack review

Unpack the ZIP into an empty directory, verify `SHA256SUMS.txt`, and run
`python3 verify_rl162_report.py .`.

Confirm the construction is labelled a valid local grammar only and the
conclusion is limited to chronological-to-phase-boundary transfer.
