# RL264 — two-trajectory low-area lift target

Date prepared: 2026-09-06
Status: **PREPARED, NOT STARTED**

## Incoming promoted state

RL263 closes the proposed selector-independent ordered-prefix weight-deficit route with an exact method barrier.

It proves that the canonical internal path is an exact coordinate on the two physical half-Collatz trajectories arising from the full-phase `N,N+4` swap:

`T_p = 3^(d_p) A_p - B_p`.

It also proves the exact ordered-prefix factorization

`C-S_p = 2^p * 3^(rho-Y_p) * (B_p-3^(d_p-1)A_p)`,

so `S_p==C (mod2^p)` is automatic on every genuine physical full-phase pair.

RL263 additionally corrects the RL262 closeout modulus interpretation: in the inherited odd-`k` scope,

`Dcal = C - 2^m * (1+(N-2)*2^(k-1))/3`,

so the full congruence modulo `2^m` is valid. This does not affect RL262's exact finite selector elimination.

## Unique RL264 objective

Do **not** resume selector-by-selector enumeration as the opening move.

Attack the genuinely independent part of the lift: under a hypothetical Gate-A violation `H<k`, prove or decisively falsify a uniform obstruction for the physical `N,N+4` trajectory pair using the exact canonical lift

`d_p=1+Y_p-X_p`,
`T_p=3^(d_p)A_p-B_p`,
`J_p=T_p+3^(d_p)-2^(d_p)`

together with terminal ownership and the retained resonance/determinant geometry.

Preferred theorem shape:

- a direct lower bound forcing `H>=k`; or
- an exact physical encounter/ownership theorem that manufactures every hypothesis of the promoted Radius-4 local theorem; or
- a finite-state/recurrence obstruction whose state contains genuinely independent absolute physical information and is proved uniform.

Requirements:

1. Do not use the ordered dyadic prefix congruence as if it were independent; RL263 proves it is a telescope.
2. Preserve the exact physical `N,N+4` pair and canonical absolute states.
3. Preserve terminal ownership and the odd-`k` theorem.
4. Radius 4 may be invoked only after primitive/full-D/exact-distance/positive-domain eligibility is explicitly proved.
5. If this route is decisively blocked, freeze the exact barrier before selecting any fallback.
6. Do not begin with a fifth arithmetic selector scan unless the physical-lift route is first closed as a barrier and a return to enumeration is explicitly justified.

Gate A and Gate B remain open. Radius 5 remains inactive.
