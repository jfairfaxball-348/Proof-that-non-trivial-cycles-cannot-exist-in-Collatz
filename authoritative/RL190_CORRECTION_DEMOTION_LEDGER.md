# RL190 correction and demotion ledger

Date: 2026-08-30

## New corrections / demotions

None.

## Frozen scope locks retained

1. The physical corrected defect is `G_i=S_(i+p)-S_p-S_i`.
2. Physical corrected flow uses `2^G`; RL173's `3^-G` functional remains auxiliary only.
3. Necessary terminal-rank intervals, mechanical words, and affine images are one-way filters, not physical realization claims.
4. Generic rank/lattice/inverse-rank barriers from RL168-RL171 remain in force.
5. All RL180-RL190 statements here remain internal to `(37,0,23,-1)`.
6. Total directional K variation is not a chronological prefix excursion without a sign-order theorem.

## RL190-specific scope locks

- The separation-43 proof uses the global RL183 ordinary/common-mechanical transition law after exact verification that all six source ranks avoid the switch and carry exceptions. It does **not** assume a longer `tau<=34` or `tau=35` shallow corridor.
- Spacing `>=46` concerns consecutive extremal `{35,36,37}` triple terminals only.
- Separation 46 is **not excluded**. It is reduced to two unresolved first-terminal ranks: `90789138715` and `101129528126`.
- `N_35<=9168536354` counts the inherited RL184 clean-start population under the same grouping framework as RL188/RL189.
- The dangerous H21 interval `D=[23369453298,41775866136]` is a necessary joint terminal-rank core, not an existence set.
- The charging result retains only the stated monotone two-level no-improvement theorem. It does not rule out finer phase/sign/height charging.
- Ordinary absolute corrected-flow remains inherited `>2787212689/6291456>443`.
- No branch, Gate A/B, non-trivial-cycle, or Collatz closure is claimed.
