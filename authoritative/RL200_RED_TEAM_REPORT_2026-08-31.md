# RL200 Red-Team Report

Date: 2026-08-31. Verdict: PASS for the scoped candidate.

Checks performed:

1. Re-derived the terminal normalized gap `7*3^35/2^21` from the inherited H21 terminal
   invariant and confirmed RL199 orientation does not change it.
2. Re-derived `i=pr mod L`, `Ai=nL+r`, and `rho(r)=2^n/3^i`.
3. Proved strict reverse-rank ordering using rigorous bounds for
   `delta=A ln2-L ln3`, rather than floating-point rank comparisons.
4. Checked the exact upper-corridor crossing between ranks `25583192105` and
   `25583192106`, and checked the old upper endpoint remains above the lower K corridor wall.
5. Recounted all 14 inherited H21 isolated deletions: exactly two fall below the new lower
   endpoint and 12 remain.
6. Recomputed the refined interval size, post-deletion count, and
   `2,213,738,806` newly removed previously surviving necessary ranks.
7. Checked the rank shifts for `tau=34` and the two extra prehistory sources and their mechanical
   compartments.
8. Exhausted powers of two modulo 9 and confirmed exactly two safe exponent classes modulo six
   for every unit target residue.
9. Replayed finite reverse constructions for representatives of all four `eta mod18` classes,
   with physical terminal `nu` and positive odd unit states.
10. Confirmed the reverse-prehistory theorem is explicitly restricted to uncoupled local
    constraints and does not claim compatibility with a global coupled pair word.
11. Confirmed terminal K is eta-blind, so the rank refinement is not mislabeled as a class
    selector.
12. Confirmed no physical incidence, H21 budget release, branch/Gate/global closure, or Collatz
    claim is made.

No correction/demotion event is required.
