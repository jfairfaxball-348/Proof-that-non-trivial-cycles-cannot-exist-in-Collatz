# RL246 target — absolute canonical-state / full-phase coupling of six physical valleys

Status: **PREPARED, NOT STARTED**.

Incoming completed generation: RL245, classification `R4_BRIDGE_REDUCED`.

## Frozen incoming obstruction

Every retained full-phase object simultaneously unresolved by Gate A and Gate B has:

- `beta(P)>=6`;
- exact derivative `P_(i+1)-P_i=u_i-u_(i+q)`;
- every negative P root negative in both genuine doubled physical envelopes at the same root;
- Branch A/B/C explicit valley geometry;
- exact canonical u-driven state automaton from RL64 parity legality;
- every legal `10` descent satisfies `J'=J/2`.

The RL245 binary-height strengthening `h in {0,1}` is demoted. Use only exact `h>=0`, with internal `h=d`.

## Unique target

Use the **absolute full-phase-selected canonical state**, not local u-pattern shape alone, to consume the six common physical valleys.

Seek exactly one of:

1. an eligible primitive full-D exact-distance-4 self-rotation satisfying every RL238/RL239 hypothesis; or
2. an exact contradiction to full-phase extendability, using the canonical `(d,J)` automaton, terminal `J=2^k`, exact full-phase divisibility/phase quotient, or an equivalent exact global-state identity.

A productive route should attach at least one Branch A/B/C valley boundary to a forced even-J event, dyadic descent count, terminal backward-rank digit, or exact phase quotient condition.

Mandatory discipline:
- do not infer a canonical mismatch merely from a local u pattern;
- do not use the demoted global `h in {0,1}` assertion;
- do not treat P or Q as Radius-4 theorem inputs;
- do not promote the RL245 scratch determinant-sector no-Radius-4 conclusion without re-deriving it from exact RL240–RL243 sources;
- no generic packing, generic physical-scale anti-concentration, bare radius shell, or continued-fraction closure;
- Gate A and Gate B remain distinct and open;
- Radius 5 remains inactive unless explicitly activated by future authority.
