# RL322 — canonical residual and bounded-carry consumer target

Date prepared: 2026-09-14
Status: PREPARED, NOT STARTED

## Absolute objective

Continue toward excluding positive non-trivial Collatz cycles from the verified RL321 ordinary-owned late-row normal form.

## Primary target

At the first external survivor, RL321 gives

`z=2^rM+eta`, `y=3^sM+J`,
`0<eta<2^r`, `0<J<3^s`,
`K=3^s(M-m)+J`,

with the physical support localization

`r=u_j-v_j`, `wt(beta)=s`,

and a post-antipode residual mismatch after exactly `v2(M)<=75` phases.

Define

`Z0=XM-Ym`, `W0=Xm-YM`.

`W0>0`; `Z0` is odd.

### Branch A — `Z0>0`

Use the genuine prefix/tail ownership and canonical positive residual to obtain a new ordinary-owned consumer. A valid advance must remove a branch/parameter, bound support independently, or produce a complete exact certificate. Do not merely relabel `(Z0,W0)` as an ordinary balanced return.

### Branch B — `Z0<0`

RL321 proves

`n=m-M`,
`2^29<n<=2^35` (externally conditional),
`m=floor(nX/D0)`,
`r>=41`, `s>=26`,
`1<=2^r-eta<2^(r-40)`.

Exploit this Beatty/carry normal form together with matched-rank support or the <=74 post-antipode exit clock. A generic scan of `n` or of parity supports is not sufficient unless a theorem makes the certificate gap-free and exhaustive.

## Fallback

If neither canonical branch loses a degree of freedom, freeze the late-row residual attack and return to RL320's root-aligned obligation: handle `kappa=0` or connect the positive-carry three-path interface to the actual reverse crossing and inherited suffix inequality.

## Frozen barriers

- no tail-residue-only attack;
- no corner-smallness-only attack;
- no homogeneous generalized-increment invariant;
- no independent ownership of RL147 mixed-height layers;
- no fixed-depth generic prefix scan;
- no demoted RL321 height-38/all-rank claim;
- no use of root-aligned `G<2^35` in the late-row-root branch.

## Scope

Internal-only frontier: `ell>=190537`.
External-certificate-conditional frontier: `ell>=49,547,666,544`.
Gate A OPEN. Gate B OPEN. Global positive non-trivial-cycle exclusion OPEN. `g=1` separate.
