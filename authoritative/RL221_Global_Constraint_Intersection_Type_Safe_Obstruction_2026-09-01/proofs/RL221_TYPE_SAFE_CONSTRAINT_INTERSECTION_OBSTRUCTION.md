# RL221 type-safe global constraint intersection obstruction

Date: 2026-09-01.

## Scope

RL221 works on the modern necessary e=16/H21 arithmetic candidate object inherited from RL217 and unchanged in cardinality by RL218--RL220.  The ambient set is the exact phase-51 family of **139,581,280 arithmetic candidates** over **45,045 live prefixes**.  These are necessary arithmetic candidates, not proved physical H21 incidences and not counterexamples.

For one candidate write the common tuple

`omega = (e, p, r, Q16, k, eta, y0, state, eta mod18, eta mod2187, nu, H_51)`

with fixed global constants

`A=217976794617`, `L=137528045312`, `p=65470613321`,
`u=103768467013`, `K0=2^37`, and e=16 terminal rank
`r=34124151203`.

`Q16` denotes the finite e=16 prefix label.  It must not be confused with the complete-word numerator used in RL214.

## RL221-T1 — admissible-constraint typing lemma

**Classification: proved elementary logic / scope theorem.**

A predicate may be used as a member of a counterexample contradiction core on `omega` only when a proved bridge has the form

`physical counterexample in the scoped branch  =>  C(omega)`.

A theorem whose subject is instead a separately chosen construction parameter (for example a fixed certified-blue seed/word family, an independently pumpable inverse block, or a bounded-star library) is not itself a predicate on `omega`.  It can enter a contradiction core only after an additional proved bridge maps every physical candidate to that construction family.

Therefore the RL219 finite-library dyadic-ray sparsity theorem and the RL220 fixed-block / bounded-star / S-unit route theorems cannot be counted as extra necessary candidate restrictions merely because they rule out proposed blue-family architectures.  Their own frozen ledgers record **zero arithmetic-candidate deletions**.  Treating them as if they shrink the e=16 phase-51 set is a logical type error.

RL220-C1 is different: `v2(3y0+1)=1` is a genuine pointwise property of every current root progression, but because it holds universally on the live family it does not shrink that family.  RL220-C2 is a zero-match screen for one sufficient bridge equality (`y0=b38`), not a theorem that a physical candidate must satisfy that equality.

## RL221-C1 — explicit common-intersection witness

**Classification: exact finite arithmetic certificate.**

The portable verifier independently reconstructs the e=16 prefix recurrence for the displayed prefix, derives its unique unit-preserving root lift, recomputes the two-sided root window, terminal-Hensel condition, and deterministic phase-16 through phase-51 mechanical-height replay.

It certifies the explicit tuple

- `Q16 = 43,079,489`;
- base `eta_* = 94,527,378`;
- base `y0_* = 632,932,199,441,596,415`;
- exact inherited k-window `28,812 <= k <= 36,180`;
- chosen `k = 28,821`;
- lifted `eta = 3,722,043,165,201`;
- root `y0 = 24,921,895,945,404,894,117,887`;
- H21 state `011`;
- `eta = 9 mod18` and `eta = 864 mod2187`;
- terminal orientation uses odd `s=eta` and
  `nu=v2(3^34 s-1)=3`, hence `1<=nu<=21`;
- phase-16 odd state `63,944,214,675,001,842,327,551`;
- all mechanical heights through phase 51 are nonnegative, with minimum 1;
- `v2(3y0+1)=1`.

The verifier also checks the exact prefix/root identity

`2^24 y16 - Q16 = 3^16 y0`

and the inherited affine lift laws

`eta = eta_* + 3^17 k`,
`y0 = y0_* + 3*2^58 k`.

The root lies strictly inside the RL215 two-sided interval

`24,913,843,845,551,577,787,381 <= y0 <= 31,285,589,992,934,194,300,574`.

Thus one concrete `omega_*` simultaneously satisfies the following five modern constraint families:

1. e=16 H21 prefix/lift plus state/orientation congruences;
2. the independent p-arc/complementary-arc two-sided root window;
3. the terminal 2-adic Hensel/valuation restriction;
4. the RL217 prefix-wide phase-51 nonnegative-height selector;
5. the RL220 pointwise first-root acceleration law `v2(3y0+1)=1`.

## RL221-T2 — no contradiction core from the selected inherited predicates

**Classification: proved consequence of RL221-C1.**

Because `omega_*` lies in all five sets, their full intersection is nonempty.  Consequently every subintersection of these five sets is also nonempty.  No contradiction core, minimal or otherwise, can be obtained merely by recombining these already-established predicates.

More generally, the exact RL217 candidate pipeline itself has a constructive consistency witness.  Re-labelling route no-go theorems from RL219--RL220 as candidate predicates does not alter this fact because RL221-T1 forbids that scope change.

This is a theorem-quality **non-closure / obstruction result**, not a theorem that no future independent constraint can close the branch.

## RL221-B1 — smallest missing discriminator exposed by the intersection

**Classification: open obligation made falsifiable; not a proved mathematical claim.**

The first load-bearing datum repeatedly left undetermined by RL214--RL220 is an independent complete-word quotient/return residue.

Let

`D = 2^A - 3^L`

and let `Qfull(d)` denote the complete phase-aligned binary-word numerator, so RL214 gives

`y0 = Qfull(d) / D`.

Bare ownership/integrality gives only `Qfull(d)=0 modD`; it does **not** determine

`(Qfull(d)/D) modD`.

Equivalently, a new argument must determine `Qfull(d) modD^2`, or derive an equally strong independent p/z-arc return numerator congruence, on the same candidate tuple.  Since every live root is

`y0 = y0_* + 3*2^58 k`

and `D` is odd and coprime to 3, such a residue becomes an exact congruence on `k` and is directly intersectable with the finite RL217 windows/cylinders.

The falsification rule is explicit: any claimed new residue theorem must be replayable on candidate tuples; an exact surviving tuple satisfying both the new residue and the RL217 conditions refutes a claimed empty intersection.

## Global conclusion

RL221 proves **no branch contradiction and no Gate closure**.  It deletes zero candidates, prefixes, states, eta classes, or ranks.  The necessary frontier remains **13,415,865,871**.

The gain is structural and exact: the current five-family intersection has a concrete survivor, and the post-RL217 blue-family no-go results are formally separated from necessary candidate predicates.  RL222 should therefore attack the missing full-period quotient/return coupling rather than perform another broad catalogue or retry closed blue-family architectures.
