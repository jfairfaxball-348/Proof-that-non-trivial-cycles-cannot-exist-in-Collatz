# RL152 session state / RL153 kickoff

RL152 establishes the missing reciprocal-depth / positive-depth bridge in the
RL136 isolation range:

- `sum_(j in J) d_j <= g(A-L)`;
- `M_- > Q_- 2^(-4g(A-L)/(3Q_-))` when negative mass is present;
- `W >= 12 sum E_d 2^(-d)-11`;
- `W > 9M_- -11 = 6Q_pre -11`.

This does not exclude a multiplicity because no inherited theorem forces a
useful lower bound on negative population/mass or exact-depth concentration.
That negative-defect dependency is frozen rather than sent back through the
RL149/RL150 barriers.

RL153 should attack the independent `g=1` primitive-owner branch directly.
RL135 gives nonnegative defect for `g=1`; RL146's height-one cyclic order lock
closed only `g>1`, so its repetition contradiction must not be imported into
the one-block case.
