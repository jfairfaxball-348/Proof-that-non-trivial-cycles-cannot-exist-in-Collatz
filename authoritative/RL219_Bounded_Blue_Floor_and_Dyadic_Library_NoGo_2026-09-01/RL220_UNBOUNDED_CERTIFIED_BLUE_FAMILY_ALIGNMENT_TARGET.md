# RL220 target — unbounded non-dyadic certified-blue family alignment

Date: 2026-09-01.

Accept the RL219 closeout and all inherited proof-state/scope locks.

## Why this is the next target

RL218/RL219 have now closed or sharply bounded the obvious blue-recognition architectures:

- fixed seed + fixed word gives only singleton k equality;
- finite fixed-word libraries remain singleton probes even after unbounded dyadic seed scaling, because the current e=16 root band has multiplicative width <2;
- the explicit RL80 LTE comb has zero intersections with the modern prefix bases;
- seed-1 reverse-tree search through depth 86 has zero matches;
- the stable externally verified interval `[1,2^71]` lies below the new physical e=16 H21 state floor and still needs an exact candidate equality/trajectory bridge.

Do not repeat those screens or merely enlarge fixed-seed depth.

## Exact incoming arithmetic interface

The e=16 family remains **139,581,280** phase-51 arithmetic candidates across **45,045** prefixes:

`y(k)=y_*+3*2^58 k`

on exact finite windows, with terminal-Hensel exclusions and the inherited phase-51 union of **3,132,617** disjoint dyadic cylinders (max 25-bit precision).

Exact root band:

`R_MIN=24,913,843,845,551,577,787,381`,
`R_MAX=31,285,589,992,934,194,300,574`.

RL218 backward-word algebra remains binding.

## Primary task

Seek an **unbounded certified-blue family whose essential parameter is not merely dyadic scaling of a finite fixed-word library**, and whose exact endpoint formula can interact with the affine RL k-lattice without degenerating to candidate-by-candidate equality probes.

Priority structures:

1. a rigorously certified arithmetic progression or finite union of progressions of blue integers extending through/above the current root band;
2. a parameterized legal-backward family in which the non-dyadic word shape varies coherently and yields an affine, polynomial, or otherwise symbolically intersectable endpoint formula;
3. a family theorem converting its parameter directly into a non-singleton congruence/class on k after the RL218 pullback;
4. a proof that broad natural classes of such varying-word families necessarily reduce to sparse exponential/S-unit equations, if positive alignment fails.

## Required discipline

Certification of the blue family comes first. Exact same-integer pullback comes second. Density, modular coverage, proximity, legality, and valuation alone remain insufficient.

Do not enumerate the 139,581,280 candidates when a family-level modular or Diophantine analysis is possible. Use the exact root band and factor-two sparsity theorem early to reject disguised dyadic libraries.

## Positive success

Exact deletion of a nontrivial candidate set/k-class/prefix/structural class/rank because the same RL integers are proved members of an independently certified unbounded blue family.

## Useful negative success

A theorem-level classification showing that a substantial natural class of unbounded varying-word blue families necessarily pulls back to sparse exponential/S-unit singleton equations, together with a precise statement of what stronger additive/affine basin theorem would still be required.

No Gate/global conclusion without an actual contradiction.
