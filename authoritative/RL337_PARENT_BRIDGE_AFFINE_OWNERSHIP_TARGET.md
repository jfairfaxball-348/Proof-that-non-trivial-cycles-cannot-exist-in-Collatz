# RL337 target — all-length affine ownership or genuine descent

Status: UNIQUE LIVE TARGET FOR RL337.

Work in the inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent branch at `(a,ell)=(217976794617,137528045312)`. Maintain the conditional least-state floor, `z<=35`, `n<=32546278588`, RL335's p<=4 physical exclusions, and RL336's q=32 all-length theorem.

Primary objective: prove a support-uniform affine/exact-state return identity, ownership invariant, or genuine descent that closes this ordered parent branch. A numerical q improvement alone is not closure. Any proposed argument must survive concatenation and arbitrary positive-run length, and must distinguish physical realizations from the conservative graph's anonymous fallback edges.

Do not promote the RL336 q=33 diagnostic. It encountered a physical p=5 pair `(24,27)`, so a broad high-pair exclusion is false at that threshold. Avoid a p-by-p or decreasing-zero-run census as the main route. Retain all roadmap and global-scope qualifications.

Success criterion: a theorem-level contradiction, owned descent, absolute canonical-interface bound, or equivalent R1 parent closure theorem with exact dependencies.
