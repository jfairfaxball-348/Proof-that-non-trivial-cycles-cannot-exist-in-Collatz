# RL73 SESSION STATE AND KICKOFF

Date: 2026-08-24

## 0. Authority and scope

**AUTHORITATIVE OUTGOING RL73 RESEARCH STATE.** Gate A remains open globally, Gate B remains open globally, and no RL/Collatz closure is claimed.

RL73 continues the RL72 global-audit roadmap and proves a new ownership-sensitive localization theorem: every hypothetical Gate-A violation with odd `27<=k<=165` must contain a post-first-mismatch maximal height-one synchronized block with at least

`40,249,491,324,522,944`

aligned `00` columns.

The detailed proof is in

`RL73_OWNERSHIP_FORCED_ZERO_AREA_MACRO_AND_LOW_K_LOCALIZATION.md`.

## 1. Verification economy rule — retained

After the current bundle checksum, internal manifest, and fast verifier suite pass, accept the frozen incoming proof-state ledger as authoritative. Do not recursively re-audit historical bundles or rerun expensive inherited finite certificates unless a new load-bearing argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers stop-and-repair.

RL73 used GitHub selectively for RL64/RL65/RL67/RL71 and RL49/RL50 exact interfaces. The old phase continued-fraction arithmetic was rerun only in a small exact verifier because the new theorem depends load-bearingly on the safe reduced-denominator floor. No expensive historical H<=24 suite was recursively rerun.

## 2. Incoming RL72 gate

Fresh RL73 checks before new mathematics:

- outer RL72 sidecar: **PASS**;
- freshly unpacked RL72 internal `SHA256SUMS.txt`: **PASS**;
- `bash verification/run_fast_rl72_verifiers.sh`: **PASS**.

No incoming failure occurred.

## 3. New RL73 theorem A — exact full-phase phase squeeze

RL65 gives

`(N-2)M = 237*3^r - 12*mathcalD - 2^(a-k+1)`.

Since `mathcalD>=0`, `M=3^(r+3)(zeta-1)`, and `N>=2^71`, RL73 derives

`boxed: zeta-1 < 79/[9(N-2)] <= 79/[9(2^71-2)].`

Classification: **analytic theorem** under retained full-phase extendability and the stable accepted `2^71` external floor.

## 4. New RL73 theorem B — global exponent-skew floor

The exact RL49 continued-fraction method, rerun with the stronger RL73 phase bound, yields the same first admissible reduced denominator

`q0=77,692,117,359,936,589,403`

and Legendre gate

`93,226,756,704,262,400,759`.

Combining the reduced-denominator dichotomy with `a/ell>log_2 3` gives the uniform integer bound

`boxed: 2a-3ell >= 13,201,833,154,443,526,323.`

Classification: **analytic + exact arithmetic/continued-fraction certificate**.

## 5. New RL73 theorem C — giant post-first-mismatch macro

For internal counts `c00,c11,e,e`,

`2a-3ell-2k+7 = 2c00-c11+e`.

The canonical synchronized prefix before the first mismatch is exactly one of

`(101)^q0`, `(101)^q11`, `(101)^q100`,

with skew contribution `P in {2,-2,3}`.

Therefore the post-prefix `00` count obeys

`c00+ >= ceil[(Delta-2k+7-P-e)/2]`.

Inside a hypothetical violation `27<=k<=165`, `25<=H<k`, one has `e<=H`. The exact worst-case audit gives

`c00+ >= 6,600,916,577,221,762,917`.

At most `H` such columns can be above height one, and the post-first-mismatch height-one motion is split into at most `e` synchronized blocks. Hence one block contains at least

`boxed: 40,249,491,324,522,944 aligned 00 columns.`

Classification: **analytic localization theorem + exact finite parameter audit**.

This is not a consecutive-`00` theorem; `11` columns may interleave inside the block.

## 6. Combined low-k nested obstruction

In a nested `g=0` hypothetical violation with odd `27<=k<=165`, both are now necessary:

- RL71: `delta_*=c162` exactly;
- RL73: a post-first-mismatch height-one synchronized block contains at least `40,249,491,324,522,944` aligned `00` columns.

This sharply localizes the missing theorem but does not contradict it.

## 7. Correction ledger retained

Do not revive or overstate:

- RL59 decreasing-height terminal potential or dependent mass/z-floor claims;
- Ansari's stronger external floor; stable accepted floor remains `2^71`;
- RL48 separable rank relaxation as a uniform proof;
- RL48 half-period radius-3 bridge;
- H<=24 as a uniform theorem;
- state/phase q-digit agreement as contradiction;
- local congruence/CRT as zero-depth control;
- `J<=2^H` as theorem;
- fixed safe-CF defect constants as uniform inputs.

RL73 additionally records that the uniform skew floor requires the reduced-denominator argument; it must not be justified merely by citing the RL50 stress convergent.

## 8. Exact proof state after RL73

- radius-3 primitive/full-D local theorem: **closed local obstruction**;
- Gate A even terminal `k`: **impossible analytically**;
- Gate A terminal `k<=25`: **closed by exact finite-certificate corollary**;
- Gate A odd `27<=k<=165`: **open**, but every violation is forced into the giant-macro regime above;
- nested `g=0`, odd `k<=165`: `delta_*=c162` exactly plus the RL73 giant macro;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 9. Recommended next target

Do not default to q=6/q=7. Attack the giant owned height-one macro directly.

The best first route is to recover RL50's exact height-one endpoint coordinates (`g,J,L,W`) and telescope an entire maximal synchronized block, while retaining RL64 full-phase completion. Seek a theorem that either:

1. upper-bounds the number of aligned `00` columns in an owned post-first-mismatch block for `k<=165`; or
2. lower-bounds its entry/active-rank weight enough that `40,249,491,324,522,944` aligned zeros violate the full-phase/prefix/terminal budget.

Any proof using only fixed local residues must be red-teamed against RL72's depth-periodicity barrier.

---

# Self-contained kickoff for RL74

Continue the Collatz R-sharp / RL research from the authoritative RL73 handover bundle and matching `.sha256` sidecar.

First verify only the current RL73 gate:

1. outer RL73 sidecar;
2. freshly unpacked internal `SHA256SUMS.txt`;
3. `bash verification/run_fast_rl73_verifiers.sh`.

Treat a checksum/verifier/foundational-definition failure or an apparent contradiction as stop-and-repair.

**Verification economy rule:** after the current gate passes, accept the frozen RL73 proof-state ledger. Do not recursively rerun historical expensive certificates unless a new load-bearing argument requires an unresolved definition or contradiction appears.

Preserve the RL73 theorem exactly:

- full phase plus the stable `N>=2^71` floor gives `zeta-1 < 79/[9(2^71-2)]`;
- the safe reduced-denominator argument gives `ell>=77,692,117,359,936,589,403`;
- hence `2a-3ell>=13,201,833,154,443,526,323`;
- every hypothetical Gate-A violation with odd `27<=k<=165` contains a post-first-mismatch maximal height-one synchronized block with at least `40,249,491,324,522,944` aligned `00` columns;
- these `00` columns need not be consecutive.

**Primary RL74 target:** prove an ownership-sensitive endpoint theorem for that giant height-one macro. Recover RL50's exact `g,J,L,W` height-one identities and combine them with RL64 full-phase extendability. The theorem should consume the giant aligned-zero count by bounding macro entry weight, endpoint state, or full-phase quotient; do not attempt to solve unrestricted height-one shortcut Collatz.

Use the nested `27<=k<=165` window as the first laboratory, where RL71 additionally gives `delta_*=c162` exactly. A successful theorem eliminating the RL73 giant macro in this band closes the entire low-`k` Gate-A sector through 165.

A secondary Gate-B experiment is allowed only if it produces a genuinely non-coboundary residual; apply RL72's S2/S3 rejection tests immediately.

Before ending RL74, freeze the exact proof state, new lemmas/failures, verifier status, and next roadmap into the next numbered authoritative bundle and sidecar.
