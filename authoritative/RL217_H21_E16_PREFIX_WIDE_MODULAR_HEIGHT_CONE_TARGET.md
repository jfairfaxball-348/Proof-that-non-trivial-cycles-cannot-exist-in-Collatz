# RL217 target — prefix-wide modular e=16 root-tail height cone

Date: 2026-09-01.

Accept all RL216 results and inherited scope locks.

## Primary target

RL216 proves that the forward deterministic height cone is a genuinely effective e=16 discrete consumer: the canonical minimum-Q prefix and all 7,369 of its lifts are impossible. The conservative remaining family contains **45,045 prefixes** and **331,927,916 arithmetic candidates**.

Generalize the successful consumer across the remaining prefixes without brute-force candidate enumeration.

1. For each prefix retain the exact lift `eta=eta_*+3^17 k` and phase-16 state `y_16=2^34 eta-1-3^16*2^13`.
2. Build a shared modular automaton for branches `k=r mod2^m`, with affine odd state and current height as the canonical state.
3. Merge/memoize mathematically equivalent states across prefixes where valid; count intersections with each finite k interval by exact range arithmetic.
4. Certify all prefix deletions reached within a productive finite horizon and update state/mod18/mod2187 populations exactly.
5. If every prefix at e=16 is eliminated, promote deletion of terminal rank `34,124,151,203` only after an independent verifier confirms complete coverage.

## Success criterion

Delete additional exact e=16 prefixes or a stronger class/rank; otherwise prove a precise finite-horizon state-compression boundary and hand off the smallest unresolved modular state family.

## Locks

RL216 certifies only the unique minimum-Q prefix. Do not extrapolate its phase-174 result to other prefixes without exact coverage.

Do not enumerate the 331,927,916 candidates individually. Use modular/range counting and shared states.

Do not treat arithmetic candidates as physical populations. Preserve the RL206 corrections, below-p `37/60/97`, p-shift carry convention, quotient-residue distinction, RL209 signed-successor scope, and all physical/charge/Gate/global locks unless an exact new proof changes them.
