# RL253 — beta=6 event-versus-rigid terminal-ownership continuation target

Date prepared: 2026-09-05
Status: **PREPARED, NOT STARTED**
Source: RL252 exact iterated zero-run packing contraction

## Authoritative target

Continue the Branch-C `beta(P)=6` attack from the first unexcluded
arithmetic scale

`(a,ell,z,q,r)=(783,494,289,317,200)`

with the tightened canonical selector range

`31<=k<=53`

and therefore `m=z-k+2=291-k>=238`.

Continue the exact deterministic canonical event-versus-rigid
terminal-ownership analysis. Seek the next theorem-sized contradiction
or material contraction under the already-promoted support/window and
zero-run machinery.

## Frozen RL252 checkpoint

The following is now inherited and must not be reproved from scratch:

- ordinary zero-run propagation has exact endpoint form
  `[s,s+L-1] -> [s+q+8,s+q+L-2]`, length `L-9`, for `L>=10`;
- at the first frontier, fivefold propagation of the terminal block gives
  `B_5=[59-t,13]`, length `t-45`, when defined;
- terminal-window packing excludes every `t>=51`;
- hence `t<=50`, `31<=k<=53`, and `m>=238`;
- the exact fifth-iterate packing is exhausted at `t=50` because its
  lower zero count is 288 rather than a contradiction.

## Binding boundaries

RL249/RL250 corrections and demotions remain binding. In particular:

- do not identify the historical RL47 `q=z+t` with the current
  determinant-2 rotation `q`;
- do not revive exploratory quadratic zero-desert bounds;
- do not reuse the demoted q-ordered `B=13/14` covering formula or
  downstream scratch claims;
- do not infer emptiness from shrinking measure, density, cylinder
  width, or finite-level compatibility alone.

RL251's Gabriel's-horn pivot remains frozen as equivalent to current
work unless a materially new invariant is explicitly supplied.

Gate A remains open. Gate B remains open. Radius 4 has not been invoked.
Radius 5 remains inactive.
