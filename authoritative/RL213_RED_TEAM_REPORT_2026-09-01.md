# RL213 independent scope / red-team report

Date: 2026-09-01. Verdict: PASS for the scoped RL213 candidate.

Checks performed:

1. Re-derived the e=16 endpoint-moment substitution from the inherited RL201 formula, including the factor of `alpha`, and checked that all eta/local-prefix terms cancel only after using the exact matched p-gap and root z-telescope.
2. Verified the remaining equality is exactly `alpha P_p=3(K0-(alpha-1)y_0)`, which is independently the inherited root `0->p` z-telescope plus `y_p-y_0=K0`. No extra eta equation remains.
3. Confirmed the conclusion is scoped to the existing RL201 moment; it does not retire a newly supplied restriction on `P_p`, a discrete ownership quotient/residue, nonlinear/global inequality, or another complete-word consumer.
4. Exhausted all 304 RL210 first-divergence start cases: `m=24..37`, smaller exponent 37, larger exponent `38..b_m`, both orientations. The verifier validates every positive witness directly modulo `2^56`, with strict increase and exact mechanical upper bounds.
5. Confirmed the only infeasible start case is the already inherited RL210 m=24 root-smaller orientation; all remaining 303 are saturated. No new m/orientation/rank deletion is inferred.
6. Confirmed the abstract G56 witnesses are not promoted as physical words or H21 populations.
7. Checked the e=16 terminal is phase `p+50` and rank `34,124,151,203`; RL209's signed-successor barrier is independently anchored only in its named backward-root survivor set, so it is not silently generalized here.
8. Confirmed all global necessary counts, RL212 holes, RL211 e=4 selector, RL206 corrections and Gate/global locks remain unchanged.

No correction or demotion is required. No rank deletion, physical incidence/charge, branch contradiction, Gate closure or global nontrivial-cycle exclusion is promoted.
