# RL186 correction and demotion ledger

Date: 2026-08-30

## New corrections / demotions

None.

## Frozen scope locks retained

1. The corrected physical defect is `G_i=S_(i+p)-S_p-S_i`.
2. The physical corrected-flow functional uses `2^G`; RL173's `3^-G` quantity remains auxiliary only.
3. Necessary local templates/maps remain one-way filters.
4. RL175 sparse resultant information is not a generic gap obstruction.
5. RL168-RL171 generic rank/lattice/inverse-rank barriers remain in force.
6. RL180-RL186 high-branch statements remain internal to `(37,0,23,-1)`.

## RL186-specific scope locks

- The late-tail height table is a necessary physical capacity restriction for **first defects of RL184 clean shallow-start corridors**; it is not a global defect-height classification.
- The exact enumeration uses all admissible shallow starting numerators satisfying the inherited strict gap corridor; no enumerated candidate is promoted as physically realized.
- `tau>=37` is capped by zero-block shallow-start density. This does not bound the total number of nonzero defects.
- `sum |f_i|>354` and the signed `>177` flow masses are variation statements, not net K excursion.
- The `>59` directional K-variation floors do not contradict the RL181 corridor.
- No branch, Gate A/B, non-trivial-cycle, or Collatz closure claim is promoted.
