# RL299 — resonance-spike / physical-quotient continuation target

Date prepared: 2026-09-11
Status: PREPARED, NOT STARTED

## Incoming classification

RL298 closes as

`SELECTOR_FRONTIER_EXACT_FINITE_PHYSICAL_CERTIFICATE_TO_A301994_WITH_RESONANCE_COORDINATES`.

Gate A remains open.
Gate B remains open.
RL297 P-bottleneck work remains parked weak-green background.

## Frozen RL298 facts

RL298 exactly resumed the post-RL262 selector predicate and identified the fifth selector

`(1436,906,530,1119,706,18,14)`.

It then derived a simpler physical quotient certificate from RL263:

- admissible phase quotients satisfy `N==19 (mod24)` and a finite exact ceiling `Nstar`;
- the internal words are the deterministic half-Collatz parity itineraries of
  `A0=(9N+5)/8`, `B0=(27N+127)/8`;
- genuine full phase requires common one-count `rho=ell-3`;
- inherited odd `k>=31` gives longest internal length `mmax=a-32`;
- a finite stopping-time bound to the exact `1<->2` cycle can therefore rule out the required one-count.

Exact frozen frontier:

- 2,648 retained selectors through `a=100000`;
- 8,057 retained selectors through `a=301994`;
- all are eliminated by the finite physical certificate;
- max `Nstar` through 100000: `803113`;
- max `Nstar` through 301994: `136073747`, at
  `(301994,190537,111457,50508,31867,3725,623)`;
- frozen finite physical stopping bounds are 291 and 588 respectively.

RL298 also proves the exact determinant-coordinate identities

`BH-zn=14`,
`qH-an=38`,
`rH-ell*n=24`.

For halving selectors `H=2K`, `n=2t`:

`KB-zt=7`,
`Kq-ta=19`,
`Kr-t*ell=12`.

## Binding correction

Do not revive the ordered-prefix alternating-corridor interpretation.

RL263 proves that the ordered dyadic prefix congruence is an automatic physical-gap telescope on genuine full-phase pairs. It is not an independent density obstruction. RL263 also restores the `2^m` modulus in the inherited odd-`k` scope.

The RL298 finite physical certificate does not depend on that discarded route.

## Primary objective A — independent replay / compression

Before extending the frontier:

1. run the RL298 fast arithmetic verifier;
2. independently replay or shard the finite physical envelope as needed;
3. audit the stopping-time-to-weight inequality including ceiling/off-by-one details;
4. preserve exact integer resonance throughout — no floating-point selector decisions.

If any frozen count fails, stop and correct RL298 before new research.

## Primary objective B — exceptional resonance structure

Identify the next retained-selector resonance spikes beyond `a=301994`.

Prefer arithmetic structure over blind cutoff growth:

- continued-fraction / semiconvergent organization of `a/ell` around `log_2 3`, but certify every retained candidate with exact integer inequalities;
- use the new `(z,B,H,n)` determinant coordinates;
- quantify how the resonance gap controls `Nstar`;
- distinguish ordinary selector blocks from exceptional quotient spikes.

## Primary objective C — physical certificate versus spike growth

For each newly certified resonance block/spike:

1. compute the exact `Nstar` envelope;
2. compare the required excess
   `E=2*(ell-3)-(a-32)`
   with a finite verified stopping bound for every required physical start;
3. if `E >= s+2`, freeze the entire block by the stopping-time-to-weight lemma;
4. if not, fall back to exact direct longest-horizon one-counts or the older exact full-phase machinery.

Do not extrapolate a global Collatz stopping theorem from finite data.

## Success condition

RL299 succeeds if it produces at least one of:

- a rigorously certified next resonance block/spike eliminated by the physical quotient certificate;
- a nontrivial exact arithmetic theorem controlling `Nstar` along retained selector families;
- a decisive barrier showing where the finite physical certificate stops scaling;
- a uniform theorem that is genuinely independent of any unproved global stopping assertion.

## Scope

Gate A remains open until an actual uniform proof is obtained.
Gate B remains open.
Radius 6+ remains frozen.
RO/divergent-orbit work remains out of scope.
The Lean formalisation project is separate.
No global non-trivial-cycle exclusion is claimed.
