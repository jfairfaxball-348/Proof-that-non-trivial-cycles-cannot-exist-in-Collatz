# RL81 target — auxiliary basin transfer and equality-level capture

Date: 2026-08-24

## Purpose

Continue from RL80's strongest positive result rather than from the demoted pure dyadic-belt intuition.

RL80 proves that pure dyadic saturation and even the full backward closure of a fixed seed set are cycle-intersection neutral as set traps. It also proves something more specific and potentially useful: under RL64's terminal all-`11` hypotheses, the full-phase auxiliary entry

`J_0=2^j(2^k+1)/3^j-1`

is an analytically certified ordinary-Collatz basin integer, and for `k>=3` so is the RL50 height-one coordinate

`n_0=(J_0-1)/2`.

The unresolved issue is that these are auxiliary quotient/full-phase coordinates, not yet identified with an original positive cycle state.

## Primary question

# Can exact RL48/RL64 full-phase reconstruction transfer this auxiliary basin capture to a genuine owned cycle integer or owned predecessor?

A successful transfer would create a real contradiction. A rigorous proof that no such transfer can be extracted from the existing coordinates would close this basin sub-route cleanly.

## Required investigations

### 1. Recover exact coordinate map only as needed

Using the frozen RL79/RL80 ledger and selective GitHub provenance, recover the exact relations among:

- original cycle/word state(s);
- RL48 full-word reconstruction variables;
- height-one `T,J`;
- RL50 `n=(J-1)/2`, `L=g(J-1)/2`, `R`, `U`, `V`;
- terminal exponent `k` and terminal synchronized suffix.

Do not reconstruct by memory or invent an ownership predicate.

### 2. Basin-preserving transfer test

Seek an exact operation taking the blue auxiliary `J_0` or `n_0` to an actual cycle integer using only transformations known to preserve ordinary basin membership, for example:

- multiplication by powers of `2`;
- finitely many exact forward shortcut-Collatz steps;
- exact legal backward predecessors;
- equality with a genuine cycle rotation/state.

Affine/rational scaling that does not preserve the ordinary basin is not enough.

### 3. Full all-`11` branch first

Use the exact RL64/RL66 all-`11` terminal branch, including

`j<=1+v3(k)`

and the maximality/phase-digit refinements, before attempting mixed tails.

Determine whether the all-`11` branch alone can be closed or structurally demoted.

### 4. Mixed/all-`00` tails second

If and only if the all-`11` transfer architecture survives, classify mixed terminal synchronized tails.

Remember:

- `11` on `J` is exactly the ordinary odd Collatz shortcut step;
- `00` on odd `J` is `(J+1)/2`, not the ordinary even branch.

Do not assume arbitrary synchronized words are Collatz trajectories on `J`.

### 5. Equality-level, not proximity-level

Do not use:

- density of blue numbers;
- log-phase density;
- residue coverage;
- a nearby blue number;
- a multiplicative interval containing a blue number.

A valid bridge must produce exact basin membership of a genuine cycle integer.

### 6. RL79 generalized-map red-team

Any candidate theorem must genuinely use the ordinary `+1` map and fail or change under generalized `T_s`.

### 7. Fast stop condition

If the exact coordinate map shows that `J_0`/`n_0` is merely an auxiliary affine difference with no basin-preserving path to an actual cycle state, prove that as a route barrier and stop this basin-transfer architecture rather than spending the session on large computations.

Then re-rank RL79's surviving non-homogeneous `s=1`/unit-lattice/content route as the next primary target.

## Desired outcome

Prefer, in order:

1. an analytic basin-preserving transfer that closes at least the terminal all-`11` RL branch;
2. a sharply formulated one-lemma transfer target with exact evidence and no hidden coordinate assumptions;
3. a rigorous transfer-impossibility/no-go theorem that freezes the blue route cleanly;
4. a new outside-coordinate `s=1` theorem discovered while analyzing the exact reconstruction.

Do not claim RL or Collatz closure without a complete chain.

## Close-out

Freeze exact proofs, finite certificates, external certificates, computational evidence, conjectures, barriers and corrections separately. Create the next numbered authoritative bundle and sidecar and verify its internal manifest and fresh-unpack fast suite.
