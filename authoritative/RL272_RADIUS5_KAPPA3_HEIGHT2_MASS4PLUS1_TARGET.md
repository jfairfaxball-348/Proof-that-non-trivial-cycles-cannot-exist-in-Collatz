# RL272 — Radius-5 `|kappa|=3` height-two mass-four-plus-one target

Date prepared: 2026-09-07
Status: **PREPARED, NOT STARTED**

Incoming classification: `RADIUS5_KAPPA3_FLAT_SECTOR_CLOSED_HEIGHT2_MASS4PLUS1_OPEN`.

## Inherited exact state
RL271 closes every flat topology carrying `|kappa|=3` under the complete positive `D=2^A-3^L` standard. The sole `|kappa|=3` family still open is the inherited height-two mass-four-plus-one topology from RL265.

Retain unchanged:
- exact source/target reversal between `kappa=-3` and `kappa=+3`;
- `qA-mL=3`;
- `gcd(A,L)|3` and `gcd(m,A)|3`;
- determinant-window identity `W_i(m)=q-g_(i-1)` where its hypotheses apply;
- complete positive `D`; never substitute a proper factor;
- negative-domain, nonprimitive, cyclic-wrap, sign/order and numerator-index red teams.

## Primary target
Close, or rigorously reduce to an explicit finite certificate, the height-two mass-four-plus-one `|kappa|=3` family.

Required workflow:
1. recover the exact RL265 height-two topology definition and sign/orientation cases;
2. derive its own zero-flow cuts, boundary relations and support estimate rather than importing a flat-family constant;
3. handle gcd-one and gcd-three determinant sectors explicitly;
4. perform infinite-to-finite reduction before any large enumeration;
5. test the complete `D=2^A-3^L` in every certificate;
6. use independent small-range replay and an independent reconstruction/checking implementation before promotion.

Preferred outcome: `RADIUS5_KAPPA3_SECTOR_CLOSED`.

Only after that closure may a successor activate `|kappa|=5`.

Frozen:
- `|kappa|=5`;
- Gate A;
- fifth retained selector;
- selector enumeration;
- general Radius-n programme.

Radius 4 remains promoted locally. Gate B, Radius 5 as a whole and global exclusion remain open.
