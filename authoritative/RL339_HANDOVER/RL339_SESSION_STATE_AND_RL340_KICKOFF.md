# RL339 session state and RL340 kickoff

RL339 is CLOSED AND FROZEN. RL340 is uniquely prepared as the successor and **has not been started**.

Scope: ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)` only. The least-state floor `m>=2^71` is external and conditional. The q=0 source band is `[2^71,2^76+2^36)`, inherited `z<=35`, and the physical ownership, affine/suffix, and q=35 theorems remain in force. Read the correction ledger before using older scratch statements.

RL339 adds exact escape of all singleton adjacent-zero totals 43..70, yielding `K>=ceil((ell-rho+1-35)/22)` and an analytic step-44 phase-pair support bonus. The new conditional carry cap is `n<=32537248343`. It also excludes every q=35 nonnegative high-to-high p=5..8 physical row under the same floor. These results do not close R1. The cap remains 12,146,996,285 above the fixed high-carry ownership threshold.

The unique RL340 target is `RL340_PARENT_BRIDGE_NEGATIVE_RETURN_IDENTITY_TARGET.md`. Start it only on a new user kickoff and after the normal start gate. Do not mix this branch with the separate Lean formalisation project.
