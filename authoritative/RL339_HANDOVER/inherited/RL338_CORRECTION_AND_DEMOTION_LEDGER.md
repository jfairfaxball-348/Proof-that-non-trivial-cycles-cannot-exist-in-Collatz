# RL338 correction and demotion ledger

Date: 2026-09-16
Status: FROZEN WITH RL338 CLOSEOUT

No inherited authoritative theorem is corrected or demoted.

## C1 — incomplete scratch successor statement corrected

During interactive scratch reporting, the p=5..8 successor scan was summarized as if the five p=7 `22->1` continuations were the only exact successors of an exceptional q=35 core. That wording omitted p=1..4.

Closeout red-team verification reconstructed p=1..4 gap-free and found 24 additional owned exact continuations. Every one has reduced q=35 charge <=-30. The five p=7 continuations remain exactly correct within p=5..8 and each has charge -88.

The promoted q=35 theorem uses the corrected exhaustive statement: p<=8 successors are either one of those 29 exact continuations, all strongly negative, or absent; p>=9 is <=-14 analytically. The theorem and final constant 43 therefore remain valid.

## C2 — scratch successor-template count removed

An interactive scratch message quoted `1,649,804` successor templates. That number depended on a deduplication/accounting convention and is not needed for proof. It is not promoted. Authority records exact gap-free ranges instead: 125 distinct `(right-context, exit-state)` interfaces, every right label 1..35, every p=1..8.

## C3 — physical-core terminology clarified

The scratch phrase “68 exact cores” is retained only with an explicit definition. A boundary core is `(B,E,q)` with `B` the exact state at index `left-1`, `E` the run-exit state at index `left+p`, and q the profile. There are 68 p=5 boundary cores. Retaining the full left-context source instead gives 83 p=5 triples. No theorem depends on conflating these quotients.

## C4 — intermediate q=33/q=34 not separately promoted

q=33 and q=34 were stepping stones to the final q=35 theorem. They are subsumed by the stronger verified statement and are not separately load-bearing authoritative results.
