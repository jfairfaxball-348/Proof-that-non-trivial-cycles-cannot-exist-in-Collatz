# RL327 parent-bridge owned-excess provenance

Date: 2026-09-15
Status: VERIFIED PROVENANCE FOR RL327 CLOSEOUT

## Incoming theorem surface

RL327 starts from RL326's ordered genuine `g=2`, `Z0>0`, `K<0` late-row parent branch. The load-bearing inherited facts are:

- `(a,ell)=(217976794617,137528045312)` and `d=a-ell=80448749305`;
- `lambda=2^a/3^ell<1+2^-40`;
- externally conditional least-state floor `m>=2^71`;
- the RL326 physical zero-run state band `2^71<=P<2^76+2^36`;
- linear mechanical excess sequence `q_t>=0` on the crossing-to-canonical matched interval;
- exact crossing-start telescope `n < 1 + I_rho - loss`;
- RL326 high-carry ownership threshold `n>=20390252058`;
- RL326 carry cap `n<=32839291403`;
- RL324's local propagation counterfamily, which bars local displacement propagation as a closure mechanism.

No RL327 theorem weakens the distinction between the internal frontier `ell>=190537` and the external-certificate-conditional frontier `ell>=49547666544`. The `2^71` least-state floor remains externally conditional.

## New finite ownership object

RL327 changes the unit of enumeration from isolated forbidden 49-gap zero blocks to **owned bridges between zero runs**. The bridge word is solved as one congruence modulo the full `3^L`, then every lift in the finite physical state band is reconstructed and checked. Shared zero plateaux are linked by actual shared physical state, not by word type alone.

This is the global ingredient that avoids the RL324 local-propagation barrier: the promoted density inequalities are consequences of exact ownership across the whole finite bridge automaton, not a local claim that one bounded displacement forces the next.

## Consumer relation

The finite automata do not by themselves prove a Collatz contradiction. They supply lower bounds on the number and placement-weight of positive mechanical excess ranks. Those bounds are consumed by the inherited linear RL326 telescope and exact Beatty/omitted-mass enclosure.

The final RL327 cap is therefore a compound theorem:

1. exact owned bridge certificates;
2. finite potential / max-plus density theorem;
3. residue-weighted analytic telescope consumer;
4. self-consistent threshold bootstrap.

All four layers are required for `n<=32596612662`.
