# RL337 session state and RL338 kickoff

RL337 is CLOSED AND FROZEN.

Authoritative branch remains the ordered genuine `g=2`, `Z0>0`, `K<0` parent at `(a,ell)=(217976794617,137528045312)`. The external least-state floor remains conditional. RL336's cap `n<=32546278588`, zero-run bound `z<=35`, q=32 theorem, and inherited ownership certificates remain in force.

RL337 adds three useful exact facts:

1. profile deformation preserves total exponent and lowers the affine carry by an explicit `1-2^-Q_k` weighted sum;
2. at fixed terminal state the profiled source is smaller than the zero-profile source — this is the corrected sign;
3. every fixed-depth right suffix of an arbitrarily long positive run has a finite profile grammar independent of total run length.

RL337 also records an exact physical p=5 diagnostic: pairs `(24,27)`, `(24,28)`, `(24,29)` share the same physical source `30437321051399780895133`, same run-exit state `31208280367336569715567`, and profile `(2,1,2,1,1)`, while the reverse orientations have no physical realization.

R1 remains OPEN.

RL338 unique live target: prove a support-uniform exact shared-state ownership theorem for the arbitrary-positive fallback layer. Use the right-aligned suffix grammar, exact residue lifting, deterministic physical state identity, or an equivalent quotient to determine whether the anonymous fallback cycles have any genuine physical cycle. Closure by acyclicity, forced descent, or exact state obstruction is acceptable.

Do not make the session a p=7,p=8,... catalogue. Finite p=5/p=6 reconstruction may be used only to discover or certify a finite interface that extends analytically to all positive-run lengths.

Do not use the demoted fixed-terminal sign argument, the mechanical-reference least-state barrier, or a cyclic interpretation of the linear `q_t` interval.
