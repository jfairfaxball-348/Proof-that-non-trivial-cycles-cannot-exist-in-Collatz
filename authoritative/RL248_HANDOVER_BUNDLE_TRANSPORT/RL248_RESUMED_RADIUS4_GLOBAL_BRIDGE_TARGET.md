# RL248 target — resume audited Radius-4 global bridge

Status: **PREPARED, NOT STARTED**.

## Inherited frozen frontier

Every simultaneous Gate-A/Gate-B survivor still has `beta(P)>=6` and the Branch A/B/C geometry frozen through RL245.

RL246 additionally proved for Branch C:

- at least one singleton q-shift `01 <-> 10` signature lies wholly inside the canonical internal path;
- it has an exact factor-three q-shift physical prefix-scale notch;
- it produces a positive RL47 phase packet;
- at its shifted `10` boundary either a canonical even-J mismatch/descent event occurs, or the genuine companion physical envelope is exactly `(-d,-d-1,-d)` with exact J mod-3 fingerprints recording height parity.

## Audited Radius-4 eligibility gate

Before applying the RL238 Radius-4 theorem, establish all of the following for the actual candidate object:

1. positive Collatz-cycle domain;
2. `D=2^A-3^L>1`;
3. primitivity;
4. genuine full-D / full-phase physical ownership as required by RL238;
5. exact cyclic adjacent-transposition distance 4 under the inherited cyclic metric;
6. any canonical-root / same-root hypotheses used by the bridge;
7. every other RL238 theorem hypothesis.

Bare `D|Q(d)` is not sufficient over arbitrary signed integer cycles.

## Active target

Consume the remaining rigid Branch-C arm, and then Branch A/B as needed, by proving either:

1. an eligible primitive positive/full-D exact cyclic-distance-4 self-rotation satisfying every audited Radius-4 hypothesis; or
2. an exact contradiction to full-phase extendability using the canonical `(d,J)` automaton, terminal `J=2^k`, ordered rank/phase selectors, or another exact global-state identity.

Gate A remains open. Gate B remains open. Radius 5 remains inactive.
