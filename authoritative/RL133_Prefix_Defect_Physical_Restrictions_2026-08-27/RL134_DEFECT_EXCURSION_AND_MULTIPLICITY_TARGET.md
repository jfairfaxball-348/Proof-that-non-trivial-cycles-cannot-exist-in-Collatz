# RL134 target — defect excursion and multiplicity at the first CF survivor

Start from the unchanged conditional frontier `L>=49,547,666,544` under inherited external `R#>=2^71` and the first reduced survivor

`(A,L)=(217,976,794,617,137,528,045,312)`.

RL133 has added actual physical structure only on the full-count `g=1` branch. Preserve that scope.

## Priority A — close or sharpen the coprime defect excursion

For `g=1`, use the proved nonnegative defect path

`h_j=floor(Aj/L)-S_j`,

its rise restriction, physical height bands, and the exact externally conditional population floors

`#{h<=3}>=176,343,262`,

`#{h<=4}>=3,370,832,656`.

Couple these to actual ownership: RL123 sliding-window rotation transport, weighted numerator differences, physical spacing, or another exact cycle-specific identity. Seek either

1. a forced rotation within the inherited closed radius-three regime; or
2. a direct contradiction.

Do not assume that a nonzero defect excursion automatically has small radius.

## Priority B — treat multiplicity `g>1` physically

An actual cycle may have full counts `(gA,gL)`. Extend the least-state prefix squeeze to that setting without reverting to count-only gcd arguments.

Classify positive prefix discrepancies satisfying

`0 < S_j log2-j log3 < g Delta`

at physically meaningful reduced/canonical cuts. Use exact Farey/continued-fraction arithmetic only after the physical prefix inequality is established. Determine whether multiplicity forces a balanced near-minimum cut, a restricted lattice family, or a direct ownership obstruction.

## Mandatory scope

- inherited external `R#>=2^71` remains externally sourced;
- internal frontier remains `L>=190,537` unless independently advanced;
- bare CF/product, fixed-depth complete residue-prefix, and count-only gcd-block routes remain retired at this survivor;
- preserve RL20/RL79/RL81 ownership/scaling/physical-state red teams, primitivity, correction/demotion, verification economy, and sustained attack;
- no Gate A/B or global Collatz closure claim without discharging its actual obligations.
