# RL189 target — N35 crossover early-defect charging and boundary consumer

Date: 2026-08-30

## Authoritative objective

Continue the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from the RL188 `N_35` crossover.

RL188 proves

`N_35<=10063027705<CLEAN=10075174499`,

so at least

`12146794`

clean starts have first nonzero defect at offset `tau<=34`.

The inherited RL187 charge assigns the same weight to every `tau<=35` start, so this new population is not yet monetized.

## Preferred theorem

Build an offset-sensitive physical consumer for the forced `tau<=34` starts. Preferred inputs are:

- exact RL186/RL187 first-defect numerator and height vocabulary;
- RL188's preserved 41-spacing theorem for the extremal triple;
- RL182 ternary predecessor reset;
- RL183 parity sensor and common-mechanical affine boundary law;
- exact mechanical phase location;
- RL185/RL187 corrected-flow charging and signed-flow framework.

Desired outputs, in order:

1. a rigorously larger ordinary corrected-flow floor from a safe new charging schedule; or
2. a stronger survival-density cut at `N_34`/`N_35`; or
3. a sign/phase boundary theorem that makes the newly forced early defects more expensive than the inherited generic `2^-(H+1)` charge.

Do not:

- reopen generic long-word BFS or arbitrary ternary enumeration;
- treat necessary terminal ranks as realized states;
- use RL173's auxiliary `3^-G` quantity as physical corrected flow;
- convert total variation into chronological K excursion without a proved sign-order theorem;
- claim the isolated `{35,36,37}` triple is impossible;
- claim branch or global closure without a genuine quota contradiction.
