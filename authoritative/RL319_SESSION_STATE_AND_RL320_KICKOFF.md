# RL319 session state and RL320 handover

Date: 2026-09-14
RL319 status: CLOSED AND FROZEN
Successor: RL320

## State summary

RL319 converts the forced first reverse mismatch into the support-independent
suffix inequality

`3^B q<=2^n E`.

It also proves an exact least-root transport for the ordered `epsilon=0`
branch. Either the least state lies in the late row, or a least-rooted reduced
balanced return exists.

At a least-rooted first-survivor return,

`G<2^35`, `v2(G)<=34`,

and the nonzero cofactor carry satisfies

`0<=kappa<2^34`.

In the remaining late-row-root branch, interface height `s>=1` gives

`y=3^s m+K`, `0<|K|<3^s2^35`, `3` not dividing `K`.

The height `s` is not bounded.

## RL320 instruction

Primary: attack the late-row interface height using genuine `D0` ownership,
the scaled-contact equations, and any applicable physical packing. Seek a
bound on `s`, a contradiction, or a contact theorem whose hypotheses are
proved in this branch.

Secondary: consume the root-aligned caps `(G,kappa)` without restarting a
fixed-depth residue-prefix scan. The crossing residue law and
`3^Bq<=2^nE` are available.

Do not treat re-cut order as invariant, do not use homogeneous `T_h`
invariants, and do not infer local denominator ownership.

Gate A/B and global exclusion remain open.
