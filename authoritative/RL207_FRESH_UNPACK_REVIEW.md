# RL207 fresh-unpack review

Promotion is conditional on completion of all recorded checks, not this prose:
ZIP CRC and sidecar; clean temporary extraction; exact candidate/payload byte
equality; internal manifest coverage/hashes; all four active portable scripts;
unchanged mathematical output JSON; reconstructed unchanged RL206 proof-state
guard; current RL207 generation/nonpromotion scope guard. The candidate's
external FRESH_UNPACK_EVIDENCE.json records the actual completed run hashes.

Analytic proof and inherited certificate status come from the canonical
ledgers/proofs, not the finite script runs or checkpoint notes. No new rank
count/exclusion is certified. The incoming authority snapshot and remote
BASE_HEAD are checked separately before the one atomic transition. Readback
must confirm the frozen session and new authority before completion is reported.
Catalogue status is stale/deferred. RL208 research is not started.
