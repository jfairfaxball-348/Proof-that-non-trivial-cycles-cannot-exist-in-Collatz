# RL341 target — absolute physical-identity obstruction

Status: UNIQUE LIVE TARGET FOR RL341; prepared, not started.

Work only in the ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, retaining the external least-state-floor qualification `m>=2^71`, q=0 band, `z<=35`, exact ownership/pruning, RL337 affine/suffix theorems, RL338 q=35 amortization, RL339 finite escape/density/phase theorem, and the promoted RL340 `(23,23,p=6)` escape certificate.

Primary objective: turn exact shared physical identity across the remaining negative-return layer into a parent-level absolute obstruction — preferably acyclicity of a canonical state quotient, owned descent, or a forced return below the least state.

First dependency: independently replay the frozen RL340 finite-interface/potential calculation that proposed `35(K-2H)-S <= -(5/13)K + 538/13` (`2H >= (92/91)K - S/35 - 538/455`). That inequality is not authoritative at startup. Promote it only if a complete finite edge/potential certificate reproduces; otherwise correct or demote it explicitly.

The inherited linear residue/telescope route is a quantified barrier: the present carry gap is `12,146,996,285`, and closing it inside the same architecture would require more than `145e9` additional weighted-support units. Therefore do not make another q-upgrade, endpoint constant, or phase-weight refinement the main route.

After replaying the candidate, retain enough exact source/run-exit data to test genuine state-level cycles. Use RL337 fixed-depth right-suffix compression or an equivalent all-length canonical interface so the argument does not devolve into p-by-p enumeration.

Success criterion: R1 closure for this ordered parent branch, or a theorem-level reduction to one exact, quantified, non-anonymous state obstruction. Preserve all conditional scope and report R1 OPEN unless closure is proved.
