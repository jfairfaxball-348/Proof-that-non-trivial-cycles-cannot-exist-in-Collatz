# RL104 target — prime-local unit-increment content at the owned first-surplus cylinder

## Purpose

Continue the unresolved raw-multiplicity first-Farey branch.  RL102 froze
prefix/suffix rotation transport and geometry-only packing; RL103 froze the
direct affine inverse-cylinder congruence.  No global closure is claimed.

## Primary target

Search for a genuinely nonlinear or prime-local restriction that uses the
ordinary `+1` increment and `D|Q(W_full)` beyond the fact `s=1`.  Candidate
objects may involve valuations or residues of a sparse prefix numerator,
the exact maximum cylinder representative, and a nontrivial prime-power
factor of the full denominator, but must retain actual integer phases.

Success requires an all-raw-`g>=2` lower-content contradiction, a bounded
nonzero denominator multiple, or a finite exactly parameterized residual
set.  A bare denominator floor or a congruence that remains valid for a
generalized `T_s` cycle is not success.

## Mandatory tests

Every candidate must identify the step that fails when `s>1`, survive the
RL20 fake and RL103 covariance tests, preserve physical-state/slope/multiple
scope, and label any external floor.  Do not resume bulk cascade scans unless
this target yields a verified multiplicity bridge or a separate explicit
strategic decision is recorded.

## Failure

Freeze the route if every proposed prime-local condition is full monodromy,
rotation transport, or another generalized-cycle identity.  Then record the
barrier and stop rather than extending ungrounded scans.
