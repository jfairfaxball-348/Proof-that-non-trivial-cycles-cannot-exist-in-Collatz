# RL94 — coupled four-tier cascade to deep radius 2,000,000 and interval propagation

Date: 2026-08-25

## 0. Executive outcome

RL94 continued the frozen RL93 coupled four-tier target under the verification-economy and sustained-attack rules.

**No Gate A global, Gate B, RL/nontrivial-cycle, or Collatz closure is claimed.** Every terminal exclusion below remains conditional on the inherited exact first-Farey/full-phase Gate-A branch.

Incoming first route survivor:

`k4 = 2,921,750,257`, with frozen corrected RL93 margin `-10,779`.

RL94 performed a sustained coupled extension rather than a single checkpoint step. The exact certified radii were advanced from

- middle `R_m=3,525,000` to `3,675,000`;
- deep `R_d=1,775,000` to `2,000,000`;
- ultra `R_u=35,000` to `335,000`.

No inherited depth floor was lost:

- middle combined minimum remains `7,500,034 at r=1,635,300`, hence `D_m=7,500,032`;
- deep combined minimum remains `10,000,034 at r=378,722`, hence `D_d=10,000,032`;
- ultra combined minimum remains `15,000,040 at r=2,900`, hence `D_u=15,000,038`.

The final four-tier staircase is therefore

- wide `(D,R,L)=(5,000,030,7,000,000,5,000,053)`;
- middle `(7,500,032,3,675,000,7,500,053)`;
- deep `(10,000,032,2,000,000,10,000,053)`;
- ultra `(15,000,038,335,000,15,000,053)`.

The exact final `A-C` support is

`mu = 1/7,000,001`,

`lambda = 5,000,000 / 52,500,238,500,033`.

The weighted decrement floor is still very large:

`floor(lambda*C)=4,014 > 2`,

so `b=k-1` remains the worst block-count stratum.

An exact support-edge audit at the final staircase finds the feasible two-corner supports `A-C`, `C-D`, and `D-E`; `A-C` gives the smallest weighted aggregate throughout the promoted RL94 interval and at its first failure. Thus the active line was re-optimized rather than merely inherited.

Using the conservative load-bearing common successor ceiling

`L_common=15,000,053`,

the exact rational interval verifier excludes all `4,224` consecutive odd values

`2,921,750,257 <= k <= 2,921,758,703`.

At the final eliminated odd value the contradiction margin is `+11,277`; at the next odd

`k=2,921,758,705`

the margin is `-4,350`.

Therefore the updated branch-specific surviving odd window is

`2,921,758,705 <= k <= 42,150,931,559`, `k` odd.

## 1. Incoming verification and GitHub provenance

The supplied RL94 handover passed:

- outer SHA-256 sidecar;
- all 146 internal manifest entries;
- supplied fresh-unpack verification;
- complete RL93 fast verifier suite.

Verification economy was applied: inherited expensive scans were accepted after this gate and were not recursively replayed.

Read-only GitHub provenance check found authoritative incoming commit

`5bde2c7201e82c5acc19993690c7c2fcb5a3d187`

with message

`RL94_Coupled_Four_Tier_Frontier_and_Ultra_Radius_2026-08-25`.

No repository write was performed during the research attack.

## 2. Exact ultra-radius replenishment

RL94 completed 60 exact 5,000-step GMP chunks modulo `2^15,000,056` over

`35,001 <= r <= 335,000`.

The new-band minimum is

`15,000,040 at r=86,111`

(with ties elsewhere in the new band). This ties but does not beat the inherited combined minimum

`15,000,040 at r=2,900`.

Hence the exact ultra certificate extends to

`d<=15,000,038 and r<=335,000 => n_next<=15,000,053`.

Classification: **new exact finite certificate + inherited-interface analytic consequence**.

## 3. Exact middle-frontier extension

RL94 completed six exact 25,000-step chunks modulo `2^7,500,056` over

`3,525,001 <= r <= 3,675,000`.

The new-band minimum is

`7,500,038 at r=3,546,704`,

which is above the inherited combined minimum `7,500,034 at r=1,635,300`.

Therefore the effective middle staircase tier extends to

`d<=7,500,032 and r<=3,675,000 => n_next<=7,500,053`.

Classification: **new exact finite certificate + inherited deeper-low-radius union**.

## 4. Exact deep-frontier extension

RL94 completed nine exact 25,000-step chunks modulo `2^10,000,056` over

`1,775,001 <= r <= 2,000,000`.

The new-band minimum is

`10,000,037 at r=1,817,629`,

above the inherited combined minimum `10,000,034 at r=378,722`.

Hence the exact deep certificate extends to

`d<=10,000,032 and r<=2,000,000 => n_next<=10,000,053`.

Classification: **new exact finite certificate + inherited-interface analytic consequence**.

## 5. Final four-tier geometry and coupling

The staircase-complement corners are

`A=(0,7,000,001)`,

`B=(5,000,031,3,675,001)`,

`C=(7,500,033,2,000,001)`,

`D=(10,000,033,335,001)`,

`E=(15,000,039,0)`.

For

`mu=1/7,000,001`,

`lambda=5,000,000/52,500,238,500,033`,

`A` and `C` lie exactly on `lambda*d+mu*r=1`, while exact corner values are

- `B = 5840309308337/5833359833337 > 1`;
- `D = 52512683555033/52500238500033 > 1`;
- `E = 2777785000000/1944453277779 > 1`.

All integer points outside the certified union therefore satisfy

`lambda*d + mu*r >= 1`.

At `R_d=2,000,000`, the same support requires at least

- `R_m >= 3,666,661`; actual `3,675,000`, slack `8,339`;
- `R_u >= 333,341`; actual `335,000`, slack `1,659`.

For the next 25,000-step deep move to `R_d=2,025,000`, unchanged-depth geometry requires

- `R_m >= 3,683,328`;
- `R_u >= 366,674`.

Thus a practical next coupled batch is middle to `3,700,000`, ultra to at least `370,000`, and deep to `2,025,000`.

The ultra depth-axis remains nonbinding through deep radius floor `3,499,993` at unchanged depths.

Classification: **updated exact four-tier supporting-line theorem + exact coupling thresholds**.

## 6. Supporting-edge re-optimization audit

At the final staircase, exact enumeration of two-corner nonnegative supporting lines gives precisely three feasible pair supports:

- `A-C`;
- `C-D`;
- `D-E`.

The exact interval verifier compares their weighted aggregate `lambda*D + mu*R` for every promoted odd `k` and at the first failure. `A-C` is no worse than either alternative throughout.

This closes a potential methodological gap: the final theorem does not rely on the assumption that the inherited edge remains optimal after the frontier moves.

Classification: **new exact convex-envelope audit**.

## 7. Top-stratum reduction

At the final support,

`floor(lambda*C)=4,014`.

Increasing `b` by one raises the weighted bad-block floor by at least `4,014`, while the explicit `2b` contribution can improve the contradiction margin by only `2`, and short-successor capacity is nondecreasing. Therefore the contradiction margin strictly decreases with `b`.

It remains sufficient to test

`b=k-1`.

Classification: **updated analytic monotonicity theorem**.

## 8. Exact RL94 odd-interval propagation

The final verifier evaluates every odd

`2,921,750,257 <= k <= 2,921,758,703`

at `b=k-1` with exact rational arithmetic, the final `A-C` support, and `L_common=15,000,053`.

All `4,224` values violate the necessary condition.

At `k=2,921,758,703`:

- `Q_min=123,139,091,657,860,279,878`;
- `D_b=15,759,623,655,746,978`;
- `R_b^max=9,943,247,233,940,797`;
- weighted bad-block bound `2,921,373,407`;
- forced multiscale-good blocks `385,295`;
- short-successor capacity `374,018`;
- contradiction margin `+11,277`.

At the next odd `k=2,921,758,705`:

- `Q_min=123,139,091,657,860,279,731`;
- `D_b=15,759,707,957,610,381`;
- `R_b^max=9,943,300,422,494,703`;
- weighted bad-block bound `2,921,389,034`;
- forced multiscale-good blocks `369,670`;
- short-successor capacity `374,020`;
- contradiction margin `-4,350`.

### Theorem 8.1 — RL94 branch-specific odd-interval exclusion

No exact first-Farey/full-phase Gate-A survivor has odd

`2,921,750,257 <= k <= 2,921,758,703`.

The updated branch-specific odd window is

`2,921,758,705 <= k <= 42,150,931,559`, `k` odd.

Classification: **new analytic theorem + exact finite 2-adic certificates + exact rational interval audit**.

## 9. Red-team audit

### RL81 common-mode freedom
Passed. No quotient state is promoted to a physical state.

### RL79 generalized-increment homogeneity
Passed. The ordinary `+1` minimal-reset constant remains essential.

### RL20 physical representative/packing separation
Passed. Weighted cover counts synchronized physical blocks, not quotient-address multiplicity.

### Primitivity
Passed. Residue/cofactor repetition is not interpreted as physical cycle-state repetition.

### First-Farey scope
Preserved. Every new exclusion is branch-specific.

### RL88 arbitrary-reset family
Passed. High reset valuation is excluded only within certified `(d,r)` regions.

### Verification economy
Passed. Only new RL94 bands were scanned; inherited expensive certificates were not replayed.

### Load-bearing successor charge
Passed. `L_common=15,000,053` was retained throughout because the ultra tier remains load-bearing.

### Convex-envelope re-optimization
Passed. Final feasible supporting edges were explicitly enumerated and compared.

## 10. Proof-state ledger additions

### New proved analytic mathematics

1. Updated four-tier support at `(R_m,R_d,R_u)=(3,675,000,2,000,000,335,000)`.
2. Exact final feasible-support enumeration and terminal-direction optimality of `A-C`.
3. Updated top-stratum monotonicity with decrement floor `4,014`.
4. Elimination of 4,224 additional consecutive odd first-Farey/full-phase terminal values.

### New exact finite certificates

1. Ultra: `r=35,001..335,000`, 60 completed chunks, new-band minimum `15,000,040 at r=86,111`; combined minimum unchanged.
2. Middle: `r=3,525,001..3,675,000`, 6 completed chunks, new-band minimum `7,500,038 at r=3,546,704`; combined minimum unchanged.
3. Deep: `r=1,775,001..2,000,000`, 9 completed chunks, new-band minimum `10,000,037 at r=1,817,629`; combined minimum unchanged.

### Correction/demotion ledger

No new RL94 theorem demotion. RL93's `15M` common-successor repair remains in force.

### Route limitation

The final margin at `k=2,921,758,705` is `-4,350`. The route remains productive, but each deep advance now requires coordinated ultra-radius replenishment and periodic middle extension. The active-support coefficient `floor(lambda*C)` has declined from `4,195` at RL93 to `4,014` at RL94, still far above the top-stratum threshold but showing the expected geometric weakening.

## 11. Why RL94 closes here

RL94 did not stop at the first successful endpoint. It propagated through nine completed deep chunks and coordinated middle/ultra replenishment until the completed batch reached deep radius `2,000,000`. The final larger scan batch produced repeated execution-time pressure; closing at a fully verified gap-free frontier preserves reliability while leaving an immediately specified next coupled step.

## 12. RL95 route selection

Continue at

`k5=2,921,758,705`.

Immediate coupled target:

1. ultra `335,001..370,000` (or modestly beyond) in completed exact chunks;
2. middle `3,675,001..3,700,000`;
3. deep `2,000,001..2,025,000`;
4. recompute global minima and safe depths;
5. re-enumerate the exact feasible support edges and optimize for the live aggregate direction;
6. retain `L_common=15,000,053` unless a rigorous mixed-charge theorem is proved;
7. propagate the exact odd interval immediately and continue the cascade while productive.

The sustained-attack rule remains mandatory.
