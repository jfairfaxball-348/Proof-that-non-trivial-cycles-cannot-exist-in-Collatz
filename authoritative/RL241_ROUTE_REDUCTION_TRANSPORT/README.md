# RL241 route-reduction transport

Completed generation: **RL241 — full-phase selected-counterflow anti-concentration / nine-dyadic-lift reduction**.

Classification: **`R4_BRIDGE_REDUCED`**.

Promoted outcomes:

- selected/root flows receive exact quantized physical log-potential interpretations under genuine full phase;
- the two selected flows factor through one symmetric core plus the inherited half-period flow;
- pure logarithmic/physical-scale anti-concentration is explicitly recorded as a method barrier after a positive-real-affine stress family;
- integer `X-Y` phase is translated into the canonical base-drift condition;
- exact full phase reduces to only nine dyadic carries `c=0,...,8`;
- the complete genuine two-half physical cycle is an explicit affine function of that one carry;
- successor RL242 targets carry elimination or a forced exact eligible Radius-4 encounter.

The canonical RL241 ZIP is carried losslessly in the base64 text parts in this directory. Verify `PART_SHA256SUMS.txt`, reconstruct with `reconstruct_rl241_bundle.py`, then check the outer authoritative `.zip.sha256` sidecar.

Canonical ZIP SHA256:
`98697aae2b112202b27bb334cbe21126d181182ea8e9f839f703a1b8422017b8`.

Gate A remains separate. Gate B and global non-trivial-cycle exclusion remain open. Radius 4 is not applied globally. Radius 5 remains inactive. Knowledge catalogues stale/deferred.
