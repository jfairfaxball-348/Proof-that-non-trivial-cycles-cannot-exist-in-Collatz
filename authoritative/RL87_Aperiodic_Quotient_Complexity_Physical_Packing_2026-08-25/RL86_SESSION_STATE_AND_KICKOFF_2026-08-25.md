# RL86 SESSION STATE AND RL87 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

## Incoming RL86 gate

Before RL86 mathematics:

- RL86 outer SHA-256: PASS;
- RL86 fresh internal manifest: PASS;
- RL85 fast verifier: PASS.

The frozen RL85 ledger was accepted under the verification-economy rule.

## RL86 theorem state

### RL↑ taxonomy

`RL↑={2^kM:k>=1}` is an unbounded set of external starting values, not an unbounded orbit.  Every member is preperiodic to the same chosen loop.

### Bare-ray neutrality

The dyadic ray exists over every positive integer base.  RL80's dyadic/backward-saturation theorem specializes to

`C intersect Pre*(RL↑)=empty`

for the chosen loop `C`, since all ray nodes lie above the genuine maximum.

### Canonical roof side ladder

For

`F_{2j}=4^jM`,

`Y_j=(2*4^jM-1)/3`,

one has

`Y_0=P=(2M-1)/3`,

`T(Y_j)=F_{2j}`,

`Y_{j+1}=4Y_j+1`.

For `j>=1`, `Y_j>M` and is external.

For every `a>=1`,

`Y_0,...,Y_(3^a-1)`

is a complete residue system modulo `3^a`.

This is the historical sibling odometer in roof coordinates.  It gives maximal finite-depth 3-adic coverage, not a forbidden address.

### Pure-odd feeder descendants

A consecutive-odd inverse chain of depth `r` from `F_{2j}` exists exactly when

`3^r |4^jM+1`,

and ends at

`X_{j,r}=2^r(4^jM+1)/3^r-1`.

Historical least-red minimality gives the exact scale-payment inequality

`(3/2)^r(R#+1)<=4^jM+1`.

### First-Farey finite re-entry theorem

Using RL85's

`M<3^27,021,536,997`

and the exact rational logarithm certificate

`3^1,000,000<2^1,584,963`,

any legal pure-odd descendant with `j>=1` and `X_{j,r}<=M` satisfies

`r<=42,828,136,343`,

`j<=12,526,437,560`.

Thus remote ray levels cannot send a pure accelerated-`r=1` feeder corridor back to roof scale.

### RL79 generalized-increment red-team

The entire feeder architecture scales to `T_c`:

`M_c=cM`, `F_{k,c}=cF_k`, `P_c=cP`, `Y_{j,c}=cY_j`,

with

`Y_{j+1,c}=4Y_{j,c}+c`.

Hence the ray/side-ladder structure alone is not an absolute `s=1` ownership discriminator.

## Frozen RL86 route verdict

RL↑ is closed as a **standalone closure architecture under current hypotheses**.

Retain the finite pure-odd re-entry theorem as a useful analytic lemma, but do not launch a brute-force search over the twelve-billion-level window without a new symbolic consumer.

No new property of the genuine maximum is forced merely by existence of the infinite ray.

## Authoritative RL87 live obligation

Return to the RL85 periodic/aperiodic splice.

At the exact first-Farey/full-phase intersection:

- every proper synchronized quotient return has span
  `<=43,234,459,194`;
- the RL73 giant macro therefore contains at least
  `930,959` spaced-distinct quotient states.

RL87 must seek a **genuine physical/full-`D` packing consumer** for those distinct states, while preserving:

- RL81's common-mode / quotient-to-physical lift barrier;
- RL79's unit-increment / generalized-cycle barrier;
- RL20 address-versus-physical/count decoupling;
- RL83 count-only insufficiency;
- the RL85 physical maximum cap.

Do not restart generic dyadic, residue, or continued-fraction work.

## Verification economy

After this bundle's outer sidecar, internal manifest, and fast RL86 verifier pass, accept the frozen ledger above.  Do not recursively rerun historical expensive certificates unless a dependency fails, a verifier fails, or a genuine contradiction triggers stop-and-repair.
