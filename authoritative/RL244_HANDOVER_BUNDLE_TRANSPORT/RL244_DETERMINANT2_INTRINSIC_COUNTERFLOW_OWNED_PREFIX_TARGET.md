# RL244 — intrinsic determinant-2 counterflow / owned-prefix physical-valley target

## Mission

Continue only the audited Radius-4 global bridge from RL243.

RL243 proves that the doubled selected shift cannot close Radius 4 by naive nonnegativity. Instead, with

`P_i=r-W_i^u(q)`, `Q_i=r-W_i^v(q)`, `ar-qell=2`,

one has

`Q_i-P_i=h_i-h_(i+q)`

and the two genuine determinant-4 physical full-word flows `A,B` satisfy

`beta(A),beta(B)>=Gamma_q-2>=min(k,8)-2`,

where `k=t+3` is odd and `Gamma_q` is the exact zero-to-positive owned-height entry charge.

## Unique target

Use this forced paired physical-valley structure together with the zero-carry canonical physical states and the RL64-RL69 ordered `d,T,H,J` / rank recurrence to obtain either:

1. an eligible primitive full-`D` exact-distance-4 self-rotation; or
2. a contradiction to genuine full phase.

Do not start Radius 5.

## Preferred attack order

1. Split by `beta(P)>0` versus `beta(P)=0`.
2. If `beta(P)>0`, exploit that every negative `P_i` is inherited by both determinant-4 physical flows on the `u` half: this is a same-index double physical valley, not merely two unrelated abstract negatives.
3. If `beta(P)=0`, use the integer mass-2 / adjacent-1-Lipschitz structure to force `P` to have exactly two unit defects. Then all unavoidable entry charge is carried explicitly by `Q=P-(h_shift-h)` at the owned zero-to-positive crossings.
4. At an entry crossing `h_i=0<h_(i+q)`, use the exact fact that each physical flow receives one of `P_i,Q_i` at the two paired half-roots `i,i+a`. Couple those roots to the actual canonical states and the height-one / terminal `T,J` data.
5. Use the exact quantitative floor `Gamma_q-2`, not generic packing or a logarithmic state-scale heuristic.
6. If exact Radius 4 appears, match every RL238/RL239 hypothesis line-by-line before applying it.

## Mandatory red teams

- `P,Q` are auxiliary half-word flows, not Radius-4 theorem inputs.
- No pure state-scale anti-concentration closure.
- No generic packing/product/CF closure.
- No bare radius-shell principle.
- No RL43/RL19 parameter transplant.
- No blanket half-prefix determinant positivity.
- Preserve the `(65,41)` missing-full-phase rejection.
- Gate A remains separate.
- Radius 5 remains inactive.

## Checkpoint classifications

- `R4_BRIDGE_PROVED`
- `R4_BRIDGE_REDUCED`
- `R4_BRIDGE_BARRIER`
