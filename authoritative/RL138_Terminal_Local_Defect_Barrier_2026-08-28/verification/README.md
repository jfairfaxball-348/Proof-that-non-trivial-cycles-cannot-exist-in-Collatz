# RL138 verification

Run `bash verification/run_fast_rl138_verifiers.sh`. It checks the exact
endpoint increments and compressed nonnegative defect family. The model is
explicitly nonphysical and is used only as a local-method barrier.
