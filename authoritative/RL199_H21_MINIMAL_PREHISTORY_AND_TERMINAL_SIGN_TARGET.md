# RL199 target — H21 minimal prehistory and terminal sign

Date: 2026-08-31.

Accept RL198's global H21 selector and exact minimal-prehistory interface:

- global H21 shallow states are exactly `011,111`;
- `e34=e33=1`;
- the co-owned `tau=35` zero-defect prefix contains no p-shift-compatible zero edge;
- all 34 costs in the `tau=34` suffix are one;
- the `tau=35` leading cost is one (`011`) or two (`111`);
- immediately before the terminal, both p-pair heights are 20, mechanical bit is 2,
  and the common owned numerator is `C_pre=14*3^34`, with `C_pre mod 8=6`;
- the terminal numerator is `7*3^35`, maximum height 21.

Primary target: use this exact preterminal object to decide a genuinely physical remaining bit.

A useful advance should do at least one of:

1. determine the final owned endpoint orientation/sign — which preterminal odd endpoint receives
   acceleration exponent one and reaches terminal height 21;
2. decide `e35`, equivalently the leading cost `d_lead=1` versus `2`;
3. connect either bit to an exact phase/rank, K/rho, complete-word normalization, Hensel/odd-residue
   or fixed-gap reconstruction constraint that excludes one global H21 state;
4. derive a physical incidence/sign theorem strong enough to relax the binding
   `{33,34,35}` H21 charging budget;
5. if these exact owned consumers are sharply insufficient, prove the named barrier and identify
   the next non-aggregate datum.

Do not return to N0/J00 abundance or the H21 zero-edge route: RL198 has excluded that consumer.
Do not infer terminal orientation merely from `C_pre mod 8`; retain endpoint ownership.
Preserve all physical-versus-necessary, finite-cutoff, rank/time and variation/excursion guardrails.
No H21 realization, exclusion or budget release is inherited unless separately proved.
