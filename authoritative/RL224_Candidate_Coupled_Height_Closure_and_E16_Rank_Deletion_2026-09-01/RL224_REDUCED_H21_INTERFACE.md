# RL224 reduced H21 interface

Date: 2026-09-01. Compact successor interface for RL225.

## Global necessary-rank state

- total necessary terminal ranks: **13,415,865,870**;
- above-p source count: **7,091,831,283**;
- below-p source count: **6,324,034,587**.

Gate A and Gate B remain globally open. Global nontrivial-cycle exclusion remains open. Physical H21 incidence and charge remain unproved.

## New e=16 closure

Terminal rank **34,124,151,203** (above-p small offset `e=16`) is now excluded.

The complete inherited e=16 family has **zero** survivors. The proof is candidate-coupled: for exact

`eta=eta_*+3^17 k`, `y_16=2^34 eta-1-3^16*2^13`, `h_16=1`,

the deterministic accelerated recurrence

`a_i=v2(3y_i+1)`,
`y_(i+1)=(3y_i+1)/2^a_i`,
`h_(i+1)=b_(i+1)-b_i+h_i-a_i`

must preserve `h_i>=0`. The full finite e=16 family is exhausted and every candidate violates this by transition index 346 at the latest.

Do not reintroduce any of the **139,581,280** RL223 phase-51 candidates or any of the **45,045** e=16 prefixes.

## Retained route locks

RL223 count-only odd-modulus saturation remains a valid blindness theorem. RL222 quotient-residue locks remain valid. No free fixed-count word completion is a legal candidate continuation.

The e=16 finite parameterization is scoped to e=16. It does not automatically describe remaining offsets.

## RL225 focus

The inherited exact above-p small-offset list below 56 had six alternatives. RL224 removes e=16, leaving the known alternatives at offsets **e=4, 28, 33, 40, 45**. RL225 should construct an exact candidate-coupled finite family for one remaining offset and attempt the same mandatory-height elimination or prove precisely why the transfer fails.

The e=4 route is a natural first target because the inherited selector already includes `eta=207 mod243` and state011, but no e=4 root-window/candidate-count formula may be assumed from the e=16 formulas without proof.

Knowledge catalogues remain stale/deferred.
