# RL347 scratch frontier

Status: FROZEN, NON-AUTHORITATIVE

The following ideas are retained for RL348 only as diagnostics. They are not promoted results.

## A. Crossing-outside source-defect skip candidate

For an over-half complete return whose q=0 complement lies wholly after the H-carry crossing, split
the inverse-oriented long return carefully at the lower/source matched endpoint. The exact matched
half-cycle quotient is governed by that source endpoint, not the right endpoint.

A plausible sufficient rejection test is to combine:
- the post-crossing corridor `1/lambda<h_source<1`;
- RL342's universal phase-potential growth bound on the residual segment;
- RL346 bounded incoming source signature;
- the genuine numerator identity `A=D0P-Xe`.

The live-session version using the right endpoint defect was invalid and must not be reused.

## B. Half-cycle contact orientation

For `L=ell`, use the promoted narrow difference parameter

`D=E-S`, `D even`, `2<=D<=2^36`.

The earlier live discussion suggested one-sided contact ranges relative to `j,k`; that orientation
was not promoted. RL348 may rederive it from the committed inverse-walk conventions if useful.

## C. Preferred order for RL348

1. Test the `L=ell` contact class through the bounded even difference `D` and exact mixed 2/3-adic
   endpoint equations.
2. Intersect the crossing-outside `L>ell` source-side H-carry corridor with bounded incoming
   signatures and genuine numerator ownership.
3. Use the decorated-rank theorem to keep every fixed decorated endpoint pair at one exact `L`.
4. Only then address the remaining `75<=L<ell` and over-half residual classes.

Do not turn any of these diagnostics into a suffix-total, q-upgrade, sigma, or coefficient ladder.
