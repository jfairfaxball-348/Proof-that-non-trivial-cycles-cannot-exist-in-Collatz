# RL316 session state and RL317 kickoff

Date: 2026-09-14
Completed RL: RL316
Incoming RL: RL317
Incoming status: PREPARED, NOT STARTED

RL316 proved an exact prefix-potential formula and used it to close the raw
row-energy calculation for the RL315 canonical interface.  The unsigned row
sum telescopes exactly to inherited full numerator ownership, so it is a
method barrier rather than an independent no-carry theorem.

For `g=2`, RL316 constructed rankwise late and early envelope words, proved
their exact numerator sum and primitive gap threshold, and showed that their
positive rational fixed states symmetrically bracket the two physical balanced
boundary states.  The RL315 quotient satisfies `n=XG+epsilon` and
`n+q_sigma=D0x`; `n mod X` deterministically decodes the outgoing row.

The frozen RL21 `X+Y`-factor countermodel saturates all of this structure while
failing only `X-Y` ownership.  RL317 must therefore attack the absolute
`D0=X-Y` dual-shadow factor, not return to unsigned row totals or `H`-only
geometry.

Fallback: independently replay the first reduced fibre before using any RL315
numerical scratch.

Authoritative target:
`authoritative/RL317_ABSOLUTE_DUAL_SHADOW_D0_OWNERSHIP_TARGET.md`.
