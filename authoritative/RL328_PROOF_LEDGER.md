# RL328 proof ledger — fixed-point descent experiments and strategic viability barrier

Date: 2026-09-15
Status: FROZEN FOR RL328 CLOSEOUT
Incoming BASE_HEAD: `f66176e3ae9dd502b0b99b90937e6f7aa5501429`
Successor: RL329

## Inherited authoritative branch

Work remains in the ordered genuine `g=2`, `Z0>0`, `K<0` late-row parent branch with `(a,ell)=(217976794617,137528045312)`, `d=80448749305`, `lambda=2^a/3^ell<1+2^-40`, RL327 cap `1<=n<=32596612662`, and high-carry ownership threshold `n>=20390252058`. RL324's local-propagation barrier remains binding.

## Preserved shorter-singleton scratch

RL328 extended the exact owned-singleton idea below RL327's total-46 layer. The furthest recorded candidate checkpoint was:

- assumption `n>=32560139786`;
- 9087 compressed singleton congruence classes representing 4034380 physical realizations;
- 474 singleton pair types;
- 233 exact shared-state pair-type links;
- 523 automaton states and 50058 edges;
- scaled potential `1000 Z <= 21889 K + 65667`;
- `K>=6008477662` at `rho=60`;
- reported telescope `n<32560139785.461258...`;
- candidate cap `n<=32560139785`.

Earlier scratch checkpoints included total-44, total-42/41, and total-40 layers.

Classification: **preserved non-promoted exact-certificate candidate / session scratch**. No standalone portable gap-free enumerator for all shorter layers is promoted here. The authoritative carry cap remains RL327's proved `n<=32596612662`.

## RL328.1 — repeatable unrestricted >=3-positive cycle barrier

RL327's conservative graph admits every transition generated by a positive run of length at least three and zero plateaux through length 49. Hence it contains a repeatable `N(49)->N(49)` edge/cycle with `Z=49`, `K=3`. Repetition shows no finite-boundary potential on this unchanged graph can prove asymptotic `Z/K<49/3`, regardless of singleton certification.

Classification: **proved analytic method barrier**.

## RL328.2 — optimistic singleton-only method ceiling

At `rho=60`, grant the unrealistic best case `Z/K<=49/3` with zero boundary. Then `K>=ceil(3(T+1)/52)=7934310304`. Feeding that through the inherited exact residue-min weighted telescope yields `n<32393913987.784203...`, still more than twelve billion above `20390252058`.

Classification: **proved analytic route-viability diagnostic, not a new physical carry cap**.

## RL328.3 — density required by the present consumer

Solving the inherited telescope exactly for the first integer `K` forcing the RHS below `20390252058` gives `K=112933933538`, about 82.117% of the matched interval, equivalently `Z/K<0.217775`.

Classification: **proved exact analytic diagnostic**.

## RL328.4 — height-only strengthening ceiling

Within the same linear coefficient family, taking a positive rank from `q_t>=1` to arbitrarily large `q_t` can at most double its loss. Even granting that maximal factor-two improvement uniformly, the high-carry floor still requires `K=64392480586`, about 46.821% of the interval, equivalently `Z/K<1.135779`, still far below `49/3`.

Classification: **proved analytic ceiling on height-only strengthening of the current linear consumer**.

## Strategic conclusion

The shorter-singleton descent is a genuine local mechanism but is strategically insufficient as the default closure route. Do not continue total-38, total-37, ... enumeration automatically. The successor needs genuinely global structure: long-positive-run ownership/descent, a smaller genuinely owned balanced return, global run/residue coupling, or a nonlinear/height-sensitive consumer materially stronger than the present linear K-count telescope.

`PARENT_DIFFICULTY_DELTA = BETTER LOCALIZED, NOT CLOSED`.
