# RL98 — closeout and pivot to RL99 global audit

Date: 2026-08-25

## 0. Purpose of this closeout

RL98 is being closed as a **strategic checkpoint / deferred attack**, not as a completed execution of the live coupled-cascade target.

No new mathematical theorem, scan band, interval exclusion, support crossover, or floor event is promoted in RL98 beyond the frozen RL97 state already carried into the session.

The intended RL98 target — sustained corrected coupled cascade beyond deep radius `3,000,000` — remains a live, high-value attack and is explicitly preserved for resumption after the RL99 audit. It is not abandoned, demoted, or declared exhausted.

The reason for the pivot is deliberate: after ninety-eight numbered research sessions, and after a long recent run of increasingly effective narrowing on one first-Farey/full-phase Gate-A branch, RL99 will be used as a **whole-program audit, synthesis, correction review, route assessment, and RL100+ roadmap-setting session**.

## 1. Frozen global theorem status entering RL99

Retain exactly:

- radius-3 primitive/full-`D`: **closed local theorem**;
- Gate A even terminal `k`: **closed analytically**;
- Gate A terminal `k<=25`: **closed by inherited exact finite certificate**;
- Gate A odd `k>=27`: **open globally**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

The recent first-Farey/full-phase cascade is branch-specific. Its successful interval narrowing must not be silently promoted to global Gate-A closure.

## 2. Frozen latest exact branch state

Authoritative latest completed mathematics is RL97.

### Exact certified tiers

- wide: `d<=5,000,030`, `r<=7,000,000` => successor `<=5,000,053`;
- middle: `d<=7,500,032`, `r<=4,350,000` => successor `<=7,500,053`;
- deep: `d<=10,000,032`, `r<=3,000,000` => successor `<=10,000,053`;
- ultra: `d<=15,000,035`, `r<=1,680,000` => successor `<=15,000,053`.

Frozen global bit-length minima:

- middle `7,500,034 at r=1,635,300` => `D_m=7,500,032`;
- deep `10,000,034 at r=378,722` => `D_d=10,000,032`;
- ultra `15,000,037 at r=575,974` => `D_u=15,000,035`.

RL97 found additional ultra ties at `r=905,728` and `r=1,368,912`, and a deep near-floor event `10,000,036 at r=2,955,163`; none lowers the frozen global floors.

### Exact support / geometry state

Final RL97 corners:

- `A=(0,7,000,001)`;
- `B=(5,000,031,4,350,001)`;
- `C=(7,500,033,3,000,001)`;
- `D=(10,000,033,1,680,001)`;
- `E=(15,000,036,0)`.

Feasible exact pair supports remain `A-C`, `C-D`, `D-E`.

Live optimal support at the frozen endpoint:

- `mu=1/7,000,001`;
- `lambda=4,000,000/52,500,238,500,033`;
- `floor(lambda*C)=3,211`.

Thus the inherited top-stratum argument still makes `b=k-1` the worst block-count stratum at the frozen point.

### Exact interval state

RL97 eliminated every odd exact first-Farey/full-phase Gate-A terminal value

`2,921,774,731 <= k <= 2,921,801,521`.

This is `13,396` additional consecutive odd values.

The first surviving odd candidate for this branch is

`k8=2,921,801,523`.

Frozen exact margin:

- at `k=2,921,801,521`: `+10,473`;
- at `k8=2,921,801,523`: `-3,549`.

Retain conservative common successor ceiling

`L_common=15,000,053`.

The branch-specific surviving odd window is

`2,921,801,523 <= k <= 42,150,931,559`, `k` odd.

## 3. Deferred RL98 cascade target — preserve exactly

The live cascade target remains:

1. ultra `1,680,001..1,705,000` or slightly beyond;
2. immediate ultra global-minimum recomputation;
3. deep `3,000,001..3,025,000`;
4. exact support rebuild and re-optimization;
5. immediate interval propagation from `k8`;
6. then middle to at least `4,375,000`, ultra to at least `1,740,000`, deep to `3,050,000`;
7. continue through adjacent `25,000`-step deep batches while productive;
8. track live floor changes, `A-C`/`C-D` competition, and the approach to the deep-radius `3,499,992` ultra depth-axis threshold.

For `R_d=3,025,000`, unchanged depth floors require:

- `R_m>=4,349,996` (current `4,350,000` already clears by four);
- `R_u>=1,700,006`.

For `R_d=3,050,000`, unchanged floors require:

- `R_m>=4,366,662`;
- `R_u>=1,733,340`.

This exact restart state is also frozen separately in `RL98_COUPLED_CASCADE_RESUME_AFTER_RL99.md`.

## 4. What RL99 is for

RL99 is not merely another audit of the most recent bundle. It must answer:

- What has actually been proved across the complete RL programme?
- Which statements are analytic theorems, exact finite certificates, inherited external certificates, computational evidence, conjectures, or method barriers?
- Which earlier claims were corrected or demoted, and what later results depend on them?
- Which results are genuinely global and which only live inside restricted branches such as safe-CF, first-Farey, full-phase, minimal-reset, or particular ownership interfaces?
- Which lines of attack have repeatedly converged on the same missing ingredient?
- Which apparently different routes are algebraically equivalent, homogeneous in the generalized-increment parameter, or otherwise blocked by known countermodels?
- What new structure from RL80–RL97 was not available at the RL72 and RL75 audits?
- Does the blue/dyadic/four-tier cascade alter the global route ranking or merely sharpen one branch?
- Is there a credible path from the current branch machinery to a global Gate-A statement?
- Should RL100 resume the cascade, attack its branch-to-global lifting problem, pivot back to Gate B, or launch another globally discriminating route?

RL99 must finish by setting a concrete RL100 target and a ranked RL100+ programme.

## 5. Verification economy and audit discipline

Use the existing verification-economy rule:

> After the current bundle checksum, internal manifest, and fast verifier suite pass, accept the frozen incoming proof-state ledger as authoritative. Do not recursively rerun historical expensive certificates unless a new argument depends on an unresolved historical definition, a verifier fails, or an apparent contradiction triggers a stop-and-repair event.

For RL99 this means:

- audit the *logical interfaces, scope, dependencies, corrections, and strategic use* of historical results;
- selectively open the source sessions needed to verify a load-bearing interface;
- do not spend the session replaying old expensive scans/certificates merely to reconfirm already frozen facts;
- if a contradiction in the ledgers is found, stop and repair it before roadmap promotion.

## 6. Archive / provenance intent

The entire incoming RL98 authoritative tree is to be archived unchanged under `sessions/RL98/`.

That archive is the exact restart source for the deferred cascade.

RL99's new authoritative handover is intentionally separate so that the audit can range over the entire repository without overwriting or obscuring the latest attack state.
