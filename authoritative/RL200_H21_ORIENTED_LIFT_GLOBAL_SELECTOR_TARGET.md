# RL200 target — H21 oriented-lift global selector

Date: 2026-08-31.

Accept RL199's exact oriented-lift reconstruction:

- lower `tau=34` endpoint `Y_0^-=2^34 eta-1`;
- upper endpoint `Y_0^+=2^34(eta+21)-1`;
- `011 <=> eta=0 mod 9`, `111 <=> eta=8 mod 9`;
- positive terminal defect iff `eta` is even, negative iff `eta` is odd;
- the four necessary classes `eta=0,8,9,17 mod 18`;
- exact terminal Hensel exclusion and the scoped common-pair information-loss theorem.

Primary target: globally constrain the oriented lift `eta` strongly enough to remove at least one
of those four classes, decide `e35`, or decide terminal sign.

Preferred consumers:

1. complete-word / fixed-gap reconstruction that recovers the absolute lower endpoint modulo an
   odd or 2-adic modulus;
2. exact rank-resolved K/rho information capable of consuming RL199's signed subunit terminal
   K increment;
3. oriented ternary-suffix or Hensel prehistory with actual exponent/height constraints before
   `tau=35`;
4. a phase/rank/ownership identity that distinguishes the lower endpoint from the upper endpoint;
5. failing selection, a sharp theorem proving a named oriented consumer insufficient and
   identifying the next datum.

Do not infer a class from the eta-independent common pair numerators/heights, do not revive the
H21 zero-edge route, and do not substitute N0/J00 abundance for oriented incidence. Necessary
rank classes are not physical populations. Preserve all Gate/global and finite-cutoff guardrails.
