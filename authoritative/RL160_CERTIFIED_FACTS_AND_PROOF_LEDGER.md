# RL160 Certified Facts and Proof Ledger

## Inherited authoritative state
- RL159 is accepted as the frozen incoming state after its transport/integrity gate and fast verification passed.
- RL159 status: productive obstruction, not global closure.
- The one-binomial resultant route (RL158) and joint distinguished-root Sylvester/SNF route (RL159) are not valid discriminators without independent non-tautological information.
- Singleton/full-phase ownership remains a live local obligation.

## Newly certified in RL160

### `RL160-A` — Fixed-radius joint-factorization barrier
**Class:** proved analytic mathematics.  
**Statement:** if each proposed local observable factors through `π_r(u)=u mod 2^r`, then their joint observable also factors through `π_r`.  
**Effect:** local coordinate stacking cannot produce an independent finite-radius discriminator.

### `RL160-B` — Ordered-valuation barrier
**Class:** proved analytic mathematics + deterministic exact certificate.  
**Statement:** unordered valuation data do not determine chronological affine closure data.  
**Witness:** `(1,2)` and `(2,1)` both have multiset `{1,2}` and sum `3`, but give `(9x+5)/8` and `(9x+7)/8`.  
**Effect:** valuation counts/multisets cannot be substituted for ordered orbit data in a closure proof.

## Consolidated method barriers
- **Dead in purely local form:** endpoint/suffix/branch/no-carry/reconstructed-valuation stacking at one fixed radius.
- **Dead as current discriminator:** same-distinguished-root polynomial elimination/resultant/Sylvester/SNF stacking without an independence theorem.
- **Dead as closure surrogate:** unordered valuation histogram, multiset, or total valuation used as if it determined the chronological affine numerator.

## Live theorem families
- ownership + weighted-difference/perimeter;
- ownership + population/packing;
- ownership + signed global defect / positive lifts;
- ownership + exact cycle product/divisibility;
- ordered valuation/phase word + global closure.

## Open obligations
1. Prove an exact ownership-sensitive quantitative lower bound or incompatibility that survives dyadic lifting.
2. Couple that bound to a whole-orbit identity or packing constraint.
3. Show the result applies to every putative non-trivial cycle, not just a finite-radius branch family.
4. Preserve chronological data whenever the closure numerator depends on order.

## Claim discipline
No item in RL160 is classified as a proof of the Collatz conjecture or as a proof that non-trivial cycles cannot exist. RL160 is a barrier synthesis and route-narrowing session.
