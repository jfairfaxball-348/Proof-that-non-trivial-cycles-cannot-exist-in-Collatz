# RL243 route-reduction transport

Completed generation: **RL243 — determinant-2 half-word / owned entry-height counterflow reduction**.

Classification: **`R4_BRIDGE_REDUCED`**.

Promoted outcomes:

- exact determinant-2 half-word identity `Q-P=h-h_shift`;
- exact two physical determinant-4 envelope identities;
- retained resonance sharpened to `19/12<a/ell<8/5`, implying `ell>=17`;
- doubled determinant-2 shift is at least eight columns from either half boundary;
- inherited full-phase terminal `k=t+3` is odd;
- intrinsic determinant-2 counterflow floor `beta(P)+beta(Q)>=min(k,8)-2`;
- stronger physical paired-valley bound `beta(A),beta(B)>=Gamma_q-2>=min(k,8)-2`;
- the earlier minimal nonnegative second-order corridor is eliminated.

Gate A, Gate B, and global exclusion remain open. Radius 5 remains inactive.

The canonical RL243 ZIP is carried losslessly as base64 text parts in this directory. Reconstruct with `reconstruct_rl243_bundle.py` and verify against the outer authoritative `.zip.sha256` sidecar.

Canonical ZIP SHA256:
`4e7a85402d9af86ec7c0dc3282aafcec18acf97f9c8ca1402849e99cb7afa08f`.
