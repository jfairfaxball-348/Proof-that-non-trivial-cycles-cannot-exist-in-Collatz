# RL209 red-team report

Date: 2026-08-31. Result: **PASS at the promoted scopes below**.

1. **Source/terminal coordinates.**  Source is `a=i-34 modL`; terminal rank is
   `r=iB modL`.  The RL209 rank cuts are performed in terminal coordinates and
   reported as above-p only after this translation.
2. **No reflected C2 theorem.**  The above-p decomposition is derived directly.
   It gives `S_a>=u`, the opposite information regime from the below-p vanishing
   root terms.  No 37/60/97 reflection is used.
3. **Lift/carry.**  A canonical wrap of `a+p` uses the lambda-quasiperiodic lift.
   The epsilon carry is unique at phase `L-p`; wrap and carry are not conflated.
4. **Pointwise root scope.**  All RL209 root paths lie in
   `[L-2^35,L)`, which is strictly above the unique carry phase.  The summed
   `|Delta K|<1/3` inequality never crosses that carry.
5. **Absolute anchor defect.**  The `1/9` and `1/8` bounds on `K_L-K_0` are
   certified by outward fixed-point logarithm intervals, not floating-point
   cancellation.
6. **Finite count completeness.**  The old backward root window is checked at
   every one of its 1,969 current survivors.  Each of the eleven RL208 backward
   dyadic bands is partitioned into exactly 4,096 disjoint sublayers.  Modular
   floor sums count every claimed rectangle; no materialized billion-rank set is
   assumed.
7. **Disjointness.**  The verifier finds zero preexisting isolated/anchor
   deletion hits in the new rectangles.  Every new cut is applied only inside
   the exact RL208 current safe band.
8. **Signed successor independence.**  Both signs and all 21 valuations are
   checked on each of the 986 exact old-root survivors.  All 41,412 pass, so no
   selector is relabelled from this failed consumer.
9. **RL195/RL201 barriers.**  The complete-word denominator equivalence and the
   continuous-mass/coupled-reverse barriers remain binding.  The endpoint
   decomposition is not claimed as an independent eta determination.
10. **Physical/Gate locks.**  Necessary-rank deletion is not physical H21
    occurrence or charge.  No branch result is promoted to Gate A/B or global
    Collatz closure.

No new correction or demotion was triggered.
