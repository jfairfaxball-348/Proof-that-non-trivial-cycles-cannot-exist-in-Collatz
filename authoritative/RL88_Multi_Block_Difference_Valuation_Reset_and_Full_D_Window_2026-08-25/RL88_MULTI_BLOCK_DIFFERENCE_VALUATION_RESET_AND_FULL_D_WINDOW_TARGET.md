# RL88 target — multi-block difference-valuation reset and full-D non-recycling

Date: 2026-08-25

## Authority and operating rule

Continue from the frozen RL87 state.

RL87 found the missing physical consumer for a **single** genuine synchronized block: synchronized depth is exactly priced by powers of two in the physical paired-state difference.  Combined with RL85's first-Farey maximum cap, this eliminates the RL73 low-`k` giant macro on the exact first-Farey/full-phase branch and forces any surviving first-Farey Gate-A violation into a finite high-`k` window.

Do not restart generic quotient finite-fiber packing, dyadic-funnel, fixed-depth residue, or continued-fraction work unless a new reset theorem requires a narrowly specified historical interface.

Apply the verification-economy rule after the current bundle checksum, internal manifest, and fast verifier pass.

No Gate A, Gate B, RL/nontrivial-cycle, or Collatz closure may be claimed unless actually proved.

## 0. Frozen quantitative input

Exact first Farey pair:

`p=114,208,327,604`,

`q=72,057,431,991`.

Physical maximum cap:

`M<400,000,000,000(3/2)^q`.

Exact logarithm certificate:

`3^1,000,000<2^1,584,963`.

Therefore every genuine synchronized physical block satisfies

`boxed: n<=C=42,150,931,628.}`

RL73 global exponent-skew floor:

`Delta>=Delta0`,

`Delta0=13,201,833,154,443,526,323`.

For a Gate-A violation with terminal `k`, the number of post-first-mismatch height-one aligned `00` columns satisfies

`boxed: Z1(k)>=ceil[(Delta0-5k+7)/2].}`

There are at most

`e<=H<=k-1`

maximal post-first-mismatch height-one synchronized blocks.

RL87 consequence:

any first-Farey Gate-A violation must have odd

`boxed: 156,601,917<=k<=42,150,931,559.}`

The lower endpoint comes from single-block physical capacity; the upper endpoint comes from RL81 terminal state

`B_j=2^(k-2)N`, `N>=2^71`, `B_j<=M`.

## 1. Exact block-entry normal form

At a height-one block entry,

`J=3A-B+1`,

`alpha=(J-1)/2`,

`delta=B-A`.

If the synchronized block has length `n`, RL87 proves

`2^n|delta`,

and therefore

`boxed: A=alpha+2^(n-1)m,}`

`boxed: B=alpha+3*2^(n-1)m.}`

For a standard maximal mismatch exit, `m` is odd and

`boxed: n=v_2(B-A).}`

This is the primary RL88 coordinate.

## 2. The exact missing theorem

Single-block depth is no longer scale-free.  The unresolved question is whether many separate block-entry valuations can be regenerated by the active excursions.

Preferred theorem shape:

> For a genuine primitive full-`D` paired phase inside the first-Farey physical maximum, the maximal height-one synchronized blocks with entry differences `delta_r` obey an owned upper budget
>
> `sum_r Phi(v_2(delta_r)) <= Budget(M,N,D,k,H)`
>
> that is incompatible with the RL73 lower bound on the total aligned-zero mass.

The simplest useful case would be

`sum_r v_2(delta_r) <= O(log M + k)`

or any bound substantially smaller than `C(k-1)`.

Do not assume such a theorem; derive or refute it.

## 3. Derive exact 01/10 reset laws first

For the raw physical difference `delta=B-A`, the mismatch maps are

`01: delta'=(3B+1-A)/2`,

`10: delta'=(B-3A-1)/2`.

These depend on the physical common mode and are precisely where RL81's free-mode barrier can re-enter.

Derive the reset in the project's exact `d,T,J,g` coordinates and, where useful, in the full-phase affine representation of `A,B` in terms of the common midpoint `N`.

Questions:

1. after a `10` return to height one, what exact affine/congruence condition determines `v_2(B-A)` at the new block entry?
2. does a high entry valuation force `N` into one residue class modulo a comparably large power of `2`?
3. are the residue conditions from successive blocks nested, independent, or freely reset by the changing word constants?
4. can the same high-valuation difference recur at two distinct phase positions without forcing a short paired-state return?

## 4. Full-D coupling

Use the genuine full-phase relation

`D=2^a-3^ell`,

`D | Q(v)+4*3^ell`,

with physical midpoint

`N=(Q(v)+4*3^ell)/D`.

`D` is odd, so it does not directly absorb powers of two.  That makes it plausible that high `v_2(B-A)` constraints become sharp congruences on the numerator/common midpoint rather than disappearing into `D`.

Seek an exact statement of the form

`N == c_r (mod 2^t_r)`

or

`D N == C_r (mod 2^t_r)`

at each high-valuation block entry.

The target is not merely to produce many congruences; it is to prove that the same physical `N` cannot satisfy the family required by the RL73 zero-mass lower bound, or to prove that the congruences automatically transport and hence are not independent.

## 5. Multiplicity / packing route

For a threshold `t`, define block entries with

`v_2(B-A)>=t`.

Because

`A=alpha mod 2^(t-1)`,

one block entry lies in a sparse physical lattice once `J` is known.

Test whether genuine cycle ownership gives a uniform multiplicity bound on such entries that is logarithmic or polynomial in `k`, rather than `O(M/2^t)`.

A raw physical-box count is not useful: `M` is exponentially larger than all current block counts.

Potential ownership hooks:

- fixed phase offset between the two paired trajectories;
- primitivity of the underlying cycle;
- exact full-`D` rotation/content;
- terminal midpoint `N` shared by all blocks;
- the fact that mismatch returns are paired `01/10` events and are limited by area `H`.

## 6. Distributional lower bound

Do not rely only on the average block length.

RL73 gives a total aligned-zero lower mass

`Z1(k)>=ceil[(Delta0-5k+7)/2]`.

With at most `k-1` blocks and individual cap `C`, derive the strongest forced tail statement for the block-length multiset.

For example, for any threshold `t<C`, if `m_t` blocks have length at least `t`, then the total capacity of the remaining blocks is at most

`m_t C + (k-1-m_t)(t-1)`.

Invert this to force a minimum number of high-valuation entries.  Then ask whether those entries can be packed in the full-`D` physical lattice.

This may be more useful than a single average.

## 7. Terminal ownership and finite k window

Use the upper window endpoint aggressively:

`k<=42,150,931,559`.

The terminal state already contains `2^(k-2)N`, so the physical object carries a large known dyadic factor near its end.  Investigate whether the block-entry dyadic valuations can be related to, or must compete with, this terminal dyadic structure.

Do not assume conservation of `v_2`; mismatch steps can reset it.

The target is an exact reset-budget theorem.

## 8. Mandatory red teams

Every proposed global valuation consumer must pass:

1. **RL81 common mode:** changing the physical common mode may satisfy large dyadic congruences without changing quotient data;
2. **RL79 generalized increment:** the synchronized difference law itself is homogeneous, so the argument must consume an absolute ordinary-`s=1` full-phase/physical fact;
3. **RL20 physical packing:** residue/congruence abundance is not a physical upper budget unless ownership and representative range are exact;
4. **primitivity:** only repeated paired physical states, not repeated differences alone, force a cycle return;
5. **first-Farey scope:** do not promote the branch-specific maximum cap to all Gate-A branches;
6. **verification economy:** do not rerun historical expensive certificates without a failed dependency.

## 9. Desired outcomes

In priority order:

1. a non-recycling theorem that makes the RL73 total zero mass incompatible with the surviving first-Farey k window;
2. a full-`D` congruence theorem giving incompatible high-valuation requirements on the shared midpoint `N`;
3. a strong multiplicity bound for high-valuation block entries;
4. a further substantial narrowing of the finite first-Farey k window;
5. a rigorous counterfamily showing that active resets can recycle arbitrary valuation while preserving all currently used full-phase conditions, thereby freezing the route cleanly.

A rigorous negative result is acceptable.  Do not fall back to the already-settled 930,959 quotient-only `O(M)` packing route.

## Close-out requirements

At the end of RL88:

- freeze the exact theorem/evidence/barrier ledger;
- preserve RL87's physical block cap and finite first-Farey k window;
- record whether multi-block valuation recycling was bounded or shown flexible;
- create the next numbered authoritative bundle and `.sha256` sidecar;
- fresh-unpack, verify the internal manifest, and run the fast verifier;
- do not claim Gate A, Gate B, RL/nontrivial-cycle, or Collatz closure unless actually proved.
