# RL219 target — recover or continue certified-blue lattice recognition

Date: 2026-09-01. Classification: **continuation of interrupted RL218 exploratory target**.

Accept the complete RL218 closeout and all inherited RL217 mathematics. RL218 promoted no new mathematics.

## Recovery-first rule

The user reported that Codex had substantial local RL218 work when it stopped. If that scratch is available to the current worker: inspect it before duplicating research; separate finished derivations from partial calculations; independently verify every proposed theorem/certificate; promote only material that passes current proof-state, scope, red-team and packaging gates.

If the scratch is unavailable, continue from the exact interface below.

## Exact incoming arithmetic interface

The modern e=16 family remains **45,045** prefixes and **139,581,280** arithmetic candidates. Each prefix has `y_0 = y_0^* + 3*2^58 k` on a short exact finite `k` interval with inherited terminal-Hensel restrictions, plus an exact RL217 phase-51 union of **3,132,617** disjoint 2-adic survivor cylinders of maximum **25-bit** precision.

The global necessary-rank frontier remains **13,415,865,871** and terminal rank **34,124,151,203** remains live.

## Mandatory inherited blue-route locks

RL80/RL81 are not to be re-litigated: pure dyadic saturation and arbitrary backward closure are cycle-intersection neutral as set-theoretic traps; large/dense/log-dense blue coverage is insufficient; proximity is not equality; auxiliary-coordinate basin capture does not transfer to a physical cycle integer without a basin-preserving equality theorem; a residue match with a certified-blue branch is not itself certified-basin membership.

## Primary mathematical task

Derive the exact algebraic form of an arbitrary finite **legal** backward shortcut-Collatz word from an exactly certified-blue seed. Record its affine/rational form, powers of 2 and 3, integrality and legality conditions at every odd-predecessor step, and the exact conditions under which the resulting integer is certified to reach the seed and hence 1.

Then substitute `y_0 = y_0^* + 3*2^58 k` and the phase-51 2-adic selector. Determine whether exact backward-tree membership yields a useful selector on `k`: an exact congruence or finite union, valuation/divisibility condition, affine-lattice intersection, exact parameterized family removing whole candidate subsets, or a precise theorem explaining why fixed-depth recognition cannot compress the present lattice.

## Success criteria

A strong positive outcome is exact elimination of candidates, whole `k` classes, prefixes, states, mod18/eta classes, or a necessary terminal rank because the **same integers** are proved certified blue.

A useful negative outcome is a precise modern no-go or quantifier/ownership obstruction explaining why finite backward recognition cannot distinguish the RL217 lattice at the required scope.

Do not spend the session merely generating more blue numbers or increasing backward depth without structural gain. Preserve all proof-state classifications, verification economy, correction/demotion ledgers, and atomic handover rules.
