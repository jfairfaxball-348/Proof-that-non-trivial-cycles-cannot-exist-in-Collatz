# RL340 proof ledger — exact negative-return identity frontier

Date: 2026-09-16
Status: CLOSED AND FROZEN
Incoming authoritative HEAD: `3504e01531b7c45699f35a4fbb42460008b83b73`
Successor: RL341

## Scope retained

RL340 worked only in the ordered genuine `g=2`, `Z0>0`, `K<0` parent branch at `(a,ell)=(217976794617,137528045312)`. The external least-state-floor qualification `m>=2^71`, q=0 source band `[2^71,2^76+2^36)`, inherited `z<=35`, RL337 exact affine/suffix identities, RL338 q=35 amortization, and RL339 singleton escape / 42-density / phase theorem remain unchanged. The separate Lean formalisation project was not touched.

R1, Gate A, Gate B, later `g=2`, `g=1`, and the global theorem remain OPEN.

## RL340.1 — promoted exact `(23,23,p=6)` escape certificate

Exact q=0 physical reconstruction was run for the final near-extremal interface type isolated during RL340, `(left,right,p)=(23,23,6)`. The complete reconstruction has mechanical factor length `L=51`; exactly 52 rational-mechanical base factors; the complete inherited admissible p=6 profile grammar; exactly 684 admissible deformed templates; exactly 10 q=0 band candidate rows; exactly 10 distinct source states; every candidate source escapes below `2^71` under deterministic odd Collatz iteration; maximum escape depth 29 odd steps.

Therefore, under the inherited external least-state-floor assumption, no live cycle in this parent branch can contain this `(23,23,p=6)` interface.

Classification: **exact gap-free finite escape certificate**, conditional only on the inherited branch, q=0 band, and external floor.

## RL340.2 — physical-identity contraction candidate, frozen but unpromoted

During research, retaining exact source/target interface identity successively removed the near-zero anonymous cycles that remain in the q=35 negative-return label quotient. A finite label-potential calculation produced the candidate inequality

`13 rc + 5p + Psi(left)-Psi(right) <= 0`

for the surviving return layer, leading formally to the candidate all-length inequality

`35(K-2H)-S <= -(5/13)K + 538/13`,

equivalently

`2H >= (92/91)K - S/35 - 538/455`.

The last abstract extremal self-loop encountered by that calculation was precisely `(23,23,p=6)`, which RL340.1 now excludes physically.

However, RL340 closeout does **not** promote this `92/91` theorem. The complete finite edge set and potential values were not packaged into a standalone replayable certificate before CLOSEOUT_LOCK. The result is therefore frozen as a high-value theorem candidate / scratch frontier only. RL341 must independently replay the finite interface calculation before using it as authoritative mathematics.

Classification: **UNPROMOTED theorem candidate**. It must not be cited as a proved RL340 theorem.

## RL340.3 — promoted direct-consumer route barrier

The inherited RL339 conditional carry cap is `n <= 32,537,248,343`, while the fixed high-carry ownership threshold is `20,390,252,058`. The remaining gap is exactly `12,146,996,285`.

In the inherited linear residue/telescope architecture, a unit of additional structural weighted-support gain changes the carry RHS by at most `1/(12 Lambda)`, with `Lambda=1+2^-40`. Hence closing this entire gap through that same linear consumer would require more than

`12 Lambda * 12,146,996,285 > 145,000,000,000`

additional weighted-support units.

At the worst inherited endpoint `rho=60`, RL339 has only `K=6,251,274,783` minimal positive ranks, so the required gain exceeds 23 units per positive rank. Consequently, another small q-charge improvement, endpoint constant improvement, or phase-weight refinement cannot plausibly close R1 through the same linear consumer.

Classification: **exact arithmetic route barrier**, not a no-cycle theorem.

## Strategic consequence

The next session should not make another q-upgrade or phase-weight refinement its primary route. The live mathematical opportunity is the exact-state phenomenon itself: anonymous near-extremal cycles disappear when source/exit identity is retained. RL341 should seek a canonical state quotient that is acyclic, forces owned descent, or forces a return below the least state, preferably using RL337 fixed-depth right-suffix compression to retain all-length coverage.

No inherited theorem is corrected or demoted.

`PARENT_DIFFICULTY_DELTA = EASIER`.
