# RL215 correction / demotion ledger

Date: 2026-09-01.

No inherited theorem or exact finite certificate is demoted.

## New scope locks

- RL215's lower bound requires the **complementary** `z=L-p` accelerated arc, its exact return `y_L=y_0`, the inherited root gap `y_p-y_0=K0`, and positivity of that arc's affine numerator.
- The lower bound is a necessary condition for a physical owned H21 word. Passing it does not construct a complete word or prove physical realization.
- The 331,935,285 surviving objects are arithmetic root candidates, not cycles, H21 populations, incidences, charges or necessary ranks.
- The 1,297,883,646 candidates removed relative to RL214 are arithmetic-lift deletions only; the e=16 terminal rank is not deleted.
- The terminal Hensel filter removes 170 candidates inside the new window but no prefix.
- Bare ownership still does not determine `(Q/D) modD`; `Q modD^2` or equivalent independent discrete information remains open.
- A shifted e=16 p-arc roof bound was checked and is weaker than the complementary-arc bound; do not count it as an additional independent deletion.
- RL213 endpoint-moment collapse and G56 saturation, RL211 flat-K denominator barrier, and RL209 signed-successor scope remain binding.
- All RL206 corrections, below-p `37/60/97`, p-shift carry and physical/charge/Gate/global locks remain binding.
