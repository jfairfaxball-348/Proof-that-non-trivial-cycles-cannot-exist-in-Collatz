# RL214 exact ownership-quotient / root-cap certificate

Date: 2026-09-01. Classification: **proved analytic mathematics plus exact finite arithmetic certificate** at the scopes separated below.

All physical implications remain conditional on a physical H21 realization of the sole inherited high branch `(37,0,23,-1)`. Arithmetic root candidates are not physical cycles or populations.

## 1. Exact full-word quotient bridge

Let the complete binary word `d=(d_0,...,d_(A-1))` be aligned so phase zero is the H21 odd root `y_0`. Expand the accelerated odd-step exponents into the full binary word: a `1` occurs at each cumulative odd-step exponent and the intervening positions are `0`.

The RL206 full-word recurrence has the unique rational solution
`R_m=Q(rot_m(d))/D`, where `D=2^A-3^L`.
At every cumulative odd-step position the full-word state is exactly the corresponding accelerated odd state. In particular at the aligned root,

`y_0 = R_0 = Q(d)/D`.

Thus the RL206 owned-solution quotient coordinate and the current H21 odd root coordinate coincide **after this explicit phase alignment and expansion**. This is the requested coordinate bridge; no identification is assumed by notation.

Bare ownership still gives only `Q(d)=D y_0`. To determine `y_0 mod D` from ownership alone one needs `Q(d) mod D^2`, exactly as in the RL206 quotient-residue exception. The bridge therefore does not manufacture an independent residue.

## 2. New global positive-p-arc root bound

Across the first `p` accelerated odd steps from the root, `h_0=h_p=0` and the total acceleration exponent is `u`. Let `P_p^(odd)>0` denote the ordinary positive affine numerator of that nonempty p-arc. Exact iteration gives

`2^u y_p = 3^p y_0 + P_p^(odd)`.

The inherited root gap is `y_p-y_0=K_0=2^37`. Therefore

`2^u K_0 = (3^p-2^u)y_0 + P_p^(odd)`.

Put `alpha=3^p/2^u`. Since `P_p^(odd)>0`,

`0 < y_0 < K_0/(alpha-1)`.                                      (1)

This inequality is not the RL201 endpoint moment. It uses positivity of a genuine full p-arc numerator.

For an entirely rational displayed bound, write `delta=A ln2-L ln3=ln(lambda)`. From `Ap-uL=1`,

`L ln(alpha)=ln2-p delta`.

The inherited `0<delta<2^-40`, together with `p<2^36`, gives `p delta<1/16`. The elementary identity
`ln2=2(1/3+1/(3*3^3)+1/(5*3^5)+...)` gives `ln2>2/3`. Hence

`ln(alpha) > 29/(48L)`.

Since `alpha-1>ln(alpha)>0`, (1) implies the exact necessary bound

`29 y_0 < 48 L K_0`,                                           (2)

so every physical root in this scope satisfies

`y_0 <= 31,285,589,992,934,194,300,574`.

## 3. Exact e=16 bounded lift family

RL212 supplies 108,950 exact e=16 root prefixes and 45,046 H21-compatible prefixes. For each H21 prefix it determines `eta mod 3^16`; the root p-gap unit condition supplies one lift `eta_* mod 3^17`.

Using the exact root recurrence, each canonical unit lift has

`4,167 <= eta_* <= 129,137,967`

and

`27,763,779,757,799 <= y_0^* <= 864,676,287,134,182,727`.

In particular

`0<y_0^*<3*2^58`.

All exact lifts of that same arithmetic prefix are

`eta = eta_* + k 3^17`,
`y_0 = y_0^* + k (3*2^58)`.

The displayed canonical range proves positivity is equivalent to `k>=0`. Combining with (2) gives:

- `k_max=36,180` for 34,652 H21 prefixes;
- `k_max=36,181` for 10,394 H21 prefixes.

Therefore the new global p-arc inequality reduces the infinite local lift family to exactly

**1,629,819,720 bounded arithmetic root candidates**.

This is a finite exact candidate family, not a physical population.

## 4. Combination with the inherited terminal valuation

The physical H21 terminal condition is `1<=nu<=21`. As in RL212, `nu>=22` is equivalent to one forbidden odd parameter residue modulo `2^22`, with

`3^(-34) mod 2^22 = 1,893,305`.

Across the bounded root family, exact modular solution of the lift progression finds only **789** candidates with `nu>=22`, occurring in 789 distinct prefixes. Thus

**1,629,818,931** bounded arithmetic candidates survive the terminal Hensel filter.

No prefix is deleted: every one of the 45,046 H21 prefixes retains at least **36,180** bounded candidates. The four inherited mod-18 classes all survive, with exact post-filter candidate counts:

- class 0: 529,801,527;
- class 8: 285,107,835;
- class 9: 529,801,531;
- class 17: 285,108,038.

All 469 RL212 reachable eta residues modulo2187 remain represented.

The canonical exact witness digest is
`cd0f9235a557a8f70c8fed89a802da95222fcc5bc80b7644e6dba57f71346136`.

## 5. Scope and consequence

RL214 proves a genuine full-word/H21 coordinate bridge and a genuinely global positive-p-arc inequality. The inequality is informative: it converts each infinite e=16 lift progression into a finite interval and the whole current e=16 arithmetic family into an exact finite set.

It does **not** supply the missing discrete quotient residue. Bare ownership determines `Q mod D=0`, whereas `y_0 mod D` requires the extra `Q mod D^2` digit or an equivalent independently anchored full-word observable. The bounded family plus the existing terminal valuation deletes no H21 prefix, state, mod18 class, or terminal rank.

The e=16 terminal rank remains `34,124,151,203` and the global necessary frontier remains **13,415,865,871**:
- above-p source: **7,091,831,284**;
- below-p source: **6,324,034,587**.

No physical H21 incidence/charge, branch contradiction, Gate closure or global nontrivial-cycle exclusion is proved.
