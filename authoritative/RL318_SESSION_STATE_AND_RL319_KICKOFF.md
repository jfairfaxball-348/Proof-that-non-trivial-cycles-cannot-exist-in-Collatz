# RL318 session state and RL319 kickoff

Date: 2026-09-14
RL318 status: CLOSED AND FROZEN
Successor: RL319

## What materially changed

RL318 admitted and froze Barina's peer-reviewed `2^71` convergence verification as an external finite computational certificate dependency, with exact provenance and local consumer replay.

Conditional on that dependency, the reduced frontier moves from the internal-only

`ell>=190,537`

to

`ell>=49,547,666,544`.

The first above-resonance survivor beyond this wall is

`(a,ell)=(217,976,794,617,137,528,045,312)`.

The generic RL310 packing route does not exclude it; the exact consumer check has more than a `21.6x` margin in the non-contradictory direction.

## What did not close

Neither RL317 `g=2` branch was eliminated.

A promising attempt to combine RL135's `m<2^75` least-root state ceiling with the RL317 arbitrary balanced cut was rejected: no theorem identifies those two cuts.  Therefore no unconditional `G<2^35` or 35-step mismatch theorem is carried forward.

The nonzero cross-content pair necessarily reverses relative order across a row, but order crossing alone is not a contradiction.

## RL319 priority

Work at the first external survivor in **cut-safe coordinates**.

Primary question:

> Can the true least-root `g=2` nonnegative-defect theorem from RL135 be transported to a balanced length-`a` cut by an explicit theorem, or can its full-word ordinary `+1` consequences be consumed directly by the RL316--RL317 full-`D` factorization without transporting the cut?

Read `RL319_CUT_SAFE_FIRST_EXTERNAL_SURVIVOR_G2_TARGET.md` for the full attack order and red teams.

## Scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
`g=1` remains separate.

`PARENT_DIFFICULTY_DELTA = EASIER`
