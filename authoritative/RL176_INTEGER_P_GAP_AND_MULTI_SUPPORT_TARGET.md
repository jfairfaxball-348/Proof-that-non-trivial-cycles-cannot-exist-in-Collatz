# RL176 — integer p-gap and multi-support target

Start from RL175.

In the physical coprime `g=1` first-survivor branch, retain `F2=3(lambda-1)(y_p-y_0)`.

For `h_p=0`, RL175 proves `F2<1/2-Delta/16` and `y_p` is the unique second-smallest physical state. Put `g_p=y_p-y_0`.

Because physical odd states are distinct, `g_p` is a positive even integer, at least `2`.

Primary target: derive an independent arithmetic restriction on `g_p` from the p-arc, chronological affine numerator, sparse/multi-support ownership, or a genuine congruence/divisibility condition. Combine it with the half-barrier only if the new relation is not algebraically equivalent to the existing same-root ownership condition.

Secondary target: for `h_p>=1`, consume the conditional lower flow `F2>6,365,000,000(2^H-1)` under inherited `m>=2^71` using a genuinely simultaneous physical or arithmetic constraint.

Do not reopen RL173's demoted `3^(-G)` physical interpretation, fixed-radius coordinate stacking, unordered valuation statistics, same-root resultant/SNF stacking without independent size or arithmetic information, or single-phase rank/chain capacity routes already frozen as barriers.

No global closure may be claimed unless all inherited obligations are discharged.
