# RL240 handover transport

Incoming completed generation: **RL239 — decision session / verified route selection**.

RL239 independently audited Radius 4 (`R4_AUDIT_PASS`), assessed Radius 5 as
a moderate new project without claiming a theorem, and selected exactly one
successor route:

`ROUTE_DECISION = R4_GLOBAL_BRIDGE`.

RL240 is **prepared but NOT STARTED**.

RL240 must attack only the same-root / return-structure global bridge to the
already-proved Radius-4 local theorem. It must not spend the session proving
Radius 5.

The weakest legitimate success is an RL-specific encounter theorem forcing an
eligible exact-distance-4 pair in every retained primitive full-`D` Gate-B
configuration. Bare cyclic geometry is insufficient; the inherited `(65,41)`
countermodel blocks the naive absolute-resultant route, so same-root phase
information or an equivalent physical return identity is required.

Gate A: open. Gate B: open. Global exclusion: open.
Knowledge catalogues: stale/deferred.
