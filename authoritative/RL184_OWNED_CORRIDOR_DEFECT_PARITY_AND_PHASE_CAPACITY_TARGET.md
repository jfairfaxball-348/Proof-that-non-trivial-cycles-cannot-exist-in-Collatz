# RL184 target — owned-corridor defect parity and phase capacity

Continue from the surviving high branch `(v,H,J,d)=(37,0,23,-1)` and RL183's physical owned-successor corridors.

## Primary target

Convert the new **25-edge owned physical corridors** into a global defect/map/width obstruction.

The preferred route is to couple at least two of:

- RL183's floor of `10,075,174,553` 25-edge ordinary corridors beginning at `h<=1`;
- exact ternary ownership depth at most 40 along every such corridor;
- the physical defect sensor
  `2|C_i <=> G_i=0` and `3|C_(i+1) <=> G_i even`;
- the common-mechanical affine numerator successor law
  `2^d C' = 3C + 2^(M-b)-2^(M-a)`;
- the 357-map three-transition necessary vocabulary and repeated-map floor `28,221,778`;
- the mechanical phase split around `4m/3`, especially the forced shallow late-mechanical populations;
- RL180/RL181 corrected-flow drift, p-gap corridor, affine-tail lower bound, and shallow width occupancy.

Seek a theorem that limits how often a defect parity/sign pattern or short affine map can recur in the physical phase compartments, or shows that the required recurrence forces too much corrected-flow drift / normalized width.

A successful result would cap the surviving shallow count/width below its certified quota or otherwise exclude `(37,0,23,-1)`.

## Preferred consumers

1. A signed/valued defect-run theorem inside the 25-edge corridors using the binary/ternary sensor and the corrected-flow increment `q_i(2^G_i-1)`.
2. A multiplicity bound for a repeated three-step affine numerator map once its phase location and ternary ownership are fixed.
3. A compartment-incidence theorem forcing too many owned shallow corridors to cross the narrow mechanical transition around `4m/3`.
4. A width or affine-tail inequality conditioned on defect parity/sign or mechanical class.

## Do not spend the session on

- simply extending the unconstrained local height/mechanical template enumeration to 25 steps;
- counting arbitrary `(k,G)` suffixes or ternary residues as physical;
- a broad local BFS without a global corridor/incidence consumer;
- generic dyadic/ternary lattice capacity;
- rank-only, monotone-chain, universal permutation, or inverse-rank routes blocked by RL168-RL171;
- blind higher odd-modulus lifting;
- RL173's demoted `3^-G` physical interpretation;
- treating necessary templates or automaton states as physically realized;
- recursive historical verification absent a live failure, unresolved dependency, or contradiction.

## Scope discipline

All RL180-RL183 refined floors, m bounds, suffix/corridor ownership, map multiplicities, and phase-location statements remain internal to the surviving `(37,0,23,-1)` high branch. Excluding that branch would require proof-state reconciliation before selecting the next branch and would not by itself prove global Collatz closure.
