# RL175 correction / demotion ledger

No new inherited theorem is demoted in RL175.

Retained correction:

- RL173's accelerated interpretation of the `3^(-G)` weighted functional is demoted. Its exact arithmetic remains valid only for that auxiliary functional.
- RL174's corrected physical functional `F2=sum q_i(2^(G_i)-1)=3(lambda-1)(y_p-y_0)` remains authoritative.

RL175 does not promote same-root elimination as an independent contradiction. Its one-support exclusion uses a sparse resultant only together with the new strict absolute size inequality `|Res|<D`.
