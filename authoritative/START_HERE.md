# Authoritative research entry point

Date: 2026-09-14
Incoming RL: RL318
Session type: NON-HOMOGENEOUS CROSS-CONTENT / ORDERED-ROW ATTACK

## Read first

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `RL317_PROOF_LEDGER.md`
5. `RL317_RED_TEAM_REPORT.md`
6. `RL318_NONHOMOGENEOUS_CROSS_CONTENT_AND_ORDERED_ROW_TARGET.md`
7. `sessions/RL317/RL317_CLOSEOUT.md` only when full frozen provenance is needed

The canonical RL317→RL318 handover is carried losslessly in
`RL317_to_RL318_Handover_BUNDLE_TRANSPORT/`; `verify-incoming` reconstructs
the exact ZIP named by `RL317_to_RL318_Handover.zip.sha256`, validates the
internal manifest, and reruns all four portable RL317 verifiers from the clean
reconstruction.

The repository is authoritative. Conversation memory is not.

## Why RL318 has priority

RL317 exactly isolates the missing cofactor residue and then proves that its
standalone integer carry orbit is the inherited RL79 `T_h` object. The only
live analytic information is now relational and non-homogeneous: a
content-`h` physical copy paired with a coprime-content shadow, or the separate
`epsilon=0` ordered-row branch.

The fallback first fibre is now exact and finite in multiplicity, with the
strongest cap `g<=9,355,556`.

## Frozen alternatives

Do not resume standalone generalized-increment invariants, unsigned row totals,
`H`-only geometry, support grammar, fixed-96 P/Q, Radius 6+, H21, or the
unpromoted RL315 descent run.

Keep the `g=1` ordinary-`+1`/Bezout branch separate.

## Binding scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
Lean formalisation is a separate project.
Knowledge catalogues are stale/deferred for this connector closeout and must
not be used as authority until refreshed/validated.
