# Authoritative research entry point

Date: 2026-09-14
Incoming RL: RL321
Session type: SIGNED-RESIDUAL OWNERSHIP + ZERO-CARRY CONSUMER

## Read first

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `RL320_CLOSEOUT.md`
5. `RL320_PROOF_LEDGER.md`
6. `RL320_RED_TEAM_REPORT.md`
7. `RL320_SESSION_STATE_AND_RL321_KICKOFF.md`
8. `RL321_SIGNED_RESIDUAL_OWNERSHIP_AND_ZERO_CARRY_TARGET.md`

The repository is authoritative. Conversation memory is not.

## Why RL321 has priority

RL320 replaces the unbounded integer quotient in the late-row contact by

`K=3^sc+theta`, `|c|<=2^35`, `0<|theta|<3^s`.

The resulting positive balanced affine pair has exact `D0/H` factor algebra
but cannot be relabelled as an ordinary balanced return. The missing input is
a support-localization or physical-run consumer for `theta`.

The secondary root-aligned branch has a phase-33 earliest-interface cap when
`kappa>0`; `kappa=0` and the connection to the actual reverse crossing remain
open.

## Frozen barriers

Do not relabel the surrogate as ordinary-owned, split RL147's combined
mixed-height ownership into separately owned layers, invoke height-one
contact theorems without their hypotheses, or restart generic fixed-depth
prefix scans.

## Scope

- Internal-only frontier: `ell>=190537`.
- External-certificate-conditional frontier: `ell>=49,547,666,544`.
- Gate A: OPEN.
- Gate B: OPEN.
- Global positive non-trivial-cycle exclusion: OPEN.
- `g=1` remains separate.

`PARENT_DIFFICULTY_DELTA = EASIER`
