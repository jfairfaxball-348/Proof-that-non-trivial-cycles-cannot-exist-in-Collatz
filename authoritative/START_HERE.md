# START HERE — authoritative RL198 handover

Current handover: **RL198 — H21 Global Height Selector and Minimal Prehistory**.  
Incoming job: **RL199**.

RL198 globally intersects RL197's six-state local H21 interface with the inherited exact
zero-prefix valuation/prehistory law.

For every surviving H21 `{33,34,35}` co-owner,
the `tau=34` and `tau=33` common p-pair heights are forced to be one:
`e34=e33=1`.  Therefore the RL197 six states collapse exactly to `011` and `111`.

Consequently the RL197 canonical H21-owned p-shift zero edge (`e34=0`) is excluded.
More strongly, the whole co-owned `tau=35` zero-defect prefix contains no p-shift-compatible
height-zero chronological edge.

The `tau=34` and `tau=33` zero-prefix valuation budgets are saturated:
all 34 and all 33 corresponding transition costs are one.  The only remaining shallow-scale
ambiguity is the leading `tau=35 -> tau=34` cost: it is one in state `011` and two in state `111`.

At the source immediately before the common H21 terminal, both p-pair heights are exactly 20,
the mechanical bit is 2, and the common owned numerator is exactly
`14*3^34 = 233480543795331966`; the terminal numerator is `7*3^35`.
The remaining terminal split/sign bit is not decided.

No H21 budget release, branch/Gate closure, non-trivial-cycle exclusion or global Collatz
closure is claimed.

Read the RL198 report, certified-facts ledger, correction/demotion ledger, red-team report and
`proofs/RL198_H21_GLOBAL_HEIGHT_SELECTOR.md`.
Run `sh verification/run_fast_rl198_verifiers.sh`.

Next target: `RL199_H21_MINIMAL_PREHISTORY_AND_TERMINAL_SIGN_TARGET.md`.
