# Authoritative start — RL287

Date prepared: 2026-09-09

Incoming completed generation:

**RL286 — excursion carry/coupon-defect bridge and state-reserve barrier for Gate A.**

RL286 primary classification:

`EXCURSION_CARRY_COUPON_DEFECT_BRIDGE_AND_STATE_RESERVE_BARRIER_PROVED`

Promoted analytic results:

- for every genuine positive first-return excursion, the shifted-shadow/Ferrers carry integer is exactly the RL280/RL281 coupon defect after normalization:
  `C_E/3^r = D_E/Q_in` and `C_E/2^L = D_E/Q_out`;
- excursion height is exactly the number of displacement/coupon cells;
- every component carry cell has 2-adic exponent in the height-supported range `0,...,h-1`;
- the one-zero family has exact carry
  `C_E = 3^h-2^h`,
  so the return-normalized carry is
  `C_E/2^h=(3/2)^h-1`;
- therefore no state-free theorem of the form “component carry capacity increases by at most excursion height” can hold uniformly;
- plain blockwise checkpoint valuation induction remains impossible and any successful recursive budget must carry an additional state-dependent reserve.

RL286 did **not** close Gate A and did not promote the conjectural global inequality

`nu_2(K-1)<=H+d-1`.

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

Read first:

`sessions/RL286/RL287_RETURN_NORMALIZED_DEFECT_RESERVE_GATE_A_TARGET.md`

Then use the frozen RL286 report:

`sessions/RL286/RL286_EXCURSION_CARRY_COUPON_DEFECT_BRIDGE_2026-09-09.md`.

RL287 is **prepared but NOT STARTED**.

Priority: augment the return-normalized defect by a state-dependent reserve that is closed under arbitrary positive zero-height boundary retention. Use the exact one-zero rigidity `h=nu_2(J_in+4)`, the RL283 boundary hazard formulation, and the RL281 multi-zero `F`/mass pricing. Falsify candidates against the inherited `H<=22` exact state certificate, but do not promote finite survival as a theorem.

Do not restart raw `nu_2(J)` block induction, per-cell valuation-Lipschitz arguments, fixed shifted-valuation tables, or larger height-cap enumeration as the principal route.

Gate B remains separate/open/frozen. Fifth selector unscanned. Radius 6+ frozen. No global non-trivial-cycle exclusion is claimed.
