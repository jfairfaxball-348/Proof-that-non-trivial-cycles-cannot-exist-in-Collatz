# Authoritative research entry point

Date: 2026-09-14
Incoming RL: RL316
Session type: REDUCED-SHADOW INTERFACE FULL-D OWNERSHIP ATTACK

## Read first

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `sessions/RL315/RL315_CLOSEOUT.md`
5. `sessions/RL315/RL315_RESEARCH_LEDGER.md`
6. `authoritative/RL316_REDUCED_SHADOW_INTERFACE_FULL_OWNERSHIP_TARGET.md`

The repository is authoritative. Conversation memory is not.

## Why RL316 has priority

RL315 proved that every `g>1` candidate already has a genuine reduced balanced
window of counts `(a,ell)`, so the previous controlled-branch repeated-level
target is no longer the correct top-level objective for `g>1`.

RL315 further rewrote every `g>1` parity word canonically as a repeated reduced
shadow plus a nonnegative integer interface and derived an exact positive
full-cofactor ownership identity.

The highest-parent-leverage question is now whether genuine full `D0H`
ownership can consume every nonzero interface without reverting to
support-by-support grammar.

## Frozen alternatives

Do not automatically resume:

- support `3,4,5,...` terminal grammar;
- fixed-96 P/Q commutation;
- Radius 6+;
- H21;
- raw Gate-A Bellman/Y ancestry;
- bare finite-modulus escalation;
- recurrence-linear/RL20 coboundary forcing.

The `g=1` Bezout/ordinary-`+1` branch remains a separate fallback.

## Binding scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
Lean formalisation is a separate project.
Knowledge catalogues are stale/deferred.
