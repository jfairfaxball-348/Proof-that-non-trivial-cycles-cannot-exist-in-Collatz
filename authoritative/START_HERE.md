# Authoritative research entry point

Date: 2026-09-13
Incoming RL: RL314
Session type: FULL STRATEGIC AUDIT / STEP-BACK

## Read first

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `sessions/RL313/RL313_CLOSEOUT.md`
5. `authoritative/RL314_FULL_STRATEGIC_AUDIT_TARGET.md`

The repository is authoritative.  Conversation memory is not.

## Why RL314 is an audit

RL313 made real progress in the divisor-aligned balanced-return programme, but
its final two meaningful checkpoints were both non-improving:

1. terminal partition numerator span cannot by itself force fragmentation;
2. an explicit all-scale positive-rational family survives bounded discrepancy,
   short equal-level return, endpoint proximity, orbitwise zero sums, and full
   activity.

Under the binding RL313 rule, that triggers a strategic step-back rather than a
deeper local continuation.

## Frozen RL313 achievements available to the audit

- gcd/divisor descent has at most one strict step and then terminalizes;
- terminal row-boundary states satisfy integer packing constraints;
- in the `lambda<3` terminal branch:
  `ell0>=116`, `d>=184`, reduced `ell>=17`;
- exactly two active shift-orbits are impossible under full-D ownership;
- terminal row numerators must span at least `2^d+3^ell0`;
- arbitrary inactive partitions need an independent fragmentation theorem;
- fully-active ownership-blind geometry is insufficient: any successful
  consumer must use genuinely new integer/full-D information.

These results are frozen resources, not an instruction to continue that route.

## Binding scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
Lean formalisation is a separate project.

Do not automatically resume:

- RL313 terminal-row/full-activity work;
- fixed-96 P/Q commutation;
- H21;
- Radius 6+;
- raw Gate-A Bellman/Y ancestry;
- finite Farey enumeration;
- selector scans;
- quotient gcd/content or fixed-prime residue programmes;
- recurrence-linear / RL20 coboundary forcing;
- fixed local pattern grammars.

The audit may recommend one of these later only after an explicit whole-program
comparison shows a new splice or materially changed reason.

Knowledge catalogues are stale/deferred.
