# Authoritative start — RL313

The unique incoming research job is RL313.

RL313 continues the materially improved balanced-return route frozen in RL312. It must not reset automatically to an older local attack.

Its top-level objective remains:

> Prove that a positive non-trivial Collatz cycle cannot exist.

Its primary target is the **divisor-aligned balanced descent / fully-active flow consumer problem**.

RL312 closed three exact gaps in the RL311 balanced-return branch:

1. exact owned balanced Radius 2 is impossible;
2. a full-D owned balanced flow cannot be supported on a single shift-orbit;
3. every balanced shift obeys an all-scale gcd-orbit dichotomy.

For the RL311 balanced shift

`A=ga`, `L=g ell`, `s=ma`,

let

`c=gcd(g,m)`.

RL312 proves:

- either all `ac` shift-orbits are active and the **canonical zero-sum** flow satisfies
  `sum |G_i|>=2ac`;
- or, after rotation, the cycle decomposes into equal-count blocks `(ca,c ell)`, giving a genuine full-D-owned balanced segment of those counts.

If `c<m`, the latter is a strict balanced-return descent.

Important: canonical L1 mass is not automatically optimal cyclic transport radius, and a descended pair does not automatically inherit the original RL311 endpoint-ratio `<3` bound.

Read, in order:

1. `AGENTS.md`;
2. `docs/RL_RESEARCH_PROTOCOL.md`;
3. `docs/RL_STATE_MACHINE.md`;
4. `authoritative/RL313_DIVISOR_ALIGNED_BALANCED_DESCENT_TARGET.md`;
5. `sessions/RL312/RL312_CLOSEOUT.md`;
6. `sessions/RL312/RL312_SHIFT_ORBIT_DICHOTOMY_AND_DIVISOR_DESCENT.md`;
7. `sessions/RL312/RL312_SINGLE_SHIFT_ORBIT_EXCLUSION.md`;
8. `sessions/RL312/RL312_BALANCED_RETURN_RADIUS2_EXCLUSION_AND_NORMALIZATION_REPAIR.md`;
9. exact historical sources named there only as needed.

## Binding corrections carried into RL313

Do not use any of the following as promoted facts:

- canonical zero-sum flow mass equals optimal cyclic transport radius;
- descended balanced endpoints inherit the original RL311 `<3` physical ratio;
- global `D|Q` gives local-segment denominator ownership;
- the demoted canonical-block strip-width lower bound globally;
- `E_j<=floor(kappa_ext/A)`;
- whole-block RL20 coboundary divisibility as an independent obstruction.

## Methodology

Work backwards from global exclusion. At every meaningful checkpoint state

`PARENT_DIFFICULTY_DELTA = EASIER | LATERAL | HARDER`.

The live obstruction is now to consume the divisor/full-activity dichotomy, not to discover another discrepancy lower bound.

If two consecutive meaningful RL313 checkpoints fail to make the parent arrow easier, freeze this route and perform the previously requested strategic step-back/audit rather than extending a local grammar.

## Frozen routes

Do not automatically resume fixed-96 P/Q, H21, Radius 6+, raw Gate-A Bellman/Y ancestry, selector scans, finite Farey enumeration, quotient gcd/content, recurrence-linear/coboundary forcing, or fixed local pattern escalation.

Radius-3/4/5, RL19, RL20, RL274, full-D ownership and quotient-layer tools remain available only when their exact hypotheses match.

## Current proof status

RL312 is closed and frozen.
Gate A remains open.
Gate B remains open.
Global non-trivial-cycle exclusion remains open.
No Collatz conjecture claim is made.
Lean formalisation remains separate.

Knowledge catalogues are `stale/deferred`; use exact authoritative and frozen session files for navigation.
