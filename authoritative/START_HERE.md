# Authoritative start — RL292

The unique incoming research job is RL292.

Read, in order:

1. `AGENTS.md`;
2. `docs/RL_RESEARCH_PROTOCOL.md`;
3. `docs/RL_STATE_MACHINE.md`;
4. `authoritative/RL292_FIXED_SEED_BELLMAN_DANGER_SET_CONTRACTION_GATE_A_TARGET.md`;
5. `sessions/RL291/RL291_CLOSEOUT.md`;
6. `sessions/RL291/RL291_SYNTHESIS_AND_FAST_TRACK_REPORT.md`;
7. `sessions/RL290/RL290_CLOSEOUT.md`;
8. `sessions/RL290/RL290_SCRATCH_FREEZE.md`.

## Incoming classification

RL291 closed as

`PROGRAMME_CONSOLIDATED_GLOBAL_REACHABILITY_OBSTRUCTION_AND_FIXED_SEED_BELLMAN_COMBINATION_FAST_TRACK_RECOMMENDED`.

Gate A remains open with exact residual

`k>=25`, `k` odd, `H_can<k`.

## RL292 mission

Attack the fixed-seed Bellman danger-set contraction exposed by the RL291 audit.

Primary sufficient target:

`Bcal(1,-13)<=0`.

Use the consolidated historical-cost architecture (zero-rank lag, coupon defect, boundary quotient, height-bounded skeleton) together with fixed-seed ballot ancestry and RL290's physical-state Bellman/checkpoint kernel.

Begin with the exact height-one kernel and seek a gauge-invariant Bellman supersolution or equivalent backward danger-set contraction.

Do not restart a retired local-charge, bare-modulus, chosen-history, fixed-zero enumeration, or raw height-cap programme.

Finite `H<=22` evidence is falsification support only.

## Scope

Gate B remains separate/open/frozen.

Fifth selector remains unscanned.

Radius 6+ remains frozen.

No global non-trivial-cycle exclusion is claimed.

The repository, not conversation history, is authoritative.
