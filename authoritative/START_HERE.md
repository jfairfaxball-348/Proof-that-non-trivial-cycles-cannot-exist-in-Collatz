# Authoritative state — RL246

Current authoritative generation: **RL246 (PREPARED, NOT STARTED)**.

Incoming completed generation: **RL245**, classification `R4_BRIDGE_REDUCED`.

Frozen RL245 result:

- every simultaneous Gate-A/Gate-B survivor still has `beta(P)>=6`;
- exact q-window derivative and Branch A/B/C common physical-valley geometry are retained;
- the RL245 checkpoint strengthening `h in {0,1}` is DEMOTED; use exact global `h>=0` only, with internal `h=d`;
- canonical full-phase legality yields a deterministic u-driven `(v,d,J)` automaton, including exact descent rule `10 => J'=J/2`;
- local u-pattern geometry alone cannot force the needed rank event; the absolute full-phase-selected state is the live interface;
- the scratch RL245 determinant-sector no-Radius-4 conclusion is NOT PROMOTED.

Use:
1. `RL246_HANDOVER_BUNDLE_TRANSPORT/RL246_ABSOLUTE_STATE_FULL_PHASE_VALLEY_COUPLING_TARGET.md`
2. `RL245_FRESH_UNPACK_VERIFICATION_2026-09-04.txt`
3. `RL245_ROUTE_REDUCTION_TRANSPORT/TRANSPORT_MANIFEST.json`

RL245 canonical ZIP SHA256:
`abd41536bf69ce3a12fa5a8ae5358bc6df3527ca8d1603b245c70e0f986de48e`.

Gate A remains open uniformly. Gate B remains open. Radius 5 remains inactive.
