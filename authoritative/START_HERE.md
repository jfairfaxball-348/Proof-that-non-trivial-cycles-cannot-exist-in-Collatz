# Authoritative research entry point

Date: 2026-09-14
Incoming RL: RL319
Session type: CUT-SAFE FIRST-EXTERNAL-SURVIVOR `g=2` ATTACK

## Read first

1. `AGENTS.md`
2. `docs/RL_RESEARCH_PROTOCOL.md`
3. `docs/RL_STATE_MACHINE.md`
4. `RL318_PROOF_LEDGER.md`
5. `RL318_RED_TEAM_REPORT.md`
6. `RL318_EXTERNAL_2P71_DEPENDENCY.md`
7. `RL318_SESSION_STATE_AND_RL319_KICKOFF.md`
8. `RL319_CUT_SAFE_FIRST_EXTERNAL_SURVIVOR_G2_TARGET.md`
9. `sessions/RL318/RL318_CLOSEOUT.md` when full frozen provenance is needed

The canonical RL318→RL319 handover is listed in
`RL318_to_RL319_HANDOVER_MANIFEST.md`.

The repository is authoritative. Conversation memory is not.

## Why RL319 has priority

RL318 conditionally contracts the reduced frontier enormously by freezing the peer-reviewed Barina `2^71` convergence verification as an external finite computational certificate dependency.

Conditional external frontier:

`ell >= 49,547,666,544`.

Internal-only frontier:

`ell >= 190,537`.

The first above-resonance survivor after the external wall is

`(a,ell)=(217,976,794,617,137,528,045,312)`.

At this pair, historical RL135 proves global nonnegative mechanical defect for `g<=6`, while RL316--RL317 give the exact balanced-row dual-shadow/full-ownership structure.  The missing theorem is a cut-safe bridge between those coordinate systems, or an equivalent no-transport full-word ownership consumer.

## Binding correction from RL318

Do **not** assume that RL315's universal balanced cut is the global least-root RL135 cut.

Accordingly, no unconditional `G<2^35`, no 35-step first-mismatch certificate, and no least-state prefix constraint is currently available for the arbitrary RL317 balanced rows.

## Frozen alternatives / barriers

Do not restart:

- standalone homogeneous `T_h` invariants (RL79 barrier);
- bare continued-fraction or residue-prefix work at the first survivor;
- generic RL310 packing without a new ordinary-owned input;
- support-by-support grammar;
- the RL316 `a<=22` scan;
- finite-prefix physical-gap congruences that collapse to RL263--RL264 affine-ray identities.

Historical RL139--RL142 contact-polynomial obstructions become relevant only after their bounded-interface hypotheses are actually proved.

Keep `g=1` separate.

## Binding scope

Gate A: OPEN.
Gate B: OPEN.
Global positive non-trivial-cycle exclusion: OPEN.
No Collatz conjecture claim is made.
Lean formalisation is a separate project.

`PARENT_DIFFICULTY_DELTA = EASIER` conditionally because of the external frontier contraction; the local first-survivor `g=2` bridge remains open.
