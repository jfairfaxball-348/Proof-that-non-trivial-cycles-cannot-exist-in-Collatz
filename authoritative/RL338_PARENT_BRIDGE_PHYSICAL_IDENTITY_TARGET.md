# RL338 target — all-length physical shared-state ownership

Status: UNIQUE LIVE TARGET FOR RL338.

Work only in the inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent branch at `(a,ell)=(217976794617,137528045312)`. Retain the external qualification on the least-state floor, RL336's `z<=35`, `n<=32546278588`, q=32 theorem, and all frozen physical exclusions.

RL337 supplies the exact affine profile-collapse identity and the all-length right-suffix profile bound. The fixed-terminal sign is:

`profiled source < zero-profile source`

for every nonzero admissible profile at the same terminal state. Do not reverse this sign.

Primary objective: replace the anonymous arbitrary-positive fallback layer by an exact or conservative physical-state quotient that is still support-uniform in positive-run length. A successful theorem may take any of these forms:

- exact shared-state acyclicity for all admissible positive returns;
- a finite right-suffix ownership automaton whose extension to arbitrary run length is proved analytically;
- a residue/state invariant forcing every genuine fallback return into owned descent;
- an equivalent exact obstruction that closes the ordered parent branch.

The p=5 diagnostic `(24,27)/(24,28)/(24,29)` sharing one source/run-exit state should be treated as evidence that pair-level labels overcount independent physical transitions, not as a theorem about the full layer.

Avoid a p=7,p=8,... catalogue as the main route. Finite reconstruction is allowed only when it certifies a fixed interface used by an all-length theorem.

Success criterion: R1 parent closure for this ordered branch, or a theorem-level reduction strictly smaller than the current anonymous fallback problem with exact dependencies and no loss of physical ownership.
