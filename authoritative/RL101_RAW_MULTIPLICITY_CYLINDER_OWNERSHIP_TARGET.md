# RL101 TARGET — raw first-surplus multiplicity, cylinder height, and full-ownership content

Date: 2026-08-25

## Authority

This is the authoritative RL101 mathematical target selected by the RL100 closeout.

RL101 is a **structural ownership/multiplicity session**, not a routine continuation of the four-tier scan.

## 1. Central obstruction

RL83 reduces the raw first-surplus count pair as

`(j,o)=(g p,g q)`.

At the smallest admissible reduced Farey slope

`(p*,q*)=(114,208,327,604,72,057,431,991)`,

the first-crossing inequality alone allows

`1<=g<=125,777,718,029`.

RL84–RL100's physical/cascade machinery is load-bearing at raw `g=1`.

The missing theorem is therefore not merely a theorem about larger reduced Farey slopes.  It must control **raw multiplicity at the same slope**.

## 2. RL101 target theorem

Prove one of the following sufficient forms.

### Form A — raw multiplicity collapse

Every genuine full-phase Gate-A violation whose reduced first-surplus slope is `p*/q*` has

`g=1`.

### Form B — multiplicity exclusion

Every raw `g>=2` branch is impossible under full ordinary-`+1` ownership.

### Form C — finite multiplicity complement

There exists an explicit finite set `G` such that every genuine survivor has `g in {1} union G`, and every `g in G` has exact branch parameters allowing certified elimination.

### Form D — multiplicity domination

Derive a multiplicity-aware physical/cylinder/reset inequality showing that the already certified raw-`g=1` contradiction engine is at least as restrictive for every `g>=2` after the correct branch parameters are substituted.

Any one of A–D is a genuine success.

## 3. Inherited exact inputs

Use without recursive historical re-audit after the outgoing integrity gate:

1. RL83:
   - `(j,o)=(g p,g q)`;
   - `j=ceil(beta o)`;
   - reduced product tube;
   - local/global equal-vs-distinct reduced-slope dichotomy;
2. RL82:
   - exact `3^o` maximum cylinder;
   - exact fixed-count numerator envelope;
   - surplus ceiling;
3. RL84:
   - normalized cylinder-height consumer;
   - maximum/minimum ownership geometry;
4. RL85:
   - raw `g=1` first-Farey physical-scale theorem;
5. RL79:
   - generalized-increment theorem and mandatory `s=1` discriminator;
6. RL20:
   - canonical gcd-block geometry;
   - `D∤Q` fake / coboundary barriers;
7. RL81:
   - physical common-mode warning;
8. RL87–RL100 branch machinery:
   - consume only after a correct multiplicity bridge is obtained.

## 4. First task — generalize the full cylinder at raw multiplicity

For raw counts

`j=g p*`, `o=g q*`,

derive the exact word-cylinder statement modulo

`2*3^(g q*)`

and the normalized least-even representative height

`theta_(w,g)=m_(w,g)/(2*3^(g q*)).`

Combine it with RL100's exact safe maximum envelope

`U_g=2^(g(p*-q*))(3^(gq*)-2^(gq*))/(2^(gp*)-3^(gq*))`.

The absolute envelope grows with `g`, but the **normalized cylinder height** may behave in the opposite direction because the cylinder modulus grows like `3^(gq*)`.

Determine the exact monotonicity of

`U_g/(2*3^(gq*))`

and whether full ownership supplies a nontrivial lower bound on `theta_(w,g)` for genuine words.

Do not infer such a lower bound merely from cylinder uniqueness.

## 5. Second task — use ordinary `+1` content, not scale-free geometry

A successful argument must consume something that fails for RL79's generalized increment `s`.

Prioritize:

- exact word numerator/content modulo the full cycle denominator `D`;
- divisibility `D|Q(w_full)` and its interaction with the first-surplus cylinder;
- additive `+1` constants before normalization;
- exact integer primitiveness/rotation ownership;
- a contradiction between the tiny first-surplus cylinder representative and a full-cycle owned lower-content condition.

Reject any proposed theorem that survives unchanged after simultaneous scaling `(states,s)`.

## 6. Third task — local/global equal-slope branch

If the reduced local first-surplus slope equals the global reduced slope, write the global counts and local counts as multiples of the same primitive pair.

Exploit RL83's exact difference-of-powers gcd structure and the fact that the local multiplicity `g` is then a proper partial multiplicity of the full cycle when the first surplus is proper.

Look for:

- a nontrivial divisor of `2^(gp*)-3^(gq*)` forced into the full denominator;
- a word-numerator content contradiction;
- an owned rotation/primitivity obstruction;
- a factor that is absent in the generalized-increment fake.

Do not revive bare proper-factor descent if it telescopes to the RL20 coboundary.

## 7. Fourth task — local/global distinct-slope branch

RL100 retains the denominator facts

- local `q>=q*`;
- if the local slope is distinct from the first mediant, `q>=78,644,250,661`;
- RL83 distinct local/global slopes obey a denominator-product lower bound.

Use these only if they couple to an actual physical or ownership upper resource.  Another denominator floor by itself is not success.

## 8. Exact success condition

RL101 succeeds only with universal quantification over the unresolved raw multiplicities or with a finite explicit multiplicity complement.

Examples:

- `g=1` is forced;
- every `g>=2` violates an owned cylinder-content inequality;
- all `g>G0` are analytically impossible and the finitely many `2<=g<=G0` branches are explicitly certified;
- the branch contradiction functional is rigorously worst at `g=1` after all multiplicity-dependent parameters are included.

## 9. Failure / pivot condition

Stop this route if serious derivation shows that:

1. the full cylinder admits arbitrarily tiny representatives compatible with all current ownership inputs for infinitely many `g`;
2. every candidate content theorem collapses under RL79 generalized-increment scaling;
3. full `D|Q` interaction reduces only to the known RL20 coboundary/proper-factor identity;
4. the only progress is another huge but non-eliminating bound on `g`.

If so, record the exact barrier.  Do **not** automatically spend the rest of the session on bulk cascade scans.  Re-rank direct denominator-content / owned Gate-B architectures first.

## 10. Deferred exact cascade

Frozen raw-`g=1` first-Farey/full-phase state:

- first live odd `k=2,921,813,805`, margin `-3,297`;
- wide `(D_w,R_w)=(5,000,030,7,000,000)`;
- middle `(D_m,R_m)=(7,500,031,4,500,000)`;
- deep `(D_d,R_d)=(10,000,032,3,250,000)`;
- ultra `(D_u,R_u)=(15,000,035,2,005,000)`;
- support `lambda=234375/3281264468752`, `mu=1/7000001`;
- feasible supports `A-C`, `C-D`, `D-E`;
- `L_common=15,000,053`;
- branch upper endpoint `42,150,931,559`, odd.

The middle floor repair `D_m=7,500,031` is mandatory.

Use `RL100_COUPLED_CASCADE_RESUME_AFTER_RL101.md` if a later session deliberately resumes the numerical route.

## 11. Mandatory red-team checklist

Every promoted RL101 theorem must pass:

1. ordinary `s=1` / RL79 discriminator;
2. RL20 `D∤Q` fake test;
3. RL81 common-mode test;
4. primitivity/physical-state ownership;
5. reduced-slope versus raw-multiplicity distinction;
6. external-floor labelling;
7. no implicit promotion of raw-`g=1` numerical exclusions to global Gate A.
