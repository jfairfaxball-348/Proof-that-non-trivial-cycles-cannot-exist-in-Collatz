# RL125 target — `L=11` depth-sensitive CRT capacity

RL124 has advanced the primitive ordinary low-odd-count frontier to `L>=11`.

For `L=11`, `D>0` begins at `Z=7`.  The inherited zero-fibre floor `W>=2Z-1` and numerator-diameter ceiling analytically exclude every `Z>=44`.  The live strip is therefore

`L=11`, `7<=Z<=43`.

## Priority A — profile capacity first

Enumerate ordered positive odd/zero run compositions by common run count.  For each primitive profile compute the exact depth-sensitive capacity floor

`max(6(t-1), (O_j-1)3^j, (Z_k-1)2^k+1, (C_(j,k)-1)3^j2^k)`

and compare it with `floor(B/D)`.  Preserve exact totals, periodic-profile totals, exclusions, and residual counts by `Z`.

## Priority B — improve compression before ownership

Use the RL123 sliding-window formula at shifts `s=2,3,...` only with its proved scope.  Seek an additional profile restriction or a stronger multi-depth CRT packing inequality.  Do not assume a run-count restriction beyond an established window argument.

## Priority C — exact residual ownership only after compression

If a residual remains, test every cyclic root for the necessary `D|Q` condition using an auditable run-profile enumerator or another gap-free exact method.  Record periodic profiles separately; do not call a divisibility hit a physical cycle.

## Mandatory red teams

Retain RL20 ownership, RL79 generalized-increment scaling, RL81 physical state ownership, primitivity/periodicity separation, Raw/Farey scope, and exact finite-certificate scope.  No Gate closure can be inferred from a low-`L` certificate.
