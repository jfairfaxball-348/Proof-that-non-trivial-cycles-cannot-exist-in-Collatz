# RL228 target — uniform candidate-coupled height exhaustion engine and e=4 stress test

## Mission

Turn the RL224–RL226 candidate-coupled height method into a **reusable exhaustion theorem/engine**, using the complete live `e=4` family as the stress test.

Do **not** make “extend to transition 44/45” the session objective.

## Binding incoming state

- necessary terminal-rank frontier: **13,415,865,870**;
- e=4 terminal rank **31,435,476,727** remains live;
- e=4 combined survivors through transition 43: **3,856,660,232**;
- lossless explicit state from phase 36 onward: `(phase,residue,precision,intercept)`;
- slope and height are derived exactly;
- naive `(phase,precision)` exact merging is retired because residue/intercept changes finite-window cardinality;
- physical H21 incidence/charge, sole-high-branch contradiction, Gate A, Gate B, and global exclusion remain open.

## Primary target

Derive an endpoint-aware exact representation or analytic criterion that controls the candidate-coupled height process without enumerating individual billions of candidates and without discarding the load-bearing residue translation.

Permitted directions include:
- exact BDD/transducer compression;
- endpoint-aware residue/state quotients;
- interval/cardinality summaries with a proof of losslessness;
- a direct analytic exhaustion inequality that bypasses explicit state growth.

## Success ladder

### A — strongest success
Empty the entire e=4 family **and** state/prove the exhaustion mechanism in parameters reusable for other exact root progressions.

### B — reusable theorem success
Prove a parametric exact compression/exhaustion lemma and obtain a stronger gap-free e=4 contraction, even if the family is not yet empty.

### C — decisive barrier success
Prove an exact state-growth/endpoint-information barrier showing that this candidate-coupled engine cannot be made uniform enough to serve as the primary rank-family route. If C occurs, hand the next session to physical H21 incidence/exhaustive charge or a broader uniform root-cone consumer.

A raw finite extension by one or two transitions **without** A, B, or C is not sufficient closeout value.

## Required red teams

1. Preserve exact finite-window counts: compare any merged states with residue-shifted endpoint cardinalities.
2. Reproduce all RL226 counts through transition 43 before claiming an extension.
3. Do not transfer e=4 formulas to e=28,33,40,45.
4. Do not call arithmetic candidate elimination a physical H21 charge.
5. If a compressor uses only `(phase,precision)` or equivalent derived slope/height data, reject it immediately unless it also encodes the lost residue/endpoint information.

## Continue / pivot rule

**CONTINUE** when a new exact invariant, reusable compressor, or parametric exhaustion condition appears.

**PIVOT** when the only available work is repeated transition-depth extension with rapidly increasing state count and no uniform theorem.

On pivot, the successor target must be **physical H21 incidence/exhaustive charge**, not another cosmetic e=4 depth increment.

## Scope

Even complete e=4 exhaustion deletes one terminal rank. It does not close Gate A, Gate B, or RL. Preserve the RL227 closure roadmap and fallback tree.
