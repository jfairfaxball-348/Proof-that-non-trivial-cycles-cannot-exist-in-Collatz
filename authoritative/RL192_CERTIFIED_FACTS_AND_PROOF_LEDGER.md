# RL192 Certified Facts and Proof Ledger

Date: 2026-08-30

This ledger is authoritative for the RL192 closeout.  Inherited facts remain governed by their originating certified sessions.

## Newly proved analytic mathematics

1. **Exact mechanical exponent-sum identity.**  For a mechanical word of length `n` beginning at integer rank `r`,
   `sum_{j<n} c_j=floor((r+nA)/L)`.

2. **Full-period necessary-overlap split.**  At the natural terminal separation `s=L`, `n=L-37`, the necessary overlap is exactly
   `E=[72797034370,103818202602]` and splits at
   `Q=88514772733` into exponent sums `A-59` and `A-58`.

3. **Full-period phase lock.**  The two zero-error affine centres on those atoms are exactly
   `2^38 exp(-Delta)` and `2^37 exp(-Delta)`, where
   `Delta=A ln2-L ln3`.

## Exact rational certificate

The RL192 verifier certifies with exact Fraction arithmetic and rigorous logarithm-series tails:

- `M=59L-37A=49013272579`;
- `Q=37B mod L=88514772733` and `M+Q=L`;
- constant exponent sums on both complete integer rank atoms;
- `0<Delta<2^-40`;
- target-centre misses `<1/4` for `2^38` and `<1/8` for `2^37`;
- universal relaxed radius at least `1/2`;
- a single-final-error relaxed witness of magnitude `<1` for both target atoms.

## New method barrier

The universal affine law together with only `|epsilon_i|<2`, exact mechanical rotation, and necessary-rank overlap cannot exclude the isolated/full-period separation.  Both permitted targets are admitted by explicit relaxed error choices at `s=L`.

This is a **method barrier / dead route for the bound-only unbounded resonance strategy**.  It is not physical realization: actual `epsilon_i` values are correlated height differences with one forced carry term.

## Inherited facts used without re-audit

- RL191 universal carry-completed gap transport and exact mechanical rank rotation;
- RL191 terminal start value, allowed target gaps, and extremal necessary-rank interval;
- RL191 spacing through 1000, population/charging bounds, and flow conversion;
- the binding H21 `{33,34,35}` family and core `D=[23369453298,41775866136]`.

## Still open / not certified

- a physical correlation theorem for the ordinary and carry `epsilon_i` sequence;
- exclusion or realization of the isolated/full-period extremal triple;
- separation exclusion beyond 1000;
- chronological `K` excursion or another ordering consumer of the flow bound;
- H21 core incidence/sign/recurrence information;
- sole high branch, Gate A, Gate B, or global Collatz closure.

## Status labels

- exponent-sum identity and phase lock: **proved analytic mathematics**;
- logarithm and miss inequalities: **exact rational certificate**;
- target inclusion under arbitrary bounded errors: **proved method barrier**;
- any physical-realization or closure interpretation: **not proved**.
