# RL227 handover bundle transport

Transport mode: **Git-tree deterministic reconstruction**.

The completed RL226 payload is stored losslessly as Git tree
`824219eda86040bd4378bb9a56ebb1965b5f4003` under
`RL226_E4_Lossless_Cylinder_Normal_Form_and_Residue_Barrier_2026-09-01/`.

To reconstruct the canonical ZIP, materialize that tree exactly and run its
`reconstruct_rl226_bundle.py`. The expected ZIP SHA256 is
`b42816c63101736acc20f051c227a17a0cdec8e3424f07772314498a6826eb8c`.

The internal `SHA256SUMS.txt`, lossless compression verifier, proof-state verifier,
red-team report, proof/correction ledgers, provenance, reduced interface, and unique
RL227 roadmap target are all inside the completed tree. The matching outer
`.zip.sha256` sidecar is top-level in `authoritative/`.

RL227 is prepared but NOT STARTED. Its required mission is the realistic RL closure
roadmap with an exhaustive fallback tree ending in `SYNTHESISE NEW SOLUTIONS` if all
inherited routes are exhausted.
