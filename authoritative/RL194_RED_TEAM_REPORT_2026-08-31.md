# RL194 Red-Team Report

Date: 2026-08-31
Result for frozen mathematical components: **PASS**.

## Independent bounded reviews

1. `reviews/RANK_AND_OWNED_RED_TEAM.md` checks global reverse-rank order,
   both-atom formula and seam, four strict corridor endpoints, gap-free
   coverage, retained deletions/counts, canonical lifts and prefix signs.
   It independently checks owned C=3^37, complete mechanical word212,
   all42 initial states/exponent ranges, both zero-return interfaces,
   consecutive-zero prohibition and the depth3-only parity-filter barrier.
   Both supplied verifiers passed.
2. `reviews/WEIGHT_OCCUPATION_RED_TEAM.md` checks the inherited q-shift and
   carry, rank summation, positive-window inversion including its additive
   normalization, moment intervals, occupation inequality, strict integer
   rounding and physical-statistic scope.  The verifier passed; toy tests
   remain algebraic regression checks, not actual physical coverage.
3. `reviews/WEIGHTED_SPEED_RED_TEAM.md` checks canonical anchoring, absence
   of carry in the claimed prefix range, both coarse inequalities, exact
   exp enclosures, source-before-update indexing, outward rounding, first
   compatible phases and final cardinality.  A separate128-bit replay with
   a looser independent coarse filter and longer rational series confirms
   all counts and the10 survivors, not merely the original code's output.

## Resolved finding

The sole wording finding was that a sentence saying spacing remained open
could obscure inherited spacing>=1001.  It was narrowed in both scratch
and packaged proofs, read back and accepted.  No numerical or mathematical
repair, inherited correction/demotion, or range extension was needed.

## Coverage and scope sign-off

All actual finite claims state complete domains: four endpoint enclosures
with global analytic monotonic coverage; starts1..1826035; all42 owned inputs
and depth3.  Canonical terminal floors are not pair spacing.  Final rank
cardinality is necessary only.  N0 is actual height occupation but not H21
incidence.  The ordinary flow/variation and binding H21 budget are unchanged.

No atom realization/exclusion, branch, Gate or global closure is claimed.
The final candidate portability, manifest, envelope, fresh-unpack and Git
identity gates are additional required closeout checks; these mathematical
reviews do not substitute for them.
