# RL180 correction / demotion ledger

No inherited theorem required correction or demotion during RL180.

Retain the existing corrections and scope restrictions:

1. `G_i=S_(p+i)-S_p-S_i` is the corrected accelerated p-shift defect.
2. The physical functional is `sum q_i(2^G_i-1)`; RL173's `sum q_i(3^-G_i-1)` remains auxiliary and is not used for physical sign claims.
3. RL175's sparse ownership resultant is not revived as an independent generic gap obstruction.
4. RL177/RL178/RL179 local and pair-state automata are necessary filters only; surviving states do not certify physical cycle existence.
5. The inherited external least-cycle floor is not used in RL180's new high-branch state floor or shallow-population results.

New route barrier recorded:

- Raising the RL179 odd-part sieve from mod 8 to mod 16/32, without an additional global consumer, does not produce a second immediate forbidden odd residue class at the tested depth. Treat blind higher-modulus lifting as low priority rather than as a closure route.

No frontier, Gate A/B, or Collatz closure claim is changed.
