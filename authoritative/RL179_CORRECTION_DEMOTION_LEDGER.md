# RL179 correction / demotion ledger

No inherited theorem is newly corrected or demoted in RL179.

Retained scope locks:

- RL173's `3^(-G)` weighted functional remains auxiliary only.
- RL174's corrected physical p-shift `G_i=S_(p+i)-S_p-S_i` remains authoritative.
- The physical identity remains `F2=sum q_i(2^G_i-1)=3(lambda-1)(y_p-y_0)`.
- RL175's sparse ownership resultant is not used as an independent gap obstruction.
- RL177's height-only automaton and RL178/RL179 pair-state propagations are necessary filters only; surviving states are not physical-existence claims.
- The external `m>=2^71` minimum is not used in any new RL179 theorem.
- RL179 does not claim exclusion of `(37,0,23,-1)`, the preferred branch, Gate A, Gate B, or non-trivial Collatz cycles.
