# RL245 target — six-unit intrinsic counterflow to owned physical Radius-4 encounter

Status: **PREPARED, NOT STARTED**.

Incoming completed generation: RL244, classification `R4_BRIDGE_REDUCED`.

## Frozen incoming obstruction
Every retained full-phase object that remains simultaneously unresolved by Gate A and Gate B satisfies

`beta(P)>=6`,

where `P_i=r-W_i^u(q)`, `ar-qell=2` and

`{A_i,B_i}={P_i,P_i-h_(i+q)}`

are the two genuine determinant-4 physical doubled-rotation envelopes over the u-half roots.

Thus at least six units of intrinsic negative determinant-2 half-flow mass exist before the owned-height charge is added, and every negative P-root is simultaneously physical in both genuine doubled rotations at the same root.

## Unique target
Do **not** continue an automatic beta-by-beta tail/certificate ladder unless it creates new physical ownership information. Instead couple the six-unit negative mass to the canonical zero-carry physical states and the ordered RL64–RL69 `d,T,H,J` / rank interface.

Seek exactly one of:

1. an eligible primitive full-D exact-distance-4 self-rotation satisfying every RL238/RL239 hypothesis, allowing the already-owned Radius-4 theorem to be applied; or
2. an exact contradiction to genuine full phase.

## Required scope discipline
- Gate A and Gate B remain distinct and open.
- Do not treat P,Q as Radius-4 theorem inputs.
- Do not infer exact distance 4 from negative mass alone.
- No generic packing, physical-scale anti-concentration, continued-fraction closure, or bare radius shell.
- No RL19/RL43 parameter transplant or blanket determinant-prefix positivity.
- Preserve the `(65,41)` missing-full-phase rejection.
- Radius 5 remains inactive unless explicitly activated by a future authoritative instruction.
