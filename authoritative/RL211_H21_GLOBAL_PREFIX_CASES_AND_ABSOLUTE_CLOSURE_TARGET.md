# RL211 target — H21 global prefix cases and absolute closure

Incoming job RL211 follows completed RL210. Apply RL206-C1/C2, the exact RL209
necessary predicate, and every RL210 prefix condition before any new count. The
frontier remains **13,415,865,871**, split **7,091,831,284 above p** and
**6,324,034,587 below p**.

## Primary task

Convert RL210's genuinely global p-shift prefix selector into a stronger physical
consumer by coupling it to independent absolute complete-word data.

For the **7,091,831,278** current above-p necessary ranks with `e>=56`, every
physical word has a first p-separated prefix divergence m with

`24<=m<=37`, `min(S_m,S'_m)=37`,

and all earlier p-separated heights equal. The coarse orientation
`S_24=37<S'_24=38` is already impossible; if m=24, the only surviving orientation
has `h_(p+24)=1`, `h_24=0`.

Attack these cases using an independent absolute relation: the fixed-K potential
`3K_i=alpha C_i+beta Y_i`, a complete-word closure/overlap identity, an absolute
height/phase anchor, the forced tau34 tail joined to an absolute moment, or an
equivalent global datum. A nonzero exact rank deletion or eta/state/sign/valuation
selector is preferred.

For the six small offsets `e=4,16,28,33,40,45`, prioritize the two locked cases
`e=4,16`, where RL210 proves p-separated height equality through the source and
`K_0=...=K_(e+1)=2^37`. Test whether this flat absolute K segment is compatible
with the forced H21 source/tail and complete-word closure. The four other small
offsets remain open and may be treated separately if the same new consumer
reaches them.

## Anti-loop requirements

Do not merely enumerate longer arbitrary exponent prefixes or relabel the RL210
congruence. Any claimed improvement must use information independent of the
endpoint identity and of the local translation family already blocked by RL201.
Preserve RL195 denominator equivalence, the lifted p-shift carry at `L-p`, the
37/60/97 below-p correction, and all physical/charge/Gate scope locks.

Unfinished searches are NOT PROMOTED. Preserve the exact surviving case set and
first uncovered dependency if the absolute consumer does not close all cases.
