# RL296 — finite wall-sector closure and Q17-owner Gate-A target

Date prepared: 2026-09-11
Status: PREPARED, NOT STARTED

## Incoming classification

RL295 closes as

`ZERO_DEPTH_WALL_NORMALIZATION_4_39_CUT_AND_Q17_OWNER_TAIL_PROVED`.

Gate A remains open with exact residual

`k>=31`, `k` odd, `H_can<k`.

Gate B remains separate/open/frozen. Fifth selector remains unscanned. Radius 6+ remains frozen. No global non-trivial-cycle exclusion is claimed.

## Promoted starting point

RL295 proves:

1. exact zero-depth wall normalization for the canonical cascade;
2. all nonexceptional sectors of `(4,39)=P o Z o Z` are P-owned within the RL292 threshold;
3. the unique early residual is `00 -> (5,191)` at source cost 6;
4. `Bcal(4,39)<=max(Bcal(P)+2,Bcal(5,191)-6)`;
5. an exact P -> Q17 certificate of cost 183;
6. the `(6,807)` distinguished spine reaches Q17 at cost 169;
7. every post-Q17 future on that spine is P-owned with constant lag 14, leaving 17 units spare inside the inherited +31 credit;
8. the only unresolved `(6,807)` material before that splice is a finite list of sixteen first-deviation sectors.

## Primary target A — reconstruct and close the finite `(5,191)` wall frontier

RL295 scratch contains strong but deliberately unpromoted finite reductions through

`(5,201)`, `(6,733)`, `(6,949)`, `(6,807)`

and candidate shallow residuals

`(5,206)`, `(5,264)`, `(5,351)`, `(6,699)`, `(6,898)`.

Reconstruct these from authority, not conversation memory.

For every owner closure require one of:

- an explicit forward P word and exact cost;
- a parametric analytic same-state P route;
- a Bellman/min-plus inequality derived from the promoted wall normalization.

Do not promote a search-only owner claim without a frozen witness.

If `(6,807)` is recovered, use the promoted Q17 theorem immediately; do not restart the superseded E_d/dyadic-ladder tail analysis unless the Q17 certificate fails.

Success for Target A is

`Bcal(5,191)<=Bcal(P)+8`

which together with RL295 gives

`Bcal(4,39)<=Bcal(P)+2`.

## Primary target B — remaining RL292 non-P front door

After `(4,39)` closes, attack:

`(2,-17)=B_-4 o B_-6`

`(2,-84)=B_-19 o B_-28`

`(3,-28)=B_-4 o P = R6 o B_-13`.

Use the promoted zero-depth normalization and checkpoint-2 sink. Prefer finite exact wall cuts and same-state owner splices over raw P cones.

If all non-P obligations close, make the inherited implication explicit and reduce the complete RL292 front door to

`Bcal(P)<=1`.

## Tight P-source target only after front-door collapse

Use the inherited RL293/RL292 static danger-tree restrictions:

- normalized-K first-boundary corridor;
- high signed drift necessity;
- dangerous first-checkpoint principal-cylinder representative;
- roots `alpha=-1,-2/3`;
- alignment of `alpha=-2/3` with the labelled height-one predecessor ray.

Target:

`Bcal(2,3)<=1`.

## Mandatory red teams

Retain all RL295 mandatory attacks:

- arbitrary zero-height boundary retention/trivial cycle;
- unbounded static pullbacks;
- cylinder isometry/finite-suffix transparency;
- neutral `(101)` gauge;
- physical mergers/equal-area diamonds;
- adjacent-swap valuation jumps;
- homogeneous Rank-1 danger towers;
- shifted phase-reset families;
- RL290 recursive Bellman chains;
- locally legal but globally unreachable predecessor families;
- RL293 immediate-danger corridor exclusions;
- H<=60 owner exceptions;
- principal-cylinder scope;
- formal identity and nonzero wall debt accounting.

In addition, every finite owner certificate must be replayed from P under the canonical recurrence.

## Forbidden principal routes

Do not restart:

- raw larger H/k caps;
- large unstructured P cones as proof;
- the superseded 4-unit/42-unit tail-gap optimizations;
- raw 200+ bit boundary words without quotient interpretation;
- residue-only debt corrections;
- chosen-history seam charges;
- generic Collatz termination assumptions;
- Gate B, fifth selector, Radius 6+.

## Success condition

RL296 succeeds strongly if it proves any of:

- complete `(4,39)` Bellman domination;
- complete non-P front-door closure;
- reduction of the front door to `Bcal(P)<=1`;
- `Bcal(P)<=1`;
- Gate A.

If a finite wall sector resists, freeze the first exact residual and the smallest additional state/debt coordinate needed.
