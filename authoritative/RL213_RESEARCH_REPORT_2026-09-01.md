# RL213 research report — global-consumer barriers at e=16

Date: 2026-09-01.

RL213 attempted the two consumers requested by the RL212 handover rather than extending the local ternary automaton mechanically.

First, the exact RL201 absolute endpoint/complete-word moment was specialized to the e=16 matched p-shift prefix. This looked promising because it contains the global prefix mass `P_p`. However, after the exact no-carry relation, local e=16 p-gap and root z-telescopes are inserted, the complete endpoint equation collapses identically to the already inherited root `0->p` telescope. It therefore contains no independent eta information at e=16. This is a useful precise barrier: a future consumer must constrain a genuinely new full-word quantity rather than merely rewrite the same endpoint identity.

Second, the RL210 large-offset first-divergence congruence was attacked exhaustively. There are 304 abstract start cases over `m=24..37`, the larger first-mismatch exponent and orientation. Exact modular continuation through phase55 reproduces the one inherited m=24 orientation obstruction, but every other 303 start case has a valid bounded strictly increasing continuation satisfying G56. Thus the 56-bit congruence plus those sequence bounds is saturated and supplies no further cut.

A final scope check considered the signed H21 successor-K route. RL201 identifies signed successor-K as potentially independent, but RL209's actual certified anchor for that quantity is restricted to the backward root-window survivor set. The e=16 terminal is at phase `p+50`, so no existing authority justifies importing that root-window corridor here.

No necessary rank is deleted. The frontier remains 13,415,865,871. The next attack should seek a genuinely discrete full-word ownership quotient/root residue (with a proved bridge to the H21 coordinates) before returning to signed successor-K only if an independent anchor can be established at the relevant phase.
