# RL78 SESSION STATE AND KICKOFF

Date: 2026-08-24

## 0. Authority and scope

**AUTHORITATIVE OUTGOING RL78 RESEARCH STATE.** Gate A remains open globally, Gate B remains open globally, and no RL/nontrivial-cycle/Collatz closure is claimed.

RL78 worked the RL77-selected target:

# **`D|Q`-sensitive bounded-radius Gate-B reconnaissance**

The session recovered the exact RL18/RL19/RL20/RL48/RL49/RL72/RL75 interfaces selectively, used the RL20 radius-4 `D∤Q` word as the mandatory negative control, proved an exact rotation ownership-defect theorem, and found/repaired a historical RL20 canonical-lift formula.

The attack does not produce an owned close-pair theorem or an absolute radius bound. The stop criterion is therefore triggered.

Detailed mathematics:

`RL78_DQ_SENSITIVE_ROTATION_DEFECT_AND_BLOCK_SCALE_BARRIER.md`.

Selected next target:

`RL79_POST_TOURNAMENT_COMMON_BARRIER_SYNTHESIS_AND_NEW_ROUTE_GENERATION_TARGET.md`.

## 1. Verification economy rule — retained

After the current bundle checksum, internal manifest and fast verifier suite pass, accept the frozen incoming proof-state ledger as authoritative. Do not recursively rerun historical expensive certificates unless a new load-bearing argument requires an unresolved definition, a verifier fails, or an apparent contradiction triggers stop-and-repair.

## 2. Incoming RL77 gate

The uploaded RL77 gate passed before RL78 mathematics:

- outer RL77 sidecar: **PASS**;
- freshly unpacked RL77 internal `SHA256SUMS.txt`: **PASS**;
- `bash verification/run_fast_rl77_verifiers.sh`: **PASS**.

No expensive historical suite was recursively rerun.

## 3. Frozen inherited state retained

- radius-3 primitive/full-`D`: closed local obstruction;
- Gate A even `k`: impossible analytically;
- Gate A `k<=25`: closed by exact finite-certificate corollary;
- Gate A odd `27<=k<=165`: open;
- RL77 product/CF/scale and unimodular-lattice results retained;
- Gate A globally open;
- Gate B globally open.

## 4. New RL78 theorem A — rotation ownership-defect invariance

For a word `d` of length `A`, weight `L`, `D=2^A-3^L`, and a prefix of length `m`, weight `p`, let `d_m` be the left rotation and `A_m` the prefix word. Then

`boxed: 2^m Q(d_m)-3^p Q(d)=D Q(A_m)`.

Therefore

`boxed: gcd(D,Q(d_m))=gcd(D,Q(d))`

for every rotation.

Define

`s=|D|/gcd(|D|,Q(d))`.

Every rational fixed phase has reduced denominator `s`. Genuine full-`D` ownership is exactly `s=1`.

Classification: **analytic theorem**.

## 5. New RL78 theorem B — pairwise normalized rotation congruences are universal

With `rho_m=2^m3^{-P_m} (mod D)`, every word satisfies

`rho_m Q(d_m)==Q(d) (mod D)`.

Thus every pair satisfies

`rho_mQ(d_m)-rho_nQ(d_n)==0 (mod D)`

without assuming `D|Q`.

Therefore pairwise/multi-rotation normalized numerator subtraction cannot be the missing ownership theorem; it passes automatically on the RL20 `D∤Q` fake.

Classification: **analytic method barrier**.

## 6. Stop-and-repair — RL20 canonical lift increment

The historical RL20 near-resonant block geometry note printed

`H_(j+1)-H_j=z^j Q(B_j)/Y`.

The exact RL20 block coboundary implies instead

`boxed: H_(j+1)-H_j=z^j 3^(-E_(j+1))Q(B_j)/Y`.

The historical verifier did not test this formula.

The one-sided imbalance, physical height bands, narrow-strip endpoints and monotonicity survive. Any quantitative strict-excursion argument using the factor-free increment is demoted pending repair.

No closed radius-3 or Gate-A theorem is affected.

## 7. New RL78 theorem C — block lift/physical-scale trade

The corrected increment gives exactly

`boxed: (H_(j+1)-H_j)/H_(j+1)=Q(B_j)/(2^a x_(j+1))`.

So block lift cost is inversely priced by physical exit scale. Positive imbalance both raises the physical scale and discounts the strip cost. This exposes the same free-scale obstruction encountered by RL76/RL77.

Classification: **analytic synthesis / method barrier**.

## 8. RL20 fake sharpened

For the exact length-184 fake:

- inherited all-rotation minimum radius: `4`;
- inherited `D∤Q` remainder retained;
- new exact fact: `gcd(D,Q)=1`;
- hence common reduced denominator `s=D`, the maximal ownership defect;
- its exact rational fixed orbit is positive and closes rationally;
- the designated root phase is the least rational phase;
- canonical `(g,a,ell)=(4,46,29)` imbalance path is `(0,1,1,1,0)`.

Classification: **new exact finite audit on inherited fake**.

## 9. RL78 route decision

No theorem was found forcing:

- two owned primitive full-`D` rotations at radius `<=3`;
- an absolute owned radius `<=4`; or
- a direct contradiction.

The natural multi-rotation residual collapses to universal unit transport, while the strict-excursion strip route after correction is scale-sensitive. The absolute ownership class `s=1` genuinely distinguishes the fake but no bound from `s=1` to word radius is known.

Therefore do **not** build a generic radius-4/5/6 case tree.

Pivot RL79 to a post-tournament common-barrier synthesis across RL76–RL78 and generate genuinely new theorem-sized routes.

## 10. Correction/demotion ledger additions

Retain all RL72–RL77 corrections and add:

- correct the RL20 narrow-strip increment by the factor `3^{-E_(j+1)}`;
- demote factor-free strict-excursion block pricing;
- demote pairwise normalized rotation congruences as ownership tests because they hold for every word;
- retain the absolute ownership-defect denominator `s` as a genuine discriminator, but not a radius theorem;
- record `gcd(D,Q)=1` for the RL20 fake and its maximal ownership defect;
- do not infer radius `<=4` from the fake's minimum radius `4`.

## 11. Exact proof state after RL78

- radius-3 primitive/full-`D`: **closed local obstruction**;
- Gate A even `k`: **closed analytically**;
- Gate A `k<=25`: **closed by exact finite-certificate corollary**;
- Gate A odd `27<=k<=165`: **open**;
- Gate A globally: **open**;
- Gate B globally: **open**;
- RL/nontrivial-cycle exclusion: **open**;
- Collatz conjecture: **not proved**.

## 12. RL78 verifier status

`python3 verification/verify_rl78_dq_rotation_and_block_scale.py`: **PASS**.

Current exact counts:

- rotation transport checks: `9,212`;
- rotation gcd-invariance checks: `8,192`;
- corrected block-lift checks: `75,287`;
- scale-trade checks: `70,374`;
- stress-family block checks: `4,176`.

The verifier also checks the mandatory RL20 fake, its maximal ownership defect, rational fixed orbit, canonical imbalance path and minimum radius `4`.

---

# Self-contained kickoff for RL79

Continue the Collatz R-sharp / RL research from the authoritative RL78 bundle and matching `.sha256` sidecar.

First verify only the current RL78 gate:

1. outer RL78 sidecar;
2. freshly unpacked internal `SHA256SUMS.txt`;
3. `bash verification/run_fast_rl78_verifiers.sh`.

Apply the verification-economy rule after those pass.

The session target is:

# **RL79 — post-tournament common-barrier synthesis and new-route generation**

Use RL72's global audit/lemma catalogue and RL75's route tournament as the baseline, then incorporate the decisive RL76–RL78 barriers:

- owned macro/pump depth is absorbed by free physical scale/context;
- product/CF converts depth/resonance into scale/successor-denominator demand but does not bound global scale;
- canonical two-pump Diophantine coordinates are unimodular, not a new independent dimension;
- natural normalized multi-rotation congruences are universal transport identities and do not consume `D|Q`;
- corrected strict-excursion strip cost is itself inversely priced by physical scale;
- local grammar can have global minimum radius `4` while ownership fails maximally.

Do not default to another Gate-A digit, generic CF scan, endpoint telescope, generic radius ladder, or a raw proper-factor/rotation residual.

The primary task is to identify and seriously test new theorem architectures that evade **all three** common failure modes:

1. coboundary/unit-transport tautology;
2. free physical-scale absorption;
3. local-grammar fake compatibility.

Use `RL79_POST_TOURNAMENT_COMMON_BARRIER_SYNTHESIS_AND_NEW_ROUTE_GENERATION_TARGET.md` for candidate mechanisms and stop rules.

Before ending, freeze exact proofs, failed candidates, recovered interfaces, correction ledger and verifier status; create the next numbered authoritative bundle and sidecar; verify the internal manifest and fresh-unpack fast suite.
