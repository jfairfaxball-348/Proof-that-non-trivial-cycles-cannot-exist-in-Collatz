# RL346 target — finish the 23/72 CRT interface and close Phase 4

Status: UNIQUE LIVE TARGET FOR RL346; prepared, not started.

Scope: inherited ordered genuine `g=2`, `Z0>0`, `K<0` parent `(a,ell)=(217976794617,137528045312)`, genuine full two-row physical cycle, exact inherited ownership/pruning, and external conditional least-state floor `m>=2^71`.

## Objective

Continue the residual-return programme and prove the exact owned phase-nondecreasing return obstruction `O_75` empty. The immediate task is to finish Phase 4 from RL345's verified 23-source/72-endpoint singleton interface.

## Inherited Phase-4 frontier

For every phase-nondecreasing complete return, `0 < P - 3^L E/2^H < 2^37`.

A fixed first 23-gap source prefix fixes the odd source modulo `2*3^23`, which exceeds `2^37`. A fixed final 72-gap suffix has at most one viable q=0 band endpoint: total gap `H>=76` gives modulus uniqueness, while all 135,356 exact band endpoints in the exceptional `H=73,74,75` classes descend below `2^71`.

Thus, after fixing exact return length, total gap and decorated boundary tags, a fixed 23/72 interface has at most one phase-nondecreasing physical middle lift.

## Required Phase-4 completion

Use that singleton to make the remaining conditions accept/reject tests: predecessor mixed 2-adic congruence and oddness; successor mixed 3-adic congruence; exact source/end row-phase decoration; row contact for early-row q=0 ownership; wrap orientation across the genuine two-row cycle; inherited physical ownership/pruning; exact phase-potential sign; and deterministic exclusion of any candidate trajectory reaching below `2^71`.

Do not call Phase 4 closed until every long-return interface is eliminated/descending or the first exact unresolved physical singleton class is frozen with all conditions. Do not make further suffix-depth shaving the main route; the unpromoted depth-71 thought from RL345 is not an inherited theorem.

Only after Phase 4 closes, perform Phase 5 on returns shorter than 75. RL344's Beatty/lattice work is scratch and must be reconstructed/red-teamed before promotion. Use Phase 6 only if a genuine arbitrary-prefix residual remains after Phases 4–5.

Success condition: prove `O_75=empty`; then RL343's closed-walk bridge contradicts the parent and closes R1.
