# RL176 correction / demotion ledger

No new inherited theorem is demoted in RL176.

Retained correction:

- RL173's accelerated interpretation of the `3^(-G)` weighted functional
  remains demoted to an auxiliary functional only.
- RL174's corrected physical p-shift defect
  `G_i=S_{p+i}-S_p-S_i` remains authoritative.
- The physical identity remains
  `F2=sum q_i(2^(G_i)-1)=3(lambda-1)(y_p-y_0)`.

RL176 does not promote the RL175 sparse-resultant ownership obstruction as
an independent gap obstruction.  The new `4 | g_p` lattice and the
first-mismatch valuation identity are derived from physical state order,
accelerated recurrence arithmetic, and the inherited half-barrier.
