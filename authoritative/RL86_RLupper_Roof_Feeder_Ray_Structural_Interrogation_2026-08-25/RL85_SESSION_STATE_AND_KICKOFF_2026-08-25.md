# RL85 SESSION STATE AND RL86 KICKOFF

Date: 2026-08-25

## Frozen global status

- radius-3 primitive/full-`D`: closed local theorem;
- Gate A even terminal `k`: closed analytically;
- Gate A terminal `k<=25`: closed by inherited exact finite certificate;
- Gate A odd `k>=27`: open;
- Gate A globally: open;
- Gate B globally: open;
- RL/nontrivial-cycle exclusion: open;
- Collatz conjecture: not proved.

## Incoming RL84 gate used

Current-session verification before RL85 mathematics:

- RL84 outer SHA-256: PASS;
- RL84 internal manifest: PASS;
- inherited RL83 sidecar: PASS;
- RL84 fast verifier: PASS.

The frozen RL84 ledger was accepted under the verification-economy rule.

## New RL85 theorem state

### Historical RL# comparison

- Historical `R#` is the least red integer over the full red set, not automatically a chosen cycle minimum.
- `F=2M`, `M`, and the immediate predecessor `P=(2M-1)/3` are not historical `R#`.
- `P->M` has accelerated valuation at least 2, so the historical pure-`r=1` corridor does not attach to the roof fork.
- Seed-independent inverse odometer structure transfers but is residue-complete and gives no lower cylinder-height theorem.

### First-Farey physical scale

At

`(p,q)=(114,208,327,604, 72,057,431,991)`,

with RL84's exact defect certificate:

- `M<400,000,000,000(3/2)^q`;
- `M` equals the least positive even `q`-digit maximum-cylinder representative;
- with `h=27,021,536,997`, `M<3^h`;
- at least `45,035,894,994` leading ternary digits are zero in the zero-padded `q`-digit representative.

### RL75/RL76 hybrid splice

For any proper synchronized height-one quotient return of span `n` inside a genuine paired full-phase cycle,

`2^(n+1)<=M-R`.

On the exact first-Farey branch:

`n<=43,234,459,194`.

Canonical repeat bounds:

- `10`: `r<=21,617,229,597`;
- `101`: `r<=13,510,768,498`.

On the RL73 giant-macro / first-Farey intersection, the synchronized block contains at least `930,959` spaced-distinct quotient states.

## RL20 comparison

- extremal Branch-L arc is owned and low, but is not automatically an RL20 balanced-return block, strict excursion, or compressed plateau;
- RL19/RL20 arbitrary-rotation weighted difference does apply to `R,M` but measures `M-R` rather than contradicting it;
- no direct RL20 closure is claimed.

## Frozen barriers

- direct historical least-red transplant to the roof fork;
- pure dyadic feeder/cylinder-height transfer;
- sibling odometer as a lower-height consumer;
- raw cycle-lemma/opposite-prefix comparison;
- Branch-H numerical length inflation without a new upper consumer.

## Primary fallback obligation after RL86

If the RL↑ roof-feeder investigation is neutral, resume the single narrow RL85 obstruction:

> consume the first-Farey bounded quotient-return span / aperiodic quotient complexity with a genuine physical/full-`D` packing theorem, or prove that such a consumer is structurally unavailable.

## Verification economy

After this bundle's sidecar, internal manifest, and fast verifier pass, accept this frozen ledger. Do not recursively rerun historical expensive certificates unless a dependency fails or a contradiction triggers stop-and-repair.

## Next target

`RL86_RLUPPER_ROOF_FEEDER_RAY_STRUCTURAL_INTERROGATION_TARGET.md`
