# RL269 — Radius-5 final determinant-one flat family

Date prepared: 2026-09-06
Status: **PREPARED, NOT STARTED**

Incoming classification: `RADIUS5_KAPPA1_2111_CLOSED`.

## Inherited determinant-one state

Closed:
- `[3,2]` (RL266);
- `[3,1,1]` and `[2,2,1]` (RL267);
- `[2,1,1,1]` (RL268).

Remaining determinant-one topology:
- `[1,1,1,1,1]` only.

## Primary target

Attack only `[1,1,1,1,1]`.

Requirements:
1. enumerate every cyclic sign/order case without omission;
2. derive the exact edge/component identity from the inherited zero-flow-cut formula;
3. retain divisibility by the full `D=2^A-3^L`;
4. exploit `qA-mL=1` and the exact determinant-window identity before computation;
5. derive an infinite reduction appropriate to five components rather than reusing an unmatched lower-component support estimate;
6. preserve negative-D, nonprimitive, cyclic-wrap, component-order, proper-factor and numerator-index red teams;
7. use finite computation only after an explicit infinite reduction.

Preferred outcomes:
- `RADIUS5_KAPPA1_11111_CLOSED`;
- `RADIUS5_KAPPA1_11111_REDUCED_TO_FINITE_CERTIFICATE`;
- `RADIUS5_KAPPA1_11111_EXACT_BARRIER`.

If `[1,1,1,1,1]` closes, the complete `|kappa|=1` determinant-one flat sector is closed. **That does not yet prove Radius 5.** The inherited Radius-5 classification still has `|kappa|=3` and `|kappa|=5` sectors, including their allowed flat and height-two topology families. Only after determinant one is complete may those sectors be activated.

Frozen until then:
- `|kappa|=3` and `|kappa|=5`;
- Gate A;
- fifth retained selector;
- selector enumeration;
- general Radius-n programme.

Radius 4 remains promoted locally. Gate B, Radius 5 and global exclusion remain open.
