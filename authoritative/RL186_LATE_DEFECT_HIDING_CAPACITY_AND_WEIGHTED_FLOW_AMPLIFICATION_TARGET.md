# RL186 target — late-defect hiding capacity and weighted-flow amplification

Date: 2026-08-30

## Authoritative objective

Continue the sole surviving high branch `(v,H,J,d)=(37,0,23,-1)` from RL185.

RL185 proves that the RL184 clean corridors force

- ordinary absolute corrected-flow variation `>150`;
- both signed corrected-flow masses `>75`;
- both signed K-variation masses `>25`;
- at least about ten million phases of each defect sign.

This is not yet competitive with the inherited K-corridor width. The next target is therefore **not** more unweighted defect enumeration. It is to prove that most clean shallow-start corridors cannot postpone their first nonzero defect into the late/high-height tail.

## Preferred theorem

For each clean 40-edge corridor, let `tau` be the first offset with nonzero defect. Prove a strong upper capacity bound for corridors with `tau>=n`, especially for `n` in the upper half of `0,...,39`.

Use the zero-prefix identity

`2^(d_0+...+d_(n-1)) C_n = 3^n C_0`

and hence `2^n | C_0` on an n-step zero-defect prefix, together with:

- RL182 physical ternary-suffix ownership;
- RL183/RL184 actual mechanical factors and affine numerator maps;
- the shallow starting numerator range;
- parity/ternary reset sensors;
- exact repeated-map restrictions where they become physical.

The desired output is a distributional bound forcing a large fraction of first defects to occur at substantially smaller H than 23/24. Feed that distribution back into RL185's height-dependent inequality

`|f_i|>2^-(H+1)`

and its coverage caps to amplify the global weighted-flow lower bound by many orders of magnitude.

## Secondary consumer

Only if weighted variation becomes comparable with the inherited K-corridor scale, attack chronological sign-reversal capacity: show that the necessary positive/negative compensation cannot oscillate often enough to keep every K prefix inside

`128,081,997,553 < K_i < 146,795,909,391`.

## Do not

- merely extend local template enumeration to longer arbitrary windows;
- treat repeated necessary maps as impossible without a physical ownership argument;
- infer K excursion from total variation alone;
- revive generic denominator/rank packing barred by RL168-RL171;
- use RL173's `3^-G` functional in place of corrected physical `2^G`;
- claim branch or global closure without a genuine quota contradiction.
