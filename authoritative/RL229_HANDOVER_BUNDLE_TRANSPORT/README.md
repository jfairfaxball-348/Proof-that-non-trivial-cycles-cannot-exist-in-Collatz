# RL229 handover bundle transport

This directory documents the lossless Git-tree transport for the completed RL228 authoritative package.

The deterministic canonical ZIP is:

`RL228_E4_Dyadic_Bulk_Fringe_and_Endpoint_Scaling_Barrier_2026-09-01.zip`

Canonical ZIP SHA256:

`4ae8b34f152c2067ddafd5e0cfa462cdc5783f1ff61fb575d7343dd0a2d0c2a9`

The completed package directory is stored directly in Git with tree SHA:

`4510c124d86e3231f175c3195c4b4cc51bc8d850`

Run `reconstruct_rl228_bundle.py` inside that package to reproduce the canonical ZIP, then compare it to the outer `.zip.sha256` sidecar.

The package contains the RL228 theorem/barrier report, proof/correction ledgers, proof state, red team, provenance, exact certificate summary, portable verifiers, internal `SHA256SUMS.txt`, session handover, and the unique RL229 target.

RL229 is prepared but NOT STARTED.
