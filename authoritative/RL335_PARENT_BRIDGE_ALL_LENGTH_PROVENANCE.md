# RL335 parent-bridge all-length provenance

Date: 2026-09-16
Status: VERIFIED PROVENANCE FOR RL335 CLOSEOUT

RL335 inherits the ordered genuine `g=2`, `Z0>0`, `K<0` late-row branch, the exact parent `(217976794617,137528045312)`, the conditional least-state floor `m>=2^71`, the physical state band, the high-carry threshold `20390252058`, RL334's zero-run bound `z<=37`, and the RL334 cap `n<=32546313237`.

The new theorem surface has three layers. First, exact physical reconstruction excludes every owned singleton, p2, p3 and p4 support that can survive in the z<=37 high-carry regime. Second, every anonymous surviving return therefore has at least five positives, and true slack rises by 43 per extra positive; this makes the p=5 fallback uniformly worst for every q<43 and supplies a genuine all-length theorem rather than a finite catalogue. Third, the q=28 charge theorem is consumed by the inherited phase/telescope machinery to obtain `n<=32546289530`.

The externally conditional status of the `2^71` floor is unchanged. No cyclic-wrap argument is used. RL324's local-propagation barrier remains respected because the promoted theorem is global support/charge accounting, not local displacement propagation.
