# RL102 handover — start here

Date: 2026-08-25

This bundle closes RL101 and hands the next structural attack to RL102.

Recommended read order:

1. `RL101_RAW_MULTIPLICITY_CYLINDER_OWNERSHIP_AND_STRICT_EXCURSION.md`
2. `RL101_SESSION_STATE_AND_RL102_KICKOFF_2026-08-25.md`
3. `RL102_OWNED_CYLINDER_CONTENT_AND_TWO_SIDED_EXCURSION_TARGET.md`
4. `RL101_COUPLED_CASCADE_RESUME_AFTER_RL102.md` only if the structural route is deliberately frozen

Core RL101 advances:

- the normalized raw-multiplicity cylinder height decreases exponentially with `g`;
- every genuine survivor has physical maximum equal to the least positive even cylinder representative;
- every `g>=2` first-surplus word has an owned primitive-block excursion with an actual state below `M/2`;
- in the proper equal-slope near-resonant branch the first-surplus endpoint is above `15M/16`.

Core unresolved theorem:

- combine the tiny physical cylinder representative with full primitive ordinary-`+1` ownership, preferably `D|Q(W_full)`, to obtain a lower-content contradiction.

Global Gate A and Gate B remain open. No nontrivial-cycle or Collatz closure is claimed.

The raw-`g=1` cascade is frozen at first live odd `2,921,813,805`, with mandatory repaired middle depth `D_m=7,500,031`.
